# Dependencies
node_modules/

# Build output
dist/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Test Setup Credentials (legacy — replaced by .env.test)
# .test-config

# Claude Code internals (settings, cache, etc. — not committed)
.claude/*
# Except for reusable skills (tracked for cross-phase usage)
!.claude/skills

# GSD worktrees (temporary isolated copies)
.worktrees/

# GSD planning and task files (internal dev workflow — not for public consumption)
.planning/
tasks/

# Local Memory
memory/

# Environment variables (credentials — NEVER commit these)
.env
.env.local
.env.*.local
.env.test
*.env
.env.*
!.env.example
!.env.test.example
!.env.*.example

# Generated config files (user-specific, not committed)
# flashquery.yml is generated by npm run setup — contains your instance ID and vault path
# Keep flashquery.example.yml and flashquery.example.yaml (templates for others)
flashquery.yml


# Test artifacts (generated during test runs)
coverage/

# Logs
*.log
.worktrees/
