interface:
  display_name: "FlashQuery Graph Testgen"
  short_description: "Create, run, and refine graph record tests"
  default_prompt: "Use $flashquery-graph-testgen to create, run, diagnose, and refine FlashQuery graph golden-model and record tests."
