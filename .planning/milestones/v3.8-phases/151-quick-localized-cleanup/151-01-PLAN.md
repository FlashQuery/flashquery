---
phase: 151-quick-localized-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/embedding/provider.ts
  - src/storage/vault.ts
  - src/services/plugin-reconciliation.ts
  - tests/unit/embedding.test.ts
  - tests/unit/vault.test.ts
  - tests/integration/plugin-reconciliation.integration.test.ts
autonomous: true
requirements:
  - REQ-001
  - REQ-002
requirements_addressed:
  - REQ-001
  - REQ-002
must_haves:
  truths:
    - "OpenAI/OpenRouter embedding provider setup fails synchronously with provider-specific missing apiKey errors, while Ollama still works without apiKey."
    - "Plugin reconciliation reads vault files through a public VaultManager absolute-path method, not a private rootPath cast."
  artifacts:
    - path: "src/embedding/provider.ts"
      provides: "Explicit provider apiKey validation for OpenAI-compatible embedding providers"
      contains: "createEmbeddingProvider"
    - path: "src/storage/vault.ts"
      provides: "Public VaultManager resolver for vault-relative absolute paths"
      contains: "interface VaultManager"
    - path: "src/services/plugin-reconciliation.ts"
      provides: "Reconciliation disk reads through public vault path API"
      contains: "readFrontmatterFromDisk"
    - path: "tests/unit/embedding.test.ts"
      provides: "T-U-001 through T-U-003 embedding provider validation coverage"
    - path: "tests/unit/vault.test.ts"
      provides: "T-U-005 and T-U-006 vault absolute-path API coverage"
    - path: "tests/integration/plugin-reconciliation.integration.test.ts"
      provides: "T-I-001 plugin reconciliation regression coverage"
  key_links:
    - from: "src/services/plugin-reconciliation.ts"
      to: "src/storage/vault.ts"
      via: "vaultManager public absolute-path resolver"
      pattern: "vaultManager\\.[A-Za-z0-9_]*(Path|path)"
    - from: "tests/unit/embedding.test.ts"
      to: "src/embedding/provider.ts"
      via: "createEmbeddingProvider negative and positive tests"
      pattern: "createEmbeddingProvider"
    - from: "tests/unit/vault.test.ts"
      to: "src/storage/vault.ts"
      via: "new public VaultManager path API"
      pattern: "resolve[A-Za-z0-9_]*(Path|path)"
---

<objective>
Implement Phase 151 REQ-001 and REQ-002 exactly as specified in the external remediation spec and test plan.

Purpose: close the embedding API-key validation and plugin reconciliation private-field access audit findings without touching later Phase 152/153 scope.
Output: focused production cleanup plus unit/integration coverage for T-U-001 through T-U-007 and T-I-001.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/151-quick-localized-cleanup/151-CONTEXT.md
@.planning/phases/151-quick-localized-cleanup/151-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md

<source_of_truth>
Per D-01, downstream implementation agents MUST read the two external docs above before editing or verifying. Use the requirements spec first for behavior and invariants, then the test plan for exact test IDs and validation. Use local ROADMAP/REQUIREMENTS/CONTEXT/PATTERNS only as summaries. Ask the user only when those documents do not answer the question.
</source_of_truth>

<scope_boundaries>
Implement only REQ-001 and REQ-002 per D-02. Do not implement REQ-003 through REQ-009 in this plan. Do not include repository-wide zero-cycle policy, broad Supabase access redesign, broad package modernization, or document tool decomposition.
</scope_boundaries>

<interfaces>
Existing contracts the executor should preserve:
- `createEmbeddingProvider(config: NonNullable<FlashQueryConfig['embedding']>): EmbeddingProvider` in `src/embedding/provider.ts`.
- `VaultManager` in `src/storage/vault.ts` currently exposes `writeMarkdown`, `readMarkdown`, `removeMarkdown`, `moveMarkdownToTrash`, and `resolvePath(area, project, filename)`.
- `readFrontmatterFromDisk(relativePath: string): Promise<Record<string, unknown>>` in `src/services/plugin-reconciliation.ts` must keep empty-object fallback on read failure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit embedding provider apiKey validation</name>
  <files>src/embedding/provider.ts, tests/unit/embedding.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md` Spec §3, §4, §6.1.1.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md` Test Plan §3, §4.1.1.
    - `.planning/phases/151-quick-localized-cleanup/151-CONTEXT.md` decisions D-01 through D-04.
    - `.planning/phases/151-quick-localized-cleanup/151-PATTERNS.md` section for `src/embedding/provider.ts`.
    - `src/embedding/provider.ts` and `tests/unit/embedding.test.ts`.
  </read_first>
  <behavior>
    - T-U-001: OpenAI config missing or empty `apiKey` throws synchronously with a message naming `openai` and `apiKey`.
    - T-U-002: OpenRouter config missing or empty `apiKey` throws synchronously with a message naming `openrouter` and `apiKey`.
    - T-U-003: Ollama config without `apiKey` still constructs and reports configured dimensions.
  </behavior>
  <action>Add tests first for T-U-001, T-U-002, and T-U-003. Then replace `config.apiKey!` in `createEmbeddingProvider` with explicit provider-specific validation for `openai` and `openrouter`; reject missing, empty, and whitespace-only keys with synchronous errors naming the lowercase provider and `apiKey`, and leave the `ollama` branch unchanged per REQ-001 and D-03. Do not change MCP response envelopes or add API-key validation to Ollama.</action>
  <verify>
    <automated>npm test -- tests/unit/embedding.test.ts --bail=1</automated>
  </verify>
  <done>REQ-001 behavior is implemented: OpenAI/OpenRouter missing-key cases fail synchronously, Ollama without apiKey remains valid, and `src/embedding/provider.ts` no longer contains `config.apiKey!`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public VaultManager path API and use it in plugin reconciliation</name>
  <files>src/storage/vault.ts, src/services/plugin-reconciliation.ts, tests/unit/vault.test.ts, tests/integration/plugin-reconciliation.integration.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md` Spec §3, §4, §6.1.2.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md` Test Plan §3, §4.1.2.
    - `.planning/phases/151-quick-localized-cleanup/151-CONTEXT.md` decisions D-01 through D-04.
    - `.planning/phases/151-quick-localized-cleanup/151-PATTERNS.md` sections for `src/storage/vault.ts` and `src/services/plugin-reconciliation.ts`.
    - `src/storage/vault.ts`, `src/services/plugin-reconciliation.ts`, `tests/unit/vault.test.ts`, and `tests/integration/plugin-reconciliation.integration.test.ts`.
  </read_first>
  <behavior>
    - T-U-005: The new public `VaultManager` absolute-path API resolves nested vault-relative paths under the configured vault root.
    - T-U-006: Unsafe relative input is rejected or normalized according to existing vault containment conventions, not ad hoc string checks.
    - T-U-007: Plugin reconciliation no longer casts `vaultManager` to access private `rootPath`.
    - T-I-001: Existing plugin reconciliation integration behavior remains stable after the path API change, with existing `.env.test` skip behavior respected.
  </behavior>
  <action>Add tests first for T-U-005 and T-U-006. Add a public `VaultManager` method for resolving a vault-relative path to an absolute path; choose a local-convention name such as `resolveVaultPath` or `resolveAbsolutePath` per the agent's discretion in D-01, implement it on `VaultManagerImpl`, and base containment/traversal handling on the existing `resolve`/`relative` vault-root pattern. Update `toAbsolutePath` in `src/services/plugin-reconciliation.ts` to call the public vault method and remove the private `vaultManager as unknown as { rootPath: string }` cast and any now-unused `join` import. Preserve plugin reconciliation empty-object fallback on read failure and all existing public behavior.</action>
  <verify>
    <automated>npm test -- tests/unit/vault.test.ts --bail=1</automated>
    <automated>npm run test:integration -- tests/integration/plugin-reconciliation.integration.test.ts --bail=1</automated>
  </verify>
  <done>REQ-002 behavior is implemented: plugin reconciliation resolves vault-relative disk paths through public VaultManager API and no longer depends on private internals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config -> embedding provider | Configured provider names and apiKey values cross into provider construction and later HTTP headers. |
| vault-relative path -> filesystem | Plugin reconciliation converts stored vault paths into filesystem reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Denial of Service | `src/embedding/provider.ts` missing apiKey handling | mitigate | Fail synchronously for OpenAI/OpenRouter missing keys so startup/config errors are immediate and do not become repeated downstream HTTP failures. |
| T-151-02 | Tampering | `src/storage/vault.ts` absolute path resolver | mitigate | Resolve vault-relative paths through `resolve`/`relative` containment logic and reject traversal outside the vault root; plugin reconciliation must call this public API. |
| T-151-SC | Tampering | npm/pip/cargo installs | accept | This plan does not install packages or change package metadata. |
</threat_model>

<verification>
Run these gates before marking the plan complete:
- `npm test -- tests/unit/embedding.test.ts tests/unit/vault.test.ts --bail=1`
- `npm run test:integration -- tests/integration/plugin-reconciliation.integration.test.ts --bail=1`

If local integration tooling or `.env.test` causes T-I-001 to skip, record the exact skip message from existing helpers in the SUMMARY. Do not replace the integration gate with a manual claim.
</verification>

<success_criteria>
- REQ-001 and REQ-002 are implemented and traced to T-U-001 through T-U-007 plus T-I-001.
- No Phase 152 or Phase 153 work is included.
- Forbidden source strings are absent from production files: `config.apiKey!` in `src/embedding/provider.ts` and `vaultManager as unknown as { rootPath: string }` in `src/services/plugin-reconciliation.ts`.
- MCP response envelopes, ESM TypeScript conventions, and existing integration behavior remain unchanged per D-03.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Remove small, independent audit findings and package metadata drift before broader refactors | Partially covered here by REQ-001 and REQ-002; plan 151-02 covers REQ-003 through REQ-005. |
| REQ | REQ-001 explicit embedding provider apiKey errors | Task 1. |
| REQ | REQ-002 public VaultManager absolute-path API for plugin reconciliation | Task 2. |
| REQ | REQ-003 remove inert projects seeder | Covered by plan 151-02. |
| REQ | REQ-004 visible pg cleanup failure handling without credential leaks | Covered by plan 151-02. |
| REQ | REQ-005 esbuild/@types/uuid metadata cleanup, lockfile, knip, audit | Covered by plan 151-02. |
| RESEARCH/SPEC | Invariants INV-01, INV-02, INV-04 | Tasks 1 and 2, threat model, verification. |
| CONTEXT | D-01 external docs are source of truth | Plan context and every task read_first. |
| CONTEXT | D-02 scope is REQ-001 through REQ-005 only | Scope boundaries and source audit. |
| CONTEXT | D-03 behavior preservation and safe logging | Tasks 1 and 2 preserve behavior; logging is covered by plan 151-02. |
| CONTEXT | D-04 test-plan-driven validation | Task behavior IDs and verification gates. |
| CONTEXT Deferred | REQ-006 through REQ-009, broad package modernization, zero-cycle policy | Explicitly excluded. |
</source_audit>

<output>
Create `.planning/phases/151-quick-localized-cleanup/151-01-SUMMARY.md` when done.
</output>
