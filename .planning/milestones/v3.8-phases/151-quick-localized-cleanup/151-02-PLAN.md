---
phase: 151-quick-localized-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 151-01
files_modified:
  - src/projects/seeder.ts
  - src/git/manager.ts
  - tsup.config.ts
  - package.json
  - package-lock.json
  - tests/unit/git-manager.test.ts
  - tests/unit/backup-command.test.ts
  - tests/unit/codebase-audit-remaining-remediation.test.ts
autonomous: true
requirements:
  - REQ-001
  - REQ-002
  - REQ-003
  - REQ-004
  - REQ-005
requirements_addressed:
  - REQ-001
  - REQ-002
  - REQ-003
  - REQ-004
  - REQ-005
must_haves:
  truths:
    - "The inert projects seeder and stale source/test references are removed or replaced only by intentional absence guards."
    - "Git backup cleanup does not silently swallow pgClient.end failures and never logs database credentials."
    - "Package metadata matches direct imports and bundled uuid types, with lockfile, knip, and audit checks clean."
    - "Static guards prove the REQ-001 and REQ-002 forbidden production patterns removed by 151-01 stay absent."
  artifacts:
    - path: "src/projects/seeder.ts"
      provides: "Deleted inert projects seeder"
      contains: "absent"
    - path: "src/git/manager.ts"
      provides: "Visible cleanup failure handling for dumpDatabase"
      contains: "dumpDatabase"
    - path: "tests/unit/git-manager.test.ts"
      provides: "T-U-010 cleanup rejection coverage"
    - path: "tests/unit/codebase-audit-remaining-remediation.test.ts"
      provides: "Static guards for T-U-004, T-U-007, T-U-008, T-U-009, T-U-011, T-U-012, T-U-013"
    - path: "package.json"
      provides: "Direct dependency metadata after esbuild/@types/uuid cleanup"
    - path: "package-lock.json"
      provides: "Lockfile reflecting package metadata cleanup"
  key_links:
    - from: "tests/unit/git-manager.test.ts"
      to: "src/git/manager.ts"
      via: "pgClient.end rejection behavior"
      pattern: "pgClient\\.end"
    - from: "tests/unit/codebase-audit-remaining-remediation.test.ts"
      to: "src/projects/seeder.ts"
      via: "intentional absence guard"
      pattern: "src/projects/seeder\\.ts"
    - from: "tests/unit/codebase-audit-remaining-remediation.test.ts"
      to: "src/embedding/provider.ts"
      via: "REQ-001 forbidden pattern guard after 151-01"
      pattern: "config\\.apiKey!"
    - from: "tests/unit/codebase-audit-remaining-remediation.test.ts"
      to: "src/services/plugin-reconciliation.ts"
      via: "REQ-002 forbidden private rootPath cast guard after 151-01"
      pattern: "vaultManager as unknown as \\{ rootPath: string \\}"
    - from: "tests/unit/codebase-audit-remaining-remediation.test.ts"
      to: "package.json"
      via: "package metadata guard"
      pattern: "@types/uuid|esbuild"
---

<objective>
Implement Phase 151 REQ-003, REQ-004, and REQ-005 exactly as specified in the external remediation spec and test plan, then add the cross-cutting static guards for REQ-001 and REQ-002 after plan 151-01 has completed.

Purpose: remove dead seeder code, make backup cleanup failures visible without credential leakage, and correct package metadata drift before broader refactors.
Output: focused production/package cleanup plus unit/static/command coverage for T-U-004, T-U-007, and T-U-008 through T-U-015.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/151-quick-localized-cleanup/151-CONTEXT.md
@.planning/phases/151-quick-localized-cleanup/151-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md

<source_of_truth>
Per D-01, downstream implementation agents MUST read the two external docs above before editing or verifying. Use the requirements spec first for behavior and invariants, then the test plan for exact test IDs and validation. Use local ROADMAP/REQUIREMENTS/CONTEXT/PATTERNS only as summaries. Ask the user only when those documents do not answer the question.
</source_of_truth>

<scope_boundaries>
Implement only REQ-003, REQ-004, and REQ-005 production changes per D-02. Because this plan depends on 151-01, also implement static verification guards for REQ-001 and REQ-002: T-U-004 and T-U-007. Do not implement additional REQ-001/REQ-002 production changes here unless they are needed to repair 151-01 work. Do not implement REQ-006 through REQ-009, repository-wide zero-cycle policy, broad Supabase access redesign, broad package modernization, or document tool decomposition.
</scope_boundaries>

<interfaces>
Existing contracts the executor should preserve:
- `GitManagerImpl.dumpDatabase(): Promise<string>` in `src/git/manager.ts` returns `.fqc/backup.json` on success and propagates primary backup failures.
- `package.json` scripts include `test`, `test:integration`, `knip`, and `audit` validation through npm/vitest.
- Static guards should assert exact forbidden patterns from Spec §6.1 and Test Plan §4.1, not unrelated broad repository bans. This plan runs after 151-01 so it can guard both REQ-001/REQ-002 outputs and REQ-003/REQ-005 outputs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove inert seeder and make backup cleanup failures visible</name>
  <files>src/projects/seeder.ts, src/git/manager.ts, tests/unit/git-manager.test.ts, tests/unit/backup-command.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md` Spec §3, §4, §6.1.3, §6.1.4.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md` Test Plan §3, §4.1.3, §4.1.4.
    - `.planning/phases/151-quick-localized-cleanup/151-CONTEXT.md` decisions D-01 through D-04 and deferred ideas.
    - `.planning/phases/151-quick-localized-cleanup/151-PATTERNS.md` sections for `src/projects/seeder.ts` and `src/git/manager.ts`.
    - `src/projects/seeder.ts`, `src/git/manager.ts`, `tests/unit/git-manager.test.ts`, and `tests/unit/backup-command.test.ts`.
  </read_first>
  <behavior>
    - T-U-008: Static guard in Task 2 proves `src/projects/seeder.ts` is absent.
    - T-U-009: Static guard in Task 2 proves production source no longer imports or calls `initProjects`.
    - T-U-010: `pgClient.end()` rejection is debug-logged or propagated according to the chosen implementation while preserving the primary operation result.
    - T-U-011: Static guard in Task 2 proves production cleanup code in `src/git/manager.ts` no longer contains `.catch(() => {})`.
  </behavior>
  <action>Confirm with `rg "initProjects|projects/seeder" src tests` before removal, then delete inert `src/projects/seeder.ts` per REQ-003. Remove or update stale seeder mocks such as the `../../src/projects/seeder.js` mock in `tests/unit/backup-command.test.ts` so no deleted module import remains. In `src/git/manager.ts`, replace `await pgClient.end().catch(() => {})` inside `dumpDatabase` cleanup with explicit cleanup error handling per REQ-004 and D-03: debug-log the cleanup failure without credentials, or propagate only when no primary backup error exists. If both the primary backup operation and `pgClient.end()` fail, preserve the primary error as the caller-observed failure. Add focused `tests/unit/git-manager.test.ts` coverage that exercises cleanup rejection and verifies no database URL or password string appears in logged cleanup metadata.</action>
  <verify>
    <automated>npm test -- tests/unit/git-manager.test.ts tests/unit/backup-command.test.ts --bail=1</automated>
    <automated>! rg -n "initProjects|projects/seeder" src tests --glob "!tests/unit/codebase-audit-remaining-remediation.test.ts"</automated>
    <automated>! rg -n "\\.catch\\(\\(\\) => \\{\\}\\)" src/git/manager.ts</automated>
  </verify>
  <done>REQ-003 and REQ-004 are complete: the dead seeder is gone, stale references are removed or intentionally guarded, and pg close cleanup failures are visible without masking primary errors or leaking credentials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static guards and package metadata cleanup</name>
  <files>tests/unit/codebase-audit-remaining-remediation.test.ts, tsup.config.ts, package.json, package-lock.json</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md` Spec §3, §4, §6.1.1 through §6.1.5.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md` Test Plan §3, §4.1.1 through §4.1.5.
    - `.planning/phases/151-quick-localized-cleanup/151-CONTEXT.md` D-01 through D-04 and deferred ideas.
    - `.planning/phases/151-quick-localized-cleanup/151-PATTERNS.md` package metadata and static guard notes.
    - `tests/unit/codebase-audit-remaining-remediation.test.ts` if it exists, plus `src/embedding/provider.ts`, `src/services/plugin-reconciliation.ts`, `tsup.config.ts`, `package.json`, and `package-lock.json`.
  </read_first>
  <behavior>
    - T-U-004: Guard proves `src/embedding/provider.ts` no longer contains `config.apiKey!`.
    - T-U-007: Guard proves `src/services/plugin-reconciliation.ts` no longer casts `vaultManager` to access private `rootPath`.
    - T-U-008: Guard proves `src/projects/seeder.ts` is absent.
    - T-U-009: Guard proves production source no longer imports or calls `initProjects`.
    - T-U-011: Guard proves `src/git/manager.ts` no longer contains `.catch(() => {})` in production cleanup.
    - T-U-012: Guard proves a direct `esbuild` type import is matched by direct package metadata, or the direct import has been removed.
    - T-U-013: Guard proves `@types/uuid` is absent from `package.json`.
    - T-U-014 and T-U-015: `npm run knip` and `npm audit` pass after lockfile refresh.
  </behavior>
  <action>Create or update `tests/unit/codebase-audit-remaining-remediation.test.ts` as a focused static guard suite for the exact Phase 151 forbidden patterns. Include T-U-004 by asserting `src/embedding/provider.ts` does not contain `config.apiKey!`. Include T-U-007 by asserting `src/services/plugin-reconciliation.ts` does not contain `vaultManager as unknown as { rootPath: string }` or private `rootPath` access patterns used by the old cast. Include T-U-008, T-U-009, T-U-011, T-U-012, and T-U-013 for the REQ-003 through REQ-005 patterns. Avoid broad repository bans; read only the named source files and package metadata. Make the `initProjects|projects/seeder` guard avoid counting its own test source text as a live dependency, for example by scanning production source separately from the guard file or building the searched tokens from string fragments. For REQ-005, either add `esbuild` as a direct dev dependency matching the installed transitive version used by `tsup`, or remove the `type { Plugin } from 'esbuild'` import from `tsup.config.ts` and replace it with a local compatible type; choose the smaller behavior-preserving change. Remove `@types/uuid` from `devDependencies` because `uuid` ships types, then refresh `package-lock.json` with npm without changing unrelated package lanes. Do not modernize `uuid` beyond the named metadata cleanup and do not include Phase 152 or Phase 153 guards.</action>
  <verify>
    <automated>npm test -- tests/unit/codebase-audit-remaining-remediation.test.ts --bail=1</automated>
    <automated>npm run knip</automated>
    <automated>npm audit</automated>
    <automated>npm test -- --bail=1</automated>
  </verify>
  <done>REQ-005 plus all Phase 151 structural guards T-U-004, T-U-007, T-U-008, T-U-009, T-U-011, T-U-012, and T-U-013 are complete, and full unit, knip, and audit gates pass or report only documented pre-existing environment skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database URL -> cleanup logging | `dumpDatabase` cleanup handles pg failures where error objects may contain connection metadata. |
| package metadata -> install/build toolchain | `package.json` and `package-lock.json` define direct build-time dependencies and audit surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-03 | Information Disclosure | `src/git/manager.ts` cleanup logging | mitigate | Log only a generic cleanup failure message plus safe error message/class after redacting or avoiding `databaseUrl`; tests must prove credential strings are absent. Block completion if a high-severity credential leak is introduced. |
| T-151-04 | Repudiation | `src/git/manager.ts` backup cleanup | mitigate | Preserve primary operation errors when cleanup also fails, so caller-observed failures remain attributable to the backup operation. |
| T-151-05 | Tampering | `package.json` / `package-lock.json` | mitigate | Refresh lockfile from npm metadata, remove stale `@types/uuid`, ensure direct `esbuild` import has direct metadata or no import, then run `npm run knip` and `npm audit`. |
| T-151-SC | Tampering | npm package metadata drift | mitigate | No new package-manager install is required unless choosing direct `esbuild`; if adding `esbuild`, use the version already present via `tsup` in `package-lock.json` and verify with `npm audit` and `npm run knip`. |
</threat_model>

<verification>
Run these gates before marking the plan complete:
- `npm test -- tests/unit/git-manager.test.ts tests/unit/backup-command.test.ts tests/unit/codebase-audit-remaining-remediation.test.ts --bail=1`
- `npm run knip`
- `npm audit`
- `npm test -- --bail=1`
</verification>

<success_criteria>
- REQ-003 through REQ-005 are implemented and traced to T-U-008 through T-U-015, and REQ-001/REQ-002 static guards T-U-004 and T-U-007 are implemented after plan 151-01.
- No Phase 152 or Phase 153 work is included.
- Forbidden source strings are absent from production files: `config.apiKey!` in `src/embedding/provider.ts`, `vaultManager as unknown as { rootPath: string }` in `src/services/plugin-reconciliation.ts`, production `initProjects|projects/seeder`, `.catch(() => {})` in `src/git/manager.ts`, and `@types/uuid` in `package.json`.
- Credential-bearing values are not emitted in cleanup logs.
- Package metadata and lockfile are consistent with `tsup.config.ts`, `uuid`, `npm run knip`, and `npm audit`.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Remove small, independent audit findings and package metadata drift before broader refactors | Partially covered here by REQ-003 through REQ-005; plan 151-01 covers REQ-001 and REQ-002. |
| REQ | REQ-001 explicit embedding provider apiKey errors | Production behavior covered by plan 151-01; T-U-004 static guard covered by Task 2 here. |
| REQ | REQ-002 public VaultManager absolute-path API for plugin reconciliation | Production behavior covered by plan 151-01; T-U-007 static guard covered by Task 2 here. |
| REQ | REQ-003 remove inert projects seeder | Task 1 and Task 2. |
| REQ | REQ-004 visible pg cleanup failure handling without credential leaks | Task 1 and Task 2. |
| REQ | REQ-005 esbuild/@types/uuid metadata cleanup, lockfile, knip, audit | Task 2. |
| RESEARCH/SPEC | Invariants INV-01, INV-04, INV-07, INV-08 | Tasks 1 and 2, threat model, verification. |
| CONTEXT | D-01 external docs are source of truth | Plan context and every task read_first. |
| CONTEXT | D-02 scope is REQ-001 through REQ-005 only | Scope boundaries and source audit. |
| CONTEXT | D-03 behavior preservation and safe logging | Task 1 and threat model. |
| CONTEXT | D-04 test-plan-driven validation | Task behavior IDs and verification gates. |
| CONTEXT Deferred | REQ-006 through REQ-009, broad package modernization, zero-cycle policy | Explicitly excluded. |
</source_audit>

<output>
Create `.planning/phases/151-quick-localized-cleanup/151-02-SUMMARY.md` when done.
</output>
