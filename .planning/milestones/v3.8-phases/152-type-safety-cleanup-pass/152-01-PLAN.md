---
phase: 152-type-safety-cleanup-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/utils/document-output.ts
  - src/services/scanner.ts
  - src/mcp/tools/llm-usage.ts
  - tests/unit/codebase-audit-remaining-remediation.test.ts
  - tests/unit/scanner.test.ts
  - tests/unit/llm-usage-tool.test.ts
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
autonomous: true
requirements:
  - REQ-006
  - REQ-007
requirements_addressed:
  - REQ-006
  - REQ-007
must_haves:
  truths:
    - "Residual document-output and scanner double assertions are gone without public response drift."
    - "Scanner reconciliation still handles active, missing, archived, duplicate, and unchanged rows."
    - "get_llm_usage keeps summary, recent, trace, by-model, and by-purpose response shapes stable."
    - "LLM usage query and grouping code no longer needs broad unsafe eslint disables or grouping non-null assertions."
  artifacts:
    - path: "src/mcp/utils/document-output.ts"
      provides: "Tighter consolidated response return typing"
      contains_not: "as unknown as Record<string, unknown>"
    - path: "src/services/scanner.ts"
      provides: "Typed active/missing and archived document select handling"
      contains_not: "as unknown as Promise"
    - path: "src/mcp/tools/llm-usage.ts"
      provides: "Typed LLM usage query chain and safe grouping helpers"
      contains_not: "!.push"
    - path: "tests/unit/codebase-audit-remaining-remediation.test.ts"
      provides: "T-U-016 through T-U-020 static guards"
  key_links:
    - from: "src/mcp/utils/document-output.ts"
      to: "tests/integration/tools-response-format.test.ts"
      via: "get_document consolidated response JSON shape"
      pattern: "buildConsolidatedResponse"
    - from: "src/services/scanner.ts"
      to: "tests/unit/scanner.test.ts"
      via: "runScanOnce reconciliation rows"
      pattern: "template_meta"
    - from: "src/mcp/tools/llm-usage.ts"
      to: "tests/unit/llm-usage-tool.test.ts"
      via: "registered get_llm_usage handler"
      pattern: "registerLlmUsageTools"
---

<objective>
Remove Phase 152 type escape findings for document output, scanner selects, and LLM usage while preserving all public behavior.

Purpose: Close REQ-006 and REQ-007 from the authoritative remediation spec without broad Supabase abstraction work.
Output: Production type cleanup plus exact static guards and focused scanner / LLM usage regression coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Downstream implementation agents MUST read the two external source-of-truth documents first for any Phase 152 question, then local planning docs, then ask the user only if those documents do not answer the question.

@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/152-type-safety-cleanup-pass/152-CONTEXT.md
@.planning/phases/152-type-safety-cleanup-pass/152-PATTERNS.md
@.planning/phases/152-type-safety-cleanup-pass/152-VALIDATION.md
@.planning/phases/151-quick-localized-cleanup/151-01-SUMMARY.md
@.planning/phases/151-quick-localized-cleanup/151-02-SUMMARY.md

<interfaces>
Phase 151 gap-fix guard state:
- `tests/unit/codebase-audit-remaining-remediation.test.ts` already contains Phase 151 guards for T-U-004, T-U-007, T-I-001, T-U-008, T-U-009, T-U-011, T-U-012, and T-U-013.
- Phase 152 tasks must append or extend guards in that file without deleting, weakening, renaming, or broadening away those Phase 151 assertions.

From src/mcp/utils/document-output.ts:
- `DocumentEnvelope` is the structured consolidated document response type.
- `buildConsolidatedResponse(envelope, include, options): DocumentEnvelope` already models optional `body`, `frontmatter`, `headings`, and `extracted_sections`.

From src/services/scanner.ts:
- `runScanOnce(config)` reconciles DB rows against vault files.
- REQ-006 requires selected fields to remain `id`, `path`, `content_hash`, `title`, `status`, `updated_at`, and `template_meta` where currently requested.

From src/mcp/tools/llm-usage.ts:
- `UsageRow` is the row shape for `fqc_llm_usage`.
- `fetchRows` uses `from`, `select`, `eq`, `gte`, `lte`, `order`, `limit`, and awaited `{ data, error }`.
- By-purpose grouping must keep `_direct` rows separate; by-model grouping uses the model/provider composite key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove REQ-006 double assertions with exact guards</name>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md
    @.planning/phases/152-type-safety-cleanup-pass/152-CONTEXT.md
    @.planning/phases/152-type-safety-cleanup-pass/152-PATTERNS.md
    @src/mcp/utils/document-output.ts
    @src/services/scanner.ts
    @tests/unit/codebase-audit-remaining-remediation.test.ts
    @tests/unit/scanner.test.ts
    @tests/unit/document-output.test.ts
  </read_first>
  <files>src/mcp/utils/document-output.ts, src/services/scanner.ts, tests/unit/codebase-audit-remaining-remediation.test.ts, tests/unit/scanner.test.ts</files>
  <behavior>
    - T-U-016: `src/mcp/utils/document-output.ts` no longer contains `as unknown as Record&lt;string, unknown&gt;`.
    - T-U-017: `src/services/scanner.ts` no longer contains `as unknown as Promise`.
    - T-U-018: scanner tests still cover active, missing, archived, duplicate, and unchanged document rows.
  </behavior>
  <action>Implement REQ-006 per D-01, D-03, and D-04. In `document-output.ts`, remove the consolidated response double assertion by tightening the return type at the caller/callee boundary so `buildConsolidatedResponse` can be returned without casting to `Record&lt;string, unknown&gt;`. In `scanner.ts`, replace the active/missing and archived Supabase select `as unknown as Promise` casts with local row/result interfaces or narrow helper typing; preserve the selected fields exactly, including `template_meta` where requested. Add exact static guards for T-U-016 and T-U-017 to `tests/unit/codebase-audit-remaining-remediation.test.ts` by appending to the existing guard suite; preserve the Phase 151 T-U-004, T-U-007, T-I-001, T-U-008, T-U-009, T-U-011, T-U-012, and T-U-013 assertions unchanged unless a Phase 152 production edit genuinely requires a compatible assertion update. Extend `tests/unit/scanner.test.ts` only if existing coverage does not explicitly exercise active/missing/archived row handling after the typed select replacement. Do not introduce repository-wide Supabase abstractions and do not touch Phase 153 document-tool decomposition.</action>
  <verify>
    <automated>npm test -- tests/unit/codebase-audit-remaining-remediation.test.ts tests/unit/scanner.test.ts tests/unit/document-output.test.ts --bail=1</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `rg -n "as unknown as Record" src/mcp/utils/document-output.ts` returns no matches.
    - Source assertion: `rg -n "as unknown as Promise" src/services/scanner.ts` returns no matches.
    - Behavior assertion: scanner row reconciliation still preserves `id`, `path`, `content_hash`, `title`, `status`, `updated_at`, and `template_meta` where currently selected.
    - Test-command assertion: targeted Vitest command and `npm run typecheck` pass.
  </acceptance_criteria>
  <done>REQ-006 is closed by production source changes and T-U-016 through T-U-018 evidence without external scanner or document-output behavior changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace REQ-007 LLM usage unsafe query and grouping escapes</name>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Remaining Remediation Requirements pt2/Codebase Audit Remaining Remediation Test Plan.md
    @.planning/phases/152-type-safety-cleanup-pass/152-CONTEXT.md
    @.planning/phases/152-type-safety-cleanup-pass/152-PATTERNS.md
    @src/mcp/tools/llm-usage.ts
    @tests/unit/codebase-audit-remaining-remediation.test.ts
    @tests/unit/llm-usage-tool.test.ts
    @tests/scenarios/directed/testcases/test_get_llm_usage_by_purpose.py
    @tests/scenarios/directed/testcases/test_get_llm_usage_by_model.py
    @tests/scenarios/integration/tests/llm_by_purpose_mode.yml
    @tests/scenarios/integration/tests/llm_by_model_mode.yml
  </read_first>
  <files>src/mcp/tools/llm-usage.ts, tests/unit/codebase-audit-remaining-remediation.test.ts, tests/unit/llm-usage-tool.test.ts, tests/scenarios/directed/DIRECTED_COVERAGE.md, tests/scenarios/integration/INTEGRATION_COVERAGE.md</files>
  <behavior>
    - T-U-019: broad block-level disables around `applyEntityFilters` and `fetchRows` are gone.
    - T-U-020: grouping code no longer uses `Map.get(...)!.push(...)` or equivalent non-null assertion push patterns.
    - T-U-021/T-U-022: by-purpose and by-model calculations keep current response shapes and arithmetic.
    - T-S-001/T-S-002 and T-Y-001/T-Y-002 remain mapped to existing scenario files or coverage rows are added if absent.
  </behavior>
  <action>Implement REQ-007 per D-01, D-03, D-04, and D-06. In `llm-usage.ts`, define narrow local structural types for the Supabase client/query chain actually used by `fetchRows`: `from`, `select`, `eq`, `gte`, `lte` or `lt` if the current implementation uses it, `order`, `limit`, and awaited `{ data: UsageRow[] | null; error: { message: string } | null }`. Type `applyEntityFilters` and `fetchRows` with those local interfaces and remove the broad block-level eslint disables for `no-explicit-any`, `no-unsafe-assignment`, `no-unsafe-call`, and `no-unsafe-member-access` around those functions. Replace `purposeGroups.get(... )!.push(...)` and `groups.get(... )!.push(...)` with a small local get-or-create helper or equivalent typed grouping flow. Preserve `_direct` separation, by-model composite key behavior, date-window upper-bound behavior, trace filtering, recent mode, and response envelopes. Add T-U-019 and T-U-020 static guards by appending to `tests/unit/codebase-audit-remaining-remediation.test.ts` without weakening existing Phase 151 guards, and add or preserve unit assertions for T-U-021 and T-U-022. Update directed/YAML coverage matrices only to record D-71, D-72, IS-16, and IS-17 if those rows are not already present; do not rewrite scenario behavior unless a required coverage ID is missing.</action>
  <verify>
    <automated>npm test -- tests/unit/codebase-audit-remaining-remediation.test.ts tests/unit/llm-usage-tool.test.ts --bail=1</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/mcp/tools/llm-usage.ts` has no block-level disables for the named unsafe eslint rules around `applyEntityFilters` or `fetchRows`.
    - Source assertion: `rg -n "!\\.push|\\.get\\([^\\n]+\\)!\\.push" src/mcp/tools/llm-usage.ts` returns no grouping non-null push matches.
    - Behavior assertion: `get_llm_usage` summary, recent, trace, by-purpose, and by-model JSON shapes match existing tests.
    - Scenario traceability assertion: D-71, D-72, IS-16, and IS-17 are present in the relevant coverage matrices or already mapped by existing entries.
    - Test-command assertion: targeted Vitest command and `npm run typecheck` pass.
  </acceptance_criteria>
  <done>REQ-007 is closed by typed query helpers, null-safe grouping, static guards T-U-019/T-U-020, and green LLM usage regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase -> scanner | Database rows are external persisted state and may contain nullable or unexpected values. |
| Supabase -> get_llm_usage | Usage rows are queried from persisted logs and transformed into MCP response JSON. |
| MCP response -> caller | Response text is public tool output and must not drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01 | Tampering | `src/services/scanner.ts` row typing | mitigate | Preserve exact selected fields and nullable handling; run scanner regression tests for active/missing/archived rows. |
| T-152-02 | Information Disclosure | `src/mcp/tools/llm-usage.ts` response transform | mitigate | Do not add new logs or fields; preserve existing public JSON shape through unit and scenario regressions. |
| T-152-03 | Denial of Service | `get_llm_usage` query typing | accept | This plan changes local types only and does not alter query filters, limits, or ordering. |
| T-152-04 | Spoofing | MCP caller identity | accept | No auth/session behavior changes are in scope; MCP remains stateless per project contract. |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the task-level automated commands after each task. At plan completion, run `npm test -- tests/unit/codebase-audit-remaining-remediation.test.ts tests/unit/scanner.test.ts tests/unit/document-output.test.ts tests/unit/llm-usage-tool.test.ts --bail=1` and `npm run typecheck`.
</verification>

<success_criteria>
REQ-006 and REQ-007 appear in `requirements` and `requirements_addressed`; T-U-016 through T-U-022 are implemented or preserved; no Phase 153 / REQ-009 work is included; public document, scanner, and LLM usage behavior remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/152-type-safety-cleanup-pass/152-01-SUMMARY.md` when done.
</output>
