---
phase: 135-tool-registry-dispatch-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/macro-registry.test.ts
  - tests/unit/macro-dispatcher.test.ts
  - tests/unit/macro-permission-prescan.test.ts
  - tests/unit/macro-hard-exclusions.test.ts
  - tests/unit/macro-caller-identity.test.ts
  - tests/integration/macro-tool-dispatch.test.ts
  - tests/config/vitest.integration.config.ts
autonomous: true
requirements:
  - MACRO-DISP-01
  - MACRO-DISP-02
  - MACRO-DISP-03
  - MACRO-DISP-04
  - MACRO-DISP-05
  - MACRO-DISP-06
  - MACRO-DISP-07
must_haves:
  truths:
    - "Dispatch and permission behavior has executable coverage before implementation changes."
    - "Test Plan section 4.6 cases T-U-156 through T-U-171 and T-I-003/T-I-004 are represented in local tests."
    - "Integration coverage is discoverable by the explicit Vitest integration include list."
  artifacts:
    - path: "tests/unit/macro-registry.test.ts"
      provides: "Registry construction tests for fq/native catalog allowlist and exclusions"
    - path: "tests/unit/macro-dispatcher.test.ts"
      provides: "Dispatcher lookup and backstop tests"
    - path: "tests/unit/macro-permission-prescan.test.ts"
      provides: "Static AST pre-scan and zero-side-effect tests"
    - path: "tests/unit/macro-hard-exclusions.test.ts"
      provides: "call_macro, template masquerade, and delegated call_model exclusion tests"
    - path: "tests/unit/macro-caller-identity.test.ts"
      provides: "Host vs purpose allowlist and no callerKind schema tests"
    - path: "tests/integration/macro-tool-dispatch.test.ts"
      provides: "Real fq.write_document and fq.search macro dispatch integration tests"
  key_links:
    - from: "tests/config/vitest.integration.config.ts"
      to: "tests/integration/macro-tool-dispatch.test.ts"
      via: "explicit include entry"
      pattern: "macro-tool-dispatch\\.test\\.ts"
---

<objective>
Create the Wave 0 test surface for Phase 135 before production implementation.

Purpose: lock the macro dispatch/permission contract from the canonical requirements and Test Plan §4.6 so implementation agents work against executable expectations.
Output: focused unit and integration tests, with the integration suite registered in the explicit Vitest include list.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-CONTEXT.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-RESEARCH.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-PATTERNS.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@src/macro/types.ts
@src/macro/evaluator.ts
@src/mcp/tools/macro.ts
@tests/unit/macro-test-helpers.ts
@tests/config/vitest.integration.config.ts

<interfaces>
From `src/macro/types.ts`:
```typescript
export interface Program { kind: 'Program'; statements: Statement[]; }
export type Statement = Binding | Pipeline | ForLoop | WhileLoop | IfStmt | ToolCall | ToolExistsCall;
export type Expr = StringLit | NumLit | NullLit | VarRef | ListLit | ObjectLit | FieldAccess | RangeExpr | BinaryExpr | UnaryExpr | Call | Pipeline | ToolCall | ToolExistsCall;
export interface ToolCall { kind: 'ToolCall'; server: string; tool: string; arg: ObjectLit | VarRef | FieldAccess | undefined; line: number; }
```

From `src/macro/evaluator.ts`:
```typescript
export interface MacroInvocationContext {
  dispatchTool?: (server: string, tool: string, arg: Record<string, MacroValue>, context: MacroInvocationContext) => ToolResult | Promise<ToolResult>;
  broker: McpBroker;
}
export async function evaluateProgram(program: Program, options?: EvaluateProgramOptions): Promise<ToolResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add registry and dispatcher unit coverage</name>
  <files>tests/unit/macro-registry.test.ts, tests/unit/macro-dispatcher.test.ts</files>
  <read_first>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md</file>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-CONTEXT.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-PATTERNS.md</file>
    <file>src/macro/types.ts</file>
    <file>src/llm/tool-registry.ts</file>
    <file>src/llm/tool-dispatcher.ts</file>
    <file>src/mcp/tool-catalog.ts</file>
    <file>src/services/mcp-broker.ts</file>
  </read_first>
  <action>Create red Vitest tests for `ToolRegistry`, `ServerEntry`, `ToolFn`, `buildToolRegistry`, and `dispatchMacroTool` per D-01 and Requirements §6.4.1/§6.4.3. `tests/unit/macro-registry.test.ts` must prove the `fq` registry entry is built from the native catalog/`assembleNativeToolRegistry`, omits `call_macro`, and can include a mocked broker server without changing dispatcher code. `tests/unit/macro-dispatcher.test.ts` must cover T-U-156 through T-U-159 plus the white-box allowlist backstop portion of T-U-163: successful `fq.search`, `unknown_server`, `unknown_tool` with `available`, brokered `brave_search.web_search`, and forbidden `fq.archive_document` refusing before handler invocation.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-registry macro-dispatcher</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/macro-registry.test.ts` imports `buildToolRegistry` from `../../src/macro/registry.js` and asserts no `callerKind` or direct `src/mcp/tools/*` imports are needed.
    - `tests/unit/macro-dispatcher.test.ts` imports `dispatchMacroTool` from `../../src/macro/dispatcher.js` and asserts error codes `unknown_server`, `unknown_tool`, and `forbidden_tools`.
    - `rg -n "T-U-156|T-U-157|T-U-158|T-U-159|T-U-163" tests/unit/macro-dispatcher.test.ts` finds all listed IDs.
    - `rg -n "call_macro|buildToolRegistry|ToolRegistry|ServerEntry|ToolFn" tests/unit/macro-registry.test.ts` finds all listed contract names.
  </acceptance_criteria>
  <done>Registry and dispatcher tests fail only because Phase 135 production modules are not implemented yet, not because of syntax/import mistakes.</done>
</task>

<task type="auto">
  <name>Task 2: Add permission, hard-exclusion, and caller-identity unit coverage</name>
  <files>tests/unit/macro-permission-prescan.test.ts, tests/unit/macro-hard-exclusions.test.ts, tests/unit/macro-caller-identity.test.ts</files>
  <read_first>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md</file>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-CONTEXT.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-PATTERNS.md</file>
    <file>src/macro/types.ts</file>
    <file>src/macro/evaluator.ts</file>
    <file>src/mcp/tools/macro.ts</file>
    <file>src/llm/template-tools.ts</file>
    <file>src/llm/tool-registry.ts</file>
  </read_first>
  <action>Create red tests for `preScanToolReferences`, `MacroCallerContext`, hard exclusions, and the public `call_macro` schema per D-01 and Test Plan §4.6. `macro-permission-prescan.test.ts` must include AST fixtures with nested `if`, `for`, `while`, expression-position `ToolCall`, and statement-position `ToolCall`, proving all forbidden/unknown references are reported before side effects. `macro-hard-exclusions.test.ts` must assert `fq.call_macro` returns `unknown_tool`, template masquerade returns `template_masquerade_tools_not_callable_from_macro`, host `fq.call_model` is allowed when present in the host allowlist, and delegated `fq.call_model` returns `forbidden_tools` with `reason: "recursive_model_excluded_from_delegated_macros"`. `macro-caller-identity.test.ts` must assert inbound calls use host allowlist, agentic-originated calls use purpose allowlist, and the Zod request schema does not expose `callerKind`.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-permission-prescan macro-hard-exclusions macro-caller-identity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-160|T-U-161|T-U-162|T-U-164" tests/unit/macro-permission-prescan.test.ts` finds all listed IDs.
    - `rg -n "T-U-165|T-U-166|T-U-167|T-U-168|template_masquerade_tools_not_callable_from_macro|recursive_model_excluded_from_delegated_macros" tests/unit/macro-hard-exclusions.test.ts` finds all listed IDs and required reason/error strings.
    - `rg -n "T-U-169|T-U-170|T-U-171|callerKind" tests/unit/macro-caller-identity.test.ts` finds all listed IDs and the schema absence assertion.
    - `rg -n "assembleNativeToolRegistry|preScanToolReferences|MacroCallerContext" tests/unit/macro-permission-prescan.test.ts tests/unit/macro-caller-identity.test.ts` finds all listed source-of-truth names.
  </acceptance_criteria>
  <done>Permission, hard-exclusion, and caller-identity tests encode the complete MACRO-DISP-02 through MACRO-DISP-07 contract from the canonical docs.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration dispatch coverage and register it</name>
  <files>tests/integration/macro-tool-dispatch.test.ts, tests/config/vitest.integration.config.ts</files>
  <read_first>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md</file>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-CONTEXT.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-VALIDATION.md</file>
    <file>tests/config/vitest.integration.config.ts</file>
    <file>tests/integration/macro-parse-error.test.ts</file>
    <file>tests/integration/macro-shell-verbs.integration.test.ts</file>
  </read_first>
  <action>Create `tests/integration/macro-tool-dispatch.test.ts` for T-I-003 and T-I-004 using existing integration helper patterns. The tests must exercise real `call_macro` with `fq.write_document({...})` and `fq.search({...})` through FlashQuery's registered native tool handlers, not a mock dispatcher. Update `tests/config/vitest.integration.config.ts` to include `tests/integration/macro-tool-dispatch.test.ts` because this config has an explicit include list. The integration tests may skip only through existing `.env.test`/Supabase helper behavior; do not add a custom blanket skip.</action>
  <verify>
    <automated>npm run test:integration -- --reporter=verbose macro-tool-dispatch</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-I-003|T-I-004|write_document|search" tests/integration/macro-tool-dispatch.test.ts` finds all listed IDs and tool names.
    - `rg -n "tests/integration/macro-tool-dispatch\\.test\\.ts" tests/config/vitest.integration.config.ts` finds exactly one include entry.
    - `npm run test:integration -- --reporter=verbose macro-tool-dispatch` is the preserved required command from Requirements §8.8.
  </acceptance_criteria>
  <done>The integration test file exists, is in the explicit include list, and expresses real native handler dispatch expectations for Phase 135.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> macro engine | Tests intentionally construct user-controlled macro source and AST fixtures. |
| MCP request schema -> caller identity | Tests must prove caller identity is not user-controlled by schema fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Elevation of Privilege | `call_macro` schema | mitigate | Add T-U-171 asserting `callerKind` is absent from the public request schema. |
| T-135-02 | Tampering | permission pre-scan tests | mitigate | Add T-U-162 proving forbidden references produce zero side effects before handlers can mutate state. |
| T-135-03 | Elevation of Privilege | dispatcher tests | mitigate | Add T-U-163 white-box test that bypasses pre-scan and verifies dispatch-time allowlist backstop blocks invocation. |
| T-135-04 | Elevation of Privilege | template masquerade tests | mitigate | Add T-U-166 asserting `template_masquerade_tools_not_callable_from_macro`. |
| T-135-05 | Elevation of Privilege | delegated model recursion tests | mitigate | Add T-U-168 asserting `recursive_model_excluded_from_delegated_macros`. |
</threat_model>

<verification>
Run both required commands after this Wave 0 plan lands; they are expected to fail until implementation plans complete, but must fail for missing behavior rather than malformed tests:

<automated>npm test -- --reporter=verbose macro-registry macro-permission-prescan macro-dispatcher</automated>
<automated>npm run test:integration -- --reporter=verbose macro-tool-dispatch</automated>
</verification>

<success_criteria>
- All Test Plan §4.6 test IDs T-U-156 through T-U-171 and T-I-003/T-I-004 are present in the expected files.
- `tests/config/vitest.integration.config.ts` includes `tests/integration/macro-tool-dispatch.test.ts`.
- No test introduces `callerKind` as an accepted public input field.
</success_criteria>

<output>
After completion, create `.planning/phases/135-tool-registry-dispatch-permissions/135-01-SUMMARY.md`.
</output>
