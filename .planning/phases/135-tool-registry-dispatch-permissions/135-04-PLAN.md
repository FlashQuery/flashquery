---
phase: 135-tool-registry-dispatch-permissions
plan: 04
type: execute
wave: 4
depends_on:
  - 135-03
files_modified:
  - src/mcp/tools/macro.ts
  - src/macro/registry.ts
  - tests/unit/macro-caller-identity.test.ts
  - tests/integration/macro-tool-dispatch.test.ts
  - tests/config/vitest.integration.config.ts
autonomous: true
requirements:
  - MACRO-DISP-01
  - MACRO-DISP-02
  - MACRO-DISP-03
  - MACRO-DISP-04
  - MACRO-DISP-05
  - MACRO-DISP-06
  - MACRO-DISP-07
must_haves:
  truths:
    - "Inbound `call_macro` uses internal host identity and never accepts `callerKind` from the user."
    - "Agentic-loop-originated macro calls can pass a purpose context that drives the allowlist."
    - "Real `fq.write_document` and `fq.search` macros dispatch through registered native handlers."
  artifacts:
    - path: "src/mcp/tools/macro.ts"
      provides: "Public `call_macro` schema and native catalog/registry wiring"
    - path: "tests/unit/macro-caller-identity.test.ts"
      provides: "Caller identity and schema absence coverage"
    - path: "tests/integration/macro-tool-dispatch.test.ts"
      provides: "Real native tool dispatch integration coverage"
    - path: "tests/config/vitest.integration.config.ts"
      provides: "Explicit integration include for macro dispatch suite"
  key_links:
    - from: "src/mcp/tools/macro.ts"
      to: "src/mcp/tool-catalog.ts"
      via: "getNativeToolCatalog(server)"
      pattern: "getNativeToolCatalog"
    - from: "src/mcp/tools/macro.ts"
      to: "src/macro/registry.ts"
      via: "buildToolRegistry"
      pattern: "buildToolRegistry"
    - from: "src/mcp/tools/macro.ts"
      to: "src/macro/evaluator.ts"
      via: "evaluateProgram registry/allowlist options"
      pattern: "evaluateProgram"
---

<objective>
Wire Phase 135 through the public `call_macro` handler and close integration validation.

Purpose: ensure caller identity is derived internally and real FlashQuery native tools execute through the macro registry/dispatcher path.
Output: public handler wiring, caller-identity tests green, integration dispatch suite green, and required phase verification commands passing.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-CONTEXT.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-RESEARCH.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-PATTERNS.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@.planning/phases/132-evaluator-core/132-04-SUMMARY.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-05-SUMMARY.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-01-SUMMARY.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-02-SUMMARY.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-03-SUMMARY.md
@src/mcp/tools/macro.ts
@src/mcp/tool-catalog.ts
@src/mcp/tool-exposure.ts
@src/macro/registry.ts
@src/macro/evaluator.ts
@tests/config/vitest.integration.config.ts

<interfaces>
From `src/mcp/tools/macro.ts`:
```typescript
export function registerMacroTools(server: McpServer, config: FlashQueryConfig): void;
```

From `src/mcp/tool-catalog.ts`:
```typescript
export function getNativeToolCatalog(server: McpServer): NativeToolDefinition[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire caller context and native catalog into call_macro</name>
  <files>src/mcp/tools/macro.ts, src/macro/registry.ts, tests/unit/macro-caller-identity.test.ts</files>
  <read_first>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md</file>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-CONTEXT.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-PATTERNS.md</file>
    <file>src/mcp/tools/macro.ts</file>
    <file>src/mcp/tool-catalog.ts</file>
    <file>src/llm/tool-dispatcher.ts</file>
    <file>src/macro/registry.ts</file>
    <file>src/macro/evaluator.ts</file>
    <file>tests/unit/macro-caller-identity.test.ts</file>
  </read_first>
  <action>Update `registerMacroTools` so inbound MCP `call_macro` creates internal `MacroCallerContext = { origin: "host" }` without adding `callerKind` to the Zod `inputSchema`. It must fetch the native catalog with `getNativeToolCatalog(server)`, construct a `NativeToolDispatchContext` (or equivalently named typed field) with at minimum `signal`, `instanceId: config.instance.id`, optional `logger`, and optional `logContext`, call `buildToolRegistry(config, { origin: "host" }, broker, catalog, nativeDispatchContext)` or the final equivalent signature, and pass the resulting `registry`/allowlist into `evaluateProgram`. If the registry is built inside `evaluateProgram`, thread `nativeDispatchContext` through `EvaluateProgramOptions` and `MacroInvocationContext` explicitly. `buildToolRegistry` must use `resolveHostToolExposure(config.hostMcpTools)` for this host path. Add an exported internal helper only if needed for agentic-loop-originated calls, e.g. `runMacroSource({ source, inputVars, callerContext, config, catalog, broker, nativeDispatchContext })`, where delegated callers supply `MacroCallerContext = { origin: "delegated", purposeName }` internally and that delegated path uses `assembleNativeToolRegistry(config, purposeName, catalog)`. Do not expose `callerKind` or purpose selection in the public MCP request schema.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-caller-identity macro-registry macro-dispatcher</automated>
  </verify>
  <acceptance_criteria>
    - `! (rg -n "callerKind" src/mcp/tools/macro.ts src/macro/registry.ts | grep -v "does NOT include\\|not expose\\|absence" | grep -v '^#')` succeeds; no public schema or production option accepts user-controlled `callerKind`.
    - `rg -n "getNativeToolCatalog|buildToolRegistry|evaluateProgram" src/mcp/tools/macro.ts` finds all required wiring calls.
    - `rg -n "NativeToolDispatchContext|instanceId: config\\.instance\\.id|signal|logContext|nativeDispatchContext" src/mcp/tools/macro.ts src/macro/evaluator.ts src/macro/registry.ts tests/unit/macro-caller-identity.test.ts` finds the native dispatch context construction/threading and tests.
    - `rg -n "MacroCallerContext|origin: 'host'|origin: 'delegated'|purposeName|resolveHostToolExposure|assembleNativeToolRegistry" src/macro/types.ts src/macro/registry.ts src/mcp/tools/macro.ts tests/unit/macro-caller-identity.test.ts` finds internal caller context support and both allowlist sources.
    - `npm test -- --reporter=verbose macro-caller-identity macro-registry macro-dispatcher` exits 0.
  </acceptance_criteria>
  <done>Caller identity is internal, host/delegated allowlists are testable, and public `call_macro` builds the registry from the native catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Close real native dispatch integration</name>
  <files>tests/integration/macro-tool-dispatch.test.ts, tests/config/vitest.integration.config.ts, src/mcp/tools/macro.ts, src/macro/registry.ts</files>
  <read_first>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md</file>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-CONTEXT.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-VALIDATION.md</file>
    <file>tests/integration/macro-tool-dispatch.test.ts</file>
    <file>tests/config/vitest.integration.config.ts</file>
    <file>tests/integration/macro-parse-error.test.ts</file>
    <file>src/mcp/tools/macro.ts</file>
    <file>src/macro/registry.ts</file>
  </read_first>
  <action>Make `tests/integration/macro-tool-dispatch.test.ts` green against real registered native handlers. The `fq.write_document` macro must create a document row and lifecycle state through the normal handler path; the `fq.search` macro must return the canonical search result shape. Ensure native handler arguments/results are adapted through the same response-envelope parsing already used by `evalToolCall`, and preserve existing write-lock/response behavior. Confirm `tests/config/vitest.integration.config.ts` contains the macro integration file once.</action>
  <verify>
    <automated>npm run test:integration -- --reporter=verbose macro-tool-dispatch</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tests/integration/macro-tool-dispatch\\.test\\.ts" tests/config/vitest.integration.config.ts` finds exactly one include entry.
    - `rg -n "write_document|fqc_documents|search|call_macro" tests/integration/macro-tool-dispatch.test.ts` finds real dispatch assertions.
    - `npm run test:integration -- --reporter=verbose macro-tool-dispatch` exits 0 or skips only through existing `.env.test`/Supabase helper logic.
  </acceptance_criteria>
  <done>Real `fq.write_document` and `fq.search` macro dispatch works through the registry/dispatcher path and integration coverage is registered.</done>
</task>

<task type="auto">
  <name>Task 3: Run required Phase 135 verification gates</name>
  <files>.planning/phases/135-tool-registry-dispatch-permissions/135-VALIDATION.md</files>
  <read_first>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md</file>
    <file>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md</file>
    <file>.planning/phases/135-tool-registry-dispatch-permissions/135-VALIDATION.md</file>
    <file>tests/config/vitest.integration.config.ts</file>
  </read_first>
  <action>Run and record the exact required verification commands from Requirements §8.8 and the phase validation strategy. Update `135-VALIDATION.md` with command, exit status, and concise outcome. Do not substitute watch mode, broad commands only, or renamed filters.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-registry macro-permission-prescan macro-dispatcher</automated>
    <automated>npm run test:integration -- --reporter=verbose macro-tool-dispatch</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --reporter=verbose macro-registry macro-permission-prescan macro-dispatcher` exits 0.
    - `npm run test:integration -- --reporter=verbose macro-tool-dispatch` exits 0 or skips only through existing `.env.test`/Supabase helper logic.
    - `rg -n "npm test -- --reporter=verbose macro-registry macro-permission-prescan macro-dispatcher|npm run test:integration -- --reporter=verbose macro-tool-dispatch" .planning/phases/135-tool-registry-dispatch-permissions/135-VALIDATION.md` finds both exact commands.
  </acceptance_criteria>
  <done>Phase 135 required verification commands are green/legitimately skipped and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public MCP params -> internal caller context | User input crosses into `call_macro`; identity must remain internal. |
| macro handler -> native catalog handlers | Public macro execution invokes privileged FlashQuery tools. |
| integration tests -> Supabase/vault | Tests mutate real test storage through normal handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-15 | Elevation of Privilege | `src/mcp/tools/macro.ts` | mitigate | Do not add `callerKind` to request schema; derive host caller context internally. |
| T-135-16 | Elevation of Privilege | delegated macro helper | mitigate | Internal helper accepts purpose context only from agentic loop code, not MCP params; delegated `fq.call_model` remains excluded. |
| T-135-17 | Tampering | real `fq.write_document` dispatch | mitigate | Use existing native handler path and tool-layer write locks; integration proves normal document lifecycle. |
| T-135-18 | Elevation of Privilege | direct dispatcher bypass | mitigate | Required unit gate includes dispatcher backstop and pre-scan tests before integration signoff. |
| T-135-19 | Elevation of Privilege | template masquerade confused deputy | mitigate | Required unit gate includes `template_masquerade_tools_not_callable_from_macro`. |
</threat_model>

<verification>
<automated>npm test -- --reporter=verbose macro-registry macro-permission-prescan macro-dispatcher</automated>
<automated>npm run test:integration -- --reporter=verbose macro-tool-dispatch</automated>
</verification>

<success_criteria>
- `call_macro` has no user-controlled `callerKind`.
- Host and delegated caller contexts derive allowlists through existing registry machinery.
- Real `fq.write_document` and `fq.search` macro dispatch integration tests pass through native handlers.
- Both required Phase 135 verification commands are recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/135-tool-registry-dispatch-permissions/135-04-SUMMARY.md`.
</output>
