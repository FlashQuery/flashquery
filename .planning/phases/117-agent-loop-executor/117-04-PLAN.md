---
phase: 117-agent-loop-executor
plan: 04
type: execute
wave: 3
depends_on: [117-02, 117-03]
files_modified:
  - src/mcp/tools/llm.ts
  - tests/unit/llm-tool.test.ts
  - tests/e2e/call-model-agent-loop.e2e.test.ts
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-03, LOOP-04, LOOP-05, LOOP-06, LOOP-07, TOOL-05, TOOL-06, VAL-117]
must_haves:
  truths:
    - "A purpose with a non-empty final native registry routes through Mode 2 and returns final assistant text after internal tool execution."
    - "Discovery resolvers and direct model calls do not run the loop."
    - "`return_messages` remains orthogonal: default Mode 2 returns `messages: []`; true returns hydrated host messages plus assistant/tool loop history."
  artifacts:
    - path: "src/mcp/tools/llm.ts"
      provides: "Mode 2 selection, executor invocation, and envelope integration"
      contains: "executeAgentLoop"
    - path: "tests/unit/llm-tool.test.ts"
      provides: "MCP handler unit coverage for Mode 2 routing and metadata"
      contains: "metadata.tools.stop_reason"
    - path: "tests/e2e/call-model-agent-loop.e2e.test.ts"
      provides: "MCP-bound loop behavior"
      contains: "ATL-E2E-02"
  key_links:
    - from: "src/mcp/tools/llm.ts"
      to: "src/llm/agent-loop.ts"
      via: "purpose non-empty registry invokes executeAgentLoop"
      pattern: "executeAgentLoop"
    - from: "src/mcp/tools/llm.ts"
      to: "src/llm/tool-registry.ts"
      via: "final assembled registry controls Mode 2"
      pattern: "nativeToolNames\\.length"
---

<objective>
Wire Mode 2 loop execution into the public `call_model` MCP tool.

Purpose: Phase 116 exposed provider tool schemas; this plan turns model tool calls into a completed internal FlashQuery loop while preserving Mode 1 and discovery behavior.
Output: `call_model` purpose calls with non-empty final native registry execute `executeAgentLoop()` and return complete Mode 2 metadata.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/117-agent-loop-executor/117-CONTEXT.md
@.planning/phases/117-agent-loop-executor/117-RESEARCH.md
@.planning/phases/117-agent-loop-executor/117-VALIDATION.md
@.planning/phases/117-agent-loop-executor/117-PATTERNS.md
@.planning/phases/116-model-visible-tool-registry/116-PATTERNS.md
@.planning/phases/116-model-visible-tool-registry/116-VERIFICATION.md
@.planning/phases/117-agent-loop-executor/117-agent-loop-executor-02-SUMMARY.md
@.planning/phases/117-agent-loop-executor/117-agent-loop-executor-03-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md

<interfaces>
Current `call_model` seams:
```typescript
export function registerLlmTools(server: McpServer, config: FlashQueryConfig): void
function toPublicToolDiagnostics(diagnostics: ToolRegistryDiagnostics): PublicToolDiagnostics
function mergeProviderTools(base: Record<string, unknown>, providerTools: OpenAiToolDefinition[]): Record<string, unknown>
```
Mode 2 selection rule:
```typescript
resolvedResolver === 'purpose' && toolRegistry?.nativeToolNames.length > 0
```
Discovery resolvers `list_models`, `list_purposes`, and `search` return before reference hydration and must stay outside Mode 2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route purpose calls with non-empty registry to the agent loop</name>
  <files>src/mcp/tools/llm.ts, tests/unit/llm-tool.test.ts</files>
  <read_first>
    - src/mcp/tools/llm.ts
    - src/llm/agent-loop.ts
    - src/llm/tool-dispatcher.ts
    - src/llm/tool-registry.ts
    - src/mcp/tool-catalog.ts
    - src/llm/client.ts
    - tests/unit/llm-tool.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
  </read_first>
  <behavior>
    - Non-empty final `toolRegistry.nativeToolNames` selects Mode 2.
    - Empty registry still uses existing Mode 1 `completeByPurpose()` path and omits `metadata.tools` unless diagnostics require existing Phase 116 exposure diagnostics.
    - Direct `resolver: "model"` and discovery resolvers never call `executeAgentLoop()`.
  </behavior>
  <action>In `src/mcp/tools/llm.ts`, after Phase 116 registry assembly and trace pre-snapshot, branch purpose calls with `toolRegistry?.nativeToolNames.length > 0` to `executeAgentLoop()`. Pass hydrated host messages, `purposeProviderParameters` containing provider tools, `nativeToolCatalog`, immutable `toolRegistry`, `config.instance.id`, `resolvedName`, `params.trace_id`, `client.chatByPurposeUnrecorded.bind(client)` or equivalent, and model cost lookup from `config.llm.models`. Remove the existing behavior that returns an assistant tool-call response as the durable `response` for provider-tool calls. Keep discovery short-circuit before hydration and before loop selection.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-agent-loop.test.ts tests/unit/llm-tool-dispatcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "executeAgentLoop" src/mcp/tools/llm.ts tests/unit/llm-tool.test.ts` finds route and tests.
    - `rg "chatByPurpose\\(" src/mcp/tools/llm.ts` does not show Mode 2 loop iteration usage; Mode 2 uses `chatByPurposeUnrecorded`.
    - Unit tests assert direct model resolver and discovery resolvers do not invoke `executeAgentLoop()`.
  </acceptance_criteria>
  <done>Purpose calls with an exposed native tool registry execute Mode 2, while Mode 1 and discovery behavior stay compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Mode 2 response envelope and returned message behavior</name>
  <files>src/mcp/tools/llm.ts, tests/unit/llm-tool.test.ts, tests/e2e/call-model-agent-loop.e2e.test.ts</files>
  <read_first>
    - src/mcp/tools/llm.ts
    - src/llm/types.ts
    - src/llm/agent-loop.ts
    - tests/unit/llm-tool.test.ts
    - tests/e2e/call-model-agent-loop.e2e.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
  </read_first>
  <behavior>
    - Durable envelope `response` is final assistant text, not a tool-call placeholder.
    - `metadata.tools` is present for Mode 2 and includes all required fields with no optional keys inside the tools object.
    - Default Mode 2 `messages` is `[]`; `return_messages: true` returns host hydrated messages plus assistant/tool loop messages and final assistant message.
  </behavior>
  <action>Map `executeAgentLoop()` output into `CallModelEnvelope`. Top-level metadata must preserve existing fields: `resolver`, `name`, `resolved_model_name`, `provider_name`, `fallback_position`, `tokens`, `cost_usd`, `latency_ms`, optional `trace_id`, optional `trace_cumulative`, and optional reference metadata. For Mode 2, set `metadata.tools` to include `native_tool_names`, public snake_case diagnostics from `toPublicToolDiagnostics()`, `stop_reason`, `iterations`, `calls_log`, and `aggregate_usage`. Build `trace_cumulative` from the pre-snapshot plus aggregate Mode 2 totals, not from per-iteration rows. Populate `messages` only when `params.return_messages === true`; include `buildReturnedMessages(hydratedMessages)` followed by loop assistant/tool history. Tool messages must not gain `name`.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts && npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "stop_reason|calls_log|aggregate_usage|return_messages" src/mcp/tools/llm.ts tests/unit/llm-tool.test.ts tests/e2e/call-model-agent-loop.e2e.test.ts` finds envelope assertions.
    - Unit/E2E tests assert Mode 2 with `return_messages` omitted returns `messages: []`.
    - Unit/E2E tests assert Mode 2 with `return_messages: true` returns at least one assistant tool-call message and one `role: "tool"` message with `tool_call_id`.
  </acceptance_criteria>
  <done>`call_model` returns complete Mode 2 envelopes with final response text, correct message population rules, trace totals, and public loop metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `call_model` | Host-authored messages, params, trace IDs, and purpose names enter FlashQuery. |
| delegated model -> loop | Provider assistant messages drive internal tool dispatch. |
| loop metadata -> MCP client | Usage and calls-log observability leaves FlashQuery in response envelope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Elevation of Privilege | Mode 2 branch in `llm.ts` | mitigate | Select Mode 2 only from final assembled registry; hard-excluded recursive/admin tools remain unavailable. |
| T-117-02 | Tampering | `params.parameters` and tool args | mitigate | Preserve existing Zod request validation and dispatcher argument validation; do not merge caller-provided Mode 3 tools. |
| T-117-03 | Information Disclosure | reference hydration boundary | mitigate | Keep host-only hydration before executor; do not hydrate loop messages or model arguments. |
| T-117-04 | Denial of Service | public `call_model` loop | mitigate | Executor guardrails from Plan 03 control all Mode 2 calls before the next provider request. |
| T-117-05 | Repudiation/Tampering | `trace_cumulative` and usage metadata | mitigate | Compute trace totals from pre-snapshot plus aggregate loop totals; never depend on per-iteration usage rows. |
</threat_model>

<verification>
Run `npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-agent-loop.test.ts tests/unit/llm-tool-dispatcher.test.ts` and `npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts`.
</verification>

<success_criteria>
Public `call_model` Mode 2 executes native tool loops internally, preserves Mode 1/discovery behavior, returns final assistant text, and exposes complete metadata without duplicate usage accounting.
</success_criteria>

<output>
After completion, create `.planning/phases/117-agent-loop-executor/117-agent-loop-executor-04-SUMMARY.md`
</output>
