---
phase: 137-trace-progress-dry-run-budgets
plan: 04
type: execute
wave: 3
depends_on:
  - 137-03
files_modified:
  - src/macro/dry-run.ts
  - src/macro/budget.ts
  - src/macro/evaluator.ts
  - src/mcp/tools/macro.ts
  - src/config/loader.ts
  - tests/unit/config.test.ts
autonomous: true
requirements:
  - MACRO-RESP-05
  - MACRO-INT-04
  - MACRO-OBS-02
  - MACRO-OBS-03
must_haves:
  truths:
    - "Dry-run runs parse and all static pre-flight checks without task registration or execution side effects."
    - "Budget limits halt macro execution with the canonical `timeout` or `budget_exceeded` envelopes."
    - "Omitted `budget.timeout_ms` uses server config `macro.default_timeout_ms`, exposed as `config.macro.defaultTimeoutMs`, defaulting to 60000 ms."
    - "Budget, dry-run, trace, and progress state remain per invocation."
  artifacts:
    - path: "src/macro/dry-run.ts"
      provides: "MacroDryRunResult builder and full pre-flight pipeline"
    - path: "src/macro/budget.ts"
      provides: "BudgetTracker for token, model-call, external-tool-call, and timeout caps"
    - path: "src/mcp/tools/macro.ts"
      provides: "Dry-run branch before task registry creation and handler option parsing"
    - path: "src/config/loader.ts"
      provides: "YAML config key `macro.default_timeout_ms` mapped to `config.macro.defaultTimeoutMs` default 60000"
    - path: "src/macro/evaluator.ts"
      provides: "Budget checks at execution boundaries"
  key_links:
    - from: "src/mcp/tools/macro.ts"
      to: "src/macro/dry-run.ts"
      via: "dry_run branch before taskRegistry.create"
      pattern: "dry_run|runDryRun"
    - from: "src/macro/evaluator.ts"
      to: "src/macro/budget.ts"
      via: "BudgetTracker boundary checks"
      pattern: "beforeModelCall|beforeExternalToolCall|afterModelCall|checkTimeout"
    - from: "src/config/loader.ts"
      to: "src/mcp/tools/macro.ts"
      via: "config.macro.defaultTimeoutMs used when budget.timeout_ms is omitted"
      pattern: "default_timeout_ms|defaultTimeoutMs|60000"
---

<objective>
Implement dry-run and runtime budget enforcement.

Purpose: REQ-053 and REQ-060 require execution controls that prevent side effects in dry-run and halt execution at precise budget boundaries.
Output: Dry-run helper, BudgetTracker, handler branching, and evaluator budget checks.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-CONTEXT.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-RESEARCH.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
@.planning/phases/136-task-lifecycle-and-cancellation/136-04-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md

<interfaces>
From src/macro/preflight.ts:
```typescript
export function collectInputVarContract(program: Program): InputVarContract;
export function validateInputVars(contract: InputVarContract, inputVars: Record<string, MacroValue>): void;
```

From src/macro/permission-prescan.ts:
```typescript
export function collectToolReferences(program: Program): ToolReference[];
export function preScanToolReferences(options: PreScanToolReferencesOptions): ToolResult | undefined;
```

From src/mcp/tools/macro.ts:
```typescript
export interface RunMacroSourceOptions {
  source: string;
  input_vars?: Record<string, MacroValue>;
  budget?: Record<string, unknown>;
  dry_run?: boolean;
  trace?: 'full' | 'summary' | 'none';
  progress?: 'full' | 'milestones' | 'silent';
}
```

From src/config/loader.ts:
```typescript
export interface FlashQueryConfig {
  // Add: macro: { defaultTimeoutMs: number }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dry-run pre-flight pipeline</name>
  <files>src/macro/dry-run.ts, src/mcp/tools/macro.ts</files>
  <read_first>
    - src/mcp/tools/macro.ts
    - src/macro/preflight.ts
    - src/macro/permission-prescan.ts
    - src/macro/forbidden-flag-scan.ts
    - src/macro/evaluator.ts
    - src/macro/task-registry.ts
    - tests/unit/macro-envelopes.test.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Create `src/macro/dry-run.ts` exporting `runDryRun` that accepts the parsed `Program`, coerced `inputVars`, registry, allowlist, generated task ID, and warnings. It must run `preScanForbiddenShellFlags`, `preflightProgram` or the existing exported equivalent, `collectInputVarContract`, `validateInputVars`, and `preScanToolReferences` before returning success. On success, return `macroResult(withWarnings({ task_id, parsed_ok: true, input_var_contract: { required, optional: [{ key, default }] }, tool_references, server_references }, warnings))`. Sort `tool_references` alphabetically as `server.tool` strings and deduplicate/sort `server_references`. In `runMacroSource`, branch when `dry_run === true` after parse and registry/pre-scan setup but before `taskRegistry.create` and before `evaluateProgram`/`execBlock`.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-envelopes macro-handler macro-permission-prescan macro-forbidden-flags</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "runDryRun|parsed_ok|input_var_contract|tool_references|server_references" src/macro/dry-run.ts src/mcp/tools/macro.ts` finds dry-run implementation and handler branch.
    - `rg -n "taskRegistry\\.create" src/mcp/tools/macro.ts` shows the dry-run branch occurs before task creation.
    - T-U-200 through T-U-204 pass, including missing input and no task registration.
  </acceptance_criteria>
  <done>Dry-run produces the canonical discriminated payload after full pre-flight and without side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BudgetTracker and evaluator boundary checks</name>
  <files>src/macro/budget.ts, src/macro/evaluator.ts</files>
  <read_first>
    - src/macro/evaluator.ts
    - src/macro/budget.ts
    - tests/unit/macro-budget.test.ts
    - tests/unit/macro-cancellation.test.ts
    - tests/unit/macro-isolation.test.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-RESEARCH.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Create `src/macro/budget.ts` exporting `MacroBudgetLimits`, `BudgetTracker`, and typed helpers for canonical envelopes. `BudgetTracker` must accept `max_total_tokens`, `max_model_calls`, `max_external_tool_calls`, and `timeout_ms`, with counters starting from the invocation's current budget. In `evalToolCall`, check timeout at safe points, call `beforeModelCall` before `fq.call_model`, call `beforeExternalToolCall` only before brokered/external tool dispatches, and call `afterModelCall` after `fq.call_model` returns to add token usage from the call result. Do not count every non-model dispatch toward `max_external_tool_calls`: REQ-060 scopes this cap to brokered/external tools only, so native `fq.*` non-model calls do not increment it. Extract `fq.call_model` tokens from current-call `metadata.tokens.input + metadata.tokens.output`; if `metadata.tokens` is absent, fallback to `metadata.trace_cumulative.total_tokens.input + metadata.trace_cumulative.total_tokens.output`. `max_model_calls` and `max_external_tool_calls` halt before dispatch with `{ error: "budget_exceeded", details: { which, limit, consumed } }`; `max_total_tokens` halts after the offending model call returns; `timeout_ms` halts at the next safe point with `{ error: "timeout", details: { timeout_ms, elapsed_ms } }`. Do not abort an in-flight handler mid-call.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-budget macro-cancellation macro-isolation</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BudgetTracker|budget_exceeded|timeout_ms|max_total_tokens|max_model_calls|max_external_tool_calls" src/macro/budget.ts src/macro/evaluator.ts` finds the tracker and all cap names.
    - `rg -n "beforeModelCall|afterModelCall|beforeExternalToolCall|checkTimeout|metadata\\.tokens|trace_cumulative|total_tokens|broker" src/macro/evaluator.ts src/macro/budget.ts` finds the required boundary checks, token extraction fallback, and broker-only external accounting.
    - T-U-211 through T-U-215 pass, and T-U-094 isolation labels remain present.
  </acceptance_criteria>
  <done>Budget caps halt at the specified boundaries and counters remain per invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parse handler options and preserve response envelopes</name>
  <files>src/mcp/tools/macro.ts, src/macro/evaluator.ts, src/config/loader.ts, tests/unit/config.test.ts, tests/unit/macro-budget.test.ts</files>
  <read_first>
    - src/mcp/tools/macro.ts
    - src/macro/evaluator.ts
    - src/macro/budget.ts
    - src/macro/dry-run.ts
    - src/config/loader.ts
    - tests/unit/config.test.ts
    - tests/unit/macro-handler.test.ts
    - tests/unit/macro-envelopes.test.ts
    - tests/unit/macro-budget.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Update `src/config/loader.ts` to add YAML key `macro.default_timeout_ms`, map it to `FlashQueryConfig.macro.defaultTimeoutMs`, and default it to `60000` ms when omitted. Add or extend `tests/unit/config.test.ts` to assert omitted `macro.default_timeout_ms` produces `config.macro.defaultTimeoutMs === 60000` and explicit YAML overrides are honored. Update `callMacroInputSchema` so `budget` is a Zod object with optional numeric `max_total_tokens`, `max_model_calls`, `max_external_tool_calls`, and `timeout_ms`, not an unconstrained record. Thread `dry_run`, `trace`, `progress`, and validated budget options through `runMacroSource` into dry-run or real-run paths. When `budget.timeout_ms` is omitted, pass `config.macro.defaultTimeoutMs` into `BudgetTracker`; add `tests/unit/macro-budget.test.ts` coverage proving omitted `budget.timeout_ms` uses the server-config default. Preserve defaults `trace: "summary"`, `progress: "milestones"`, and `dry_run: false`. Preserve existing source/source_ref exclusivity behavior and keep Phase 138 `source_ref` semantics untouched. Ensure expected dry-run, budget, and timeout envelopes return `isError: false` unless the existing runtime-error path requires `isError: true`.</action>
  <verify>
    <automated>npm test -- --reporter=verbose config macro-handler macro-envelopes macro-budget macro-trace macro-progress</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "default_timeout_ms|defaultTimeoutMs|60000|macro" src/config/loader.ts tests/unit/config.test.ts` finds config schema/default coverage.
    - `rg -n "max_total_tokens|max_model_calls|max_external_tool_calls|timeout_ms|dry_run|trace|progress|defaultTimeoutMs" src/mcp/tools/macro.ts tests/unit/macro-budget.test.ts` finds schema, option threading, and omitted-timeout default coverage.
    - `rg -n "z\\.object|z\\.number" src/mcp/tools/macro.ts` confirms budget validation uses Zod numeric fields.
    - Focused handler, dry-run, trace, progress, and budget tests pass together.
  </acceptance_criteria>
  <done>The public `call_macro` option surface drives dry-run, trace, progress, and budget behavior through typed handler wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP request params to budget/dry-run controls | Caller-provided budget, dry_run, trace, and progress values affect execution. |
| Dry-run pre-flight to side-effecting execution | Dry-run must stop before task registry creation and dispatch. |
| Tool/model dispatch to budget tracker | Untrusted tool results may report token usage used for budget accounting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04-01 | E | runMacroSource dry-run branch | mitigate | Branch before `taskRegistry.create` and before evaluator execution; T-U-202 proves no registered task. |
| T-137-04-02 | D | BudgetTracker timeout | mitigate | Check `timeout_ms` at safe points and after in-flight handlers complete. |
| T-137-04-03 | E | budget parser and config default | mitigate | Validate budget caps with Zod numeric fields, use `config.macro.defaultTimeoutMs` when `budget.timeout_ms` is omitted, and reject malformed external input before execution. |
| T-137-04-04 | T | token accounting | mitigate | Read model token usage after `fq.call_model` returns and halt immediately if `max_total_tokens` is exceeded. |
</threat_model>

<verification>
Run `npm test -- --reporter=verbose config macro-envelopes macro-budget macro-handler macro-trace macro-progress macro-cancellation macro-isolation macro-permission-prescan macro-forbidden-flags`.
</verification>

<success_criteria>
REQ-053 and REQ-060 pass focused tests: dry-run executes the full static pre-flight chain with no side effects, omitted `budget.timeout_ms` uses `config.macro.defaultTimeoutMs` defaulting to 60000, and all budget caps halt with canonical envelopes at the specified boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/137-trace-progress-dry-run-budgets/137-04-SUMMARY.md`
</output>
