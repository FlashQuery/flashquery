---
phase: 137-trace-progress-dry-run-budgets
plan: 02
type: execute
wave: 1
depends_on:
  - 137-01
files_modified:
  - src/macro/trace-builder.ts
  - src/macro/evaluator.ts
  - src/macro/builtins.ts
  - src/mcp/utils/response-formats.ts
autonomous: true
requirements:
  - MACRO-OBS-02
  - MACRO-RESP-05
must_haves:
  truths:
    - "Trace mode is applied when each trace step is written."
    - "`trace: \"none\"` omits the response `trace` field."
    - "Large trace values are capped at 2KB and surface `trace_value_truncated` in `warnings[]`."
  artifacts:
    - path: "src/macro/trace-builder.ts"
      provides: "Mode-aware TraceBuilder with truncation and warnings"
    - path: "src/macro/evaluator.ts"
      provides: "Evaluator integration with TraceBuilder and warnings"
    - path: "src/macro/builtins.ts"
      provides: "Echo/status trace calls routed through TraceBuilder"
  key_links:
    - from: "src/macro/evaluator.ts"
      to: "src/macro/trace-builder.ts"
      via: "MacroInvocationContext trace helper"
      pattern: "traceBuilder|TraceBuilder"
    - from: "src/macro/evaluator.ts"
      to: "src/mcp/utils/response-formats.ts"
      via: "withWarnings on success payload"
      pattern: "withWarnings\\("
---

<objective>
Implement mode-aware trace writing and warning propagation.

Purpose: REQ-047 requires trace filtering at write time, and REQ-056 requires degraded behavior to surface through the shared `warnings[]` convention.
Output: TraceBuilder, warning plumbing, and evaluator/builtin integration.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-CONTEXT.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-RESEARCH.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md

<interfaces>
From src/mcp/utils/response-formats.ts:
```typescript
export type WarningCode = ...;
export function withWarnings<T extends Record<string, unknown>>(payload: T, warnings: WarningCode[]): T & { warnings?: WarningCode[] };
export interface TraceStep { kind: 'tool_call' | 'model_call' | 'log' | 'progress' | 'fail' | 'exit'; ... }
```

From src/macro/evaluator.ts:
```typescript
export interface MacroInvocationContext {
  trace: TraceStep[];
  budget: MacroBudget;
  progress: MacroProgressEntry[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TraceBuilder and warning collection contracts</name>
  <files>src/macro/trace-builder.ts, src/mcp/utils/response-formats.ts</files>
  <read_first>
    - src/mcp/utils/response-formats.ts
    - src/macro/evaluator.ts
    - src/macro/builtins.ts
    - tests/unit/macro-trace.test.ts
    - tests/unit/macro-warnings.test.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Create `src/macro/trace-builder.ts` exporting `TraceMode = "full" | "summary" | "none"` and `TraceBuilder`. `TraceBuilder.add(step)` must add `at` at write time, return no retained step for `none`, omit `args` and `result` for `summary`, keep all fields for `full`, cap serialized `args` and `result` values over 2048 bytes to the sentinel `"<truncated: NNNN bytes>"`, and append `trace_value_truncated` once per invocation to a warnings collector. In `src/mcp/utils/response-formats.ts`, extend the canonical warning-code type/array to include `trace_value_truncated`, `progress_throttled`, and `broker_unavailable` if not already present, without changing existing response helper shapes.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-trace macro-warnings response-formats</automated>
  </verify>
  <acceptance_criteria>
    - `src/macro/trace-builder.ts` exports `TraceBuilder` and `TraceMode`.
    - `rg -n "2048|<truncated: .* bytes>|trace_value_truncated" src/macro/trace-builder.ts` finds cap, sentinel, and warning logic.
    - `rg -n "progress_throttled|broker_unavailable|trace_value_truncated" src/mcp/utils/response-formats.ts` finds all Phase 137 warning codes.
  </acceptance_criteria>
  <done>TraceBuilder implements write-time mode behavior and warning-code exports support Phase 137 warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire TraceBuilder through evaluator and builtins</name>
  <files>src/macro/evaluator.ts, src/macro/builtins.ts</files>
  <read_first>
    - src/macro/evaluator.ts
    - src/macro/builtins.ts
    - src/macro/trace-builder.ts
    - tests/unit/macro-trace.test.ts
    - tests/unit/macro-warnings.test.ts
    - tests/unit/macro-isolation.test.ts
    - .planning/phases/136-task-lifecycle-and-cancellation/136-04-SUMMARY.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Extend `EvaluateProgramOptions` with `trace?: "full" | "summary" | "none"` and preserve default `"summary"`. Add per-invocation `warnings: WarningCode[]` and a TraceBuilder-backed write path to `MacroInvocationContext`; keep state on the context object, not module-level. Replace the local `pushTrace` implementation and builtin direct trace writes so `tool_call`, `model_call`, `log`, `progress`, `fail`, and `exit` steps all route through TraceBuilder. Update `buildSuccessPayload` so it uses `withWarnings`, includes `trace` unless mode is `"none"`, and never returns an empty trace to represent deliberate omission.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-trace macro-warnings macro-builtins macro-isolation</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "trace:\\? .*full.*summary.*none|TraceBuilder|warnings" src/macro/evaluator.ts src/macro/builtins.ts` shows the new options and context wiring.
    - `rg -n "context\\.trace\\.push" src/macro/evaluator.ts src/macro/builtins.ts` returns no production direct trace pushes outside TraceBuilder internals.
    - `npm test -- --reporter=verbose macro-trace macro-warnings` passes.
  </acceptance_criteria>
  <done>All trace writes are mode-aware at write time and warnings propagate on successful macro payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Macro tool result to trace payload | Tool/model args and results may contain sensitive or large data. |
| Per-invocation context to shared module scope | Trace/warning state must not leak across invocations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-02-01 | I | TraceBuilder | mitigate | Apply `summary`/`none` while writing each step and omit retained args/results outside `full`. |
| T-137-02-02 | D | TraceBuilder | mitigate | Replace values over 2048 bytes with sentinel strings and record `trace_value_truncated`. |
| T-137-02-03 | I | MacroInvocationContext | mitigate | Store builder and warnings on the invocation context only; keep T-U-094 isolation tests green. |
</threat_model>

<verification>
Run `npm test -- --reporter=verbose macro-trace macro-warnings macro-builtins macro-isolation response-formats`.
</verification>

<success_criteria>
REQ-047 and REQ-056 pass their focused tests: trace modes are write-time, truncation uses the documented sentinel, `trace: "none"` omits the field, and warnings use the shared convention.
</success_criteria>

<output>
After completion, create `.planning/phases/137-trace-progress-dry-run-budgets/137-02-SUMMARY.md`
</output>
