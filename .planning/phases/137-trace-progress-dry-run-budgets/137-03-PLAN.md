---
phase: 137-trace-progress-dry-run-budgets
plan: 03
type: execute
wave: 2
depends_on:
  - 137-02
files_modified:
  - src/macro/progress-emitter.ts
  - src/macro/evaluator.ts
  - src/macro/builtins.ts
  - src/mcp/tools/macro.ts
  - src/macro/safe-points.ts
autonomous: true
requirements:
  - MACRO-OBS-03
  - MACRO-INT-07
must_haves:
  truths:
    - "Explicit `status` and automatic progress events use one mode-aware emitter."
    - "Progress notifications use the captured `_meta.progressToken` and are no-ops without it."
    - "Progress and token state remain per invocation and preserve T-U-094 isolation behavior."
    - "Auto-progress hooks preserve the Phase 136 `MACRO_SAFE_POINTS` cancellation boundary contract."
  artifacts:
    - path: "src/macro/progress-emitter.ts"
      provides: "ProgressEmitter with full, milestones, silent modes and 100ms throttle"
    - path: "src/mcp/tools/macro.ts"
      provides: "Handler extraction and threading of progressToken"
    - path: "src/macro/evaluator.ts"
      provides: "Auto-progress boundaries for loops, model calls, and tool calls"
    - path: "src/macro/safe-points.ts"
      provides: "Canonical cancellation boundary names that progress hooks must preserve"
  key_links:
    - from: "src/mcp/tools/macro.ts"
      to: "src/macro/evaluator.ts"
      via: "EvaluateProgramOptions.progressToken"
      pattern: "progressToken"
    - from: "src/macro/builtins.ts"
      to: "src/macro/progress-emitter.ts"
      via: "status builtin emission"
      pattern: "progressEmitter|emitProgress"
---

<objective>
Implement mode-aware progress emission and progress-token capture.

Purpose: REQ-048 and REQ-063 require a shared progress path for author-explicit and automatic progress, with no-token degradation and 100 ms throttling.
Output: ProgressEmitter, evaluator/builtin progress routing, and handler progressToken threading.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-CONTEXT.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-RESEARCH.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
@.planning/phases/136-task-lifecycle-and-cancellation/136-04-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md

<interfaces>
From src/mcp/tools/macro.ts:
```typescript
server.registerTool('call_macro', { inputSchema: callMacroInputSchema.shape }, async (params, extra) => { ... });
// extra: RequestHandlerExtra<ServerRequest, ServerNotification>
// progressToken source: extra._meta?.progressToken
// progress notification sink:
// extra.sendNotification({
//   method: 'notifications/progress',
//   params: { progressToken, progress, total?, message? },
// });
```

From src/macro/evaluator.ts:
```typescript
export interface MacroProgressEntry {
  message: string;
  progress?: number;
  total?: number;
  at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProgressEmitter with mode and throttle behavior</name>
  <files>src/macro/progress-emitter.ts, src/macro/evaluator.ts</files>
  <read_first>
    - src/macro/evaluator.ts
    - src/macro/builtins.ts
    - src/macro/safe-points.ts
    - src/macro/trace-builder.ts
    - tests/unit/macro-progress.test.ts
    - tests/unit/macro-isolation.test.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Create `src/macro/progress-emitter.ts` exporting `ProgressMode = "full" | "milestones" | "silent"` and `ProgressEmitter`. The emitter must accept `progressToken?: string | number`, a notification callback compatible with MCP `notifications/progress`, the per-invocation warnings array, and a clock. Implement `emitExplicitStatus`, `emitForLoopIteration`, `emitModelCallStart`, `emitModelCallFinish`, and `emitToolCallStart`. `full` emits all five categories, `milestones` emits explicit status and model-call start/finish only, `silent` emits none. Missing token emits none for every mode. Throttle emitted notifications to a minimum 100 ms interval and add `progress_throttled` once per invocation when an emission is suppressed by throttle. Read `src/macro/safe-points.ts` before integration and design emitter calls so they can be placed adjacent to, not instead of, the `MACRO_SAFE_POINTS` cancellation probes added by the Phase 136 gap fixes.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-progress macro-isolation</automated>
  </verify>
  <acceptance_criteria>
    - `src/macro/progress-emitter.ts` exports `ProgressEmitter` and `ProgressMode`.
    - `rg -n "100|progress_throttled|emitExplicitStatus|emitForLoopIteration|emitModelCallStart|emitModelCallFinish|emitToolCallStart" src/macro/progress-emitter.ts` finds throttle, warning, and emission methods.
    - T-U-194 through T-U-198 exist and pass for the emitter-level tests.
  </acceptance_criteria>
  <done>ProgressEmitter implements the canonical progress modes, no-token no-op behavior, and throttle warning behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route explicit status and auto-progress through the shared emitter</name>
  <files>src/macro/evaluator.ts, src/macro/builtins.ts</files>
  <read_first>
    - src/macro/evaluator.ts
    - src/macro/builtins.ts
    - src/macro/safe-points.ts
    - src/macro/progress-emitter.ts
    - tests/unit/macro-progress.test.ts
    - tests/unit/macro-builtins.test.ts
    - tests/unit/macro-cancellation.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Extend `EvaluateProgramOptions` and `MacroInvocationContext` with `progress?: "full" | "milestones" | "silent"`, `progressToken?: string | number`, and the ProgressEmitter instance. Default mode is `"milestones"`. Replace direct `context.progress.push` and `progressSink` calls in the `status` builtin with `emitExplicitStatus`. Add auto-progress calls at for-loop iteration start, model-call start, model-call finish, and tool-call start. Treat `server === "fq" && tool === "call_model"` as a model-call boundary; all other dispatched calls are tool-call boundaries. Route all progress trace steps through the same trace builder from Plan 02. Preserve the Phase 136 safe-point module contract: keep `context.checkCancelled(MACRO_SAFE_POINTS.forLoopIteration)`, `MACRO_SAFE_POINTS.whileLoopIteration`, and `MACRO_SAFE_POINTS.beforeToolCall(server, tool)` probes intact, and insert progress emission next to those probes without renaming or replacing the safe-point constants.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-progress macro-builtins macro-cancellation macro-isolation</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "emitForLoopIteration|emitModelCallStart|emitModelCallFinish|emitToolCallStart|emitExplicitStatus" src/macro/evaluator.ts src/macro/builtins.ts` finds all five shared-path calls.
    - `rg -n "MACRO_SAFE_POINTS\\.forLoopIteration|MACRO_SAFE_POINTS\\.whileLoopIteration|MACRO_SAFE_POINTS\\.beforeToolCall" src/macro/evaluator.ts` still finds the Phase 136 safe-point constants after progress integration.
    - `rg -n "context\\.progress\\.push|progressSink" src/macro/evaluator.ts src/macro/builtins.ts` shows no bypass for explicit status or automatic progress outside ProgressEmitter setup.
    - Existing cancellation safe-point tests still pass, proving progress additions did not skip safe points.
  </acceptance_criteria>
  <done>Explicit and automatic progress use the same mode-aware, token-aware path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Capture progressToken in the MCP handler</name>
  <files>src/mcp/tools/macro.ts</files>
  <read_first>
    - src/mcp/tools/macro.ts
    - src/macro/evaluator.ts
    - tests/unit/macro-handler.test.ts
    - tests/unit/macro-progress.test.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-RESEARCH.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>In `registerMacroTools`, type the handler's second argument as the MCP SDK `extra: RequestHandlerExtra<ServerRequest, ServerNotification>` where practical, or inspect the installed SDK types before implementation if local generics differ. Extract the MCP request progress token exactly from `extra._meta?.progressToken` while tolerating absent metadata. Thread the token into `runMacroSource` and then `evaluateProgram`. Add a notification callback that invokes `extra.sendNotification({ method: "notifications/progress", params: { progressToken, progress, ...(total === undefined ? {} : { total }), ...(message === undefined ? {} : { message }) } })`. Do not use the older `notify` wording unless the local SDK exposes it as an alias; the planned SDK path is `sendNotification`. Do not throw when `sendNotification` is absent or token is missing; missing token must be a no-op. Preserve the MCP handler error convention: catch runtime failures and return text-content ToolResults with `isError: true` only for unexpected runtime failures.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-handler macro-progress</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "_meta\\?\\.progressToken|progressToken|notifications/progress|sendNotification|RequestHandlerExtra|ServerNotification" src/mcp/tools/macro.ts` shows exact SDK extraction and notification wiring.
    - T-U-233 and T-U-234 pass.
    - `callMacroInputSchema` still accepts `progress: z.enum(['full', 'milestones', 'silent']).optional()`.
  </acceptance_criteria>
  <done>`call_macro` captures request progress tokens and progress emission degrades cleanly without a token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP request metadata to engine context | `_meta.progressToken` is caller-provided metadata. |
| Engine progress events to MCP notification channel | Live notifications may reveal execution milestones. |
| Per-invocation progress state to shared process | Concurrent sessions must not share progress state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-03-01 | I | ProgressEmitter | mitigate | Honor `silent` and missing-token no-op for explicit status and automatic events. |
| T-137-03-02 | I | registerMacroTools | mitigate | Capture progressToken per request and never store it in module-level state. |
| T-137-03-03 | D | ProgressEmitter | mitigate | Apply 100 ms throttle and surface `progress_throttled` warning on success payloads. |
| T-137-03-04 | I | auto-progress boundaries | mitigate | Route all auto-progress through the same mode checks as explicit `status`. |
</threat_model>

<verification>
Run `npm test -- --reporter=verbose macro-progress macro-handler macro-builtins macro-cancellation macro-isolation`.
</verification>

<success_criteria>
REQ-048 and REQ-063 pass focused tests: progress modes are enforced for explicit and automatic emissions, no-token calls are no-ops, progressToken is captured per request, and throttling warnings propagate.
</success_criteria>

<output>
After completion, create `.planning/phases/137-trace-progress-dry-run-budgets/137-03-SUMMARY.md`
</output>
