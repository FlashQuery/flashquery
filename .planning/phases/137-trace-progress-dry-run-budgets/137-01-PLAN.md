---
phase: 137-trace-progress-dry-run-budgets
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/macro-trace.test.ts
  - tests/unit/macro-warnings.test.ts
  - tests/unit/macro-progress.test.ts
  - tests/unit/macro-handler.test.ts
  - tests/unit/macro-envelopes.test.ts
  - tests/unit/macro-budget.test.ts
autonomous: true
requirements:
  - MACRO-OBS-02
  - MACRO-OBS-03
  - MACRO-RESP-05
  - MACRO-INT-04
  - MACRO-INT-07
must_haves:
  truths:
    - "Unit tests pin trace modes full, summary, none, truncation, and write-time mode behavior before implementation changes."
    - "Unit tests pin progress modes full, milestones, silent, throttling, and no-token no-op behavior before implementation changes."
    - "Unit tests pin MacroExecutionResult shape, dry-run envelopes, warnings, budget halt envelopes, and progress-token handler threading before implementation changes."
  artifacts:
    - path: "tests/unit/macro-trace.test.ts"
      provides: "T-U-187, T-U-188, T-U-189, T-U-190, T-U-193 trace contract coverage"
    - path: "tests/unit/macro-progress.test.ts"
      provides: "T-U-194 through T-U-198 progress contract coverage"
    - path: "tests/unit/macro-envelopes.test.ts"
      provides: "T-U-199 MacroExecutionResult shape coverage and T-U-200 through T-U-204 dry-run envelope coverage"
    - path: "tests/unit/macro-budget.test.ts"
      provides: "T-U-211 through T-U-215 budget coverage"
    - path: "tests/unit/macro-handler.test.ts"
      provides: "T-U-233 and T-U-234 progress-token capture coverage"
  key_links:
    - from: "tests/unit/macro-trace.test.ts"
      to: "src/macro/trace-builder.ts"
      via: "TraceBuilder public contract"
      pattern: "TraceBuilder|trace: \"full\"|trace: \"summary\"|trace: \"none\""
    - from: "tests/unit/macro-progress.test.ts"
      to: "src/macro/progress-emitter.ts"
      via: "ProgressEmitter public contract"
      pattern: "progress: \"full\"|progress: \"milestones\"|progress: \"silent\""
    - from: "tests/unit/macro-budget.test.ts"
      to: "src/macro/budget.ts"
      via: "BudgetTracker public contract"
      pattern: "max_total_tokens|max_model_calls|max_external_tool_calls|timeout_ms"
---

<objective>
Create the failing contract tests for Phase 137 before implementation.

Purpose: Phase 137 changes cross-cutting execution behavior, so the tests must pin the canonical Macro Language Test Plan rows before helper modules and handler wiring are changed.
Output: Unit coverage files that encode Spec Phase 8 requirements REQ-047, REQ-048, REQ-053, REQ-056, REQ-060, and REQ-063.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-CONTEXT.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-RESEARCH.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-VALIDATION.md
@.planning/phases/136-task-lifecycle-and-cancellation/136-04-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md

<interfaces>
From src/macro/evaluator.ts:
```typescript
export interface MacroInvocationContext {
  inputVars: Record<string, MacroValue>;
  trace: TraceStep[];
  log: string[];
  budget: MacroBudget;
  taskId: string;
  sessionId?: string;
  progress: MacroProgressEntry[];
  progressSink?: (entry: MacroProgressEntry, context: MacroInvocationContext) => void | Promise<void>;
  checkCancelled(atSafePoint: string): void | Promise<void>;
}

export function createInvocationContext(options?: EvaluateProgramOptions): MacroInvocationContext;
export async function evaluateProgram(program: Program, options?: EvaluateProgramOptions): Promise<ToolResult>;
```

From src/mcp/tools/macro.ts:
```typescript
export const callMacroInputSchema: z.ZodObject<...>;
export async function runMacroSource(options: RunMacroSourceOptions): Promise<RunMacroSourceResult>;
export interface RegisterMacroToolsResult {
  registrationSessionId: string;
}
export function registerMacroTools(server: McpServer, options: RegisterMacroToolsOptions): RegisterMacroToolsResult;
```

From src/mcp/utils/response-formats.ts:
```typescript
export interface TraceStep {
  kind: 'tool_call' | 'model_call' | 'log' | 'progress' | 'fail' | 'exit';
  name?: string;
  args?: unknown;
  result?: unknown;
  message?: string;
  at: string;
  elapsed_ms?: number;
}
export function withWarnings<T extends Record<string, unknown>>(payload: T, warnings: WarningCode[]): T & { warnings?: WarningCode[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trace and warning contract tests</name>
  <files>tests/unit/macro-trace.test.ts, tests/unit/macro-warnings.test.ts</files>
  <read_first>
    - tests/unit/macro-trace.test.ts
    - tests/unit/response-formats.test.ts
    - tests/unit/macro-introspection.test.ts
    - src/mcp/utils/response-formats.ts
    - src/macro/evaluator.ts
    - src/macro/builtins.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Add test rows T-U-187, T-U-188, T-U-189, T-U-190, and T-U-193 to `tests/unit/macro-trace.test.ts`. The tests must assert `trace: "full"` includes `args` and `result` on `tool_call` and `model_call`, `trace: "summary"` omits only `args` and `result`, `trace: "none"` makes `trace` absent rather than `[]`, values larger than 2048 bytes become exactly `"<truncated: NNNN bytes>"`, and trace mode is applied by the write helper before retaining full values. Add `tests/unit/macro-warnings.test.ts` rows T-U-209 and T-U-210 asserting `trace_value_truncated` and `broker_unavailable` warnings are emitted through `warnings[]` on successful macro payloads.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-trace macro-warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-187|T-U-188|T-U-189|T-U-190|T-U-193" tests/unit/macro-trace.test.ts` finds all five IDs.
    - `rg -n "T-U-209|T-U-210|trace_value_truncated|broker_unavailable" tests/unit/macro-warnings.test.ts` finds both IDs and both warning codes.
    - The focused test command is expected to fail before Plan 02 implementation and pass after Plan 02.
  </acceptance_criteria>
  <done>Trace and warning tests express the canonical Test Plan rows without changing production behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add progress mode and progress-token contract tests</name>
  <files>tests/unit/macro-progress.test.ts, tests/unit/macro-handler.test.ts</files>
  <read_first>
    - tests/unit/macro-builtins.test.ts
    - tests/unit/macro-cancellation.test.ts
    - tests/unit/macro-test-helpers.ts
    - src/macro/evaluator.ts
    - src/macro/builtins.ts
    - src/mcp/tools/macro.ts
    - tests/integration/macro-call-macro-session.test.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Create `tests/unit/macro-progress.test.ts` for T-U-194 through T-U-198. Assert `progress: "full"` emits explicit `status`, for-loop iteration start, model-call start/finish, and tool-call start notifications; `progress: "milestones"` emits explicit `status` and model-call start/finish only; `progress: "silent"` emits nothing including explicit `status`; rapid emissions are throttled to at least 100 ms and record `progress_throttled`; and absent progressToken is a no-op. Extend `tests/unit/macro-handler.test.ts` with T-U-233 and T-U-234 asserting `_meta.progressToken` is extracted from handler `extra` and passed to the engine, and that missing token suppresses notification attempts. Keep the Phase 136 fallback-session contract pinned by `tests/integration/macro-call-macro-session.test.ts`: `registerMacroTools` returns `RegisterMacroToolsResult` with a per-registration UUID `registrationSessionId`, and handler tests must not stub or refactor that return path away.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-progress macro-handler macro-builtins</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-194|T-U-195|T-U-196|T-U-197|T-U-198" tests/unit/macro-progress.test.ts` finds all five IDs.
    - `rg -n "T-U-233|T-U-234|progressToken|_meta" tests/unit/macro-handler.test.ts` finds both IDs and token extraction assertions.
    - `rg -n "RegisterMacroToolsResult|registrationSessionId|return \\{ registrationSessionId \\}" src/mcp/tools/macro.ts tests/unit/macro-handler.test.ts` confirms the handler still exposes the Phase 136 natural-session fallback return contract.
    - Test descriptions preserve REQ-039 ac3 wording for auto-progress at for-loop and model-call boundaries.
  </acceptance_criteria>
  <done>Progress and progress-token tests pin mode-aware emission through one path for explicit and automatic events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dry-run and budget contract tests</name>
  <files>tests/unit/macro-envelopes.test.ts, tests/unit/macro-budget.test.ts</files>
  <read_first>
    - tests/unit/macro-envelopes.test.ts
    - tests/unit/macro-cancellation.test.ts
    - tests/unit/macro-isolation.test.ts
    - src/mcp/tools/macro.ts
    - src/macro/evaluator.ts
    - src/macro/preflight.ts
    - src/macro/permission-prescan.ts
    - .planning/phases/137-trace-progress-dry-run-budgets/137-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Extend `tests/unit/macro-envelopes.test.ts` with T-U-199 through T-U-204. For T-U-199, assert real execution returns the `MacroExecutionResult` shape from the canonical Test Plan: `task_id` is a UUID, `result` is present, and `trace`, `token_total`, `model_calls`, `external_tool_calls`, and `warnings` are optional fields that appear only when the corresponding execution path produces them. For T-U-200 through T-U-204, assert dry-run returns `{ task_id, parsed_ok: true, input_var_contract, tool_references, server_references }`, omits a `dry_run` flag, generates but does not register a task, reports missing required `input_var` through the same `invalid_input` envelope as real execution, and sorts `tool_references` alphabetically with deduplicated `server_references`. Create `tests/unit/macro-budget.test.ts` with T-U-211 through T-U-215 for `timeout_ms`, `max_total_tokens`, `max_model_calls`, `max_external_tool_calls`, and per-invocation budget isolation. Include an in-flight timeout boundary test using the same mid-dispatch pattern as `tests/unit/macro-cancellation.test.ts` and assert the macro returns `timeout` after the handler completes.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-envelopes macro-budget macro-isolation macro-cancellation</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-199|T-U-200|T-U-201|T-U-202|T-U-203|T-U-204|MacroExecutionResult|task_id|token_total|model_calls|external_tool_calls|warnings" tests/unit/macro-envelopes.test.ts` finds T-U-199, all dry-run IDs, and result-shape assertions.
    - `rg -n "T-U-211|T-U-212|T-U-213|T-U-214|T-U-215|max_total_tokens|max_model_calls|max_external_tool_calls|timeout_ms" tests/unit/macro-budget.test.ts` finds all budget IDs and cap names.
    - Existing `T-U-094` labels in `tests/unit/macro-isolation.test.ts` remain unchanged.
  </acceptance_criteria>
  <done>Dry-run and budget tests encode the required envelopes, task-registry non-registration, budget caps, timeout boundary, and isolation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit tests to production contracts | Test fixtures encode expected behavior that later production changes must satisfy. |
| Handler metadata to progress emitter | Untrusted MCP request metadata may contain progress tokens and must not leak across tests or invocations. |
| Budget and trace fixtures to process memory | Tests must prove helpers do not keep full omitted payloads or module-level budget/progress state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01-01 | I | trace tests | mitigate | T-U-193 asserts omitted trace data is not retained in full under `summary` or `none`. |
| T-137-01-02 | I | progress-token tests | mitigate | T-U-233/T-U-234 assert token capture is per handler invocation and absent tokens produce no notifications. |
| T-137-01-03 | E | dry-run tests | mitigate | T-U-202 asserts dry-run does not register tasks or execute side-effecting dispatch paths. |
| T-137-01-04 | D | budget tests | mitigate | T-U-211 through T-U-215 assert timeout and budget caps halt deterministically. |
</threat_model>

<verification>
Run `npm test -- --reporter=verbose macro-trace macro-progress macro-envelopes macro-warnings macro-budget macro-handler macro-builtins macro-isolation macro-cancellation`. Before implementation, new tests may be red; after Plans 02 through 04, this command must pass.
</verification>

<success_criteria>
All required Test Plan row IDs for trace, progress, MacroExecutionResult shape, dry-run, warnings, budgets, and progress-token capture exist in unit tests and fail only because the corresponding production behavior is not implemented yet.
</success_criteria>

<output>
After completion, create `.planning/phases/137-trace-progress-dry-run-budgets/137-01-SUMMARY.md`
</output>
