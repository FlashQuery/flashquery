---
phase: 163-multi-file-batch-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/utils/batch-input.ts
  - src/mcp/utils/response-formats.ts
  - src/mcp/tools/documents/archive.ts
  - src/mcp/tools/documents/remove.ts
  - src/mcp/tools/compound.ts
  - tests/unit/batch-input-shape.test.ts
autonomous: true
requirements:
  - REQ-018
  - REQ-019
must_haves:
  truths:
    - "Batch-capable schemas accept a mixed array of bare identifiers and { identifier, version_token } objects."
    - "Unsupported positional version_tokens arrays and identifier-token maps are not added to any scoped tool schema."
    - "Batch item status wrappers can represent succeeded, conflicted, and failed entries without colliding with legacy per-tool success payload fields."
  artifacts:
    - path: "src/mcp/utils/batch-input.ts"
      provides: "Shared batch identifier schema, normalization, and per-item token association helpers"
      exports: ["batchIdentifierItemSchema", "batchIdentifiersSchema", "normalizeBatchIdentifiers"]
    - path: "src/mcp/utils/response-formats.ts"
      provides: "Shared BatchItemResult wrappers for REQ-018"
      exports: ["batchSucceeded", "batchConflicted", "batchFailed"]
    - path: "tests/unit/batch-input-shape.test.ts"
      provides: "T-U-026 and T-U-027 schema coverage"
  key_links:
    - from: "src/mcp/tools/documents/archive.ts"
      to: "src/mcp/utils/batch-input.ts"
      via: "archive_document identifiers schema"
      pattern: "batchIdentifiersSchema"
    - from: "src/mcp/tools/documents/remove.ts"
      to: "src/mcp/utils/batch-input.ts"
      via: "remove_document identifiers schema"
      pattern: "batchIdentifiersSchema"
    - from: "src/mcp/tools/compound.ts"
      to: "src/mcp/utils/batch-input.ts"
      via: "insert_doc_link and apply_tags document identifier schemas"
      pattern: "batchIdentifierItemSchema|batchIdentifiersSchema"
---

<objective>
Create the shared input and response contracts for Phase 163, then prove the public Zod schemas accept mixed batch items while rejecting unsupported positional token arrays.

Purpose: REQ-019 must be enforced at the MCP boundary before handlers rely on mixed objects, and REQ-018 needs a single result wrapper convention so success payload fields such as archive status cannot overwrite the unified item status.
Output: Shared helpers, widened schemas, and unit coverage for T-U-026 and T-U-027.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/163-multi-file-batch-contract/163-CONTEXT.md
@.planning/phases/163-multi-file-batch-contract/163-RESEARCH.md
@.planning/phases/163-multi-file-batch-contract/163-PATTERNS.md
@.planning/phases/163-multi-file-batch-contract/163-VALIDATION.md
@.planning/phases/162-version-fingerprint-check/162-VERIFICATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md

<read_first_note>
All downstream agents MUST read the two external Vault Write Coherency source-of-truth docs before asking questions or making changes. Prefer those docs over local summaries for REQ-018, REQ-019, and the test gate.
</read_first_note>

<interfaces>
From src/mcp/utils/document-version.ts:
- computeVersionToken(raw: string | Buffer): string
- pickExpectedVersion({ expected_version, if_match }): string | undefined
- buildVersionMismatchEnvelope({ identifier, versionToken, targetedRegion, message? }): ErrorEnvelope

From src/mcp/utils/response-formats.ts:
- ErrorEnvelope has error, message, identifier?, details?, targeted_region?, version_token?
- jsonToolResult(payload): ToolResult serializes JSON text without isError
- documentArchiveResult(input) currently returns status: "archived"
- documentRemovalResult(input) deletes version_token because the file is gone
- batchResult(results: unknown[]): unknown[] currently returns the array unchanged
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing mixed batch schema unit coverage</name>
  <files>tests/unit/batch-input-shape.test.ts</files>
  <read_first>
    @tests/unit/insert-doc-link.test.ts
    @tests/unit/archive-document.test.ts
    @src/mcp/tools/documents/archive.ts
    @src/mcp/tools/documents/remove.ts
    @src/mcp/tools/compound.ts
    @.planning/phases/163-multi-file-batch-contract/163-CONTEXT.md
    @.planning/phases/163-multi-file-batch-contract/163-PATTERNS.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
  </read_first>
  <behavior>
    - T-U-026: capture schemas for archive_document, remove_document, insert_doc_link, and apply_tags; each accepts a single string, string array, and mixed Array<string | { identifier, version_token }>.
    - T-U-027: each scoped schema rejects a top-level version_tokens field and no schema accepts identifier-to-token maps.
    - Memory targets in apply_tags remain valid with entity_type: "memory" and identifier only.
  </behavior>
  <action>Create tests using existing server.registerTool capture patterns. Assert safeParse success for mixed object arrays on archive_document.identifiers, remove_document.identifiers, insert_doc_link.identifiers, and apply_tags document target forms. Assert safeParse failure for version_tokens at the top level and for object entries missing identifier or version_token. Do not modify production code in this task.</action>
  <verify>
    <automated>npm test -- --grep "batch-input-shape"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tests include literal test IDs T-U-026 and T-U-027.
    - Behavior assertion: tests cover all four scoped tool surfaces from D-REQ-019: archive_document, remove_document, insert_doc_link, and apply_tags document targets.
    - Command assertion: npm test -- --grep "batch-input-shape" fails before Task 2 production changes and passes after Task 2.
  </acceptance_criteria>
  <done>Unit tests define the exact mixed input and rejection contract before schema implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared batch contracts and widen tool schemas</name>
  <files>src/mcp/utils/batch-input.ts, src/mcp/utils/response-formats.ts, src/mcp/tools/documents/archive.ts, src/mcp/tools/documents/remove.ts, src/mcp/tools/compound.ts, tests/unit/batch-input-shape.test.ts</files>
  <read_first>
    @src/mcp/utils/response-formats.ts
    @src/mcp/utils/document-version.ts
    @src/mcp/tools/documents/archive.ts
    @src/mcp/tools/documents/remove.ts
    @src/mcp/tools/compound.ts
    @tests/unit/batch-input-shape.test.ts
    @.planning/phases/163-multi-file-batch-contract/163-RESEARCH.md
    @.planning/phases/163-multi-file-batch-contract/163-VALIDATION.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
  </read_first>
  <action>Create src/mcp/utils/batch-input.ts with a strict Zod object branch for { identifier: string, version_token: string }, a union item schema, a string-or-array identifiers schema, and normalizeBatchIdentifiers returning ordered items shaped { identifier: string, version_token?: string, index: number }. Import these schemas into archive_document, remove_document, and insert_doc_link identifiers. For apply_tags, widen only document-target forms: identifiers accepts the same mixed array, and targets accepts document targets with version_token while preserving memory targets unchanged. Add BatchItemResult types and helper functions in response-formats.ts: batchSucceeded(identifier, data), batchConflicted(identifier, envelope), and batchFailed(identifier, error). The helpers must produce item.status exactly "succeeded", "conflicted", or "failed"; if data has its own status field, keep that legacy payload under data so item.status remains the REQ-018 status. Do not add version_tokens or identifier-token maps.</action>
  <verify>
    <automated>npm test -- --grep "batch-input-shape"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/mcp/utils/batch-input.ts exports batchIdentifierItemSchema, batchIdentifiersSchema, and normalizeBatchIdentifiers.
    - Source assertion: no non-comment source line in touched files contains a schema property named version_tokens.
    - Behavior assertion: single-string schemas still accept existing callers exactly; string arrays still parse; mixed arrays parse.
    - Behavior assertion: apply_tags memory target objects do not require or consume version_token.
    - Command assertion: npm test -- --grep "batch-input-shape" exits 0.
  </acceptance_criteria>
  <done>REQ-019 schema/input contract and REQ-018 shared status wrappers exist and are covered by T-U-026/T-U-027.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller -> Zod schemas | Untrusted tool params enter archive_document, remove_document, insert_doc_link, and apply_tags. |
| Helper contract -> handlers | Normalized identifiers carry token association that later write handlers must preserve. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Tampering | batch input schemas | mitigate | Use z.strictObject for mixed item objects; reject malformed objects and do not add version_tokens. |
| T-163-02 | Tampering | normalizeBatchIdentifiers | mitigate | Keep identifier and version_token co-located in each normalized item with the original index. |
| T-163-04 | Repudiation | BatchItemResult helpers | mitigate | Ensure every item exposes a top-level status of succeeded, conflicted, or failed so partial failures cannot be hidden. |
| T-163-SC | Tampering | npm installs | accept | No package installs are planned for this phase. |
</threat_model>

<verification>
Run npm test -- --grep "batch-input-shape" and npm run typecheck. Confirm grep -R "version_tokens" over non-comment lines in src/mcp/tools and src/mcp/utils does not find a newly accepted schema property.
</verification>

<success_criteria>
T-U-026 and T-U-027 are implemented and green; all scoped tool schemas parse the mixed input shape; shared response wrappers preserve the unified REQ-018 status field.
</success_criteria>

<output>
Create .planning/phases/163-multi-file-batch-contract/163-01-SUMMARY.md when done.
</output>
