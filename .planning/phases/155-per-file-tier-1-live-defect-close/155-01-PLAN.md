---
phase: 155-per-file-tier-1-live-defect-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/document-lock.ts
  - src/services/scanner.ts
  - tests/unit/document-lock-registry.test.ts
  - tests/unit/with-document-lock.test.ts
  - tests/unit/lock-helper-only.test.ts
autonomous: true
requirements:
  - REQ-001
  - REQ-009
requirements_addressed:
  - REQ-001
  - REQ-009
must_haves:
  truths:
    - "The Phase-155 document lock facade exists and is the only public API for document write locks."
    - "Same-process document locking is per-file and bounded by a 1024-stripe registry."
    - "Scanner frontmatter repair writes participate in the same per-file coordination."
    - "Legacy write-lock table usage is isolated as a temporary pass-through inside the facade."
  artifacts:
    - path: "src/services/document-lock.ts"
      provides: "withDocumentLock and withDocumentLocks facade for Phase-155 per-file Tier 1 locking"
      exports: ["withDocumentLock", "withDocumentLocks"]
    - path: "src/services/scanner.ts"
      provides: "repairFrontmatter write path wrapped through the document-lock facade"
    - path: "tests/unit/document-lock-registry.test.ts"
      provides: "T-U-001/T-U-002 and basic Phase-155 key scaffolding coverage"
    - path: "tests/unit/with-document-lock.test.ts"
      provides: "T-U-016/T-U-017/T-U-018 helper behavior coverage"
    - path: "tests/unit/lock-helper-only.test.ts"
      provides: "T-U-019 facade-only static guard"
  key_links:
    - from: "src/services/scanner.ts"
      to: "src/services/document-lock.ts"
      via: "withDocumentLock around repairFrontmatter writes"
      pattern: "withDocumentLock"
    - from: "src/services/document-lock.ts"
      to: "src/services/write-lock.ts"
      via: "temporary legacy Tier 2 pass-through only"
      pattern: "acquireLock|releaseLock"
---

<objective>
Create the Phase 155 document-lock facade and scanner repair integration.

Purpose: establish the single `withDocumentLock` / `withDocumentLocks` API with bounded same-process per-file locking while keeping the legacy table lock as the temporary cross-process pass-through for later replacement.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Downstream implementation and verification agents MUST read these product docs first before asking scope questions:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md

Then read:
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/155-per-file-tier-1-live-defect-close/155-RESEARCH.md
@.planning/phases/155-per-file-tier-1-live-defect-close/155-VALIDATION.md

Locked boundaries:
- Full REQ-003 realpath/case-folding canonical derivation is Phase 159. This plan adds only basic Phase-155 key scaffolding and tests that it is not raw vault-relative keying.
- Native Postgres advisory locks, `fqc_write_locks` retirement, and `flashquery unlock` removal are Phase 158. This plan calls legacy `src/services/write-lock.ts` only inside the new facade as a temporary pass-through.
- Durable atomic write consolidation is Phase 156.
</context>

<tasks>

<task id="155-01-01" type="auto" tdd="true">
  <name>Add document-lock facade unit coverage</name>
  <read_first>
    @src/services/write-lock.ts
    @src/config/types.ts
    @tests/unit/
  </read_first>
  <files>tests/unit/document-lock-registry.test.ts, tests/unit/with-document-lock.test.ts, tests/unit/lock-helper-only.test.ts</files>
  <action>Create failing tests for T-U-001, T-U-002, T-U-016, T-U-017, T-U-018, and T-U-019. Include an explicit Phase-155 basic key scaffolding check from Test Plan §4.1.3: helper input must be an absolute/resolved path or must be normalized only after existing path validation, and the test name must state that full REQ-003 canonical realpath/case-folding is deferred to Phase 159. Test sorted acquisition for `withDocumentLocks([pathB, pathA], fn)`, release on success and error, bounded 1024 stripe allocation, and no low-level primitive export outside `src/services/document-lock.ts`.</action>
  <verify>
    <automated>npm test -- --grep "document-lock|with-document-lock|lock-helper-only"</automated>
  </verify>
  <acceptance_criteria>The required unit/static test IDs exist, fail before implementation, and cover helper API, bounded stripes, sorted multi-lock order, release semantics, temporary pass-through error handling, and Phase-155 basic key scaffolding without claiming full REQ-003.</acceptance_criteria>
  <done>Unit/static tests exist for T-U-001, T-U-002, T-U-016, T-U-017, T-U-018, T-U-019, and Phase-155 basic key scaffolding.</done>
</task>

<task id="155-01-02" type="auto" tdd="true">
  <name>Implement document-lock facade</name>
  <read_first>
    @src/services/write-lock.ts
    @src/services/scanner.ts
    @src/storage/vault.ts
  </read_first>
  <files>src/services/document-lock.ts</files>
  <action>Create `src/services/document-lock.ts`. Export only `withDocumentLock(config, filePath, fn)` and `withDocumentLocks(config, filePaths, fn)`, plus public error types if needed by callers. Use installed `async-mutex` with 1024 bounded stripes for Tier 1. Derive a Phase-155 basic key from an already resolved absolute path and produce per-file legacy resource strings such as `document:${basicKey}`. When `config.locking.enabled` is true, call existing `acquireLock` / `releaseLock` from `src/services/write-lock.ts` inside this module only. Acquire Tier 1 first, then temporary legacy Tier 2; release temporary Tier 2 before Tier 1. Acquire multi-path locks in sorted basic-key order and release in reverse order.</action>
  <verify>
    <automated>npm test -- --grep "document-lock|with-document-lock|lock-helper-only"</automated>
  </verify>
  <acceptance_criteria>The helper facade passes unit/static tests, lower-level lock details do not leak to call sites, and legacy write-lock usage is isolated to `src/services/document-lock.ts` for Phase 155.</acceptance_criteria>
  <done>`src/services/document-lock.ts` provides the Phase-155 facade and passes helper tests.</done>
</task>

<task id="155-01-03" type="auto" tdd="false">
  <name>Route scanner frontmatter repair through the facade</name>
  <read_first>
    @src/services/scanner.ts
    @src/services/document-lock.ts
  </read_first>
  <files>src/services/scanner.ts</files>
  <action>Wrap the scanner `repairFrontmatter` vault write path with `withDocumentLock` so scanner-triggered per-document repairs coordinate with MCP document writes. Keep `scanMutex`, token-stability logic, and scanner scheduling unchanged. Do not consolidate durable write primitives or change unchanged-file no-write behavior.</action>
  <verify>
    <automated>npm test -- --grep "scanner|document-lock|with-document-lock"</automated>
  </verify>
  <acceptance_criteria>Scanner frontmatter repair is no longer a vault write path outside the Phase-155 facade, and existing scanner no-write invariants remain intact.</acceptance_criteria>
  <done>`repairFrontmatter` adopts `withDocumentLock` without broad scanner refactors.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-155-01 | Tampering | lock-key derivation | mitigate | Derive Phase-155 keys only after existing path validation/resolution; do not key directly on raw caller strings. |
| T-155-03 | Denial of Service | Tier 1 registry | mitigate | Use fixed 1024 stripes so memory does not scale with unique file count. |
| T-155-06 | Tampering | scanner repair writes | mitigate | Route `repairFrontmatter` through `withDocumentLock` while preserving existing scanner invariants. |
</threat_model>

<verification>
- `npm test -- --grep "document-lock|with-document-lock|lock-helper-only"`
- `npm test -- --grep "scanner|document-lock|with-document-lock"`
- `rg -n "from ['\\\"].*/services/write-lock|acquireLock|releaseLock" src/services src/mcp/tools | grep -v "src/services/write-lock.ts" | grep -v "src/services/document-lock.ts" && exit 1 || true`
</verification>

<success_criteria>
1. `withDocumentLock` and `withDocumentLocks` exist as the only exported document-lock facade.
2. Same-process Tier 1 locking is bounded and per-file keyed.
3. Temporary legacy Tier 2 is isolated inside the facade.
4. Scanner `repairFrontmatter` uses the facade without changing unrelated scanner behavior.
</success_criteria>

<output>
Create `.planning/phases/155-per-file-tier-1-live-defect-close/155-01-SUMMARY.md` when done.
</output>
