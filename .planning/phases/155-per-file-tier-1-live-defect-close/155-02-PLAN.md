---
phase: 155-per-file-tier-1-live-defect-close
plan: 02
type: execute
wave: 2
depends_on:
  - 155-01-PLAN.md
files_modified:
  - src/mcp/tools/documents/write.ts
  - src/mcp/tools/documents/archive.ts
  - src/mcp/tools/documents/remove.ts
  - src/mcp/tools/documents/copy.ts
  - src/mcp/tools/documents/move.ts
  - src/mcp/tools/compound.ts
  - src/mcp/tool-help/call_macro.tool.md
  - tests/unit/macro-no-lock-imports.test.ts
  - tests/integration/per-file-lock.test.ts
  - tests/integration/apply-tags-concurrent.integration.test.ts
  - tests/integration/insert-doc-link-race.integration.test.ts
  - tests/integration/call-macro-per-step-lock.integration.test.ts
autonomous: true
requirements:
  - REQ-001
  - REQ-009
  - REQ-010
  - REQ-025
requirements_addressed:
  - REQ-001
  - REQ-009
  - REQ-010
  - REQ-025
must_haves:
  truths:
    - "Document tools no longer acquire the shared coarse 'documents' lock directly."
    - "Concurrent same-document compound tag/link mutations no longer silently lose updates."
    - "Macro engine files remain free of document-lock imports/calls."
    - "call_macro help documents per-step locking and no macro-spanning atomicity."
  artifacts:
    - path: "src/mcp/tools/documents/write.ts"
      provides: "write_document create/update flows routed through withDocumentLock"
    - path: "src/mcp/tools/documents/archive.ts"
      provides: "archive_document per-item document locking"
    - path: "src/mcp/tools/documents/remove.ts"
      provides: "remove_document per-item document locking"
    - path: "src/mcp/tools/documents/copy.ts"
      provides: "copy_document Phase-155 destination/file lock migration"
    - path: "src/mcp/tools/documents/move.ts"
      provides: "move_document source/destination helper usage without Phase-161 expansion"
    - path: "src/mcp/tools/compound.ts"
      provides: "locked apply_tags, insert_doc_link, insert_in_doc, and replace_doc_section document mutations"
    - path: "src/mcp/tool-help/call_macro.tool.md"
      provides: "REQ-025 macro locking semantics documentation"
    - path: "tests/unit/macro-no-lock-imports.test.ts"
      provides: "T-U-038 static macro no-lock guard"
  key_links:
    - from: "src/mcp/tools/documents/write.ts"
      to: "src/services/document-lock.ts"
      via: "withDocumentLock around file mutation"
      pattern: "withDocumentLock"
    - from: "src/mcp/tools/compound.ts"
      to: "src/services/document-lock.ts"
      via: "withDocumentLock/withDocumentLocks around document read-modify-write"
      pattern: "withDocumentLock|withDocumentLocks"
    - from: "src/mcp/tool-help/call_macro.tool.md"
      to: "src/mcp/tools/macro.ts"
      via: "documentation only; no macro-level lock import/call"
      pattern: "call_macro"
---

<objective>
Migrate document and compound write call sites to the Phase-155 lock facade and close the live `apply_tags` / `insert_doc_link` lost-update defect.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Downstream implementation and verification agents MUST read these product docs first before asking scope questions:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md

Then read:
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/155-per-file-tier-1-live-defect-close/155-RESEARCH.md
@.planning/phases/155-per-file-tier-1-live-defect-close/155-VALIDATION.md
@.planning/phases/155-per-file-tier-1-live-defect-close/155-01-SUMMARY.md

Boundaries:
- Do not add lock imports or calls to `src/mcp/tools/macro.ts` or `src/macro/evaluator.ts`.
- Do not implement version tokens, durable atomic writes, native advisory locks, full folder locks, or full destination-lock semantics.
- `copy_document` and `move_document` receive only the call-site migration needed for same-process per-file behavior; Phase 161 owns full destination-path guarantees.
</context>

<tasks>

<task id="155-02-01" type="auto" tdd="true">
  <name>Add call-site migration tests and macro static guard</name>
  <read_first>
    @tests/integration/archive-document-lock.test.ts
    @tests/integration/write-document.integration.test.ts
    @tests/integration/macro-write-lock.integration.test.ts
    @src/mcp/tools/macro.ts
    @src/macro/evaluator.ts
  </read_first>
  <files>tests/unit/macro-no-lock-imports.test.ts, tests/integration/per-file-lock.test.ts, tests/integration/apply-tags-concurrent.integration.test.ts, tests/integration/insert-doc-link-race.integration.test.ts, tests/integration/call-macro-per-step-lock.integration.test.ts</files>
  <action>Create tests for T-U-038, T-I-001, T-I-002, T-I-017, T-I-018, T-I-049, T-I-050, and T-I-051. T-U-038 reads `src/mcp/tools/macro.ts` and `src/macro/evaluator.ts` as text and fails if they contain `withDocumentLock`, `withDocumentLocks`, `document-lock`, `write-lock`, `acquireLock`, or `releaseLock`. T-I-001/T-I-002 verify unrelated-file concurrency and same-file serialization through `write_document`. T-I-017 races two document `apply_tags` calls with disjoint tags and expects the union. T-I-018 races `insert_doc_link` with `write_document` and expects both changes to survive. T-I-049 verifies parallel `call_macro` invocations rely on per-step tool locks. T-I-050/T-I-051 must honestly reflect Phase 155 boundaries: if `version_token` is not present yet, make T-I-050 an explicit skipped boundary test naming Phase 162/REQ-012 rather than pretending Phase 155 completed it; T-I-051 can assert the current opt-in/no-token last-writer contract if supported by existing macro behavior.</action>
  <verify>
    <automated>npm test -- --grep "macro-no-lock"</automated>
    <automated>npm run test:integration -- --grep "per-file|apply-tags|insert-doc-link|call-macro-per-step"</automated>
  </verify>
  <acceptance_criteria>Required unit/integration test IDs exist, macro static guard fails on macro-level lock imports/calls, and later-phase version-token behavior is represented as a named boundary rather than completed scope.</acceptance_criteria>
  <done>Call-site migration tests and macro static guard are in place.</done>
</task>

<task id="155-02-02" type="auto" tdd="true">
  <name>Migrate document tools to withDocumentLock</name>
  <read_first>
    @src/services/document-lock.ts
    @src/mcp/tools/documents/write.ts
    @src/mcp/tools/documents/archive.ts
    @src/mcp/tools/documents/remove.ts
    @src/mcp/tools/documents/copy.ts
    @src/mcp/tools/documents/move.ts
  </read_first>
  <files>src/mcp/tools/documents/write.ts, src/mcp/tools/documents/archive.ts, src/mcp/tools/documents/remove.ts, src/mcp/tools/documents/copy.ts, src/mcp/tools/documents/move.ts</files>
  <action>Replace coarse `'documents'` locking in document tools with `withDocumentLock` or `withDocumentLocks`. For `write_document`, resolve/validate enough to identify the absolute path, enter the per-file lock, then recheck existence and re-read/re-resolve inside the lock before mutation. For `archive_document` and `remove_document`, lock per resolved item and preserve ordered batch envelopes and rollback behavior. For `copy_document`, lock the destination absolute path around destination existence check and write without claiming full Phase 161 destination semantics. For `move_document`, use `withDocumentLocks` for source and destination paths around the current move sequence while preserving existing EXDEV fallback behavior.</action>
  <verify>
    <automated>npm run test:integration -- --grep "per-file"</automated>
    <automated>rg -n "acquireLock\\([^\\n]*['\\\"]documents['\\\"]|releaseLock\\([^\\n]*['\\\"]documents['\\\"]" src/mcp/tools/documents src/mcp/tools/compound.ts && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>Document tools no longer use the shared `'documents'` lock directly; same-file writes serialize through the helper; unrelated-file writes do not contend on one Tier 1 mutex.</acceptance_criteria>
  <done>Document tools use the Phase-155 facade and pass per-file integration coverage.</done>
</task>

<task id="155-02-03" type="auto" tdd="true">
  <name>Migrate compound tools and document macro semantics</name>
  <read_first>
    @src/services/document-lock.ts
    @src/mcp/tools/compound.ts
    @src/mcp/tool-help/call_macro.tool.md
  </read_first>
  <files>src/mcp/tools/compound.ts, src/mcp/tool-help/call_macro.tool.md</files>
  <action>Wrap `insert_doc_link` source document mutations in per-file locks. Resolve enough to identify each source path, acquire locks in sorted order when multiple source documents are mutated, then re-resolve/re-read inside the lock before frontmatter mutation and write. Wrap `apply_tags` only for document targets; memory targets remain DB-only and outside document locking. Migrate `insert_in_doc` and `replace_doc_section` from coarse `'documents'` locking to `withDocumentLock` while preserving inside-lock re-read behavior. Update `call_macro` help text to document REQ-025: every macro step uses its called tool's per-file lock, `call_macro` is not atomic and holds no macro-spanning lock, explicit `version_token` threading is the future opt-in safety model, and auto-threading/atomic macro execution are deferred.</action>
  <verify>
    <automated>npm test -- --grep "macro-no-lock"</automated>
    <automated>npm run test:integration -- --grep "apply-tags|insert-doc-link|call-macro-per-step"</automated>
    <automated>rg -n "withDocumentLock|withDocumentLocks|document-lock|write-lock|acquireLock|releaseLock" src/mcp/tools/macro.ts src/macro/evaluator.ts && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>`apply_tags` and `insert_doc_link` no longer silently lose same-document updates; compound section tools use per-file locks; macro engine files remain lock-free and help text describes per-step locking.</acceptance_criteria>
  <done>Compound write paths use the Phase-155 facade and macro semantics are guarded/documented.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-155-02 | Tampering | document/compound writes | mitigate | Re-resolve or re-read inside `withDocumentLock` before read-modify-write. |
| T-155-04 | Elevation of Privilege | macro layer | mitigate | Static guard forbids lock imports/calls in macro engine files; macro steps use normal tool permissions. |
| T-155-05 | Information Disclosure | conflict responses | accept | Preserve existing response helpers and avoid adding secrets to error text. |
</threat_model>

<verification>
- `npm test -- --grep "macro-no-lock"`
- `npm run test:integration -- --grep "per-file|apply-tags|insert-doc-link|call-macro-per-step"`
- `rg -n "acquireLock\([^\n]*['\"]documents['\"]|releaseLock\([^\n]*['\"]documents['\"]" src/mcp/tools/documents src/mcp/tools/compound.ts && exit 1 || true`
- `rg -n "withDocumentLock|withDocumentLocks|document-lock|write-lock|acquireLock|releaseLock" src/mcp/tools/macro.ts src/macro/evaluator.ts && exit 1 || true`
</verification>

<success_criteria>
1. Two concurrent writes to different documents can both complete without a shared global document lock.
2. Two concurrent writes to the same document serialize so the later write observes the earlier write's bytes.
3. Concurrent `apply_tags` or `insert_doc_link` calls on one document never silently lose updates.
4. `call_macro` relies on called tools' per-file locks and exposes no macro-spanning lock behavior.
</success_criteria>

<output>
Create `.planning/phases/155-per-file-tier-1-live-defect-close/155-02-SUMMARY.md` when done.
</output>
