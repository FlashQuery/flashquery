---
phase: 164-close-gap-document-repair-and-plugin-reconciliation-lock-con
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/plugin-reconciliation.ts
  - src/utils/frontmatter.ts
  - tests/unit/plugin-reconciliation.test.ts
  - tests/unit/single-write-primitive.test.ts
  - tests/integration/atomic-write-frontmatter.integration.test.ts
  - tests/integration/records-reconciliation.integration.test.ts
autonomous: true
requirements: [REQ-007, REQ-009, REQ-020, REQ-023]
must_haves:
  truths:
    - "Plugin reconciliation frontmatter writes take document-path locks before atomicWriteFrontmatter reaches writeVaultFile."
    - "Plugin coordination locks remain in place for records reconciliation sequencing and are not replaced by document locks."
    - "Memory and concurrent plugin unregister Phase 157 behavior remains unchanged."
  artifacts:
    - path: "src/services/plugin-reconciliation.ts"
      provides: "Document-path lock wrapping for reconciliation frontmatter writes"
      contains: "atomicWriteFrontmatter"
    - path: "src/utils/frontmatter.ts"
      provides: "Primitive-only frontmatter merge/write helper"
      contains: "writeVaultFile"
    - path: "tests/integration/atomic-write-frontmatter.integration.test.ts"
      provides: "T-I-040 representative frontmatter/writeVaultFile routing evidence"
      contains: "T-I-040"
    - path: "tests/integration/records-reconciliation.integration.test.ts"
      provides: "T-I-044 records reconciliation behavior evidence"
      contains: "T-I-044"
  key_links:
    - from: "src/services/plugin-reconciliation.ts"
      to: "src/services/document-lock.ts"
      via: "withAncestorDirectoryLocksShared outside withDocumentLock around atomicWriteFrontmatter"
      pattern: "withAncestorDirectoryLocksShared[\\s\\S]*withDocumentLock[\\s\\S]*atomicWriteFrontmatter"
    - from: "src/utils/frontmatter.ts"
      to: "src/storage/vault-write.ts"
      via: "atomicWriteFrontmatter delegates to writeVaultFile with caller-provided lockConfig"
      pattern: "writeVaultFile\\(absolutePath, updatedContent, \\{ lockConfig \\}\\)"
---

<objective>
Close the plugin reconciliation frontmatter write lock gap.

Purpose: Keep REQ-023 plugin coordination intact while ensuring the markdown frontmatter write side effect uses the same document-path lock contract as normal document writes.
Output: Locked reconciliation frontmatter write path plus focused unit/integration evidence for T-I-040 and T-I-044.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-CONTEXT.md
@.planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-RESEARCH.md
@.planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-PATTERNS.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-02-SUMMARY.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-03-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@src/services/plugin-reconciliation.ts
@src/utils/frontmatter.ts
@src/services/document-lock.ts
@src/services/plugin-coordination-lock.ts
@src/storage/vault-write.ts
@tests/unit/plugin-reconciliation.test.ts
@tests/unit/single-write-primitive.test.ts
@tests/integration/atomic-write-frontmatter.integration.test.ts
@tests/integration/records-reconciliation.integration.test.ts

<interfaces>
From src/services/plugin-reconciliation.ts:
- executeReconciliationActions(result, pluginId, instanceId, fqcInstanceId?, databaseUrl?): Promise<ReconciliationActionSummary>
- The added-document branch calls atomicWriteFrontmatter(toAbsolutePath(doc.path), { [FM.OWNER]: pluginId, [FM.TYPE]: doc.typeId }).

From src/utils/frontmatter.ts:
- atomicWriteFrontmatter(absolutePath: string, updates: Record<string, unknown>, lockConfig?: LockAssertionConfig): Promise<void>
- This helper merges frontmatter and delegates to writeVaultFile; it must not acquire document or directory locks internally per D-06.

From Phase 157:
- Records reconciliation preambles are serialized with withPluginCoordinationLock; this is separate from document-file locking and must remain intact per D-05 and REQ-023.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reconciliation lock-contract tests</name>
  <files>tests/unit/plugin-reconciliation.test.ts, tests/unit/single-write-primitive.test.ts, tests/integration/atomic-write-frontmatter.integration.test.ts, tests/integration/records-reconciliation.integration.test.ts</files>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    .planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-CONTEXT.md
    .planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-PATTERNS.md
    .planning/phases/157-records-memory-plugins-audit-guards/157-02-SUMMARY.md
    src/services/plugin-reconciliation.ts
    src/utils/frontmatter.ts
    src/services/document-lock.ts
    src/services/plugin-coordination-lock.ts
    tests/unit/plugin-reconciliation.test.ts
    tests/unit/single-write-primitive.test.ts
    tests/integration/atomic-write-frontmatter.integration.test.ts
    tests/integration/records-reconciliation.integration.test.ts
  </read_first>
  <behavior>
    - Unit coverage expects executeReconciliationActions added-document frontmatter writes to call atomicWriteFrontmatter only inside withAncestorDirectoryLocksShared and withDocumentLock per D-05.
    - Single-write primitive source coverage still recognizes atomicWriteFrontmatter as the writeVaultFile route, not a new direct filesystem writer per D-06.
    - T-I-044 remains focused on records reconciliation serialization and is not weakened or replaced by document-lock assertions per REQ-023.
  </behavior>
  <action>Add tests before production edits. In tests/unit/plugin-reconciliation.test.ts, mock document-lock helpers if practical and assert added-document auto-track frontmatter writes are wrapped in the required order; otherwise add a source guard that checks src/services/plugin-reconciliation.ts for withAncestorDirectoryLocksShared, withDocumentLock, and atomicWriteFrontmatter in the same shouldWriteFrontmatter branch. Update tests/unit/single-write-primitive.test.ts only if the expected plugin reconciliation source pattern changes. Extend tests/integration/atomic-write-frontmatter.integration.test.ts to run the representative frontmatter write path under FQC_LOCK_ASSERT=true where the test can provide lockConfig and ambient lock context. Do not remove or relax records-reconciliation.integration.test.ts T-I-044.</action>
  <acceptance_criteria>
    FQC_LOCK_ASSERT=true npm test -- tests/unit/plugin-reconciliation.test.ts tests/unit/single-write-primitive.test.ts passes after implementation, and the added lock-contract assertion fails or demonstrates missing current coverage before implementation.
  </acceptance_criteria>
  <verify>
    <automated>FQC_LOCK_ASSERT=true npm test -- tests/unit/plugin-reconciliation.test.ts tests/unit/single-write-primitive.test.ts</automated>
  </verify>
  <done>Tests require reconciliation frontmatter writes to hold document-path locks while preserving the existing single-primitive and REQ-023 test contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap plugin reconciliation frontmatter writes</name>
  <files>src/services/plugin-reconciliation.ts, src/utils/frontmatter.ts</files>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    .planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-CONTEXT.md
    .planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-RESEARCH.md
    .planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-PATTERNS.md
    .planning/phases/157-records-memory-plugins-audit-guards/157-02-SUMMARY.md
    src/services/plugin-reconciliation.ts
    src/utils/frontmatter.ts
    src/services/document-lock.ts
    src/services/plugin-coordination-lock.ts
    src/storage/vault-write.ts
  </read_first>
  <action>In src/services/plugin-reconciliation.ts, add the narrowest config or lock assertion input needed so the added-document shouldWriteFrontmatter branch can call withAncestorDirectoryLocksShared(config, absPath, () => withDocumentLock(config, absPath, () => atomicWriteFrontmatter(absPath, updates, config))). Keep existing plugin coordination and database sequencing behavior from Phase 157 intact; do not replace withPluginCoordinationLock or broaden it. In src/utils/frontmatter.ts, keep atomicWriteFrontmatter a primitive-adjacent merge/write helper that accepts lockConfig and delegates to writeVaultFile; do not import document-lock helpers into frontmatter.ts. Preserve PIR-02 post-write read/hash/update behavior after the locked frontmatter write.</action>
  <acceptance_criteria>
    Source and unit tests show plugin reconciliation writes to markdown frontmatter acquire shared ancestor directory locks and withDocumentLock before atomicWriteFrontmatter reaches writeVaultFile. No memory, records, or plugin unregister coarse lock behavior is reintroduced.
  </acceptance_criteria>
  <verify>
    <automated>FQC_LOCK_ASSERT=true npm test -- tests/unit/plugin-reconciliation.test.ts tests/unit/single-write-primitive.test.ts</automated>
    <automated>npm test -- tests/unit/no-coarse-resource-locks.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Plugin reconciliation frontmatter writes use the document-path lock contract and atomicWriteFrontmatter remains lock-assertion-aware but primitive-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify reconciliation behavior and primitive routing together</name>
  <files>tests/integration/atomic-write-frontmatter.integration.test.ts, tests/integration/records-reconciliation.integration.test.ts</files>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    .planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-VALIDATION.md
    .planning/phases/157-records-memory-plugins-audit-guards/157-02-SUMMARY.md
    src/services/plugin-reconciliation.ts
    src/utils/frontmatter.ts
    tests/integration/atomic-write-frontmatter.integration.test.ts
    tests/integration/records-reconciliation.integration.test.ts
  </read_first>
  <action>Run and, if needed, extend focused integration coverage so T-I-040 observes frontmatter writes routed through writeVaultFile while lock assertions are enabled, and T-I-044 still proves concurrent write_record reconciliation does not double-apply actions. Preserve existing HAS_SUPABASE and session-capable database guards. Do not change memory behavior or concurrent unregister assertions except to keep them passing if imports/signatures changed.</action>
  <acceptance_criteria>
    FQC_LOCK_ASSERT=true npm run test:integration -- tests/integration/atomic-write-frontmatter.integration.test.ts tests/integration/records-reconciliation.integration.test.ts passes with .env.test, or skips through existing environment guards with a recorded reason. T-I-044 remains present and asserts concurrent write_record reconciliation behavior.
  </acceptance_criteria>
  <verify>
    <automated>FQC_LOCK_ASSERT=true npm run test:integration -- tests/integration/atomic-write-frontmatter.integration.test.ts tests/integration/records-reconciliation.integration.test.ts</automated>
  </verify>
  <done>T-I-040 and T-I-044 provide focused evidence that frontmatter writes are primitive-routed under lock assertion and records reconciliation coordination is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin reconciliation -> vault markdown file | Plugin policy can trigger ownership/type frontmatter writes on tracked documents. |
| Plugin reconciliation -> Postgres/Supabase plugin tables | DB sequencing and ownership rows must remain coherent under concurrent records operations. |
| atomicWriteFrontmatter -> writeVaultFile | Frontmatter helper crosses into durable filesystem write primitive with ambient-lock assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-04 | Tampering | plugin-reconciliation.ts added-document frontmatter write | mitigate | Wrap atomicWriteFrontmatter in withAncestorDirectoryLocksShared plus withDocumentLock before writeVaultFile is reached. |
| T-164-05 | Tampering | plugin coordination versus document locks | mitigate | Preserve Phase 157 withPluginCoordinationLock behavior and add document locks only around markdown file writes. |
| T-164-06 | Tampering | atomicWriteFrontmatter | mitigate | Keep helper delegated to writeVaultFile with caller-provided lockConfig; forbid internal lock acquisition so call-site order stays explicit. |
| T-164-SC | Tampering | npm installs | accept | No package installs in this plan; package legitimacy audit says no external packages. |
</threat_model>

<verification>
Run the focused unit and integration commands in task verifies. Also run npm run typecheck after production signature changes.
</verification>

<success_criteria>
- Plugin reconciliation frontmatter writes use shared ancestor directory locks plus withDocumentLock before writeVaultFile.
- atomicWriteFrontmatter does not acquire locks internally and remains routed through writeVaultFile.
- Existing REQ-023 records reconciliation and no-coarse-resource-lock guards remain green.
</success_criteria>

<output>
Create `.planning/phases/164-close-gap-document-repair-and-plugin-reconciliation-lock-con/164-02-SUMMARY.md` when done.
</output>
