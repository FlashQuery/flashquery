---
phase: 142-host-surface-and-consumer-context
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/config.test.ts
  - tests/unit/mcp-broker-registry.test.ts
  - .planning/phases/142-host-surface-and-consumer-context/142-VALIDATION.md
autonomous: true
requirements:
  - REQ-005
  - REQ-006
  - REQ-007
  - REQ-008
  - REQ-009
  - REQ-010
  - REQ-031
  - REQ-113
  - REQ-116
must_haves:
  truths:
    - "`host:` is optional and `host: {}` resolves to no brokered host tools and disabled host search."
    - "`host.mcp_servers` and `purposes.<name>.mcp_servers` reject unknown server IDs with consumer-scoped errors."
    - "The shared registry returns host and purpose filtered views from the same underlying tool entries."
  artifacts:
    - path: "tests/unit/config.test.ts"
      provides: "REQ-005..010 and REQ-113 config coverage"
    - path: "tests/unit/mcp-broker-registry.test.ts"
      provides: "T-U-036 and T-U-037 consumer filtering coverage"
    - path: ".planning/phases/142-host-surface-and-consumer-context/142-VALIDATION.md"
      provides: "Phase D validation checklist updates"
  key_links:
    - from: "src/config/loader.ts"
      to: "tests/unit/config.test.ts"
      via: "loadConfig host and purpose validation"
      pattern: "host\\.mcp_servers|purposes\\..*\\.mcp_servers"
    - from: "src/services/mcp-broker/registry.ts"
      to: "tests/unit/mcp-broker-registry.test.ts"
      via: "ToolRegistry.listToolsForConsumer"
      pattern: "listToolsForConsumer"
---

<objective>
Lock the Phase D config and registry contracts before host dispatch work builds on them.

Purpose: The host MCP surface must be additive to existing `host_mcp_tools`, and all later host/delegated behavior depends on one shared registry with per-consumer views.
Output: Focused unit coverage and validation checklist updates for config defaults, strict server references, and consumer filtering.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/142-host-surface-and-consumer-context/142-CONTEXT.md
@.planning/phases/142-host-surface-and-consumer-context/142-RESEARCH.md
@.planning/phases/142-host-surface-and-consumer-context/142-VALIDATION.md
@.planning/phases/142-host-surface-and-consumer-context/142-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<plan_notes>
Downstream implementation agents must read the two MCP Broker source docs above before resolving requirement or test questions. If this plan conflicts with those docs, implement the source docs and record the discrepancy in the summary.
</plan_notes>

<interfaces>
From `src/config/loader.ts`: `loadConfig`, `FlashQueryConfig.host`, `FlashQueryConfig.mcpServers`, `FlashQueryConfig.llm.purposes[].mcpServers`, `getResolvedHostToolExposure`.
From `src/services/mcp-broker/types.ts`: `ConsumerContext = { kind:'host'; traceId; interactive? } | { kind:'purpose'; purposeId; traceId; interactive? }`.
From `src/services/mcp-broker/registry.ts`: `ToolRegistry.listToolsForConsumer(ctx)` and `ToolRegistry.listVisibleServerIds(ctx)` are the visibility gates.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Pin host config defaults and strict server references</name>
  <files>tests/unit/config.test.ts, .planning/phases/142-host-surface-and-consumer-context/142-VALIDATION.md</files>
  <read_first>
    @tests/unit/config.test.ts
    @src/config/loader.ts
    @.planning/phases/142-host-surface-and-consumer-context/142-CONTEXT.md
    @.planning/phases/142-host-surface-and-consumer-context/142-VALIDATION.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - REQ-005: omitted `host:` exposes no brokered host servers.
    - REQ-006: `host: {}` resolves to `{ mcpServers: [], toolSearch: 'disabled' }`.
    - REQ-007: unknown `host.mcp_servers` fails with `[host]` and the missing ID.
    - REQ-008: unknown purpose `mcp_servers` fails with `[purpose:<name>]` and the missing ID.
    - REQ-009: host `tool_search` defaults to `disabled`.
    - REQ-010 and REQ-113: `host.tool_search: enabled` parses as the source spec §6.6 host section.
  </behavior>
  <action>Extend existing config tests with source test-plan labels for the Phase D config cases. Preserve the existing `host_mcp_tools` assertions and explicitly assert that `host.mcp_servers` is additive and distinct from `host_mcp_tools.tools`. Update the validation map rows for config coverage from pending to planned with the exact test names added.</action>
  <acceptance_criteria>
    - `tests/unit/config.test.ts` contains assertions for omitted `host:`, empty `host: {}`, host unknown server, purpose unknown server, default disabled host search, and enabled host search.
    - Source assertion: no test treats `host_mcp_tools` and `host.mcp_servers` as the same selector.
    - Validation assertion: `142-VALIDATION.md` references the concrete config test names under REQ-005..010 and REQ-113.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/config.test.ts</automated>
  </verify>
  <done>Host config contract is covered by fast unit tests and recorded in the Phase 142 validation strategy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Label shared registry consumer filtering coverage</name>
  <files>tests/unit/mcp-broker-registry.test.ts, .planning/phases/142-host-surface-and-consumer-context/142-VALIDATION.md</files>
  <read_first>
    @tests/unit/mcp-broker-registry.test.ts
    @src/services/mcp-broker/registry.ts
    @src/services/mcp-broker/types.ts
    @.planning/phases/142-host-surface-and-consumer-context/142-VALIDATION.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-U-036 / REQ-031 / REQ-116: host sees only tools from `host.mcp_servers`.
    - T-U-037 / REQ-031: a purpose sees only tools from its own `mcp_servers`.
    - Hidden tools from a third server are absent from both filtered views unless configured for that consumer.
  </behavior>
  <action>Extend or relabel registry tests so `T-U-036` and `T-U-037` are explicit in test names or comments. Assert clone safety remains intact by mutating a returned host-view tool and proving the purpose-view copy is unchanged. Do not add a second registry or any host-only filtering helper.</action>
  <acceptance_criteria>
    - `tests/unit/mcp-broker-registry.test.ts` has explicit `T-U-036` and `T-U-037` coverage labels.
    - Behavior assertion: a host context excludes a server not listed in `host.mcpServers`.
    - Behavior assertion: a purpose context excludes host-only servers and other-purpose servers.
    - Source assertion: registry filtering still goes through `ToolRegistry.listToolsForConsumer`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/mcp-broker-registry.test.ts</automated>
  </verify>
  <done>REQ-031 and REQ-116 registry filtering are explicitly covered before dispatch work depends on them.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config to registry | User YAML controls which brokered servers host and purpose consumers can see. |
| registry to consumers | Filtered views are the authorization boundary for discovery and dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Elevation of Privilege | `ToolRegistry.listToolsForConsumer` | mitigate | T-U-036/T-U-037 assert hidden servers never appear in filtered host or purpose views. |
| T-142-02 | Tampering | `host:` config parsing | mitigate | Config tests assert unknown server IDs fail loudly and cannot silently widen visibility. |
| T-142-03 | Repudiation | validation ledger | mitigate | `142-VALIDATION.md` records exact Phase D test labels and commands for config/filtering. |
| T-142-SC | Tampering | package installs | accept | No package-manager install is planned; use existing Vitest and broker modules. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/config.test.ts tests/unit/mcp-broker-registry.test.ts`.
</verification>

<success_criteria>
Config and registry tests prove REQ-005..010, REQ-031, REQ-113, and REQ-116 without introducing new dependencies or duplicate visibility logic.
</success_criteria>

<output>
Create `.planning/phases/142-host-surface-and-consumer-context/142-01-SUMMARY.md` when done.
</output>
