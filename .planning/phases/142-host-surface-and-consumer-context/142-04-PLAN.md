---
phase: 142-host-surface-and-consumer-context
plan: 4
type: execute
wave: 3
depends_on:
  - 142-02
  - 142-03
files_modified:
  - src/services/tool-search/tool-search-service.ts
  - src/services/mcp-broker/index.ts
  - tests/integration/tool-search/host-index.integration.test.ts
  - tests/integration/mcp-broker/client-lifecycle.test.ts
autonomous: true
requirements:
  - REQ-010
  - REQ-031
  - REQ-117
  - REQ-118
must_haves:
  truths:
    - "`host.tool_search: enabled` builds a host index containing host-visible FQ-native and brokered tools."
    - "Host and delegated callers share one broker client/server instance for the same configured server."
    - "TOFU pins and pending drift state are shared across host and delegated consumers."
  artifacts:
    - path: "src/services/tool-search/tool-search-service.ts"
      provides: "Host index build and update lifecycle"
    - path: "src/services/mcp-broker/index.ts"
      provides: "Shared broker state and context-forwarded lazy spawn"
    - path: "tests/integration/tool-search/host-index.integration.test.ts"
      provides: "Host search Phase D coverage"
    - path: "tests/integration/mcp-broker/client-lifecycle.test.ts"
      provides: "T-I-031 and T-I-032 shared spawn/TOFU coverage"
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/services/tool-search/tool-search-service.ts"
      via: "initializeHostToolSearchForServer"
      pattern: "buildForHost"
    - from: "src/services/mcp-broker/index.ts"
      to: "tests/integration/mcp-broker/client-lifecycle.test.ts"
      via: "one BrokerClient map per broker"
      pattern: "#clients"
---

<objective>
Close host search and shared broker-state behavior for host and delegated consumers.

Purpose: Host brokered exposure must use the same lazy-spawn and TOFU state as delegated purpose calls while keeping host search synchronized with visible brokered tools.
Output: Host index integration coverage and shared spawn/TOFU integration coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/142-host-surface-and-consumer-context/142-CONTEXT.md
@.planning/phases/142-host-surface-and-consumer-context/142-RESEARCH.md
@.planning/phases/142-host-surface-and-consumer-context/142-PATTERNS.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-06-SUMMARY.md
@.planning/phases/140-tofu-schema-pinning-and-tool-list-change-handling/140-02-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<plan_notes>
Downstream implementation agents must read the two MCP Broker source docs above before resolving requirement or test questions. Use the existing broker and tool-search services; do not create host-only broker clients or a second indexer implementation.
</plan_notes>

<interfaces>
From `src/services/tool-search/tool-search-service.ts`: `ToolSearchService.createEmpty`, `buildForHost`, and `createHostIndexSink`.
From `src/services/mcp-broker/index.ts`: `McpBroker.#clients` owns shared `BrokerClient` instances; `listToolsForConsumer(ctx)` forwards `snapshotOptionsFromConsumerContext(ctx)` into `ensureConnected`.
From `src/services/mcp-broker/types.ts`: `ToolIndexSink.addTools/removeTools` and `ToolListSnapshotOptions`.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Verify host search indexes host-visible brokered tools</name>
  <files>src/services/tool-search/tool-search-service.ts, tests/integration/tool-search/host-index.integration.test.ts</files>
  <read_first>
    @src/services/tool-search/tool-search-service.ts
    @src/mcp/server.ts
    @src/services/mcp-broker/index.ts
    @tests/integration/tool-search/host-index.integration.test.ts
    @tests/scenarios/integration/tests/search_tools_workflow.yml
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - REQ-010: host index builds at startup when `host.tool_search: enabled`.
    - Phase 141 carried behavior: host index covers FQ-native plus host-visible brokered tools.
    - REQ-031: host index excludes brokered tools from servers not listed in `host.mcp_servers`.
  </behavior>
  <action>Extend `host-index.integration.test.ts` to prove Phase D host index behavior through the public `search_tools` host handler or the existing test-visible host search service. If production code lacks a concrete host index update needed for the tests, implement it in `ToolSearchService` using the existing `createHostIndexSink(hostServerIds)` path. Preserve Phase 141 list_changed synchronous update semantics and description override indexing.</action>
  <acceptance_criteria>
    - Enabled host search returns a brokered result from `basic__echo` when `host.mcp_servers:[basic]`.
    - Enabled host search returns FQ-native results from the host-native catalog.
    - A brokered server visible only to a purpose does not appear in host search results.
    - `description_override` is searchable in the host index.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/tool-search/host-index.integration.test.ts</automated>
  </verify>
  <done>Host search is proven for the Phase D host-visible brokered surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove shared lazy spawn and shared TOFU across consumers</name>
  <files>src/services/mcp-broker/index.ts, tests/integration/mcp-broker/client-lifecycle.test.ts</files>
  <read_first>
    @src/services/mcp-broker/index.ts
    @src/services/mcp-broker/client.ts
    @src/services/mcp-broker/tofu.ts
    @src/services/mcp-broker/types.ts
    @tests/integration/mcp-broker/client-lifecycle.test.ts
    @tests/integration/mcp-broker/tofu-list-changed.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-031 / REQ-117: host and delegated calls to the same server spawn one shared process/client.
    - T-I-032 / REQ-118: first observation by one consumer pins TOFU for the other consumer.
    - Consumer `interactive` and `traceId` still flow into `ensureConnected` through `snapshotOptionsFromConsumerContext`.
  </behavior>
  <action>Add focused integration tests using the existing fixture server process counters or PID observation. Drive `broker.listToolsForConsumer({kind:'host', ...})` and `broker.listToolsForConsumer({kind:'purpose', purposeId, ...})` against the same server in both orders and assert a single shared client/spawn. Then mutate or use the quirky fixture to prove a TOFU pin established through host visibility is reused for a delegated purpose and a delegated pin is reused for host visibility. If needed, expose a narrow test-only observation helper rather than changing public broker APIs.</action>
  <acceptance_criteria>
    - Host-first then purpose listing/call creates exactly one server process for the shared server.
    - Purpose-first then host listing/call creates exactly one server process for the shared server.
    - A TOFU drift observed after host first-observation blocks the same tool for the delegated purpose without a fresh first-trust path.
    - A TOFU drift observed after purpose first-observation blocks the same tool for host without a fresh first-trust path.
    - Source assertion: `McpBroker` still owns one `#clients` map and one in-memory TOFU store per broker instance.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/mcp-broker/client-lifecycle.test.ts</automated>
  </verify>
  <done>REQ-117 and REQ-118 are proven with integration tests over the shared broker instance.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host search query to index | Host can query indexed brokered metadata selected by config. |
| host/purpose consumer to broker client map | Different consumer contexts share server process and TOFU state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-12 | Elevation of Privilege | host search index | mitigate | Host index uses host consumer visibility and excludes purpose-only servers in integration tests. |
| T-142-13 | Tampering | shared TOFU pins | mitigate | Integration tests prove schema drift blocks across host and purpose consumers from one shared TOFU store. |
| T-142-14 | Denial of Service | lazy spawn race | mitigate | Shared-client integration tests assert one process for host/purpose first-reference races. |
| T-142-15 | Information Disclosure | search results | mitigate | Host search result docs include descriptions/help metadata only, no raw tool args/results/errors. |
| T-142-SC | Tampering | package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm run test:integration -- --run tests/integration/tool-search/host-index.integration.test.ts tests/integration/mcp-broker/client-lifecycle.test.ts`.
</verification>

<success_criteria>
Host search, shared lazy spawn, and shared TOFU pins are verified through integration tests using the existing broker and search services.
</success_criteria>

<output>
Create `.planning/phases/142-host-surface-and-consumer-context/142-04-SUMMARY.md` when done.
</output>
