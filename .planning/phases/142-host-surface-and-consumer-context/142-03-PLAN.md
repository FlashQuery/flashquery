---
phase: 142-host-surface-and-consumer-context
plan: 3
type: execute
wave: 2
depends_on:
  - 142-01
files_modified:
  - src/services/mcp-broker/trace.ts
  - src/mcp/tools/llm.ts
  - src/mcp/tools/macro.ts
  - src/macro/registry.ts
  - tests/unit/llm-tool-dispatcher.test.ts
  - tests/unit/macro-registry.test.ts
  - tests/e2e/mcp-broker.e2e.test.ts
autonomous: true
requirements:
  - REQ-065
  - REQ-066
  - REQ-067
  - REQ-114
  - REQ-115
must_haves:
  truths:
    - "`call_model` metadata includes a `tool_calls` array for brokered purpose calls."
    - "Host-invoked macros and nested macro re-entry preserve the outer consumer trace scope."
    - "Nested delegated autonomous macro calls preserve `interactive: false` across `fq.call_macro` re-entry."
  artifacts:
    - path: "src/services/mcp-broker/trace.ts"
      provides: "Trace snapshot fields for brokered tool calls"
    - path: "src/mcp/tools/llm.ts"
      provides: "`call_model` metadata `tool_calls` exposure"
    - path: "src/mcp/tools/macro.ts"
      provides: "Outer consumer-context propagation for public and nested `call_macro`"
    - path: "tests/e2e/mcp-broker.e2e.test.ts"
      provides: "T-E-D1 trace/context inheritance gate"
  key_links:
    - from: "src/llm/tool-dispatcher.ts"
      to: "src/services/mcp-broker/trace.ts"
      via: "recordBrokeredToolCall"
      pattern: "recordBrokeredToolCall"
    - from: "src/mcp/tools/macro.ts"
      to: "src/macro/registry.ts"
      via: "MacroCallerContext -> makeBrokerConsumerContext"
      pattern: "callerContext.*interactive|traceId"
---

<objective>
Complete broker trace visibility and nested `ConsumerContext` inheritance.

Purpose: Host and delegated consumers must share broker state without losing trace scope, cost accounting, or autonomous `interactive:false` behavior across nested macro frames.
Output: Trace metadata, macro context propagation, focused unit tests, and an E2E Phase D gate.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/142-host-surface-and-consumer-context/142-CONTEXT.md
@.planning/phases/142-host-surface-and-consumer-context/142-RESEARCH.md
@.planning/phases/142-host-surface-and-consumer-context/142-PATTERNS.md
@.planning/phases/142-host-surface-and-consumer-context/142-CARRY-FORWARD-FROM-140.md
@.planning/phases/139-broker-foundation-registry-and-dispatch/139-05-SUMMARY.md
@.planning/phases/140-tofu-schema-pinning-and-tool-list-change-handling/140-05-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<plan_notes>
Downstream implementation agents must read the two MCP Broker source docs above before resolving requirement or test questions. `interactive` inheritance from the Phase 140 carry-forward is mandatory, not optional cleanup.
</plan_notes>

<interfaces>
From `src/services/mcp-broker/trace.ts`: `recordBrokeredToolCall`, `getBrokeredToolCallTraceSnapshot`, `clearBrokeredToolCallTrace`.
From `src/macro/types.ts`: `MacroCallerContext` includes origin and `interactive?`; extend only if needed to carry an existing `ConsumerContext` or trace ID.
From `src/macro/registry.ts`: `makeBrokerConsumerContext(callerContext,nativeDispatchContext)` already propagates `callerContext.interactive`.
From `src/mcp/tools/macro.ts`: public `call_macro` currently derives `currentSessionId` from MCP extra or registration session ID.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expose brokered `tool_calls` trace metadata</name>
  <files>src/services/mcp-broker/trace.ts, src/mcp/tools/llm.ts, tests/unit/llm-tool-dispatcher.test.ts</files>
  <read_first>
    @src/services/mcp-broker/trace.ts
    @src/llm/tool-dispatcher.ts
    @src/mcp/tools/llm.ts
    @tests/unit/llm-tool-dispatcher.test.ts
    @tests/scenarios/integration/tests/cost_per_call_resolution.yml
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - REQ-065: `call_model` metadata gains `tool_calls`.
    - REQ-066: trace entries include enough data to prove host vs purpose scope and resolved cost.
    - Failed thrown broker transport calls do not inflate cost; returned `isError:true` calls may record because the upstream call completed.
  </behavior>
  <action>Extend trace entry shape only as needed to include `consumer` or equivalent scope metadata while preserving existing `server`, `tool`, `count`, and `cost` fields used by prior scenarios. In `call_model` response metadata, emit `tool_calls` from `getBrokeredToolCallTraceSnapshot(traceId)` for each invocation, using an empty array when no brokered calls occurred. Keep field names snake_case in public metadata if existing metadata uses snake_case. Update focused unit tests to assert cost accumulation, scope metadata, and empty-array behavior.</action>
  <acceptance_criteria>
    - `call_model` success metadata includes `tool_calls: []` when no brokered tools are called.
    - A brokered purpose call returns metadata with `tool_calls[0].server`, `tool_calls[0].tool`, `tool_calls[0].count`, and `tool_calls[0].cost`.
    - Trace entries include consumer scope or trace identity sufficient to distinguish purpose and host usage in tests.
    - Source assertion: trace recording still occurs after `Broker.callTool` returns, not before.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/llm-tool-dispatcher.test.ts</automated>
  </verify>
  <done>REQ-065 and the delegated side of REQ-066 are observable in public `call_model` metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve consumer context across nested macro frames</name>
  <files>src/mcp/tools/macro.ts, src/macro/registry.ts, tests/unit/macro-registry.test.ts, tests/e2e/mcp-broker.e2e.test.ts</files>
  <read_first>
    @src/mcp/tools/macro.ts
    @src/macro/registry.ts
    @src/macro/types.ts
    @src/llm/agent-loop.ts
    @tests/unit/macro-registry.test.ts
    @tests/e2e/mcp-broker.e2e.test.ts
    @.planning/phases/142-host-surface-and-consumer-context/142-CARRY-FORWARD-FROM-140.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - REQ-067: host-invoked macros inherit host trace scope.
    - REQ-114: nested macro frames inherit consumer filtering context.
    - REQ-115: `ConsumerContext` is established once at the outermost frame.
    - Phase 140 carry-forward: nested delegated autonomous `fq.call_macro` preserves `interactive: false` and returns `tool_unavailable_pending_user_decision`, not `needs_user_input`.
  </behavior>
  <action>Add a narrow parent-consumer-context propagation path for macro execution. Public host `call_macro` must establish `{ kind:'host', traceId: currentSessionId, interactive:true }` once and pass equivalent fields through `MacroCallerContext` / native dispatch context so nested `fq.call_macro` re-entry uses the outer context instead of reconstructing a fresh host default. Delegated purpose macro execution must pass its purpose context and `interactive:false` when no chat adapter is available. Update `makeBrokerConsumerContext` only if required to carry trace ID and purpose ID without losing existing origin behavior.</action>
  <acceptance_criteria>
    - Unit assertion: nested host macro broker dispatch uses the same trace ID as the outer host `call_macro`.
    - Unit assertion: nested delegated purpose macro preserves `purposeId` and cannot see host-only brokered servers.
    - Unit assertion: nested delegated autonomous macro with pending drift returns `tool_unavailable_pending_user_decision` and not `needs_user_input`.
    - E2E assertion: T-E-D1 includes host macro trace inheritance evidence in `tests/e2e/mcp-broker.e2e.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/macro-registry.test.ts && npm run test:e2e -- --run tests/e2e/mcp-broker.e2e.test.ts</automated>
  </verify>
  <done>REQ-067, REQ-114, REQ-115, and the Phase 140 `interactive:false` carry-forward are implemented and tested.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delegated model to macro engine | Delegated tool calls may re-enter macro execution without chat mediation. |
| trace map to public metadata | Internal trace snapshots become model-visible metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-08 | Repudiation | `tool_calls` metadata | mitigate | Include deterministic server/tool/count/cost and consumer scope; tests assert host/purpose trace separation. |
| T-142-09 | Elevation of Privilege | nested macro `ConsumerContext` | mitigate | Establish context once at outer frame and inherit it; tests assert hidden host-only tools remain hidden to purpose macros. |
| T-142-10 | Tampering | schema drift autonomous handling | mitigate | Preserve `interactive:false` so autonomous delegated callers fail closed instead of prompting or silently approving drift. |
| T-142-11 | Information Disclosure | trace metadata | mitigate | Expose only sanitized trace fields; no raw args, raw results, stderr, or normalized error `raw` values. |
| T-142-SC | Tampering | package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/llm-tool-dispatcher.test.ts tests/unit/macro-registry.test.ts` and `npm run test:e2e -- --run tests/e2e/mcp-broker.e2e.test.ts`.
</verification>

<success_criteria>
Brokered tool calls produce public `tool_calls` metadata, host macro calls inherit trace scope, nested macro frames preserve consumer visibility, and autonomous delegated nested calls preserve `interactive:false`.
</success_criteria>

<output>
Create `.planning/phases/142-host-surface-and-consumer-context/142-03-SUMMARY.md` when done.
</output>
