---
phase: 136-task-lifecycle-and-cancellation
plan: 04
type: execute
wave: 3
depends_on:
  - 136-03
files_modified:
  - tests/integration/macro-concurrency.test.ts
  - tests/config/vitest.integration.config.ts
  - tests/scenarios/framework/fqc_test_utils.py
  - tests/scenarios/directed/helpers/macro_cancellation_harness.ts
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - tests/scenarios/directed/testcases/test_macro_cancellation.py
  - tests/scenarios/directed/testcases/test_macro_no_partial_side_effects_after_cancel.py
autonomous: true
requirements:
  - MACRO-INT-01
  - MACRO-OBS-05
  - MACRO-OBS-06
must_haves:
  truths:
    - "Concurrent macro invocations across simulated sessions do not leak variables, trace, task entries, budget counters, progress, or cancellation state."
    - "The new integration suite is included in the explicit Vitest integration config."
    - "Directed cancellation coverage is added without overwriting existing memory lifecycle M-01/M-02 rows."
  artifacts:
    - path: "tests/integration/macro-concurrency.test.ts"
      provides: "T-I-002 concurrent isolation coverage"
      contains: "T-I-002"
    - path: "tests/config/vitest.integration.config.ts"
      provides: "Explicit macro-concurrency integration registration"
      contains: "macro-concurrency.test.ts"
    - path: "tests/scenarios/directed/testcases/test_macro_cancellation.py"
      provides: "Directed cancellation safe-point scenario"
      contains: "COVERAGE"
    - path: "tests/scenarios/directed/testcases/test_macro_no_partial_side_effects_after_cancel.py"
      provides: "Directed no-post-cancel side-effect scenario"
      contains: "COVERAGE"
    - path: "tests/scenarios/directed/helpers/macro_cancellation_harness.ts"
      provides: "In-process directed cancellation hook used by managed scenarios"
      contains: "MacroTaskRegistry"
    - path: "tests/scenarios/directed/DIRECTED_COVERAGE.md"
      provides: "Non-colliding macro lifecycle coverage rows"
      contains: "MLC-01"
  key_links:
    - from: "tests/config/vitest.integration.config.ts"
      to: "tests/integration/macro-concurrency.test.ts"
      via: "explicit include entry"
      pattern: "macro-concurrency\\.test\\.ts"
    - from: "tests/scenarios/directed/DIRECTED_COVERAGE.md"
      to: "tests/scenarios/directed/testcases/test_macro_cancellation.py"
      via: "non-colliding macro lifecycle coverage ID"
      pattern: "MLC-01"
---

<objective>
Close Phase 136 integration and directed scenario coverage, including explicit handling of the `M-01`/`M-02` directed coverage collision.

Purpose: prove session isolation under concurrent execution and add public scenario coverage for cancellation/no-post-cancel side effects without corrupting existing directed ledgers.
Output: `macro-concurrency` integration suite, integration config registration, and directed macro lifecycle scenario rows/files with non-colliding IDs.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-task-lifecycle-and-cancellation/136-CONTEXT.md
@.planning/phases/136-task-lifecycle-and-cancellation/136-RESEARCH.md
@.planning/phases/136-task-lifecycle-and-cancellation/136-VALIDATION.md
@.planning/phases/135-tool-registry-dispatch-permissions/135-04-SUMMARY.md

<interfaces>
Integration pattern from `tests/integration/macro-tool-dispatch.test.ts`:
```typescript
const server = createMcpServer(config, '0.1.0');
const client = new Client({ name: 'macro-tool-dispatch-test', version: '1.0.0' });
const [clientTransport, serverTransport] = InMemoryTransport.createLinkedPair();
await Promise.all([server.connect(serverTransport), client.connect(clientTransport)]);
```

Current directed collision:
```text
M-01 and M-02 already belong to memory lifecycle in tests/scenarios/directed/DIRECTED_COVERAGE.md.
Use new macro lifecycle IDs such as MLC-01 and MLC-02 for Phase 136 scenario rows; mention the source Test Plan T-S-001/T-S-002 mapping in row text.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add T-I-002 macro concurrency integration</name>
  <files>tests/integration/macro-concurrency.test.ts, tests/config/vitest.integration.config.ts</files>
  <read_first>
    - tests/integration/macro-tool-dispatch.test.ts
    - tests/config/vitest.integration.config.ts
    - src/mcp/server.ts
    - src/mcp/tools/macro.ts
    - src/macro/task-registry.ts
    - .planning/phases/135-tool-registry-dispatch-permissions/135-04-SUMMARY.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <behavior>
    - T-I-002 runs two or more concurrent macro invocations across simulated sessions.
    - Assertions prove no leakage for variables, trace buffers, task IDs/list visibility, budget counters, progress streams, and cancellation flags.
    - Multiple tasks can be `working` simultaneously across sessions.
    - The new file is registered in `tests/config/vitest.integration.config.ts`.
  </behavior>
  <action>
    Create `tests/integration/macro-concurrency.test.ts` using the existing `Client` + `InMemoryTransport` pattern from `macro-tool-dispatch.test.ts`. Use separate clients/transports or trusted internal `runMacroSource` calls with distinct `sessionId` values if public MCP session identity is not yet exposed. The test must be named with `T-I-002` and assert distinct `task_id` values, isolated `trace`, isolated `progress`, isolated `external_tool_calls` budget increments, and same-session-only `list_tasks` output while both runs are active. Add `'tests/integration/macro-concurrency.test.ts'` to the explicit include list in `tests/config/vitest.integration.config.ts`.
  </action>
  <verify>
    <automated>npm run test:integration -- --reporter=verbose macro-concurrency</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/integration/macro-concurrency.test.ts`
    - `rg -n "T-I-002|session-a|session-b|task_id|trace|progress|external_tool_calls|list_tasks" tests/integration/macro-concurrency.test.ts`
    - `rg -n "tests/integration/macro-concurrency\\.test\\.ts" tests/config/vitest.integration.config.ts`
    - `npm run test:integration -- --reporter=verbose macro-concurrency` passes or skips only through existing `.env.test`/Supabase guards.
  </acceptance_criteria>
  <done>T-I-002 exists, is registered, and proves concurrent per-session isolation across variables, trace, tasks, budgets, progress, and cancellation state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add directed macro cancellation scenarios with non-colliding IDs</name>
  <files>tests/scenarios/framework/fqc_test_utils.py, tests/scenarios/directed/helpers/macro_cancellation_harness.ts, tests/scenarios/directed/DIRECTED_COVERAGE.md, tests/scenarios/directed/testcases/test_macro_cancellation.py, tests/scenarios/directed/testcases/test_macro_no_partial_side_effects_after_cancel.py</files>
  <read_first>
    - tests/scenarios/directed/DIRECTED_COVERAGE.md
    - tests/scenarios/directed/WRITING_SCENARIOS.md
    - tests/scenarios/directed/testcases/test_macro_forbidden_shell_flag.py
    - tests/scenarios/directed/testcases/test_macro_vault_jail_escape.py
    - tests/scenarios/framework/fqc_test_utils.py
    - src/mcp/tools/macro.ts
    - src/macro/task-registry.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <behavior>
    - Existing memory lifecycle rows `M-01` and `M-02` remain unchanged.
    - Add Phase 136 macro lifecycle rows with non-colliding IDs `MLC-01` and `MLC-02`, mapping back to Test Plan T-S-001/T-S-002 in the Behavior text.
    - The directed managed runner has an executable in-process cancellation hook that can create a `MacroTaskRegistry`, start a macro run, call `taskRegistry.cancel(taskId, sessionId)`, and return the resulting macro envelope to Python.
    - `test_macro_cancellation.py` verifies cancellation safe-point behavior and canonical `cancelled` envelope.
    - `test_macro_no_partial_side_effects_after_cancel.py` verifies cancellation mid-loop produces no document mutation after the safe point.
  </behavior>
  <action>
    Update `tests/scenarios/framework/fqc_test_utils.py` with a read-only `FQCServer.config_path` property if the generated managed-server config path is not already exposed; keep the existing `_config_path` lifecycle and cleanup behavior unchanged. Create `tests/scenarios/directed/helpers/macro_cancellation_harness.ts` as a test-only Node/tsx helper that loads the generated config, constructs a real `MacroTaskRegistry`, starts `runMacroSource` with an explicit `sessionId`, waits until the registry contains the working task, invokes `taskRegistry.cancel(taskId, sessionId)`, and prints a compact JSON result containing the macro envelope, observed task id, safe point, and any vault file contents needed by the no-side-effects assertion. The helper must support two modes: `cancellation` for T-S-001/MLC-01 and `no-partial-side-effects` for T-S-002/MLC-02. Update `tests/scenarios/directed/DIRECTED_COVERAGE.md` by adding a new section such as `## Phase 136 Macro Task Lifecycle And Cancellation` with rows `MLC-01` and `MLC-02`. Do not edit the existing memory `M-01`/`M-02` rows. Create the two Python scenario files using the existing directed `TestContext`/managed-server pattern, and have them call the TS helper via `subprocess.run` using the managed server's `config_path` and vault path. Set `COVERAGE = ["MLC-01"]` and `COVERAGE = ["MLC-02"]` respectively, with comments noting the Macro Test Plan's original T-S-001/T-S-002 M-01/M-02 labels collided with memory lifecycle IDs. The directed scenarios must fail if cancellation cannot be driven; do not accept a documented harness limitation as success and do not overwrite memory rows.
  </action>
  <verify>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed test_macro_cancellation test_macro_no_partial_side_effects_after_cancel</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MLC-01|MLC-02|T-S-001|T-S-002" tests/scenarios/directed/DIRECTED_COVERAGE.md tests/scenarios/directed/testcases/test_macro_cancellation.py tests/scenarios/directed/testcases/test_macro_no_partial_side_effects_after_cancel.py`
    - `rg -n "MacroTaskRegistry|taskRegistry\\.cancel|runMacroSource|no-partial-side-effects|cancellation" tests/scenarios/directed/helpers/macro_cancellation_harness.ts`
    - `rg -n "config_path|macro_cancellation_harness|subprocess\\.run" tests/scenarios/framework/fqc_test_utils.py tests/scenarios/directed/testcases/test_macro_cancellation.py tests/scenarios/directed/testcases/test_macro_no_partial_side_effects_after_cancel.py`
    - `rg -n "^\\| M-01 \\| Save memory|^\\| M-02 \\| Search memory" tests/scenarios/directed/DIRECTED_COVERAGE.md`
    - `! rg -n "COVERAGE = \\[\"M-0[12]\"\\]" tests/scenarios/directed/testcases/test_macro_cancellation.py tests/scenarios/directed/testcases/test_macro_no_partial_side_effects_after_cancel.py`
    - `! rg -n "documented harness limitation|pending harness gap|if direct in-process cancellation is unavailable|reports only" tests/scenarios/directed/testcases/test_macro_cancellation.py tests/scenarios/directed/testcases/test_macro_no_partial_side_effects_after_cancel.py tests/scenarios/directed/helpers/macro_cancellation_harness.ts`
    - `python3 tests/scenarios/directed/run_suite.py --managed test_macro_cancellation test_macro_no_partial_side_effects_after_cancel` passes.
  </acceptance_criteria>
  <done>Directed macro cancellation scenarios and coverage rows exist with non-colliding IDs, the managed directed runner can drive in-process cancellation through the test helper, the directed command passes, and existing memory M-01/M-02 rows are preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 136 final focused gates</name>
  <files>.planning/phases/136-task-lifecycle-and-cancellation/136-VALIDATION.md</files>
  <read_first>
    - .planning/phases/136-task-lifecycle-and-cancellation/136-VALIDATION.md
    - tests/config/vitest.integration.config.ts
    - tests/scenarios/directed/DIRECTED_COVERAGE.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>
    Run the final Phase 136 verification commands and update `136-VALIDATION.md` statuses/results for the rows touched by this phase. Required commands: `npm test -- --reporter=verbose macro-task-registry macro-cancellation macro-session-scope`, `npm run test:integration -- --reporter=verbose macro-concurrency`, `python3 tests/scenarios/directed/run_suite.py --managed test_macro_cancellation test_macro_no_partial_side_effects_after_cancel`, and `npm run build`. Record any integration environment-driven skip exactly, including the command output summary. The directed cancellation command is mandatory and must pass; do not record a directed harness limitation or pending cancellation gap as an acceptable result.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-task-registry macro-cancellation macro-session-scope && npm run test:integration -- --reporter=verbose macro-concurrency && python3 tests/scenarios/directed/run_suite.py --managed test_macro_cancellation test_macro_no_partial_side_effects_after_cancel && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "macro-task-registry macro-cancellation macro-session-scope|macro-concurrency|test_macro_cancellation|test_macro_no_partial_side_effects_after_cancel|npm run build" .planning/phases/136-task-lifecycle-and-cancellation/136-VALIDATION.md`
    - `rg -n "✅ green|PASS|passed|environment-skipped" .planning/phases/136-task-lifecycle-and-cancellation/136-VALIDATION.md`
    - Final chained verification command exits 0. Integration tests may use existing `.env.test`/Supabase skip guards, but the directed cancellation command must pass and must not be marked skipped due to missing harness support.
  </acceptance_criteria>
  <done>Final focused unit, integration, directed scenario, and build gates are run and recorded for Phase 136.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulated session A -> simulated session B | Concurrent macro execution must not leak state or cancellation across sessions. |
| Directed scenario coverage matrix -> historical memory coverage | New macro lifecycle rows must not overwrite existing `M-01`/`M-02` memory lifecycle rows. |
| Cancellation -> document mutation tools | No document mutations may occur after a cancellation-safe point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-04-01 | Information Disclosure | concurrent `list_tasks` | mitigate | T-I-002 asserts same-session task visibility only during concurrent runs. |
| T-136-04-02 | Tampering | post-cancel document writes | mitigate | `MLC-02` scenario asserts no document mutations after cancellation. |
| T-136-04-03 | Repudiation | directed coverage IDs | mitigate | Use `MLC-01`/`MLC-02` and preserve memory `M-01`/`M-02` rows with grep gates. |
| T-136-04-04 | Denial of Service | concurrent task registry | mitigate | T-I-002 proves multiple working tasks can coexist without shared budget/progress/cancellation state. |
</threat_model>

<verification>
`npm test -- --reporter=verbose macro-task-registry macro-cancellation macro-session-scope && npm run test:integration -- --reporter=verbose macro-concurrency && python3 tests/scenarios/directed/run_suite.py --managed test_macro_cancellation test_macro_no_partial_side_effects_after_cancel && npm run build`
</verification>

<success_criteria>
- `tests/integration/macro-concurrency.test.ts` exists, is registered, and covers T-I-002.
- Directed macro lifecycle coverage uses `MLC-01`/`MLC-02`; memory `M-01`/`M-02` rows remain unchanged.
- Directed managed cancellation scenarios pass by using an in-process cancellation helper; documented harness limitations are not accepted as a successful outcome.
- Focused unit, integration, directed, and build commands are run and recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/136-task-lifecycle-and-cancellation/136-04-SUMMARY.md`.
</output>
