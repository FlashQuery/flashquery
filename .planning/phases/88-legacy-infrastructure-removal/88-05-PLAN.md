---
phase: 88-legacy-infrastructure-removal
plan: 05
type: execute
wave: 4
depends_on:
  - "88-03"
  - "88-04"
files_modified:
  - tests/integration/discovery-scenarios.test.ts
  - tests/integration/discovery-errors.test.ts
  - tests/integration/discovery-multi-plugin.test.ts
autonomous: true
requirements:
  - TEST-14

must_haves:
  truths:
    - "discovery-scenarios.test.ts, discovery-errors.test.ts, discovery-multi-plugin.test.ts are deleted"
    - "No integration test file imports from discovery-orchestrator.ts, discovery-coordinator.ts, or plugin-skill-invoker.ts"
    - "npm run test:integration passes (no import errors from deleted test files)"
  artifacts:
    - path: "tests/integration/"
      provides: "Integration test directory free of files importing deleted source services"
  key_links:
    - from: "tests/integration/"
      to: "src/services/discovery-orchestrator.ts"
      via: "ABSENT — all importers deleted"
      pattern: "ABSENT: executeDiscovery"
---

<objective>
Delete the three discovery integration test files whose content is overwhelmingly based on executeDiscovery() from the deleted discovery-orchestrator.ts. Per RESEARCH.md open question 2, deletion is the valid outcome of the D-15 "review" for files where the majority of tests depend on deleted imports.

Purpose: These 3 files (390, 496, and 754 lines respectively, totalling 1640 lines) import executeDiscovery() from discovery-orchestrator.ts on their first import line and call it throughout. With 10, 27, and 33 legacy references respectively, surgical cleaning would leave nearly empty test shells. Deletion is the correct outcome.

The plugin-records.integration.test.ts requires no changes for Phase 88 — it has no legacy imports and its reconciliation side effects are benign (temp vault dirs, assertions on record IDs not ownership state).

Output: 3 integration test files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-legacy-infrastructure-removal/88-CONTEXT.md
@.planning/phases/88-legacy-infrastructure-removal/88-RESEARCH.md
@.planning/phases/88-legacy-infrastructure-removal/88-04-SUMMARY.md

<interfaces>
Files to delete (confirmed by code audit):

tests/integration/discovery-scenarios.test.ts
- 390 lines; imports executeDiscovery from discovery-orchestrator.ts (line 22)
- imports reloadPluginSkills from plugin-skill-invoker.ts (line 21)
- imports DiscoveryQueueItem from scanner.ts (line 23) — that type is deleted in Plan 01
- 10 calls to executeDiscovery throughout; tests Scenario A/B/C push-notification discovery
- All test content tied to deleted infrastructure

tests/integration/discovery-errors.test.ts
- 496 lines; imports executeDiscovery from discovery-orchestrator.ts
- 27 total legacy references (invokeChangeNotifications, watcher_claims, etc.)
- Tests error recovery paths for on_document_discovered/on_document_changed callbacks (deleted)
- All 6 test scenarios test deleted callback infrastructure

tests/integration/discovery-multi-plugin.test.ts
- 754 lines; 33 total legacy references
- Tests multi-plugin callback orchestration — entire test content is notification-based
- Tests include watcher_claims assertions, callback ordering — all deleted infrastructure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 3 discovery integration test files</name>
  <read_first>
    - tests/integration/discovery-scenarios.test.ts lines 1-25 — confirm imports from deleted files (final verification before delete)
  </read_first>
  <files>
    tests/integration/discovery-scenarios.test.ts,
    tests/integration/discovery-errors.test.ts,
    tests/integration/discovery-multi-plugin.test.ts
  </files>
  <action>
Delete all 3 files via Bash:

```bash
rm /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/tests/integration/discovery-scenarios.test.ts
rm /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/tests/integration/discovery-errors.test.ts
rm /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/tests/integration/discovery-multi-plugin.test.ts
```

Rationale (per D-15 review outcome): All three files' primary content tests the discovery skill invocation pattern (executeDiscovery from discovery-orchestrator.ts, callback invocations, watcher_claims state). With 10-33 legacy references per file in files of 390-754 lines, there is no residual test value — the subject under test no longer exists. Deletion is the correct outcome of the D-15 review.

After deleting, run `npm run test:integration` to confirm no import errors remain from these files and the integration suite passes.
  </action>
  <verify>
    <automated>ls /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/tests/integration/ | grep -E "discovery-scenarios|discovery-errors|discovery-multi-plugin" && echo "ERROR: files still exist" || echo "OK: all 3 deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/integration/` shows NO discovery-scenarios.test.ts
    - `ls tests/integration/` shows NO discovery-errors.test.ts
    - `ls tests/integration/` shows NO discovery-multi-plugin.test.ts
    - `grep -r "discovery-orchestrator\|plugin-skill-invoker" tests/integration/` returns ZERO matches
    - `npm run test:integration` exits 0 (or exits with only pre-existing failures from the deferred list in MEMORY.md, not from import errors on deleted modules)
  </acceptance_criteria>
  <done>All 3 discovery integration test files deleted; integration test suite runs without import errors from deleted source files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite | Test file deletions only — no production code changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-05 | Information Disclosure | Deletion of discovery integration tests | accept | Tests covered deleted push-notification infrastructure; no active production behavior is left uncovered |
</threat_model>

<verification>
After the task:
- `ls tests/integration/ | grep discovery-scen` returns nothing
- `ls tests/integration/ | grep discovery-err` returns nothing
- `ls tests/integration/ | grep discovery-multi` returns nothing
- `grep -r "executeDiscovery" tests/` returns ZERO matches
- `npm run test:integration` passes (0 new failures)
</verification>

<success_criteria>
- 3 discovery integration test files deleted
- No test file in tests/integration/ imports from the 6 deleted source files
- Integration test suite runs without import errors
</success_criteria>

<output>
After completion, create `.planning/phases/88-legacy-infrastructure-removal/88-05-SUMMARY.md`
</output>
