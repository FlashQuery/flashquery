---
phase: 121
plan: 03
type: execute
wave: 2
depends_on:
  - 121-01
  - 121-02
files_modified:
  - src/constants/frontmatter-fields.ts
  - tests/unit/frontmatter-fields.test.ts
  - tests/unit/no-hardcoded-extensions.test.ts
  - tests/scenarios/framework/fqc_client.py
  - tests/scenarios/directed/run_suite.py
  - tests/scenarios/integration/run_integration.py
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
  - tests/scenarios/directed/testcases/test_foundation_json_response.py
  - tests/scenarios/integration/tests/foundation_json_response.yml
autonomous: true
requirements:
  - FND-07
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
must_haves:
  truths:
    - "Frontmatter constants include consolidation-managed fields before migrated code uses them."
    - "A guard test prevents new hardcoded managed fq_* literals in source/test code outside allowlisted contexts."
    - "Directed and YAML integration scenario runners can assert JSON paths in MCP response payloads."
    - "Coverage ledgers include Phase 121 foundation rows and at least one runnable foundation scenario each."
  artifacts:
    - path: "src/constants/frontmatter-fields.ts"
      provides: "central frontmatter constants including consolidation fields"
      contains: "ARCHIVED_AT"
    - path: "tests/scenarios/directed/DIRECTED_COVERAGE.md"
      provides: "directed foundation coverage rows"
    - path: "tests/scenarios/integration/INTEGRATION_COVERAGE.md"
      provides: "integration foundation coverage rows"
    - path: "tests/scenarios/integration/tests/foundation_json_response.yml"
      provides: "YAML JSON-path assertion fixture"
  key_links:
    - from: "tests/scenarios/framework/fqc_client.py"
      to: "tests/scenarios/directed/run_suite.py"
      via: "parsed JSON response helper"
    - from: "tests/scenarios/integration/run_integration.py"
      to: "tests/scenarios/integration/tests/foundation_json_response.yml"
      via: "expect_json_* assertions"
---

<objective>
Add frontmatter constant guardrails and scenario JSON assertion scaffolding so future tool migration phases can prove behavior at the scenario layer in the same phase as implementation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-foundation-metadata-response-helpers-test-harness/121-CONTEXT.md
@.planning/phases/121-foundation-metadata-response-helpers-test-harness/121-RESEARCH.md
@.planning/phases/121-foundation-metadata-response-helpers-test-harness/121-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
</context>

<traceability>
| Requirement | Unit | Integration | E2E | Directed | Integration scenario |
|---|---|---|---|---|---|
| FND-07 | `tests/unit/frontmatter-fields.test.ts`, hardcoded `fq_*` guard | not required unless constants are consumed by a handler smoke | not required | `D-foundation-frontmatter-1` | `INT-foundation-frontmatter-1` |
| TEST-01 | traceability table in all 121 plans | summary verification | n/a | coverage ledger rows | coverage ledger rows |
| TEST-02 | scenario/helper/frontmatter unit tests | n/a | n/a | n/a | n/a |
| TEST-03 | runner integration fixture if implemented | runner fixture execution | n/a | n/a | `INT-foundation-json-1` |
| TEST-04 | n/a | n/a | protocol smoke from Plan 02 | `D-foundation-json-1` | `INT-foundation-json-1` |
| TEST-05 | directed coverage rows and runnable test | n/a | n/a | `test_foundation_json_response.py` | n/a |
| TEST-06 | YAML coverage rows and runnable workflow | YAML runner JSON assertion support | n/a | n/a | `foundation_json_response.yml` |
</traceability>

<threat_model>
Threats:
- A broad regex guard could produce noisy false positives and train agents to ignore it.
- JSON assertion support could break existing substring scenario tests.
- Scenario coverage ledgers could be updated without runnable tests.

Mitigations:
- Hardcoded `fq_*` guard must use an explicit allowlist for constants, docs, fixtures, migrations, and generated reports.
- JSON assertions are additive; existing `expect_contains` / substring behavior remains.
- Acceptance requires both coverage rows and runnable foundation scenario files.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add consolidation frontmatter constants and hardcoded managed-field guard</name>
  <files>src/constants/frontmatter-fields.ts, tests/unit/frontmatter-fields.test.ts, tests/unit/no-hardcoded-extensions.test.ts</files>
  <read_first>
    - src/constants/frontmatter-fields.ts
    - tests/unit/frontmatter-fields.test.ts
    - tests/unit/no-hardcoded-extensions.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
  </read_first>
  <action>
Update `src/constants/frontmatter-fields.ts` to include consolidation-managed fields needed by later phases:
```typescript
ARCHIVED_AT: 'fq_archived_at',
ORIGINAL_PATH: 'fq_original_path',
```

If implementation research rejects `ORIGINAL_PATH` as premature, include only `ARCHIVED_AT` and document the decision in the summary. Do not add a field that later code will not use.

Expand frontmatter tests:
- assert `FM.ARCHIVED_AT === 'fq_archived_at'`.
- assert `FrontmatterFieldName` includes the new constant through type usage if practical.
- add a hardcoded managed-field guard that scans `src/**/*.ts` and `tests/**/*.ts` for string literals matching `fq_[a-z_]+`.
- allowlist `src/constants/frontmatter-fields.ts`, explicit test fixture strings, docs embedded in tests, and database column references where `fq_*` is not frontmatter access.
  </action>
  <acceptance_criteria>
    - `src/constants/frontmatter-fields.ts` contains `ARCHIVED_AT`.
    - `tests/unit/frontmatter-fields.test.ts` asserts `FM.ARCHIVED_AT`.
    - A unit test scans for hardcoded managed frontmatter literals or extends the closest existing guard test.
    - `npm test -- tests/unit/frontmatter-fields.test.ts tests/unit/no-hardcoded-extensions.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add directed scenario JSON assertion helper and foundation test</name>
  <files>tests/scenarios/framework/fqc_client.py, tests/scenarios/directed/run_suite.py, tests/scenarios/directed/testcases/test_foundation_json_response.py, tests/scenarios/directed/DIRECTED_COVERAGE.md</files>
  <read_first>
    - tests/scenarios/framework/fqc_client.py
    - tests/scenarios/directed/run_suite.py
    - tests/scenarios/directed/DIRECTED_COVERAGE.md
    - tests/scenarios/directed/WRITING_SCENARIOS.md
  </read_first>
  <action>
Add additive JSON assertion support for directed scenarios.

Implement helper functions in the scenario framework:
```python
def parse_mcp_json(tool_result: dict) -> object:
    ...

def get_json_path(payload: object, path: str) -> object:
    # supports dotted keys and [index], e.g. "results[0].error"
```

Add a runnable directed test `tests/scenarios/directed/testcases/test_foundation_json_response.py` that calls a helper-backed representative tool from Plan 02, parses JSON, and asserts at least:
- identification field exists.
- expected error path has `error` and no runtime transport failure.

Update `DIRECTED_COVERAGE.md` with rows:
- `D-foundation-json-1`
- `D-foundation-json-2`
- `D-foundation-tools-1`
- `D-foundation-frontmatter-1`
  </action>
  <acceptance_criteria>
    - `tests/scenarios/framework/fqc_client.py` or a sibling helper contains `parse_mcp_json`.
    - `tests/scenarios/directed/testcases/test_foundation_json_response.py` exists.
    - `tests/scenarios/directed/DIRECTED_COVERAGE.md` contains `D-foundation-json-1`.
    - `python tests/scenarios/directed/run_suite.py --managed foundation` exits 0 or documents an environment skip through existing scenario mechanisms.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add YAML integration JSON-path assertions and foundation workflow</name>
  <files>tests/scenarios/integration/run_integration.py, tests/scenarios/integration/tests/foundation_json_response.yml, tests/scenarios/integration/INTEGRATION_COVERAGE.md</files>
  <read_first>
    - tests/scenarios/integration/run_integration.py
    - tests/scenarios/integration/README.md
    - tests/scenarios/integration/INTEGRATION_COVERAGE.md
    - tests/scenarios/integration/tests/append_and_search.yml
  </read_first>
  <action>
Add additive JSON-path assertion keys to the YAML integration runner:
```yaml
expect_json_path: "identifier"
expect_json_equals:
  path: "error"
  value: "not_found"
expect_json_contains:
  path: "warnings"
  value: "memory_disabled"
expect_json_array_length:
  path: "results"
  equals: 2
```

The exact YAML shape may differ if a simpler existing pattern fits better, but it must support path lookup into arrays and objects.

Create `tests/scenarios/integration/tests/foundation_json_response.yml` that exercises a helper-backed representative success and expected-error path from Plan 02.

Update `INTEGRATION_COVERAGE.md` with rows:
- `INT-foundation-json-1`
- `INT-foundation-json-2`
- `INT-foundation-tools-1`
- `INT-foundation-frontmatter-1`
  </action>
  <acceptance_criteria>
    - `tests/scenarios/integration/run_integration.py` contains `expect_json_path` or equivalent JSON assertion support.
    - `tests/scenarios/integration/tests/foundation_json_response.yml` exists.
    - `tests/scenarios/integration/INTEGRATION_COVERAGE.md` contains `INT-foundation-json-1`.
    - `python tests/scenarios/integration/run_integration.py --managed foundation` exits 0 or documents an environment skip through existing scenario mechanisms.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
```bash
npm test -- tests/unit/frontmatter-fields.test.ts tests/unit/no-hardcoded-extensions.test.ts
python tests/scenarios/directed/run_suite.py --managed foundation
python tests/scenarios/integration/run_integration.py --managed foundation
npm run build
```
</verification>

<success_criteria>
- FND-07 and TEST-01 through TEST-06 have executable coverage scaffolding.
- Scenario JSON assertions are available to later phases without breaking legacy text assertions.
- Coverage ledgers contain foundation rows tied to runnable tests.
</success_criteria>
