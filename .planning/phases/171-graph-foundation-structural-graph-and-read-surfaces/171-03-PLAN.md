---
phase: 171-graph-foundation-structural-graph-and-read-surfaces
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/supabase.ts
  - src/storage/schema-verify.ts
  - tests/integration/graph/graph-schema.test.ts
autonomous: true
requirements:
  - GR-005
must_haves:
  truths:
    - "Graph DDL creates all graph tables idempotently with chunk-keyed nodes and instance isolation."
    - "Initial `fqc_graph_nodes` DDL includes nullable Phase 172 metadata columns without relying on later ALTER migrations."
  artifacts:
    - path: "src/storage/supabase.ts"
      provides: "Graph DDL in buildSchemaDDL"
      contains: "fqc_graph_nodes"
    - path: "src/storage/schema-verify.ts"
      provides: "Graph schema verification"
    - path: "tests/integration/graph/graph-schema.test.ts"
      provides: "T-I-002/T-I-003/T-I-004/T-I-025/T-I-044 coverage"
  key_links:
    - from: "src/storage/supabase.ts"
      to: "tests/integration/graph/graph-schema.test.ts"
      via: "DDL column, constraint, and idempotency assertions"
      pattern: "T-I-044|provenance_basis|community_summary"
---

<objective>
Add graph schema DDL and verification with the complete `fqc_graph_nodes` metadata inventory.

Purpose: Product Req §6.2.1 AC2 requires all node metadata columns in the initial DDL, including nullable later-phase fields.
Output: Idempotent graph DDL, schema verification, and graph schema integration tests including T-I-044.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-CONTEXT.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add complete graph DDL and schema verification</name>
  <files>src/storage/supabase.ts, src/storage/schema-verify.ts, tests/integration/graph/graph-schema.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md
    - src/storage/supabase.ts
    - src/storage/schema-verify.ts
    - tests/integration/embedding/chunk-schema.test.ts
  </read_first>
  <behavior>
    - T-I-002/T-I-025: graph tables, constraints, indexes, and idempotent rerun behavior pass.
    - T-I-003/T-I-004: hard deletes cascade through chunk-keyed graph nodes/edges and duplicate graph nodes are rejected.
    - T-I-044: initial `CREATE TABLE IF NOT EXISTS fqc_graph_nodes` declares the full node inventory without relying on later `ALTER TABLE` migrations.
  </behavior>
  <action>Add `fqc_graph_nodes`, `fqc_graph_edges`, `fqc_pending_edges`, and graph maintenance state DDL to `buildSchemaDDL(dimensions)` using `fqc_` prefixes. `fqc_graph_nodes.chunk_id` must be the primary key and reference `fqc_chunks(id) ON DELETE CASCADE`. The initial node table must declare every Req §6.2.1 AC2 column: `chunk_id`, `instance_id`, `provenance_basis`, `question_status`, `question_resolution`, `community_id`, `community_label`, `community_summary`, `key_claims` JSONB, `chunk_summary`, `certainty_level`, `staleness_risk`, `external_refs` JSONB, `temporal_markers` JSONB, `analyzed_content_hash`, `analyzed_by_model`, `analyzed_at`, `created_at`, and `updated_at`. Later-phase fields remain nullable but present now. `fqc_graph_edges` must include directed source/target node FKs, relation, confidence, confidence_score, reasoning, model, status, metadata JSONB, timestamps, and required indexes for instance filtering, source lookup, target lookup, relation filtering, status/staleness filtering, and JSONB containment where graph reads use it. Extend `verifySchema()` to check the graph tables, critical columns, constraints, and indexes. Do not use Apache AGE, Neo4j, pg_graphql, or other graph extensions.</action>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/graph/graph-schema.test.ts</automated>
  </verify>
  <done>GR-005 is covered, including T-I-044 full node-column inventory and schema verification.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Runtime -> Postgres DDL/schema verification | Startup creates persistent graph data structures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-04 | Information Disclosure | graph tables | mitigate | Every graph table includes `instance_id`; later read plans must filter on it. |
| T-171-05 | Tampering | schema drift | mitigate | `verifySchema()` checks graph critical columns and constraints, including T-I-044 node inventory. |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed. |
</threat_model>

<verification>
Run `npm run test:integration -- --run tests/integration/graph/graph-schema.test.ts`.
</verification>

<success_criteria>
Graph DDL is idempotent, chunk-keyed, instance-isolated, and declares the complete node metadata inventory in the initial table definition.
</success_criteria>

<output>
Create `.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-03-SUMMARY.md` when done.
</output>
