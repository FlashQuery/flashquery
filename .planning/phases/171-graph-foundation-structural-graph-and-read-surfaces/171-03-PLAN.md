---
phase: 171-graph-foundation-structural-graph-and-read-surfaces
plan: 03
type: execute
wave: 2
depends_on:
  - 171-01
files_modified:
  - src/graph/queries.ts
  - src/graph/response.ts
  - src/mcp/tools/graph.ts
  - src/mcp/server.ts
  - src/mcp/tool-metadata.ts
  - src/mcp/tool-help/query_graph.tool.md
  - tests/unit/graph-query.test.ts
  - tests/unit/graph-query-status-filter.test.ts
  - tests/unit/graph-question-lifecycle.test.ts
  - tests/unit/graph-provenance.test.ts
  - tests/integration/graph/query-graph.test.ts
  - tests/integration/graph/provenance-question.test.ts
autonomous: true
requirements:
  - GR-016A
  - GR-017
  - GR-020A
  - GR-024A
must_haves:
  truths:
    - "A read-only `query_graph` MCP tool is registered with metadata and deterministic disabled behavior."
    - "Graph traversal reads are bounded by depth, hop, limit, relation filters, and cycle protection."
    - "Graph reads are instance-isolated and expose expected errors without raw database or LLM payload leakage."
    - "Provenance and question lifecycle read shapes prioritize extracted edges before inferred edges."
    - "Surface-specific inactive filtering defaults match the product docs."
  artifacts:
    - path: "src/mcp/tools/graph.ts"
      provides: "`query_graph` MCP registration"
    - path: "src/graph/queries.ts"
      provides: "Graph read helper contracts"
    - path: "src/graph/response.ts"
      provides: "Graph JSON envelope helpers"
    - path: "src/mcp/tool-metadata.ts"
      provides: "`query_graph` metadata"
    - path: "src/mcp/server.ts"
      provides: "Native graph tool registration"
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/mcp/tools/graph.ts"
      via: "`registerGraphTools(server, config)`"
      pattern: "registerGraphTools"
    - from: "src/mcp/tools/graph.ts"
      to: "src/graph/queries.ts"
      via: "validated action dispatch"
      pattern: "neighbors|path|subgraph|schema|stats"
    - from: "src/graph/queries.ts"
      to: "fqc_graph_edges"
      via: "instance-filtered Supabase/Postgres reads"
      pattern: "instance_id"
---

<objective>
Add the read-only `query_graph` MCP tool and shared graph read helpers for primitive reads, provenance/question shaping, bounded traversal, status filtering, and canonical graph response envelopes.

Purpose: This exposes deterministic graph data through MCP without requiring Tier 3 classification or graph lint/community implementation.
Output: Registered `query_graph`, graph query helpers, metadata/help, and Test Plan §4.3 query/provenance coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-CONTEXT.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-RESEARCH.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-VALIDATION.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md

<interfaces>
From prior plan 171-01, use `src/graph/vocabulary.ts`, `src/graph/edge-validation.ts`, and graph table DDL.
Existing anchors:
- `src/mcp/server.ts` imports tool registration modules and validates registered tools have metadata.
- `src/mcp/tool-metadata.ts` exports `TOOL_METADATA` and `current(...)` metadata helper.
- `src/mcp/utils/response-formats.ts` exports canonical JSON helpers.
- `src/mcp/tools/documents/get.ts` and `src/mcp/tools/compound.ts` show `server.registerTool()` plus Zod inputSchema style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement graph query helpers and canonical envelopes</name>
  <files>src/graph/queries.ts, src/graph/response.ts, tests/unit/graph-query.test.ts, tests/unit/graph-query-status-filter.test.ts, tests/unit/graph-question-lifecycle.test.ts, tests/unit/graph-provenance.test.ts, tests/integration/graph/query-graph.test.ts, tests/integration/graph/provenance-question.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md
    - .planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-PATTERNS.md
    - src/graph/vocabulary.ts
    - src/mcp/utils/response-formats.ts
    - src/mcp/utils/document-connections.ts
    - tests/unit/response-formats.test.ts
  </read_first>
  <behavior>
    - Test Plan T-U-028/T-U-029/T-U-060/T-U-069: invalid action/parameter combinations return expected errors; traversal validates max depth/relation filters; cyclic graphs terminate; schema action reflects loaded vocabulary/config feature flags.
    - Test Plan T-U-030: inactive filtering defaults differ by search/get_document/query/provenance as specified.
    - Test Plan T-U-031/T-U-032 and T-I-017/T-I-028: question lifecycle and provenance read shapes are queryable and extracted edges sort before inferred edges.
    - Test Plan T-I-008/T-I-026/T-I-036/T-I-037/T-I-042: seeded rows return node, edge, neighbor, path, subgraph, stats, schema, contradiction, impact, provenance, stale contradiction, and symmetric-edge behavior.
  </behavior>
  <action>Create `src/graph/queries.ts` with pure action validation plus Supabase/Postgres read helpers for `node`, `edges`, `neighbors`, `path`, `subgraph`, `stats`, `schema`, `provenance_chain`, `impact`, `contradictions`, `weak_paths`, `ungrounded_edges`, `community_for`, `community_members`, and `list_communities`. For community/lint-dependent actions, return contract-shaped empty/not-applicable results until Phase 172 populates stable data. Enforce `instance_id`, `max_depth`, `max_hops`, `limit`, `relations`, `direction`, `include_stale`, `include_resolved`, and document status filters. Implement visited-set/cycle protection and stable sorting; symmetric relations must be stored once but returned from either endpoint. Create graph envelope helpers that wrap existing `jsonToolResult`, `jsonExpectedError`, `jsonRuntimeError`, and `withWarnings` without exposing raw DB errors, raw LLM output, API keys, or database URLs.</action>
  <acceptance_criteria>
    - Source assertion: every helper that reads graph tables accepts `instanceId` and filters on `instance_id`.
    - Source assertion: traversal helpers enforce a configured or hard maximum depth/hops and maintain visited node/edge sets.
    - Behavior assertion: disabled graph and invalid parameter combinations use expected-error envelopes with `isError:false`.
    - Behavior assertion: seeded symmetric `contradicts` or `duplicates` edge is returned querying from either endpoint without a duplicate reverse row.
    - Test command: `npm test -- --run tests/unit/graph-query.test.ts tests/unit/graph-query-status-filter.test.ts tests/unit/graph-question-lifecycle.test.ts tests/unit/graph-provenance.test.ts` passes.
    - Integration command: `npm run test:integration -- --run tests/integration/graph/query-graph.test.ts tests/integration/graph/provenance-question.test.ts` passes or gracefully skips when `.env.test` is not configured.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/graph-query.test.ts tests/unit/graph-query-status-filter.test.ts tests/unit/graph-question-lifecycle.test.ts tests/unit/graph-provenance.test.ts</automated>
    <automated>npm run test:integration -- --run tests/integration/graph/query-graph.test.ts tests/integration/graph/provenance-question.test.ts</automated>
  </verify>
  <done>GR-016A, GR-017, GR-020A, and GR-024A graph read helper behavior is implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register `query_graph` MCP tool, metadata, and help</name>
  <files>src/mcp/tools/graph.ts, src/mcp/server.ts, src/mcp/tool-metadata.ts, src/mcp/tool-help/query_graph.tool.md, tests/unit/graph-query.test.ts, tests/integration/graph/query-graph.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md
    - .planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-CONTEXT.md
    - .planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-PATTERNS.md
    - src/mcp/server.ts
    - src/mcp/tool-metadata.ts
    - src/mcp/tools/documents/get.ts
    - src/mcp/tools/compound.ts
    - src/graph/queries.ts
  </read_first>
  <behavior>
    - Test Plan T-I-009: disabled `query_graph` returns canonical unsupported envelope.
    - Tool metadata assertion: `assertRegisteredToolsHaveMetadata()` passes after `query_graph` registration.
    - GR-024A: success, warning, unsupported, and expected-error envelopes use canonical JSON MCP response conventions.
  </behavior>
  <action>Create `registerGraphTools(server, config)` in `src/mcp/tools/graph.ts` using `server.registerTool('query_graph', ...)` with Zod input schema for graph read actions and common filters. Register it from `src/mcp/server.ts` in the native tool block. Add `query_graph` to `TOOL_METADATA` as a current `doc-read` and `read-only` tool with clear use/do-not-use text. Add `src/mcp/tool-help/query_graph.tool.md` documenting actions, filters, disabled behavior, and Phase 172 not-applicable actions. Choose the discoverable disabled behavior allowed by D-03: keep `query_graph` registered and return a canonical `unsupported` expected-error envelope when graph is disabled.</action>
  <acceptance_criteria>
    - Source assertion: `src/mcp/server.ts` imports and calls `registerGraphTools(server, config)`.
    - Source assertion: `TOOL_METADATA` has a `query_graph` entry before metadata validation runs.
    - Behavior assertion: graph-disabled `query_graph` returns JSON parseable `{ error: "unsupported", ... }` with `isError:false`.
    - Test command: `npm test -- --run tests/unit/graph-query.test.ts` passes.
    - Integration command: `npm run test:integration -- --run tests/integration/graph/query-graph.test.ts` passes or gracefully skips when `.env.test` is not configured.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/graph-query.test.ts</automated>
    <automated>npm run test:integration -- --run tests/integration/graph/query-graph.test.ts</automated>
  </verify>
  <done>`query_graph` is registered, discoverable, metadata-valid, and returns canonical read-only graph responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `query_graph` | Untrusted action parameters and traversal limits enter graph read code. |
| Graph query helpers -> Supabase | Read helpers traverse persisted graph topology across documents/chunks. |
| Database errors -> MCP envelope | Internal failures are converted into user-facing JSON tool results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-11 | Information Disclosure | `src/graph/queries.ts` | mitigate | Every query filters by `instance_id` and tests seed cross-instance rows to ensure isolation. |
| T-171-12 | Denial of Service | traversal actions | mitigate | Enforce `max_depth`, `max_hops`, `limit`, relation filters, and visited-set/cycle protection. |
| T-171-13 | Denial of Service | disabled graph mode | mitigate | Registered disabled `query_graph` returns `unsupported` before DB traversal or hidden graph work. |
| T-171-14 | Information Disclosure | MCP graph envelopes | mitigate | Expected/runtime error helpers redact DB URLs, API keys, raw LLM output, and stack-like payloads. |
| T-171-15 | Tampering | action schema | mitigate | Zod validates action/filter combinations and rejects writes; `query_graph` is read-only. |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed. |
</threat_model>

<verification>
Run focused tests:
- `npm test -- --run tests/unit/graph-query.test.ts tests/unit/graph-query-status-filter.test.ts tests/unit/graph-question-lifecycle.test.ts tests/unit/graph-provenance.test.ts`
- `npm run test:integration -- --run tests/integration/graph/query-graph.test.ts tests/integration/graph/provenance-question.test.ts`
</verification>

<success_criteria>
`query_graph` is an MCP-visible, read-only, bounded, instance-isolated graph read surface with deterministic disabled and expected-error behavior.
</success_criteria>

<output>
Create `.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-03-SUMMARY.md` when done.
</output>
