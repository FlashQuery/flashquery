---
phase: 171-graph-foundation-structural-graph-and-read-surfaces
plan: 04
type: execute
wave: 3
depends_on:
  - 171-02
  - 171-03
files_modified:
  - src/graph/search-expansion.ts
  - src/graph/document-summary.ts
  - src/mcp/tools/compound.ts
  - src/mcp/tools/documents/get.ts
  - src/mcp/utils/document-output.ts
  - src/mcp/utils/document-connections.ts
  - tests/unit/graph-search-ranking.test.ts
  - tests/integration/graph/search-graph-expansion.test.ts
  - tests/integration/graph/get-document-graph.test.ts
autonomous: true
requirements:
  - GR-016A
  - GR-018
  - GR-019
  - GR-020A
  - GR-024A
must_haves:
  truths:
    - "Existing `search` callers see no output shape drift unless graph options are requested."
    - "Graph-expanded search adds connected graph results with deterministic ranking and graph match attribution."
    - "`get_document include:[\"graph_summary\"]` returns graph counts/flags and graph-primary connections."
    - "Graph-aware connection limits are validated with guidance instead of silently mixing legacy and graph options."
    - "Inactive targets are hidden by default for search/get_document and included only by explicit opt-in."
  artifacts:
    - path: "src/mcp/tools/compound.ts"
      provides: "Graph-expanded unified search input and response shaping"
    - path: "src/mcp/tools/documents/get.ts"
      provides: "`graph_summary` include and graph-aware connection schema"
    - path: "src/mcp/utils/document-output.ts"
      provides: "Document envelope graph summary wiring"
    - path: "src/mcp/utils/document-connections.ts"
      provides: "Graph-primary plus embedding-only connection overlays"
    - path: "src/graph/search-expansion.ts"
      provides: "Graph expansion ranking and match attribution"
    - path: "src/graph/document-summary.ts"
      provides: "Document graph summary aggregation"
  key_links:
    - from: "src/mcp/tools/compound.ts"
      to: "src/graph/search-expansion.ts"
      via: "optional graph expansion from semantic seed chunks"
      pattern: "graph"
    - from: "src/mcp/utils/document-output.ts"
      to: "src/graph/document-summary.ts"
      via: "`include:[\"graph_summary\"]`"
      pattern: "graph_summary"
    - from: "src/mcp/utils/document-connections.ts"
      to: "src/graph/queries.ts"
      via: "graph-primary document connections"
      pattern: "graph_limit_per_chunk|include_embedding_only"
---

<objective>
Extend existing read surfaces additively: graph-expanded unified search and graph-aware `get_document` output.

Purpose: Users can retrieve graph context through existing tools without breaking callers that do not opt into graph options.
Output: Search expansion helpers, document graph summary/connections, and Test Plan §4.3 search/get-document coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-CONTEXT.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-RESEARCH.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-VALIDATION.md
@.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md

<interfaces>
From prior plan 171-03, use `src/graph/queries.ts` for instance-filtered graph reads and graph response envelopes.
Existing anchors:
- `src/mcp/tools/compound.ts` registers unified `search` and merges filesystem/semantic results.
- `src/mcp/tools/documents/get.ts` registers `get_document` with `include` enum and `connections` options.
- `src/mcp/utils/document-output.ts` builds `DocumentEnvelope` and validates include/connection options before I/O.
- `src/mcp/utils/document-connections.ts` builds stored-vector document connections.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add graph-expanded unified search without existing-call drift</name>
  <files>src/graph/search-expansion.ts, src/mcp/tools/compound.ts, tests/unit/graph-search-ranking.test.ts, tests/integration/graph/search-graph-expansion.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md
    - .planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-CONTEXT.md
    - .planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-PATTERNS.md
    - src/mcp/tools/compound.ts
    - src/graph/queries.ts
    - tests/unit/search.test.ts
  </read_first>
  <behavior>
    - Test Plan T-U-061: graph-expanded ranking orders by relation significance, non-stale, confidence, then seed relevance.
    - Test Plan T-I-010: existing mixed search output remains backward compatible without graph parameters.
    - Test Plan T-I-011/T-I-012: graph expansion adds edge-connected results with graph match source; disabled graph expansion returns warning or expected error.
    - Test Plan T-I-038: `include_community:true` and `path_to` add bounded metadata or no-path warnings without changing base results.
  </behavior>
  <action>Add optional graph expansion parameters to unified `search` additively. Preserve existing `mode: filesystem|semantic|mixed`, `entity_types`, result fields, and warnings when graph options are absent. When graph expansion is requested, expand only from semantic seed chunks and graph-connected rows, add `match_source` attribution for graph expansion, apply inactive/stale filtering defaults for search, and rank graph results deterministically by relation significance, non-stale status, confidence score, and seed relevance. Disabled graph expansion must return a canonical warning or expected error per the contract, without mutating graph tables or calling LLMs.</action>
  <acceptance_criteria>
    - Source assertion: existing search schema fields and no-graph response keys are preserved when graph options are omitted.
    - Source assertion: graph expansion reads use `instance_id` and bounded limits.
    - Behavior assertion: disabled graph expansion does not call graph traversal helpers that query DB for topology.
    - Behavior assertion: graph-expanded results include graph-specific `match_source` attribution distinguishable from `filesystem` and `semantic`.
    - Test command: `npm test -- --run tests/unit/graph-search-ranking.test.ts tests/unit/search.test.ts` passes.
    - Integration command: `npm run test:integration -- --run tests/integration/graph/search-graph-expansion.test.ts` passes or gracefully skips when `.env.test` is not configured.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/graph-search-ranking.test.ts tests/unit/search.test.ts</automated>
    <automated>npm run test:integration -- --run tests/integration/graph/search-graph-expansion.test.ts</automated>
  </verify>
  <done>GR-018 graph-expanded search is implemented additively with backward compatibility coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add graph-aware `get_document` summary and connections</name>
  <files>src/graph/document-summary.ts, src/mcp/tools/documents/get.ts, src/mcp/utils/document-output.ts, src/mcp/utils/document-connections.ts, tests/integration/graph/get-document-graph.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/Graph-Enhanced-Document-Intelligence Test Plan.md
    - .planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-CONTEXT.md
    - .planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-PATTERNS.md
    - src/mcp/tools/documents/get.ts
    - src/mcp/utils/document-output.ts
    - src/mcp/utils/document-connections.ts
    - src/graph/queries.ts
    - tests/unit/document-output.test.ts
    - tests/unit/document-connections.test.ts
    - tests/integration/embedding/get-document-connections.test.ts
  </read_first>
  <behavior>
    - Test Plan T-I-013: `include:["graph_summary"]` returns document-level graph counts and flags.
    - Test Plan T-I-014/T-I-015: graph-primary connections exclude embedding-only neighbors by default; `include_embedding_only:true` appends embedding-only neighbors after graph-connected targets.
    - Test Plan T-I-016: inactive connection targets are hidden by default and included with opt-in.
    - Test Plan T-I-027/T-I-039: legacy `connections.limit_per_chunk` remains compatible when graph-aware options are absent and returns split-limit validation guidance when graph-aware options are present.
  </behavior>
  <action>Add `graph_summary` to the `get_document` include enum and response builder. Add graph-aware connection options: `graph_limit_per_chunk`, `embedding_limit_per_chunk`, `include_embedding_only`, `include_inactive_targets`, `relations`, and `include_stale`. Build graph-primary connection overlays with `basis`, `direction`, `relation`, `confidence_score`, `reasoning`, and `stale`; append embedding-only neighbors only when `include_embedding_only:true`. Preserve legacy `connections.limit_per_chunk` behavior for non-graph-aware calls, but return an `invalid_input` expected-error with guidance to use split graph/embedding limits when legacy `limit_per_chunk` is combined with graph-aware options.</action>
  <acceptance_criteria>
    - Source assertion: `include` schema accepts `graph_summary`.
    - Source assertion: graph-aware connection options are validated before document I/O where possible.
    - Behavior assertion: existing `include:["connections"]` with legacy options retains its prior shape when no graph options are passed.
    - Behavior assertion: graph-primary connections exclude inactive targets by default and include them only with `include_inactive_targets:true`.
    - Integration command: `npm run test:integration -- --run tests/integration/graph/get-document-graph.test.ts` passes or gracefully skips when `.env.test` is not configured.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/document-output.test.ts tests/unit/document-connections.test.ts</automated>
    <automated>npm run test:integration -- --run tests/integration/graph/get-document-graph.test.ts</automated>
  </verify>
  <done>GR-019 graph-aware `get_document` output is implemented while preserving existing connection behavior for non-graph callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> search/get_document graph options | Untrusted optional graph parameters extend existing public read tools. |
| Search/get_document -> graph topology | Existing read surfaces now join graph rows and document/chunk state. |
| Graph/embedding results -> MCP response | Mixed-origin read data is merged and ranked for users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-16 | Information Disclosure | graph-expanded search | mitigate | All graph expansion reads filter by `instance_id`; inactive targets are hidden by default. |
| T-171-17 | Denial of Service | search graph expansion and `path_to` | mitigate | Use explicit limits, bounded path lookup, and no recursive unbounded expansion. |
| T-171-18 | Denial of Service | graph-disabled options | mitigate | Disabled graph options return warning/expected error before graph DB reads or hidden work. |
| T-171-19 | Tampering | graph-aware connection options | mitigate | Zod/pre-I/O validation rejects conflicting legacy and graph-aware limit options. |
| T-171-20 | Information Disclosure | read-surface errors | mitigate | Use canonical expected/runtime envelopes and do not include raw DB URLs, keys, prompt bodies, or raw LLM payloads. |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed. |
</threat_model>

<verification>
Run focused tests:
- `npm test -- --run tests/unit/graph-search-ranking.test.ts tests/unit/search.test.ts tests/unit/document-output.test.ts tests/unit/document-connections.test.ts`
- `npm run test:integration -- --run tests/integration/graph/search-graph-expansion.test.ts tests/integration/graph/get-document-graph.test.ts`
</verification>

<success_criteria>
Graph-aware `search` and `get_document` work only when requested, preserve old caller contracts by default, and return bounded graph context through canonical MCP envelopes.
</success_criteria>

<output>
Create `.planning/phases/171-graph-foundation-structural-graph-and-read-surfaces/171-04-SUMMARY.md` when done.
</output>
