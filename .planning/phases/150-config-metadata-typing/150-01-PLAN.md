---
phase: 150-config-metadata-typing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/config/loader.ts
  - tests/unit/config-runtime-metadata.test.ts
  - tests/unit/llm-config-sync.test.ts
autonomous: true
requirements:
  - REQ-012
user_setup: []
must_haves:
  truths:
    - "Loaded config warning metadata remains observable through getDeprecationWarnings and getStartupWarnings."
    - "getResolvedHostToolExposure returns stored loader metadata for loaded configs and recomputes from hostMcpTools for manually constructed configs."
    - "getLlmApiKeyRefs returns raw provider api_key references such as ${OPENAI_API_KEY} and never exposes resolved secret values."
    - "src/config/loader.ts no longer attaches or reads selected underscore metadata via broad as unknown as Record<string, unknown> side-channel casts."
  artifacts:
    - path: "src/config/loader.ts"
      provides: "Typed runtime metadata storage plus preserved config accessors"
      exports:
        - "loadConfig"
        - "getDeprecationWarnings"
        - "getStartupWarnings"
        - "getResolvedHostToolExposure"
        - "getLlmApiKeyRefs"
    - path: "tests/unit/config-runtime-metadata.test.ts"
      provides: "T-U-026..T-U-029 unit and static coverage for REQ-012"
    - path: "tests/unit/llm-config-sync.test.ts"
      provides: "Existing sync regression coverage updated if direct _rawLlmApiKeyRefs helper breaks"
  key_links:
    - from: "src/config/loader.ts"
      to: "src/llm/config-sync.ts"
      via: "getLlmApiKeyRefs(config)"
      pattern: "getLlmApiKeyRefs"
    - from: "tests/unit/config-runtime-metadata.test.ts"
      to: "src/config/loader.ts"
      via: "loadConfig and exported accessor imports"
      pattern: "loadConfig|getDeprecationWarnings|getStartupWarnings|getResolvedHostToolExposure|getLlmApiKeyRefs"
    - from: "src/config/loader.ts"
      to: "src/mcp/tool-exposure.ts"
      via: "resolveHostToolExposure fallback"
      pattern: "resolveHostToolExposure\\(config\\.hostMcpTools\\)"
---

<objective>
Remove the runtime-only underscore metadata side channel from config loading while keeping all public config accessor behavior intact.

Purpose: Close REQ-012 from the technical debt remediation requirements without broadening into a config schema rewrite.
Output: Focused REQ-012 unit/static coverage, typed config runtime metadata storage in `src/config/loader.ts`, and green focused/type/lint gates.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/150-config-metadata-typing/150-CONTEXT.md
@.planning/phases/150-config-metadata-typing/150-RESEARCH.md
@.planning/phases/150-config-metadata-typing/150-VALIDATION.md
@.planning/phases/150-config-metadata-typing/150-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md

<interfaces>
From `src/config/loader.ts`:
- `export interface FlashQueryConfig` is the public config shape; do not add public underscore metadata fields unless deliberately documenting an internal extension.
- `export function loadConfig(configPath = './flashquery.yml'): FlashQueryConfig`
- `export function getDeprecationWarnings(config: FlashQueryConfig): string[]`
- `export function getStartupWarnings(config: FlashQueryConfig): string[]`
- `export function getResolvedHostToolExposure(config: FlashQueryConfig): ResolvedHostToolExposure`
- `export function getLlmApiKeyRefs(config: FlashQueryConfig): Map<string, string>`

Current selected side-channel sites are the writes near `src/config/loader.ts` step 10 and the accessor reads immediately below. Replace only `_deprecationWarnings`, `_startupWarnings`, `_resolvedHostToolExposure`, and `_rawLlmApiKeyRefs` broad-cast usage for REQ-012.

From `tests/unit/llm-config-sync.test.ts`:
- `withRawApiKeyRefs(config, refs)` currently writes `_rawLlmApiKeyRefs` directly. Replace it only if needed after production metadata storage changes, preferably by using `loadConfig` in the affected assertion or by importing an intentionally exported narrow test helper if production exposes one.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add REQ-012 failing/targeted unit and static tests</name>
  <files>tests/unit/config-runtime-metadata.test.ts, tests/unit/llm-config-sync.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md` sections `6.5.1`, `7.5`, and `8.8`; consult this before implementation questions.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md` section `4.6.1` and checklist entries `T-U-026` through `T-U-029`; consult this before implementation questions.
    - `.planning/phases/150-config-metadata-typing/150-CONTEXT.md`
    - `.planning/phases/150-config-metadata-typing/150-PATTERNS.md`
    - `.planning/phases/150-config-metadata-typing/150-VALIDATION.md`
    - `src/config/loader.ts`
    - `tests/unit/config.test.ts`
    - `tests/unit/llm-config-sync.test.ts`
  </read_first>
  <behavior>
    - T-U-026: loading YAML that produces deprecation and startup warning metadata makes `getDeprecationWarnings(config)` and `getStartupWarnings(config)` return the loaded metadata through accessors.
    - T-U-027: `getResolvedHostToolExposure(loadConfig(...))` returns the resolved stored metadata for loaded config; `getResolvedHostToolExposure(manualConfig)` still recomputes from `manualConfig.hostMcpTools`.
    - T-U-028: loading YAML with `llm.providers[].api_key: ${OPENAI_API_KEY}` while `process.env.OPENAI_API_KEY` contains a resolved secret returns the raw literal `${OPENAI_API_KEY}` from `getLlmApiKeyRefs(config)` and the map values do not contain the resolved secret.
    - T-U-029: a static assertion against `src/config/loader.ts` fails while any selected `_deprecationWarnings`, `_startupWarnings`, `_resolvedHostToolExposure`, or `_rawLlmApiKeyRefs` line is attached/read through `as unknown as Record<string, unknown>`.
  </behavior>
  <action>Create `tests/unit/config-runtime-metadata.test.ts` using existing Vitest temp YAML patterns from `tests/unit/config.test.ts`. Cover T-U-026 through T-U-029 explicitly in test names. Keep static T-U-029 focused on selected metadata side-channel keys in `src/config/loader.ts`; do not scan the entire repo or fail unrelated casts. If `tests/unit/llm-config-sync.test.ts` needs adjustment for the later production change, leave behavior assertions intact: provider `api_key_ref` must remain `${OPENAI_API_KEY}`, not `sk-resolved-secret`.</action>
  <verify>
    <automated>npm test -- tests/unit/config-runtime-metadata.test.ts tests/unit/llm-config-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/unit/config-runtime-metadata.test.ts` exists and contains visible references to `T-U-026`, `T-U-027`, `T-U-028`, and `T-U-029`.
    - Behavior assertion: T-U-028 asserts `${OPENAI_API_KEY}` is present in `getLlmApiKeyRefs(config)` output and `sk-resolved-secret` is absent from raw ref values.
    - Static assertion: T-U-029 targets the selected underscore metadata keys in `src/config/loader.ts`, not all casts in all source files.
    - Test command assertion: before Task 2, focused tests may be RED only for the expected missing REQ-012 implementation; after Task 2, the same command must exit 0.
  </acceptance_criteria>
  <done>T-U-026..T-U-029 coverage exists, is executable with Vitest, and pins the REQ-012 behavior before production metadata storage is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace selected underscore side-channel casts with typed metadata storage</name>
  <files>src/config/loader.ts, tests/unit/llm-config-sync.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md` sections `6.5.1`, `7.5`, and `8.8`; consult this before implementation questions.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md` section `4.6.1` and checklist entries `T-U-026` through `T-U-029`; consult this before implementation questions.
    - `.planning/phases/150-config-metadata-typing/150-CONTEXT.md`
    - `.planning/phases/150-config-metadata-typing/150-RESEARCH.md`
    - `.planning/phases/150-config-metadata-typing/150-PATTERNS.md`
    - `src/config/loader.ts`
    - `src/llm/config-sync.ts`
    - `tests/unit/config-runtime-metadata.test.ts`
    - `tests/unit/llm-config-sync.test.ts`
  </read_first>
  <behavior>
    - Accessors keep the same output contract for loaded configs.
    - Missing metadata remains tolerant: warning accessors return empty arrays, `getLlmApiKeyRefs` returns an empty `Map`, and host exposure falls back to `resolveHostToolExposure(config.hostMcpTools)`.
    - Raw LLM API key refs are captured before env expansion and remain raw strings.
  </behavior>
  <action>In `src/config/loader.ts`, add a module-local typed metadata store, preferably `WeakMap<FlashQueryConfig, ConfigRuntimeMetadata>`, with fields `deprecationWarnings`, `startupWarnings`, `resolvedHostToolExposure`, and `rawLlmApiKeyRefs`. Replace the selected step-10 underscore writes with one typed metadata write after `resolvedHostToolExposure` is computed and before accessors are needed. Update `getDeprecationWarnings`, `getStartupWarnings`, `getResolvedHostToolExposure`, and `getLlmApiKeyRefs` to read from the typed metadata store, preserving fallback behavior exactly. Do not add `_deprecationWarnings`, `_startupWarnings`, `_resolvedHostToolExposure`, or `_rawLlmApiKeyRefs` to public `FlashQueryConfig`; do not change schema validation, env expansion, LLM capability validation, host exposure selector behavior, or config file path behavior. If the existing sync unit helper still writes `_rawLlmApiKeyRefs`, replace it with a typed path that preserves the existing `syncLlmConfigToDb` raw-ref assertion.</action>
  <verify>
    <automated>npm test -- tests/unit/config-runtime-metadata.test.ts tests/unit/llm-config-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/config/loader.ts` defines a typed metadata abstraction such as `ConfigRuntimeMetadata` plus `WeakMap<FlashQueryConfig, ConfigRuntimeMetadata>` or an equivalently explicit internal/symbol approach allowed by REQ-012.
    - Source assertion: selected metadata keys `_deprecationWarnings`, `_startupWarnings`, `_resolvedHostToolExposure`, and `_rawLlmApiKeyRefs` are no longer attached/read through repeated `as unknown as Record<string, unknown>` casts.
    - Behavior assertion: `getResolvedHostToolExposure` still calls `resolveHostToolExposure(config.hostMcpTools)` when metadata is absent.
    - Behavior assertion: `getLlmApiKeyRefs(config)` returns a new empty `Map` when metadata is absent and returns the raw loaded refs when metadata exists.
    - Test command assertion: `npm test -- tests/unit/config-runtime-metadata.test.ts tests/unit/llm-config-sync.test.ts` exits 0.
  </acceptance_criteria>
  <done>REQ-012 production behavior is implemented without selected broad side-channel casts, while existing config accessor and LLM sync behavior remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Run final REQ-012 gates and record completion evidence</name>
  <files>src/config/loader.ts, tests/unit/config-runtime-metadata.test.ts, tests/unit/llm-config-sync.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md` sections `6.5.1`, `7.5`, and `8.8`; consult this before implementation questions.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md` section `4.6.1` and checklist entries `T-U-026` through `T-U-029`; consult this before implementation questions.
    - `.planning/phases/150-config-metadata-typing/150-VALIDATION.md`
    - `src/config/loader.ts`
    - `tests/unit/config-runtime-metadata.test.ts`
    - `tests/unit/llm-config-sync.test.ts`
  </read_first>
  <action>Run the focused config tests, selected cast-removal grep, typecheck, and lint. If any gate fails, fix only the REQ-012 files in this plan and rerun the failed gate. Create `.planning/phases/150-config-metadata-typing/150-01-SUMMARY.md` with the commands run, whether T-U-026..T-U-029 passed, and any intentional residuals. Do not add a schema push task because no schema-relevant files are in scope.</action>
  <verify>
    <automated>npm test -- tests/unit/config-runtime-metadata.test.ts tests/unit/llm-config-sync.test.ts &amp;&amp; ! rg -n "as unknown as Record&lt;string, unknown&gt;.*_(deprecationWarnings|startupWarnings|resolvedHostToolExposure|rawLlmApiKeyRefs)|_(deprecationWarnings|startupWarnings|resolvedHostToolExposure|rawLlmApiKeyRefs).*as unknown as Record&lt;string, unknown&gt;" src/config/loader.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: focused config tests exit 0.
    - CLI assertion: selected cast-removal grep returns no matches for the four metadata keys in `src/config/loader.ts`.
    - CLI assertion: `npm run typecheck` exits 0.
    - CLI assertion: `npm run lint` exits 0.
    - Artifact assertion: `.planning/phases/150-config-metadata-typing/150-01-SUMMARY.md` exists and records T-U-026, T-U-027, T-U-028, T-U-029, typecheck, lint, and the selected grep result.
  </acceptance_criteria>
  <done>All REQ-012 gates are green and the execution summary contains reproducible evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config file -> config loader | User-authored config is parsed, env-expanded, normalized, and annotated with runtime metadata. |
| Config loader -> LLM config sync | Raw API key references cross into DB persistence through `getLlmApiKeyRefs`; resolved secrets must not cross. |
| Config loader -> MCP host exposure | Host tool exposure decisions cross from config metadata into MCP tool registration and delegated surfaces. |
| Public `FlashQueryConfig` consumers -> runtime metadata | External callers receive a public config object and must not depend on hidden underscore fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Information Disclosure | `getLlmApiKeyRefs`, `syncLlmConfigToDb` | mitigate | Preserve raw pre-expansion refs in typed metadata; T-U-028 must assert `${OPENAI_API_KEY}` is stored and resolved `sk-resolved-secret` is absent. |
| T-150-02 | Elevation of Privilege | `getResolvedHostToolExposure` | mitigate | Preserve stored loaded exposure and fallback recomputation from `config.hostMcpTools`; T-U-027 must cover both paths to prevent host tool exposure drift. |
| T-150-03 | Tampering | `FlashQueryConfig` public shape | mitigate | Store runtime metadata outside public underscore fields or via documented internal typing; T-U-029 must prove selected broad side-channel casts are gone. |
| T-150-04 | Repudiation | Config warning accessors | mitigate | Preserve deprecation/startup warning accessors; T-U-026 must prove warning metadata remains observable after load. |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; no package legitimacy checkpoint is required. |
</threat_model>

<verification>
Overall phase checks:
- `npm test -- tests/unit/config-runtime-metadata.test.ts tests/unit/llm-config-sync.test.ts`
- `rg -n "as unknown as Record<string, unknown>.*_(deprecationWarnings|startupWarnings|resolvedHostToolExposure|rawLlmApiKeyRefs)|_(deprecationWarnings|startupWarnings|resolvedHostToolExposure|rawLlmApiKeyRefs).*as unknown as Record<string, unknown)" src/config/loader.ts` returns no matches.
- `npm run typecheck`
- `npm run lint`
</verification>

<success_criteria>
- REQ-012 appears in this plan frontmatter and is fully covered by T-U-026..T-U-029.
- Public `FlashQueryConfig` consumers keep the same external config shape.
- Selected runtime metadata no longer uses repeated underscore-field broad casts in `src/config/loader.ts`.
- Config accessors preserve warning, host exposure, fallback, and raw API key reference behavior.
- No schema push task is added.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Remove runtime-only underscore side-channel casts while preserving config accessor behavior | Covered by Tasks 1-3 |
| REQ | REQ-012 | Covered by Tasks 1-3 and frontmatter `requirements` |
| RESEARCH | Use typed metadata storage, preserve raw refs, no new packages, avoid config rewrite | Covered by Tasks 2-3 |
| CONTEXT | Locked scope: replace four selected metadata side-channel reads/writes | Covered by Tasks 1-2 |
| CONTEXT | Preserve accessors and raw API key refs | Covered by Tasks 1-2 |
| CONTEXT | Add T-U-026..T-U-029 unit/static coverage | Covered by Task 1 |
| CONTEXT | Deferred: broader config rewrite and unrelated cast removal | Excluded explicitly in Tasks 1-2 |
</source_audit>

<output>
Create `.planning/phases/150-config-metadata-typing/150-01-SUMMARY.md` when done.
</output>
