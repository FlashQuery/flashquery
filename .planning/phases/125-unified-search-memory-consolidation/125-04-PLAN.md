---
phase: 125-unified-search-memory-consolidation
plan: 04
type: execute
wave: 4
depends_on: [125-02, 125-03]
files_modified:
  - tests/integration/search.integration.test.ts
  - tests/integration/write-memory.integration.test.ts
  - tests/e2e/protocol.test.ts
  - src/mcp/tools/compound.ts
  - src/mcp/tools/memory.ts
  - .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, MEM-01, MEM-02, MEM-03, MEM-04]
must_haves:
  truths:
    - "write_memory(create/update) composes with search and get_memory through Supabase-backed tests."
    - "search returns document and memory identification shapes in one JSON results list."
    - "archived documents and memories are excluded by default and included only when requested."
    - "E2E protocol tests exercise final tool names."
  artifacts:
    - path: "tests/integration/search.integration.test.ts"
      provides: "unified search integration coverage"
    - path: "tests/e2e/protocol.test.ts"
      provides: "MCP round trips for search/write_memory/get_memory/archive_memory"
---

<objective>
Prove final search and memory tools through integration and MCP protocol workflows.

Purpose: verify the contracts against DB/filesystem behavior, not only handler mocks.
Output: integration suites, E2E protocol coverage, and updated traceability evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-unified-search-memory-consolidation/125-CONTEXT.md
@.planning/phases/125-unified-search-memory-consolidation/125-RESEARCH.md
@.planning/phases/125-unified-search-memory-consolidation/125-PATTERNS.md
@.planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
@tests/integration/search-all.integration.test.ts
@tests/integration/save-memory-tags.test.ts
@tests/integration/write-document.integration.test.ts
@tests/e2e/protocol.test.ts

<interfaces>
Executors must read the two product docs above before making any requirement or test-scope decision. If a question remains after those docs, phase artifacts, and code analogs, ask the user.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Unified Search Integration Suite</name>
  <files>tests/integration/search.integration.test.ts, tests/integration/search-all.integration.test.ts</files>
  <read_first>
    - tests/integration/search-all.integration.test.ts
    - tests/integration/write-document.integration.test.ts
    - tests/integration/documents.integration.test.ts
    - tests/integration/write-memory.integration.test.ts
    - tests/helpers/supabase.ts
  </read_first>
  <action>Create `tests/integration/search.integration.test.ts` by porting `search-all.integration.test.ts` to final `search` calls. Cover document filesystem search, memory list-mode without semantic provider call, mixed document+memory search under one global limit, archived document/memory exclusion by default, `include_archived:true`, explicit disabled memory domain `unsupported`, and embedding unavailable fallback behavior where fixture stubs allow it.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/search.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/search.integration.test.ts` contains `entity_types`.
    - `tests/integration/search.integration.test.ts` contains `mode: 'filesystem'` or `mode: "filesystem"`.
    - `tests/integration/search.integration.test.ts` contains `include_archived`.
    - `tests/integration/search.integration.test.ts` contains `global limit`.
    - Document fixtures do not pass divergent top-level `tags` and `frontmatter.fq_tags` to `write_document`.
  </acceptance_criteria>
  <done>SRCH-01..SRCH-06 are covered through integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MCP Protocol Round Trips For Final Tools</name>
  <files>tests/e2e/protocol.test.ts</files>
  <read_first>
    - tests/e2e/protocol.test.ts
    - tests/helpers/mcp-server-fixture.ts
    - tests/fixtures/flashquery.e2e.yaml
    - tests/fixtures/flashquery.e2e.host-filtered.yaml
  </read_first>
  <action>Update `tests/e2e/protocol.test.ts` to assert final tool discovery includes `search`, `write_memory`, `get_memory`, and `archive_memory`. Add round trips: `write_memory(mode:"create") -> search(entity_types:["memories"], query:"", tags:[...])`; `write_memory(mode:"update") -> get_memory` returns new memory_id and previous linkage when included; `search(entity_types:["documents"], mode:"filesystem")` parses JSON; mixed `search` returns document and memory identification shapes; disabled explicit memory domain returns JSON `unsupported` with `isError:false` under host-filtered config. When document setup uses `write_document`, avoid conflicting `tags` and `frontmatter.fq_tags`. Keep older legacy-tool E2E assertions only if Phase 128 still needs coexistence checks.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/protocol.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/protocol.test.ts` contains `write_memory`.
    - `tests/e2e/protocol.test.ts` contains `search`.
    - `tests/e2e/protocol.test.ts` contains `JSON.parse`.
    - `tests/e2e/protocol.test.ts` contains `archive_memory`.
  </acceptance_criteria>
  <done>MCP clients can exercise final Phase 125 tools end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Update Traceability With Integration Evidence</name>
  <files>.planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md</files>
  <read_first>
    - .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
    - tests/integration/search.integration.test.ts
    - tests/integration/write-memory.integration.test.ts
    - tests/e2e/protocol.test.ts
  </read_first>
  <action>Update TRACEABILITY rows for SRCH-01 through SRCH-06 and MEM-01 through MEM-04 to cite the implemented integration and E2E tests from this plan. Keep scenario columns pending until Plan 05. Mark unit/integration/E2E evidence complete where corresponding commands passed.</action>
  <verify>
    <automated>grep -E 'search.integration.test.ts|write-memory.integration.test.ts|protocol.test.ts' .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `TRACEABILITY.md` contains `tests/integration/search.integration.test.ts`.
    - `TRACEABILITY.md` contains `tests/integration/write-memory.integration.test.ts`.
    - `TRACEABILITY.md` contains `tests/e2e/protocol.test.ts`.
  </acceptance_criteria>
  <done>Traceability reflects implemented integration and E2E evidence.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-08 | Integrity | integration result visibility | mitigate | Integration tests verify write -> search -> get and archive -> search visibility transitions. |
</threat_model>

<verification>
`npm run test:integration -- tests/integration/search.integration.test.ts tests/integration/write-memory.integration.test.ts`
`npm run test:e2e -- tests/e2e/protocol.test.ts`
</verification>

<success_criteria>
- Final search/memory workflows pass integration tests.
- E2E protocol suite exercises final Phase 125 tools and parses JSON envelopes.
- Traceability rows cite unit, integration, and E2E evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/125-unified-search-memory-consolidation/125-04-SUMMARY.md`.
</output>
