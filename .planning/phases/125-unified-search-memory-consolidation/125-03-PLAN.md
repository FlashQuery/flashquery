---
phase: 125-unified-search-memory-consolidation
plan: 03
type: execute
wave: 3
depends_on: [125-01, 125-02]
files_modified:
  - src/mcp/tools/compound.ts
  - src/mcp/tools/documents.ts
  - src/mcp/tools/memory.ts
  - src/mcp/tool-metadata.ts
  - src/mcp/utils/search-results.ts
  - tests/unit/search.test.ts
  - tests/unit/tool-metadata.test.ts
  - tests/unit/tool-exposure.test.ts
  - .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06]
must_haves:
  truths:
    - "search is the final unified document/memory search surface."
    - "search validates filesystem/semantic/mixed/list-mode inputs exactly as the product spec requires."
    - "search applies one global limit after merge, dedupe, sort, and deterministic tie-breaking."
    - "search handles disabled doc-read/memory domains with silent narrowing, warnings, or unsupported errors as specified."
  artifacts:
    - path: "src/mcp/tools/compound.ts"
      provides: "registered final search tool"
      contains: "'search'"
    - path: "src/mcp/utils/search-results.ts"
      provides: "unit-testable search validation and merge helpers"
    - path: "tests/unit/search.test.ts"
      provides: "SRCH-01..SRCH-06 unit coverage"
---

<objective>
Implement the final `search` primitive and its unit-tested validation/merge behavior.

Purpose: replace legacy domain-specific search surfaces with a structured JSON tool that supports filesystem, semantic, mixed, and list-mode behavior across documents and memories.
Output: registered `search`, final metadata, pure helper tests, and updated traceability evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-unified-search-memory-consolidation/125-CONTEXT.md
@.planning/phases/125-unified-search-memory-consolidation/125-RESEARCH.md
@.planning/phases/125-unified-search-memory-consolidation/125-PATTERNS.md
@.planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
@src/mcp/tools/compound.ts
@src/mcp/tools/documents.ts
@src/mcp/tools/memory.ts
@src/mcp/tool-metadata.ts
@src/mcp/utils/response-formats.ts

<interfaces>
Executors must read the two product docs above before making any requirement or test-scope decision. If a question remains after those docs, phase artifacts, and code analogs, ask the user.

Final `search` output:
```json
{
  "query": "planning",
  "entity_types": ["documents", "memories"],
  "mode": "mixed",
  "total": 2,
  "warnings": [],
  "results": []
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Search Validation And Merge Unit Helpers</name>
  <files>src/mcp/utils/search-results.ts, tests/unit/search.test.ts</files>
  <read_first>
    - src/mcp/tools/compound.ts
    - tests/unit/search-all.test.ts
    - tests/unit/search-documents.test.ts
    - tests/unit/search-memory-list.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <behavior>
    - `mode` accepts only `filesystem`, `semantic`, or `mixed`; omitted mode defaults to `mixed`.
    - Empty query with no filters returns `invalid_input` unless `list_all:true`.
    - Empty query with tags or path_filter enters list-mode and requires explicit entity_types.
    - Semantic mode with empty query returns `invalid_input`.
    - Mixed-mode dedupes by `fq_id` / `memory_id`, keeps highest score, and aggregates `match_source`.
    - Global `limit` is applied after merge/dedupe/sort, not per entity type.
    - Tie-break order is deterministic by `entity_type` then identifier/path.
  </behavior>
  <action>Create `src/mcp/utils/search-results.ts` with pure helpers for validation, mode/list intent resolution, entity type normalization, result key extraction, mixed dedupe, sort, warning aggregation, and final limit application. Create `tests/unit/search.test.ts` by porting relevant assertions from `search-all.test.ts`, `search-documents.test.ts`, and `search-memory-list.test.ts`, then add the product Test Plan §4.1 U1-U12 cases where pure helpers can cover them.</action>
  <verify>
    <automated>npm test -- tests/unit/search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/utils/search-results.ts` contains `resolveSearchMode`.
    - `src/mcp/utils/search-results.ts` contains `mergeSearchResults`.
    - `tests/unit/search.test.ts` contains `list_all`.
    - `tests/unit/search.test.ts` contains `global limit`.
    - `tests/unit/search.test.ts` contains `memory_category_disabled`.
  </acceptance_criteria>
  <done>Search validation and ranking core are unit-testable outside the MCP handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register Final search Handler</name>
  <files>src/mcp/tools/compound.ts, src/mcp/tool-metadata.ts, tests/unit/search.test.ts, tests/unit/tool-metadata.test.ts, tests/unit/tool-exposure.test.ts</files>
  <read_first>
    - src/mcp/tools/compound.ts
    - src/mcp/tools/documents.ts
    - src/mcp/tools/memory.ts
    - src/mcp/tool-metadata.ts
    - src/mcp/utils/search-results.ts
    - src/mcp/utils/response-formats.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
  </read_first>
  <action>Register final `search` in `src/mcp/tools/compound.ts` using the helper module. Reuse `searchDocumentsSemantic`, `listMarkdownFiles`, `parseDocMeta`, and `searchMemoriesSemantic`. Add memory list-mode DB queries filtered by `status:'active'`, `is_latest:true`, tags, plugin_scope where relevant, and `include_archived:false` by default. Return `jsonToolResult({ query, entity_types, mode, total, warnings?, results })`. Promote `search` from future to current/final in `src/mcp/tool-metadata.ts` and replace `D.search` with the literal product description from Requirements §4.12. Keep legacy search tool registrations unless this plan explicitly ports/updates the associated tests; broad final absence remains Phase 128.</action>
  <verify>
    <automated>npm test -- tests/unit/search.test.ts tests/unit/tool-metadata.test.ts tests/unit/tool-exposure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/tools/compound.ts` contains `server.registerTool(` and `'search'`.
    - `src/mcp/tools/compound.ts` contains `jsonToolResult`.
    - `src/mcp/tool-metadata.ts` contains `current('search'`.
    - `tests/unit/tool-exposure.test.ts` contains `search`.
  </acceptance_criteria>
  <done>Final `search` is exposed and unit-covered without regressing metadata/tool exposure rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover Disabled Domains And Embedding Fallbacks</name>
  <files>src/mcp/tools/compound.ts, src/mcp/utils/search-results.ts, tests/unit/search.test.ts, tests/unit/tool-exposure.test.ts</files>
  <read_first>
    - src/mcp/tool-metadata.ts
    - tests/unit/tool-exposure.test.ts
    - tests/fixtures/flashquery.e2e.host-filtered.yaml
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
  </read_first>
  <action>Add explicit unit coverage for cross-domain degradation: omitted `entity_types` narrows to enabled searchable domains; explicit `["documents","memories"]` with memory disabled returns a `warnings:["memory_category_disabled"]` payload and document results; explicit `["memories"]` with memory disabled returns `{ error:"unsupported", identifier:"memories", details:{ disabled_category:"memory" } }` with `isError:false`. Add embedding unavailable coverage: mixed/filesystem can fall back with warning when filesystem/tag criteria exist; explicit semantic with no provider and no fallback returns `unsupported`.</action>
  <verify>
    <automated>npm test -- tests/unit/search.test.ts tests/unit/tool-exposure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/search.test.ts` contains `unsupported`.
    - `tests/unit/search.test.ts` contains `embedding`.
    - `tests/unit/search.test.ts` contains `memory_category_disabled`.
  </acceptance_criteria>
  <done>SRCH-06 and embedding fallback semantics are covered at the unit level.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-06 | Information Disclosure | disabled-domain search | mitigate | Use metadata/category eligibility to omit or reject unavailable domains without leaking disabled memory data. |
| T-125-07 | Denial of Service | unbounded list-mode | mitigate | Require explicit `list_all:true` for empty unfiltered listings and apply final global `limit`. |
| T-125-08 | Integrity | ranking/dedupe | mitigate | Pure merge helper unit tests prove deterministic result ordering and source aggregation. |
</threat_model>

<verification>
`npm test -- tests/unit/search.test.ts tests/unit/tool-metadata.test.ts tests/unit/tool-exposure.test.ts`
</verification>

<success_criteria>
- `search` is current/final metadata and registered.
- Search validation, list-mode, disabled-domain behavior, fallback behavior, and global ranking are unit-covered.
- New output is JSON, not markdown/prose sections.
</success_criteria>

<output>
After completion, create `.planning/phases/125-unified-search-memory-consolidation/125-03-SUMMARY.md`.
</output>
