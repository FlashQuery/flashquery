---
phase: 125-unified-search-memory-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
  - src/storage/supabase.ts
  - src/utils/schema-migration.ts
  - src/mcp/utils/response-formats.ts
  - src/mcp/utils/memory-output.ts
  - tests/unit/supabase.test.ts
  - tests/unit/schema-migration.test.ts
  - tests/unit/response-formats.test.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, MEM-01, MEM-02, MEM-03, MEM-04]
must_haves:
  truths:
    - "TRACEABILITY.md maps every Phase 125 requirement to unit, integration, E2E, directed, and integration scenario evidence."
    - "fqc_memory schema has idempotent is_latest and archived_at migrations."
    - "Existing memory version chains are backfilled so parents with child versions are not latest."
    - "Memory output helpers produce canonical identification blocks and expected error envelopes."
  artifacts:
    - path: ".planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md"
      provides: "SRCH-01..SRCH-06 and MEM-01..MEM-04 five-layer evidence table"
    - path: "src/storage/supabase.ts"
      provides: "memory schema support for is_latest and archived_at"
      contains: "ALTER TABLE IF EXISTS fqc_memory ADD COLUMN IF NOT EXISTS is_latest"
    - path: "src/mcp/utils/memory-output.ts"
      provides: "memory identification/include/error helper surface for Phase 125 tools"
  key_links:
    - from: "src/mcp/tools/memory.ts"
      to: "src/mcp/utils/memory-output.ts"
      via: "planned helper import in Plan 02"
      pattern: "memory-output"
    - from: "src/mcp/tools/compound.ts"
      to: "src/storage/supabase.ts"
      via: "search latest/archived filters in Plan 03"
      pattern: "is_latest|archived_at"
---

<objective>
Create the Phase 125 traceability foundation and memory schema required by the unified search and memory contracts.

Purpose: establish evidence tracking before code changes and unblock `write_memory`, `get_memory`, `archive_memory`, and `search` latest/archived behavior.
Output: TRACEABILITY.md, memory schema migration, and shared memory output helpers.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/125-unified-search-memory-consolidation/125-CONTEXT.md
@.planning/phases/125-unified-search-memory-consolidation/125-RESEARCH.md
@.planning/phases/125-unified-search-memory-consolidation/125-VALIDATION.md
@.planning/phases/125-unified-search-memory-consolidation/125-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
@src/storage/supabase.ts
@src/mcp/utils/response-formats.ts
@tests/unit/supabase.test.ts
@tests/unit/response-formats.test.ts

<interfaces>
Executors must read the two product docs above before making any requirement or test-scope decision. If a question remains after those docs, phase artifacts, and code analogs, ask the user.

Required memory identification block:
```typescript
{
  memory_id: string;
  content_preview: string;
  tags: string[];
  plugin_scope: string;
  created_at: string;
  updated_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instantiate Phase 125 Traceability Before Coding</name>
  <files>.planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md</files>
  <read_first>
    - .planning/phases/125-unified-search-memory-consolidation/125-CONTEXT.md
    - .planning/phases/125-unified-search-memory-consolidation/125-VALIDATION.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <action>Create `.planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md` before modifying production code. Include rows for `SRCH-01`, `SRCH-02`, `SRCH-03`, `SRCH-04`, `SRCH-05`, `SRCH-06`, `MEM-01`, `MEM-02`, `MEM-03`, and `MEM-04`. Columns must be `Requirement`, `Product source`, `Unit`, `Integration`, `E2E`, `Directed scenario`, `Integration scenario`, `Status`. Populate planned evidence with `tests/unit/search.test.ts`, `tests/integration/search.integration.test.ts`, `tests/unit/write-memory.test.ts`, `tests/integration/write-memory.integration.test.ts`, `tests/unit/get-memory.test.ts`, `tests/e2e/protocol.test.ts`, and scenario labels `D-search-*`, `D-wmem-*`, `INT-search-*`, `INT-wmem-*` from the product test plan. Set initial status to `pending`.</action>
  <verify>
    <automated>test -f .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md && grep -E 'SRCH-01|SRCH-02|SRCH-03|SRCH-04|SRCH-05|SRCH-06|MEM-01|MEM-02|MEM-03|MEM-04' .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md | wc -l | grep -E '10|[1-9][0-9]+'</automated>
  </verify>
  <acceptance_criteria>
    - `TRACEABILITY.md` contains the exact string `SRCH-01`.
    - `TRACEABILITY.md` contains the exact string `SRCH-06`.
    - `TRACEABILITY.md` contains the exact string `MEM-04`.
    - `TRACEABILITY.md` contains `tests/unit/search.test.ts`.
    - `TRACEABILITY.md` contains `tests/unit/write-memory.test.ts`.
  </acceptance_criteria>
  <done>Traceability exists before production code changes and maps every Phase 125 requirement to five verification layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Memory Schema Columns And Backfill</name>
  <files>src/storage/supabase.ts, src/utils/schema-migration.ts, tests/unit/supabase.test.ts, tests/unit/schema-migration.test.ts</files>
  <read_first>
    - src/storage/supabase.ts
    - src/utils/schema-migration.ts
    - tests/unit/supabase.test.ts
    - tests/unit/schema-migration.test.ts
    - .planning/phases/125-unified-search-memory-consolidation/125-RESEARCH.md
  </read_first>
  <action>Add idempotent DDL to `buildSchemaDDL` so `fqc_memory` has `is_latest BOOLEAN DEFAULT true` and `archived_at TIMESTAMPTZ`. Add `ALTER TABLE IF EXISTS fqc_memory ADD COLUMN IF NOT EXISTS is_latest BOOLEAN DEFAULT true;` and `ALTER TABLE IF EXISTS fqc_memory ADD COLUMN IF NOT EXISTS archived_at TIMESTAMPTZ;`. Add idempotent backfill SQL: set null `is_latest` to true, then set a parent row `is_latest = false` when another row has `previous_version_id = parent.id`. Add an index supporting default visibility, for example `CREATE INDEX IF NOT EXISTS idx_fqc_memory_latest_status ON fqc_memory (instance_id, status, is_latest);`. Add stable SQL constants to `src/utils/schema-migration.ts` only if that keeps tests readable.</action>
  <verify>
    <automated>npm test -- tests/unit/supabase.test.ts tests/unit/schema-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/storage/supabase.ts` contains `is_latest BOOLEAN DEFAULT true`.
    - `src/storage/supabase.ts` contains `archived_at TIMESTAMPTZ`.
    - `src/storage/supabase.ts` contains `previous_version_id = parent.id`.
    - `src/storage/supabase.ts` contains `idx_fqc_memory_latest_status`.
    - `tests/unit/supabase.test.ts` contains `is_latest`.
  </acceptance_criteria>
  <done>Memory schema supports latest-version and archival semantics required by Phase 125.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Memory Output Helpers</name>
  <files>src/mcp/utils/memory-output.ts, src/mcp/utils/response-formats.ts, tests/unit/response-formats.test.ts, tests/unit/get-memory.test.ts</files>
  <read_first>
    - src/mcp/utils/response-formats.ts
    - src/mcp/utils/document-write.ts
    - src/mcp/utils/document-output.ts
    - tests/unit/response-formats.test.ts
    - tests/unit/get-memory.test.ts
  </read_first>
  <action>Create `src/mcp/utils/memory-output.ts` with helpers for content preview generation, memory identification payload construction, include handling for `include: ["content", "tags_full"]`, ordered batch result assembly, and canonical expected error envelopes. Use `memoryIdentification`, `jsonToolResult`, and `jsonExpectedError` from `response-formats.ts`. Define helper types for memory DB rows including `id`, `content`, `tags`, `plugin_scope`, `created_at`, `updated_at`, `version`, `previous_version_id`, `is_latest`, and `archived_at`.</action>
  <verify>
    <automated>npm test -- tests/unit/response-formats.test.ts tests/unit/get-memory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/utils/memory-output.ts` contains `buildMemoryResult`.
    - `src/mcp/utils/memory-output.ts` contains `content_preview`.
    - `src/mcp/utils/memory-output.ts` contains `tags_full`.
    - `src/mcp/utils/memory-output.ts` contains `jsonExpectedError`.
  </acceptance_criteria>
  <done>Memory tools can share final JSON envelope construction in later plans.</done>
</task>

<task type="auto">
  <name>Task 4: Run Blocking Schema Push</name>
  <files>src/storage/supabase.ts</files>
  <read_first>
    - src/storage/supabase.ts
    - .env.test.example
  </read_first>
  <action>[BLOCKING] Run the database schema push/migration path required by this repo after the DDL changes are complete. Because FlashQuery stores idempotent DDL in `buildSchemaDDL`, use the existing startup/test migration path or project-local Supabase setup documented in `docs/ARCHITECTURE.md`. If `.env.test` is unavailable or Supabase credentials are missing, record the skip in `125-01-SUMMARY.md` with the exact missing variable and keep integration tests dependency-gated.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `125-01-SUMMARY.md` records either schema push success or exact dependency-gated skip reason.
  </acceptance_criteria>
  <done>Schema changes are pushed or explicitly dependency-gated before downstream memory/search plans rely on them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> memory helpers | Untrusted IDs, include values, and mode parameters become structured output/errors in later plans. |
| FlashQuery DDL -> Supabase | Idempotent schema migration modifies memory table shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Tampering | memory latest backfill | mitigate | Backfill only derived `is_latest` from explicit `previous_version_id` links; do not change content/tags/status. |
| T-125-02 | Information Disclosure | memory output helpers | mitigate | Default helper output is identification plus preview; full content only when include contains `content`. |
| T-125-03 | Repudiation | traceability | mitigate | TRACEABILITY.md is created before coding and later plans update evidence rows. |
</threat_model>

<verification>
`npm test -- tests/unit/supabase.test.ts tests/unit/schema-migration.test.ts tests/unit/response-formats.test.ts tests/unit/get-memory.test.ts`
`npm run build`
</verification>

<success_criteria>
- Traceability exists for all Phase 125 requirements.
- `fqc_memory` schema supports `is_latest` and `archived_at`.
- Shared memory output helpers exist and are unit-covered.
- Schema push/build gate is completed or explicitly dependency-gated.
</success_criteria>

<output>
After completion, create `.planning/phases/125-unified-search-memory-consolidation/125-01-SUMMARY.md`.
</output>
