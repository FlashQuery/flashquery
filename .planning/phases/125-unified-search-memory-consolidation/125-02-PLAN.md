---
phase: 125-unified-search-memory-consolidation
plan: 02
type: execute
wave: 2
depends_on: [125-01]
files_modified:
  - src/mcp/tools/memory.ts
  - src/mcp/tool-metadata.ts
  - src/mcp/utils/memory-output.ts
  - tests/unit/write-memory.test.ts
  - tests/unit/get-memory.test.ts
  - tests/unit/memory-tools.test.ts
  - tests/unit/tool-metadata.test.ts
  - tests/integration/write-memory.integration.test.ts
  - .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-04]
must_haves:
  truths:
    - "write_memory(mode:\"create\") replaces save_memory semantics with JSON output and is_latest:true."
    - "write_memory(mode:\"update\") creates a new latest version and rejects non-latest updates with conflict."
    - "get_memory uses memory_ids single-or-array input, include vocabulary, and ordered JSON results."
    - "archive_memory uses memory_ids single-or-array input, archives full chains, and preserves archived_at on re-archive."
  artifacts:
    - path: "src/mcp/tools/memory.ts"
      provides: "final memory write/read/archive handlers"
      contains: "write_memory"
    - path: "tests/unit/write-memory.test.ts"
      provides: "write_memory high-risk contract coverage"
    - path: "tests/integration/write-memory.integration.test.ts"
      provides: "version-chain persistence and archive visibility coverage"
---

<objective>
Implement final memory writer, reader, and archive contracts for MEM-01 through MEM-04.

Purpose: make memory lifecycle behavior structured, version-safe, batch-capable where required, and ready for unified search.
Output: `write_memory`, migrated `get_memory`, migrated `archive_memory`, metadata, and focused unit/integration coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-unified-search-memory-consolidation/125-CONTEXT.md
@.planning/phases/125-unified-search-memory-consolidation/125-RESEARCH.md
@.planning/phases/125-unified-search-memory-consolidation/125-PATTERNS.md
@.planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
@src/mcp/tools/memory.ts
@src/mcp/tool-metadata.ts
@src/mcp/utils/memory-output.ts
@src/mcp/utils/response-formats.ts
@tests/unit/memory-tools.test.ts
@tests/unit/get-memory.test.ts

<interfaces>
Executors must read the two product docs above before making any requirement or test-scope decision. If a question remains after those docs, phase artifacts, and code analogs, ask the user.

Final `write_memory` input:
```typescript
{
  mode: 'create' | 'update';
  content?: string;
  memory_id?: string;
  tags?: string[];
  plugin_scope?: string;
  include?: Array<'content' | 'tags_full'>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add write_memory Unit Contract</name>
  <files>tests/unit/write-memory.test.ts, src/mcp/tools/memory.ts, src/mcp/utils/memory-output.ts</files>
  <read_first>
    - src/mcp/tools/memory.ts
    - src/mcp/utils/memory-output.ts
    - tests/unit/memory-tools.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <behavior>
    - Missing or unknown `mode` returns `invalid_input`.
    - Create requires `content`, defaults `plugin_scope` to `global`, rejects generated fields, validates tags, and returns memory identification.
    - Update requires `memory_id` and at least one of `content` or `tags`.
    - Array-like rich mutation payloads are rejected.
    - Non-latest update returns `conflict` with `isError:false`.
    - Update flips previous `is_latest:false` and inserts new `is_latest:true`.
  </behavior>
  <action>Create `tests/unit/write-memory.test.ts` by porting relevant `save_memory` and `update_memory` assertions from `tests/unit/memory-tools.test.ts` into final `write_memory` calls. Add tests for mode validation, generated-field rejection, non-latest conflict, transaction/update calls, tag replacement, plugin_scope default, include content behavior, and JSON parseability.</action>
  <verify>
    <automated>npm test -- tests/unit/write-memory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/write-memory.test.ts` contains `mode is required`.
    - `tests/unit/write-memory.test.ts` contains `is_latest`.
    - `tests/unit/write-memory.test.ts` contains `previous_version_id`.
    - `tests/unit/write-memory.test.ts` contains `conflict`.
  </acceptance_criteria>
  <done>`write_memory` expected behavior is captured before handler migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement write_memory And Metadata</name>
  <files>src/mcp/tools/memory.ts, src/mcp/tool-metadata.ts, tests/unit/write-memory.test.ts, tests/unit/tool-metadata.test.ts</files>
  <read_first>
    - src/mcp/tools/memory.ts
    - src/mcp/tool-metadata.ts
    - src/mcp/utils/memory-output.ts
    - src/mcp/utils/response-formats.ts
    - src/utils/tag-validator.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
  </read_first>
  <action>Register `write_memory` in `src/mcp/tools/memory.ts`. Reuse `save_memory` plugin_scope fuzzy matching, tag validation, lock behavior, DB insert, and background embedding for create mode, but return `jsonToolResult`. Implement update mode by reading target `memory_id`, requiring `is_latest:true`, preserving existing content/tags/plugin_scope when omitted, inserting a new row with `version + 1`, `previous_version_id`, `is_latest:true`, and setting the previous row `is_latest:false` in the same locked critical section. Promote `write_memory` from `future` to current/final in `src/mcp/tool-metadata.ts` and replace `D.writeMemory` with the product description template from Requirements §4.18.</action>
  <verify>
    <automated>npm test -- tests/unit/write-memory.test.ts tests/unit/tool-metadata.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/tools/memory.ts` contains `'write_memory'`.
    - `src/mcp/tools/memory.ts` contains `mode: z.enum(['create', 'update'])`.
    - `src/mcp/tools/memory.ts` contains `is_latest`.
    - `src/mcp/tool-metadata.ts` contains `current('write_memory'`.
    - `src/mcp/tools/memory.ts` contains `jsonToolResult`.
  </acceptance_criteria>
  <done>`write_memory` final handler is exposed and unit-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate get_memory And archive_memory JSON Contracts</name>
  <files>src/mcp/tools/memory.ts, src/mcp/tool-metadata.ts, tests/unit/get-memory.test.ts, tests/unit/memory-tools.test.ts</files>
  <read_first>
    - src/mcp/tools/memory.ts
    - tests/unit/get-memory.test.ts
    - tests/unit/memory-tools.test.ts
    - src/mcp/utils/memory-output.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
  </read_first>
  <action>Update `get_memory` to accept `memory_ids` and optional `include: ["content","tags_full"]`. Single input returns one JSON object or one expected error envelope. Array input returns an ordered JSON array with per-element not_found envelopes. Default output includes identification and `content_preview`; `content` appears only when requested. Update `archive_memory` to accept `memory_ids: string | string[]`, resolve full version chains, set `status:'archived'` and shared `archived_at` across the chain, preserve existing `archived_at` on re-archive, and return ordered memory identification blocks or expected errors. Update metadata descriptions for `get_memory` and `archive_memory` to final vocabulary.</action>
  <verify>
    <automated>npm test -- tests/unit/get-memory.test.ts tests/unit/memory-tools.test.ts tests/unit/tool-metadata.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/tools/memory.ts` contains `include`.
    - `src/mcp/tools/memory.ts` contains `memory_ids`.
    - `src/mcp/tools/memory.ts` contains `archived_at`.
    - `tests/unit/get-memory.test.ts` contains `JSON.parse`.
    - `tests/unit/memory-tools.test.ts` contains `archive_memory`.
  </acceptance_criteria>
  <done>`get_memory` and `archive_memory` match MEM-03/MEM-04 final JSON contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Prove Memory Persistence Integration</name>
  <files>tests/integration/write-memory.integration.test.ts, .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md</files>
  <read_first>
    - tests/integration/save-memory-tags.test.ts
    - tests/helpers/supabase.ts
    - .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <action>Create `tests/integration/write-memory.integration.test.ts` or port `tests/integration/save-memory-tags.test.ts` to cover final `write_memory`, `get_memory`, and `archive_memory`. Include create happy path, update version-chain transaction, non-latest conflict, previous-version direct get, archive full-chain, archived_at idempotency, and latest-only visibility prerequisites for search. Update TRACEABILITY MEM-01 through MEM-04 rows with implemented evidence when tests pass.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/write-memory.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/write-memory.integration.test.ts` contains `write_memory`.
    - `tests/integration/write-memory.integration.test.ts` contains `is_latest`.
    - `tests/integration/write-memory.integration.test.ts` contains `archive_memory`.
    - `TRACEABILITY.md` contains `MEM-01`.
  </acceptance_criteria>
  <done>Memory final contracts are proven against Supabase-backed persistence.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-03 | Tampering | `write_memory` generated fields | mitigate | Reject caller-supplied generated fields and build DB rows server-side. |
| T-125-04 | Race/Tampering | latest-version update | mitigate | Use existing memory write lock and update previous/new latest state in one critical section. |
| T-125-05 | Information Disclosure | `get_memory` content | mitigate | Default output is preview-only; full content gated by `include:["content"]`. |
</threat_model>

<verification>
`npm test -- tests/unit/write-memory.test.ts tests/unit/get-memory.test.ts tests/unit/memory-tools.test.ts tests/unit/tool-metadata.test.ts`
`npm run test:integration -- tests/integration/write-memory.integration.test.ts`
</verification>

<success_criteria>
- `write_memory`, `get_memory`, and `archive_memory` return parseable JSON.
- Expected validation/not-found/conflict results use `isError:false`.
- Memory version chains maintain exactly one latest row.
- Chain archival sets/preserves `archived_at` and hides archived rows from default future search.
</success_criteria>

<output>
After completion, create `.planning/phases/125-unified-search-memory-consolidation/125-02-SUMMARY.md`.
</output>
