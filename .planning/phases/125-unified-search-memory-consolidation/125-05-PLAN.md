---
phase: 125-unified-search-memory-consolidation
plan: 05
type: execute
wave: 5
depends_on: [125-04]
files_modified:
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
  - tests/scenarios/directed/testcases
  - tests/scenarios/integration/tests
  - .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, MEM-01, MEM-02, MEM-03, MEM-04]
must_haves:
  truths:
    - "Scenario ledgers are updated before scenario files are ported."
    - "Directed scenarios cover final search/list-mode/memory version behaviors."
    - "Integration scenarios use final search/write_memory/get_memory/archive_memory tool names."
    - "Legacy scenario rows are ported or explicitly dependency-gated, not silently dropped."
  artifacts:
    - path: "tests/scenarios/directed/DIRECTED_COVERAGE.md"
      provides: "Phase 125 directed behavior coverage"
    - path: "tests/scenarios/integration/INTEGRATION_COVERAGE.md"
      provides: "Phase 125 cross-tool workflow coverage"
---

<objective>
Port directed and integration scenario coverage to the final Phase 125 tool surface.

Purpose: ensure user-visible workflows use `search`, `write_memory`, `get_memory`, and `archive_memory`, and preserve legacy behavior coverage before Phase 128 removes old names.
Output: updated coverage ledgers, ported Python/YAML scenarios, and traceability scenario evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-unified-search-memory-consolidation/125-CONTEXT.md
@.planning/phases/125-unified-search-memory-consolidation/125-RESEARCH.md
@.planning/phases/125-unified-search-memory-consolidation/125-VALIDATION.md
@.planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
@tests/scenarios/directed/DIRECTED_COVERAGE.md
@tests/scenarios/integration/INTEGRATION_COVERAGE.md

<interfaces>
Executors must read the two product docs above before making any requirement or test-scope decision. If a question remains after those docs, phase artifacts, and code analogs, ask the user.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Scenario Coverage Ledgers First</name>
  <files>tests/scenarios/directed/DIRECTED_COVERAGE.md, tests/scenarios/integration/INTEGRATION_COVERAGE.md</files>
  <read_first>
    - tests/scenarios/directed/DIRECTED_COVERAGE.md
    - tests/scenarios/integration/INTEGRATION_COVERAGE.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <action>Update directed coverage rows for final `search` behaviors: document title/path/tag search, memory semantic/tag search, list_memories replacement through `search(entity_types:["memories"], query:"", tags:[...])`, empty query validation, mixed-mode dedupe/source aggregation, global limit, archived filtering, and disabled-domain degradation. Update memory rows for `write_memory(create)`, `write_memory(update)`, latest-chain uniqueness, non-latest conflict, tag replacement, `get_memory` previous-version retrieval, and `archive_memory` chain archival. Update integration coverage rows to replace `search_all`, `search_documents`, `search_memory`, `list_memories`, `save_memory`, and `update_memory` with final tool calls. Preserve existing row IDs where the test plan asks for continuity; if symbolic labels like `D-search-*` are not the ledger namespace, map them to the next available existing namespace and record the mapping in the row notes.</action>
  <verify>
    <automated>grep -n "write_memory" tests/scenarios/directed/DIRECTED_COVERAGE.md && grep -n "entity_types" tests/scenarios/integration/INTEGRATION_COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `DIRECTED_COVERAGE.md` contains `write_memory`.
    - `DIRECTED_COVERAGE.md` contains `archive_memory`.
    - `INTEGRATION_COVERAGE.md` contains `search`.
    - `INTEGRATION_COVERAGE.md` contains `entity_types`.
  </acceptance_criteria>
  <done>Scenario ledgers name final Phase 125 coverage before scenario files change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port Directed Scenario Files</name>
  <files>tests/scenarios/directed/testcases</files>
  <read_first>
    - tests/scenarios/directed/README.md
    - tests/scenarios/directed/WRITING_SCENARIOS.md
    - tests/scenarios/directed/DIRECTED_COVERAGE.md
    - tests/scenarios/framework/fqc_client.py
  </read_first>
  <action>Port or add directed Python scenarios for the ledger rows updated in Task 1. Replace legacy calls with final calls: `search_documents` -> `search(entity_types:["documents"])`, `search_memory` -> `search(entity_types:["memories"])`, `list_memories` -> `search(entity_types:["memories"], query:"", tags:[...])`, `save_memory` -> `write_memory(mode:"create")`, and `update_memory` -> `write_memory(mode:"update")`. Ensure assertions parse JSON envelopes and check IDs, `match_source`, `total`, `warnings`, `is_latest`, `previous_version_id`, and `archived_at` where applicable. If scenario setup still calls `apply_tags`, every call must include at least one of `add_tags` or `remove_tags` because Phase 124 now rejects no-op tag operations.</action>
  <verify>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed search memory || python3 tests/scenarios/directed/run_suite.py --list</automated>
  </verify>
  <acceptance_criteria>
    - At least one directed testcase contains `write_memory`.
    - At least one directed testcase contains `entity_types`.
    - At least one directed testcase contains `JSON` parsing or equivalent parsed envelope assertion.
  </acceptance_criteria>
  <done>Directed scenarios exercise final Phase 125 tool behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port YAML Integration Scenarios</name>
  <files>tests/scenarios/integration/tests, .planning/phases/125-unified-search-memory-consolidation/TRACEABILITY.md</files>
  <read_first>
    - tests/scenarios/integration/README.md
    - tests/scenarios/integration/INTEGRATION_COVERAGE.md
    - tests/scenarios/integration/run_integration.py
  </read_first>
  <action>Port YAML workflows listed in the product test plan: document write -> unified search -> get_document; write_memory(create) -> search; write_memory(update) -> search latest-only; archive_memory -> default search excludes and include_archived includes; tag-filter workflows use unified search. Update TRACEABILITY scenario columns for SRCH-01 through SRCH-06 and MEM-01 through MEM-04 with the scenario IDs that passed. Do not remove scenario coverage without a row note explaining the replacement or Phase 128 dependency. If YAML setup uses `apply_tags`, pass `add_tags` or `remove_tags`; if it uses `write_document` with both `tags` and `frontmatter.fq_tags`, make them identical or remove one source of tag truth.</action>
  <verify>
    <automated>python3 tests/scenarios/integration/run_integration.py --managed search memory || python3 tests/scenarios/integration/run_integration.py --list</automated>
  </verify>
  <acceptance_criteria>
    - At least one YAML scenario contains `write_memory`.
    - At least one YAML scenario contains `archive_memory`.
    - At least one YAML scenario contains `entity_types`.
    - `TRACEABILITY.md` contains scenario IDs for `SRCH-01` and `MEM-01`.
  </acceptance_criteria>
  <done>Integration scenarios compose final search and memory tools across user workflows.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-09 | Integrity | scenario evidence | mitigate | Ledgers are updated before scenario files and traceability records exact scenario IDs. |
</threat_model>

<verification>
`python3 tests/scenarios/directed/run_suite.py --managed search memory`
`python3 tests/scenarios/integration/run_integration.py --managed search memory`

If the managed labels differ after ledger updates, use the runner `--list` output to run the exact Phase 125 scenario names and record those names in the summary.
</verification>

<success_criteria>
- Directed and integration scenario coverage uses final Phase 125 tool names.
- Legacy scenario coverage is ported or explicitly marked as Phase 128-gated.
- TRACEABILITY.md includes scenario evidence for every Phase 125 requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/125-unified-search-memory-consolidation/125-05-SUMMARY.md`.
</output>
