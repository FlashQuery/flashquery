---
phase: 160-folder-locks-manage-directory-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 160-01
  - 160-02
files_modified:
  - src/mcp/tools/files.ts
  - tests/unit/manage-directory.test.ts
  - tests/integration/folder-lock.integration.test.ts
  - tests/integration/manage-directory-advisory-lock.integration.test.ts
  - tests/config/vitest.integration.config.ts
autonomous: true
requirements:
  - REQ-007
  - REQ-024
must_haves:
  truths:
    - "`manage_directory` exposes a Zod-validated public rename/move action shape for folders"
    - "`manage_directory` rename/move/remove takes an exclusive advisory directory lock before structural filesystem changes"
    - "`manage_directory` create remains lock-free for exclusive directory locks and idempotent"
    - "A public `manage_directory` folder rename/move waits behind an in-flight descendant file write or returns the configured timeout response"
    - "Two structural operations on the same folder do not both proceed"
    - "Contention and timeout responses keep the existing ordered per-path JSON result shape with outer `isError: false`"
  artifacts:
    - path: "src/mcp/tools/files.ts"
      provides: "REQ-024 `manage_directory` exclusive directory-lock migration"
      contains: "withDirectoryLockExclusive"
    - path: "tests/integration/folder-lock.integration.test.ts"
      provides: "T-I-011 rename/move folder coordination coverage"
      contains: "T-I-011"
    - path: "tests/integration/manage-directory-advisory-lock.integration.test.ts"
      provides: "T-I-046 and T-I-047 integration coverage"
      contains: "T-I-046"
    - path: "tests/unit/manage-directory.test.ts"
      provides: "Response-shape and create-lock-free guard coverage"
      contains: "lock_timeout"
  key_links:
    - from: "src/mcp/tools/files.ts"
      to: "src/services/document-lock.ts"
      via: "`withDirectoryLockExclusive(config, sourceAbsPath, ...)` around rename/move/remove structural filesystem operations"
      pattern: "withDirectoryLockExclusive"
    - from: "tests/integration/folder-lock.integration.test.ts"
      to: "public `manage_directory` rename/move action"
      via: "T-I-011 races a gated descendant `write_document` with `manage_directory` folder rename/move"
      pattern: "T-I-011|rename|move"
    - from: "tests/integration/manage-directory-advisory-lock.integration.test.ts"
      to: "Postgres `pg_locks`"
      via: "exclusive advisory `dir:` key visibility while gated rename/move/remove is active"
      pattern: "pg_locks|T-I-046"
---

<objective>
Migrate `manage_directory` structural operations to advisory directory locks while preserving its caller-visible contract.

Purpose: Complete REQ-024 and the exclusive half of REQ-007 for public folder operations without changing `manage_directory` response semantics or locking additive folder creation.
Output: `src/mcp/tools/files.ts` public rename/move support plus exclusive-lock migration and `T-I-011`, `T-I-046`, and `T-I-047` integration evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-RESEARCH.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-01-SUMMARY.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-02-SUMMARY.md
@.planning/phases/158-tier-2-lock-table-retirement-session-check/158-02-SUMMARY.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-03-SUMMARY.md
@src/services/document-lock.ts
@src/mcp/tools/files.ts
@tests/unit/manage-directory.test.ts
@tests/integration/manage-directory.integration.test.ts
@tests/integration/folder-lock.integration.test.ts
@tests/config/vitest.integration.config.ts

Before changing code or tests, downstream executor/checker/reviewer/verifier agents MUST read:
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md`
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md`
Use those docs first for all requirements, test IDs, acceptance criteria, and implementation questions. Ask the user only if those docs are silent or conflict with current repo reality.

<interfaces>
From `src/services/document-lock.ts` after Plan 01:
- `withDirectoryLockExclusive<T>(config: FlashQueryConfig, dirPath: string, fn: () => Promise<T>): Promise<T>`
- `LockTimeoutError` has `reason: "lock_timeout"`, `resource`, and `timeoutSeconds`.
From `src/mcp/tools/files.ts`:
- `registerFileTools(server, config)` registers `manage_directory`.
- `manage_directory` returns `{ ...jsonToolResult({ results }), isError: false }` for ordered per-path results.
- Existing conflict entries use `error: "conflict"` and `details.reason` values such as `not_directory` and `directory_not_empty`.
- Phase 160 must add the public action/input shape for folder rename/move per D-03, with source and destination folder paths validated before filesystem mutation.
</interfaces>
</context>

<source_audit>
GOAL: Users can perform folder operations without racing in-flight descendant file writes, while unrelated file writes continue concurrently. Covered by public `manage_directory` rename/move/remove locks and integration tests.
REQ-007: Covered for structural folder operations via exclusive `dir:` advisory locks that conflict with Plan 02 shared ancestor locks, including canonical rename/move T-I-011.
REQ-024: Covered by all tasks through `manage_directory` rename/move/remove migration and T-I-046/T-I-047.
RESEARCH: Preserves ordered per-path JSON results, outer `isError: false`, folder creation lock-free behavior, public rename/move workflow, and no legacy lock-table usage.
CONTEXT: Implements D-01, D-03, D-04, and D-05. D-02 was implemented in Plans 01-02. Deferred destination-path locking, EXDEV fallback, version tokens, multi-file batches, and macro-specific semantics are not implemented.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manage_directory rename/move, advisory-lock, and response-shape tests</name>
  <files>tests/unit/manage-directory.test.ts, tests/integration/folder-lock.integration.test.ts, tests/integration/manage-directory-advisory-lock.integration.test.ts, tests/config/vitest.integration.config.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
    @src/services/document-lock.ts
    @src/mcp/tools/files.ts
    @tests/unit/manage-directory.test.ts
    @tests/integration/manage-directory.integration.test.ts
    @tests/integration/folder-lock.integration.test.ts
    @tests/config/vitest.integration.config.ts
  </read_first>
  <behavior>
    - Public schema/tool metadata accept a folder rename/move action shape per D-03, with ordered per-path operations that include source and destination folder paths.
    - `T-I-011`: a gated `write_document` to `Notes/A.md` holds a shared advisory lock on `Notes/`; a concurrent public `manage_directory` rename/move of `Notes/` waits for the write to complete or returns the configured `lock_timeout` response.
    - `T-I-046`: `manage_directory({ action: "rename" | "move" | "remove" })` takes an exclusive advisory lock on the operated source folder's canonical `dir:` path, visible through `pg_locks` while the structural critical section is gated.
    - `T-I-047`: two concurrent `manage_directory` structural calls on the same source folder produce exactly one successful structural operation and one expected per-path conflict or `lock_timeout` envelope with outer `isError: false`.
    - Unit coverage proves `create` does not call `withDirectoryLockExclusive` and preserves idempotent created/unchanged behavior.
    - Unit coverage proves `LockTimeoutError` maps to an ordered result item with `error: "conflict"` and `details.reason: "lock_timeout"` without throwing an outer runtime error.
  </behavior>
  <action>Create or extend RED tests for the public `manage_directory` rename/move workflow required by D-03. In `tests/unit/manage-directory.test.ts`, assert the Zod input schema/tool metadata accept a conservative rename/move shape with source and destination folder paths, reject traversal/vault-root escapes for both paths, keep ordered result items, keep `create` exclusive-lock-free, and map `LockTimeoutError` to `error: "conflict"` with `details.reason: "lock_timeout"`. In `tests/integration/folder-lock.integration.test.ts`, add canonical `T-I-011` using public `write_document` plus public `manage_directory` rename/move, not remove-only coverage. In `tests/integration/manage-directory-advisory-lock.integration.test.ts`, follow the same mock MCP server and config style as `manage-directory.integration.test.ts`, plus session-capable skip behavior and `pg_locks` inspection by derived `dir:` key for T-I-046/T-I-047 across rename/move/remove as applicable. Register the file in `tests/config/vitest.integration.config.ts`. Include D-01, D-03, D-04, `manage_directory`, `rename`, `move`, `pg_try_advisory_lock`, `T-I-011`, `T-I-046`, `T-I-047`, and `manage-directory-advisory` in test names or comments where useful for traceability.</action>
  <verify>
    <automated>npm test -- tests/unit/manage-directory.test.ts --testNamePattern "manage_directory|rename|move|directory-lock|lock_timeout|create"</automated>
    <automated>npm run test:integration -- tests/integration/folder-lock.integration.test.ts --testNamePattern "T-I-011|rename|move|folder-lock"</automated>
    <automated>npm run test:integration -- tests/integration/manage-directory-advisory-lock.integration.test.ts --testNamePattern "T-I-046|T-I-047|manage-directory-advisory"</automated>
  </verify>
  <acceptance_criteria>
    The tests fail before `files.ts` adds rename/move schema support and imports/calls `withDirectoryLockExclusive`; `tests/config/vitest.integration.config.ts` discovers both integration files; and the required evidence command `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"` discovers T-I-011, T-I-046, and T-I-047. Remove-only behavior is not accepted as a substitute for T-I-011.
  </acceptance_criteria>
  <done>`T-I-011`, `T-I-046`, and `T-I-047` are explicitly represented in runnable tests and response-shape expectations before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement manage_directory rename/move and exclusive structural locks</name>
  <files>src/mcp/tools/files.ts, tests/unit/manage-directory.test.ts, tests/integration/folder-lock.integration.test.ts, tests/integration/manage-directory-advisory-lock.integration.test.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-01-SUMMARY.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-02-SUMMARY.md
    @src/services/document-lock.ts
    @src/mcp/tools/files.ts
  </read_first>
  <action>In `src/mcp/tools/files.ts`, import `rename` from `node:fs/promises` if needed, plus `LockTimeoutError` and `withDirectoryLockExclusive` from `../../services/document-lock.js`. Add the public `manage_directory` rename/move action shape required by D-03 and the canonical Test Plan: keep `create` / `remove` backward-compatible, and accept a conservative structural operation input where each ordered path operation supplies a source folder path and destination folder path for `rename` or `move`. Update tool metadata/help text and Zod validation so both source and destination paths pass the existing normalization, segment validation, `validateVaultPath`, and vault-root rejection before filesystem mutation. Leave `action === "create"` unchanged and lock-free for exclusive directory locks per D-03 and T-I-013. For `action === "remove"`, wrap the existing `stat`, `isDirectory`, `readdir`, non-empty conflict, and `rmdir` logic in `withDirectoryLockExclusive(config, absPath, async () => ...)`. For `action === "rename"` or `"move"`, acquire `withDirectoryLockExclusive(config, sourceAbsPath, async () => ...)` around source `stat`, destination existence checks, parent-directory validation, and filesystem `rename(sourceAbsPath, destAbsPath)`. This phase locks the operated source folder to satisfy REQ-024 and T-I-011; do not add Phase 161 destination-path advisory locking or EXDEV fallback. Catch `LockTimeoutError` inside the per-path loop and push the existing expected conflict envelope shape with `identifier: source input path`, `error: "conflict"`, and `details.reason: "lock_timeout"` plus timeout metadata if consistent with other Phase 159 envelopes. Preserve ordered `results`, outer `isError: false`, permission errors, not-found behavior, path-exists/destination conflict behavior, directory-not-empty behavior for remove, and logging for true runtime errors. Do not recreate legacy `services/write-lock`, `directory:` table locks, or `locking.ttl_seconds` behavior.</action>
  <verify>
    <automated>npm test -- tests/unit/manage-directory.test.ts --testNamePattern "manage_directory|rename|move|directory-lock|lock_timeout|create|remove"</automated>
    <automated>npm run test:integration -- tests/integration/folder-lock.integration.test.ts --testNamePattern "T-I-011|rename|move|folder-lock"</automated>
    <automated>npm run test:integration -- tests/integration/manage-directory-advisory-lock.integration.test.ts --testNamePattern "T-I-046|T-I-047|manage-directory-advisory"</automated>
  </verify>
  <acceptance_criteria>
    `src/mcp/tools/files.ts` accepts and documents a public folder rename/move workflow; calls `withDirectoryLockExclusive` on rename/move/remove structural source folders; `create` has no exclusive directory-lock call; T-I-011 proves public rename/move waits behind a descendant shared file-write lock; timeout/contention returns an ordered per-path expected conflict with outer `isError: false`; and no source references to the retired `services/write-lock` service or `fqc_write_locks` are introduced.
  </acceptance_criteria>
  <done>`manage_directory` supports public folder rename/move and uses advisory exclusive directory locks for rename/move/remove while preserving its public contract under success, expected conflict, and timeout cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller to `manage_directory` | Untrusted directory paths are normalized, sanitized, validated, then used for filesystem operations and advisory locks. |
| `manage_directory` to filesystem | Validated source and destination paths drive `stat`, `readdir`, `mkdir`, `rmdir`, and folder `rename`. |
| `manage_directory` to lock service | Validated absolute folder paths become exclusive canonical `dir:` lock resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-09 | Tampering | `manage_directory` path handling | mitigate | Preserve existing `normalizePath`, segment validation, and `validateVaultPath` before lock acquisition; tests retain traversal/symlink conflict behavior. |
| T-160-10 | Denial of Service | exclusive folder lock waits | mitigate | `withDirectoryLockExclusive` uses bounded Phase 159 timeout and per-path `LockTimeoutError` handling. |
| T-160-11 | Repudiation | `manage_directory` response contract | mitigate | Unit/integration tests require ordered `results`, outer `isError: false`, and unchanged conflict envelope shape for `lock_contention` / `lock_timeout` cases. |
| T-160-12 | Tampering | lock bypass on structural operations | mitigate | `T-I-011` proves public rename/move queues behind descendant writes; `T-I-046` proves exclusive `dir:` lock visibility during rename/move/remove; `T-I-047` proves concurrent structural operations do not both proceed. |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned. |
</threat_model>

<verification>
Run both task commands, `npm run typecheck`, `npm run build`, and the required evidence command `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"`. Record Supabase/session-capability skips without printing secrets.
</verification>

<success_criteria>
REQ-024 acceptance criteria 1-4 are satisfied; REQ-007 exclusive structural folder rename/move/delete behavior is connected to shared descendant file-write locks from Plan 02; `T-I-011`, `T-I-046`, and `T-I-047` are present and executable; `manage_directory` create remains lock-free for exclusive directory locks.
</success_criteria>

<output>
Create `.planning/phases/160-folder-locks-manage-directory-migration/160-03-SUMMARY.md` when done.
</output>
