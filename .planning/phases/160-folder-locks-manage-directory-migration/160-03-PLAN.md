---
phase: 160-folder-locks-manage-directory-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 160-01
  - 160-02
files_modified:
  - src/mcp/tools/files.ts
  - tests/unit/manage-directory.test.ts
  - tests/integration/manage-directory-advisory-lock.integration.test.ts
  - tests/config/vitest.integration.config.ts
autonomous: true
requirements:
  - REQ-007
  - REQ-024
must_haves:
  truths:
    - "`manage_directory` remove takes an exclusive advisory directory lock before stat/readdir/rmdir"
    - "`manage_directory` create remains lock-free for exclusive directory locks and idempotent"
    - "Two structural operations on the same folder do not both proceed"
    - "Contention and timeout responses keep the existing ordered per-path JSON result shape with outer `isError: false`"
  artifacts:
    - path: "src/mcp/tools/files.ts"
      provides: "REQ-024 `manage_directory` exclusive directory-lock migration"
      contains: "withDirectoryLockExclusive"
    - path: "tests/integration/manage-directory-advisory-lock.integration.test.ts"
      provides: "T-I-046 and T-I-047 integration coverage"
      contains: "T-I-046"
    - path: "tests/unit/manage-directory.test.ts"
      provides: "Response-shape and create-lock-free guard coverage"
      contains: "lock_timeout"
  key_links:
    - from: "src/mcp/tools/files.ts"
      to: "src/services/document-lock.ts"
      via: "`withDirectoryLockExclusive(config, absPath, ...)` around remove-only structural filesystem operations"
      pattern: "withDirectoryLockExclusive"
    - from: "tests/integration/manage-directory-advisory-lock.integration.test.ts"
      to: "Postgres `pg_locks`"
      via: "exclusive advisory `dir:` key visibility while gated remove is active"
      pattern: "pg_locks|T-I-046"
---

<objective>
Migrate `manage_directory` structural operations to advisory directory locks while preserving its caller-visible contract.

Purpose: Complete REQ-024 and the exclusive half of REQ-007 for public folder operations without changing `manage_directory` response semantics or locking additive folder creation.
Output: `src/mcp/tools/files.ts` exclusive-lock migration plus `T-I-046` and `T-I-047` integration evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-RESEARCH.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-01-SUMMARY.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-02-SUMMARY.md
@.planning/phases/158-tier-2-lock-table-retirement-session-check/158-02-SUMMARY.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-03-SUMMARY.md
@src/services/document-lock.ts
@src/mcp/tools/files.ts
@tests/unit/manage-directory.test.ts
@tests/integration/manage-directory.integration.test.ts
@tests/integration/folder-lock.integration.test.ts
@tests/config/vitest.integration.config.ts

Before changing code or tests, downstream executor/checker/reviewer/verifier agents MUST read:
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md`
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md`
Use those docs first for all requirements, test IDs, acceptance criteria, and implementation questions. Ask the user only if those docs are silent or conflict with current repo reality.

<interfaces>
From `src/services/document-lock.ts` after Plan 01:
- `withDirectoryLockExclusive<T>(config: FlashQueryConfig, dirPath: string, fn: () => Promise<T>): Promise<T>`
- `LockTimeoutError` has `reason: "lock_timeout"`, `resource`, and `timeoutSeconds`.
From `src/mcp/tools/files.ts`:
- `registerFileTools(server, config)` registers `manage_directory`.
- `manage_directory` returns `{ ...jsonToolResult({ results }), isError: false }` for ordered per-path results.
- Existing conflict entries use `error: "conflict"` and `details.reason` values such as `not_directory` and `directory_not_empty`.
</interfaces>
</context>

<source_audit>
GOAL: Users can perform folder operations without racing in-flight descendant file writes, while unrelated file writes continue concurrently. Covered by exclusive `manage_directory` remove locks and integration tests.
REQ-007: Covered for structural folder operations via exclusive `dir:` advisory locks that conflict with Plan 02 shared ancestor locks.
REQ-024: Covered by all tasks through `manage_directory` migration and T-I-046/T-I-047.
RESEARCH: Preserves ordered per-path JSON results, outer `isError: false`, folder creation lock-free behavior, and no legacy lock-table usage.
CONTEXT: Implements D-01, D-03, D-04, and D-05. D-02 was implemented in Plans 01-02. Deferred destination locks, EXDEV fallback, version tokens, multi-file batches, and macro-specific semantics are not implemented.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manage_directory advisory-lock and response-shape tests</name>
  <files>tests/unit/manage-directory.test.ts, tests/integration/manage-directory-advisory-lock.integration.test.ts, tests/config/vitest.integration.config.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
    @src/services/document-lock.ts
    @src/mcp/tools/files.ts
    @tests/unit/manage-directory.test.ts
    @tests/integration/manage-directory.integration.test.ts
    @tests/integration/folder-lock.integration.test.ts
    @tests/config/vitest.integration.config.ts
  </read_first>
  <behavior>
    - `T-I-046`: `manage_directory({ action: "remove" })` takes an exclusive advisory lock on the operated folder's canonical `dir:` path, visible through `pg_locks` while the remove critical section is gated.
    - `T-I-047`: two concurrent `manage_directory` remove calls on the same folder produce exactly one successful structural operation and one expected per-path conflict or `lock_timeout` envelope with outer `isError: false`.
    - Unit coverage proves `create` does not call `withDirectoryLockExclusive` and preserves idempotent created/unchanged behavior.
    - Unit coverage proves `LockTimeoutError` maps to an ordered result item with `error: "conflict"` and `details.reason: "lock_timeout"` without throwing an outer runtime error.
  </behavior>
  <action>Create `tests/integration/manage-directory-advisory-lock.integration.test.ts` with the same mock MCP server and config style as `manage-directory.integration.test.ts`, plus session-capable skip behavior and `pg_locks` inspection by derived `dir:` key. Register the file in `tests/config/vitest.integration.config.ts`. Extend `tests/unit/manage-directory.test.ts` or create missing targeted unit coverage to mock `withDirectoryLockExclusive`, assert `create` does not call it, and assert timeout/error envelope preservation. Include D-01, D-03, D-04, `manage_directory`, `pg_try_advisory_lock`, `T-I-046`, `T-I-047`, and `manage-directory-advisory` in test names or comments where useful for traceability.</action>
  <verify>
    <automated>npm test -- tests/unit/manage-directory.test.ts --testNamePattern "manage_directory|directory-lock|lock_timeout|create"</automated>
    <automated>npm run test:integration -- tests/integration/manage-directory-advisory-lock.integration.test.ts --testNamePattern "T-I-046|T-I-047|manage-directory-advisory"</automated>
  </verify>
  <acceptance_criteria>
    The tests fail before `files.ts` imports and calls `withDirectoryLockExclusive`; the integration file is included in `tests/config/vitest.integration.config.ts`; and the required evidence command `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"` discovers these tests.
  </acceptance_criteria>
  <done>`T-I-046` and `T-I-047` are explicitly represented in runnable tests and response-shape expectations before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap manage_directory remove with exclusive directory lock</name>
  <files>src/mcp/tools/files.ts, tests/unit/manage-directory.test.ts, tests/integration/manage-directory-advisory-lock.integration.test.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-01-SUMMARY.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-02-SUMMARY.md
    @src/services/document-lock.ts
    @src/mcp/tools/files.ts
  </read_first>
  <action>In `src/mcp/tools/files.ts`, import `LockTimeoutError` and `withDirectoryLockExclusive` from `../../services/document-lock.js`. Keep all path normalization, segment validation, `validateVaultPath`, and vault-root rejection before lock acquisition. Leave `action === "create"` unchanged and lock-free for exclusive directory locks per D-03 and `T-I-013`. For `action === "remove"`, wrap the existing `stat`, `isDirectory`, `readdir`, non-empty conflict, and `rmdir` logic in `withDirectoryLockExclusive(config, absPath, async () => ...)`. Catch `LockTimeoutError` inside the per-path loop and push the existing expected conflict envelope shape with `identifier: inputPath`, `error: "conflict"`, and `details.reason: "lock_timeout"` plus timeout metadata if consistent with other Phase 159 envelopes. Preserve ordered `results`, outer `isError: false`, permission errors, not-found behavior, directory-not-empty behavior, and logging for true runtime errors. Do not recreate legacy `services/write-lock`, `directory:` table locks, or `locking.ttl_seconds` behavior.</action>
  <verify>
    <automated>npm test -- tests/unit/manage-directory.test.ts --testNamePattern "manage_directory|directory-lock|lock_timeout|create|remove"</automated>
    <automated>npm run test:integration -- tests/integration/manage-directory-advisory-lock.integration.test.ts --testNamePattern "T-I-046|T-I-047|manage-directory-advisory"</automated>
  </verify>
  <acceptance_criteria>
    `src/mcp/tools/files.ts` calls `withDirectoryLockExclusive` only on the remove path; `create` has no exclusive directory-lock call; timeout/contention returns an ordered per-path expected conflict with outer `isError: false`; and no source references to the retired `services/write-lock` service or `fqc_write_locks` are introduced.
  </acceptance_criteria>
  <done>`manage_directory` uses advisory exclusive directory locks for structural removal and preserves its public contract under success, expected conflict, and timeout cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller to `manage_directory` | Untrusted directory paths are normalized, sanitized, validated, then used for filesystem operations and advisory locks. |
| `manage_directory` to filesystem | Validated paths drive `stat`, `readdir`, `mkdir`, and `rmdir`. |
| `manage_directory` to lock service | Validated absolute folder paths become exclusive canonical `dir:` lock resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-09 | Tampering | `manage_directory` path handling | mitigate | Preserve existing `normalizePath`, segment validation, and `validateVaultPath` before lock acquisition; tests retain traversal/symlink conflict behavior. |
| T-160-10 | Denial of Service | exclusive folder lock waits | mitigate | `withDirectoryLockExclusive` uses bounded Phase 159 timeout and per-path `LockTimeoutError` handling. |
| T-160-11 | Repudiation | `manage_directory` response contract | mitigate | Unit/integration tests require ordered `results`, outer `isError: false`, and unchanged conflict envelope shape for `lock_contention` / `lock_timeout` cases. |
| T-160-12 | Tampering | lock bypass on structural operations | mitigate | `T-I-046` proves exclusive `dir:` lock visibility during remove; `T-I-047` proves concurrent structural operations do not both proceed. |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned. |
</threat_model>

<verification>
Run both task commands, `npm run typecheck`, `npm run build`, and the required evidence command `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"`. Record Supabase/session-capability skips without printing secrets.
</verification>

<success_criteria>
REQ-024 acceptance criteria 1-4 are satisfied; REQ-007 exclusive structural folder operation behavior is connected to shared descendant file-write locks from Plan 02; `T-I-046` and `T-I-047` are present and executable; `manage_directory` create remains lock-free for exclusive directory locks.
</success_criteria>

<output>
Create `.planning/phases/160-folder-locks-manage-directory-migration/160-03-SUMMARY.md` when done.
</output>
