---
phase: 160-folder-locks-manage-directory-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 160-02
  - 160-03
files_modified:
  - tests/scenarios/integration/tests/folder_coordination.yml
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
  - .planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md
autonomous: true
requirements:
  - REQ-007
  - REQ-024
must_haves:
  truths:
    - "The integration scenario catalog includes `INT-WCO-01` / `T-Y-001` folder coordination evidence"
    - "The phase validation strategy records final evidence for helper, integration, typecheck, build, and required roadmap commands"
    - "Scenario coverage either proves the in-flight folder coordination workflow or records a blocking runner limitation without claiming false coverage"
  artifacts:
    - path: "tests/scenarios/integration/tests/folder_coordination.yml"
      provides: "T-Y-001 / INT-WCO-01 folder coordination scenario"
      contains: "INT-WCO-01"
    - path: "tests/scenarios/integration/INTEGRATION_COVERAGE.md"
      provides: "Coverage registration for INT-WCO-01"
      contains: "T-Y-001"
    - path: ".planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md"
      provides: "Updated validation status and final evidence map"
      contains: "folder-lock|manage-directory-advisory"
  key_links:
    - from: "tests/scenarios/integration/tests/folder_coordination.yml"
      to: "public MCP tools"
      via: "managed integration scenario steps for `write_document` and `manage_directory` folder coordination"
      pattern: "write_document|manage_directory"
    - from: "INTEGRATION_COVERAGE.md"
      to: "folder_coordination.yml"
      via: "INT-WCO-01 coverage row"
      pattern: "INT-WCO-01"
---

<objective>
Add scenario-level Phase 160 evidence and finalize validation bookkeeping.

Purpose: Close the Test Plan `T-Y-001` / `INT-WCO-01` requirement where the YAML runner supports it, and make the final phase verification command set explicit for executors and verifiers.
Output: `folder_coordination.yml`, coverage-matrix update, and validation status update.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-RESEARCH.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-02-SUMMARY.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-03-SUMMARY.md
@tests/scenarios/integration/README.md
@tests/scenarios/integration/tests/removal_directory_maintenance.yml
@tests/scenarios/integration/INTEGRATION_COVERAGE.md

Before changing tests or validation docs, downstream executor/checker/reviewer/verifier agents MUST read:
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md`
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md`
Use those docs first for all requirements, test IDs, acceptance criteria, and implementation questions. Ask the user only if those docs are silent or conflict with current repo reality.
</context>

<source_audit>
GOAL: Users can perform folder operations without racing in-flight descendant file writes, while unrelated file writes continue concurrently. Covered by scenario and final evidence.
REQ-007: Covered by T-Y-001 / INT-WCO-01 and final integration evidence.
REQ-024: Covered by final evidence from Plan 03 and validation bookkeeping.
RESEARCH: Uses existing YAML integration scenario framework and coverage matrix; no new packages.
CONTEXT: Implements D-01 and D-04 scenario/evidence requirements. Deferred destination locks, EXDEV fallback, version tokens, multi-file batches, and macro-specific semantics are not implemented.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add INT-WCO-01 folder coordination YAML scenario</name>
  <files>tests/scenarios/integration/tests/folder_coordination.yml, tests/scenarios/integration/INTEGRATION_COVERAGE.md</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
    @tests/scenarios/integration/README.md
    @tests/scenarios/integration/tests/removal_directory_maintenance.yml
    @tests/scenarios/integration/INTEGRATION_COVERAGE.md
  </read_first>
  <action>Create `tests/scenarios/integration/tests/folder_coordination.yml` for `T-Y-001` / `INT-WCO-01` per D-04. The scenario must use managed public-tool workflow steps to write a document under `_wco/notes/`, attempt the folder operation while the write is in flight, and assert that the folder operation queues behind the write and completes after. If the current YAML DSL cannot express a true concurrent in-flight write/folder-operation queue, do not replace it with a sequential approximation; instead add the scenario file only as an explicit blocked/skipped artifact if the runner supports that state, record the runner limitation in `.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md`, and leave `INT-WCO-01` as blocked pending scenario-runner support while `T-I-011` carries deterministic integration proof. Update `tests/scenarios/integration/INTEGRATION_COVERAGE.md` so `INT-WCO-01` maps to `T-Y-001` and REQ-007 without marking blocked coverage as passed.</action>
  <verify>
    <automated>python3 tests/scenarios/integration/run_integration.py --managed folder_coordination</automated>
  </verify>
  <acceptance_criteria>
    `folder_coordination.yml` exists, includes coverage `INT-WCO-01` and/or `T-Y-001`, uses public FlashQuery operations, and either passes the true in-flight coordination workflow or is explicitly recorded as blocked/skipped because the runner cannot express concurrency. `INTEGRATION_COVERAGE.md` contains a row or entry tying `INT-WCO-01` to `T-Y-001` and REQ-007 without marking blocked coverage as passed.
  </acceptance_criteria>
  <done>The scenario layer contains explicit Phase 160 folder coordination coverage or a documented DSL limitation with deterministic integration coverage carrying the true concurrency proof.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Phase 160 validation evidence</name>
  <files>.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-02-SUMMARY.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-03-SUMMARY.md
  </read_first>
  <action>Update `.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md` statuses for completed tasks and record final evidence commands. Include `npm test -- --grep "directory-lock|with-directory-lock|lock-helper-only"`, `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"`, `python3 tests/scenarios/integration/run_integration.py --managed folder_coordination`, `npm run typecheck`, and `npm run build`. If integration tests skip because `HAS_SESSION_CAPABLE_DATABASE_URL` is false or `.env.test` uses a transaction-mode pooler, record the skip reason without printing secrets. Do not weaken the required evidence command or mark skipped advisory behavior as passed.</action>
  <verify>
    <automated>npm test -- --grep "directory-lock|with-directory-lock|lock-helper-only"</automated>
    <automated>npm run test:integration -- --grep "folder-lock|manage-directory-advisory"</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    Validation status references all required Test Plan cases `T-I-011`, `T-I-012`, `T-I-013`, `T-I-046`, `T-I-047`, `T-Y-001`, and `INT-WCO-01`; the exact required evidence command appears unchanged; skips are documented as skips, not passes.
  </acceptance_criteria>
  <done>Phase 160 has a complete validation record for helper, integration, scenario, typecheck, build, and required roadmap evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenario runner to MCP tools | YAML inputs exercise public tool calls against a managed FlashQuery process. |
| Validation docs to verifier | Recorded evidence influences whether the phase is accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-13 | Repudiation | scenario coverage matrix | mitigate | Register `INT-WCO-01` / `T-Y-001` explicitly and avoid claiming concurrency proof beyond what the YAML runner can express. |
| T-160-14 | Information Disclosure | validation evidence | mitigate | Record env capability skips without printing Supabase URLs, service role keys, or `DATABASE_URL` secrets. |
| T-160-15 | Tampering | final evidence command set | mitigate | Preserve exact required command `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"` and do not replace it with narrower-only evidence. |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned. |
</threat_model>

<verification>
Run all task verification commands. The phase is not ready for verification until `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"` has been run and its pass/skip result recorded.
</verification>

<success_criteria>
`T-Y-001` / `INT-WCO-01` is planned and registered where supported; final validation names every required test ID; and the required evidence command is preserved for `$gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/160-folder-locks-manage-directory-migration/160-04-SUMMARY.md` when done.
</output>
