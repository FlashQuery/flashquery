---
phase: 160-folder-locks-manage-directory-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 160-01
files_modified:
  - src/mcp/tools/documents/write.ts
  - src/mcp/tools/documents/archive.ts
  - src/mcp/tools/documents/remove.ts
  - src/mcp/tools/documents/copy.ts
  - src/mcp/tools/documents/move.ts
  - src/mcp/tools/compound.ts
  - src/services/scanner.ts
  - tests/unit/document-tool-lock-call-sites.test.ts
  - tests/integration/folder-lock.integration.test.ts
  - tests/config/vitest.integration.config.ts
autonomous: true
requirements:
  - REQ-007
must_haves:
  truths:
    - "Every FlashQuery-mediated file write holds shared advisory locks on ancestor directories while the per-file write lock is active"
    - "Two sibling file writes under the same folder can both proceed because their directory locks are shared"
    - "The shared-lock side needed for folder rename/move coordination is present on every descendant file write"
    - "Existing document and compound lock timeout response envelopes remain unchanged"
  artifacts:
    - path: "src/mcp/tools/documents/write.ts"
      provides: "Shared ancestor directory locks around write_document create/update writes"
      contains: "withAncestorDirectoryLocksShared"
    - path: "src/mcp/tools/compound.ts"
      provides: "Shared ancestor directory locks around compound document mutations"
      contains: "withAncestorDirectoryLocksShared"
    - path: "src/services/scanner.ts"
      provides: "Shared ancestor directory locks around scanner frontmatter repair writes"
      contains: "withAncestorDirectoryLocksShared"
    - path: "tests/integration/folder-lock.integration.test.ts"
      provides: "T-I-012 and T-I-013 folder-lock integration evidence plus shared-lock setup for T-I-011"
      contains: "T-I-012"
  key_links:
    - from: "document/compound/scanner write paths"
      to: "src/services/document-lock.ts"
      via: "outer `withAncestorDirectoryLocksShared(config, absPath, ...)` wrapping existing `withDocumentLock` / `withDocumentLocks` critical sections"
      pattern: "withAncestorDirectoryLocksShared[\\s\\S]*withDocumentLock"
    - from: "tests/integration/folder-lock.integration.test.ts"
      to: "Postgres `pg_locks`"
      via: "derived `dir:` advisory keys observed while write gates are held"
      pattern: "pg_locks|dir:"
---

<objective>
Migrate file-writing paths to hold REQ-007 shared ancestor directory locks and add folder-lock integration evidence.

Purpose: Prevent folder structural operations from racing in-flight descendant file writes while preserving parallel sibling file writes and existing per-file lock semantics.
Output: Document, compound, and scanner write paths wrapped with shared ancestor directory locks plus `T-I-012` and `T-I-013` integration tests. Plan 03 adds the public `manage_directory` rename/move action and completes canonical `T-I-011`.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-RESEARCH.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@.planning/phases/160-folder-locks-manage-directory-migration/160-01-SUMMARY.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-03-SUMMARY.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-04-SUMMARY.md
@src/services/document-lock.ts
@src/mcp/tools/documents/write.ts
@src/mcp/tools/documents/archive.ts
@src/mcp/tools/documents/remove.ts
@src/mcp/tools/documents/copy.ts
@src/mcp/tools/documents/move.ts
@src/mcp/tools/compound.ts
@src/services/scanner.ts
@tests/unit/document-tool-lock-call-sites.test.ts
@tests/integration/two-tier-lock.integration.test.ts
@tests/integration/lock-timeout.integration.test.ts
@tests/config/vitest.integration.config.ts

Before changing code or tests, downstream executor/checker/reviewer/verifier agents MUST read:
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md`
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md`
Use those docs first for all requirements, test IDs, acceptance criteria, and implementation questions. Ask the user only if those docs are silent or conflict with current repo reality.

<interfaces>
From `src/services/document-lock.ts` after Plan 01:
- `withAncestorDirectoryLocksShared<T>(config: FlashQueryConfig, filePath: string, fn: () => Promise<T>): Promise<T>`
- `withDocumentLock<T>(config: FlashQueryConfig, filePath: string, fn: () => Promise<T>): Promise<T>`
- `withDocumentLocks<T>(config: FlashQueryConfig, filePaths: string[], fn: () => Promise<T>): Promise<T>`
- `LockTimeoutError` maps to existing `lock_timeout` conflict envelopes in document and compound tools.
</interfaces>
</context>

<source_audit>
GOAL: Users can perform folder operations without racing in-flight descendant file writes, while unrelated file writes continue concurrently. Covered here for file-write shared locks and completed with rename/move structural coverage in Plan 03.
REQ-007: Covered here by wrapping all file-write paths with shared ancestor directory locks and proving T-I-012 and T-I-013. T-I-011 requires public `manage_directory` rename/move and is completed in Plan 03 per D-03.
REQ-024: Not claimed by this plan; Plan 03 migrates `manage_directory`.
RESEARCH: Follows shared `pg_try_advisory_lock_shared` semantics, no directory Tier 1, and Phase 159 timeout envelopes.
CONTEXT: Implements D-01, D-02, and D-04 for folder-lock integration tests. D-03 is left to Plan 03. Deferred Phase 161-163 items are not implemented.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source and integration coverage for shared ancestor file-write locks</name>
  <files>tests/unit/document-tool-lock-call-sites.test.ts, tests/integration/folder-lock.integration.test.ts, tests/config/vitest.integration.config.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-CONTEXT.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-PATTERNS.md
    @src/services/document-lock.ts
    @tests/integration/two-tier-lock.integration.test.ts
    @tests/integration/lock-timeout.integration.test.ts
    @tests/config/vitest.integration.config.ts
    @src/mcp/tools/documents/write.ts
    @src/mcp/tools/documents/archive.ts
    @src/mcp/tools/documents/remove.ts
    @src/mcp/tools/documents/copy.ts
    @src/mcp/tools/documents/move.ts
    @src/mcp/tools/compound.ts
    @src/services/scanner.ts
  </read_first>
  <behavior>
    - Source guard: `write_document`, `archive_document`, `remove_document`, `copy_document`, `move_document`, `insert_doc_link`, `apply_tags`, `insert_in_doc`, `replace_doc_section`, and scanner `repairFrontmatter` wrap file writes with `withAncestorDirectoryLocksShared`.
    - Shared-lock setup for `T-I-011`: a gated `write_document` to `Notes/A.md` holds a shared `dir:` lock on `Notes/`, so Plan 03 can prove a concurrent public `manage_directory` rename/move waits behind it.
    - `T-I-012`: concurrent `write_document` calls to `Notes/A.md` and `Notes/B.md` both enter their write critical sections while sharing the same folder lock.
    - `T-I-013`: `manage_directory` create path does not take an exclusive advisory lock; `pg_locks` has no exclusive `dir:` entry during folder creation.
  </behavior>
  <action>Create RED integration coverage in `tests/integration/folder-lock.integration.test.ts` following the session-capable skip pattern from `two-tier-lock.integration.test.ts` and the advisory key derivation pattern from `lock-timeout.integration.test.ts`. Register the file in `tests/config/vitest.integration.config.ts`. Use deterministic gates rather than sleep-only assertions where possible. Include `T-I-012`, `T-I-013`, `folder-lock`, and `manage-directory-advisory` in test names so `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"` selects the evidence. Include a shared-lock observability helper/test for the T-I-011 setup, but do not substitute remove-only behavior for the canonical rename/move workflow; Plan 03 owns the public `manage_directory` rename/move T-I-011 assertion per D-03. Extend `tests/unit/document-tool-lock-call-sites.test.ts` to fail until all owned file-write call sites import and call `withAncestorDirectoryLocksShared` around existing per-file locks.</action>
  <verify>
    <automated>npm test -- tests/unit/document-tool-lock-call-sites.test.ts --testNamePattern "ancestor|directory-lock|write_document|compound|scanner"</automated>
    <automated>npm run test:integration -- tests/integration/folder-lock.integration.test.ts --testNamePattern "T-I-012|T-I-013|folder-lock"</automated>
  </verify>
  <acceptance_criteria>
    The unit/source guard and integration tests fail before production wrapping exists. The integration file is included in `tests/config/vitest.integration.config.ts`, and the phase evidence command `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"` discovers the new folder-lock tests. Remove-only structural coverage is not used as a stand-in for rename/move.
  </acceptance_criteria>
  <done>`T-I-012` and `T-I-013` are explicitly represented in runnable tests, and the shared-lock setup for `T-I-011` is present without claiming rename/move completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap all file-write paths with shared ancestor directory locks</name>
  <files>src/mcp/tools/documents/write.ts, src/mcp/tools/documents/archive.ts, src/mcp/tools/documents/remove.ts, src/mcp/tools/documents/copy.ts, src/mcp/tools/documents/move.ts, src/mcp/tools/compound.ts, src/services/scanner.ts, tests/unit/document-tool-lock-call-sites.test.ts, tests/integration/folder-lock.integration.test.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/160-folder-locks-manage-directory-migration/160-01-SUMMARY.md
    @.planning/phases/159-lock-timeout-canonical-key-derivation/159-03-SUMMARY.md
    @.planning/phases/159-lock-timeout-canonical-key-derivation/159-04-SUMMARY.md
    @src/services/document-lock.ts
    @src/mcp/tools/documents/write.ts
    @src/mcp/tools/documents/archive.ts
    @src/mcp/tools/documents/remove.ts
    @src/mcp/tools/documents/copy.ts
    @src/mcp/tools/documents/move.ts
    @src/mcp/tools/compound.ts
    @src/services/scanner.ts
  </read_first>
  <action>Import `withAncestorDirectoryLocksShared` from `src/services/document-lock.ts` in every owned file-write module. For each file write, wrap the existing per-file `withDocumentLock` or `withDocumentLocks` critical section inside `withAncestorDirectoryLocksShared(config, fileAbsPath, async () => ...)` per D-02, so shared ancestor directory locks are held while the per-file lock and actual vault mutation run. Preserve all existing `LockTimeoutError` catch blocks and response envelopes from Phase 159; do not introduce a new response shape. In `copy_document` and `move_document`, lock only the actual source/current file-write paths required by Phase 160 and do not implement Phase 161 destination-path locking or EXDEV fallback changes. In `src/services/scanner.ts`, wrap `repairFrontmatter` writes with shared ancestor directory locks around the existing `withDocumentLock`. Keep `call_macro` macro-spanning behavior unchanged.</action>
  <verify>
    <automated>npm test -- tests/unit/document-tool-lock-call-sites.test.ts tests/unit/write-document.test.ts tests/unit/copy-document.test.ts tests/unit/move-document.test.ts tests/unit/archive-document.test.ts tests/unit/document-batch-lock-contention.test.ts tests/unit/replace-doc-section.test.ts --testNamePattern "ancestor|directory-lock|lock timeout|lock_timeout|write_document|archive_document|remove_document|copy_document|move_document|replace_doc_section"</automated>
    <automated>npm run test:integration -- tests/integration/folder-lock.integration.test.ts --testNamePattern "T-I-012|T-I-013|folder-lock"</automated>
  </verify>
  <acceptance_criteria>
    Source guards prove all listed file-write call sites use `withAncestorDirectoryLocksShared`; `T-I-012` proves sibling writes do not block each other on shared directory locks; `T-I-013` proves folder creation takes no exclusive directory lock. Existing lock-timeout envelopes remain `details.reason: "lock_timeout"` where Phase 159 established them. Canonical rename/move T-I-011 remains assigned to Plan 03 after the public `manage_directory` action exists.
  </acceptance_criteria>
  <done>All file-write paths hold shared ancestor directory advisory locks while preserving existing per-file serialization, timeout behavior, and response shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller to document tools | Untrusted identifiers and paths resolve to filesystem writes that now acquire ancestor directory locks. |
| File write path to folder operation path | Concurrent public tools coordinate only through shared/exclusive advisory locks. |
| Test process to Postgres | Integration tests inspect `pg_locks` using derived advisory keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-05 | Tampering | document/compound/scanner write paths | mitigate | Source guard requires `withAncestorDirectoryLocksShared` around all owned write paths so folder operations cannot bypass descendant writes. |
| T-160-06 | Denial of Service | sibling file writes | mitigate | Use PostgreSQL shared locks only for ancestor directories; T-I-012 proves sibling writes do not serialize on directory locks. |
| T-160-07 | Tampering | path traversal/canonicalization | mitigate | Directory helper from Plan 01 uses canonical `dir:` entries and vault-root-bounded ancestor enumeration. |
| T-160-08 | Repudiation | response-shape regression | mitigate | Existing Phase 159 unit tests and this plan's source tests preserve `lock_timeout` conflict envelopes. |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned. |
</threat_model>

<verification>
Run both task commands, `npm run typecheck`, and the required evidence command `npm run test:integration -- --grep "folder-lock|manage-directory-advisory"`. Record session-capable database skips with the concrete `HAS_SESSION_CAPABLE_DATABASE_URL` reason if the local `.env.test` cannot prove advisory-lock behavior.
</verification>

<success_criteria>
REQ-007 shared file-write behavior is satisfied for file writes; Test Plan `T-I-012` and `T-I-013` are present and executable; the shared-lock side of `T-I-011` is ready for Plan 03's public rename/move proof; no Phase 161 destination-lock or EXDEV behavior is implemented.
</success_criteria>

<output>
Create `.planning/phases/160-folder-locks-manage-directory-migration/160-02-SUMMARY.md` when done.
</output>
