---
phase: 138-handler-source-resolution-scenario-closure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/macro-handler.test.ts
  - tests/unit/macro-source-ref.test.ts
  - tests/unit/macro-fence-extractor.test.ts
autonomous: true
requirements:
  - MACRO-SRC-01
  - MACRO-SRC-02
must_haves:
  truths:
    - "Downstream agents MUST read the Macro Language Requirements and Test Plan before implementation."
    - "`callMacroInputSchema` accepts documented production fields and rejects task-spec fields."
    - "Invalid source/source_ref combinations return canonical `invalid_input` envelopes before parse or execution."
  artifacts:
    - path: "tests/unit/macro-handler.test.ts"
      provides: "T-U-216 through T-U-224 handler/source validation coverage"
    - path: "tests/unit/macro-source-ref.test.ts"
      provides: "source_ref format and block-name validation coverage"
    - path: "tests/unit/macro-fence-extractor.test.ts"
      provides: "existing named-block extraction coverage remains green"
---

<objective>
Add the Phase 138 handler contract tests before production source_ref work.

Purpose: pin the public `call_macro` request schema and source/source_ref invalid-input matrix so implementation cannot accidentally execute malformed requests or regress prior inline behavior.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-CONTEXT.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-RESEARCH.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-PATTERNS.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-VALIDATION.md
@.planning/phases/137-trace-progress-dry-run-budgets/137-05-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the production request schema</name>
  <files>tests/unit/macro-handler.test.ts</files>
  <read_first>
    - tests/unit/macro-handler.test.ts
    - tests/unit/macro-test-helpers.ts
    - src/mcp/tools/macro.ts
    - .planning/phases/138-handler-source-resolution-scenario-closure/138-CONTEXT.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Extend `tests/unit/macro-handler.test.ts` with Test Plan rows `T-U-216` and `T-U-217`. Add a schema test that calls `callMacroInputSchema.safeParse` with exactly these accepted top-level fields: `source`, `source_ref`, `input_vars`, `budget.max_total_tokens`, `budget.max_model_calls`, `budget.max_external_tool_calls`, `budget.timeout_ms`, `dry_run`, `trace`, and `progress`. Assert `task`, `taskHint`, `pollInterval`, and `ttl` are not accepted by using `.strict()` behavior if the production schema is made strict, or by asserting the handler never exposes those keys in parsed output if the project keeps Zod's stripping behavior. Add a defaults test through `runMacroSource` or the handler path proving omitted `trace`, `progress`, `dry_run`, and `budget.timeout_ms` preserve the documented defaults: `trace: "summary"`, `progress: "milestones"`, `dry_run: false`, and timeout from `config.macro.defaultTimeoutMs` or `60000` fallback.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-handler</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-216|T-U-217|max_total_tokens|max_model_calls|max_external_tool_calls|timeout_ms|taskHint|pollInterval|ttl" tests/unit/macro-handler.test.ts` finds both IDs, all budget keys, and all rejected task-spec names.
    - `npm test -- --reporter=verbose macro-handler` exits 0 after implementation.
  </acceptance_criteria>
  <done>Handler schema tests encode MACRO-SRC-01 and the deferred task-spec exclusion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin source/source_ref exclusivity failures</name>
  <files>tests/unit/macro-handler.test.ts, tests/unit/macro-source-ref.test.ts</files>
  <read_first>
    - tests/unit/macro-handler.test.ts
    - tests/unit/macro-source-ref.test.ts
    - src/mcp/tools/macro.ts
    - src/macro/source-ref.ts
    - src/macro/errors.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Add Test Plan rows `T-U-218`, `T-U-219`, `T-U-220`, `T-U-221`, `T-U-222`, and `T-U-223`. Assert the handler/source resolver returns `jsonExpectedError` payloads with top-level `error: "invalid_input"` and these exact details: both `source` and `source_ref` populated -> `{ reason: "exactly_one_required" }`; neither populated -> `{ reason: "exactly_one_required" }`; `source: ""` -> `{ reason: "empty_source" }`; `source_ref: ""` -> `{ reason: "empty_source_ref" }`; `source_ref: "::foo"` -> `{ reason: "invalid_source_ref_format" }`; and `source_ref: "Macros/lib.md::bad name"` -> `{ reason: "invalid_block_name_format" }`. The tests must prove no parse/evaluate path is invoked for these failures by passing invalid macro syntax in the populated source where applicable and still receiving the source validation envelope, not `parse_error`.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-handler macro-source-ref</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-218|T-U-219|T-U-220|T-U-221|T-U-222|T-U-223" tests/unit/macro-handler.test.ts tests/unit/macro-source-ref.test.ts` finds all six IDs.
    - `rg -n "exactly_one_required|empty_source|empty_source_ref|invalid_source_ref_format|invalid_block_name_format" tests/unit/macro-handler.test.ts tests/unit/macro-source-ref.test.ts` finds every reason string.
    - Focused tests exit 0 after Plan 02 implementation.
  </acceptance_criteria>
  <done>Invalid source selection paths are pinned as canonical expected-error responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve existing named-block helper coverage</name>
  <files>tests/unit/macro-source-ref.test.ts, tests/unit/macro-fence-extractor.test.ts</files>
  <read_first>
    - tests/unit/macro-source-ref.test.ts
    - tests/unit/macro-fence-extractor.test.ts
    - src/macro/source-ref.ts
    - src/macro/fence-extractor.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Audit existing `T-U-010` through `T-U-018` and `T-U-001` through `T-U-009` coverage. If any helper test row from the Test Plan is missing, add it with exact reason strings: `no_macro_blocks`, `ambiguous_macro_block`, `block_not_found`, `duplicate_block_name`, and `malformed_fence_attributes`. Do not change production code in this task unless a test currently asserts the wrong canonical reason from the Macro Language Requirements.</action>
  <verify>
    <automated>npm test -- --reporter=verbose macro-source-ref macro-fence-extractor</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-010|T-U-011|T-U-012|T-U-013|T-U-014|T-U-015|T-U-016|T-U-017|T-U-018" tests/unit/macro-source-ref.test.ts` finds every source-ref helper row.
    - `rg -n "T-U-001|T-U-002|T-U-003|T-U-004|T-U-005|T-U-006|T-U-007|T-U-008|T-U-009" tests/unit/macro-fence-extractor.test.ts` finds every fence extractor row.
    - `npm test -- --reporter=verbose macro-source-ref macro-fence-extractor` exits 0.
  </acceptance_criteria>
  <done>Named-block helper tests are complete and ready for handler reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client to handler schema | Untrusted request fields enter `call_macro`. |
| Source selector to executor | Invalid selectors must not reach parse/evaluation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01-01 | T | handler schema | mitigate | T-U-216 proves only documented production fields are accepted/exposed. |
| T-138-01-02 | E | source validation | mitigate | T-U-218 through T-U-223 prove invalid source selectors halt before execution. |
| T-138-01-03 | I | error envelopes | mitigate | Tests assert stable `details.reason` strings without leaking raw file-system details. |
</threat_model>

<verification>
Run `npm test -- --reporter=verbose macro-handler macro-source-ref macro-fence-extractor`. After Plan 02, this command must pass.
</verification>

<success_criteria>
Phase 138 handler contract tests cover `MACRO-SRC-01` and `MACRO-SRC-02` with exact Test Plan IDs and canonical invalid-input reasons.
</success_criteria>

<output>
After completion, create `.planning/phases/138-handler-source-resolution-scenario-closure/138-01-SUMMARY.md`
</output>
