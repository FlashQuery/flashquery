---
phase: 138-handler-source-resolution-scenario-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 138-02
files_modified:
  - tests/integration/macro-write-lock.integration.test.ts
  - tests/e2e/macro-call-macro.test.ts
  - tests/config/vitest.integration.config.ts
autonomous: true
requirements:
  - MACRO-SRC-01
  - MACRO-SRC-02
  - MACRO-SRC-03
  - MACRO-SRC-04
  - MACRO-INT-02
must_haves:
  truths:
    - "Macro-executed writes inherit existing FlashQuery write-lock behavior through tool handlers."
    - "Real MCP transport coverage proves public `call_macro` success, dry-run, parse-error, and progress paths."
    - "No macro-specific write lock implementation is added."
  artifacts:
    - path: "tests/integration/macro-write-lock.integration.test.ts"
      provides: "T-I-009 through T-I-011 and MACRO-INT-02 coverage"
    - path: "tests/e2e/macro-call-macro.test.ts"
      provides: "T-E-001 through T-E-004 real transport coverage"
---

<objective>
Close macro write-lock inheritance and real MCP transport coverage.

Purpose: prove Phase 138's public handler behavior survives the real transport and that macros rely on existing tool-layer locks for writes instead of creating separate concurrency semantics.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-CONTEXT.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-RESEARCH.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-PATTERNS.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-VALIDATION.md
@.planning/phases/138-handler-source-resolution-scenario-closure/138-02-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add macro write-lock inheritance integration tests</name>
  <files>tests/integration/macro-write-lock.integration.test.ts, tests/config/vitest.integration.config.ts</files>
  <read_first>
    - tests/integration/archive-document-lock.test.ts
    - tests/integration/macro-concurrency.test.ts
    - tests/integration/macro-tool-dispatch.test.ts
    - tests/config/vitest.integration.config.ts
    - src/macro/dispatcher.ts
    - src/mcp/tools/documents.ts
    - src/services/write-lock.ts
    - src/mcp/tools/macro.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Create `tests/integration/macro-write-lock.integration.test.ts` and add it to `tests/config/vitest.integration.config.ts`. Add Test Plan rows `T-I-009`, `T-I-010`, and `T-I-011`. The tests must execute macros that call real FlashQuery write tools through the dispatcher, with locking enabled, and assert writes serialize or return the existing conflict envelope from `acquireLock` (`error: "conflict"`, `details.reason: "lock_contention"`). Include archive/remove concurrency coverage if the existing integration fixtures make this feasible. Assert by grep/read that production macro code does not call `acquireLock` directly; lock acquisition must occur in the underlying tool handlers.</action>
  <verify>
    <automated>npm run test:integration -- --reporter=verbose macro-write-lock archive-document-lock macro-concurrency</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-I-009|T-I-010|T-I-011|lock_contention|acquireLock|call_macro" tests/integration/macro-write-lock.integration.test.ts` finds all IDs and lock assertions.
    - `rg -n "macro-write-lock.integration.test.ts" tests/config/vitest.integration.config.ts` finds the include entry.
    - `! rg -n "acquireLock\\(" src/macro src/mcp/tools/macro.ts` succeeds, proving no macro-layer lock was added.
    - Focused integration command exits 0 or uses existing credential skip behavior.
  </acceptance_criteria>
  <done>MACRO-INT-02 is covered by integration tests that prove inheritance from the tool layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real MCP transport E2E coverage</name>
  <files>tests/e2e/macro-call-macro.test.ts</files>
  <read_first>
    - tests/e2e/http-transport.test.ts
    - tests/integration/macro-parse-error.test.ts
    - tests/integration/macro-source-ref.integration.test.ts
    - src/server/http.ts
    - src/mcp/server.ts
    - src/mcp/tools/macro.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
  </read_first>
  <action>Create `tests/e2e/macro-call-macro.test.ts` for Test Plan rows `T-E-001`, `T-E-002`, `T-E-003`, and `T-E-004`. Follow `tests/e2e/http-transport.test.ts` for `StreamableHTTPServerTransport` and SSE parsing. Cover: a real MCP `call_macro` success path with inline source; a dry-run request that returns `parsed_ok: true` and performs no side effect; a parse-error request returning `error: "parse_error"` with `isError: false`; and progress notification observation using `_meta.progressToken` and the existing `notifications/progress` path. Use ephemeral ports and avoid requiring a web UI.</action>
  <verify>
    <automated>npx vitest run tests/e2e/macro-call-macro.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-E-001|T-E-002|T-E-003|T-E-004|StreamableHTTPServerTransport|notifications/progress|progressToken" tests/e2e/macro-call-macro.test.ts` finds all IDs and transport/progress machinery.
    - `npx vitest run tests/e2e/macro-call-macro.test.ts --reporter=verbose` exits 0.
  </acceptance_criteria>
  <done>Real transport E2E coverage proves the public MCP handler behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Macro engine to write tools | Macro calls may trigger state mutation through existing tools. |
| HTTP/MCP transport to handler | Real clients submit macro requests and receive progress notifications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-03-01 | E | write locks | mitigate | Integration tests assert macro writes inherit existing lock contention behavior. |
| T-138-03-02 | D | concurrent writes | mitigate | T-I-009/T-I-010 verify serialization/conflict under lock pressure. |
| T-138-03-03 | S | progress token routing | mitigate | E2E progress test uses request `_meta.progressToken`; no cross-session token reuse. |
</threat_model>

<verification>
Run `npm run test:integration -- --reporter=verbose macro-write-lock archive-document-lock macro-concurrency` and `npx vitest run tests/e2e/macro-call-macro.test.ts --reporter=verbose`.
</verification>

<success_criteria>
Macro write operations are proven to inherit FlashQuery's existing write-lock table behavior, and the real MCP transport validates success, dry-run, parse-error, and progress paths.
</success_criteria>

<output>
After completion, create `.planning/phases/138-handler-source-resolution-scenario-closure/138-03-SUMMARY.md`
</output>
