---
phase: 143-diagnostic-cli-and-remaining-macro-extensions
plan: 3
type: tdd
wave: 2
depends_on:
  - 143-02
files_modified:
  - src/macro/tokens.ts
  - src/macro/types.ts
  - src/macro/parser.ts
  - src/macro/evaluator.ts
  - tests/unit/macro-parser.test.ts
  - tests/unit/macro-evaluator.test.ts
  - tests/macro-framework/cases/control-flow/03-for-with-if-fail.yml
  - tests/macro-framework/cases/control-flow/04-while-with-fail.yml
autonomous: true
requirements:
  - REQ-104
must_haves:
  truths:
    - "`continue` and `break` inside `for` and `while` loops execute with nearest-loop semantics."
    - "`continue` and `break` outside loops are parse-time errors."
    - "`break` exits only the loop block and execution continues after `done`."
  artifacts:
    - path: "src/macro/tokens.ts"
      provides: "Continue/Break tokens and reserved keywords"
    - path: "src/macro/types.ts"
      provides: "ContinueStmt and BreakStmt AST nodes"
    - path: "src/macro/evaluator.ts"
      provides: "Loop-control runtime signals"
  key_links:
    - from: "src/macro/parser.ts"
      to: "src/macro/evaluator.ts"
      via: "ContinueStmt/BreakStmt AST nodes"
      pattern: "ContinueStmt|BreakStmt"
---

<objective>
Add `continue` and `break` loop-control statements for REQ-104.

Purpose: Macro authors need normal loop-control behavior for rundoc workflows and guard clauses without abusing `fail` or `exit`.
Output: Lexer/parser/type/evaluator support plus unit tests for T-U-040..043.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first priority="mandatory">
1. /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
2. /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

These two source MCP Broker docs are authoritative. If any generated GSD artifact conflicts with them, the source MCP Broker docs win unless the user explicitly overrides.
</read_first>

@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-CONTEXT.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-RESEARCH.md
@src/macro/tokens.ts
@src/macro/types.ts
@src/macro/parser.ts
@src/macro/evaluator.ts
@tests/unit/macro-parser.test.ts
@tests/unit/macro-evaluator.test.ts
@tests/macro-framework/README.md
@tests/macro-framework/cases/control-flow/03-for-with-if-fail.yml
@tests/macro-framework/cases/control-flow/04-while-with-fail.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED parser tests for loop-control placement</name>
  <files>tests/unit/macro-parser.test.ts</files>
  <read_first>
    - tests/unit/macro-parser.test.ts
    - src/macro/parser.ts
    - src/macro/tokens.ts
    - src/macro/types.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-U-040: `continue` outside a loop is a parse-time error.
    - T-U-041: `break` outside a loop is a parse-time error.
    - Parser accepts `continue` and `break` inside `for`, `while`, and nested `if` bodies inside loops.
  </behavior>
  <action>Extend `tests/unit/macro-parser.test.ts` with RED tests named for T-U-040 and T-U-041. Assert a stable parse-error reason such as `loop_control_outside_loop` for top-level `continue` and `break`. Assert parsed AST node kinds for valid loop-control statements inside `for` and `while` bodies, including inside an `if` nested within a loop. Do not change production code in this task except test-only imports/types if required.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because `continue` and `break` are not recognized as statements.
    - Parse-time assertions do not accept runtime error behavior.
  </acceptance_criteria>
  <done>RED parser tests pin T-U-040/T-U-041 and valid AST shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parser and evaluator loop-control semantics</name>
  <files>src/macro/tokens.ts, src/macro/types.ts, src/macro/parser.ts, src/macro/evaluator.ts, tests/unit/macro-parser.test.ts, tests/unit/macro-evaluator.test.ts, tests/macro-framework/cases/control-flow/03-for-with-if-fail.yml, tests/macro-framework/cases/control-flow/04-while-with-fail.yml</files>
  <read_first>
    - src/macro/tokens.ts
    - src/macro/types.ts
    - src/macro/parser.ts
    - src/macro/evaluator.ts
    - tests/unit/macro-parser.test.ts
    - tests/unit/macro-evaluator.test.ts
    - tests/macro-framework/README.md
    - tests/macro-framework/cases/control-flow/03-for-with-if-fail.yml
    - tests/macro-framework/cases/control-flow/04-while-with-fail.yml
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-U-042: `continue` inside a `for` loop skips remaining statements for that iteration and proceeds to the next item.
    - T-U-043: `break` inside a `while` loop exits past `done` and following statements run.
    - Nested `if` inside a loop propagates loop control to the nearest containing loop.
  </behavior>
  <action>Add `Continue` and `Break` keyword tokens before `Identifier`, include them in `RESERVED_KEYWORDS`, add `ContinueStmt` and `BreakStmt` to `Statement`, and parse them with a loop-depth parameter so placement errors are parse-time. Add internal evaluator control signals such as `MacroContinueError` and `MacroBreakError`; throw them from `execStatement` for the new statement kinds and catch them only in `ForLoop` and `WhileLoop`. Preserve cancellation/progress checks and existing `exit`, `fail`, and `needs_user_input` behavior. Add evaluator tests for T-U-042 and T-U-043 in `tests/unit/macro-evaluator.test.ts`. Review the macro testing framework control-flow pilots that currently call `continue`/`break` Tier 2 pending; update them or add sibling cases so `npm run test:macro-framework` exercises real `continue` and `break` semantics after implementation.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-parser.test.ts tests/unit/macro-evaluator.test.ts</automated>
    <automated>npm run test:macro-framework</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `continue` outside loops and `break` outside loops return parse errors with line and near-token details.
    - `continue` in `for` skips later body statements for that iteration.
    - `break` in `while` exits the loop but does not halt the macro.
    - Existing loop, if, pipeline, and builtin tests remain green.
    - Macro testing framework control-flow coverage no longer describes `continue`/`break` as production-pending without a real Phase 143 coverage case.
  </acceptance_criteria>
  <done>REQ-104 unit tests pass and TypeScript exhaustiveness is updated for new statement kinds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Macro source to parser | Untrusted macro source controls loop statements. |
| Parser AST to evaluator | Parser validation must prevent impossible runtime loop-control states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-03-PARSE | Tampering | `src/macro/parser.ts` | mitigate | Enforce loop depth during parsing and reject loop-control outside loops before execution. |
| T-143-03-DOS | Denial of Service | `src/macro/evaluator.ts` | mitigate | Preserve existing timeout/cancellation checks in loops while adding continue/break handling. |
| T-143-03-CONFUSION | Spoofing | Macro control flow | mitigate | `break` exits only nearest loop and `continue` advances only nearest loop, matching source REQ-104. |
| T-143-SC | Tampering | npm installs | accept | No package-manager installs are planned; Package Legitimacy Audit says no new packages. |
</threat_model>

<verification>
Focused gate: `npm test -- --run tests/unit/macro-parser.test.ts tests/unit/macro-evaluator.test.ts`
Build gate: `npm run build`
</verification>

<success_criteria>
REQ-104 is implemented and ready for composed rundoc scenario closure in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-03-SUMMARY.md` when done.
</output>
