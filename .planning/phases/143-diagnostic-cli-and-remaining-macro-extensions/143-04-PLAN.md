---
phase: 143-diagnostic-cli-and-remaining-macro-extensions
plan: 4
type: tdd
wave: 3
depends_on:
  - 143-02
  - 143-03
files_modified:
  - src/macro/introspection.ts
  - tests/unit/macro-introspection.test.ts
  - tests/integration/mcp-broker/client-lifecycle.test.ts
  - tests/integration/macro-concurrency.test.ts
autonomous: true
requirements:
  - REQ-109
  - REQ-110
must_haves:
  truths:
    - "`<server>._exists()` calls `Broker.isConnected(serverId, { deepProbe: true, timeoutMs: 250 })`."
    - "`_exists()` returns false for unconfigured or hung-but-alive servers."
    - "Concurrent macros sharing one brokered server do not cross-contaminate responses, context, or trace data."
  artifacts:
    - path: "src/macro/introspection.ts"
      provides: "Deep-probe macro `_exists()` binding"
    - path: "tests/unit/macro-introspection.test.ts"
      provides: "REQ-109 unit contract"
    - path: "tests/integration/macro-concurrency.test.ts"
      provides: "T-I-050 shared broker macro safety"
  key_links:
    - from: "src/macro/introspection.ts"
      to: "src/services/mcp-broker/index.ts"
      via: "broker.isConnected(server, { deepProbe: true, timeoutMs: 250 })"
      pattern: "deepProbe.*true"
---

<objective>
Finalize macro broker health and concurrency contracts for REQ-109 and REQ-110.

Purpose: Macro guard clauses must not treat hung servers as healthy, and shared broker use must be proven safe at the macro layer.
Output: Deep-probe `_exists()` implementation plus focused unit/integration coverage, including T-I-050.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first priority="mandatory">
1. /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
2. /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

These two source MCP Broker docs are authoritative. If any generated GSD artifact conflicts with them, the source MCP Broker docs win unless the user explicitly overrides.
</read_first>

@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-CONTEXT.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-RESEARCH.md
@src/macro/introspection.ts
@src/services/mcp-broker/index.ts
@src/services/mcp-broker/client.ts
@tests/unit/macro-introspection.test.ts
@tests/integration/mcp-broker/client-lifecycle.test.ts
@tests/integration/macro-concurrency.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make macro `_exists()` use the required deep probe</name>
  <files>src/macro/introspection.ts, tests/unit/macro-introspection.test.ts, tests/integration/mcp-broker/client-lifecycle.test.ts</files>
  <read_first>
    - src/macro/introspection.ts
    - tests/unit/macro-introspection.test.ts
    - tests/integration/mcp-broker/client-lifecycle.test.ts
    - src/services/mcp-broker/client.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - REQ-109 / T-I-023: macro-facing `_exists()` calls deep probe with 250 ms timeout.
    - T-S-010 unit analogue: unconfigured/Null broker remains false.
    - T-S-011 integration support: deep probe catches a hung server where shallow probe would not.
  </behavior>
  <action>Update `src/macro/introspection.ts` so brokered `_exists()` calls `broker.isConnected(server, { deepProbe: true, timeoutMs: 250 })`. Replace the 5-second wrapper for macro-facing broker probes with the source-required 250 ms deep probe behavior. Update `tests/unit/macro-introspection.test.ts` expectations from one-argument `isConnected(server)` to the exact options object. Extend `tests/integration/mcp-broker/client-lifecycle.test.ts` only if the existing deep/shallow SIGSTOP tests do not already assert T-I-023 and T-I-024 clearly.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-introspection.test.ts</automated>
    <automated>npm run test:integration -- --run tests/integration/mcp-broker/client-lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit test asserts `broker.isConnected` called with `brave_search` and `{ deepProbe: true, timeoutMs: 250 }`.
    - Existing `fq._exists()` remains true without dispatching a tool handler.
    - Hung-server integration coverage remains green or is added under T-I-023.
  </acceptance_criteria>
  <done>REQ-109 focused unit and broker health integration gates pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared-server concurrent macro safety coverage</name>
  <files>tests/integration/macro-concurrency.test.ts</files>
  <read_first>
    - tests/integration/macro-concurrency.test.ts
    - src/mcp/tools/macro.ts
    - src/macro/registry.ts
    - src/services/mcp-broker/index.ts
    - tests/fixtures/mcp-servers/server-basic.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-050: macro M1 invokes `basic.slow(ms=200)` while macro M2 invokes `basic.echo(msg="m2")` concurrently and each receives its own response.
    - REQ-110: both macros share the same brokered server process and no per-server call mutex is added unless the test proves SDK id correlation fails.
    - Response, caller context, and trace data do not cross-contaminate.
  </behavior>
  <action>Extend `tests/integration/macro-concurrency.test.ts` with T-I-050 using the existing `McpBroker`, fixture server config, `runMacroSource`, and shared broker instance. Configure two concurrent source strings against the same `basic` server: one slow call and one echo call. Assert both complete, the echo response is not replaced by the slow response, trace arrays contain only each macro's expected labels/results, and `getClientDebugSnapshot('basic')?.spawnCount` is one or otherwise proves a shared client process. Do not add a broker-side mutex as part of this task.</action>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/macro-concurrency.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - T-I-050 test passes under the standard integration Vitest config.
    - Two concurrent macro calls complete with distinct expected payloads.
    - Shared broker spawn count or equivalent debug evidence shows a single server process.
    - No production concurrency lock is added unless the test failure demonstrates it is required.
  </acceptance_criteria>
  <done>REQ-110 has macro-level integration coverage and build remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Macro guard to broker health API | Macro source queries external server availability. |
| Concurrent macro frames to shared broker client | Independent macro invocations share one JSON-RPC transport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-04-HEALTH | Denial of Service | `src/macro/introspection.ts` | mitigate | Use deep `tools/list` probe with 250 ms timeout so hung servers return false. |
| T-143-04-CROSS | Tampering | shared brokered server calls | mitigate | Validate T-I-050 concurrent macro calls for response, context, and trace isolation. |
| T-143-04-CONFUSION | Elevation of Privilege | macro allowlist / consumer context | mitigate | Use existing `runMacroSource` registry/allowlist path and assert shared-server calls remain within configured visibility. |
| T-143-SC | Tampering | npm installs | accept | No package-manager installs are planned; Package Legitimacy Audit says no new packages. |
</threat_model>

<verification>
Focused gate: `npm test -- --run tests/unit/macro-introspection.test.ts`
Integration gate: `npm run test:integration -- --run tests/integration/mcp-broker/client-lifecycle.test.ts tests/integration/macro-concurrency.test.ts`
Build gate: `npm run build`
</verification>

<success_criteria>
REQ-109 and REQ-110 are implemented or proven at focused unit/integration levels and ready for scenario closure in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-04-SUMMARY.md` when done.
</output>
