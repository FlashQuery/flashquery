---
phase: 143-diagnostic-cli-and-remaining-macro-extensions
plan: 2
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/macro.ts
  - src/macro/evaluator.ts
  - src/macro/parser.ts
  - src/macro/types.ts
  - tests/unit/macro-self.test.ts
  - tests/macro-framework/cases/isolation/11-self-via-input-vars-with-state-notes.yml
autonomous: true
requirements:
  - REQ-103
must_haves:
  truths:
    - "A source_ref-loaded macro can read `_self.path`, `_self.frontmatter`, `_self.title`, `_self.tags`, and `_self.fq_id`."
    - "Inline macro source has no `_self` and accessing `_self.*` returns the required runtime error."
    - "`_self` is a macro-start snapshot and assignments to `_self.*` are parse-time errors."
  artifacts:
    - path: "tests/unit/macro-self.test.ts"
      provides: "T-U-038/T-U-039 coverage"
    - path: "src/mcp/tools/macro.ts"
      provides: "source_ref metadata extraction"
    - path: "src/macro/evaluator.ts"
      provides: "root `_self` binding"
  key_links:
    - from: "src/mcp/tools/macro.ts"
      to: "src/macro/evaluator.ts"
      via: "self snapshot option passed into evaluateProgram"
      pattern: "self"
---

<objective>
Implement `_self` source_ref binding for REQ-103.

Purpose: Rundoc macros need a stable, read-only view of their source document metadata so they can update the source document intentionally without relying on mutable live state.
Output: `_self` parser/runtime support, source_ref snapshot extraction, and unit tests for source_ref-only, read-only, and snapshot semantics.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first priority="mandatory">
1. /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
2. /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

These two source MCP Broker docs are authoritative. If any generated GSD artifact conflicts with them, the source MCP Broker docs win unless the user explicitly overrides.
</read_first>

@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-CONTEXT.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-RESEARCH.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-VALIDATION.md
@.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-PATTERNS.md
@src/mcp/tools/macro.ts
@src/macro/evaluator.ts
@src/macro/parser.ts
@src/macro/types.ts
@tests/unit/macro-source-ref.test.ts
@tests/macro-framework/README.md
@tests/macro-framework/cases/isolation/11-self-via-input-vars-with-state-notes.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED `_self` parser and runtime tests</name>
  <files>tests/unit/macro-self.test.ts</files>
  <read_first>
    - tests/unit/macro-self.test.ts
    - tests/unit/macro-source-ref.test.ts
    - src/mcp/tools/macro.ts
    - src/macro/parser.ts
    - src/macro/evaluator.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-U-038: inline source access to `_self.path` produces runtime error message equivalent to "`_self` is only available when the macro was loaded via source_ref."
    - T-U-038: evaluator option/source_ref path binds `_self.path`, `_self.frontmatter`, `_self.title`, `_self.tags`, and `_self.fq_id`.
    - T-U-039: `_self.frontmatter.x = 1` and `_self.path = "x"` are parse-time errors.
    - T-S-006/T-S-007 unit analogue: snapshot object is cloned so later mutation of the original parsed frontmatter does not change `_self.frontmatter`.
  </behavior>
  <action>Create `tests/unit/macro-self.test.ts` with RED tests using `parseMacroSource`, `evaluateProgram`, and `resolveMacroSourceForRequest` where useful. Use exact source test IDs T-U-038 and T-U-039 in test names. Include assertions for `_self.path`, `_self.title`, `_self.tags`, `_self.fq_id`, nested `_self.frontmatter.targets`, inline runtime error text, parse-error `details.reason`, and snapshot immutability.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-self.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The focused command fails before implementation for `_self` missing behavior.
    - Tests distinguish parse-time errors from runtime errors.
    - Tests assert the literal `_self` syntax from the source requirements, not a `$self` alias.
  </acceptance_criteria>
  <done>RED tests pin every REQ-103 behavior in the source docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind source_ref `_self` snapshot into macro evaluation</name>
  <files>src/mcp/tools/macro.ts, src/macro/evaluator.ts, src/macro/parser.ts, src/macro/types.ts, tests/unit/macro-self.test.ts, tests/macro-framework/cases/isolation/11-self-via-input-vars-with-state-notes.yml</files>
  <read_first>
    - src/mcp/tools/macro.ts
    - src/macro/evaluator.ts
    - src/macro/parser.ts
    - src/macro/types.ts
    - tests/unit/macro-self.test.ts
    - tests/macro-framework/README.md
    - tests/macro-framework/cases/isolation/11-self-via-input-vars-with-state-notes.yml
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <action>Extend the source_ref resolution result in `src/mcp/tools/macro.ts` to include a macro-start self snapshot from the resolved document: path relative to vault/source identifier, parsed frontmatter object, title, tags, and fq_id. Add an evaluator option such as `self`/`selfSnapshot` and bind it as the special `_self` root in the initial environment before program execution. Extend the parser/types only as needed to support `_self.*` field access exactly as source syntax and to reject assignments whose target starts with `_self.` at parse time. Inline source must not bind `_self`; evaluator access must produce the required runtime error. Clone snapshot data at the evaluator boundary so `_self.frontmatter` cannot auto-refresh. Review the macro testing framework isolation case that currently simulates `_self` via input vars; update it or add a sibling case so `npm run test:macro-framework` exercises real `_self` behavior instead of leaving Phase 142 workaround notes as the only framework coverage.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-self.test.ts tests/unit/macro-source-ref.test.ts</automated>
    <automated>npm run test:macro-framework</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `_self.path`, `_self.frontmatter.*`, `_self.title`, `_self.tags`, and `_self.fq_id` evaluate from source_ref-loaded macros.
    - Inline macro source access returns a runtime error containing `_self` and `source_ref`.
    - Assigning to `_self.*` returns a parse error before evaluation.
    - Snapshot values do not change after the original parsed metadata object is mutated in test.
    - Macro testing framework coverage either uses real `_self` or explicitly documents why the legacy input-vars workaround remains as separate backward-compatible coverage.
  </acceptance_criteria>
  <done>REQ-103 unit tests pass and no existing source_ref tests regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault document to macro runtime | Source document frontmatter/body become macro-visible data. |
| Macro source to parser | Untrusted macro source attempts writes or hidden field access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-02-SELF-MUT | Tampering | `src/macro/parser.ts` / `src/macro/evaluator.ts` | mitigate | Reject `_self.*` assignment at parse time and bind cloned snapshot data only. |
| T-143-02-INFO | Information Disclosure | `_self.frontmatter` | accept | `_self` exposes only the source document metadata already selected by `source_ref`; no external secrets are introduced. |
| T-143-02-CONFUSION | Spoofing | `_self` special root | mitigate | Bind `_self` only for source_ref invocation; inline access gets the specified runtime error. |
| T-143-SC | Tampering | npm installs | accept | No package-manager installs are planned; Package Legitimacy Audit says no new packages. |
</threat_model>

<verification>
Focused gate: `npm test -- --run tests/unit/macro-self.test.ts tests/unit/macro-source-ref.test.ts`
Build gate: `npm run build`
</verification>

<success_criteria>
REQ-103 is implemented at parser/runtime level and ready for directed/YAML/E2E closure in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/143-diagnostic-cli-and-remaining-macro-extensions/143-02-SUMMARY.md` when done.
</output>
