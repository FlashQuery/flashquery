---
phase: 129-correct-delegated-tier-eligibility-derivation
plan: 02
type: execute
wave: 2
depends_on:
  - 129-01
files_modified:
  - tests/unit/llm-tool-registry.test.ts
  - tests/unit/tool-exposure.test.ts
  - tests/integration/tool-registry.test.ts
  - tests/e2e/call-model-agent-loop.e2e.test.ts
autonomous: true
requirements:
  - POST-01
must_haves:
  truths:
    - "Delegated registry assembly expands corrected broad tiers through purpose config."
    - "Host catalog restrictions, per-purpose exclusions, and hard exclusions still compose after tier derivation changes."
    - "Host MCP tier exposure remains separate from delegated data-category filtering."
    - "An MCP-level or equivalent round-trip proves corrected delegated native tool names reach Mode 2 metadata."
  artifacts:
    - path: "tests/unit/llm-tool-registry.test.ts"
      provides: "Registry tier expansion and exclusion unit coverage"
      contains: "insert_in_doc"
    - path: "tests/integration/tool-registry.test.ts"
      provides: "I-tier-1 through I-tier-5 integration coverage"
      contains: "maintain_vault"
    - path: "tests/unit/tool-exposure.test.ts"
      provides: "Host exposure regression guard"
      contains: "get_llm_usage"
    - path: "tests/e2e/call-model-agent-loop.e2e.test.ts"
      provides: "MCP/equivalent delegated registry round-trip"
      contains: "replace_doc_section"
  key_links:
    - from: "src/mcp/tool-metadata.ts"
      to: "src/llm/tool-registry.ts"
      via: "TOOL_TIERS materialized from getToolNamesByTier"
      pattern: "TOOL_TIERS"
    - from: "tests/integration/tool-registry.test.ts"
      to: "src/llm/tool-registry.ts"
      via: "assembleNativeToolRegistry"
      pattern: "assembleNativeToolRegistry"
---

<objective>
Prove the corrected delegated tier derivation survives purpose registry assembly, host catalog intersection, exclusion layers, and MCP-level delegated metadata.

Purpose: Close §3.11.1.1 I-tier-1 through I-tier-5 plus the E2E/equivalent MCP proof while protecting unrelated host exposure behavior.
Output: Updated registry, host exposure, integration, and E2E tests that consume the Plan 01 metadata-derived tier outputs.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-CONTEXT.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-RESEARCH.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-VALIDATION.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@src/mcp/tool-metadata.ts
@src/llm/tool-registry.ts
@src/mcp/tool-exposure.ts
@tests/unit/llm-tool-registry.test.ts
@tests/unit/tool-exposure.test.ts
@tests/e2e/call-model-agent-loop.e2e.test.ts

<interfaces>
From `src/llm/tool-registry.ts`:
```typescript
export const TOOL_TIERS = {
  'tier:read-only': getToolNamesByTier('tier:read-only'),
  'tier:read-write': getToolNamesByTier('tier:read-write'),
} as const satisfies Record<string, readonly string[]>;
export function assembleNativeToolRegistry(
  config: FlashQueryConfig,
  purposeName: string,
  catalog: NativeToolDefinition[],
  options?: ToolRegistryAssemblyOptions
): ToolRegistryAssembly;
```

From `src/mcp/tool-exposure.ts`:
```typescript
export function resolveHostToolExposure(config?: HostMcpToolsConfig): ResolvedHostToolExposure;
```
</interfaces>
</context>

<source_traceability>
| Source | Coverage In This Plan |
|--------|------------------------|
| POST-01 | Verifies metadata-derived tiers are consumed by delegated registry assembly. |
| §3.11.1.1 I-tier-1 | `tools: ["tier:read-only"]` includes `list_vault`. |
| §3.11.1.1 I-tier-2 | `tools: ["tier:read-write"]` includes `copy_document`, `insert_in_doc`, and `replace_doc_section`. |
| §3.11.1.1 I-tier-3 | `excludedTools: ["insert_in_doc"]` removes the derived tier member. |
| §3.11.1.1 I-tier-4 | `call_model` stays hard-excluded even if requested. |
| §3.11.1.1 I-tier-5 | Explicit `maintain_vault` remains allowed through explicit purpose declaration when present in host catalog. |
| CONTEXT D-08 | `get_llm_usage` remains absent from delegated tiers and present in host read-only exposure. |
</source_traceability>

<phase_traceability_table>
| Requirement | Unit | Integration | E2E / MCP Equivalent | Directed Scenario | Integration Scenario | Coverage Ledger | Docs Evidence |
|-------------|------|-------------|-----------------------|-------------------|----------------------|-----------------|---------------|
| POST-01 / §3.11.1.1 | Plan 01 plus `tests/unit/llm-tool-registry.test.ts`, `tests/unit/tool-exposure.test.ts` | `tests/integration/tool-registry.test.ts` I-tier-1..I-tier-5 | `tests/e2e/call-model-agent-loop.e2e.test.ts` delegated registry round-trip | Plan 03: `tests/scenarios/directed/testcases/test_delegated_tier_eligibility.py` | Plan 03: `tests/scenarios/integration/tests/delegated_tier_eligibility.yml` | Plan 03: `DIRECTED_COVERAGE.md`, `INTEGRATION_COVERAGE.md`, `129-VALIDATION.md` | Plan 03: `docs/LLM Providers Models and Purposes.md`, `129-MIGRATION-CALLOUT.md` |
</phase_traceability_table>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update registry and host exposure unit guards</name>
  <files>tests/unit/llm-tool-registry.test.ts, tests/unit/tool-exposure.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md` sections 3.11.1 and 3.11.1.1
    - `src/mcp/tool-metadata.ts`
    - `src/llm/tool-registry.ts`
    - `src/mcp/tool-exposure.ts`
    - `tests/unit/llm-tool-registry.test.ts`
    - `tests/unit/tool-exposure.test.ts`
  </read_first>
  <behavior>
    - `TOOL_TIERS["tier:read-only"]` includes `list_vault`.
    - `TOOL_TIERS["tier:read-write"]` includes `copy_document`, `insert_in_doc`, and `replace_doc_section`.
    - Delegated `TOOL_TIERS` excludes `get_llm_usage`, while host `resolveHostToolExposure({ tools: ["tier:read-only"] })` still includes `get_llm_usage`.
  </behavior>
  <action>Update `READ_ONLY_TOOLS` and `READ_WRITE_EXTRA_TOOLS` expectations in `tests/unit/llm-tool-registry.test.ts` to match corrected metadata-derived broad tiers: add `list_vault` to read-only and add `copy_document`, `insert_in_doc`, and `replace_doc_section` to read-write extras while preserving deterministic order from `TOOL_METADATA`. Add explicit negative assertions that `TOOL_TIERS["tier:read-only"]` and `TOOL_TIERS["tier:read-write"]` do not contain `get_llm_usage`, `call_model`, `maintain_vault`, removed legacy names, or dead project names. Update the host-disabled catalog test that currently filters `['get_document', 'list_vault', 'search']` so its expected delegated result includes `list_vault` when host-enabled. In `tests/unit/tool-exposure.test.ts`, keep or strengthen the regression assertion that host `tier:read-only` contains `get_llm_usage`; do not change `src/mcp/tool-exposure.ts` to use delegated `DATA_CATEGORIES`.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool-registry.test.ts tests/unit/tool-exposure.test.ts</automated>
    <automated>grep -q 'list_vault' tests/unit/llm-tool-registry.test.ts && grep -q 'copy_document' tests/unit/llm-tool-registry.test.ts && grep -q 'insert_in_doc' tests/unit/llm-tool-registry.test.ts && grep -q 'replace_doc_section' tests/unit/llm-tool-registry.test.ts</automated>
    <automated>grep -q 'get_llm_usage' tests/unit/tool-exposure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/llm-tool-registry.test.ts` asserts corrected `TOOL_TIERS` membership for all four newly included tools.
    - `tests/unit/llm-tool-registry.test.ts` asserts delegated broad tiers do not include `get_llm_usage`.
    - `tests/unit/tool-exposure.test.ts` continues to assert host `tier:read-only` includes `get_llm_usage`.
    - No production code change is made in `src/mcp/tool-exposure.ts`.
  </acceptance_criteria>
  <done>Unit tests prove registry consumers see corrected tiers and host exposure behavior remains explicitly separate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add I-tier integration coverage through purpose config assembly</name>
  <files>tests/integration/tool-registry.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md` sections 3.11.1 and 3.11.1.1
    - `src/llm/tool-registry.ts`
    - `src/config/loader.ts`
    - `tests/unit/llm-tool-registry.test.ts`
    - `tests/integration/llm-config-sync.test.ts`
    - `tests/integration/tool-registry.test.ts` if it already exists
  </read_first>
  <behavior>
    - I-tier-1: purpose `tools: ["tier:read-only"]` assembles `nativeToolNames` containing `list_vault`.
    - I-tier-2: purpose `tools: ["tier:read-write"]` assembles `nativeToolNames` containing `copy_document`, `insert_in_doc`, and `replace_doc_section`.
    - I-tier-3: `excludedTools: ["insert_in_doc"]` removes the tool from a read-write tier expansion.
    - I-tier-4: `call_model` is not exposed even when requested.
    - I-tier-5: explicit `tools: ["maintain_vault"]` appears when host catalog includes `maintain_vault`.
  </behavior>
  <action>Create `tests/integration/tool-registry.test.ts` if it does not exist; otherwise extend it. Use the existing `assembleNativeToolRegistry` test patterns and a realistic `FlashQueryConfig` purpose object. Build a catalog containing the final registered native tool definitions needed for the assertions; include `list_vault`, `copy_document`, `insert_in_doc`, `replace_doc_section`, `call_model`, and `maintain_vault`. Add tests named or labelled for I-tier-1 through I-tier-5 from §3.11.1.1. Assert exact `nativeToolNames` membership for corrected tools, exact absence for `call_model`, exact `diagnostics.excluded` for `insert_in_doc`, and explicit `maintain_vault` inclusion when requested by name and present in the host catalog. Do not change `src/llm/tool-registry.ts` unless these tests reveal a stale hard-coded expectation; the intended consumer contract is already `TOOL_TIERS`.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/tool-registry.test.ts</automated>
    <automated>grep -q 'I-tier-1' tests/integration/tool-registry.test.ts && grep -q 'I-tier-5' tests/integration/tool-registry.test.ts</automated>
    <automated>grep -q 'maintain_vault' tests/integration/tool-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/tool-registry.test.ts` exists and contains I-tier-1, I-tier-2, I-tier-3, I-tier-4, and I-tier-5 coverage labels or test names.
    - Integration tests prove `list_vault`, `copy_document`, `insert_in_doc`, and `replace_doc_section` are accepted through purpose tier expansion.
    - Integration tests prove `call_model` hard exclusion and `insert_in_doc` per-purpose exclusion still win.
    - Integration tests prove explicit `maintain_vault` remains reachable by explicit purpose tool declaration when host-enabled.
  </acceptance_criteria>
  <done>Registry assembly integration coverage closes §3.11.1.1 I-tier-1 through I-tier-5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add MCP-equivalent delegated registry round-trip</name>
  <files>tests/e2e/call-model-agent-loop.e2e.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md` sections 3.11.1 and 3.11.1.1
    - `src/mcp/tools/llm.ts`
    - `src/llm/tool-registry.ts`
    - `tests/e2e/call-model-agent-loop.e2e.test.ts`
  </read_first>
  <behavior>
    - A managed `call_model` purpose run with a delegated tier emits metadata proving corrected native tool names are model-visible.
    - `metadata.tools.native_tool_names` or the existing equivalent diagnostics include `list_vault`, `copy_document`, `insert_in_doc`, and `replace_doc_section` for `tier:read-write`.
    - The same metadata excludes `get_llm_usage` and `call_model`.
  </behavior>
  <action>Extend `tests/e2e/call-model-agent-loop.e2e.test.ts` with the narrowest existing managed/mock-provider pattern that exercises `call_model` resolver `purpose` with a purpose configured for `tools: ["tier:read-write"]`. Assert the public response metadata tools block includes corrected delegated native tool names `list_vault`, `copy_document`, `insert_in_doc`, and `replace_doc_section`. Assert it excludes `get_llm_usage` and `call_model`. If the existing E2E harness cannot expose provider tools without a live provider, use the existing equivalent MCP round-trip pattern in this file that captures public `metadata.tools.native_tool_names` from a deterministic mock provider; do not introduce a new test server framework.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts</automated>
    <automated>grep -q 'replace_doc_section' tests/e2e/call-model-agent-loop.e2e.test.ts && grep -q 'get_llm_usage' tests/e2e/call-model-agent-loop.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/call-model-agent-loop.e2e.test.ts` has an MCP/equivalent round-trip assertion for corrected delegated `tier:read-write` metadata.
    - The E2E assertion names all four corrected tools exactly: `list_vault`, `copy_document`, `insert_in_doc`, and `replace_doc_section`.
    - The E2E assertion explicitly checks `get_llm_usage` and `call_model` are absent from delegated broad tier metadata.
  </acceptance_criteria>
  <done>E2E/equivalent MCP proof demonstrates the corrected delegated surface through a public `call_model` path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| purpose config -> registry assembly | User-authored purpose tools and exclusions become delegated model-visible functions. |
| host catalog -> delegated assembly | Host-hidden tools must not be regained by delegated tier or explicit selection. |
| MCP response metadata -> verification | Public metadata used as proof must reflect actual provider-visible tool definitions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-05 | Elevation of Privilege | `assembleNativeToolRegistry` | mitigate | Integration tests prove host catalog intersection, hard exclusions, and per-purpose exclusions still win after derived tier expansion. |
| T-129-06 | Information Disclosure | delegated `tier:read-only` / `tier:read-write` | mitigate | Unit/E2E tests assert `get_llm_usage` is absent from delegated broad tiers. |
| T-129-07 | Tampering | `src/mcp/tool-exposure.ts` | mitigate | Host exposure unit tests assert host read-only still includes `get_llm_usage`; no production host exposure edit is planned. |
| T-129-08 | Repudiation | Scenario/docs drift | mitigate | E2E metadata assertions provide public observable evidence before scenario/docs closure in Plan 03. |
</threat_model>

<verification>
Run:
- `npm test -- tests/unit/llm-tool-registry.test.ts tests/unit/tool-exposure.test.ts`
- `npm run test:integration -- tests/integration/tool-registry.test.ts`
- `npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts`
</verification>

<success_criteria>
POST-01 is proven beyond pure metadata: registry expansion, exclusions, host boundaries, and MCP-level delegated metadata all reflect the corrected broad tier behavior with no unrelated host exposure change.
</success_criteria>

<output>
After completion, create `.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-02-SUMMARY.md`.
</output>
