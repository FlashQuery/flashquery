---
phase: 129-correct-delegated-tier-eligibility-derivation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tool-metadata.ts
  - tests/unit/tool-metadata.test.ts
autonomous: true
requirements:
  - POST-01
must_haves:
  truths:
    - "Delegated tier membership is computed from canonical tool metadata, not a hand-maintained allow-list."
    - "`tier:read-only` includes `list_vault` and excludes `get_llm_usage`."
    - "`tier:read-write` includes the full read-only set plus `copy_document`, `insert_in_doc`, and `replace_doc_section`."
    - "Hard-excluded, removed, admin-tier, host-ineligible, and delegated-excluded tools do not enter broad delegated tiers."
  artifacts:
    - path: "src/mcp/tool-metadata.ts"
      provides: "Metadata-derived delegated tier eligibility"
      contains: "delegatedExclusionReason?: string"
    - path: "tests/unit/tool-metadata.test.ts"
      provides: "U-tier-1 through U-tier-9 durable unit coverage"
      contains: "list_vault"
  key_links:
    - from: "src/mcp/tool-metadata.ts"
      to: "src/llm/tool-registry.ts"
      via: "getToolNamesByTier"
      pattern: "getToolNamesByTier"
    - from: "tests/unit/tool-metadata.test.ts"
      to: "src/mcp/tool-metadata.ts"
      via: "direct helper assertions"
      pattern: "getToolNamesByTier\\('tier:read-write'\\)"
---

<objective>
Replace delegated broad-tier allow-list behavior with canonical metadata-derived eligibility and lock the derivation with focused unit tests.

Purpose: Close POST-01 / product requirements §3.11.1 at the source of truth before downstream registry, scenario, and docs evidence consumes the corrected tiers.
Output: `src/mcp/tool-metadata.ts` derives broad delegated tiers from metadata, and `tests/unit/tool-metadata.test.ts` covers §3.11.1.1 U-tier-1 through U-tier-9.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-CONTEXT.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-RESEARCH.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-VALIDATION.md
@.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@src/mcp/tool-metadata.ts
@tests/unit/tool-metadata.test.ts

<interfaces>
From `src/mcp/tool-metadata.ts`:
```typescript
export type ToolCategory = 'doc-read' | 'doc-write' | 'memory' | 'plugin' | 'llm' | 'system';
export type ToolTier = 'read-only' | 'read-write' | 'admin';
export type ToolTierSelector = 'tier:read-only' | 'tier:read-write';
export interface ToolMetadata {
  name: string;
  status: ToolStatus;
  categories: ToolCategory[];
  tier: ToolTier;
  hostEligible: boolean;
  delegatedEligible: boolean;
  delegatedHardExcludedReason?: string;
  legacyNames?: string[];
  replacement?: string;
  description: string;
}
export function getToolNamesByTier(tier: ToolTierSelector): string[];
```
</interfaces>
</context>

<source_traceability>
| Source | Coverage In This Plan |
|--------|------------------------|
| POST-01 | Removes `CURRENT_DELEGATED_TIER_ORDER` / `CURRENT_DELEGATED_TIER_TOOLS`; computes delegated tier eligibility from metadata. |
| §3.11.1 action 1-5 | Implemented in `src/mcp/tool-metadata.ts`. |
| §3.11.1 action 6-7 | Proved by exact unit membership and expected-diff assertions. |
| §3.11.1.1 U-tier-1..U-tier-9 | Implemented in `tests/unit/tool-metadata.test.ts`. |
| CONTEXT D-01 | Every task reads product requirements §3.11.1 / §3.11.1.1 before edits. |
| CONTEXT D-02..D-08 | Implemented and tested in metadata helper/unit layer. |
</source_traceability>

<phase_traceability_table>
| Requirement | Unit | Integration | E2E / MCP Equivalent | Directed Scenario | Integration Scenario | Coverage Ledger | Docs Evidence |
|-------------|------|-------------|-----------------------|-------------------|----------------------|-----------------|---------------|
| POST-01 / §3.11.1.1 | `tests/unit/tool-metadata.test.ts` U-tier-1..U-tier-9 | Plan 02: `tests/integration/tool-registry.test.ts` I-tier-1..I-tier-5 | Plan 02: `tests/e2e/call-model-agent-loop.e2e.test.ts` delegated registry round-trip | Plan 03: `tests/scenarios/directed/testcases/test_delegated_tier_eligibility.py` | Plan 03: `tests/scenarios/integration/tests/delegated_tier_eligibility.yml` | Plan 03: `DIRECTED_COVERAGE.md`, `INTEGRATION_COVERAGE.md`, `129-VALIDATION.md` | Plan 03: `docs/LLM Providers Models and Purposes.md`, `129-MIGRATION-CALLOUT.md` |
</phase_traceability_table>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive delegated tier eligibility from metadata</name>
  <files>src/mcp/tool-metadata.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md` sections 3.11.1 and 3.11.1.1
    - `.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-CONTEXT.md`
    - `.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-PATTERNS.md`
    - `src/mcp/tool-metadata.ts`
  </read_first>
  <behavior>
    - `getToolNamesByTier("tier:read-only")` returns only read-only tools passing common delegated eligibility.
    - `getToolNamesByTier("tier:read-write")` returns the read-only set plus read-write tools passing common delegated eligibility.
    - `get_llm_usage` is excluded from both broad delegated tiers because its category is `llm`.
  </behavior>
  <action>In `src/mcp/tool-metadata.ts`, remove `CURRENT_DELEGATED_TIER_ORDER` and `CURRENT_DELEGATED_TIER_TOOLS` completely per D-02. Add `delegatedExclusionReason?: string` to `ToolMetadata` next to `delegatedHardExcludedReason` per D-03. Add `DATA_CATEGORIES` containing exactly `doc-read`, `doc-write`, `memory`, and `plugin` per D-04. Add a shared helper such as `isDelegatedTierEligible(metadata)` that returns true only when `hostEligible === true`, `status !== "removed"`, `delegatedHardExcludedReason === undefined`, `delegatedExclusionReason === undefined`, and at least one category is in `DATA_CATEGORIES` per D-05. Refactor `current()` so `delegatedEligible` uses that helper and no name membership check. Refactor `getToolNamesByTier()` to iterate `TOOL_METADATA` in declaration order, apply the helper, include only `tier === "read-only"` for `tier:read-only`, and include read-only plus `tier === "read-write"` for `tier:read-write` per D-06. Do not edit `src/mcp/tool-exposure.ts`; host tier expansion must remain based on host metadata and must still include `get_llm_usage` for host `tier:read-only`.</action>
  <verify>
    <automated>npm test -- tests/unit/tool-metadata.test.ts</automated>
    <automated>grep -v '^#' src/mcp/tool-metadata.ts | grep -c 'CURRENT_DELEGATED_TIER_ORDER' | grep '^0$'</automated>
    <automated>grep -v '^#' src/mcp/tool-metadata.ts | grep -c 'CURRENT_DELEGATED_TIER_TOOLS' | grep '^0$'</automated>
    <automated>grep -q 'delegatedExclusionReason?: string' src/mcp/tool-metadata.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/tool-metadata.ts` has no `CURRENT_DELEGATED_TIER_ORDER` or `CURRENT_DELEGATED_TIER_TOOLS` tokens outside historical planning artifacts.
    - `src/mcp/tool-metadata.ts` contains `delegatedExclusionReason?: string`.
    - `src/mcp/tool-metadata.ts` contains the exact data-category values `doc-read`, `doc-write`, `memory`, and `plugin` in the delegated tier filter, while broad delegated tier filtering does not include `llm` or `system`.
    - `getToolNamesByTier("tier:read-only")` and `getToolNamesByTier("tier:read-write")` derive from `TOOL_METADATA`, not from a replacement name array.
  </acceptance_criteria>
  <done>Delegated broad tier eligibility is metadata-derived, common eligibility has one helper/rule path, and host exposure code remains untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add durable U-tier unit coverage</name>
  <files>tests/unit/tool-metadata.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md` sections 3.11.1 and 3.11.1.1
    - `.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-CONTEXT.md`
    - `src/mcp/tool-metadata.ts`
    - `tests/unit/tool-metadata.test.ts`
  </read_first>
  <behavior>
    - U-tier-1: read-only set includes `list_vault` and excludes `get_llm_usage`, read-write, and admin tools.
    - U-tier-2/U-tier-3: read-write set includes read-only set plus `copy_document`, `insert_in_doc`, and `replace_doc_section`.
    - U-tier-4..U-tier-8: hard-excluded, admin, removed, non-data-category, and delegated-excluded metadata are excluded.
    - U-tier-9: expected behavior diff is exactly `+list_vault`, `+copy_document`, `+insert_in_doc`, and `+replace_doc_section`.
  </behavior>
  <action>Update `tests/unit/tool-metadata.test.ts` to replace the current narrow expected delegated tier expectations with §3.11.1.1 U-tier coverage. Use exact arrays for `getToolNamesByTier("tier:read-only")` and `getToolNamesByTier("tier:read-write")` where practical, preserving `TOOL_METADATA` declaration order. Assert `list_vault` is in read-only, `copy_document`, `insert_in_doc`, and `replace_doc_section` are in read-write, every read-only tool is also in read-write, and `get_llm_usage` is in neither delegated broad tier. Spot-check hard-excluded tools `call_model`, `register_plugin`, `unregister_plugin`, `clear_pending_reviews`, `force_file_scan`, `reconcile_documents`, `maintain_vault`, and `get_plugin_info`. Spot-check removed tools `create_document`, `update_document`, `search_documents`, `save_memory`, `update_memory`, `create_record`, and `update_record`. Add a synthetic fixture/helper test for `delegatedExclusionReason` without setting the field on any production `TOOL_METADATA` entry. Add a durable expected-diff assertion whose added names are exactly `list_vault`, `copy_document`, `insert_in_doc`, and `replace_doc_section`; do not preserve the old allow-list in production code.</action>
  <verify>
    <automated>npm test -- tests/unit/tool-metadata.test.ts</automated>
    <automated>grep -q 'list_vault' tests/unit/tool-metadata.test.ts && grep -q 'copy_document' tests/unit/tool-metadata.test.ts && grep -q 'insert_in_doc' tests/unit/tool-metadata.test.ts && grep -q 'replace_doc_section' tests/unit/tool-metadata.test.ts</automated>
    <automated>grep -q 'get_llm_usage' tests/unit/tool-metadata.test.ts</automated>
    <automated>grep -q 'delegatedExclusionReason' tests/unit/tool-metadata.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tool-metadata.test.ts` contains assertions for U-tier-1 through U-tier-9 from §3.11.1.1.
    - The corrected tools `list_vault`, `copy_document`, `insert_in_doc`, and `replace_doc_section` are asserted in their correct broad delegated tier expansions.
    - `get_llm_usage` is explicitly asserted absent from both delegated broad tiers.
    - No current production `TOOL_METADATA` entry has a populated `delegatedExclusionReason`.
  </acceptance_criteria>
  <done>Unit coverage fails loudly for any return to hand-maintained delegated tier drift or accidental non-data-category exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| purpose config -> delegated model-visible native tools | User-authored `tools` selectors expand into functions a delegated model can call. |
| metadata registry -> host/delegated selectors | Canonical metadata controls both host and delegated exposure consumers. |
| tests/docs -> developer confidence | Coverage and docs must not falsely report corrected exposure while code still uses stale allow-lists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Information Disclosure / Elevation of Privilege | `getToolNamesByTier` in `src/mcp/tool-metadata.ts` | mitigate | Apply data-category filter exactly to `doc-read`, `doc-write`, `memory`, and `plugin`; unit-test `get_llm_usage` absence from both delegated broad tiers. |
| T-129-02 | Elevation of Privilege | `delegatedHardExcludedReason` and `delegatedExclusionReason` handling | mitigate | Common eligibility helper rejects both hard-excluded and delegated-excluded tools; tests spot-check hard-excluded names and synthetic delegated exclusion. |
| T-129-03 | Tampering | Hand-maintained replacement allow-list | mitigate | Remove `CURRENT_DELEGATED_TIER_ORDER` and `CURRENT_DELEGATED_TIER_TOOLS`; grep gate blocks the tokens in source. |
| T-129-04 | Information Disclosure | `src/mcp/tool-exposure.ts` host behavior | mitigate | Plan explicitly forbids host data-category filtering changes; Plan 02 runs host exposure regression tests. |
</threat_model>

<verification>
Run:
- `npm test -- tests/unit/tool-metadata.test.ts`
- `grep -v '^#' src/mcp/tool-metadata.ts | grep -c 'CURRENT_DELEGATED_TIER_ORDER' | grep '^0$'`
- `grep -v '^#' src/mcp/tool-metadata.ts | grep -c 'CURRENT_DELEGATED_TIER_TOOLS' | grep '^0$'`
</verification>

<success_criteria>
POST-01 metadata derivation is implemented with no replacement delegated tier allow-list, all D-01 through D-08 metadata-level decisions are represented in code/tests, and Plan 02 can consume `getToolNamesByTier` without registry rewrites.
</success_criteria>

<output>
After completion, create `.planning/phases/129-correct-delegated-tier-eligibility-derivation/129-01-SUMMARY.md`.
</output>
