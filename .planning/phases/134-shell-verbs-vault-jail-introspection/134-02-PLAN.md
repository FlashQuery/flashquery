---
phase: 134-shell-verbs-vault-jail-introspection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/macro/forbidden-flag-scan.ts
  - src/macro/evaluator.ts
  - tests/unit/macro-forbidden-flags.test.ts
autonomous: true
requirements:
  - MACRO-SHELL-03
must_haves:
  truths:
    - "Forbidden shell mutation flags are rejected before any macro statement executes."
    - "Forbidden flags are detected in nested loop, conditional, binding, expression, and pipeline positions."
    - "Allowed shell flags are not blocked by the forbidden-flag scanner."
  artifacts:
    - path: "src/macro/forbidden-flag-scan.ts"
      provides: "AST pre-scan for sed/find mutation flags"
      exports: ["preScanForbiddenShellFlags"]
    - path: "src/macro/evaluator.ts"
      provides: "pre-execution scanner wiring before input validation and execBlock"
      contains: ["preScanForbiddenShellFlags"]
    - path: "tests/unit/macro-forbidden-flags.test.ts"
      provides: "T-U-144 through T-U-150 forbidden flag coverage"
      contains: ["T-U-144", "T-U-150"]
  key_links:
    - from: "src/macro/evaluator.ts"
      to: "src/macro/forbidden-flag-scan.ts"
      via: "pre-exec call before `execBlock`"
      pattern: "preScanForbiddenShellFlags\\(program\\)"
    - from: "src/macro/forbidden-flag-scan.ts"
      to: "src/macro/types.ts"
      via: "recursive AST traversal"
      pattern: "case 'WhileLoop'"
---

<objective>
Add the forbidden shell flag pre-scan for read-only shell safety.

Purpose: MACRO-SHELL-03 requires `sed -i`, `sed --in-place`, `sed --i`, `find -exec`, `find --exec`, `find -delete`, and `find --delete` to fail before any statement can create trace/log/progress effects.
Output: scanner module, evaluator pre-exec wiring, and T-U-144 through T-U-150 tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-CONTEXT.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-RESEARCH.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-VALIDATION.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-PATTERNS.md
@.planning/phases/133-standard-library-builtins/133-VERIFICATION.md

<interfaces>
Canonical source-of-truth rule for every task in this plan: the first two files in `<read_first>` are mandatory and must be read before source edits.

Create this production contract:
```typescript
export function preScanForbiddenShellFlags(program: Program): void;
```

The scanner must throw expected errors compatible with `evaluateProgram`:
```typescript
new MacroExpectedError("forbidden_shell_flag", "Macro shell flag is forbidden.", {
  verb: "sed" | "find",
  flag: string,
  reason: "sed_in_place_mutates_files" | "find_exec_mutates_or_executes" | "find_delete_mutates_files",
  line: number,
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add forbidden flag scanner tests</name>
  <files>tests/unit/macro-forbidden-flags.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/shellbuiltins.ts
    - tests/unit/macro-forbidden-flags.test.ts
    - src/macro/evaluator.ts
    - src/macro/preflight.ts
    - src/macro/types.ts
    - tests/unit/macro-preflight.test.ts
    - tests/unit/macro-builtins.test.ts
  </read_first>
  <behavior>
    - T-U-144: `sed -i "s/a/b/" file` returns expected `forbidden_shell_flag`.
    - T-U-145: `sed --in-place "s/a/b/" file` returns expected `forbidden_shell_flag`.
    - T-U-146: `sed -ie "s/a/b/" file` returns expected `forbidden_shell_flag`.
    - T-U-147: `find / --exec echo {}` returns expected `forbidden_shell_flag`.
    - T-U-148: `find / --delete` returns expected `forbidden_shell_flag`.
    - T-U-149: a forbidden flag inside a `for` or `while` body is caught before execution.
    - T-U-150: `echo "before"` before a later forbidden flag does not append log or trace output.
  </behavior>
  <action>Create `tests/unit/macro-forbidden-flags.test.ts` with tests named by exact IDs T-U-144 through T-U-150. Drive failures through `evaluateProgram(parseProgram(source))`, not direct scanner calls only, so evaluator pre-exec ordering is proven. Include at least one direct scanner unit for nested AST traversal if it keeps the test clear, but the acceptance proof must include evaluator output with `isError: false`, `error: "forbidden_shell_flag"`, and empty side-effect channels for T-U-150.</action>
  <verify>
    <automated>npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-forbidden-flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-144|T-U-145|T-U-146|T-U-147|T-U-148|T-U-149|T-U-150" tests/unit/macro-forbidden-flags.test.ts` prints every ID.
    - Tests assert `error: "forbidden_shell_flag"` with `details.reason` values `sed_in_place_mutates_files`, `find_exec_mutates_or_executes`, and `find_delete_mutates_files`.
    - T-U-150 asserts no `echo "before"` text appears in returned `log` or `trace`.
  </acceptance_criteria>
  <done>Forbidden flag behavior is specified by failing tests before scanner implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement forbidden flag scan and evaluator preflight wiring</name>
  <files>src/macro/forbidden-flag-scan.ts, src/macro/evaluator.ts, tests/unit/macro-forbidden-flags.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/shellbuiltins.ts
    - src/macro/forbidden-flag-scan.ts
    - src/macro/evaluator.ts
    - src/macro/preflight.ts
    - src/macro/types.ts
    - tests/unit/macro-forbidden-flags.test.ts
  </read_first>
  <action>Create `src/macro/forbidden-flag-scan.ts` by following the recursive visitor shape in `src/macro/preflight.ts`. Visit every statement and expression kind in `src/macro/types.ts`, including `ForLoop`, `WhileLoop`, `IfStmt`, `Binding` RHS, pipelines, tool calls, object/list entries, field access targets, ranges, binary expressions, unary expressions, and nested call args. Detect forbidden flags only on calls named `sed` and `find`: for named args use `rawShortFlag` containing `i` for bundled sed short flags, names `i`, `in-place`, and `delete` or `exec` as applicable; for positional string flags catch literal `"-i"`, strings starting with `"-i"` such as `"-ie"`, `"--i"`, `"--in-place"`, `"-exec"`, `"--exec"`, `"-delete"`, and `"--delete"`. Throw `MacroExpectedError("forbidden_shell_flag", ...)` with concrete `verb`, `flag`, `reason`, and `line`. In `src/macro/evaluator.ts`, import and call `preScanForbiddenShellFlags(program)` before existing preflight/input validation and before `execBlock`. Do not add Phase 135 permission pre-scan or tool dispatch rules.</action>
  <verify>
    <automated>npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-forbidden-flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export function preScanForbiddenShellFlags|forbidden_shell_flag|sed_in_place_mutates_files|find_exec_mutates_or_executes|find_delete_mutates_files|case 'WhileLoop'" src/macro/forbidden-flag-scan.ts` finds all required strings.
    - `rg -n "preScanForbiddenShellFlags\\(program\\)" src/macro/evaluator.ts` proves evaluator wiring.
    - `npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-forbidden-flags.test.ts` exits 0.
  </acceptance_criteria>
  <done>T-U-144 through T-U-150 pass and forbidden shell flags halt before any macro execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macro AST -> shell execution | Macro author-supplied calls and flags reach shell builtin dispatch unless blocked. |
| preflight -> runtime | Scanner output determines whether execution may begin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-04 | Tampering | `sed` calls | mitigate | Reject `-i`, bundled `-i*`, `--i`, and `--in-place` before execution with `forbidden_shell_flag`. |
| T-134-05 | Elevation of Privilege | `find` calls | mitigate | Reject `-exec` and `--exec` before execution so macros cannot run arbitrary commands. |
| T-134-06 | Tampering | `find` calls | mitigate | Reject `-delete` and `--delete` before execution so macros cannot remove vault files. |
| T-134-07 | Repudiation | evaluator preflight order | mitigate | Run scanner before `execBlock` and prove no prior side effects in T-U-150. |
</threat_model>

<verification>
```bash
npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-forbidden-flags.test.ts
```
</verification>

<success_criteria>
MACRO-SHELL-03 is complete when T-U-144 through T-U-150 pass and evaluator preflight rejects forbidden shell flags before any statement executes.
</success_criteria>

<output>
After completion, create `.planning/phases/134-shell-verbs-vault-jail-introspection/134-02-SUMMARY.md`.
</output>
