---
phase: 134-shell-verbs-vault-jail-introspection
plan: 04
type: execute
wave: 3
depends_on:
  - 134-03
files_modified:
  - src/macro/introspection.ts
  - src/macro/types.ts
  - src/macro/parser.ts
  - src/macro/evaluator.ts
  - tests/unit/macro-introspection.test.ts
autonomous: true
requirements:
  - MACRO-SHELL-05
must_haves:
  truths:
    - "`fq._exists()` returns true without dispatching a tool handler."
    - "Brokered `<server>._exists()` calls `McpBroker.isConnected(serverId)` exactly once per evaluation."
    - "With `NullMcpBroker`, brokered `_exists()` returns false."
    - "Unsupported leading-underscore namespace methods parse and fail at runtime rather than as parse errors."
  artifacts:
    - path: "src/macro/introspection.ts"
      provides: "Namespace introspection resolver"
      exports: ["resolveNamespaceIntrospection"]
    - path: "src/macro/types.ts"
      provides: "AST method field for namespace introspection"
      contains: ["ToolExistsCall"]
    - path: "src/macro/parser.ts"
      provides: "Parser support for leading-underscore namespace calls"
      contains: ["_exists"]
    - path: "src/macro/evaluator.ts"
      provides: "Default broker-backed introspection wiring"
      contains: ["broker", "resolveNamespaceIntrospection"]
    - path: "tests/unit/macro-introspection.test.ts"
      provides: "T-U-152 through T-U-155 introspection coverage"
      contains: ["T-U-152", "T-U-155"]
  key_links:
    - from: "src/macro/evaluator.ts"
      to: "src/macro/introspection.ts"
      via: "evalToolExists delegates to resolver"
      pattern: "resolveNamespaceIntrospection"
    - from: "src/macro/introspection.ts"
      to: "src/services/mcp-broker.ts"
      via: "broker.isConnected(serverId)"
      pattern: "isConnected"
---

<objective>
Implement `_exists()` namespace introspection through native and broker layers.

Purpose: MACRO-SHELL-05 needs a minimal runtime introspection hook now, while full namespaced tool dispatch and permission rules remain Phase 135.
Output: `src/macro/introspection.ts`, AST/parser/evaluator support, and T-U-152 through T-U-155 tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-CONTEXT.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-RESEARCH.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-VALIDATION.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-PATTERNS.md
@.planning/phases/134-shell-verbs-vault-jail-introspection/134-03-SUMMARY.md
@.planning/phases/133-standard-library-builtins/133-VERIFICATION.md

<interfaces>
Canonical source-of-truth rule for every task in this plan: the first two files in `<read_first>` are mandatory and must be read before source edits.

Current broker contract:
```typescript
export interface McpBroker {
  isConnected(serverId: string): Promise<boolean>;
  getToolHandler(serverId: string, toolName: string): NativeToolHandler | null;
}
export class NullMcpBroker implements McpBroker;
```

Extend the AST minimally:
```typescript
export interface ToolExistsCall {
  kind: 'ToolExistsCall';
  server: string;
  method: string;
  line: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add namespace introspection tests</name>
  <files>tests/unit/macro-introspection.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/mockbrokers.ts
    - tests/unit/macro-introspection.test.ts
    - src/services/mcp-broker.ts
    - src/macro/evaluator.ts
    - src/macro/parser.ts
    - src/macro/types.ts
    - tests/unit/mcp-broker.test.ts
    - tests/unit/macro-parser.test.ts
  </read_first>
  <behavior>
    - T-U-152: `exit fq._exists()` returns `true`.
    - T-U-153: `exit brave_search._exists()` with `NullMcpBroker` returns `false`.
    - T-U-154: `exit fq._unknown_method()` parses and returns runtime error with `details.reason: "unsupported_introspection_method"`.
    - T-U-155: a mock broker records exactly one `isConnected("brave_search")` call per `_exists()` evaluation; two evaluations make two calls, proving no caching.
  </behavior>
  <action>Create `tests/unit/macro-introspection.test.ts` with exact IDs T-U-152 through T-U-155. Drive behavior through `evaluateProgram(parseProgram(source), { broker })` or the final evaluator option name chosen in Task 2. Assert `dispatchTool` is not called for `_exists()` by passing a throwing mock dispatch function. For T-U-154, first assert `parseProgram` succeeds for `fq._unknown_method()` so the unsupported method is a runtime error, not a parse error.</action>
  <verify>
    <automated>npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-introspection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-152|T-U-153|T-U-154|T-U-155" tests/unit/macro-introspection.test.ts` prints every ID.
    - T-U-154 includes an assertion that parsing `fq._unknown_method()` is successful before evaluation.
    - T-U-155 asserts exact broker call count and server ID.
  </acceptance_criteria>
  <done>Introspection behavior is specified by failing tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement native and brokered introspection</name>
  <files>src/macro/introspection.ts, src/macro/types.ts, src/macro/parser.ts, src/macro/evaluator.ts, tests/unit/macro-introspection.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/mockbrokers.ts
    - src/macro/introspection.ts
    - src/macro/evaluator.ts
    - src/macro/parser.ts
    - src/macro/types.ts
    - src/services/mcp-broker.ts
    - tests/unit/macro-introspection.test.ts
  </read_first>
  <action>Create `src/macro/introspection.ts` exporting `resolveNamespaceIntrospection(server, method, broker, context)` or equivalent. It must return `true` for `server === "fq"` and `method === "_exists"` without dispatching a handler. For any other server and `method === "_exists"`, call `await broker.isConnected(server)` exactly once and return that boolean; default to `new NullMcpBroker()` when the evaluator has no broker option. For any leading-underscore method other than `_exists`, throw `new MacroRuntimeError("Unsupported namespace introspection method.", line, { reason: "unsupported_introspection_method", server, method })`. Extend `ToolExistsCall` in `src/macro/types.ts` with `method`, update `src/macro/parser.ts` so all leading-underscore zero-arg namespace calls parse into that AST instead of failing parse for unknown methods, and update `src/macro/evaluator.ts` options/context with `broker?: McpBroker`. Replace the old `toolExists` fallback for this AST path with the resolver. Do not implement `_list_tools()`, `_capabilities()`, `_version()`, full tool dispatch, Phase 135 permissions, or broker transport.</action>
  <verify>
    <automated>npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-introspection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export .*resolveNamespaceIntrospection|unsupported_introspection_method|isConnected|NullMcpBroker" src/macro/introspection.ts src/macro/evaluator.ts` finds required strings.
    - `rg -n "method: string|method: tool\\.image|ToolExistsCall" src/macro/types.ts src/macro/parser.ts` proves AST method support.
    - `npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-introspection.test.ts` exits 0.
  </acceptance_criteria>
  <done>T-U-152 through T-U-155 pass and `_exists()` routes through native/broker layers without caching or dispatching tool handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macro namespace call -> engine introspection | Macro-authored leading-underscore methods enter engine-resolved runtime logic. |
| macro evaluator -> broker | Brokered server names are probed through `McpBroker.isConnected`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-12 | Spoofing | `fq._exists()` | mitigate | Return native `fq` availability directly without consulting spoofable broker registry state. |
| T-134-13 | Spoofing | brokered `_exists()` | mitigate | Call `McpBroker.isConnected(serverId)` on every evaluation and do not cache results. |
| T-134-14 | Elevation of Privilege | leading-underscore methods | mitigate | Reject unsupported methods with runtime `unsupported_introspection_method` and do not dispatch handlers. |
</threat_model>

<verification>
```bash
npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-introspection.test.ts
```
</verification>

<success_criteria>
MACRO-SHELL-05 is complete when T-U-152 through T-U-155 pass and unsupported leading-underscore methods fail at runtime while `_exists()` uses native/broker availability only.
</success_criteria>

<output>
After completion, create `.planning/phases/134-shell-verbs-vault-jail-introspection/134-04-SUMMARY.md`.
</output>
