---
phase: 157-records-memory-plugins-audit-guards
plan: 03
type: execute
wave: 2
depends_on:
  - 157-02
files_modified:
  - src/mcp/tools/plugins.ts
  - tests/integration/unregister-plugin-races.integration.test.ts
  - tests/unit/no-coarse-resource-locks.test.ts
autonomous: true
requirements:
  - REQ-023
must_haves:
  truths:
    - "Concurrent unregister_plugin calls leave one complete unregister result plus one clear expected response or conflict/error."
    - "Plugin unregister does not return success after partial cleanup failure."
    - "No coarse records, memory, or plugins lock literals remain in source."
    - "The phase has final execution evidence for T-U-036, T-I-043, T-I-044, and T-I-045."
  artifacts:
    - path: "src/mcp/tools/plugins.ts"
      provides: "unregister_plugin without coarse plugins lock and with scoped plugin coordination"
    - path: "tests/integration/unregister-plugin-races.integration.test.ts"
      provides: "T-I-045 concurrent unregister_plugin coverage"
    - path: "tests/unit/no-coarse-resource-locks.test.ts"
      provides: "T-U-036 static guard against coarse records/memory/plugins locks"
  key_links:
    - from: "src/mcp/tools/plugins.ts"
      to: "src/services/plugin-coordination-lock.ts"
      via: "withPluginCoordinationLock around unregister_plugin cleanup sequence"
      pattern: "withPluginCoordinationLock"
    - from: "tests/unit/no-coarse-resource-locks.test.ts"
      to: "src/mcp/tools"
      via: "source scan"
      pattern: "acquireLock.*records|acquireLock.*memory|acquireLock.*plugins"
---

<objective>
Guard concurrent plugin unregister operations and add the final no-coarse-resource-locks static gate.

Purpose: complete REQ-023 by ensuring plugins cannot be half-unregistered after coarse plugin lock removal and by proving records/memory/plugins no longer depend on the old lock table.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Downstream implementation and verification agents MUST read these product docs first before asking scope questions:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md

Then read:
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-CONTEXT.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-RESEARCH.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-PATTERNS.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-VALIDATION.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-RECONCILIATION-AUDIT.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-02-SUMMARY.md
@src/services/plugin-coordination-lock.ts
@src/mcp/tools/plugins.ts
@tests/unit/plugin-tools.test.ts
@tests/integration/crm.integration.test.ts
@tests/integration/plugin-registration.test.ts

Locked boundaries:
- Implement only the plugin unregister and final static-check slice of REQ-023.
- Reuse `withPluginCoordinationLock` from Plan 157-02; do not broaden it into the future document lock subsystem.
- Remove `acquireLock(..., "plugins", ...)` and `releaseLock(..., "plugins")` from `unregister_plugin`.
- Do not drop `fqc_write_locks`, remove `flashquery unlock`, or remove document/macro use of `src/services/write-lock.ts`.
- Do not replace the old plugins lock with another global lock; coordination key must include FQC instance id, plugin id, and plugin instance name.
</context>

<tasks>

<task id="157-03-01" type="auto" tdd="true">
  <name>Add concurrent unregister_plugin race coverage</name>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-CONTEXT.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-RESEARCH.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-PATTERNS.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-VALIDATION.md
    @src/services/plugin-coordination-lock.ts
    @src/mcp/tools/plugins.ts
    @tests/integration/crm.integration.test.ts
    @tests/integration/plugin-registration.test.ts
    @tests/unit/plugin-tools.test.ts
  </read_first>
  <files>tests/integration/unregister-plugin-races.integration.test.ts</files>
  <action>Create Test Plan §4.5.1 case `T-I-045` in `tests/integration/unregister-plugin-races.integration.test.ts`. Reuse plugin setup patterns from `tests/integration/crm.integration.test.ts` and `tests/integration/plugin-registration.test.ts`: register a plugin into `fqc_plugin_registry` for `config.instance.id`, create any necessary owned memory/document/pending-review rows that the current `unregister_plugin` cleanup touches, register `registerPluginTools`, then dispatch two concurrent `unregister_plugin({ plugin_id, force: true })` calls for the same plugin with `Promise.allSettled`. Assert one response contains `status: "unregistered"` and the other is a structured expected not-found/not-registered/conflict/error response, not a second success. After both settle, query relevant `fqc_*` tables touched by unregister cleanup, including `fqc_plugin_registry`, `fqc_pending_plugin_review`, and owned `fqc_memory` rows, and assert no partial-delete state remains. Use grep text `unregister-plugin` in the test name.</action>
  <verify>
    <automated>npm run test:integration -- --grep "unregister-plugin"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the test file contains `T-I-045`, two concurrent `unregister_plugin` calls, and post-race checks for `fqc_plugin_registry`, `fqc_pending_plugin_review`, and `fqc_memory`.
    - Behavior assertion: the race produces exactly one complete unregister result and one clear non-success response.
    - Command assertion: `npm run test:integration -- --grep "unregister-plugin"` exits 0 when `.env.test` is configured, or skips only through the standard integration env guard.
  </acceptance_criteria>
  <done>T-I-045 exists and exposes partial unregister or duplicate-success behavior before implementation.</done>
</task>

<task id="157-03-02" type="auto" tdd="true">
  <name>Migrate unregister_plugin off coarse plugins lock</name>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-CONTEXT.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-RESEARCH.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-PATTERNS.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-VALIDATION.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-02-SUMMARY.md
    @src/services/plugin-coordination-lock.ts
    @src/mcp/tools/plugins.ts
    @tests/unit/plugin-tools.test.ts
    @tests/integration/unregister-plugin-races.integration.test.ts
  </read_first>
  <files>src/mcp/tools/plugins.ts, tests/unit/plugin-tools.test.ts</files>
  <action>In `src/mcp/tools/plugins.ts`, remove the `acquireLock` / `releaseLock` import from `../../services/write-lock.js`, remove the `config.locking.enabled` block that acquires resource `"plugins"`, and remove the `finally` release for `"plugins"`. Import `withPluginCoordinationLock` from `../../services/plugin-coordination-lock.js`. Wrap the entire `unregister_plugin` inventory and cleanup sequence after parameter parsing in `withPluginCoordinationLock(config, { pluginId: plugin_id, pluginInstance: instanceName }, async () => { ... })`, using the same default `instanceName` logic already used by register/get flows. Keep the existing unknown-plugin response shape with `error: "not_found"` for the second racer after the first delete wins. Change cleanup blocks that currently log and continue on Supabase `update`/`delete` failures so they return `jsonRuntimeError(...)` or throw to the outer catch before returning `status: "unregistered"`; do not claim success after failed `fqc_memory`, `fqc_pending_plugin_review`, `fqc_plugin_registry`, document ownership, `PluginManager`, type registry, or manifest reload cleanup. Add a concise code comment near the wrapper referencing `REQ-023` and explaining the key is per FQC instance, plugin id, and plugin instance, not a global `"plugins"` lock. Update `tests/unit/plugin-tools.test.ts` mocks only as needed for the removed write-lock dependency and new coordination helper.</action>
  <verify>
    <automated>npm test -- tests/unit/plugin-tools.test.ts</automated>
    <automated>npm run test:integration -- --grep "unregister-plugin"</automated>
    <automated>rg "acquireLock|releaseLock|('plugins'|\\\"plugins\\\")" src/mcp/tools/plugins.ts && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/mcp/tools/plugins.ts` contains no `acquireLock`, no `releaseLock`, and no coarse lock resource literal `"plugins"` / `'plugins'`.
    - Source assertion: `unregister_plugin` calls `withPluginCoordinationLock` with `{ pluginId: plugin_id, pluginInstance: instanceName }`.
    - Source assertion: cleanup failures cannot fall through to a `status: "unregistered"` result.
    - Behavior assertion: T-I-045 passes and exactly one concurrent unregister can claim completion.
  </acceptance_criteria>
  <done>`unregister_plugin` is coordinated per plugin instance and no longer returns success after partial cleanup failure.</done>
</task>

<task id="157-03-03" type="auto" tdd="true">
  <name>Add final no-coarse-resource-locks gate</name>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-CONTEXT.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-RESEARCH.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-PATTERNS.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-VALIDATION.md
    @src/mcp/tools/memory.ts
    @src/mcp/tools/records.ts
    @src/mcp/tools/plugins.ts
    @src/services/write-lock.ts
  </read_first>
  <files>tests/unit/no-coarse-resource-locks.test.ts</files>
  <action>Create Test Plan §4.5.1 case `T-U-036` in `tests/unit/no-coarse-resource-locks.test.ts`. Implement a Vitest source scan over `src/` that fails if it finds an `acquireLock` or `releaseLock` call whose argument list contains `'records'`, `"records"`, `'memory'`, `"memory"`, `'plugins'`, or `"plugins"`. Keep the test narrow: it must not fail on remaining document lock use, `src/services/write-lock.ts` itself, comments in product docs, or future Phase 158 table-retirement work. Include grep text `no-coarse-resource-locks` in the `describe` or test name. This is the final phase gate and must be run after Plans 157-01 and 157-02 have removed memory and records lock calls.</action>
  <verify>
    <automated>npm test -- --grep "no-coarse-resource-locks"</automated>
    <automated>npm run test:integration -- --grep "memory-no-coarse-lock|records-reconciliation|unregister-plugin"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/unit/no-coarse-resource-locks.test.ts` contains `T-U-036` and scans `src/`.
    - Source assertion: the static test rejects `records`, `memory`, and `plugins` lock call arguments but allows unrelated document lock code.
    - Command assertion: both required ROADMAP evidence commands pass: `npm test -- --grep "no-coarse-resource-locks"` and `npm run test:integration -- --grep "memory-no-coarse-lock|records-reconciliation|unregister-plugin"`.
  </acceptance_criteria>
  <done>T-U-036 exists and the full Phase 157 required evidence passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller -> `unregister_plugin` | Concurrent callers can attempt to unregister the same plugin. |
| `unregister_plugin` -> Supabase tables | Multi-table cleanup can partially fail without checked error handling. |
| `unregister_plugin` -> PluginManager/manifest state | In-memory/plugin filesystem state must not claim success if DB cleanup failed. |
| Static gate -> source tree | Guard must catch old coarse lock usage without blocking document locks owned by later phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-08 | Tampering | partial unregister state | mitigate | Wrap `unregister_plugin` in `withPluginCoordinationLock` and return/throw on cleanup errors before `status: "unregistered"`. |
| T-157-09 | Repudiation | duplicate unregister success | mitigate | T-I-045 asserts one success plus one clear non-success response under concurrent calls. |
| T-157-10 | Tampering | accidental global plugins lock replacement | mitigate | Lock helper key includes FQC instance, plugin id, and plugin instance; grep rejects `"plugins"` coarse lock usage. |
| T-157-11 | Information Disclosure | static scan output | accept | Test only reports file paths and snippets in local developer output; no runtime exposure. |
| T-157-12 | Denial of Service | hidden memory/records/plugins race regressions | mitigate | Final gate runs T-U-036 plus T-I-043, T-I-044, and T-I-045 evidence commands. |
| T-157-SC | Tampering | package installs | accept | No package-manager installs are planned for this plan. |
</threat_model>

<verification>
- `npm test -- tests/unit/plugin-tools.test.ts`
- `npm test -- --grep "no-coarse-resource-locks"`
- `npm run test:integration -- --grep "unregister-plugin"`
- `npm run test:integration -- --grep "memory-no-coarse-lock|records-reconciliation|unregister-plugin"`
</verification>

<success_criteria>
1. `src/mcp/tools/plugins.ts` has no coarse plugins lock usage.
2. Concurrent unregister is coordinated per plugin instance and cannot claim success after partial cleanup failure.
3. T-U-036, T-I-043, T-I-044, and T-I-045 are implemented and runnable through the required ROADMAP evidence commands.
4. Phase 157 leaves document/macro lock-table retirement work untouched for later phases.
</success_criteria>

<output>
Create `.planning/phases/157-records-memory-plugins-audit-guards/157-03-SUMMARY.md` when done.
</output>
