---
phase: 157-records-memory-plugins-audit-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/memory.ts
  - tests/integration/memory-no-coarse-lock.integration.test.ts
autonomous: true
requirements:
  - REQ-023
must_haves:
  truths:
    - "Concurrent write_memory updates continue to converge through fqc_memory_create_version after the coarse memory lock is removed."
    - "write_memory does not introduce a replacement mutex, advisory lock, table lock, or global memory lock."
    - "Existing non_latest_memory_version and not_found conflict envelopes remain caller-visible."
  artifacts:
    - path: "src/mcp/tools/memory.ts"
      provides: "write_memory handler without acquireLock/releaseLock memory usage"
    - path: "tests/integration/memory-no-coarse-lock.integration.test.ts"
      provides: "T-I-043 concurrent memory update integration coverage"
  key_links:
    - from: "src/mcp/tools/memory.ts"
      to: "fqc_memory_create_version"
      via: "Supabase RPC in write_memory update mode"
      pattern: "rpc\\('fqc_memory_create_version'"
    - from: "tests/integration/memory-no-coarse-lock.integration.test.ts"
      to: "src/mcp/tools/memory.ts"
      via: "registered write_memory handler"
      pattern: "Promise\\.all.*write_memory|write_memory.*Promise\\.all"
---

<objective>
Remove the redundant coarse memory lock while proving memory update races still converge through the existing database versioning RPC.

Purpose: satisfy the memory portion of REQ-023 without replacing `acquireLock('memory', ...)` with another lock.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Downstream implementation and verification agents MUST read these product docs first before asking scope questions:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md

Then read:
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-CONTEXT.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-RESEARCH.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-PATTERNS.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-VALIDATION.md
@.planning/phases/156-atomic-durable-write-primitive-consolidation/156-VERIFICATION.md
@src/mcp/tools/memory.ts
@tests/unit/write-memory.test.ts
@tests/integration/write-memory.integration.test.ts

Locked boundaries:
- Implement only the memory slice of REQ-023.
- Remove `acquireLock` / `releaseLock` usage for resource literal `'memory'` from `src/mcp/tools/memory.ts`.
- Do not add a replacement lock for memory: no mutex, advisory lock, table lock, or new global coordination helper.
- Preserve `write_memory` update-mode handling of `fqc_memory_create_version`, including RPC error code `23505`, RPC error code `P0002`, and `details.reason = "non_latest_memory_version"`.
</context>

<tasks>

<task id="157-01-01" type="auto" tdd="true">
  <name>Add concurrent memory update integration test</name>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-CONTEXT.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-RESEARCH.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-PATTERNS.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-VALIDATION.md
    @src/mcp/tools/memory.ts
    @tests/integration/write-memory.integration.test.ts
    @tests/unit/write-memory.test.ts
  </read_first>
  <files>tests/integration/memory-no-coarse-lock.integration.test.ts</files>
  <action>Create Test Plan §4.5.1 case `T-I-043` in `tests/integration/memory-no-coarse-lock.integration.test.ts`. Reuse the existing integration server harness pattern from `tests/integration/write-memory.integration.test.ts`: register `registerMemoryTools`, create one memory with `write_memory({ mode: "create", content, tags })`, then dispatch two concurrent update calls against the same `memory_id` with `Promise.allSettled`. Assert exactly one latest row remains for the chain by querying `fqc_memory` for `previous_version_id` / `id` descendants under `config.instance.id` and counting `is_latest = true`. Assert one operation succeeds and the contending operation either returns the existing expected `conflict` envelope with `details.reason = "non_latest_memory_version"` or another existing expected non-latest response from the RPC path. Use the grep text `memory-no-coarse-lock` in the `describe` or test name so the ROADMAP command selects it.</action>
  <verify>
    <automated>npm run test:integration -- --grep "memory-no-coarse-lock"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/integration/memory-no-coarse-lock.integration.test.ts` contains `T-I-043` and `Promise.allSettled` or `Promise.all` around two `write_memory` update calls.
    - Behavior assertion: the test proves no chain has two rows with `is_latest = true` after the race.
    - Command assertion: `npm run test:integration -- --grep "memory-no-coarse-lock"` exits 0 when `.env.test` is configured, or skips only through the standard integration env guard.
  </acceptance_criteria>
  <done>T-I-043 exists and fails before the implementation if the handler still depends on a coarse memory lock or produces duplicate latest rows.</done>
</task>

<task id="157-01-02" type="auto" tdd="true">
  <name>Remove write_memory coarse lock without replacement</name>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-CONTEXT.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-RESEARCH.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-PATTERNS.md
    @.planning/phases/157-records-memory-plugins-audit-guards/157-VALIDATION.md
    @src/mcp/tools/memory.ts
    @tests/unit/write-memory.test.ts
    @tests/integration/memory-no-coarse-lock.integration.test.ts
  </read_first>
  <files>src/mcp/tools/memory.ts, tests/unit/write-memory.test.ts</files>
  <action>In `src/mcp/tools/memory.ts`, remove the `acquireLock` / `releaseLock` import from `../../services/write-lock.js`, remove the `config.locking.enabled` block that calls `acquireLock(..., "memory", { ttlSeconds: config.locking.ttlSeconds })`, and remove the `finally` release block for resource `"memory"`. Leave the `try/catch` around `write_memory` intact. Do not add any replacement coordination. Keep the existing `supabase.rpc("fqc_memory_create_version", ...)` update path and preserve mappings for `insertError.code === "23505"`, messages containing `Cannot update a non-latest memory version`, and `insertError.code === "P0002"`. Update `tests/unit/write-memory.test.ts` only as needed to stop mocking `../../services/write-lock.js`; do not weaken assertions for `non_latest_memory_version` or `Memory not found` envelopes.</action>
  <verify>
    <automated>npm test -- tests/unit/write-memory.test.ts</automated>
    <automated>npm run test:integration -- --grep "memory-no-coarse-lock"</automated>
    <automated>rg "('memory'|\\\"memory\\\")" src/mcp/tools/memory.ts | rg "acquireLock|releaseLock" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/mcp/tools/memory.ts` has no `acquireLock`, no `releaseLock`, and no literal lock resource `"memory"` or `'memory'` passed to a lock helper.
    - Source assertion: `src/mcp/tools/memory.ts` still calls `fqc_memory_create_version`.
    - Behavior assertion: update races still return one success plus an expected conflict/not-found style response, never two latest rows.
  </acceptance_criteria>
  <done>The memory handler relies only on `fqc_memory_create_version` for update races and the focused unit/integration checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller -> `write_memory` | Untrusted caller can dispatch concurrent updates to the same memory chain. |
| `write_memory` -> Supabase RPC | Correctness depends on `fqc_memory_create_version` row locking and uniqueness constraints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-01 | Tampering | `write_memory` update mode | mitigate | Keep all update races routed through `fqc_memory_create_version`; T-I-043 asserts no duplicate `is_latest = true` rows. |
| T-157-02 | Denial of Service | accidental replacement global memory lock | mitigate | Task action forbids replacement mutex/advisory/table locks and source grep checks no memory lock helper remains. |
| T-157-03 | Repudiation | conflict response mapping | mitigate | Preserve `non_latest_memory_version` and `not_found` envelopes and run existing `write-memory` unit coverage. |
| T-157-SC | Tampering | package installs | accept | No package-manager installs are planned for this plan. |
</threat_model>

<verification>
- `npm test -- tests/unit/write-memory.test.ts`
- `npm run test:integration -- --grep "memory-no-coarse-lock"`
</verification>

<success_criteria>
1. `src/mcp/tools/memory.ts` no longer imports or calls `acquireLock` / `releaseLock`.
2. No replacement memory lock is introduced.
3. T-I-043 proves concurrent updates converge through `fqc_memory_create_version`.
</success_criteria>

<output>
Create `.planning/phases/157-records-memory-plugins-audit-guards/157-01-SUMMARY.md` when done.
</output>
