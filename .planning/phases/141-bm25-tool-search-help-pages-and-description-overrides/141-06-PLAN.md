---
phase: 141-bm25-tool-search-help-pages-and-description-overrides
plan: 6
type: execute
wave: 4
depends_on:
  - 141-01
  - 141-02
  - 141-04
  - 141-05
files_modified:
  - src/services/tool-search/tool-search-service.ts
  - src/services/mcp-broker/types.ts
  - src/services/mcp-broker/index.ts
  - src/mcp/server.ts
  - tests/unit/tool-search/search-tools-handler.test.ts
  - tests/integration/tool-search/host-index.integration.test.ts
autonomous: true
requirements:
  - REQ-010
  - REQ-087
  - REQ-100
must_haves:
  truths:
    - "`host.tool_search: enabled` builds a host BM25 index at session start."
    - "The host index includes FQ-native tools plus brokered tools visible through `host.mcp_servers`."
    - "Host-visible brokered list changes synchronously update the host index within 1 second."
  artifacts:
    - path: "src/services/tool-search/tool-search-service.ts"
      provides: "Host index lifecycle and host-visible ToolIndexSink filtering"
    - path: "tests/integration/tool-search/host-index.integration.test.ts"
      provides: "T-I-038, T-I-039, and T-I-040 coverage"
    - path: "src/services/mcp-broker/index.ts"
      provides: "list_changed add/remove fanout into host index sink"
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/services/tool-search/tool-search-service.ts"
      via: "host.toolSearch enabled startup build"
      pattern: "host.*toolSearch.*enabled"
    - from: "src/services/mcp-broker/index.ts"
      to: "src/services/tool-search/tool-search-service.ts"
      via: "ToolIndexSink addTools/removeTools"
      pattern: "indexSink"
---

<objective>
Implement concrete host search index lifecycle for REQ-087.

Purpose: Host search is required in Phase C when `host.tool_search: enabled`; a host-capable hook is not enough.
Output: Host index creation, host-visible brokered update filtering, and explicit T-I-038..040 integration tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-CONTEXT.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-RESEARCH.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-PATTERNS.md
@.planning/phases/140-tofu-schema-pinning-and-tool-list-change-handling/140-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<interfaces>
From Plan 141-01:
`PureBM25Indexer.addTools/removeTools/search/getStats`.

From Plan 141-02/04:
`TOOL_META` supplies FQ-native docs for the host-visible native portion.

From Plan 141-05:
`ToolSearchService` and `fq.search_tools` exist for per-purpose search and can be extended with host index lifecycle.

From `src/services/mcp-broker/types.ts`:
export interface ToolIndexSink { addTools(tools: BrokeredTool[]): void; removeTools(keys: RegistryKey[]): void; }
export type ConsumerContext = { kind: 'host'; traceId: string; interactive?: boolean } | { kind: 'purpose'; purposeId: string; traceId: string; interactive?: boolean };

Canonical source priority: if generated plan text conflicts with the MCP Broker Requirements or MCP Broker Test Plan, implement the canonical docs and mention the conflict in SUMMARY.md.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build host index at session start</name>
  <files>src/services/tool-search/tool-search-service.ts, src/services/mcp-broker/types.ts, src/mcp/server.ts, tests/integration/tool-search/host-index.integration.test.ts</files>
  <read_first>
    @src/config/loader.ts
    @src/mcp/server.ts
    @src/services/tool-search/tool-search-service.ts
    @src/services/mcp-broker/index.ts
    @tests/integration/mcp-broker/tofu-list-changed.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-038: host index is built at session start when `host.tool_search: enabled`.
    - REQ-087: host index covers FQ-native tools and brokered tools visible through `host.mcp_servers`.
    - REQ-100: brokered host index docs use downstream `description`, including `description_override`.
  </behavior>
  <action>When parsed config has `host.toolSearch === "enabled"`, create/build a host index during MCP server startup/session initialization. Populate it with host-visible FQ-native tools plus brokered tools from `host.mcpServers` using the same registry and consumer-visibility filter as other broker consumers. If `host.tool_search` is disabled, do not build the host index. Provide a test-visible way to query host index stats/results without exposing a new public MCP tool beyond the canonical `fq.search_tools` behavior.</action>
  <acceptance_criteria>
    - Behavior assertion: enabled host config builds a nonempty host index containing at least one FQ-native tool.
    - Behavior assertion: enabled host config with `host.mcp_servers:[basic]` includes a brokered host-visible tool.
    - Behavior assertion: disabled host config does not create/build the host index.
    - Behavior assertion: brokered host result description reflects `description_override`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/tool-search/host-index.integration.test.ts</automated>
  </verify>
  <done>REQ-087 host startup lifecycle is concrete and T-I-038 is covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update host index on host-visible list_changed</name>
  <files>src/services/tool-search/tool-search-service.ts, src/services/mcp-broker/index.ts, tests/integration/tool-search/host-index.integration.test.ts</files>
  <read_first>
    @src/services/mcp-broker/index.ts
    @src/services/mcp-broker/registry.ts
    @src/services/tool-search/tool-search-service.ts
    @tests/integration/mcp-broker/tofu-list-changed.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-039: host index updates on `list_changed` for host-visible servers.
    - T-I-040: `list_changed` triggers indexer `addTools` and `removeTools` within 1 second of receipt.
  </behavior>
  <action>Implement the host index as a real `ToolIndexSink` subscriber or equivalent sink path behind the existing Phase 140 list-changed fanout. Apply host consumer visibility at `addTools` and `removeTools` time so only tools from servers visible to `host.mcp_servers` affect the host index. Use the existing synchronous `McpBroker.applyToolListSnapshot` add/remove seam; do not introduce polling.</action>
  <acceptance_criteria>
    - Behavior assertion: a host-visible changed brokered tool is removed and re-added in the host index after `notifications/tools/list_changed`.
    - Behavior assertion: a non-host-visible changed brokered tool does not enter the host index.
    - Timing assertion: observed host index `addTools`/`removeTools` effect occurs within 1 second using the existing wait helper pattern.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/tool-search/host-index.integration.test.ts</automated>
  </verify>
  <done>REQ-087 host list_changed behavior and T-I-039/T-I-040 are covered explicitly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config to search index | `host.mcp_servers` determines brokered tools that may enter the host index. |
| broker list_changed to host index | External server list changes mutate host-visible search state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-11 | Elevation of Privilege | host index visibility | mitigate | Apply host consumer visibility at startup and at add/remove update time. |
| T-141-12 | Tampering | host index list_changed updates | mitigate | Use existing synchronous registry diff and `ToolIndexSink` add/remove seam; tests assert timing and filtering. |
| T-141-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm run test:integration -- --run tests/integration/tool-search/host-index.integration.test.ts`.
</verification>

<success_criteria>
`host.tool_search: enabled` builds and maintains a host-visible index containing FQ-native and brokered tools, with explicit T-I-038, T-I-039, and T-I-040 coverage.
</success_criteria>

<output>
Create `.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-06-SUMMARY.md` when done.
</output>
