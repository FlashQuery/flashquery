---
phase: 141-bm25-tool-search-help-pages-and-description-overrides
plan: 7
type: execute
wave: 5
depends_on:
  - 141-03
  - 141-05
  - 141-06
files_modified:
  - tests/integration/tool-search/search-tools.integration.test.ts
  - tests/integration/mcp-broker/tofu-list-changed.test.ts
  - tests/e2e/mcp-broker.e2e.test.ts
  - tests/config/vitest.benchmark.config.ts
  - package.json
autonomous: true
requirements:
  - REQ-080
  - REQ-088
  - REQ-093
  - REQ-096
  - REQ-098
  - REQ-100
  - REQ-101
  - REQ-102
must_haves:
  truths:
    - "POC ranking and placement fixtures pass against production search."
    - "`description_override` is visible downstream and absent from TOFU hash inputs."
    - "Phase C integration and E2E test IDs are represented explicitly."
  artifacts:
    - path: "tests/integration/tool-search/search-tools.integration.test.ts"
      provides: "T-I-026, T-I-033..037, and T-I-041..049 coverage"
    - path: "tests/integration/mcp-broker/tofu-list-changed.test.ts"
      provides: "REQ-101/REQ-102 regression coverage"
    - path: "tests/e2e/mcp-broker.e2e.test.ts"
      provides: "T-E-C1 Phase C gate"
  key_links:
    - from: "tests/fixtures/tool-search/queries.json"
      to: "tests/integration/tool-search/search-tools.integration.test.ts"
      via: "ranking fixture assertions"
      pattern: "queries.json"
---

<objective>
Add integration, benchmark, override/TOFU, and E2E coverage for the completed Phase C search surface.

Purpose: Unit tests prove pieces; this plan proves real broker/native composition under the project test harness.
Output: Focused integration and E2E coverage for the Phase C test matrix not owned by host-index Plan 141-06.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-CONTEXT.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-VALIDATION.md
@.planning/phases/140-tofu-schema-pinning-and-tool-list-change-handling/140-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<interfaces>
Integration fixtures already available from prior phases:
`tests/fixtures/mcp-servers/server-basic.ts` for deterministic broker success and `tests/fixtures/mcp-servers/server-quirky.ts` for list-changed/drift behavior.

Canonical source priority: if generated plan text conflicts with the MCP Broker Requirements or MCP Broker Test Plan, implement the canonical docs and mention the conflict in SUMMARY.md.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add production search integration and benchmark coverage</name>
  <files>tests/integration/tool-search/search-tools.integration.test.ts, tests/config/vitest.benchmark.config.ts, package.json</files>
  <read_first>
    @src/services/tool-search/indexer.ts
    @src/services/tool-search/search-tools-handler.ts
    @src/services/tool-search/tool-search-service.ts
    @src/services/tool-search/tool-meta.ts
    @tests/fixtures/tool-search/queries.json
    @tests/fixtures/tool-search/queries-call-macro.json
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-033: `fq.search_tools` is registered with correct signature.
    - T-I-034: result envelope contains required fields.
    - T-I-035: `has_help` and `help_hint` discriminate native vs brokered results.
    - T-I-035a: disabled purposes keep flat-list behavior.
    - T-I-036: enabled purpose builds index at engine-init time.
    - T-I-037: per-purpose index covers FQ-native and brokered tools the purpose can see.
    - T-I-041: 48 `queries.json` ranking queries pass.
    - T-I-042: 18 `queries-call-macro.json` placement queries pass.
    - T-I-043: 254-tool p95 latency, index size, and build budgets pass.
    - T-I-044: 1000-tool p95 latency budget passes.
    - T-I-045: empty corpus/cold start returns `[]`.
    - T-I-046: search invocation audit includes required fields.
    - T-I-047: `help:true` on every FQ-native tool returns `.tool.md` body.
    - T-I-048: every FQ-native error path includes help footer.
    - T-I-049: malformed `.tool.md` fixtures fail build/startup validation.
  </behavior>
  <action>Create `tests/integration/tool-search/search-tools.integration.test.ts` using existing broker fixture conventions. Drive production `createMcpServer` or direct service APIs where appropriate, but assertions must observe public MCP/search behavior for registered tool/signature/envelope/help cases. Port the POC ranking and call_macro placement fixture checks against production search. Add or use a benchmark command only if the existing Vitest benchmark config is the right fit; otherwise keep deterministic timing assertions inside the integration file using `node:perf_hooks`. Do not add a dependency.</action>
  <acceptance_criteria>
    - Behavior assertion: T-I-033..037 and T-I-041..049 are represented explicitly in this file.
    - Behavior assertion: T-I-041 and T-I-042 load graduated fixture files, not copied inline arrays.
    - CLI assertion: focused integration command passes or skips only according to existing `.env.test` skip rules.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/tool-search/search-tools.integration.test.ts</automated>
  </verify>
  <done>Production search, help, audit, malformed metadata, and performance integration tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect description_override and TOFU separation</name>
  <files>tests/integration/tool-search/search-tools.integration.test.ts, tests/integration/mcp-broker/tofu-list-changed.test.ts</files>
  <read_first>
    @src/services/mcp-broker/registry.ts
    @src/services/mcp-broker/tofu.ts
    @src/services/mcp-broker/index.ts
    @tests/integration/mcp-broker/tofu-list-changed.test.ts
    @tests/integration/tool-search/search-tools.integration.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-026: `description_override` substitutes in flat tool list, search results, and index entry.
    - T-I-027 regression: override does not affect TOFU hash.
    - T-I-032a regression: removing override between starts does not trigger re-approval.
  </behavior>
  <action>Add integration assertions that configure a brokered fixture tool with `description_override: "X"` and prove the delegated flat tool list and `fq.search_tools` result both show `"X"`. Inspect only test helper state necessary to prove the BM25 document used `"X"`. Extend the existing Phase 140 TOFU integration test with focused assertions for REQ-101 and REQ-102 after Phase 141 search wiring, preserving the current upstream hash input in `BrokerClient`/`McpBroker`.</action>
  <acceptance_criteria>
    - Behavior assertion: downstream description equals the override in flat and search surfaces.
    - Behavior assertion: hash equality proves override changes/removal do not cause TOFU re-approval.
    - Source assertion: no production hash call site uses registered downstream `description` when `upstreamDescription` is present.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/tool-search/search-tools.integration.test.ts tests/integration/mcp-broker/tofu-list-changed.test.ts</automated>
  </verify>
  <done>REQ-100..102 are regression-protected after search wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the Phase C E2E broker gate</name>
  <files>tests/e2e/mcp-broker.e2e.test.ts</files>
  <read_first>
    @tests/e2e/mcp-broker.e2e.test.ts
    @tests/fixtures/mcp-servers/server-basic.ts
    @src/mcp/server.ts
    @src/mcp/tools/llm.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-E-C1: `tool_search: enabled` purpose discovers a brokered tool through `fq.search_tools`, returns the overridden description, omits native help fields, and dispatches the discovered tool successfully.
  </behavior>
  <action>Extend `tests/e2e/mcp-broker.e2e.test.ts` with a Phase C case using an HTTP MCP session, a purpose with `toolSearch:"enabled"` and `mcpServers:["basic"]`, and an OpenAI-compatible mock response sequence. The first model response calls `fq.search_tools`; the second invokes the returned brokered registry key; final response proves dispatch completion. Configure one brokered tool override with high lexical overlap to the query and assert the returned `SearchResult.description` is the override.</action>
  <acceptance_criteria>
    - Behavior assertion: initial model-visible tools include `fq.search_tools` and not `basic__echo`.
    - Behavior assertion: `fq.search_tools` result includes the overridden brokered description and no FQ-native `help_hint`.
    - Behavior assertion: direct brokered dispatch after search succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- --run tests/e2e/mcp-broker.e2e.test.ts</automated>
  </verify>
  <done>T-E-C1 passes through the real MCP session path.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration fixture server to broker | External fixture tool descriptions and schema drift enter broker state. |
| performance corpus to process memory | Large synthetic indexes exercise memory/latency budgets. |
| override config to TOFU hash | User-authored override must not affect upstream trust decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-13 | Tampering | `description_override` tests | mitigate | Assertions prove override is downstream-only and hash input remains upstream. |
| T-141-14 | Denial of Service | search performance | mitigate | 254-tool and 1000-tool budget tests catch runaway scoring/index size. |
| T-141-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run focused integration, E2E, and any benchmark command added by this plan.
</verification>

<success_criteria>
Phase C integration tests cover T-I-026, T-I-033..037, T-I-041..049, REQ-101/102 regressions, and T-E-C1. Host index T-I-038..040 remains owned by Plan 141-06.
</success_criteria>

<output>
Create `.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-07-SUMMARY.md` when done.
</output>
