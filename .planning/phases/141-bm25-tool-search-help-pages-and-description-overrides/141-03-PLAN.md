---
phase: 141-bm25-tool-search-help-pages-and-description-overrides
plan: 3
type: execute
wave: 2
depends_on:
  - 141-01
  - 141-02
files_modified:
  - src/services/tool-search/tool-search-service.ts
  - src/services/tool-search/search-tools-handler.ts
  - src/services/mcp-broker/types.ts
  - src/services/mcp-broker/trace.ts
  - src/services/mcp-broker/index.ts
  - src/mcp/tools/llm.ts
  - src/llm/tool-registry.ts
  - src/llm/agent-loop.ts
  - tests/unit/tool-search/search-tools-handler.test.ts
  - tests/unit/llm-agent-loop.test.ts
autonomous: true
requirements:
  - REQ-011
  - REQ-069
  - REQ-082
  - REQ-083
  - REQ-084
  - REQ-085
  - REQ-086
  - REQ-087
  - REQ-100
must_haves:
  truths:
    - "`fq.search_tools` returns ranked SearchResult envelopes."
    - "Search-enabled purposes inject only `fq.search_tools` up front."
    - "Disabled purposes keep the existing flat native + brokered tool list."
    - "Search audit records exclude raw tool args and result payloads."
  artifacts:
    - path: "src/services/tool-search/tool-search-service.ts"
      provides: "Per-consumer BM25 index manager and ToolIndexSink implementation"
    - path: "src/services/tool-search/search-tools-handler.ts"
      provides: "`fq.search_tools` handler"
      exports: ["createSearchToolsHandler"]
    - path: "src/llm/agent-loop.ts"
      provides: "tool_search enabled/disabled provider-tool shaping"
  key_links:
    - from: "src/services/mcp-broker/index.ts"
      to: "src/services/tool-search/tool-search-service.ts"
      via: "ToolIndexSink add/remove"
      pattern: "indexSink"
    - from: "src/llm/agent-loop.ts"
      to: "src/services/tool-search/search-tools-handler.ts"
      via: "per-invocation search context"
      pattern: "search_tools"
---

<objective>
Wire BM25 into the tool surface with `fq.search_tools`, per-purpose search indexes, list-changed index updates, audit events, and enabled/disabled delegated injection behavior.

Purpose: This turns the tested indexer and help metadata into the actual model-facing search experience.
Output: Search service, search handler, audit trace extension, and agent-loop tool-list shaping.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-CONTEXT.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-RESEARCH.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-PATTERNS.md
@.planning/phases/140-tofu-schema-pinning-and-tool-list-change-handling/140-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<interfaces>
From Plan 141-01:
`PureBM25Indexer.build`, `addTools`, `removeTools`, `search`, `getStats`.

From Plan 141-02:
`TOOL_META`/`getToolMeta` supplies native descriptions, help hints, and help bodies.

From `src/services/mcp-broker/types.ts`:
export interface ToolIndexSink { addTools(tools: BrokeredTool[]): void; removeTools(keys: RegistryKey[]): void; }
export type ConsumerContext = { kind: 'host'; traceId: string; interactive?: boolean } | { kind: 'purpose'; purposeId: string; traceId: string; interactive?: boolean };

Canonical source priority: if generated plan text conflicts with the MCP Broker Requirements or MCP Broker Test Plan, implement the canonical docs and mention the conflict in SUMMARY.md.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement ToolSearchService and fq.search_tools handler</name>
  <files>src/services/tool-search/tool-search-service.ts, src/services/tool-search/search-tools-handler.ts, src/services/mcp-broker/types.ts, src/services/mcp-broker/trace.ts, src/services/mcp-broker/index.ts, src/mcp/tools/llm.ts, tests/unit/tool-search/search-tools-handler.test.ts</files>
  <read_first>
    @src/services/tool-search/indexer.ts
    @src/services/tool-search/tool-meta.ts
    @src/services/mcp-broker/types.ts
    @src/services/mcp-broker/index.ts
    @src/services/mcp-broker/registry.ts
    @src/services/mcp-broker/trace.ts
    @src/mcp/tools/llm.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-033 lower-level: `fq.search_tools` has signature `query: string, limit?: number = 8`.
    - T-I-034 lower-level: `SearchResult` includes `server`, `tool`, `registry_key`, `description`, `arg_summary`, `score`, `normalizedScore`.
    - T-I-035 lower-level: native results use `server:"flashquery"` and include `has_help:true` plus `help_hint`; brokered results omit help fields or set `has_help:false`.
    - T-I-045 lower-level: empty corpus and cold-start search return `[]`, not an error.
    - T-I-046 lower-level: audit records `{consumer, query, result_count, latency_us, trace_id}` and excludes raw tool args/results.
  </behavior>
  <action>Create `ToolSearchService` that can build per-purpose indexes from eligible native catalog entries and `Broker.listToolsForConsumer(ctx)`, maintain a host-capable index, and expose a `ToolIndexSink` for broker list-changed add/remove. Create `fq.search_tools` handler with input `query: string` and `limit?: number = 8`, returning JSON text with an array of `SearchResult` envelopes. Map native tool docs to `server:"flashquery"` and `registry_key` equal to the bare native tool name; map brokered docs to `serverId`, `toolName`, and registry key. Extend broker trace types with a sanitized search audit event per REQ-069.</action>
  <acceptance_criteria>
    - Source assertion: search docs use registered downstream `description`, not `upstreamDescription`.
    - Source assertion: audit event type contains query/result count/latency/trace but no raw arguments or result payload.
    - Behavior assertion: native search result includes `has_help:true` and canonical help hint.
    - Behavior assertion: brokered search result with override returns overridden `description` and no native help hint.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/tool-search/search-tools-handler.test.ts</automated>
  </verify>
  <done>REQ-082..084, REQ-100, and REQ-069 behavior exists behind focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shape delegated tool injection for tool_search purposes</name>
  <files>src/llm/tool-registry.ts, src/llm/agent-loop.ts, src/mcp/tools/llm.ts, tests/unit/llm-agent-loop.test.ts, tests/unit/tool-search/search-tools-handler.test.ts</files>
  <read_first>
    @src/llm/tool-registry.ts
    @src/llm/agent-loop.ts
    @src/mcp/tools/llm.ts
    @tests/unit/llm-agent-loop.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-035a: `tool_search: disabled` purposes keep flat-list behavior and do not get search-only injection.
    - T-I-036: `tool_search: enabled` builds a per-invocation index at `call_model` engine-init time.
    - T-I-037: enabled-purpose index includes FQ-native eligible tools and purpose-visible brokered tools.
    - REQ-085: enabled purposes initially expose only `fq.search_tools` plus any documented always-present tools.
    - REQ-086: disabled purposes preserve existing flat native + brokered provider definitions.
  </behavior>
  <action>Read purpose config `toolSearch`/`tool_search` as already parsed by config. For enabled purposes with at least one eligible tool, keep `nativeToolNames` broad enough for direct dispatch after search discovery, build the per-invocation `ToolSearchService` index before the first model call, and send only the OpenAI tool definition for `fq.search_tools` in `providerTools` up front. For disabled purposes, leave current `getProviderTools(options) + brokeredTools.map(toOpenAiBrokeredToolDefinition)` behavior intact. Register `search_tools` as an FQ-native MCP tool through the existing `registerLlmTools` or adjacent LLM tool registration path.</action>
  <acceptance_criteria>
    - Behavior assertion: enabled-purpose provider request contains `fq.search_tools` and does not contain ordinary eligible tools such as `get_document` or `basic__echo`.
    - Behavior assertion: disabled-purpose provider request still contains ordinary eligible native and brokered tools.
    - Behavior assertion: a direct tool call to a search-discovered native/brokered tool still dispatches because the immutable native/broker visibility gates remain populated.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/llm-agent-loop.test.ts tests/unit/tool-search/search-tools-handler.test.ts</automated>
  </verify>
  <done>REQ-011, REQ-085, REQ-086, and host-capable REQ-087 service hooks are implemented without regressing disabled behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer context to search index | Consumer visibility determines which tools can be indexed and returned. |
| model query to audit trace | Search queries enter trace storage. |
| broker registry to downstream search | Overridden descriptions become model-visible index text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-06 | Elevation of Privilege | `ToolSearchService` | mitigate | Build indexes only from native eligibility and `Broker.listToolsForConsumer(ctx)`. |
| T-141-07 | Information Disclosure | search audit | mitigate | Audit only consumer, query, result count, latency, and trace ID; no args/results/help bodies. |
| T-141-08 | Tampering | `description_override` search path | mitigate | Use registry downstream `description` for search while Plan 04 protects TOFU upstream hash behavior. |
| T-141-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/tool-search/search-tools-handler.test.ts tests/unit/llm-agent-loop.test.ts`.
</verification>

<success_criteria>
`fq.search_tools` works for native and brokered tools, enabled purposes are search-only up front, disabled purposes remain flat, and search audit is sanitized.
</success_criteria>

<output>
Create `.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-03-SUMMARY.md` when done.
</output>
