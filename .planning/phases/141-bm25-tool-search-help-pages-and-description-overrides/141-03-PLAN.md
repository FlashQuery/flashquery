---
phase: 141-bm25-tool-search-help-pages-and-description-overrides
plan: 3
type: execute
wave: 2
depends_on:
  - 141-02
files_modified:
  - src/llm/tool-dispatcher.ts
  - tests/unit/llm-tool-dispatcher.test.ts
autonomous: true
requirements:
  - REQ-093
  - REQ-096
  - REQ-098
must_haves:
  truths:
    - "`help: true` on FQ-native tools returns local help before normal schema validation."
    - "FQ-native tool errors include the canonical help-pointer footer."
    - "Brokered `help: true` and brokered errors remain pass-through."
  artifacts:
    - path: "src/llm/tool-dispatcher.ts"
      provides: "Native help sentinel and FQ-native error footer"
    - path: "tests/unit/llm-tool-dispatcher.test.ts"
      provides: "Lower-level T-I-028, T-I-029, T-I-047, and T-I-048 regressions"
  key_links:
    - from: "src/llm/tool-dispatcher.ts"
      to: "src/services/tool-search/tool-meta.ts"
      via: "help true lookup"
      pattern: "help.*true|getToolMeta"
---

<objective>
Implement dispatcher-only help semantics after `TOOL_META` exists.

Purpose: This separates native/brokered dispatch behavior from metadata plumbing and help-page authoring.
Output: `help:true` sentinel handling, native-only error footer, and focused dispatcher tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-CONTEXT.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<interfaces>
From Plan 141-02:
`getToolMeta(name)` returns FQ-native description, help hint, args summary, and raw help body.

From `src/llm/tool-dispatcher.ts`:
export async function dispatchNativeToolCall(options: DispatchNativeToolCallOptions): Promise&lt;NativeToolDispatchResult&gt;;
export async function dispatchToolCalls(options: DispatchToolCallsOptions): Promise&lt;DispatchToolCallsResult&gt;;

Canonical source priority: if generated plan text conflicts with the MCP Broker Requirements or MCP Broker Test Plan, implement the canonical docs and mention the conflict in SUMMARY.md.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add native help:true pre-validation sentinel</name>
  <files>src/llm/tool-dispatcher.ts, tests/unit/llm-tool-dispatcher.test.ts</files>
  <read_first>
    @src/llm/tool-dispatcher.ts
    @src/services/tool-search/tool-meta.ts
    @tests/unit/llm-tool-dispatcher.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-047 lower-level: native `help:true` bypasses Zod validation and returns `.tool.md` body.
    - REQ-093: `help:true` applies to FQ-native tools only after normal native visibility/catalog lookup.
  </behavior>
  <action>Update `dispatchNativeToolCall` so `args.help === true` is detected after native visibility/catalog lookup and before `toZodObjectSchema(...).parse(args)`. Return the raw help-page body from `TOOL_META` as a successful tool result and ignore other args. Do not add `help` to every native Zod schema.</action>
  <acceptance_criteria>
    - Behavior assertion: `{help:true}` with missing required args succeeds with help body text.
    - Behavior assertion: unknown or not-visible native tool names still fail before help lookup.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/llm-tool-dispatcher.test.ts</automated>
  </verify>
  <done>REQ-093 native sentinel behavior is green in dispatcher tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add native-only help footer and brokered pass-through regressions</name>
  <files>src/llm/tool-dispatcher.ts, tests/unit/llm-tool-dispatcher.test.ts</files>
  <read_first>
    @src/llm/tool-dispatcher.ts
    @src/services/mcp-broker/types.ts
    @tests/unit/llm-tool-dispatcher.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-I-028 lower-level: brokered `isError` output is not appended with FlashQuery help footer.
    - T-I-029 lower-level: brokered arguments containing `help:true` are passed to `Broker.callTool` unchanged.
    - T-I-048 lower-level: FQ-native validation, handler, abort, and thrown-error paths include the canonical help footer exactly once.
  </behavior>
  <action>Append the canonical footer only on FQ-native failures: `For full documentation, examples, and parameter details, call \`{toolName}\` with \`help: true\`.` Cover validation errors, aborted native dispatch errors, native handler `isError:true`, and thrown handler failures. Keep brokered dispatch unchanged except for tests proving `help:true` and brokered errors are pass-through.</action>
  <acceptance_criteria>
    - Behavior assertion: native failures include the footer exactly once.
    - Behavior assertion: brokered `isError:true` response has no FlashQuery help footer.
    - Behavior assertion: brokered `Broker.callTool` receives raw args containing `help:true`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/llm-tool-dispatcher.test.ts</automated>
  </verify>
  <done>REQ-096 and REQ-098 are implemented without changing brokered pass-through semantics.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model args to native dispatcher | Untrusted model args may request `help:true` before normal schema validation. |
| brokered upstream errors to model | External server errors cross through FlashQuery without native decoration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-05 | Elevation of Privilege | help sentinel | mitigate | Sentinel branch applies only after native tool visibility/catalog lookup. |
| T-141-06 | Spoofing | brokered errors | mitigate | Brokered errors and brokered `help:true` are not wrapped with FQ-native help text. |
| T-141-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/llm-tool-dispatcher.test.ts`.
</verification>

<success_criteria>
Native help works before schema validation, native errors point to help, and brokered help/error semantics remain pass-through.
</success_criteria>

<output>
Create `.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-03-SUMMARY.md` when done.
</output>
