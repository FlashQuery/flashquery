---
phase: 141-bm25-tool-search-help-pages-and-description-overrides
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/tool-search/tool-meta.ts
  - src/mcp/tool-catalog.ts
  - src/mcp/server.ts
  - tests/unit/tool-search/tool-meta.test.ts
autonomous: true
requirements:
  - REQ-089
  - REQ-090
  - REQ-091
  - REQ-092
  - REQ-094
  - REQ-095
  - REQ-099
must_haves:
  truths:
    - "FlashQuery loads validated FQ-native `.tool.md` metadata before exposing native tools."
    - "Invalid help metadata fails startup/build validation with clear errors."
    - "Native MCP registration descriptions come from `TOOL_META.description`."
  artifacts:
    - path: "src/services/tool-search/tool-meta.ts"
      provides: "TOOL_META loader, validator, and help-page lookup"
      exports: ["DEFAULT_HELP_HINT", "loadToolMeta", "getToolMeta"]
    - path: "tests/unit/tool-search/tool-meta.test.ts"
      provides: "T-U-028..033 and T-U-044 loader/startup coverage"
    - path: "src/mcp/tool-catalog.ts"
      provides: "Native registration description substitution from TOOL_META"
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/services/tool-search/tool-meta.ts"
      via: "startup validation before native exposure"
      pattern: "loadToolMeta|validateToolMeta"
---

<objective>
Create the validated `TOOL_META` loader/startup path for FQ-native help pages without authoring the page batch in this plan.

Purpose: This isolates metadata plumbing from help-page content and dispatcher behavior so the executor can finish it inside one context window.
Output: Loader, startup validation, registration-description substitution, and focused unit tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-CONTEXT.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-RESEARCH.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

<interfaces>
From `src/mcp/tool-catalog.ts`:
export function wrapServerWithToolCatalog(server: McpServer, options?: ToolCatalogOptions): McpServer;
export function getNativeToolCatalog(server: McpServer): NativeToolDefinition[];

From `src/mcp/tool-metadata.ts`:
export function getToolMetadata(name: string): ToolMetadata | undefined;
export function assertRegisteredToolsHaveMetadata(catalog: Array&lt;{ name: string }&gt;): void;

Canonical source priority: if generated plan text conflicts with the MCP Broker Requirements or MCP Broker Test Plan, implement the canonical docs and mention the conflict in SUMMARY.md.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build validated TOOL_META loader</name>
  <files>src/services/tool-search/tool-meta.ts, tests/unit/tool-search/tool-meta.test.ts</files>
  <read_first>
    @src/mcp/tool-metadata.ts
    @src/config/loader.ts
    @tests/unit/tool-metadata.test.ts
    @tests/unit/config.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-U-028: missing `name` fails validation.
    - T-U-029: name/file basename mismatch fails validation.
    - T-U-030: missing description suffix mentioning `help` and `true` fails validation.
    - T-U-031: duplicate frontmatter `name` fails validation.
    - T-U-032: description shorter than 40 chars warns but does not fail.
    - T-U-033: canonical default `help_hint` matches the verbatim string from the canonical requirements.
  </behavior>
  <action>Create `src/services/tool-search/tool-meta.ts` using existing `gray-matter` and Zod. Load only static source-tree files matching `src/mcp/tools/*.tool.md`; do not accept caller-provided paths for production loading. Validate required frontmatter `name`, `description`, `help_hint`, `tier`, and `args`, plus filename/name match, duplicate names, YAML/frontmatter parse failures, and required description suffix. Return diagnostics with warnings for short descriptions. Export a lookup that returns description, help hint, tier, args summary, and raw help body.</action>
  <acceptance_criteria>
    - Source assertion: no new package is added.
    - Source assertion: loader has a production path fixed to `src/mcp/tools/*.tool.md`.
    - Behavior assertion: invalid metadata fixtures fail with clear messages for T-U-028..031.
    - Behavior assertion: short descriptions warn but do not fail.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/tool-search/tool-meta.test.ts</automated>
  </verify>
  <done>REQ-090, REQ-091, and REQ-094 are covered by green unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire TOOL_META into startup and registration descriptions</name>
  <files>src/mcp/tool-catalog.ts, src/mcp/server.ts, tests/unit/tool-search/tool-meta.test.ts</files>
  <read_first>
    @src/mcp/tool-catalog.ts
    @src/mcp/server.ts
    @src/mcp/tool-metadata.ts
    @tests/unit/native-tool-catalog.test.ts
    @tests/unit/tool-search/tool-meta.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-U-044: `TOOL_META` is built at startup and feeds native registration descriptions.
    - REQ-092: startup has a built metadata registry before MCP tool exposure.
    - REQ-095: startup/build validation fails on malformed or incomplete `.tool.md` metadata.
    - REQ-099: metadata loads once at startup; no hot reload watcher is added.
  </behavior>
  <action>Call the metadata loader during `createMcpServer` before final native schema/catalog validation. Update `wrapServerWithToolCatalog` so native tool registration descriptions prefer `TOOL_META.description`, while preserving existing host exposure filtering and catalog capture. Assert every registered FQ-native tool has metadata, but let Plan 141-04 provide the actual page files. Do not add file watchers or hot reload behavior.</action>
  <acceptance_criteria>
    - Behavior assertion: missing page for a registered native tool fails the startup validation test.
    - Behavior assertion: native catalog description reflects `.tool.md` frontmatter when present.
    - Source assertion: no `watch`, `fs.watch`, or hot-reload loop is introduced.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/tool-search/tool-meta.test.ts tests/unit/native-tool-catalog.test.ts</automated>
  </verify>
  <done>REQ-089, REQ-092, REQ-095, and REQ-099 are wired without mixing dispatcher or page-authoring work into this plan.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.tool.md` filesystem to startup registry | Local source metadata becomes model-visible descriptions and help content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-03 | Tampering | `.tool.md` loader | mitigate | Static source-tree glob, Zod validation, duplicate/name mismatch fail-fast. |
| T-141-04 | Denial of Service | startup validation | accept | Validation runs once at startup/build scope; no hot reload watcher is planned. |
| T-141-SC | Tampering | npm installs | accept | Uses existing `gray-matter`; no package-manager install is planned. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/tool-search/tool-meta.test.ts tests/unit/native-tool-catalog.test.ts`.
</verification>

<success_criteria>
The metadata loader and startup registration path are complete, tested, and ready for help-page files from Plan 141-04.
</success_criteria>

<output>
Create `.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-02-SUMMARY.md` when done.
</output>
