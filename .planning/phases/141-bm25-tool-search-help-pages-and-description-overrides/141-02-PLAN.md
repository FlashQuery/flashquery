---
phase: 141-bm25-tool-search-help-pages-and-description-overrides
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/tool-search/tool-meta.ts
  - tests/unit/tool-search/tool-meta.test.ts
autonomous: true
requirements:
  - REQ-090
  - REQ-091
  - REQ-094
must_haves:
  truths:
    - "FlashQuery can parse and validate `.tool.md` metadata in isolation."
    - "Invalid help metadata reports clear validation errors."
    - "The canonical default help hint is available to later page and search work."
  artifacts:
    - path: "src/services/tool-search/tool-meta.ts"
      provides: "TOOL_META loader, validator, diagnostics, and lookup primitives"
      exports: ["DEFAULT_HELP_HINT", "loadToolMeta", "validateToolMeta"]
    - path: "tests/unit/tool-search/tool-meta.test.ts"
      provides: "T-U-028..033 loader validation coverage"
---

<objective>
Create the `.tool.md` loader and validator only.

Purpose: This plan intentionally does not wire startup enforcement, because the page corpus is created in later plans. Startup/catalog enforcement is deferred to Plan 141-11 after all `.tool.md` pages exist.
</objective>

<context>
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-CONTEXT.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-RESEARCH.md
@.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md

Canonical source priority: if generated plan text conflicts with the MCP Broker Requirements or MCP Broker Test Plan, implement the canonical docs and mention the conflict in SUMMARY.md.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build validated TOOL_META loader primitives</name>
  <files>src/services/tool-search/tool-meta.ts, tests/unit/tool-search/tool-meta.test.ts</files>
  <read_first>
    @src/mcp/tool-metadata.ts
    @src/config/loader.ts
    @tests/unit/tool-metadata.test.ts
    @tests/unit/config.test.ts
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Broker/MCP Broker Test Plan.md
  </read_first>
  <behavior>
    - T-U-028: missing `name` fails validation.
    - T-U-029: name/file basename mismatch fails validation.
    - T-U-030: missing description suffix mentioning `help` and `true` fails validation.
    - T-U-031: duplicate frontmatter `name` fails validation.
    - T-U-032: description shorter than 40 chars warns but does not fail.
    - T-U-033: canonical default `help_hint` matches the verbatim string from the canonical requirements.
  </behavior>
  <action>Create `src/services/tool-search/tool-meta.ts` using existing `gray-matter` and Zod. Expose a production loader for static source-tree files matching `src/mcp/tools/*.tool.md` and a testable validation entry point that accepts fixture records. Validate required frontmatter `name`, `description`, `help_hint`, `tier`, and `args`, plus filename/name match, duplicate names, YAML/frontmatter parse failures, and required description suffix. Return diagnostics with warnings for short descriptions. Do not call this from `createMcpServer` yet and do not assert every registered tool has a page in this plan.</action>
  <acceptance_criteria>
    - Source assertion: no new package is added.
    - Source assertion: production loader path is fixed to `src/mcp/tools/*.tool.md`.
    - Behavior assertion: invalid metadata fixtures fail with clear messages for T-U-028..031.
    - Behavior assertion: short descriptions warn but do not fail.
    - Source assertion: `src/mcp/server.ts` and `src/mcp/tool-catalog.ts` are not modified by this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/tool-search/tool-meta.test.ts</automated>
  </verify>
  <done>REQ-090, REQ-091, and REQ-094 are covered by loader-only tests without premature startup enforcement.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source markdown to runtime metadata | Local `.tool.md` files become tool descriptions and help text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-03 | Tampering | metadata loader | mitigate | Read only static `src/mcp/tools/*.tool.md` production paths and validate frontmatter strictly. |
| T-141-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/tool-search/tool-meta.test.ts`.
</verification>

<success_criteria>
The metadata loader validates `.tool.md` shape and canonical help-hint behavior, with startup enforcement deferred until after page batches complete.
</success_criteria>

<output>
Create `.planning/phases/141-bm25-tool-search-help-pages-and-description-overrides/141-02-SUMMARY.md` when done.
</output>
