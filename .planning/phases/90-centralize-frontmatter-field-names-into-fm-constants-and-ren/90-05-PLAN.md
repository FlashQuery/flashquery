---
phase: 90-centralize-frontmatter-field-names-into-fm-constants-and-ren
plan: 05
type: execute
wave: 3
depends_on:
  - "90-02-PLAN.md"
  - "90-03-PLAN.md"
  - "90-04-PLAN.md"
files_modified:
  - tests/unit/frontmatter-sanitizer.test.ts
  - tests/unit/resolve-document.test.ts
  - tests/unit/compound-tools.test.ts
  - tests/integration/compound-tools.integration.test.ts
  - tests/integration/frontmatter-ordering.integration.test.ts
  - tests/scenarios/framework/fqc_vault.py
  - tests/scenarios/directed/testcases/test_reconciliation_frontmatter_discovery.py
  - tests/scenarios/directed/testcases/test_reconciliation_modification.py
  - tests/scenarios/directed/testcases/test_reconciliation_disassociation.py
  - tests/scenarios/directed/testcases/test_reconciliation_resurrection.py
  - tests/scenarios/directed/testcases/test_reconciliation_movement.py
  - tests/scenarios/directed/testcases/test_reconciliation_resurrection_with_on_moved.py
  - tests/scenarios/directed/testcases/test_reconciliation_untrack_policy.py
  - tests/scenarios/directed/testcases/test_reconciliation_keep_tracking_stability.py
  - tests/scenarios/directed/testcases/test_frontmatter_preservation.py
  - tests/scenarios/directed/testcases/test_document_update_partial.py
  - tests/scenarios/directed/testcases/test_plugin_lifecycle.py
  - tests/scenarios/directed/testcases/test_plugin_mixed_tables.py
  - tests/scenarios/integration/run_integration.py
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - ORD-01
  - ORD-02
  - ORD-03
  - ORD-04

must_haves:
  truths:
    - "frontmatter-sanitizer unit test asserts user fields appear before FQ fields (inverted assertion)"
    - "frontmatter-sanitizer unit test uses new fq_* field name strings"
    - "resolve-document unit test asserts resolvedVia equals FM.ID (fq_id not fqc_id)"
    - "compound-tools unit test asserts response contains FM.TITLE and FM.ID strings"
    - "compound-tools integration test asserts response contains FM.CREATED and FM.UPDATED strings"
    - "Integration ordering tests turn GREEN (ORD-01 through ORD-04)"
    - "Python fqc_vault.py _ORDERED_FIELDS uses new fq_* field names in correct order"
    - "Python VaultDocument properties (fqc_id, title, tags, status) read from new fq_* frontmatter keys"
    - "Python VaultDocument create_file method writes new fq_* frontmatter keys"
    - "All 14 Python test files use new fq_* field name strings"
    - "Full test suite green: npm test && npm run test:integration"
  artifacts:
    - path: "tests/unit/frontmatter-sanitizer.test.ts"
      provides: "Updated ordering assertion (user first) and new field name strings"
    - path: "tests/unit/resolve-document.test.ts"
      provides: "resolvedVia assertion updated to fq_id"
    - path: "tests/unit/compound-tools.test.ts"
      provides: "FM.TITLE and FM.ID assertions"
    - path: "tests/integration/compound-tools.integration.test.ts"
      provides: "FM.CREATED and FM.UPDATED assertions"
    - path: "tests/scenarios/framework/fqc_vault.py"
      provides: "Central Python helper with all fq_* field names and updated VaultDocument properties"
  key_links:
    - from: "tests/unit/frontmatter-sanitizer.test.ts"
      to: "src/mcp/utils/frontmatter-sanitizer.ts"
      via: "asserts user-first key order"
      pattern: "custom.*fq_title|user.*before.*fq"
    - from: "tests/scenarios/framework/fqc_vault.py"
      to: "FQC server frontmatter output"
      via: "_ORDERED_FIELDS tuple and VaultDocument.fq_id property"
      pattern: "_ORDERED_FIELDS.*fq_title|fq_id.*self\\.frontmatter"
---

<objective>
Update all test files to match the new fq_* field names and the new user-first ordering behavior. This is the final wave — all Wave 2 source changes are complete, so tests can now be updated to assert correct new behavior rather than old behavior. This plan turns the phase fully GREEN.

Purpose: After Wave 2, 4 TypeScript test files fail (asserting old field names) and 14 Python test files fail (using old field name strings). This plan fixes all of them and verifies the full suite passes.
Output: 19 updated test files, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-CONTEXT.md
@.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-RESEARCH.md
@.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-01-SUMMARY.md
@.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-02-SUMMARY.md
@.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-03-SUMMARY.md
@.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-04-SUMMARY.md

<interfaces>
<!-- FM values for asserting in tests -->
FM.TITLE    = 'fq_title'
FM.STATUS   = 'fq_status'
FM.TAGS     = 'fq_tags'
FM.CREATED  = 'fq_created'
FM.UPDATED  = 'fq_updated'
FM.OWNER    = 'fq_owner'
FM.TYPE     = 'fq_type'
FM.INSTANCE = 'fq_instance'
FM.ID       = 'fq_id'

<!-- frontmatter-sanitizer.test.ts: the "maintains key order" test (line 47) must be rewritten -->
Current assertion (WRONG after Plan 02):
  const expectedStart = ['fqc_id', 'status', 'title', 'tags', 'created', 'updated'];
  const resultStart = keys.slice(0, expectedStart.length);
  expect(resultStart).toEqual(expectedStart);

New assertion (user fields FIRST, then FQ fields):
  // Input has one user field 'custom' and FQ fields
  // After the fix, 'custom' must appear before any fq_* fields
  expect(keys[0]).toBe('custom');  // user field is first
  const fqFields = keys.filter(k => k.startsWith('fq_'));
  expect(fqFields).toEqual(['fq_title', 'fq_status', 'fq_tags', 'fq_created', 'fq_updated']);
  // The custom field appears before all fq_ fields
  expect(keys.indexOf('custom')).toBeLessThan(keys.indexOf('fq_title'));

Also update the INPUT object in that test to use new field names:
  const input = {
    fq_tags: ['tag1'],
    custom: 'value',
    fq_id: 'test-uuid',
    fq_title: 'Test Doc',
    fq_status: 'active',
    fq_created: '2026-04-12T00:00:00Z',
    fq_updated: '2026-04-12T01:00:00Z',
  };

Also update all other tests in frontmatter-sanitizer.test.ts that use old field name strings:
  - 'fqc_id' → 'fq_id'
  - 'title' → 'fq_title'
  - 'status' → 'fq_status'
  (in the test inputs and result assertions)

<!-- resolve-document.test.ts: line 180 -->
Current: expect(result.resolvedVia).toBe('fqc_id');
New:     expect(result.resolvedVia).toBe(FM.ID);
(Add import { FM } from '../../src/constants/frontmatter-fields.js')

<!-- compound-tools.test.ts: lines 1563, 1565 -->
Current:
  expect(text).toContain('title');   // line 1563
  expect(text).toContain('fqc_id'); // line 1565
New:
  expect(text).toContain(FM.TITLE);  // 'fq_title'
  expect(text).toContain(FM.ID);     // 'fq_id'
(Add import { FM } from '../../src/constants/frontmatter-fields.js')

<!-- compound-tools.integration.test.ts: lines 869, 870 -->
Current:
  expect(text).toContain('created');  // line 869
  expect(text).toContain('updated');  // line 870
New:
  expect(text).toContain(FM.CREATED); // 'fq_created'
  expect(text).toContain(FM.UPDATED); // 'fq_updated'
(Add import { FM } from '../../src/constants/frontmatter-fields.js')

<!-- Python fqc_vault.py key changes -->

_ORDERED_FIELDS (line 103) — full replacement:
BEFORE: _ORDERED_FIELDS = ("fqc_id", "status", "title", "tags", "created", "updated")
AFTER:  _ORDERED_FIELDS = (
    "fq_title", "fq_status", "fq_tags", "fq_created", "fq_updated",
    "fq_owner", "fq_type", "fq_instance", "fq_id",
)

VaultDocument properties (lines 124-137) — rename all 4:
BEFORE:
  @property
  def fqc_id(self) -> str | None:
      return self.frontmatter.get("fqc_id")

  @property
  def title(self) -> str | None:
      return self.frontmatter.get("title")

  @property
  def tags(self) -> list[str]:
      return self.frontmatter.get("tags", []) or []

  @property
  def status(self) -> str | None:
      return self.frontmatter.get("status")

AFTER:
  @property
  def fq_id(self) -> str | None:
      return self.frontmatter.get("fq_id")

  @property
  def fq_title(self) -> str | None:
      return self.frontmatter.get("fq_title")

  @property
  def fq_tags(self) -> list[str]:
      return self.frontmatter.get("fq_tags", []) or []

  @property
  def fq_status(self) -> str | None:
      return self.frontmatter.get("fq_status")

create_file method fm dict (lines ~384-391):
BEFORE:
  fm: dict[str, Any] = {
      "fqc_id": fqc_id or str(uuid4()),
      "status": status,
      "title": title,
      "tags": normalized_tags,
      "created": now,
      "updated": now,
  }
AFTER:
  fm: dict[str, Any] = {
      "fq_title": title,
      "fq_status": status,
      "fq_tags": normalized_tags,
      "fq_created": now,
      "fq_updated": now,
      "fq_id": fqc_id or str(uuid4()),
  }
Note: Order matches _ORDERED_FIELDS (user fields will come before these in the serialization output).

create_file method signature: parameter `fqc_id` may stay as `fqc_id` for backward compatibility or be renamed to `fq_id`. Check whether any test passes `fqc_id=` as a keyword argument — if so, keep the parameter name but update the dict key.

write_frontmatter method touch_updated block (line ~426):
BEFORE: fm["updated"] = _now_iso()
AFTER:  fm["fq_updated"] = _now_iso()

_serialize_frontmatter function — the tags flow-style check:
BEFORE: if key == "tags" and isinstance(val, list):
AFTER:  if key == "fq_tags" and isinstance(val, list):

<!-- Python VaultDocument property usage in test files -->
The following test files use doc.fqc_id, doc.title, doc.tags, doc.status as Python attributes.
After renaming the properties, these call sites must also be updated:
- tests/scenarios/framework/fqc_vault.py internal uses (find_files, inspect_file)
- tests/scenarios/directed/testcases/test_search_after_create.py
- tests/scenarios/directed/testcases/test_document_update_partial.py
- tests/scenarios/directed/testcases/test_document_copy_and_move.py
- tests/scenarios/directed/testcases/test_document_archive_and_search.py
- tests/scenarios/directed/testcases/test_create_read_update.py
- tests/scenarios/directed/testcases/test_content_frontmatter_ops.py
- tests/scenarios/directed/testcases/test_tag_operations.py
These files use doc.fqc_id, doc.title, doc.status, doc.tags — update to doc.fq_id, doc.fq_title, doc.fq_status, doc.fq_tags.

Alternative approach: keep property aliases for backward compat. Add aliased properties:
  fqc_id = property(lambda self: self.fq_id)  # backward-compat alias
This avoids touching 7+ additional test files that use doc.fqc_id etc. Consider this if the test files are not in the scope of the 14 Python files listed in CONTEXT.md.

DECISION: The CONTEXT.md lists 14 Python files. The files test_search_after_create.py, test_document_update_partial.py, test_document_copy_and_move.py, test_document_archive_and_search.py, test_create_read_update.py, test_content_frontmatter_ops.py, test_tag_operations.py are NOT in the list of 14 Python files. Therefore, add backward-compatible property aliases to VaultDocument instead of renaming the properties in those test files:
  fqc_id = property(lambda self: self.fq_id)
  title   = property(lambda self: self.fq_title)
  tags    = property(lambda self: self.fq_tags)
  status  = property(lambda self: self.fq_status)
(Or use @property with the old name delegating to the new property.)
This keeps the 14-file scope intact and avoids breaking test files outside scope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update 4 TypeScript test files and complete ordering integration tests</name>
  <files>
    tests/unit/frontmatter-sanitizer.test.ts,
    tests/unit/resolve-document.test.ts,
    tests/unit/compound-tools.test.ts,
    tests/integration/compound-tools.integration.test.ts,
    tests/integration/frontmatter-ordering.integration.test.ts
  </files>
  <read_first>
    - tests/unit/frontmatter-sanitizer.test.ts (full file — all test inputs and assertions that use old field names)
    - tests/unit/resolve-document.test.ts lines 175–185 (the fqc_id assertion at line 180)
    - tests/unit/compound-tools.test.ts lines 1558–1570 (the title and fqc_id assertions)
    - tests/integration/compound-tools.integration.test.ts lines 862–872 (the created/updated assertions)
    - tests/integration/frontmatter-ordering.integration.test.ts (the stub file from Plan 01 — make the ordering assertions concrete and correct now that source is updated)
  </read_first>
  <action>
    **frontmatter-sanitizer.test.ts:**
    - Add import: `import { FM } from '../../src/constants/frontmatter-fields.js';`
    - Rewrite the "maintains key order" test:
      - Update INPUT object to use new fq_* keys (fq_id, fq_title, fq_status, fq_tags, fq_created, fq_updated, and keep 'custom' as user field)
      - Remove old `expectedStart` and `resultStart` assertion
      - Add new assertion: user field 'custom' appears at `keys[0]` (first)
      - Add new assertion: `keys.indexOf('custom') < keys.indexOf(FM.TITLE)` (user fields before FQ fields)
      - Add new assertion that FQ fields appear in order: ['fq_title', 'fq_status', 'fq_tags', 'fq_created', 'fq_updated', 'fq_id'] (minus those not in input)
    - Update "removes all internal fields" test: change input keys `fqc_id` → `fq_id`, `title` → `fq_title`, `status` → `fq_status`; update result assertions accordingly
    - Update "preserves user-provided fields" test: change input `fqc_id` → `fq_id`, `title` → `fq_title`, `tags` → `fq_tags`, `status` → `fq_status`; update assertions
    - Update any other test inputs that use old field name strings

    **resolve-document.test.ts:**
    - Add import: `import { FM } from '../../src/constants/frontmatter-fields.js';`
    - Change `expect(result.resolvedVia).toBe('fqc_id')` → `expect(result.resolvedVia).toBe(FM.ID)`

    **compound-tools.test.ts:**
    - Add import: `import { FM } from '../../src/constants/frontmatter-fields.js';`
    - Change `expect(text).toContain('title')` (near line 1563) → `expect(text).toContain(FM.TITLE)`
    - Change `expect(text).toContain('fqc_id')` (near line 1565) → `expect(text).toContain(FM.ID)`

    **compound-tools.integration.test.ts:**
    - Add import: `import { FM } from '../../src/constants/frontmatter-fields.js';`
    - Change `expect(text).toContain('created')` (near line 869) → `expect(text).toContain(FM.CREATED)`
    - Change `expect(text).toContain('updated')` (near line 870) → `expect(text).toContain(FM.UPDATED)`

    **frontmatter-ordering.integration.test.ts:**
    - This file was written as stubs in Plan 01. Now implement the full assertions:
    - ORD-01: After create_document with user fields, read vault file, assert user fields come before fq_title/fq_id etc.
    - ORD-02: After update_document, re-read frontmatter, assert ordering still user-first
    - ORD-03: After update_doc_header (e.g., add tag), re-read, assert ordering user-first
    - ORD-04: After plugin reconciliation write, assert fq_owner/fq_type appear after user fields

    Run `npm test` after TypeScript test updates. All 4 unit test files must now be GREEN.
    Run `npm run test:integration` to check integration tests.
  </action>
  <verify>
    <automated>npm test -- tests/unit/frontmatter-sanitizer.test.ts tests/unit/resolve-document.test.ts tests/unit/compound-tools.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - frontmatter-sanitizer.test.ts: all tests GREEN including the rewritten ordering test
    - resolve-document.test.ts: `toBe(FM.ID)` assertion passes
    - compound-tools.test.ts: `toContain(FM.TITLE)` and `toContain(FM.ID)` pass
    - compound-tools.integration.test.ts: `toContain(FM.CREATED)` and `toContain(FM.UPDATED)` pass
    - `grep "fqc_id\|'title'\|'status'\|'created'\|'updated'" tests/unit/frontmatter-sanitizer.test.ts` returns 0 matches for field-name string assertions (field names in comments OK)
    - `npm test` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Python test framework and 14 Python test files</name>
  <files>
    tests/scenarios/framework/fqc_vault.py,
    tests/scenarios/directed/testcases/test_reconciliation_frontmatter_discovery.py,
    tests/scenarios/directed/testcases/test_reconciliation_modification.py,
    tests/scenarios/directed/testcases/test_reconciliation_disassociation.py,
    tests/scenarios/directed/testcases/test_reconciliation_resurrection.py,
    tests/scenarios/directed/testcases/test_reconciliation_movement.py,
    tests/scenarios/directed/testcases/test_reconciliation_resurrection_with_on_moved.py,
    tests/scenarios/directed/testcases/test_reconciliation_untrack_policy.py,
    tests/scenarios/directed/testcases/test_reconciliation_keep_tracking_stability.py,
    tests/scenarios/directed/testcases/test_frontmatter_preservation.py,
    tests/scenarios/directed/testcases/test_document_update_partial.py,
    tests/scenarios/directed/testcases/test_plugin_lifecycle.py,
    tests/scenarios/directed/testcases/test_plugin_mixed_tables.py,
    tests/scenarios/integration/run_integration.py
  </files>
  <read_first>
    - tests/scenarios/framework/fqc_vault.py (full file — all sections to update: _ORDERED_FIELDS, VaultDocument properties, create_file, write_frontmatter, _serialize_frontmatter)
    - tests/scenarios/directed/testcases/test_reconciliation_frontmatter_discovery.py line 130 (find the field name string)
    - tests/scenarios/directed/testcases/test_reconciliation_modification.py lines 166–169
    - tests/scenarios/directed/testcases/test_reconciliation_disassociation.py lines 301–305
    - tests/scenarios/directed/testcases/test_reconciliation_resurrection.py line 250
    - tests/scenarios/directed/testcases/test_reconciliation_movement.py lines 405–407
    - tests/scenarios/directed/testcases/test_reconciliation_resurrection_with_on_moved.py lines 288, 465
    - tests/scenarios/directed/testcases/test_reconciliation_untrack_policy.py line 246
    - tests/scenarios/directed/testcases/test_reconciliation_keep_tracking_stability.py line 224
    - tests/scenarios/directed/testcases/test_frontmatter_preservation.py line 478
    - tests/scenarios/directed/testcases/test_document_update_partial.py lines 114, 284
    - tests/scenarios/directed/testcases/test_plugin_lifecycle.py lines 163, 183
    - tests/scenarios/directed/testcases/test_plugin_mixed_tables.py line 240
    - tests/scenarios/integration/run_integration.py lines 344–360, 534–545
  </read_first>
  <action>
    **fqc_vault.py — apply all changes:**

    1. _ORDERED_FIELDS (line 103): Replace with full FQ field list in new order:
       ```python
       _ORDERED_FIELDS = (
           "fq_title", "fq_status", "fq_tags", "fq_created", "fq_updated",
           "fq_owner", "fq_type", "fq_instance", "fq_id",
       )
       ```

    2. VaultDocument properties (lines 124-137):
       Rename the 4 properties: `fqc_id` → `fq_id`, `title` → `fq_title`, `tags` → `fq_tags`, `status` → `fq_status`.
       Update each `self.frontmatter.get("...")` call to use new key names.
       Add backward-compatible aliases (property delegates) to avoid breaking test files outside the 14-file scope:
       ```python
       # Backward-compat aliases for test files not yet updated:
       fqc_id = property(lambda self: self.fq_id)
       title   = property(lambda self: self.fq_title)
       tags    = property(lambda self: self.fq_tags)
       status  = property(lambda self: self.fq_status)
       ```

    3. create_file method:
       - Rename parameter `fqc_id` → `fq_id` (keep as keyword arg)
       - Update fm dict to use new field names in preferred write order:
         `"fq_title": title, "fq_status": status, "fq_tags": normalized_tags, "fq_created": now, "fq_updated": now, "fq_id": fq_id or str(uuid4())`

    4. write_frontmatter method: change `fm["updated"] = _now_iso()` → `fm["fq_updated"] = _now_iso()`

    5. _serialize_frontmatter: change `if key == "tags"` → `if key == "fq_tags"`

    6. Any internal uses of `doc.fqc_id`, `doc.title`, `doc.status`, `doc.tags` within fqc_vault.py itself (e.g., find_files method, inspect_file): update to use `doc.fq_id`, `doc.fq_title`, `doc.fq_status`, `doc.fq_tags`

    **13 directed Python test files:**
    For each file listed in CONTEXT.md, read the lines specified and update the field name strings:
    - `"fqc_id"` → `"fq_id"`
    - `"title"` → `"fq_title"` (only when used as a frontmatter field key, not as Python variable names or doc strings)
    - `"status"` → `"fq_status"` (only when used as frontmatter field key, not MCP response text like "Status: active")
    - `"tags"` → `"fq_tags"` (only when frontmatter field key)
    - `"created"` → `"fq_created"` (only when frontmatter field key)
    - `"updated"` → `"fq_updated"` (only when frontmatter field key)
    - `"fqc_owner"` → `"fq_owner"`
    - `"fqc_type"` → `"fq_type"`
    - `"fqc_instance"` → `"fq_instance"`

    BOUNDARY: MCP response text parsing strings like `r"^Status:\s*(.+)$"` (the regex matching display labels) are NOT changed. Only frontmatter YAML key strings change.

    **run_integration.py:**
    Update lines ~344-360 and ~534-545:
    - `_EXTRACT_PATTERNS` dict key `"fqc_id"` → `"fq_id"` (the Python dict key used internally)
    - `r"^FQC ID:\s*(.+)$"` regex stays unchanged (matches MCP response label "FQC ID:" which is NOT renamed)
    - `"title"` key in `_EXTRACT_PATTERNS` → `"fq_title"` (if the label changed to "FQ Title:" — verify first; if label unchanged, leave key unchanged)
    - `"status"` key in `_EXTRACT_PATTERNS` → `"fq_status"` (same logic)
    - Update `_ACTION_EXTRACT_FIELDS["vault.write"]` tuple: `("fqc_id", ...)` → `("fq_id", ...)`
    - Update `_extract(result.text, "fqc_id", "path")` calls → `_extract(result.text, "fq_id", "path")`
    - Update `resp_fields.get("fqc_id")` → `resp_fields.get("fq_id")`

    Note on run_integration.py: If MCP response labels ("FQC ID:", "Title:", "Status:") did NOT change in Phase 90, then only the Python internal dict keys (used to index into resp_fields) need updating, and the regex patterns themselves stay the same.

    After all Python changes, run `npm test && npm run test:integration` for full suite validation.
  </action>
  <verify>
    <automated>npm test && npm run test:integration 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `grep "_ORDERED_FIELDS" tests/scenarios/framework/fqc_vault.py` shows fq_title, fq_id in the tuple
    - `grep "def fq_id\|def fq_title\|def fq_status\|def fq_tags" tests/scenarios/framework/fqc_vault.py` — 4 property definitions
    - `grep "\"fqc_id\"\|\"fqc_owner\"\|\"fqc_type\"\|\"fqc_instance\"\|\"title\"\|\"status\"\|\"created\"\|\"updated\"\|\"tags\"" tests/scenarios/framework/fqc_vault.py` — 0 matches for frontmatter key string contexts (regex patterns and comments OK)
    - npm test exits 0 (full unit suite green)
    - npm run test:integration exits 0 (including all 4 ordering integration tests)
    - frontmatter-ordering.integration.test.ts: ORD-01 through ORD-04 GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python test framework → FQC server | Python writes vault files then calls FQC via MCP; field names must match server expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-08 | Tampering | fqc_vault.py create_file | accept | fq_id still auto-generated from uuid4() when not provided — no user-controllable input for UUID generation |
| T-90-09 | Spoofing | run_integration.py _EXTRACT_PATTERNS | accept | Regex matches server-generated display labels — renaming internal dict keys has no security impact |
</threat_model>

<verification>
- `npm test` exits 0 — all unit tests green including frontmatter-sanitizer (rewritten ordering test passes), resolve-document (FM.ID assertion), compound-tools (FM.TITLE, FM.ID)
- `npm run test:integration` exits 0 — compound-tools integration and all 4 ordering tests green
- `grep "fq_title\|fq_status\|fq_id\|fq_owner" tests/scenarios/framework/fqc_vault.py` — present in _ORDERED_FIELDS, properties, create_file
- `grep "_ORDERED_FIELDS.*fqc_id\|\"fqc_id\"" tests/scenarios/framework/fqc_vault.py` — 0 matches
- `grep "FM\.TITLE\|FM\.ID\|FM\.CREATED\|FM\.UPDATED" tests/unit/compound-tools.test.ts tests/unit/resolve-document.test.ts tests/unit/frontmatter-sanitizer.test.ts` — present in all 3 files
</verification>

<success_criteria>
- All 4 TypeScript test files fully updated with FM constants and new field name assertions
- Integration ordering tests ORD-01 through ORD-04 GREEN
- fqc_vault.py _ORDERED_FIELDS uses new fq_* names in correct order
- VaultDocument has fq_id, fq_title, fq_tags, fq_status properties (with backward-compat aliases)
- VaultDocument.create_file writes fq_title, fq_status, fq_tags, fq_created, fq_updated, fq_id
- All 13 directed test files use new fq_* field name strings for frontmatter key references
- run_integration.py internal dict keys updated
- Full suite green: npm test && npm run test:integration exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-05-SUMMARY.md`
</output>
