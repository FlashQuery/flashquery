---
phase: 90
plan: 07
type: execute
wave: 2
depends_on: [90-06]
files_modified:
  - tests/unit/resolve-document.test.ts
  - src/mcp/tools/documents.ts
  - src/mcp/tools/compound.ts
autonomous: true
requirements: [REF-03, REF-04]

must_haves:
  truths:
    - "resolve-document.test.ts mock YAML strings use fq_id, fq_title, fq_created, fq_status (not old names)"
    - "documents.ts line 608 reads data[FM.TITLE] for docTitle (not data.title)"
    - "Error messages in compound.ts and documents.ts say fq_id (not fqc_id)"
    - "npm test passes — all unit tests green after mock string updates"
  artifacts:
    - path: "tests/unit/resolve-document.test.ts"
      provides: "updated mock YAML with fq_* keys"
      contains: "fq_id:"
    - path: "src/mcp/tools/documents.ts"
      provides: "FM.TITLE used for docTitle in get_document"
      contains: "FM.TITLE"
    - path: "src/mcp/tools/compound.ts"
      provides: "error message with fq_id"
      contains: "fq_id"
  key_links:
    - from: "resolve-document.test.ts mock strings"
      to: "targetedScan implementation"
      via: "matching fq_id key names"
      pattern: "fq_id:"
    - from: "get_document"
      to: "vault frontmatter"
      via: "data[FM.TITLE]"
      pattern: "data\\[FM\\.TITLE\\]"
---

<objective>
Fix three remaining gap items after Plan 90-06 unblocks targetedScan: update test mock YAML strings (WR-03), fix data.title in get_document (IN-01), and update two error message strings (IN-02).

Purpose: With targetedScan reading fq_id correctly, test mocks must also use fq_id so tests actually exercise the fixed code path. The data.title bug silently degrades embedding quality. Error string consistency aids AI callers.
Output: All four gaps from VERIFICATION.md closed. npm test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-06-SUMMARY.md
</context>

<interfaces>
<!-- FM constant values -->
<!-- From src/constants/frontmatter-fields.ts -->
```typescript
export const FM = {
  TITLE:    'fq_title',   // maps to old 'title'
  STATUS:   'fq_status',  // maps to old 'status'
  TAGS:     'fq_tags',    // maps to old 'tags'
  CREATED:  'fq_created', // maps to old 'created'
  UPDATED:  'fq_updated',
  OWNER:    'fq_owner',
  TYPE:     'fq_type',
  INSTANCE: 'fq_instance',
  ID:       'fq_id',      // maps to old 'fqc_id'
} as const;
```

<!-- FM is already imported in documents.ts at line 31 -->
<!-- FM is already imported in compound.ts at line 33 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update mock YAML strings in resolve-document.test.ts from old key names to fq_* names</name>

  <read_first>
    - tests/unit/resolve-document.test.ts (read the FULL file to see all mock strings in current state)
    - src/constants/frontmatter-fields.ts (confirm fq_* values)
  </read_first>

  <files>tests/unit/resolve-document.test.ts</files>

  <action>
The test file uses inline YAML strings as mock file content for vi.mocked(fsPromises.readFile).mockResolvedValue(). These strings use old frontmatter key names. After Plan 90-06 fixes targetedScan to read fq_id, the mock content must also use fq_id (and other fq_* keys) so that tests verify the correct code path.

Apply the following replacements to ALL inline YAML mock strings in the test file. The pattern is any string passed to mockResolvedValue() that contains frontmatter (between --- delimiters).

**Key renames to apply within mock YAML content:**
- `fqc_id:` → `fq_id:`
- `title:` → `fq_title:` (only within YAML frontmatter mock strings, NOT in test description text)
- `created:` → `fq_created:` (only within YAML frontmatter mock strings)
- `status:` → `fq_status:` (only within YAML frontmatter mock strings)
- `tags:` → `fq_tags:` (only within YAML frontmatter mock strings, if present)

**Specific locations (line numbers are approximate — read the file to confirm):**

Line 222 — reconciliation test mock:
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\ntitle: My Note\n---\n# Hello` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\nfq_title: My Note\n---\n# Hello` ``

Line 339 — TSA-03 identity resolution test:
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\ntitle: My Doc\n---\n# Hello` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\nfq_title: My Doc\n---\n# Hello` ``

Line 373 — TSA-03 "only updates identity fields" test (has title, tags, updated):
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\ntitle: Original Title\ntags: [a, b]\nupdated: 2025-01-01T00:00:00Z\n---\n# Content` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\nfq_title: Original Title\nfq_tags: [a, b]\nfq_updated: 2025-01-01T00:00:00Z\n---\n# Content` ``

Line 392 — the expect call in that same test currently checks `frontmatterArg.fqc_id` and `frontmatterArg.title`. These must also be updated:
BEFORE: `expect(frontmatterArg.title).toBe('Original Title');`
AFTER:  `expect(frontmatterArg[FM.TITLE]).toBe('Original Title');`

BEFORE: `expect(frontmatterArg.fqc_id).toBe(SAMPLE_UUID);`
AFTER:  `expect(frontmatterArg[FM.ID]).toBe(SAMPLE_UUID);`

(Note: FM is already imported at line 3 of the test file.)

Line 401 — TSA-05 mutex test:
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\n---\n# Content` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\n---\n# Content` ``

Line 420 — TSA-05 per-file mutex test:
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\n---\n# Content` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\n---\n# Content` ``

Line 467 — TSA-03 field scoping test (has created, status):
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\ncreated: 2025-01-01T00:00:00Z\nstatus: active\n---\n# Content` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\nfq_created: 2025-01-01T00:00:00Z\nfq_status: active\n---\n# Content` ``

Line 522 — PLG-03 targetedScan resolved DbRow test:
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\ntitle: My Doc\n---\n# Content` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\nfq_title: My Doc\n---\n# Content` ``

Line 574 — PLG-03 all-required-fields test (has created, status, tags):
BEFORE: `` `---\nfqc_id: ${SAMPLE_UUID}\ncreated: 2025-01-01T00:00:00Z\nstatus: active\ntags: [test]\n---\n# Content` ``
AFTER:  `` `---\nfq_id: ${SAMPLE_UUID}\nfq_created: 2025-01-01T00:00:00Z\nfq_status: active\nfq_tags: [test]\n---\n# Content` ``

Also check: the test description string at line 169 says `resolvedVia=fqc_id` — this is a test description string, NOT mock YAML content. Update it to say `resolvedVia=fq_id` for accuracy:
BEFORE: `'UUID input — queries DB by id and returns resolved document with resolvedVia=fqc_id'`
AFTER:  `'UUID input — queries DB by id and returns resolved document with resolvedVia=fq_id'`

After applying all replacements, verify no `fqc_id:` remains in any mock YAML string (only `fq_id:`).
  </action>

  <verify>
    <automated>
# Zero mock YAML strings with old key names
grep -n "fqc_id:" /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/tests/unit/resolve-document.test.ts

# fq_id: must appear in mock content strings (at least 8 occurrences)
grep -c "fq_id:" /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/tests/unit/resolve-document.test.ts

# Run unit tests — must pass
cd /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery && npm test 2>&1 | tail -30
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "fqc_id:" tests/unit/resolve-document.test.ts` returns ZERO lines
    - `grep -c "fq_id:" tests/unit/resolve-document.test.ts` returns 8 or more (one per mock string location)
    - Mock strings containing `created:` or `status:` in frontmatter now use `fq_created:` and `fq_status:`
    - Mock strings containing `title:` in frontmatter now use `fq_title:`
    - `frontmatterArg.fqc_id` and `frontmatterArg.title` references replaced with `frontmatterArg[FM.ID]` and `frontmatterArg[FM.TITLE]`
    - `npm test` passes with zero failures in resolve-document.test.ts
  </acceptance_criteria>

  <done>All mock YAML frontmatter strings in resolve-document.test.ts use fq_* field names. Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix data.title in documents.ts get_document and update fqc_id error strings</name>

  <read_first>
    - src/mcp/tools/documents.ts (read lines 595-620 to confirm current state of line 608; read lines 360-380 to confirm line 371 error string)
    - src/mcp/tools/compound.ts (read lines 125-145 to confirm line 139 error string)
    - src/constants/frontmatter-fields.ts (confirm FM.TITLE value)
  </read_first>

  <files>src/mcp/tools/documents.ts, src/mcp/tools/compound.ts</files>

  <action>
**IN-01 fix — documents.ts line 608 (get_document docTitle):**

BEFORE:
```typescript
const docTitle = typeof data.title === 'string' ? data.title : relativePath;
```
AFTER:
```typescript
const docTitle = typeof data[FM.TITLE] === 'string' ? data[FM.TITLE] as string : relativePath;
```

FM is already imported in documents.ts at line 31. No new import needed.

**IN-02 fix — documents.ts line 371 (create_document guard error message):**

The current error message at line 371 contains the string `fqc_id`. Find the exact line (it is inside a guard that checks if an existing document at the target path has an fqc_id):
BEFORE: `text: \`Error: Document already exists at "${relativePath}" with fqc_id "${existingFqcId}". Use update_document to modify an existing document.\``
AFTER:  `text: \`Error: Document already exists at "${relativePath}" with fq_id "${existingFqcId}". Use update_document to modify an existing document.\``

**IN-02 fix — compound.ts line 139 (update_doc_header guard error message):**

BEFORE: `text: 'Error: Unable to provision document. Document must have an fqc_id to be modified.'`
AFTER:  `text: 'Error: Unable to provision document. Document must have an fq_id to be modified.'`
  </action>

  <verify>
    <automated>
# IN-01: FM.TITLE must appear in the docTitle assignment
grep -n "FM\.TITLE" /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/src/mcp/tools/documents.ts | grep -i "docTitle"

# IN-01: data.title must NOT appear for docTitle
grep -n "data\.title" /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/src/mcp/tools/documents.ts

# IN-02: no 'fqc_id' in user-facing error strings
grep -n "fqc_id" /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/src/mcp/tools/documents.ts
grep -n "fqc_id" /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/src/mcp/tools/compound.ts

# TypeScript clean
cd /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery && npx tsc --noEmit 2>&1 | head -30

# All unit tests still pass
cd /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery && npm test 2>&1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "data\.title" src/mcp/tools/documents.ts` returns ZERO lines (the old data.title at line 608 is gone)
    - `grep -n "FM\.TITLE" src/mcp/tools/documents.ts` shows the docTitle line now uses `data[FM.TITLE]`
    - `grep -n "fqc_id" src/mcp/tools/documents.ts` returns ZERO lines (no old-name strings in error messages or logic)
    - `grep -n "fqc_id" src/mcp/tools/compound.ts` returns ZERO lines
    - `npx tsc --noEmit` exits with code 0
    - `npm test` passes
  </acceptance_criteria>

  <done>data.title fixed to data[FM.TITLE] in get_document. Both fqc_id error strings updated to fq_id. TypeScript compiles and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mock → production code | Test mock YAML strings must match what production code expects to read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-07-01 | Information Disclosure | get_document embedding title | mitigate | Using FM.TITLE ensures correct vault field is read; degrades gracefully to relativePath if missing |
| T-90-07-02 | Tampering | test mock divergence | accept | Test mocks updated to match production keys; unit tests catch regressions |
</threat_model>

<verification>
After completing both tasks, run the full verification:

```bash
cd /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery

# Gap 1 (CR-01/WR-01) — should be zero from plan 90-06
grep -n "parsed\.data\.fqc_id\|parsed\.data\.created\b\|parsed\.data\.status\b\|fm\.fqc_id" src/mcp/utils/resolve-document.ts

# Gap 2 (WR-03) — test mocks
grep -n "fqc_id:" tests/unit/resolve-document.test.ts

# Gap 3 (IN-01)
grep -n "data\.title" src/mcp/tools/documents.ts

# Gap 4 (IN-02)
grep -n "fqc_id" src/mcp/tools/documents.ts src/mcp/tools/compound.ts

# Full unit test suite
npm test
```

All grep commands must return zero matches. npm test must pass.
</verification>

<success_criteria>
- Zero occurrences of fqc_id: in resolve-document.test.ts mock YAML strings
- data[FM.TITLE] used for docTitle in documents.ts get_document
- Error messages in compound.ts and documents.ts say fq_id not fqc_id
- npm test passes (all unit tests green)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/90-centralize-frontmatter-field-names-into-fm-constants-and-ren/90-07-SUMMARY.md`
</output>
