---
phase: 128-legacy-surface-removal-final-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 128-01
files_modified:
  - src/mcp/tool-metadata.ts
  - src/mcp/tool-exposure.ts
  - src/llm/tool-registry.ts
  - src/config/loader.ts
  - tests/unit/tool-metadata.test.ts
  - tests/unit/tool-exposure.test.ts
  - tests/unit/llm-tool-registry.test.ts
  - tests/unit/config.test.ts
  - tests/unit/llm-config.test.ts
  - tests/integration/llm-config-sync.test.ts
  - tests/e2e/protocol.test.ts
autonomous: true
requirements:
  - DOC-10
  - MEM-05
  - SYS-05
  - TEST-07
must_haves:
  truths:
    - "Removed and dead tool names cannot be selected for host MCP exposure."
    - "Delegated tool assembly cannot regain removed names through tiers or explicit purpose config."
    - "Legacy names fail validation with suggestions and no compatibility aliases."
  artifacts:
    - path: "src/mcp/tool-metadata.ts"
      provides: "Final metadata status, replacement suggestions, delegated tiers"
      contains: "getLegacyToolSuggestion"
    - path: "src/mcp/tool-exposure.ts"
      provides: "Host selector rejection for unavailable names"
      contains: "isCurrentHostSelectable"
    - path: "tests/e2e/protocol.test.ts"
      provides: "MCP listTools absence assertions"
      contains: "listTools"
  key_links:
    - from: "src/mcp/tool-metadata.ts"
      to: "src/llm/tool-registry.ts"
      via: "TOOL_TIERS and delegated hard exclusions"
      pattern: "TOOL_TIERS"
    - from: "src/mcp/tool-exposure.ts"
      to: "src/config/loader.ts"
      via: "resolveHostToolExposure"
      pattern: "resolveHostToolExposure"
---

<objective>
Finalize metadata, config validation, host exposure, and delegated registry behavior for removed/dead tool names.

Purpose: Before deleting handlers and stale tests, the central registry must represent the final surface: removed names are suggestion metadata only, dead project tools are not active, and transitional tools remain eligible only as transitional.
Output: Registry/config/delegated/protocol tests that prove removed names cannot appear on host or delegated surfaces.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/128-legacy-surface-removal-final-audit/128-CONTEXT.md
@.planning/phases/128-legacy-surface-removal-final-audit/128-RESEARCH.md
@.planning/phases/128-legacy-surface-removal-final-audit/128-PATTERNS.md
@.planning/phases/128-legacy-surface-removal-final-audit/TRACEABILITY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md

Implementation agents MUST read the two product docs above before resolving scope or test questions. If those docs and `.planning/ROADMAP.md` conflict, use `.planning/ROADMAP.md` for the Phase 128 boundary and the product docs for detailed contract inside that boundary.

<interfaces>
From `src/mcp/tool-metadata.ts`:
```typescript
export type ToolStatus = 'final' | 'transitional' | 'removed' | 'dead';
export interface ToolMetadata { name: string; status: ToolStatus; categories: ToolCategory[]; tier: ToolTier; hostEligible: boolean; delegatedEligible: boolean; replacement?: string; description: string; }
export function getLegacyToolSuggestion(name: string): { replacement: string; message: string } | undefined;
export function getToolNamesByTier(tier: ToolTierSelector): string[];
```

From `src/mcp/tool-exposure.ts`:
```typescript
export function validateToolSelectors(selectors: readonly string[] = []): string[];
export function resolveHostToolExposure(config?: HostMcpToolsConfig): ResolvedHostToolExposure;
```

From `src/llm/tool-registry.ts`:
```typescript
export const TOOL_TIERS: { readonly 'tier:read-only': readonly string[]; readonly 'tier:read-write': readonly string[] };
export function assembleNativeToolRegistry(...): ToolRegistryAssembly;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize metadata status and selector validation</name>
  <files>src/mcp/tool-metadata.ts, src/mcp/tool-exposure.ts, src/config/loader.ts, tests/unit/tool-metadata.test.ts, tests/unit/tool-exposure.test.ts, tests/unit/config.test.ts, tests/unit/llm-config.test.ts</files>
  <read_first>
    - src/mcp/tool-metadata.ts
    - src/mcp/tool-exposure.ts
    - src/config/loader.ts
    - tests/unit/tool-metadata.test.ts
    - tests/unit/tool-exposure.test.ts
    - tests/unit/config.test.ts
    - tests/unit/llm-config.test.ts
    - .planning/phases/128-legacy-surface-removal-final-audit/128-CONTEXT.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <behavior>
    - `append_to_doc`, `create_document`, `update_document`, `update_doc_header`, `search_documents`, `save_memory`, `update_memory`, `search_memory`, `list_memories`, `force_file_scan`, `reconcile_documents`, `create_directory`, `remove_directory`, `create_record`, `update_record`, and `search_all` have `status: "removed"` metadata with replacement suggestions where the product docs require suggestions.
    - `list_projects` and `get_project_info` are not host-selectable, not delegated-selectable, and not current/final tools.
    - `get_briefing` and `insert_doc_link` remain `status: "transitional"` and are not returned by `getLegacyToolSuggestion`.
    - Config validation rejects removed names in `host_mcp_tools.tools`, `host_mcp_tools.excluded_tools`, LLM purpose `tools`, and LLM purpose `excluded_tools` with text containing the old name, the replacement where known, and `does not alias legacy tool names` or equivalent no-alias wording.
  </behavior>
  <action>Update tests first, then production. Replace any temporary Phase 127 local deny-list behavior with final metadata-driven availability for all removed/dead names. Preserve replacement suggestion metadata; do not create aliases or silent rewrites. Ensure transitional `get_briefing` and `insert_doc_link` stay host/delegated eligible according to their current categories and tiers.</action>
  <acceptance_criteria>
    - `npm test -- tests/unit/tool-metadata.test.ts tests/unit/tool-exposure.test.ts tests/unit/config.test.ts tests/unit/llm-config.test.ts`
    - `grep -q "getLegacyToolSuggestion" src/mcp/tool-metadata.ts`
    - `grep -q "get_briefing" src/mcp/tool-metadata.ts && grep -q "insert_doc_link" src/mcp/tool-metadata.ts`
    - `! grep -n "PHASE_127_LOCALLY_REPLACED_TOOLS" src/mcp/tool-exposure.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/tool-metadata.test.ts tests/unit/tool-exposure.test.ts tests/unit/config.test.ts tests/unit/llm-config.test.ts</automated>
  </verify>
  <done>Metadata and config validation make removed/dead names unavailable while preserving suggestion text and transitional tool eligibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove delegated and protocol absence</name>
  <files>src/llm/tool-registry.ts, tests/unit/llm-tool-registry.test.ts, tests/integration/llm-config-sync.test.ts, tests/e2e/protocol.test.ts</files>
  <read_first>
    - src/llm/tool-registry.ts
    - src/mcp/tool-metadata.ts
    - src/mcp/tool-catalog.ts
    - src/mcp/server.ts
    - tests/unit/llm-tool-registry.test.ts
    - tests/integration/llm-config-sync.test.ts
    - tests/e2e/protocol.test.ts
    - .planning/phases/128-legacy-surface-removal-final-audit/128-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <behavior>
    - `TOOL_TIERS["tier:read-only"]` and `TOOL_TIERS["tier:read-write"]` contain only current final or transitional tools.
    - Delegated assembly returns removed/dead names in diagnostics instead of provider tools if they are requested explicitly or through stale tier expectations.
    - MCP `listTools` does not contain any removed/dead names and still contains final replacements plus `get_briefing` and `insert_doc_link`.
  </behavior>
  <action>Update delegated tier expectations to derive from final metadata or match the final tool list exactly. Add protocol assertions for the complete removed/dead set and positive assertions for `write_document`, `search`, `write_memory`, `manage_directory`, `maintain_vault`, `remove_document`, `get_briefing`, and `insert_doc_link` where host-enabled by default. Keep `call_model` hard-excluded from delegated tools.</action>
  <acceptance_criteria>
    - `npm test -- tests/unit/llm-tool-registry.test.ts && npm run test:integration -- tests/integration/llm-config-sync.test.ts && npm run test:e2e -- tests/e2e/protocol.test.ts`
    - `grep -q "append_to_doc" tests/e2e/protocol.test.ts`
    - `grep -q "not.toContain" tests/e2e/protocol.test.ts`
    - `grep -q "get_briefing" tests/e2e/protocol.test.ts && grep -q "insert_doc_link" tests/e2e/protocol.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool-registry.test.ts && npm run test:integration -- tests/integration/llm-config-sync.test.ts && npm run test:e2e -- tests/e2e/protocol.test.ts</automated>
  </verify>
  <done>Host listTools and delegated provider tools cannot expose removed/dead names, and transitional names remain intentionally present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config -> host tool exposure | Untrusted config selectors choose public MCP surface. |
| YAML purpose config -> delegated tool belt | Purpose config influences provider-visible native tools. |
| MCP client -> listTools | Host clients inspect the callable tool surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-02-01 | Elevation of Privilege | `src/llm/tool-registry.ts` | mitigate | Removed/dead names must not appear in provider tools, even when explicitly requested or selected by tier. |
| T-128-02-02 | Tampering | `src/config/loader.ts` | mitigate | Reject legacy names at startup with suggestions and no alias rewrite. |
| T-128-02-03 | Information Disclosure | `tests/e2e/protocol.test.ts` | mitigate | Protocol assertions prove dead project tools and legacy surfaces are absent from `listTools`. |
</threat_model>

<traceability>
| Requirement | Evidence in this plan |
|-------------|-----------------------|
| DOC-10 | Document legacy names become removed metadata and fail host/delegated/protocol checks. |
| MEM-05 | Memory legacy names become removed metadata and fail host/delegated/protocol checks. |
| SYS-05 | Dead project tools remain unavailable in metadata, host selection, delegated selection, and protocol tests. |
| TEST-07 | Removed/merged names have migration suggestions or absence assertions, not compatibility aliases. |
</traceability>

<verification>
Run the focused unit, integration, and E2E commands in task verification. Do not delete handlers in this plan unless needed to make metadata/protocol tests accurate; handler deletion is owned by Plan 128-03.
</verification>

<success_criteria>
Central metadata, config validation, delegated registry, and protocol listing encode the final reduced tool surface.
</success_criteria>

<output>
After completion, create `.planning/phases/128-legacy-surface-removal-final-audit/128-02-SUMMARY.md`.
</output>
