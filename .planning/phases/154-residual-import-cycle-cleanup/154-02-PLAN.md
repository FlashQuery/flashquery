---
phase: 154-residual-import-cycle-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 154-01
  - 154-03
files_modified:
  - src/llm/errors.ts
  - src/llm/runtime-types.ts
  - src/llm/client.ts
  - src/llm/resolver.ts
  - src/llm/config-sync-types.ts
  - src/llm/config-sync.ts
  - src/llm/purpose-template-bindings.ts
  - src/llm/reference-metadata.ts
  - src/llm/reference-resolver.ts
  - src/llm/types.ts
  - src/embedding/dimensions.ts
  - src/embedding/provider.ts
  - src/embedding/background-embed.ts
  - src/storage/supabase.ts
  - src/logging/logger.ts
  - tests/unit/circular-deps.test.ts
  - tests/unit/llm-config.test.ts
  - tests/unit/llm-tool-registry.test.ts
  - tests/unit/llm-client.test.ts
  - tests/unit/llm-config-sync.test.ts
  - tests/unit/purpose-template-bindings.test.ts
  - tests/unit/template-tools.test.ts
  - tests/unit/reference-resolver.test.ts
  - tests/unit/embedding-provider.test.ts
  - tests/integration/reference-resolver.integration.test.ts
autonomous: true
requirements:
  - REQ-011
  - REQ-010
  - REQ-012
must_haves:
  truths:
    - "LLM runtime, template, reference, embedding, storage, and logging modules do not form any production madge cycle."
    - "Model fallback, LLM error classification, cost recording, config DB sync, purpose-template validation, template tool schema assembly, document reference hydration, injected-reference metadata, embedding dimensions, and background embedding scheduling remain unchanged."
    - "The final production `src/` graph has zero circular dependencies under pinned madge."
    - "Project quality gates pass after all Phase 154 refactors."
  artifacts:
    - path: src/llm/errors.ts
      provides: "Leaf LLM error classes shared by client and resolver"
    - path: src/llm/runtime-types.ts
      provides: "Leaf chat/client/result interfaces shared without importing client implementation"
    - path: src/llm/config-sync-types.ts
      provides: "Leaf ConfigSyncAdapter and ConfigSyncResult types"
    - path: src/llm/reference-metadata.ts
      provides: "Leaf injected-reference and template metadata interfaces"
    - path: src/embedding/dimensions.ts
      provides: "Dependency-light embedding dimension resolver"
    - path: tests/unit/circular-deps.test.ts
      provides: "T-U-031 final zero-cycle guard plus T-U-033 and T-U-034 targeted guards"
  key_links:
    - from: src/llm/resolver.ts
      to: src/llm/errors.ts
      via: "LlmFallbackError import without importing llm/client.ts"
      pattern: "from './errors.js'"
    - from: src/llm/purpose-template-bindings.ts
      to: src/llm/config-sync-types.ts
      via: "ConfigSyncAdapter type import"
      pattern: "from './config-sync-types.js'"
    - from: src/llm/types.ts
      to: src/llm/reference-metadata.ts
      via: "InjectedReferenceMetadata type import"
      pattern: "from './reference-metadata.js'"
    - from: src/storage/supabase.ts
      to: src/embedding/dimensions.ts
      via: "getEmbeddingDimensions import without importing embedding/provider.ts"
      pattern: "from '../embedding/dimensions.js'"
---

<objective>
Break the REQ-011 LLM runtime/template/reference/embedding/storage/logging import cycle family and run the final Phase 154 static and quality gates.

Purpose: Separate shared types, errors, metadata, adapter contracts, and embedding dimension policy into dependency-light leaves so concrete runtime modules no longer import each other cyclically.
Output: Leaf modules, updated imports, behavior-preserving regression coverage, final zero-cycle guard, and project quality gate evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md
@.planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md
@.planning/phases/154-residual-import-cycle-cleanup/154-03-SUMMARY.md
@AGENTS.md

Downstream implementation agents must read the Phase 154 planning docs first, especially `154-CONTEXT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `154-PATTERNS.md`, before making implementation choices. If questions remain, consult those docs first and escalate only unanswered ambiguity.

Research is intentionally skipped for Phase 154. Do not create or require `154-RESEARCH.md`.

Relevant interfaces and contracts:
- Plan 01 should have moved `FlashQueryConfig` to `src/config/types.ts` and removed concrete LLM registry/runtime imports from `src/config/loader.ts`.
- Plan 03 should have moved MCP lifecycle registration/lookup to a dependency-light registry and removed the `mcp/server.ts` <-> `server/shutdown.ts` dynamic cycle.
- `src/llm/client.ts` exports `LlmHttpError`, `LlmNetworkError`, `ChatMessage`, `LlmCompletionResult`, `LlmClient`, `OpenAICompatibleLlmClient`, `NullLlmClient`, and `initLlm`.
- `src/llm/resolver.ts` exports `LlmFallbackError` and `PurposeResolver`; resolver fallback behavior must not import `src/llm/client.ts`.
- `src/llm/config-sync.ts` owns `syncConfigAdapter`, `syncPurposeTemplateBindings`, and `syncLlmConfigToDb`; `ConfigSyncAdapter` and `ConfigSyncResult` should be leaf types.
- `src/llm/reference-resolver.ts` owns reference parsing, template rendering, hydration, `buildInjectedReferences`, and `computePromptChars`; injected-reference metadata interfaces should be leaf types.
- `src/embedding/provider.ts` owns provider implementations and `initEmbedding`; `getEmbeddingDimensions` should live in a dependency-light module used by both provider and storage.
- `src/logging/logger.ts` must preserve log format and credential-safety behavior while importing config types from the leaf.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add REQ-011 and final static graph guards</name>
  <files>tests/unit/circular-deps.test.ts, tests/unit/llm-client.test.ts, tests/unit/llm-config-sync.test.ts, tests/unit/purpose-template-bindings.test.ts, tests/unit/template-tools.test.ts, tests/unit/reference-resolver.test.ts, tests/unit/embedding-provider.test.ts, tests/integration/reference-resolver.integration.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, .planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md, .planning/phases/154-residual-import-cycle-cleanup/154-03-SUMMARY.md, tests/unit/circular-deps.test.ts, tests/unit/llm-client.test.ts, tests/unit/llm-config-sync.test.ts, tests/unit/purpose-template-bindings.test.ts, tests/unit/template-tools.test.ts, tests/unit/reference-resolver.test.ts, tests/unit/embedding-provider.test.ts, tests/integration/reference-resolver.integration.test.ts</read_first>
  <behavior>
    - T-U-031: the pinned static guard runs `npx --yes madge@8.0.0 src --extensions ts --circular` and asserts exit code 0 for the final production `src/` graph unless Matt explicitly approved a documented residual.
    - T-U-033: the static guard fails if any cycle line contains `llm/client.ts`, `llm/resolver.ts`, `llm/config-sync.ts`, `llm/purpose-template-bindings.ts`, `llm/template-tools.ts`, `llm/reference-resolver.ts`, `llm/types.ts`, `embedding/provider.ts`, `embedding/background-embed.ts`, `storage/supabase.ts`, or `logging/logger.ts`.
    - T-U-034: if Plan 03 did not add it, the static guard fails if any cycle line contains both `mcp/server.ts` and `server/shutdown.ts`, including dynamic-import edges.
    - T-U-036 and T-I-010: existing LLM/reference/embedding behavior remains asserted; add narrow direct tests only for newly extracted pure leaves not already covered.
  </behavior>
  <action>Extend `tests/unit/circular-deps.test.ts` with T-U-031 final zero-cycle behavior using the existing `spawnSync('npx', ['--yes', 'madge@8.0.0', ...])` pattern, plus T-U-033 targeted REQ-011 family guards and T-U-034 if not already present from Plan 03. Preserve older Phase 149 targeted assertions until the new final guard passes. Add direct unit tests for `src/llm/errors.ts`, `src/llm/config-sync-types.ts`, `src/llm/reference-metadata.ts`, or `src/embedding/dimensions.ts` only if extraction creates behavior not already covered by existing suites. Keep external-provider integration behind existing environment gates. Do not rewrite public MCP response envelopes or document-reference semantics.</action>
  <verify>
    <automated>npm test -- tests/unit/circular-deps.test.ts tests/unit/llm-client.test.ts tests/unit/llm-config-sync.test.ts tests/unit/purpose-template-bindings.test.ts tests/unit/template-tools.test.ts tests/unit/reference-resolver.test.ts tests/unit/embedding-provider.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/reference-resolver.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>T-U-031, T-U-033, and T-U-034 are represented in static tests; T-U-036 regression coverage remains green after implementation; T-I-010 passes or skips only through existing environment gates; matching madge cycle lines are included in failure messages.</acceptance_criteria>
  <done>REQ-011 and final graph test coverage exists before production import changes are completed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract LLM/runtime leaves and remove REQ-011 cycles</name>
  <files>src/llm/errors.ts, src/llm/runtime-types.ts, src/llm/client.ts, src/llm/resolver.ts, src/llm/config-sync-types.ts, src/llm/config-sync.ts, src/llm/purpose-template-bindings.ts, src/llm/reference-metadata.ts, src/llm/reference-resolver.ts, src/llm/types.ts, src/embedding/dimensions.ts, src/embedding/provider.ts, src/embedding/background-embed.ts, src/storage/supabase.ts, src/logging/logger.ts, tests/unit/circular-deps.test.ts, tests/unit/llm-client.test.ts, tests/unit/llm-config-sync.test.ts, tests/unit/purpose-template-bindings.test.ts, tests/unit/template-tools.test.ts, tests/unit/reference-resolver.test.ts, tests/unit/embedding-provider.test.ts, tests/integration/reference-resolver.integration.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, .planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md, .planning/phases/154-residual-import-cycle-cleanup/154-03-SUMMARY.md, AGENTS.md, src/config/types.ts, src/llm/client.ts, src/llm/resolver.ts, src/llm/config-sync.ts, src/llm/purpose-template-bindings.ts, src/llm/template-tools.ts, src/llm/reference-resolver.ts, src/llm/types.ts, src/embedding/provider.ts, src/embedding/background-embed.ts, src/storage/supabase.ts, src/logging/logger.ts</read_first>
  <behavior>
    - `PurposeResolver` fallback order, transient/permanent error handling, 429 delay cap, and `LlmFallbackError` attempts remain unchanged.
    - `OpenAICompatibleLlmClient` still records costs and usage metadata as before, including fallback detail.
    - Config DB sync still deletes YAML rows, preserves runtime-owned rows, warns/skips as before, and syncs purpose-template bindings.
    - Purpose-template validation, template tool schema assembly, document reference hydration, injected-reference metadata, embedding provider factory/dimensions, and background embedding scheduling remain unchanged.
    - Logger output format remains `[YYYY-MM-DD HH:MM:SS] LEVEL  Message` or request-correlated equivalent.
  </behavior>
  <action>Extract `LlmHttpError` and `LlmNetworkError` from `src/llm/client.ts`, and `LlmFallbackError` from `src/llm/resolver.ts`, into `src/llm/errors.ts`; update both client and resolver to import from that leaf so resolver does not import client. Extract `ChatMessage`, `LlmCompletionResult`, `LlmClient`, and any shared model-selection result types into `src/llm/runtime-types.ts` when needed to avoid client implementation imports; preserve exports from `client.ts` if public callers currently import them there. Move `ConfigSyncAdapter` and `ConfigSyncResult` to `src/llm/config-sync-types.ts`; update `config-sync.ts` and `purpose-template-bindings.ts` to import type-only from the leaf. Move `TemplateParamDeclaration`, `TemplateParamUsage`, `TemplateItemMetadata`, `InjectedReferenceMetadata`, and related metadata-only interfaces to `src/llm/reference-metadata.ts`; update `reference-resolver.ts` and `types.ts` to import type-only from that leaf. Move `getEmbeddingDimensions` to `src/embedding/dimensions.ts` with type-only dependency on `FlashQueryConfig` from `src/config/types.ts`; update `provider.ts` and `storage/supabase.ts` to use it without storage importing provider implementation. Update `logging/logger.ts` to import `FlashQueryConfig` from `src/config/types.ts` or introduce a narrower local logging config type. Use ESM `.js` specifiers, `import type` for type-only dependencies, and avoid unrelated Supabase, document semantic, or MCP envelope changes. If macro-visible native tool or LLM types move, update macro imports and run T-C-011.</action>
  <verify>
    <automated>npm test -- tests/unit/circular-deps.test.ts tests/unit/llm-client.test.ts tests/unit/llm-config-sync.test.ts tests/unit/purpose-template-bindings.test.ts tests/unit/template-tools.test.ts tests/unit/reference-resolver.test.ts tests/unit/embedding-provider.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/reference-resolver.integration.test.ts</automated>
    <automated>npx --yes madge@8.0.0 src --extensions ts --circular</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run knip</automated>
    <automated>npm run build</automated>
    <automated>if rg -n "from ['\\\"].*(llm/(types|runtime-types|tool-registry|client)|native-tool)" src/macro tests/scenarios tests/unit tests/integration; then npm run test:macro-framework; else echo "T-C-011 not triggered"; fi</automated>
    <automated>npx --yes madge src --extensions ts --circular</automated>
  </verify>
  <acceptance_criteria>`npx --yes madge@8.0.0 src --extensions ts --circular` exits 0; unpinned parity command `npx --yes madge src --extensions ts --circular` also exits 0 because roadmap/context require it when unpinned madge is used anywhere; T-U-031 through T-U-036 pass, T-I-010 passes or skips only through existing gates, T-C-007 through T-C-010 pass, and T-C-011 passes if macro-visible types moved; `rg -n "from './client\\.js'|from '../llm/client\\.js'" src/llm/resolver.ts src/llm/types.ts` finds no back-edge import.</acceptance_criteria>
  <done>REQ-011 is complete and the full Phase 154 production graph and project quality gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP call_model input -> LLM runtime | User/model messages and reference syntax are hydrated before provider calls. |
| LLM runtime -> Supabase calls log/config sync | Usage, cost, and runtime config state cross into persistent storage. |
| Embedding provider -> storage schema | Config-driven embedding dimensions control pgvector DDL and provider setup. |
| Logger -> filesystem/stdout | Runtime events are emitted without exposing credentials or changing MCP stdout expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-011-01 | Tampering | `src/llm/reference-resolver.ts` metadata extraction | mitigate | Preserve `buildInjectedReferences`, hydration, and integration coverage for `{{ref:...}}` / `{{id:...}}` metadata. |
| T-154-011-02 | Repudiation | LLM cost/calls logging | mitigate | Keep cost recording and logger format covered by existing LLM/client and logger-adjacent tests; do not change response envelopes. |
| T-154-011-03 | Denial of Service | Background embedding scheduling | mitigate | Preserve fire-and-forget scheduling and pending retry behavior; no synchronous provider calls added to write paths. |
| T-154-011-04 | Tampering | Embedding dimension extraction | mitigate | Add/retain tests proving LLM embedding purpose dimensions override legacy config and fallback remains 1536. |
| T-154-SC | Tampering | npm installs | accept | No new dependency installs are planned; madge commands are validation-only and include pinned `madge@8.0.0` for the required guard. |
</threat_model>

<verification>
Run focused unit and integration suites listed in task verification.
Run final static and quality gates: `npx --yes madge@8.0.0 src --extensions ts --circular`, `npx --yes madge src --extensions ts --circular`, `npm run typecheck`, `npm run lint`, `npm run knip`, and `npm run build`.
Run `npm run test:macro-framework` if macro-visible native tool or LLM types moved.
</verification>

<success_criteria>
REQ-011 and Phase 154 are complete when the production `src/` graph has zero circular dependencies, the REQ-011 behavior tests and selected reference integration coverage pass or skip only through existing gates, and T-C-007 through T-C-010 pass.
</success_criteria>

<output>
Create `.planning/phases/154-residual-import-cycle-cleanup/154-02-SUMMARY.md` when done.
</output>
