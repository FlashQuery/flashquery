---
phase: 154-residual-import-cycle-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 154-01
files_modified:
  - src/llm/errors.ts
  - src/llm/runtime-types.ts
  - src/llm/client.ts
  - src/llm/resolver.ts
  - tests/unit/llm-client.test.ts
autonomous: true
requirements:
  - REQ-011
must_haves:
  truths:
    - "LLM client and resolver no longer import each other through concrete runtime classes or shared response types."
    - "Model fallback, transient/permanent error classification, 429 retry delay handling, and cost/usage recording remain unchanged."
  artifacts:
    - path: src/llm/errors.ts
      provides: "Leaf LLM error classes shared by client and resolver"
    - path: src/llm/runtime-types.ts
      provides: "Leaf chat/client/result interfaces shared without importing client implementation"
  key_links:
    - from: src/llm/resolver.ts
      to: src/llm/errors.ts
      via: "LlmFallbackError and provider error imports without importing llm/client.ts"
      pattern: "from './errors.js'"
    - from: src/llm/client.ts
      to: src/llm/runtime-types.ts
      via: "shared runtime contract import or re-export"
      pattern: "from './runtime-types.js'"
---

<objective>
Break the REQ-011 LLM client/resolver runtime import cycle by extracting shared runtime contracts and error classes into dependency-light leaves.

Purpose: Let fallback resolution and provider execution share error/type contracts without either module importing the other's concrete implementation.
Output: `src/llm/errors.ts`, `src/llm/runtime-types.ts`, updated client/resolver imports, and focused LLM runtime regression coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md
@.planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md
@AGENTS.md

Downstream implementation agents must read the Phase 154 planning docs first, especially `154-CONTEXT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `154-PATTERNS.md`, before making implementation choices. If questions remain, consult those docs first and escalate only unanswered ambiguity.

Research is intentionally skipped for Phase 154. Do not create or require `154-RESEARCH.md`.

Relevant interfaces and contracts:
- Plan 01 should have moved `FlashQueryConfig` to `src/config/types.ts`.
- `src/llm/client.ts` exports `LlmHttpError`, `LlmNetworkError`, `ChatMessage`, `LlmCompletionResult`, `LlmClient`, `OpenAICompatibleLlmClient`, `NullLlmClient`, and `initLlm`.
- `src/llm/resolver.ts` exports `LlmFallbackError` and `PurposeResolver`; resolver fallback behavior must not import `src/llm/client.ts`.
- `154-PATTERNS.md` identifies `src/llm/errors.ts` and `src/llm/runtime-types.ts` as exact-source-extract leaves.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve LLM runtime fallback and error behavior</name>
  <files>tests/unit/llm-client.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, .planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md, tests/unit/llm-client.test.ts, src/llm/client.ts, src/llm/resolver.ts</read_first>
  <behavior>
    - T-U-036: `PurposeResolver` fallback order, transient/permanent error handling, 429 delay cap, and `LlmFallbackError` attempts remain unchanged.
    - T-U-036: `OpenAICompatibleLlmClient` still records costs and usage metadata as before, including fallback detail.
  </behavior>
  <action>Update `tests/unit/llm-client.test.ts` only where current coverage would not catch moving `LlmHttpError`, `LlmNetworkError`, `LlmFallbackError`, `ChatMessage`, `LlmCompletionResult`, or `LlmClient` into leaf modules. Keep assertions at the public client/resolver behavior level; do not add tests that assert internal file paths except for the no-back-edge grep in Task 2 verification.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-client.test.ts</automated>
  </verify>
  <acceptance_criteria>T-U-036 LLM runtime fallback/error coverage is still meaningful before production imports move; tests may be red until Task 2 but fail for behavior drift rather than implementation details.</acceptance_criteria>
  <done>Runtime behavior expectations are locked before breaking the client/resolver cycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract LLM runtime errors and types</name>
  <files>src/llm/errors.ts, src/llm/runtime-types.ts, src/llm/client.ts, src/llm/resolver.ts, tests/unit/llm-client.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, .planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md, AGENTS.md, src/config/types.ts, src/llm/client.ts, src/llm/resolver.ts, tests/unit/llm-client.test.ts</read_first>
  <behavior>
    - `PurposeResolver` fallback order, transient/permanent error handling, 429 delay cap, and `LlmFallbackError` attempts remain unchanged.
    - `OpenAICompatibleLlmClient` still records costs and usage metadata as before, including fallback detail.
    - Existing public imports from `src/llm/client.ts` continue to work through re-exports if callers currently use that path.
  </behavior>
  <action>Extract `LlmHttpError` and `LlmNetworkError` from `src/llm/client.ts`, and `LlmFallbackError` from `src/llm/resolver.ts`, into `src/llm/errors.ts`. Extract `ChatMessage`, `LlmCompletionResult`, `LlmClient`, and any shared model-selection/result types needed to avoid client implementation imports into `src/llm/runtime-types.ts`. Update `client.ts` and `resolver.ts` to import from the leaves, use `import type` for type-only dependencies, and preserve re-exports from `client.ts` when current callers import those public contracts there. Do not change provider request payloads, fallback selection, cost recording, response envelopes, or retry semantics.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-client.test.ts</automated>
    <automated>rg -n "from './client\\.js'|from '../llm/client\\.js'" src/llm/resolver.ts src/llm/types.ts && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>`src/llm/resolver.ts` has no import from `src/llm/client.ts`; `client.ts` and `resolver.ts` share runtime contracts only through leaf modules; T-U-036 runtime client/resolver tests pass.</acceptance_criteria>
  <done>The client/resolver portion of REQ-011 is behavior-preserving and acyclic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP call_model input -> LLM runtime | User/model messages cross into provider fallback and error classification logic. |
| LLM runtime -> usage persistence | Cost and usage metadata cross into persistent storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-011A-01 | Repudiation | LLM fallback/cost metadata | mitigate | Preserve existing `tests/unit/llm-client.test.ts` coverage for fallback attempts and cost recording. |
| T-154-011A-02 | Denial of Service | 429 retry delay handling | mitigate | Preserve delay cap and transient/permanent classification tests while moving error classes. |
| T-154-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm test -- tests/unit/llm-client.test.ts`.
Run `rg -n "from './client\\.js'|from '../llm/client\\.js'" src/llm/resolver.ts src/llm/types.ts && exit 1 || exit 0` to confirm the resolver back-edge is gone.
Full REQ-011 family and whole-phase madge gates are intentionally deferred to Plan 154-06.
</verification>

<success_criteria>
This REQ-011 slice is complete when client/resolver shared contracts live in dependency-light leaves, resolver no longer imports client, public exports remain compatible, and focused LLM runtime behavior tests pass.
</success_criteria>

<output>
Create `.planning/phases/154-residual-import-cycle-cleanup/154-02-SUMMARY.md` when done.
</output>
