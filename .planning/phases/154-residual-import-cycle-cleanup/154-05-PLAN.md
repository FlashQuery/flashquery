---
phase: 154-residual-import-cycle-cleanup
plan: 05
type: execute
wave: 2
depends_on:
  - 154-01
files_modified:
  - src/embedding/dimensions.ts
  - src/embedding/provider.ts
  - src/embedding/background-embed.ts
  - src/storage/supabase.ts
  - src/logging/logger.ts
  - tests/unit/embedding-provider.test.ts
autonomous: true
requirements:
  - REQ-011
must_haves:
  truths:
    - "Storage and logging modules no longer import concrete embedding provider or config loader implementations for shared policy/types."
    - "Embedding dimension resolution, provider factory behavior, background embedding scheduling, and logger output remain unchanged."
  artifacts:
    - path: src/embedding/dimensions.ts
      provides: "Dependency-light embedding dimension resolver"
    - path: src/logging/logger.ts
      provides: "Logger using leaf config types rather than config loader runtime"
  key_links:
    - from: src/storage/supabase.ts
      to: src/embedding/dimensions.ts
      via: "getEmbeddingDimensions import without importing embedding/provider.ts"
      pattern: "from '../embedding/dimensions.js'"
    - from: src/logging/logger.ts
      to: src/config/types.ts
      via: "FlashQueryConfig type import from Plan 01 leaf"
      pattern: "from '../config/types.js'"
---

<objective>
Break the REQ-011 embedding/storage/logging import cycle slice by extracting embedding dimension policy and replacing config-loader type imports with leaves.

Purpose: Keep storage schema setup and logging behavior stable without depending on concrete provider or config loader implementations.
Output: `src/embedding/dimensions.ts`, updated embedding/storage/logging imports, and focused embedding regression coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md
@.planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md
@AGENTS.md

Downstream implementation agents must read the Phase 154 planning docs first, especially `154-CONTEXT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `154-PATTERNS.md`, before making implementation choices. If questions remain, consult those docs first and escalate only unanswered ambiguity.

Research is intentionally skipped for Phase 154. Do not create or require `154-RESEARCH.md`.

Relevant interfaces and contracts:
- Plan 01 should have moved `FlashQueryConfig` to `src/config/types.ts`.
- `src/embedding/provider.ts` owns provider implementations and `initEmbedding`; `getEmbeddingDimensions` should live in a dependency-light module used by both provider and storage.
- `src/storage/supabase.ts` currently imports embedding dimension policy from concrete provider implementation.
- `src/logging/logger.ts` must preserve log format and credential-safety behavior while importing config types from the leaf.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve embedding and logging behavior</name>
  <files>tests/unit/embedding-provider.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, .planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md, tests/unit/embedding-provider.test.ts, src/embedding/provider.ts, src/embedding/background-embed.ts, src/storage/supabase.ts, src/logging/logger.ts</read_first>
  <behavior>
    - T-U-036: embedding provider factory/dimensions and background embedding scheduling behavior remain unchanged.
    - T-U-036: LLM embedding purpose dimensions override legacy config and fallback remains 1536.
    - Logger output format remains `[YYYY-MM-DD HH:MM:SS] LEVEL  Message` or the existing request-correlated equivalent.
  </behavior>
  <action>Update `tests/unit/embedding-provider.test.ts` only where existing coverage would not detect moving `getEmbeddingDimensions` into a leaf. Add logger assertions only if current tests already cover logger-adjacent behavior in this suite or an existing local test file; do not introduce broad logging test churn in this plan.</action>
  <verify>
    <automated>npm test -- tests/unit/embedding-provider.test.ts</automated>
  </verify>
  <acceptance_criteria>T-U-036 embedding dimension/provider coverage remains meaningful before production imports move; tests may be red until Task 2 but fail for behavior drift rather than file movement alone.</acceptance_criteria>
  <done>Embedding/storage/logging behavior expectations are locked before extracting dimension policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract embedding dimension policy and leaf config imports</name>
  <files>src/embedding/dimensions.ts, src/embedding/provider.ts, src/embedding/background-embed.ts, src/storage/supabase.ts, src/logging/logger.ts, tests/unit/embedding-provider.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, .planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md, AGENTS.md, src/config/types.ts, src/embedding/provider.ts, src/embedding/background-embed.ts, src/storage/supabase.ts, src/logging/logger.ts, tests/unit/embedding-provider.test.ts</read_first>
  <behavior>
    - Embedding provider factory/dimensions, LLM purpose override behavior, and default 1536 fallback remain unchanged.
    - Background embedding scheduling remains fire-and-forget and does not add synchronous provider calls to write paths.
    - Logger output format and credential-safety behavior remain unchanged.
  </behavior>
  <action>Move `getEmbeddingDimensions` and any pure dimension defaults/helpers from `src/embedding/provider.ts` to `src/embedding/dimensions.ts` with type-only dependency on `FlashQueryConfig` from `src/config/types.ts`. Update `provider.ts` and `storage/supabase.ts` to import the helper from the leaf so storage does not import concrete provider implementation. Update `background-embed.ts` only if it imports provider or config types in a way that participates in the REQ-011 cycle; preserve scheduling semantics. Update `logging/logger.ts` to import `FlashQueryConfig` from `src/config/types.ts` or introduce a narrower local logging config type if that better matches existing behavior. Use ESM `.js` specifiers and avoid unrelated Supabase schema, vault, MCP, or provider API changes.</action>
  <verify>
    <automated>npm test -- tests/unit/embedding-provider.test.ts</automated>
    <automated>rg -n "from '../embedding/provider\\.js'|from '../config/loader\\.js'" src/storage/supabase.ts src/logging/logger.ts && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>`src/storage/supabase.ts` no longer imports `src/embedding/provider.ts`; `src/logging/logger.ts` no longer imports `src/config/loader.ts`; T-U-036 embedding tests pass; logger format remains unchanged.</acceptance_criteria>
  <done>The embedding/storage/logging portion of REQ-011 is behavior-preserving and acyclic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedding config -> storage schema | Config-driven embedding dimensions control pgvector DDL and provider setup. |
| Logger -> filesystem/stdout | Runtime events are emitted without exposing credentials or changing MCP stdout expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-011C-01 | Tampering | Embedding dimension extraction | mitigate | Preserve tests proving LLM embedding purpose dimensions override legacy config and fallback remains 1536. |
| T-154-011C-02 | Information Disclosure | Logger config type extraction | mitigate | Preserve logger redaction/output behavior and avoid adding config value logging. |
| T-154-011C-03 | Denial of Service | Background embedding scheduling | mitigate | Preserve fire-and-forget scheduling; no synchronous provider calls added to write paths. |
| T-154-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `npm test -- tests/unit/embedding-provider.test.ts`.
Run `rg -n "from '../embedding/provider\\.js'|from '../config/loader\\.js'" src/storage/supabase.ts src/logging/logger.ts && exit 1 || exit 0`.
Full REQ-011 family and whole-phase madge gates are intentionally deferred to Plan 154-06.
</verification>

<success_criteria>
This REQ-011 slice is complete when storage and logging use dependency-light leaves, embedding behavior remains stable, and focused tests pass.
</success_criteria>

<output>
Create `.planning/phases/154-residual-import-cycle-cleanup/154-05-SUMMARY.md` when done.
</output>
