---
phase: 154-residual-import-cycle-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/request-lifecycle-registry.ts
  - src/mcp/server.ts
  - src/server/shutdown.ts
  - tests/unit/mcp-request-drain.test.ts
  - tests/unit/mcp-server-correlation.test.ts
  - tests/integration/server/shutdown-mcp-drain.test.ts
autonomous: true
requirements:
  - REQ-012
must_haves:
  truths:
    - "`src/mcp/server.ts` and `src/server/shutdown.ts` do not import each other directly or dynamically."
    - "MCP server creation registers lifecycle state, closed sessions unregister and remove lookup state, and shutdown drains registered servers through a dependency-light registry."
    - "Shutdown retains the 15-second drain deadline and count-only timeout warnings for hung MCP handlers."
  artifacts:
    - path: src/mcp/request-lifecycle-registry.ts
      provides: "Dependency-light registry for MCP server lifecycle lookup, active-server listing, and cleanup"
    - path: src/mcp/server.ts
      provides: "MCP server creation wired to registry without importing shutdown coordination"
    - path: src/server/shutdown.ts
      provides: "Shutdown drain using registry without dynamic import of mcp/server.ts"
    - path: tests/integration/server/shutdown-mcp-drain.test.ts
      provides: "T-I-011 drain semantics coverage"
  key_links:
    - from: src/mcp/server.ts
      to: src/mcp/request-lifecycle-registry.ts
      via: "register lifecycle and lookup export"
      pattern: "request-lifecycle-registry"
    - from: src/server/shutdown.ts
      to: src/mcp/request-lifecycle-registry.ts
      via: "active registered server lifecycle drain"
      pattern: "getRegisteredMcp"
    - from: tests/integration/server/shutdown-mcp-drain.test.ts
      to: src/server/shutdown.ts
      via: "MCP_REQUEST_DRAIN_TIMEOUT_MS 15-second behavior"
      pattern: "MCP_REQUEST_DRAIN_TIMEOUT_MS"
---

<objective>
Break the REQ-012 MCP server/shutdown lifecycle import cycle by extracting shared lifecycle registry state into a dependency-light module.

Purpose: Allow MCP server setup and shutdown coordination to share request lifecycle state without direct or dynamic back-edge imports, while retaining the 15-second drain semantics from Phase 148.
Output: Lifecycle registry module, updated server/shutdown wiring, and T-U-037/T-I-011 regression coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md
@.planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md
@AGENTS.md

Downstream implementation agents must read the Phase 154 planning docs first, especially `154-CONTEXT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `154-PATTERNS.md`, before making implementation choices. If questions remain, consult those docs first and escalate only unanswered ambiguity.

Research is intentionally skipped for Phase 154. Do not create or require `154-RESEARCH.md`.

Relevant interfaces and contracts:
- `src/mcp/request-lifecycle.ts` exports `McpRequestLifecycle`, `McpDrainResult`, and `createMcpRequestLifecycle`.
- `src/mcp/server.ts` currently owns a `WeakMap<McpServer, McpRequestLifecycle>`, exports `getMcpRequestLifecycleForServer(server)`, creates a lifecycle in `createMcpServer`, registers the server for shutdown, wraps handlers for correlation/lifecycle tracking, and deletes lifecycle state on transport close.
- `src/server/shutdown.ts` currently owns a `Set<McpServer>`, exports `registerMcpServerForShutdown` and `unregisterMcpServerForShutdown`, and dynamically imports `getMcpRequestLifecycleForServer` from `src/mcp/server.ts` inside `drainMcpRequests`.
- Phase 148 history established the 15-second MCP drain deadline and tests in `tests/unit/mcp-request-drain.test.ts`, `tests/unit/mcp-server-correlation.test.ts`, and `tests/integration/server/shutdown-mcp-drain.test.ts`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lifecycle registry regression coverage</name>
  <files>tests/unit/mcp-request-drain.test.ts, tests/unit/mcp-server-correlation.test.ts, tests/integration/server/shutdown-mcp-drain.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, tests/unit/mcp-request-drain.test.ts, tests/unit/mcp-server-correlation.test.ts, tests/integration/server/shutdown-mcp-drain.test.ts, src/mcp/request-lifecycle.ts, src/mcp/server.ts, src/server/shutdown.ts</read_first>
  <behavior>
    - T-U-037: registering a server lifecycle makes lookup return the same lifecycle object.
    - T-U-037: unregistering or closed-session cleanup removes the server from global shutdown drain registration and removes lookup state.
    - T-U-037: correlation/catalog wrapping behavior in `createMcpServer` remains unchanged.
    - T-I-011: shutdown waits for active handlers, times out with remaining count for hung handlers, and drains with `MCP_REQUEST_DRAIN_TIMEOUT_MS`.
  </behavior>
  <action>Add a narrow unit test for the new registry contract once the registry module exists, or stage the test with imports matching the planned module name `src/mcp/request-lifecycle-registry.ts`. Update existing server correlation tests to continue using public `createMcpServer` and `getMcpRequestLifecycleForServer` behavior rather than reading SDK internals. Keep integration tests focused on `tests/integration/server/shutdown-mcp-drain.test.ts` and preserve fake-timer assertions around `MCP_REQUEST_DRAIN_TIMEOUT_MS`. Do not add web UI, server-side session state, or unrelated transport behavior.</action>
  <verify>
    <automated>npm test -- tests/unit/mcp-request-drain.test.ts tests/unit/mcp-server-correlation.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/server/shutdown-mcp-drain.test.ts</automated>
  </verify>
  <acceptance_criteria>T-U-037 tests prove register, lookup, unregister, and cleanup behavior; T-I-011 still proves 15-second drain semantics and timeout warning behavior; tests may be red before registry implementation but must fail for the current server/shutdown back-edge or missing registry behavior.</acceptance_criteria>
  <done>REQ-012 behavior expectations are locked before changing lifecycle wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract MCP lifecycle registry and remove shutdown back-edge</name>
  <files>src/mcp/request-lifecycle-registry.ts, src/mcp/server.ts, src/server/shutdown.ts, tests/unit/mcp-request-drain.test.ts, tests/unit/mcp-server-correlation.test.ts, tests/integration/server/shutdown-mcp-drain.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, AGENTS.md, src/mcp/request-lifecycle.ts, src/mcp/server.ts, src/server/shutdown.ts, tests/unit/mcp-request-drain.test.ts, tests/unit/mcp-server-correlation.test.ts, tests/integration/server/shutdown-mcp-drain.test.ts</read_first>
  <behavior>
    - `createMcpServer` creates a lifecycle, registers it for shutdown, wraps handlers with lifecycle/correlation tracking, and unregisters/deletes state on transport close exactly as before.
    - `getMcpRequestLifecycleForServer(server)` remains available to tests/callers and preserves the existing uninitialized lifecycle error message unless tests intentionally document a better equivalent.
    - `ShutdownCoordinator.drainMcpRequests()` uses the registry to drain either the explicit `mcpServer` option or all registered servers with the existing 15-second timeout.
  </behavior>
  <action>Create `src/mcp/request-lifecycle-registry.ts` as a dependency-light module importing only `McpServer` type/value as required by the SDK and `McpRequestLifecycle` from `src/mcp/request-lifecycle.ts`. Move the server-to-lifecycle WeakMap, shutdown registered server set, `registerMcpServerForShutdown`, `unregisterMcpServerForShutdown`, `getMcpRequestLifecycleForServer`, and any cleanup/listing helper into this registry. Update `src/mcp/server.ts` to create the lifecycle, register it through the registry, re-export `getMcpRequestLifecycleForServer` if existing tests/imports require that public path, and call registry cleanup on `transport.onclose`. Update `src/server/shutdown.ts` to import registry helpers directly and delete the dynamic `await import('../mcp/server.js')` path. Preserve `MCP_REQUEST_DRAIN_TIMEOUT_MS = 15_000`, count-only timeout warnings, and existing shutdown log text unless tests reveal exact source behavior requires a tiny wording preservation change.</action>
  <verify>
    <automated>npm test -- tests/unit/mcp-request-drain.test.ts tests/unit/mcp-server-correlation.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/server/shutdown-mcp-drain.test.ts</automated>
    <automated>sh -c 'npx --yes madge@8.0.0 src --extensions ts --circular > /tmp/fq-154-mcp-cycle.txt 2>&1 || true; ! rg "mcp/server\\.ts.*server/shutdown\\.ts|server/shutdown\\.ts.*mcp/server\\.ts" /tmp/fq-154-mcp-cycle.txt'</automated>
  </verify>
  <acceptance_criteria>`rg -n "server/shutdown|registerMcpServerForShutdown|unregisterMcpServerForShutdown" src/mcp/server.ts` shows imports only from the new registry or no shutdown import; `rg -n "import\\('../mcp/server\\.js'\\)|from '../mcp/server\\.js'" src/server/shutdown.ts` finds no server back-edge; T-U-037 and T-I-011 pass; targeted pinned madge output has no cycle line containing both `mcp/server.ts` and `server/shutdown.ts`. Full zero-cycle madge is intentionally deferred to Plan 154-06 after REQ-010 and REQ-011 are complete.</acceptance_criteria>
  <done>REQ-012 is complete with lifecycle state shared through a dependency-light registry and shutdown drain behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> registered tool handler | Untrusted tool calls enter tracked handlers and must decrement lifecycle counts in finally blocks. |
| Process signal -> shutdown coordinator | Shutdown drains in-flight request state before process exit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-012-01 | Denial of Service | MCP request drain registry | mitigate | Preserve `waitForIdle(MCP_REQUEST_DRAIN_TIMEOUT_MS)` and timeout metadata; integration test hung handler behavior. |
| T-154-012-02 | Repudiation | Shutdown logging | mitigate | Preserve count-only warnings and existing drain log semantics so shutdown evidence remains observable without leaking request payloads. |
| T-154-012-03 | Tampering | Lifecycle cleanup | mitigate | Unit-test unregister/delete behavior so stale closed sessions are not drained later. |
| T-154-SC | Tampering | npm installs | accept | No new package-manager install is planned; madge is validation-only. |
</threat_model>

<verification>
Run `npm test -- tests/unit/mcp-request-drain.test.ts tests/unit/mcp-server-correlation.test.ts`.
Run `npm run test:integration -- tests/integration/server/shutdown-mcp-drain.test.ts`.
Run `sh -c 'npx --yes madge@8.0.0 src --extensions ts --circular > /tmp/fq-154-mcp-cycle.txt 2>&1 || true; ! rg "mcp/server\\.ts.*server/shutdown\\.ts|server/shutdown\\.ts.*mcp/server\\.ts" /tmp/fq-154-mcp-cycle.txt'` and confirm no cycle contains both `mcp/server.ts` and `server/shutdown.ts`. This targeted REQ-012 assertion intentionally allows unrelated REQ-010 and REQ-011 cycles to remain until their plans complete. Final zero-cycle and project quality gates are owned by Plan 154-06 after all cycle families are complete.
</verification>

<success_criteria>
REQ-012 is complete when `src/mcp/server.ts` and `src/server/shutdown.ts` share lifecycle state only through the registry, T-U-037 and T-I-011 pass, and the pinned madge output has no MCP server/shutdown cycle.
</success_criteria>

<output>
Create `.planning/phases/154-residual-import-cycle-cleanup/154-03-SUMMARY.md` when done.
</output>
