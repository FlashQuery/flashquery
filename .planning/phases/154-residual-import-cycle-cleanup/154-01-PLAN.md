---
phase: 154-residual-import-cycle-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/types.ts
  - src/config/loader.ts
  - src/llm/capabilities.ts
  - src/llm/tool-policy.ts
  - src/llm/tool-registry.ts
  - tests/unit/circular-deps.test.ts
  - tests/unit/llm-config.test.ts
  - tests/unit/llm-tool-registry.test.ts
autonomous: true
requirements:
  - REQ-010
must_haves:
  truths:
    - "Config parsing and validation no longer import concrete LLM runtime, template-tool, or registry modules that import back into config."
    - "LLM admission validation, hard-excluded native-tool behavior, tool-tier expansion, and config runtime metadata accessors behave the same after extraction."
    - "Static cycle tests fail on any remaining madge cycle containing config/loader.ts."
  artifacts:
    - path: src/config/types.ts
      provides: "Leaf FlashQueryConfig type definitions shared by config, LLM, logger, embedding, storage, MCP, and shutdown modules"
    - path: src/llm/tool-policy.ts
      provides: "Dependency-light tool tier and hard-exclusion constants for config-facing validation"
    - path: tests/unit/circular-deps.test.ts
      provides: "T-U-032 config loader circular-dependency guard"
  key_links:
    - from: src/config/loader.ts
      to: src/config/types.ts
      via: "type-only FlashQueryConfig import or local export handoff"
      pattern: "import type .*FlashQueryConfig.*config/types"
    - from: src/config/loader.ts
      to: src/llm/tool-policy.ts
      via: "policy/constants import that does not import config/loader.ts"
      pattern: "from '../llm/tool-policy.js'"
    - from: tests/unit/circular-deps.test.ts
      to: src/config/loader.ts
      via: "T-U-032 forbidden cycle fragment assertion"
      pattern: "config/loader.ts"
---

<objective>
Break the REQ-010 config/LLM policy import cycle family by extracting config-facing types and policy constants into dependency-light leaves.

Purpose: Make `src/config/loader.ts` a config parser/validator that does not depend on concrete LLM runtime, template-tool, or registry modules that can import back into config.
Output: Leaf config/policy modules, updated imports, and focused static/regression tests for T-U-032 and T-U-035.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md
@.planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md
@AGENTS.md

Downstream implementation agents must read the Phase 154 planning docs first, especially `154-CONTEXT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `154-PATTERNS.md`, before making implementation choices. If questions remain, consult those docs first and escalate only unanswered ambiguity.

Research is intentionally skipped for Phase 154. Do not create or require `154-RESEARCH.md`.

Relevant interfaces and contracts:
- `src/config/loader.ts` currently exports `FlashQueryConfig`, `loadConfig`, `resolveConfigPath`, `getDeprecationWarnings`, `getStartupWarnings`, `getResolvedHostToolExposure`, and `getLlmApiKeyRefs`.
- `src/config/loader.ts` currently imports `validateAllPurposeMode2Admissions` from `src/llm/capabilities.ts` and `HARD_EXCLUDED_NATIVE_TOOLS` / `TOOL_TIERS` from `src/llm/tool-registry.ts`; these are the REQ-010 imports to remove or redirect to dependency-light leaves.
- `src/llm/capabilities.ts` exports `modelCapabilitiesWithDefaults`, `buildModelCapabilityDiagnostics`, `validatePurposeMode2Admission`, `validateAllPurposeMode2Admissions`, and `assertResponseFormatAllowedWithTools`.
- `src/llm/tool-registry.ts` exports provider schema assembly behavior, including `assembleNativeToolRegistry`, `toOpenAiToolDefinition`, `mergeModelVisibleToolRegistries`, `TOOL_TIERS`, and `HARD_EXCLUDED_NATIVE_TOOLS`.
- `154-PATTERNS.md` identifies `src/config/types.ts` and `src/llm/tool-policy.ts` as exact-source-extract leaf modules and warns not to broaden scope into repository-wide module policy.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add REQ-010 config cycle and policy regression guards</name>
  <files>tests/unit/circular-deps.test.ts, tests/unit/llm-config.test.ts, tests/unit/llm-tool-registry.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, tests/unit/circular-deps.test.ts, tests/unit/llm-config.test.ts, tests/unit/llm-tool-registry.test.ts, src/config/loader.ts, src/llm/capabilities.ts, src/llm/tool-registry.ts</read_first>
  <behavior>
    - T-U-032: the static guard fails if madge output contains any cycle line involving `config/loader.ts`.
    - T-U-035: config loader tests still prove LLM admission validation, hard-excluded native-tool rejection, tool-tier expansion, and runtime metadata accessors.
    - T-U-035: tool registry tests still prove exact read-only and read-write tier contents and hard-excluded native tool behavior after constants move.
  </behavior>
  <action>Extend `tests/unit/circular-deps.test.ts` with a named T-U-032 assertion using the existing madge runner and line-parser style from `154-PATTERNS.md`; it should identify any cycle line containing `config/loader.ts` and include matching lines in the assertion message. Add or update narrow tests in `tests/unit/llm-config.test.ts` and `tests/unit/llm-tool-registry.test.ts` only where current coverage would not catch policy extraction regressions. Keep T-U-031 final zero-cycle assertion for Plan 154-06; this plan owns only the REQ-010 targeted guard and behavior regression coverage.</action>
  <verify>
    <automated>npm test -- tests/unit/circular-deps.test.ts tests/unit/llm-config.test.ts tests/unit/llm-tool-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>T-U-032 exists by name or explicit requirement label; failure output prints matching `config/loader.ts` cycle lines; T-U-035 behavior remains asserted without relying on web UI, live providers, or Supabase; tests may be red before production changes but must be meaningful and specific.</acceptance_criteria>
  <done>REQ-010 has static and regression tests ready to prove the config cycle cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract dependency-light config and tool policy leaves</name>
  <files>src/config/types.ts, src/config/loader.ts, src/llm/capabilities.ts, src/llm/tool-policy.ts, src/llm/tool-registry.ts, tests/unit/circular-deps.test.ts, tests/unit/llm-config.test.ts, tests/unit/llm-tool-registry.test.ts</files>
  <read_first>.planning/phases/154-residual-import-cycle-cleanup/154-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/154-residual-import-cycle-cleanup/154-PATTERNS.md, AGENTS.md, src/config/loader.ts, src/llm/capabilities.ts, src/llm/tool-registry.ts, src/mcp/tool-metadata.ts, tests/unit/circular-deps.test.ts, tests/unit/llm-config.test.ts, tests/unit/llm-tool-registry.test.ts</read_first>
  <behavior>
    - `loadConfig` parses, normalizes, validates, and records runtime metadata exactly as before.
    - `validateAllPurposeMode2Admissions` and `validatePurposeMode2Admission` keep the same admission results and error text semantics.
    - `TOOL_TIERS`, `HARD_EXCLUDED_NATIVE_TOOLS`, and `ToolTierName` remain available to existing callers without creating a config-loader cycle.
  </behavior>
  <action>Create `src/config/types.ts` by extracting public config type declarations such as `FlashQueryConfig` from `src/config/loader.ts`; keep YAML parsing, Zod schemas, runtime metadata WeakMap, `loadConfig`, and metadata accessors in `loader.ts`. Create `src/llm/tool-policy.ts` or the smallest equivalent leaf for `TOOL_TIERS`, `ToolTierName`, and `HARD_EXCLUDED_NATIVE_TOOLS`; if importing `src/mcp/tool-metadata.ts` from this leaf would create a new config-to-MCP-to-config cycle, split pure selector data from metadata helpers so `config/loader.ts` depends only on the pure leaf. Update `src/config/loader.ts` to import `FlashQueryConfig` from the type leaf and config-facing LLM policy from dependency-light modules; it must not import `src/llm/tool-registry.ts`, `src/llm/template-tools.ts`, `src/llm/client.ts`, or concrete modules that create a runtime path back to loader. Update `src/llm/capabilities.ts` to type-import `FlashQueryConfig` from `src/config/types.ts`, and update `src/llm/tool-registry.ts` to re-export moved policy constants if needed for backward-compatible imports. Preserve ESM `.js` import specifiers and public MCP behavior.</action>
  <verify>
    <automated>npm test -- tests/unit/circular-deps.test.ts tests/unit/llm-config.test.ts tests/unit/llm-tool-registry.test.ts</automated>
    <automated>sh -c 'npx --yes madge@8.0.0 src --extensions ts --circular > /tmp/fq-154-config-cycle.txt 2>&1 || true; ! rg "config/loader\\.ts" /tmp/fq-154-config-cycle.txt'</automated>
  </verify>
  <acceptance_criteria>`rg -n "from '../llm/(tool-registry|template-tools|client)\\.js'|from './(tool-registry|template-tools|client)\\.js'" src/config/loader.ts` finds no concrete LLM runtime/template/registry import; targeted pinned madge output contains no cycle line with `config/loader.ts`; T-U-035 focused tests pass; config runtime metadata accessors continue to work from `src/config/loader.ts`. Full zero-cycle madge is intentionally deferred to Plan 154-06 after REQ-011 and REQ-012 are complete.</acceptance_criteria>
  <done>`src/config/loader.ts` depends only on dependency-light type/policy modules for LLM validation and REQ-010 is behavior-preserving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config -> config loader | Untrusted local config values are parsed and validated before startup behavior. |
| Config policy -> LLM/tool exposure | Tool and purpose validation controls which native/template tools can become model-visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-010-01 | Tampering | `src/config/loader.ts` validation | mitigate | Preserve existing Zod/config validation and add T-U-035 regression tests for LLM admission and tool selector behavior. |
| T-154-010-02 | Elevation of Privilege | `TOOL_TIERS` / `HARD_EXCLUDED_NATIVE_TOOLS` extraction | mitigate | Keep exact allowlists/exclusions covered by `tests/unit/llm-tool-registry.test.ts`; do not infer tiers dynamically. |
| T-154-SC | Tampering | npm installs | accept | No new package-manager install is planned; madge is invoked with pinned `npx --yes madge@8.0.0` as an existing validation command. |
</threat_model>

<verification>
Run `npm test -- tests/unit/circular-deps.test.ts tests/unit/llm-config.test.ts tests/unit/llm-tool-registry.test.ts`.
Run `sh -c 'npx --yes madge@8.0.0 src --extensions ts --circular > /tmp/fq-154-config-cycle.txt 2>&1 || true; ! rg "config/loader\\.ts" /tmp/fq-154-config-cycle.txt'` and confirm no `config/loader.ts` cycle lines remain. This targeted REQ-010 assertion intentionally allows unrelated REQ-011 and REQ-012 cycles to remain until their plans complete. Final zero-cycle, typecheck, lint, knip, build, and parity madge gates are owned by Plan 154-06 after all cycle families are complete.
</verification>

<success_criteria>
REQ-010 is complete when `src/config/loader.ts` imports no concrete LLM runtime/template/registry module that imports back into config, T-U-032 and T-U-035 pass, and targeted pinned madge output has no cycle containing `config/loader.ts`.
</success_criteria>

<output>
Create `.planning/phases/154-residual-import-cycle-cleanup/154-01-SUMMARY.md` when done.
</output>
