---
phase: 147-tooling-and-dependency-hygiene
plan: 2
type: execute
wave: 2
depends_on:
  - 147-01
files_modified:
  - package.json
  - package-lock.json
  - knip.ts
  - tests/unit/knip-config.test.ts
autonomous: true
requirements:
  - REQ-007
must_haves:
  truths:
    - "A developer can run `npm run knip` from the repo root"
    - "Knip excludes required worktree, vendor, and build noise"
    - "Preflight runs Knip directly or through a documented staged script"
  artifacts:
    - path: "knip.ts"
      provides: "Typed ESM Knip configuration and reachability policy"
      exports: ["default"]
    - path: "package.json"
      provides: "`knip` script and preflight integration or staged script"
    - path: "tests/unit/knip-config.test.ts"
      provides: "T-U-015 static assertion for required Knip exclusions"
  key_links:
    - from: "package.json"
      to: "knip.ts"
      via: "`npm run knip`"
      pattern: "\"knip\""
    - from: "tests/unit/knip-config.test.ts"
      to: "knip.ts"
      via: "config import or exact static string assertions"
      pattern: "\\.claude/worktrees/\\*\\*|src/node_modules/\\*\\*|src/dist/\\*\\*"
---

<objective>
Add a usable Knip baseline for REQ-007, including project-specific exclusions, a package script, static coverage, and preflight wiring or an explicit staged rollout.

Purpose: Make dependency/export reachability analysis actionable without worktree, build, or vendor noise.
Output: `knip.ts`, `npm run knip`, T-U-015 coverage, and preflight integration.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read these first, before implementation choices. If these source docs disagree with this plan, the source docs win; ask the user only for ambiguity still unresolved after reading them:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/147-tooling-and-dependency-hygiene/147-CONTEXT.md
@.planning/phases/147-tooling-and-dependency-hygiene/147-RESEARCH.md
@.planning/phases/147-tooling-and-dependency-hygiene/147-PATTERNS.md
@.planning/phases/147-tooling-and-dependency-hygiene/147-VALIDATION.md
@AGENTS.md
@package.json
@tests/config/vitest.preflight.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add T-U-015 Knip exclusion assertion</name>
  <files>tests/unit/knip-config.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md
    - .planning/phases/147-tooling-and-dependency-hygiene/147-PATTERNS.md
    - tests/unit/no-hardcoded-extensions.test.ts
    - tests/unit/config-template.test.ts
  </read_first>
  <behavior>
    - Test 1: `knip.ts` exposes or contains `.claude/worktrees/**`, `src/node_modules/**`, and `src/dist/**`.
    - Test 2: the test failure message names the missing glob so Knip policy regressions are actionable.
  </behavior>
  <action>Create `tests/unit/knip-config.test.ts` following the local Vitest static assertion pattern. The test may import `knip.ts` if practical, or read it as UTF-8 text and assert exact required glob strings. Write the test before creating `knip.ts`; the first focused run should fail because the config does not exist yet.</action>
  <acceptance_criteria>
    - The test file contains `T-U-015` in the test name or assertion description.
    - The test checks all three required globs exactly: `.claude/worktrees/**`, `src/node_modules/**`, `src/dist/**`.
    - The test does not require network access or mutate package metadata.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/knip-config.test.ts</automated>
  </verify>
  <done>T-U-015 exists and fails until the Knip config is added, then passes after Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed Knip config and package script</name>
  <files>knip.ts, package.json, package-lock.json</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Test Plan.md
    - .planning/phases/147-tooling-and-dependency-hygiene/147-RESEARCH.md
    - .planning/phases/147-tooling-and-dependency-hygiene/147-PATTERNS.md
    - package.json
    - package-lock.json
  </read_first>
  <action>Install approved dev dependency `knip` with npm. Create `knip.ts` as an ESM TypeScript default export, preferably typed with `KnipConfig`, with a policy comment stating whether the baseline is production-surface-only or includes test entrypoints. Configure `entry`, `project`, and `ignore` so `.claude/worktrees/**`, `src/node_modules/**`, `src/dist/**`, and generated build output such as `dist/**` are excluded. Add a root `package.json` script named `knip`, using `knip --production` if the policy is production-only. Do not add broad ignore-all settings that hide actionable output.</action>
  <acceptance_criteria>
    - `package.json` has a `knip` script.
    - `package.json` has `knip` in `devDependencies`.
    - `knip.ts` documents the reachability policy in a source comment.
    - `knip.ts` contains required ignores for `.claude/worktrees/**`, `src/node_modules/**`, and `src/dist/**`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/knip-config.test.ts && npm run knip</automated>
  </verify>
  <done>`npm run knip` executes with a project-specific baseline and T-U-015 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Knip into preflight or staged preflight script</name>
  <files>package.json, .planning/phases/147-tooling-and-dependency-hygiene/147-dependency-baseline.md</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Test Plan.md
    - .planning/phases/147-tooling-and-dependency-hygiene/147-CONTEXT.md
    - .planning/phases/147-tooling-and-dependency-hygiene/147-VALIDATION.md
    - package.json
  </read_first>
  <action>Add Knip to the `preflight` chain directly after `typecheck` through `npm run knip`, or add `preflight:knip` and call it from `preflight`. If `npm run knip` has intentional false positives that make full preflight gating unsuitable, keep the exact staged script in `package.json` and append the false-positive gate rationale to `147-dependency-baseline.md`, naming each ignored export/dependency and why it is intentional per REQ-007.</action>
  <acceptance_criteria>
    - `package.json` preflight references `knip` or `preflight:knip`.
    - If staged rollout is used, `147-dependency-baseline.md` contains `Knip staged rollout` and exact false-positive rationale.
    - `npm run knip` output is actionable and does not report `.claude/worktrees`, `src/node_modules`, or `src/dist` paths.
  </acceptance_criteria>
  <verify>
    <automated>npm run knip && npm run typecheck && npm run lint</automated>
  </verify>
  <done>REQ-007 local script and preflight policy are implemented with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> static analyzer | Knip consumes project files and package scripts to decide reachability. |
| package scripts -> preflight | New analyzer command becomes part of developer release hygiene. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-KNIP-01 | Denial of Service | `npm run knip` | mitigate | Required ignores exclude worktrees, vendor folders, and build output so analysis stays actionable. |
| T-147-KNIP-02 | Repudiation | Knip staged rollout rationale | mitigate | Any deferred preflight gate must be documented with exact false positives in `147-dependency-baseline.md`. |
| T-147-SC | Tampering | `knip` npm install | mitigate | RESEARCH.md Package Legitimacy Audit marks `knip` OK; install with npm and commit the generated lockfile. |
</threat_model>

<verification>
Nyquist validation source: `.planning/phases/147-tooling-and-dependency-hygiene/147-VALIDATION.md`, rows 147-03-01 and 147-03-02.
Run `npm test -- --run tests/unit/knip-config.test.ts`, `npm run knip`, `npm run typecheck`, and `npm run lint`.
</verification>

<success_criteria>
REQ-007 is satisfied: Knip has a script/config, required exclusions, static assertion coverage, and preflight integration or an explicit staged gate.
</success_criteria>

<output>
Create `.planning/phases/147-tooling-and-dependency-hygiene/147-02-SUMMARY.md` when done.
</output>
