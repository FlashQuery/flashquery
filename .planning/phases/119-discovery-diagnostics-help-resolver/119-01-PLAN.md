---
phase: 119-discovery-diagnostics-help-resolver
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/llm-tool.test.ts
  - tests/unit/llm-template-tools.test.ts
  - tests/unit/llm-tool-registry.test.ts
  - tests/scenarios/directed/testcases/test_call_model_help_resolver.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - VAL-119
must_haves:
  truths:
    - "Focused tests fail first for the missing help resolver contract."
    - "Focused tests pin discovery diagnostics for purposes, models, and search metadata before implementation."
    - "A directed public help scenario skeleton exists and is ready to prove MCP behavior after implementation."
  artifacts:
    - path: "tests/unit/llm-tool.test.ts"
      provides: "RED contracts for help, resolver enum, no-envelope discovery behavior, capability diagnostics, and search metadata"
    - path: "tests/unit/llm-template-tools.test.ts"
      provides: "Template diagnostics contract coverage reused by list_purposes discovery"
    - path: "tests/unit/llm-tool-registry.test.ts"
      provides: "Native registry diagnostics contract coverage reused by list_purposes discovery"
    - path: "tests/scenarios/directed/testcases/test_call_model_help_resolver.py"
      provides: "Public directed scenario skeleton for resolver='help'"
  key_links:
    - from: "tests/unit/llm-tool.test.ts"
      to: "src/mcp/tools/llm.ts"
      via: "registered call_model handler invocation"
      pattern: "resolver.*help"
    - from: "tests/unit/llm-tool.test.ts"
      to: "src/llm/capabilities.ts"
      via: "unknown-vs-false capability diagnostic assertions"
      pattern: "unknown declaration|declared unsupported"
    - from: "tests/scenarios/directed/testcases/test_call_model_help_resolver.py"
      to: "call_model"
      via: "managed MCP client call"
      pattern: "resolver.*help"
---

<objective>
Create the Phase 119 RED validation surface before implementation.

Purpose: DISC-01 through DISC-04 are public JSON contracts. The first plan pins those contracts with failing unit and directed-scenario coverage so Plan 02 can implement to exact behavior instead of inferring shapes.
Output: Focused RED tests and a directed help scenario skeleton for VAL-119.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/119-discovery-diagnostics-help-resolver/119-CONTEXT.md
@.planning/phases/119-discovery-diagnostics-help-resolver/119-RESEARCH.md
@.planning/phases/119-discovery-diagnostics-help-resolver/119-VALIDATION.md
@.planning/phases/119-discovery-diagnostics-help-resolver/119-PATTERNS.md
@.planning/phases/118-template-discovery-masquerade-dispatch/118-05-SUMMARY.md
@.planning/phases/117-agent-loop-executor/117-05-SUMMARY.md
@.planning/phases/116-model-visible-tool-registry/116-VALIDATION.md

<interfaces>
From `src/mcp/tools/llm.ts`:
```typescript
export const callModelMessageSchema = z.discriminatedUnion('role', [...]);
export function hasModelVisibleTools(toolRegistry: ToolRegistryAssembly | undefined): boolean;
export function registerLlmTools(server: McpServer, config: FlashQueryConfig): void;
```

From `src/llm/capabilities.ts`:
```typescript
export type StructuredModelCapabilities = {
  tool_calling?: boolean;
  usage_on_tool_calls?: boolean;
  strict_tools?: boolean;
  parallel_tool_calls?: boolean;
  structured_outputs_with_tools?: boolean;
};
export function modelCapabilitiesWithDefaults(model: Pick<LlmModel, 'capabilities'>, provider: LlmProvider): StructuredModelCapabilities;
```

From `src/llm/template-tools.ts`:
```typescript
export interface TemplateToolDiagnostics {
  template_tools: Array<{ name: string; template_path: string; description: string; parameters: Record<string, unknown> }>;
  template_tool_warnings: Array<{ template_path: string; code: string; message: string; source?: string }>;
  dangling_template_paths: Array<{ template_path: string; source?: string }>;
  template_tool_conflicts: Array<{ name: string; template_paths: string[]; sources: Array<{ kind: 'template' | 'native'; template_path?: string; name?: string }> }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED help resolver and discovery drift contracts</name>
  <files>tests/unit/llm-tool.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md`
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md`
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md`
    - `.planning/phases/119-discovery-diagnostics-help-resolver/119-PATTERNS.md`
    - `tests/unit/llm-tool.test.ts`
    - Analog: `tests/unit/llm-tool.test.ts` existing discovery resolver and ATL-U-15 blocks.
    - Target: `src/mcp/tools/llm.ts`
  </read_first>
  <behavior>
    - RED test: `resolver: "help"` is accepted without `name`, `messages`, or configured LLM client.
    - RED test: help response is raw JSON, not a `CallModelEnvelope`, and `Object.keys(parsed)` exactly equals `["summary","reference_syntax","template_bindings","modes","envelope","errors","discovery","examples"]`.
    - RED test: help discovery lists every supported resolver value: `model`, `purpose`, `list_models`, `list_purposes`, `search`, `help`.
    - RED test: discovery resolvers ignore `return_messages`, `name`, and `messages` and do not include envelope-only keys such as `metadata`, `usage`, or `messages`.
    - RED test: `resolver: "search"` with query terms `tool_calling`, `usage_on_tool_calls`, `template_tools`, `template_tool_conflicts`, `dangling_template_paths`, and `help` returns raw JSON with `results.models` and `results.purposes` arrays present.
  </behavior>
  <action>Add failing tests for DISC-03, DISC-04, and VAL-119 in the existing `llm-tool.test.ts` discovery area. Assert exact top-level help key order, raw JSON/no-envelope behavior, resolver-list drift protection, and search metadata terms. Do not implement production behavior in this task.</action>
  <acceptance_criteria>
    - `rg -n "resolver: ['\\\"]help['\\\"]|Object\\.keys\\(.*help|reference_syntax|template_bindings|usage_on_tool_calls|dangling_template_paths" tests/unit/llm-tool.test.ts` prints matches.
    - `npm test -- tests/unit/llm-tool.test.ts` fails because production code does not yet accept or build the new help/search diagnostics contract.
    - The new tests contain no `.skip`, `.todo`, or `it.only`; verify with `rg -n "\\.(skip|todo|only)\\(" tests/unit/llm-tool.test.ts` and inspect that no new match was introduced.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts</automated>
  </verify>
  <done>RED contract tests exist for help, resolver drift, raw discovery behavior, and enriched search metadata; the focused command fails for missing implementation only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED diagnostics contracts for purposes and models</name>
  <files>tests/unit/llm-tool.test.ts, tests/unit/llm-template-tools.test.ts, tests/unit/llm-tool-registry.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md`
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md`
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md`
    - `.planning/phases/119-discovery-diagnostics-help-resolver/119-PATTERNS.md`
    - `tests/unit/llm-tool.test.ts`
    - `tests/unit/llm-template-tools.test.ts`
    - `tests/unit/llm-tool-registry.test.ts`
    - Analog: `tests/unit/llm-template-tools.test.ts` collision/warning diagnostics tests.
    - Analog: `tests/unit/llm-tool-registry.test.ts` hard-excluded/native diagnostics tests.
    - Target: `src/llm/capabilities.ts`, `src/llm/template-tools.ts`, `src/llm/tool-registry.ts`, `src/mcp/tools/llm.ts`
  </read_first>
  <behavior>
    - RED test: `list_purposes` includes `native_tools`, `native_tool_diagnostics`, `template_tools`, `template_tool_warnings`, `template_tool_conflicts`, and `dangling_template_paths` on every purpose, using empty arrays when no diagnostics exist.
    - RED test: configured purpose native tool declarations expose usable tool names before invoking `resolver: "purpose"`.
    - RED test: `list_models` includes `capability_diagnostics` entries that distinguish `state: "unknown_declaration"` from `state: "declared_unsupported"`.
    - RED test: unknown capability diagnostics include remediation text containing `capabilities.<key>: true|false`; false diagnostics do not tell the user to declare a missing key.
  </behavior>
  <action>Add failing unit tests that pin DISC-01 and DISC-02 without changing production code. Keep assertions machine-readable and additive: existing model/purpose discovery fields must still be asserted where the current tests already cover them.</action>
  <acceptance_criteria>
    - `rg -n "native_tools|native_tool_diagnostics|capability_diagnostics|unknown_declaration|declared_unsupported|template_tool_warnings" tests/unit/llm-tool.test.ts tests/unit/llm-template-tools.test.ts tests/unit/llm-tool-registry.test.ts` prints matches.
    - `npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-template-tools.test.ts tests/unit/llm-tool-registry.test.ts` fails only on the newly pinned missing discovery diagnostic behavior.
    - Existing template and native registry tests still compile; no imports use CommonJS `require`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-template-tools.test.ts tests/unit/llm-tool-registry.test.ts</automated>
  </verify>
  <done>RED diagnostics contracts cover list_purposes native/template arrays and list_models unknown-vs-false capability explanations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create directed help scenario skeleton</name>
  <files>tests/scenarios/directed/testcases/test_call_model_help_resolver.py</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md`
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md`
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md`
    - `.planning/phases/119-discovery-diagnostics-help-resolver/119-PATTERNS.md`
    - `tests/scenarios/directed/testcases/test_discovery_resolvers.py`
    - `tests/scenarios/directed/testcases/test_call_model_template_discovery.py`
    - `tests/scenarios/directed/testcases/test_call_model_template_tool_conflicts.py`
    - Analog: `tests/scenarios/directed/testcases/test_discovery_resolvers.py` managed server and MCP client pattern.
    - Target: `tests/scenarios/directed/testcases/test_call_model_help_resolver.py`
  </read_first>
  <action>Create a new directed scenario file for ATL-DS-15 that calls `call_model` with `resolver: "help"` through the managed MCP fixture. The scenario should initially fail until Plan 02 implements help. It must assert raw JSON, exact top-level keys, documented resolver values, and absence of `metadata`, `usage`, and `messages` envelope keys.</action>
  <acceptance_criteria>
    - `test_call_model_help_resolver.py` exists and imports the same managed scenario helpers used by `test_discovery_resolvers.py`.
    - `rg -n "ATL-DS-15|resolver.*help|reference_syntax|template_bindings|CallModelEnvelope|metadata|usage" tests/scenarios/directed/testcases/test_call_model_help_resolver.py` prints matches.
    - `python3 tests/scenarios/directed/testcases/test_call_model_help_resolver.py --managed` fails because the production resolver does not yet exist.
  </acceptance_criteria>
  <verify>
    <automated>python3 tests/scenarios/directed/testcases/test_call_model_help_resolver.py --managed</automated>
  </verify>
  <done>Directed help scenario skeleton exists with public MCP assertions and is RED for missing implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client to `call_model` discovery resolver | Untrusted params request diagnostic/help JSON without `messages`. |
| Test fixtures to managed FlashQuery process | Directed scenarios observe public MCP behavior and must not rely on private implementation internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Information Disclosure | `resolver: "help"` tests | mitigate | Pin raw protocol help only; tests must assert no document body, template body, prompt, `metadata`, `usage`, or `messages` envelope payload is returned. |
| T-119-02 | Tampering | Discovery diagnostics tests | mitigate | RED tests require additive fields and empty arrays instead of renaming or dropping existing public discovery fields. |
| T-119-03 | Repudiation | Validation evidence | mitigate | Task verification commands are focused and recorded in the summary with failing RED evidence before implementation. |
</threat_model>

<verification>
Expected Wave 0 state is RED:
- `npm test -- tests/unit/llm-tool.test.ts` fails on missing help/discovery contract.
- `npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-template-tools.test.ts tests/unit/llm-tool-registry.test.ts` fails on missing diagnostic fields.
- `python3 tests/scenarios/directed/testcases/test_call_model_help_resolver.py --managed` fails on missing public help behavior.
</verification>

<success_criteria>
Plan 01 succeeds when all RED tests and scenario skeletons exist, are specific to DISC-01 through DISC-04 and VAL-119, and fail for missing implementation rather than syntax/import errors.
</success_criteria>

<output>
After completion, create `.planning/phases/119-discovery-diagnostics-help-resolver/119-01-SUMMARY.md`.
</output>
