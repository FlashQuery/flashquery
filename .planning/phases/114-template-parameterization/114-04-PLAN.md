---
phase: 114-template-parameterization
plan: 04
type: execute
wave: 4
depends_on:
  - 114-03
files_modified:
  - tests/integration/reference-resolver.integration.test.ts
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - TMPL-05
  - VAL-114
must_haves:
  truths:
    - "Real vault templates with frontmatter render through resolver integration tests."
    - "Real document params use Supabase-backed document identity resolution."
    - "Plain documents ignore template_params in a Supabase-backed test."
    - "Alias `_template` and `_items` work against real vault files."
  artifacts:
    - path: "tests/integration/reference-resolver.integration.test.ts"
      provides: "Supabase-backed template resolver integration coverage"
      contains: "ATL-I-04"
  key_links:
    - from: "tests/integration/reference-resolver.integration.test.ts"
      to: "src/llm/reference-resolver.ts"
      via: "resolveReferences"
      pattern: "template_params"
---

<objective>
Add Supabase-backed integration coverage for resolver template behavior.

Purpose: Prove Phase 114 works with real vault markdown, frontmatter parsing, DB document rows, and identifier resolution.
Output: Integration tests extending the Phase 113 reference resolver integration suite.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/114-template-parameterization/114-RESEARCH.md
@.planning/phases/114-template-parameterization/114-PATTERNS.md
@.planning/phases/114-template-parameterization/114-VALIDATION.md
@.planning/phases/113-document-reference-system-core/113-04-SUMMARY.md
@tests/integration/reference-resolver.integration.test.ts
@src/llm/reference-resolver.ts

<interfaces>
Existing integration helper:
```typescript
async function seedDocument(vaultPath: string, relPath: string, title: string, body: string, extra: Record<string, unknown> = {}): Promise<string>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real-vault template and document-param integration tests</name>
  <files>tests/integration/reference-resolver.integration.test.ts</files>
  <read_first>
    - tests/integration/reference-resolver.integration.test.ts
    - .planning/phases/114-template-parameterization/114-PATTERNS.md section "tests/integration/reference-resolver.integration.test.ts"
    - .planning/phases/114-template-parameterization/114-VALIDATION.md "Per-Task Verification Map"
  </read_first>
  <behavior>
    - Test [I-TMPL-01]: real vault document with `fq_template: true` and `fq_params` renders path-keyed string params.
    - Test [I-TMPL-02]: real plain document containing `{{name}}` ignores supplied `template_params`.
    - Test [I-TMPL-03]: real template with `type: "document"` param resolves a seeded document by path and by `fq_id`.
    - Test [I-TMPL-04]: missing document param returns `template_param_doc_not_found`.
  </behavior>
  <action>
    Extend the existing `describe.skipIf(!HAS_SUPABASE)` suite. Use `seedDocument` with extra frontmatter `{ fq_template: true, fq_params: { name: { type: "string", required: true }, source: { type: "document", required: true } } }`. Call `resolveReferences(parsed, config, supabaseManager, embeddingProvider, logger, templateParams)` directly. Use `hydrateMessages` and `buildInjectedReferences` to assert rendered body and metadata. Do not add a new database setup path; reuse existing `beforeAll`/`afterAll` cleanup.
  </action>
  <verify>
    <automated>npm run test:integration -- tests/integration/reference-resolver.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\[I-TMPL-0(1|2|3|4)\\]" tests/integration/reference-resolver.integration.test.ts` returns four IDs.
    - `rg -n "fq_template|fq_params|template_param_doc_not_found|template_params" tests/integration/reference-resolver.integration.test.ts` finds all integration contract strings.
    - Integration command passes or skips only through the existing `HAS_SUPABASE` guard.
  </acceptance_criteria>
  <done>Real-vault template rendering and document-param resolution are covered in integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real-vault alias and _items integration tests</name>
  <files>tests/integration/reference-resolver.integration.test.ts</files>
  <read_first>
    - tests/integration/reference-resolver.integration.test.ts
    - .planning/phases/114-template-parameterization/114-PATTERNS.md sections "Resolver integration assertion pattern" and "Shared Patterns"
    - tests/unit/reference-resolver.test.ts
  </read_first>
  <behavior>
    - Test [I-TMPL-05]: two aliases render the same real template path with different parameter values.
    - Test [I-TMPL-06]: `_items` injects seeded documents/templates in caller order with `_separator`.
    - Test [I-TMPL-07]: `_items` failed entry returns `multi_ref_item_failed` with alias and zero-based index in detail.
  </behavior>
  <action>
    Add integration cases using `{{ref:@first}}`, `{{ref:@second}}`, and `{{ref:@background}}` parsed references. Set `templateParams.first._template` and `templateParams.second._template` to the same template path with different values. For `_items`, include at least one string document path and one object `{ _template: "Templates/item.md", label: "..." }`; assert hydrated content order and separator text exactly. Assert metadata for the list alias includes `resolved_to_count: 2` and `items` in the same order.
  </action>
  <verify>
    <automated>npm run test:integration -- tests/integration/reference-resolver.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\[I-TMPL-0(5|6|7)\\]" tests/integration/reference-resolver.integration.test.ts` returns three IDs.
    - `rg -n "_template|_items|_separator|resolved_to_count|multi_ref_item_failed" tests/integration/reference-resolver.integration.test.ts` finds all alias/list strings.
    - `npm run test:integration -- tests/integration/reference-resolver.integration.test.ts` passes or skips only through the existing `HAS_SUPABASE` guard.
  </acceptance_criteria>
  <done>Real-vault alias and ordered list injection behavior is integration-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test MCP-like params -> real vault | Integration inputs exercise real filesystem and Supabase rows. |
| Alias/list values -> resolver | `_template` and `_items` entries select multiple documents/templates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-13 | T/I | Real vault document identifiers | mitigate | Integration tests must prove template and document params use seeded vault-relative paths and fq_id through existing Supabase-backed resolver. |
| T-114-14 | I | Ambiguous/missing document params | mitigate | Integration failures assert typed `template_param_doc_not_found`/`multi_ref_item_failed` rather than silently choosing content. |
| T-114-15 | T | Reserved `_items` controls | mitigate | Integration metadata asserts reserved fields are controls and do not leak as rendered params. |
| T-114-16 | D | Integration environment | accept | Existing `HAS_SUPABASE` guard skips when `.env.test` is incomplete; phase gate still runs the command and records skip/pass. |
</threat_model>

<verification>
Run `npm run test:integration -- tests/integration/reference-resolver.integration.test.ts`.
</verification>

<success_criteria>
Supabase-backed integration tests validate all Phase 114 template, document param, alias, and `_items` resolver behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/114-template-parameterization/114-04-SUMMARY.md`.
</output>
