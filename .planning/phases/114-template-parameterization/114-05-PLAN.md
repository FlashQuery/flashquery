---
phase: 114-template-parameterization
plan: 05
type: execute
wave: 5
depends_on:
  - 114-04
files_modified:
  - tests/scenarios/directed/testcases/test_call_model_template_parameterization.py
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/114-template-parameterization/114-VALIDATION.md
autonomous: false
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - TMPL-05
  - VAL-114
must_haves:
  truths:
    - "Public call_model scenario proves parameterized templates render through the MCP surface."
    - "Public call_model scenario proves template failures abort before provider dispatch."
    - "Coverage ledgers and requirement statuses identify Phase 114 runnable validation."
    - "Documentation review checkpoint records whether user-facing docs are intentionally deferred or updated."
  artifacts:
    - path: "tests/scenarios/directed/testcases/test_call_model_template_parameterization.py"
      provides: "Managed public call_model template parameterization scenario"
      contains: "test_call_model_template_parameterization"
    - path: "tests/scenarios/directed/DIRECTED_COVERAGE.md"
      provides: "Directed coverage traceability rows for template behavior"
      contains: "test_call_model_template_parameterization"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TMPL-01..TMPL-05 and VAL-114 status closure"
      contains: "**VAL-114**"
  key_links:
    - from: "tests/scenarios/directed/testcases/test_call_model_template_parameterization.py"
      to: "src/mcp/tools/llm.ts"
      via: "public call_model MCP request"
      pattern: "template_params"
    - from: ".planning/REQUIREMENTS.md"
      to: "tests/scenarios/directed/testcases/test_call_model_template_parameterization.py"
      via: "traceability status"
      pattern: "VAL-114"
---

<objective>
Close Phase 114 through public scenario validation, coverage traceability, and docs review.

Purpose: Prove user-observable `call_model.template_params` behavior and leave the milestone ledgers ready for Phase 115.
Output: Managed directed scenario, coverage updates, requirements/roadmap closure, and a documentation review checkpoint.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/114-template-parameterization/114-RESEARCH.md
@.planning/phases/114-template-parameterization/114-PATTERNS.md
@.planning/phases/114-template-parameterization/114-VALIDATION.md
@.planning/phases/113-document-reference-system-core/113-04-SUMMARY.md
@tests/scenarios/directed/testcases/test_call_model_reference_system_core.py
@tests/scenarios/directed/DIRECTED_COVERAGE.md
@tests/scenarios/integration/INTEGRATION_COVERAGE.md

<interfaces>
Directed scenario command:
```bash
python3 tests/scenarios/directed/run_suite.py --managed test_call_model_template_parameterization
```

Phase gate:
```bash
npm run build && npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-tool.test.ts && npm run test:integration -- tests/integration/reference-resolver.integration.test.ts && python3 tests/scenarios/directed/run_suite.py --managed test_call_model_template_parameterization
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add managed public call_model template scenario</name>
  <files>tests/scenarios/directed/testcases/test_call_model_template_parameterization.py</files>
  <read_first>
    - tests/scenarios/directed/testcases/test_call_model_reference_system_core.py
    - .planning/phases/114-template-parameterization/114-PATTERNS.md section "tests/scenarios/directed/testcases/test_call_model_template_parameterization.py"
    - .planning/phases/114-template-parameterization/114-VALIDATION.md "Full suite command"
  </read_first>
  <behavior>
    - Step ATL-DS-04: path-keyed template params render through public `call_model` and returned `metadata.injected_references[]` includes template metadata.
    - Step ATL-DS-05: alias `_template` renders the same template twice with different values and `_items` injects an ordered list with separator text.
    - Step ATL-DS-06: missing required param, invalid document param, and invalid `_items` value fail with typed reasons and do not increment mock provider call count.
    - Step ATL-DS-07: plain document ignores `template_params`, and substituted value containing `{{ref:missing.md}}` remains literal with no recursive failure.
  </behavior>
  <action>
    Create `TEST_NAME = "test_call_model_template_parameterization"` using the Phase 113 managed scenario structure. Reuse the local OpenAI-compatible mock provider. Add `_write_doc`, `_call(client, content, **extra)`, and `_json(result)` helpers. Write vault files with frontmatter keys `fq_template`, `fq_params`, and `fq_status: active`. Call `call_model` with public `template_params={...}`. Assert successful responses include rendered text, `return_messages` hydrated content, and metadata keys `template`, `template_path`, `template_params_used`, `resolved_to_count`, and `items`. For failure checks, record `before_fail_calls = mock_provider.call_count`, assert `not invalid.ok`, JSON `error == "reference_resolution_failed"`, reasons include `template_missing_required_param`, `template_param_doc_not_found`, and `multi_ref_invalid_value`, and assert provider call count is unchanged.
  </action>
  <verify>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed test_call_model_template_parameterization</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/scenarios/directed/testcases/test_call_model_template_parameterization.py`.
    - `rg -n "TEST_NAME = \"test_call_model_template_parameterization\"|template_params|fq_template|fq_params|template_missing_required_param|template_param_doc_not_found|multi_ref_invalid_value" tests/scenarios/directed/testcases/test_call_model_template_parameterization.py` finds all listed strings.
    - Directed command passes 1/1 suite with all scenario steps passing.
  </acceptance_criteria>
  <done>Public MCP behavior for template params, aliases, `_items`, non-recursion, and fail-fast errors is covered by a managed scenario.</done>
</task>

<task type="auto">
  <name>Task 2: Update coverage ledgers and phase traceability</name>
  <files>tests/scenarios/directed/DIRECTED_COVERAGE.md, tests/scenarios/integration/INTEGRATION_COVERAGE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/114-template-parameterization/114-VALIDATION.md</files>
  <read_first>
    - tests/scenarios/directed/DIRECTED_COVERAGE.md
    - tests/scenarios/integration/INTEGRATION_COVERAGE.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/114-template-parameterization/114-VALIDATION.md
    - .agents/skills/document-maintenance/SKILL.md
  </read_first>
  <action>
    Register the new public scenario in `DIRECTED_COVERAGE.md` using existing table style with coverage text for parameterized templates, alias `_template`, `_items`, typed template failures, and non-recursive plain/template behavior. Register integration coverage in `INTEGRATION_COVERAGE.md` for real-vault document params and list injection using `tests/integration/reference-resolver.integration.test.ts`. Mark TMPL-01, TMPL-02, TMPL-03, TMPL-04, TMPL-05, and VAL-114 complete in `.planning/REQUIREMENTS.md` only after the full phase gate passes. Update `.planning/ROADMAP.md` Phase 114 from `Plans: TBD` to `Plans: 5 plans` with plan list entries `114-01-PLAN.md` through `114-05-PLAN.md`; after execution, mark Phase 114 complete only if build, unit, integration, and directed commands passed or integration skipped through `HAS_SUPABASE`. Update `114-VALIDATION.md` task statuses and approval to reflect executed commands.
  </action>
  <verify>
    <automated>npm run build && npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-tool.test.ts && npm run test:integration -- tests/integration/reference-resolver.integration.test.ts && python3 tests/scenarios/directed/run_suite.py --managed test_call_model_template_parameterization</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_call_model_template_parameterization|template_params|_items" tests/scenarios/directed/DIRECTED_COVERAGE.md tests/scenarios/integration/INTEGRATION_COVERAGE.md` finds coverage rows.
    - `rg -n "\\[x\\] \\*\\*(TMPL-01|TMPL-02|TMPL-03|TMPL-04|TMPL-05|VAL-114)\\*\\*" .planning/REQUIREMENTS.md` finds all six completed requirements after full gate success.
    - `rg -n "114-01-PLAN.md|114-02-PLAN.md|114-03-PLAN.md|114-04-PLAN.md|114-05-PLAN.md|\\*\\*Plans:\\*\\* 5 plans" .planning/ROADMAP.md` finds the finalized Phase 114 plan list.
    - Full phase gate command succeeds, with any Supabase skip explicitly coming from the existing integration guard.
  </acceptance_criteria>
  <done>Coverage ledgers, requirements, roadmap, and validation strategy reflect Phase 114 delivered behavior and passing commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Documentation review checkpoint</name>
  <files>README.md, docs/ARCHITECTURE.md, docs/**/*, .planning/phases/114-template-parameterization/114-05-SUMMARY.md</files>
  <read_first>
    - .agents/skills/document-maintenance/SKILL.md
    - .agents/skills/document-maintenance/rules/readme-review.md
    - .agents/skills/document-maintenance/rules/architecture-review.md
    - README.md
    - docs/ARCHITECTURE.md
  </read_first>
  <what-built>Phase 114 added user-facing `call_model.template_params` behavior for parameterized vault templates, aliases, document params, and `_items` list injection.</what-built>
  <how-to-verify>
    1. Apply the README and ARCHITECTURE.md review rules from `.agents/skills/document-maintenance`.
    2. Confirm whether user-facing template docs are intentionally deferred to Phase 119 help/discovery, or update the relevant docs now if they already describe `call_model` references.
    3. Record the decision in `114-05-SUMMARY.md` with exact wording: `Documentation review: updated` or `Documentation review: deferred to Phase 119`.
  </how-to-verify>
  <action>
    Perform the document-maintenance review after all automated Phase 114 validation commands pass. If README or architecture docs already document `call_model` references, update them with concrete public fields `template_params`, `fq_template`, `fq_params`, `_template`, `_items`, and `_separator`. If docs coverage is intentionally owned by Phase 119 help/discovery, do not edit docs; record `Documentation review: deferred to Phase 119` in the summary and cite `.planning/ROADMAP.md` Phase 119 as the owner.
  </action>
  <resume-signal>Type `docs verified` after recording the docs decision, or describe the required docs edits.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/114-template-parameterization/114-05-SUMMARY.md && rg -n "Documentation review: (updated|deferred to Phase 119)" .planning/phases/114-template-parameterization/114-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary contains exactly one of `Documentation review: updated` or `Documentation review: deferred to Phase 119`.
    - If docs were updated, `git diff --name-only` includes the changed docs.
    - If docs were deferred, Phase 119 remains the documented owner of help/discovery behavior in `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>Docs review is explicitly resolved before Phase 114 is marked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public MCP caller -> FQC subprocess | Directed scenario sends untrusted `template_params` through the public tool surface. |
| Vault template files -> model prompt | Scenario proves markdown/frontmatter controls prompt content. |
| Coverage/requirements ledgers -> phase status | Completion metadata controls downstream milestone decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-17 | T/I | Public `template_params` | mitigate | Directed scenario asserts path-keyed and alias-keyed params hydrate only intended seeded documents/templates. |
| T-114-18 | T | Recursive prompt injection | mitigate | Directed scenario asserts `{{ref:missing.md}}` introduced by substituted values remains literal and does not create failures/metadata. |
| T-114-19 | I | Ambiguous/wrong document params | mitigate | Public failure scenario asserts invalid document params fail before provider dispatch with `template_param_doc_not_found`. |
| T-114-20 | T/E | Reserved field collision | mitigate | Public scenario and metadata assertions cover `_template`, `_items`, and `_separator` as controls, not rendered user params. |
| T-114-21 | R | Traceability status | mitigate | Requirements and roadmap are updated only after full gate success; exact commands are recorded in validation and summary. |
</threat_model>

<verification>
Full phase gate: `npm run build && npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-tool.test.ts && npm run test:integration -- tests/integration/reference-resolver.integration.test.ts && python3 tests/scenarios/directed/run_suite.py --managed test_call_model_template_parameterization`.
</verification>

<success_criteria>
Phase 114 public behavior is validated, all six requirement IDs are marked complete only after tests pass, coverage ledgers include the new behavior, and documentation review is resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/114-template-parameterization/114-05-SUMMARY.md`.
</output>
