---
phase: 159-lock-timeout-canonical-key-derivation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/document-lock.ts
  - tests/unit/lock-key-derivation.test.ts
  - tests/unit/lock-helper-only.test.ts
autonomous: true
requirements:
  - REQ-003
must_haves:
  truths:
    - "Existing-file aliases through symlink and dot segments resolve to one file lock identity"
    - "Missing destination files derive their file lock from real parent path plus basename"
    - "Case-insensitive vault filesystems fold case before file lock hashing"
    - "File and directory lock identities use separate namespaces"
  artifacts:
    - path: "src/services/document-lock.ts"
      provides: "Canonical file and directory lock-entry derivation before Tier 1/Tier 2 acquisition"
      contains: "file:"
    - path: "tests/unit/lock-key-derivation.test.ts"
      provides: "T-U-006 through T-U-010 canonical-key coverage"
      contains: "T-U-006"
  key_links:
    - from: "src/services/document-lock.ts"
      to: "withDocumentLock / withDocumentLocks"
      via: "canonical entries are derived before stripe, burst, sort, and advisory key generation"
      pattern: "await.*uniqueSortedEntries|derive.*Lock"
    - from: "tests/unit/lock-key-derivation.test.ts"
      to: "src/services/document-lock.ts"
      via: "test-only derivation helper or public facade fake PoolClient assertions"
      pattern: "canonical-key|case-fold|symlink"
---

<objective>
Implement REQ-003 full canonical key derivation inside the document lock service.

Purpose: Ensure every lock participant derives the same path-based file or directory identity before in-process striping, burst grouping, sorted multi-lock ordering, and advisory hashing.
Output: Canonical derivation code plus unit coverage for Test Plan §4.1.3 `T-U-006` through `T-U-010`.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-CONTEXT.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-RESEARCH.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-VALIDATION.md
@.planning/phases/159-lock-timeout-canonical-key-derivation/159-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@.planning/phases/158-tier-2-lock-table-retirement-session-check/158-01-SUMMARY.md
@src/services/document-lock.ts
@tests/unit/document-lock-tier2.test.ts
@tests/unit/with-document-lock.test.ts
@tests/unit/lock-helper-only.test.ts

Before changing code or tests, downstream implementation agents MUST read:
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md`
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md`
Use those docs first for all requirements and test-plan questions. Ask the user only if those docs do not answer the question or conflict with current repo reality.

<interfaces>
From `src/services/document-lock.ts`:
- `export class LockTimeoutError extends Error`
- `export async function withDocumentLock<T>(config: FlashQueryConfig, filePath: string, fn: () => Promise<T>): Promise<T>`
- `export async function withDocumentLocks<T>(config: FlashQueryConfig, filePaths: string[], fn: () => Promise<T>): Promise<T>`
- Current internal `toPhase155BasicKey()` and `toEntry()` are Phase 155 scaffolding and must be replaced per D-02.
- Current `runWithTier2()` hashes `entry.resource`; this plan changes the resource from `document:<basicKey>` to `file:<canonicalPath>` or `dir:<canonicalPath>`.
</interfaces>
</context>

<source_audit>
GOAL: Canonical file/directory identity is covered by Task 1 and Task 2.
REQ-003: Covered by Task 1 RED tests and Task 2 implementation.
RESEARCH: Canonical realpath, parent-realpath destination, case probe, namespace, and no raw vault-relative keys are covered.
CONTEXT: D-01 and D-02 are implemented here. Deferred folder shared/exclusive acquisition, destination acquisition semantics, version tokens, and multi-file batch contracts are not implemented here because CONTEXT explicitly defers them to later phases.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical key RED coverage for T-U-006 through T-U-010</name>
  <files>tests/unit/lock-key-derivation.test.ts, tests/unit/lock-helper-only.test.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/159-lock-timeout-canonical-key-derivation/159-CONTEXT.md
    @.planning/phases/159-lock-timeout-canonical-key-derivation/159-PATTERNS.md
    @src/services/document-lock.ts
    @tests/unit/with-document-lock.test.ts
    @tests/unit/lock-helper-only.test.ts
  </read_first>
  <behavior>
    - Test `T-U-006 canonical-key symlink`: two paths resolving through an actual symlink to the same existing file produce identical canonical lock resources and advisory keys.
    - Test `T-U-007 canonical-key destination`: a missing file path derives from `realpath(parent)` plus `basename`, not `realpath(file)`.
    - Test `T-U-008 case-fold`: with the case-sensitivity probe forced to case-insensitive, case variants produce identical file resources; with case-sensitive, no folding is applied.
    - Test `T-U-009 namespace`: `file:<path>` and `dir:<path>` for the same resolved path hash to different advisory bigint keys.
    - Test `T-U-010 vault-relative`: relative input is resolved against `config.instance.vault.path` and raw relative strings never appear in `basicKey`, `resource`, or advisory-key input.
  </behavior>
  <action>Create `tests/unit/lock-key-derivation.test.ts` with isolated temp vault setup using `mkdtemp`, `symlink`, `mkdir`, `writeFile`, and cleanup via `rm`. Use a narrow test-only export from `document-lock.ts` if needed, named under `__testing` or another explicit non-production namespace; update `tests/unit/lock-helper-only.test.ts` so it still forbids lower-level acquire/release primitive exports while permitting the chosen test-only derivation hook. Reference D-01, D-02, and test IDs `T-U-006` through `T-U-010` in test names.</action>
  <verify>
    <automated>npm test -- tests/unit/lock-key-derivation.test.ts --testNamePattern "T-U-006|T-U-007|T-U-008|T-U-009|T-U-010|canonical-key|case-fold|symlink"</automated>
  </verify>
  <acceptance_criteria>
    The new tests exist, include all required test IDs, and fail before the production implementation because current code uses `path.normalize`, `document:` resources, and absolute-only Phase 155 keying.
  </acceptance_criteria>
  <done>`tests/unit/lock-key-derivation.test.ts` proves the required REQ-003 behaviors are specified before implementation, and `lock-helper-only` still blocks accidental public low-level lock primitive exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace Phase 155 basic keys with canonical file and directory lock entries</name>
  <files>src/services/document-lock.ts, tests/unit/lock-key-derivation.test.ts, tests/unit/lock-helper-only.test.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    @.planning/phases/158-tier-2-lock-table-retirement-session-check/158-01-SUMMARY.md
    @.planning/phases/159-lock-timeout-canonical-key-derivation/159-RESEARCH.md
    @src/services/document-lock.ts
  </read_first>
  <action>In `src/services/document-lock.ts`, replace `toPhase155BasicKey`, `toEntry`, and synchronous `uniqueSortedEntries` with async canonical derivation per D-02. Accept relative or absolute paths by resolving relative paths against `config.instance.vault.path`. For file keys, use `realpath(file)` when the file exists and `realpath(dirname(file)) + path.sep + basename(file)` when it does not. For directory keys, use `realpath(dir)`. Add a one-time per-vault case-sensitivity probe that creates and removes a temp probe under the vault root, caches by resolved vault root, and case-folds only when the probe says the filesystem is case-insensitive. Prefix canonical resource strings with `file:` or `dir:` before hashing. Keep `withDocumentLock` and `withDocumentLocks` as the only production facades; expose only narrow test-only derivation helpers if Task 1 used them. Preserve de-dupe by canonical key, sorted acquisition order, active burst grouping, and held-lock AsyncLocalStorage semantics using canonical keys.</action>
  <verify>
    <automated>npm test -- tests/unit/lock-key-derivation.test.ts tests/unit/document-lock-tier1.test.ts tests/unit/document-lock-tier2.test.ts tests/unit/with-document-lock.test.ts tests/unit/lock-helper-only.test.ts --testNamePattern "canonical-key|case-fold|symlink|T-U-006|T-U-007|T-U-008|T-U-009|T-U-010|T-U-016|T-U-017"</automated>
  </verify>
  <acceptance_criteria>
    `document-lock.ts` no longer contains `toPhase155BasicKey` or `resource: \`document:`. Canonical entries are derived before Tier 1 stripe calculation, `toBurstKey`, sorting, and `toAdvisoryKey`. Relative inputs are never hashed directly.
  </acceptance_criteria>
  <done>All REQ-003 unit tests pass, existing Tier 1/Tier 2 facade tests still pass, and directory resource derivation with a `dir:` namespace exists for Phase 160 consumers without adding shared/exclusive acquisition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller to lock helper | Untrusted relative or absolute path input reaches canonical key derivation through document tools. |
| Filesystem to lock identity | Symlinks, case behavior, and missing destinations affect the canonical resource hashed for mutual exclusion. |
| Lock helper to Postgres | Canonical resources become parameterized bigint advisory-lock keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Tampering | `src/services/document-lock.ts` path canonicalization | mitigate | Resolve relative paths against `config.instance.vault.path`, use `realpath`, and never hash raw vault-relative input. |
| T-159-02 | Spoofing | Symlink/case aliases | mitigate | Unit tests `T-U-006` and `T-U-008` prove aliases map to one file resource where filesystem semantics require it. |
| T-159-03 | Tampering | File vs directory identity | mitigate | Prefix advisory hash input with `file:` or `dir:` and prove namespace separation with `T-U-009`. |
| T-159-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned; existing dependencies only. |
</threat_model>

<verification>
Run the plan task commands plus `npm run typecheck` if TypeScript signatures changed.
</verification>

<success_criteria>
REQ-003 acceptance criteria 1-5 are implemented in `document-lock.ts`; `T-U-006` through `T-U-010` are present and green; Phase 159 deferred items are not implemented.
</success_criteria>

<output>
Create `.planning/phases/159-lock-timeout-canonical-key-derivation/159-01-SUMMARY.md` when done.
</output>
