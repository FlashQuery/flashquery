---
phase: 158-tier-2-lock-table-retirement-session-check
plan: 02
type: execute
wave: 2
depends_on:
  - 158-01
files_modified:
  - src/services/write-lock.ts
  - src/storage/supabase.ts
  - src/storage/schema-verify.ts
  - src/config/types.ts
  - src/config/loader.ts
  - src/index.ts
  - src/cli/commands/unlock.ts
  - flashquery.yml
  - flashquery.test.yml
  - tests/unit/no-legacy-write-lock-imports.test.ts
  - tests/unit/schema-verify.test.ts
  - tests/unit/config-loader.test.ts
  - tests/integration/fqc-write-locks-drop.integration.test.ts
autonomous: true
requirements:
  - REQ-004
must_haves:
  truths:
    - "FlashQuery startup drops the obsolete fqc_write_locks table if it exists."
    - "No production source imports or calls the old write-lock service."
    - "The flashquery unlock command is absent from the CLI surface."
    - "Legacy locking.ttl_seconds remains config-load compatible and emits one deprecation warning."
  artifacts:
    - path: "src/storage/supabase.ts"
      provides: "One-way legacy fqc_write_locks drop and no recreate DDL"
    - path: "src/storage/schema-verify.ts"
      provides: "Schema verification without fqc_write_locks as a required table"
    - path: "tests/unit/no-legacy-write-lock-imports.test.ts"
      provides: "T-U-011 static legacy-reference guard"
    - path: "tests/integration/fqc-write-locks-drop.integration.test.ts"
      provides: "T-I-005 and T-I-006 startup retirement coverage"
  key_links:
    - from: "src/storage/supabase.ts"
      to: "src/storage/schema-verify.ts"
      via: "initSupabase then verifySchema"
      pattern: "verifySchema"
    - from: "src/config/loader.ts"
      to: "src/index.ts"
      via: "getDeprecationWarnings startup logging"
      pattern: "getDeprecationWarnings"
---

<objective>
Retire the legacy `fqc_write_locks` table, service, config TTL semantics, and manual unlock CLI for REQ-004.

Purpose: once REQ-002 uses native advisory locks, stale table locks and manual recovery must disappear without breaking existing config files that still contain `locking.ttl_seconds`.
Output: legacy service/CLI/schema removed, startup drop added, config compatibility preserved, and T-U-011/T-I-005/T-I-006 implemented.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-tier-2-lock-table-retirement-session-check/158-CONTEXT.md
@.planning/phases/158-tier-2-lock-table-retirement-session-check/158-RESEARCH.md
@.planning/phases/158-tier-2-lock-table-retirement-session-check/158-VALIDATION.md
@.planning/phases/158-tier-2-lock-table-retirement-session-check/158-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@.planning/phases/157-records-memory-plugins-audit-guards/157-VERIFICATION.md
@src/storage/supabase.ts
@src/storage/schema-verify.ts
@src/config/loader.ts
@src/config/types.ts
@src/index.ts

<interfaces>
From `src/storage/supabase.ts`: `initSupabase(config)` owns DDL execution and final schema verification logging.

From `src/storage/schema-verify.ts`: `verifySchema(client)` checks required tables and columns after DDL. Remove only the obsolete `fqc_write_locks` requirement.

From `src/config/loader.ts`: `loadConfig`, `getDeprecationWarnings(config)`, and `getStartupWarnings(config)` feed startup warnings in `src/index.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add legacy-retirement guards and integration tests</name>
  <files>tests/unit/no-legacy-write-lock-imports.test.ts, tests/unit/schema-verify.test.ts, tests/unit/config-loader.test.ts, tests/integration/fqc-write-locks-drop.integration.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    - .planning/phases/158-tier-2-lock-table-retirement-session-check/158-CONTEXT.md
    - .planning/phases/158-tier-2-lock-table-retirement-session-check/158-VALIDATION.md
    - .planning/phases/158-tier-2-lock-table-retirement-session-check/158-PATTERNS.md
    - src/storage/supabase.ts
    - src/storage/schema-verify.ts
    - src/config/loader.ts
    - src/index.ts
    - tests/unit/lock-helper-only.test.ts
    - tests/integration/archive-document-lock.test.ts
  </read_first>
  <behavior>
    - T-U-011: static guard fails when production `src/` contains `from '.*/services/write-lock'`, `acquireLock`, `releaseLock`, `isLocked`, `flashquery unlock`, or unauthorized `fqc_write_locks` references.
    - T-I-005: startup against a DB containing `fqc_write_locks` drops the table; a follow-up select reports relation does not exist.
    - T-I-006: config with `locking.ttl_seconds: 30` loads successfully and startup emits one deprecation warning.
  </behavior>
  <action>Create the T-U-011 static test and T-I-005/T-I-006 integration tests per D-03 and D-05. Keep the static guard precise: allow explicit occurrences in this test file, product/planning docs, and the production one-way drop statement only if the implementation still needs the literal. Avoid a bare `grep -c`; if shelling out in tests, filter comments or parse files in TypeScript so explanatory text does not self-fail.</action>
  <verify>
    <automated>npm test -- tests/unit/no-legacy-write-lock-imports.test.ts tests/unit/schema-verify.test.ts tests/unit/config-loader.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/fqc-write-locks-drop.integration.test.ts</automated>
    <traceability>Required evidence string preserved for product docs: npm test -- --grep "advisory-lock|lock-startup|legacy-write-lock"</traceability>
    <traceability>Executable fallback: npm test -- tests/unit/no-legacy-write-lock-imports.test.ts --testNamePattern "legacy-write-lock"</traceability>
    <traceability>Required integration evidence string preserved for product docs: npm run test:integration -- --grep "two-tier|fqc-write-locks-drop|lock-startup|session-capable"</traceability>
    <traceability>Executable fallback: npm run test:integration -- tests/integration/fqc-write-locks-drop.integration.test.ts</traceability>
  </verify>
  <acceptance_criteria>
    - Test IDs T-U-011, T-I-005, and T-I-006 appear in test names or comments.
    - Static guard fails before deleting/unregistering the legacy service and CLI.
    - Integration test creates or verifies a legacy table explicitly, then proves startup removes it.
    - Config compatibility test proves `ttl_seconds` is accepted but not present as effective TTL behavior.
  </acceptance_criteria>
  <done>REQ-004 test coverage exists for static legacy removal, drop-on-startup behavior, and legacy TTL config deprecation compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove legacy lock table, service, CLI, and TTL behavior</name>
  <files>src/services/write-lock.ts, src/storage/supabase.ts, src/storage/schema-verify.ts, src/config/types.ts, src/config/loader.ts, src/index.ts, src/cli/commands/unlock.ts, flashquery.yml, flashquery.test.yml</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    - .planning/phases/158-tier-2-lock-table-retirement-session-check/158-CONTEXT.md
    - .planning/phases/158-tier-2-lock-table-retirement-session-check/158-RESEARCH.md
    - .planning/phases/158-tier-2-lock-table-retirement-session-check/158-PATTERNS.md
    - src/services/write-lock.ts
    - src/storage/supabase.ts
    - src/storage/schema-verify.ts
    - src/config/types.ts
    - src/config/loader.ts
    - src/index.ts
    - src/cli/commands/unlock.ts
    - flashquery.yml
    - flashquery.test.yml
  </read_first>
  <action>Implement D-03 exactly. Remove `src/services/write-lock.ts` and the `unlockCommand` import/registration from `src/index.ts`; delete `src/cli/commands/unlock.ts` or leave no CLI-exported command if deletion requires test updates. Remove `fqc_write_locks` create/recreate/index DDL from `src/storage/supabase.ts`, remove it from `verifySchema` required tables and schema verification logs, and add a startup DDL step that runs `DROP TABLE IF EXISTS fqc_write_locks` with a single debug-level log line. Update `LockingSchema` and `FlashQueryConfig` so effective config no longer exposes `ttlSeconds`, while raw `locking.ttl_seconds` remains parse-compatible and contributes one `getDeprecationWarnings(config)` warning. Update `flashquery.yml` and `flashquery.test.yml` comments to remove TTL semantics; do not add Phase 159 `lock_timeout_seconds` behavior beyond documentation if already present in source docs.</action>
  <verify>
    <automated>npm test -- tests/unit/no-legacy-write-lock-imports.test.ts tests/unit/schema-verify.test.ts tests/unit/config-loader.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/fqc-write-locks-drop.integration.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
    <traceability>Required evidence string preserved for product docs: npm test -- --grep "advisory-lock|lock-startup|legacy-write-lock"</traceability>
    <traceability>Executable fallback: npm test -- tests/unit/no-legacy-write-lock-imports.test.ts tests/unit/schema-verify.test.ts tests/unit/config-loader.test.ts --testNamePattern "legacy-write-lock|ttl_seconds"</traceability>
  </verify>
  <acceptance_criteria>
    - `src/services/write-lock.ts` and `src/cli/commands/unlock.ts` are deleted or unreachable from production exports and CLI registration.
    - `src/index.ts` no longer imports or registers `unlockCommand`.
    - `src/storage/supabase.ts` contains no create/index/recreate DDL for `fqc_write_locks`; only the one-way drop statement may remain.
    - `src/storage/schema-verify.ts` no longer requires or logs `fqc_write_locks`.
    - Existing YAML with `locking.ttl_seconds` loads successfully and emits exactly one deprecation warning through the existing startup-warning path.
    - No production source uses `ttlSeconds`, `acquireLock`, `releaseLock`, `isLocked`, or manual table lock semantics.
  </acceptance_criteria>
  <done>Legacy lock-table and manual unlock behavior are fully retired while old config files remain load-compatible with a deprecation warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| startup -> database schema | Startup DDL modifies/removes legacy database objects. |
| user config -> startup warnings | YAML may contain obsolete lock settings that must not re-enable stale lock behavior. |
| CLI user -> command dispatch | Removed manual unlock command must not leave an accidental destructive table mutation path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-04 | Tampering | `DROP TABLE IF EXISTS fqc_write_locks` | mitigate | Use a constant table name only; do not interpolate user input into DDL. |
| T-158-05 | Repudiation | legacy table removal logging | mitigate | Emit one debug-level startup line when the retirement drop runs. |
| T-158-06 | Denial of Service | stale lock table behavior | mitigate | Remove service/CLI/table recreation and add T-U-011 static guard so stale rows cannot block future writes. |
| T-158-07 | Information Disclosure | config deprecation logging | mitigate | Warn only about the key name; do not log database URLs or secret values. |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; only source/tests/config/docs change. |
</threat_model>

<verification>
Overall checks for this plan:
- `npm test -- tests/unit/no-legacy-write-lock-imports.test.ts tests/unit/schema-verify.test.ts tests/unit/config-loader.test.ts`
- `npm run test:integration -- tests/integration/fqc-write-locks-drop.integration.test.ts`
- `npm run typecheck`
- `npm run build`
- Preserve required product evidence strings and document executable fallbacks using `--testNamePattern` or direct file targets.
</verification>

<success_criteria>
REQ-004 is complete when `fqc_write_locks` is no longer created, required, or used; startup drops the obsolete table if present; `flashquery unlock` is unavailable; old `locking.ttl_seconds` config remains accepted with one deprecation warning; and T-U-011/T-I-005/T-I-006 pass.
</success_criteria>

<output>
Create `.planning/phases/158-tier-2-lock-table-retirement-session-check/158-02-SUMMARY.md` when done.
</output>
