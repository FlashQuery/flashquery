---
phase: 133-standard-library-builtins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/macro/evaluator.ts
  - src/macro/preflight.ts
  - tests/unit/macro-preflight.test.ts
  - tests/unit/macro-test-helpers.ts
autonomous: true
requirements:
  - MACRO-SRC-07
  - MACRO-SRC-08
  - MACRO-BI-04
must_haves:
  truths:
    - "Missing required input_var keys are reported together before any macro statement executes."
    - "Explicit null input_vars are treated as present values, not missing values."
    - "input_var defaults support string, number, null, list, and object literals."
  artifacts:
    - path: "src/macro/preflight.ts"
      provides: "collectInputVarContract, validateInputVars, and MacroPreflightError"
      exports: ["collectInputVarContract", "validateInputVars", "MacroPreflightError"]
    - path: "src/macro/evaluator.ts"
      provides: "extension of the Phase 132 preflight execution hook, named builtin args, and standard builtin dispatch contract"
      contains: ["MacroNamedArgs", "collectInputVarContract", "validateInputVars"]
    - path: "tests/unit/macro-preflight.test.ts"
      provides: "T-U-097 through T-U-108 input_var coverage"
      contains: ["T-U-097", "T-U-108"]
  key_links:
    - from: "src/macro/evaluator.ts"
      to: "src/macro/preflight.ts"
      via: "pre-exec collectInputVarContract and validateInputVars"
      pattern: "collectInputVarContract\\(program\\)"
    - from: "src/macro/evaluator.ts"
      to: "MacroBuiltin"
      via: "positional plus named args dispatch"
      pattern: "builtin\\(positional, named, context\\)"
---

<objective>
Add the evaluator contract changes and input preflight needed before any standard builtin can run safely.

Purpose: Phase 133 requires `input_var` declarations to be scanned before execution and named args to be available for `input_var --default` and later `status --progress/--total`. Phase 132 already added an evaluator preflight pass for `exit` arity; Phase 133 must extend that existing pre-execution path rather than introducing a second independent preflight traversal.
Output: `src/macro/preflight.ts`, evaluator preflight wiring, named-arg builtin dispatch, and T-U-097 through T-U-108 coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/133-standard-library-builtins/133-CONTEXT.md
@.planning/phases/133-standard-library-builtins/133-RESEARCH.md
@.planning/phases/133-standard-library-builtins/133-VALIDATION.md
@.planning/phases/133-standard-library-builtins/133-PATTERNS.md
@.planning/phases/132-evaluator-core/132-01-SUMMARY.md
@.planning/phases/132-evaluator-core/132-02-SUMMARY.md
@.planning/phases/132-evaluator-core/132-03-SUMMARY.md
@.planning/phases/132-evaluator-core/132-04-SUMMARY.md

<interfaces>
From `src/macro/evaluator.ts` before this plan:
```typescript
export type MacroBuiltin = (
  args: MacroValue[],
  context: MacroInvocationContext
) => MacroValue | Promise<MacroValue>;

export interface EvaluateProgramOptions {
  builtins?: Record<string, MacroBuiltin>;
  inputVars?: Record<string, MacroValue>;
  input_vars?: Record<string, MacroValue>;
}
```

Create this contract:
```typescript
export type MacroNamedArgs = Record<string, MacroValue>;
export type MacroBuiltin = (
  positional: MacroValue[],
  named: MacroNamedArgs,
  context: MacroInvocationContext
) => MacroValue | Promise<MacroValue>;
```

From `src/macro/types.ts`:
```typescript
export interface Call {
  kind: 'Call';
  name: string;
  args: Arg[];
  line: number;
}
export type Arg = NamedArg | PositionalArg;
export interface NamedArg { kind: 'NamedArg'; name: string; value: Expr; rawShortFlag?: string; }
export interface PositionalArg { kind: 'PositionalArg'; value: Expr; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add input_var preflight contract tests</name>
  <files>tests/unit/macro-preflight.test.ts, tests/unit/macro-test-helpers.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/builtins.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
    - src/macro/evaluator.ts
    - src/macro/types.ts
    - src/macro/parser.ts
    - tests/unit/macro-test-helpers.ts
  </read_first>
  <behavior>
    - T-U-097: `x = input_var "x"\nexit $x` with `inputVars: { x: 5 }` returns result `5`.
    - T-U-098: `inputVars: {}` for `input_var "x"` returns expected `invalid_input` with `details.missing_inputs: ["x"]` and `isError: false`.
    - T-U-099: two missing required declarations return both keys in `missing_inputs`.
    - T-U-100/T-U-101: `--default 5` applies only when the key is absent; explicit `null` wins.
    - T-U-102/T-U-103: non-literal key and boolean default are rejected before macro execution.
    - T-U-104/T-U-105: list and object defaults bind correctly.
    - T-U-106: undeclared extra input_vars are accepted.
    - T-U-107/T-U-108: list input_vars iterate in `for`; nested object input_vars are accessible by field.
  </behavior>
  <action>Create `tests/unit/macro-preflight.test.ts` with Vitest cases named with exact IDs T-U-097 through T-U-108. Use `parseProgram`, `parseToolPayload`, and `resultOf`. Update `tests/unit/macro-test-helpers.ts` so `basicBuiltins` callbacks compile with the new `(positional, named, context)` signature; keep the helper small and do not hide production behavior behind test-only builtin implementations for `input_var`.</action>
  <verify>
    <automated>npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-preflight.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "T-U-097|T-U-098|T-U-099|T-U-100|T-U-101|T-U-102|T-U-103|T-U-104|T-U-105|T-U-106|T-U-107|T-U-108" tests/unit/macro-preflight.test.ts` shows every ID.
    - Focused Vitest command initially fails before implementation and passes after Task 2.
    - Tests assert `error: "invalid_input"` and `details.missing_inputs` for missing input cases.
    - Tests assert no `echo`/trace output happens before missing-input failure by checking returned payload `trace` is absent or empty for preflight failure.
  </acceptance_criteria>
  <done>Input preflight behavior is fully specified by failing tests for T-U-097 through T-U-108.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement preflight and named-arg builtin dispatch</name>
  <files>src/macro/preflight.ts, src/macro/evaluator.ts, tests/unit/macro-preflight.test.ts, tests/unit/macro-test-helpers.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/builtins.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
    - src/macro/evaluator.ts
    - src/macro/types.ts
    - src/macro/parser.ts
    - tests/unit/macro-test-helpers.ts
    - tests/unit/macro-preflight.test.ts
  </read_first>
  <action>Create `src/macro/preflight.ts` exporting `InputVarContract`, `InputVarDefault`, `MacroPreflightError`, `collectInputVarContract(program: Program)`, and `validateInputVars(contract, inputVars)`. `collectInputVarContract` must walk `Binding`, `Pipeline`, `ToolCall`, `ToolExistsCall`, `ForLoop`, `WhileLoop`, `IfStmt`, and every nested expression kind currently in `src/macro/types.ts`. For `input_var`, require first positional arg kind `StringLit`; otherwise throw `new MacroPreflightError("invalid_input", "input_var first argument must be a string literal.", { reason: "input_var_key_must_be_literal", line })`. Treat calls with a named arg `default` as optional and convert only literal defaults (`StringLit`, `NumLit`, `NullLit`, `ListLit`, `ObjectLit`) to `MacroValue`; reject `Call` defaults named `true` or `false` with `{ reason: "input_var_default_must_be_literal" }`. `validateInputVars` must use `Object.prototype.hasOwnProperty.call` so `null` is present, and must report `{ required_inputs, optional_inputs, provided_inputs, missing_inputs }` for all missing keys at once. In `src/macro/evaluator.ts`, extend the existing Phase 132 `preflightProgram(program)` flow so `exit` arity and input-var validation run in the same pre-execution path after `createInvocationContext` and before `execBlock`; map `MacroPreflightError` through `jsonExpectedError`; keep the existing T-U-091 `exit_argument_count` preflight behavior intact. Add `MacroNamedArgs`, update `MacroBuiltin` to `(positional, named, context)`, add `evalCallArgs` that evaluates positional and named args in source order, pass named args into builtins, and add temporary evaluator-owned `input_var` handling only if `standardBuiltins` from Plan 02 is not imported yet. Do not implement shell verbs, tool dispatch permissions, dry-run, budgets, or a task registry.</action>
  <verify>
    <automated>npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-preflight.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export interface InputVarContract|export class MacroPreflightError|export function collectInputVarContract|export function validateInputVars" src/macro/preflight.ts` finds all exports.
    - `rg -n "MacroNamedArgs|builtin\\(positional, named, context\\)|collectInputVarContract\\(program\\)|validateInputVars" src/macro/evaluator.ts` finds the dispatch and preflight wiring.
    - `rg -n "hasOwnProperty\\.call\\(.*inputVars|Object\\.prototype\\.hasOwnProperty\\.call" src/macro/preflight.ts src/macro/evaluator.ts` proves presence-based null handling.
    - `npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-preflight.test.ts` exits 0.
  </acceptance_criteria>
  <done>Preflight runs before execution, named args reach builtins, and T-U-097 through T-U-108 pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller input_vars -> evaluator | Untrusted JSON-shaped caller values enter macro runtime bindings. |
| parsed macro AST -> preflight | Macro author source determines required input contract and defaults. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering | `src/macro/preflight.ts` | mitigate | Reject dynamic `input_var` keys with `details.reason: "input_var_key_must_be_literal"` before execution. |
| T-133-02 | Information Disclosure | `src/macro/evaluator.ts` | mitigate | Run preflight before `execBlock` so missing inputs cannot trigger trace, logs, progress, or tool calls. |
| T-133-03 | Tampering | `validateInputVars` | mitigate | Use own-property presence checks so explicit `null` cannot be confused with missing input. |
</threat_model>

<verification>
Run after plan completion:
```bash
npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-preflight.test.ts
npx vitest run --config tests/config/vitest.unit.config.ts tests/unit/macro-*.test.ts
```
</verification>

<success_criteria>
MACRO-SRC-07, MACRO-SRC-08, and MACRO-BI-04 are covered by passing T-U-097 through T-U-108, with all source-doc reading paths preserved in both tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/133-standard-library-builtins/133-01-SUMMARY.md`.
</output>
