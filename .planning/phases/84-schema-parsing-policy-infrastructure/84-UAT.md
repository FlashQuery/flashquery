---
status: complete
phase: 84-schema-parsing-policy-infrastructure
source: [84-01-SUMMARY.md, 84-02-SUMMARY.md, 84-03-SUMMARY.md]
started: 2026-04-20T17:39:00Z
updated: 2026-04-20T17:41:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Unit test suite passes
expected: |
  Run: npm test -- declarative-policies global-type-registry plugin-manager
  All 45 tests pass with 0 failures across 3 test files (declarative-policies: 6 tests, global-type-registry: 4 tests, plugin-manager: 35 tests).
result: pass

### 2. DDL includes last_seen_updated_at column
expected: |
  The plugin table DDL generated by buildPluginTableDDL() includes a `last_seen_updated_at TIMESTAMPTZ` column alongside the other implicit columns (fqc_id, fqc_owner, etc.). This column is present in src/plugins/manager.ts implicitCols array.
result: pass

### 3. Policy field parsing with conservative defaults
expected: |
  A plugin YAML with no policy fields on a document type entry still loads successfully. The parsed DocumentTypePolicy has: access='read-write', on_added='ignore', on_moved='keep-tracking', on_modified='ignore', and track_as/template/field_map all undefined. Verified by declarative-policies.test.ts test 2.
result: pass

### 4. Auto-track validation throws at parse time
expected: |
  A plugin YAML with `on_added: auto-track` but no `track_as` field throws a parse-time error with a message mentioning "track_as". The error is caught during plugin registration, not at runtime. Verified by declarative-policies.test.ts test 3.
result: pass

### 5. Type registry populated after plugin registration
expected: |
  After registering two plugins, buildGlobalTypeRegistry() returns a Map containing all document type IDs from both plugins, each with the correct pluginId. If two plugins define the same type ID, the first registration wins and logger.warn is called. Verified by global-type-registry.test.ts.
result: pass

### 6. Registry wired into MCP plugin tools
expected: |
  Calling register_plugin or unregister_plugin via MCP triggers a rebuild of globalTypeRegistry. After register_plugin completes, the new plugin's document types appear in the registry. Verified by 84-02 wiring (three call sites in plugins.ts).
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
