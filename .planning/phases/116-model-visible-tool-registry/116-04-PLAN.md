---
phase: 116-model-visible-tool-registry
plan: 04
type: execute
wave: 3
depends_on:
  - 116-02
  - 116-03
files_modified:
  - src/llm/types.ts
  - src/mcp/tools/llm.ts
  - tests/unit/llm-tool.test.ts
  - tests/unit/llm-client.test.ts
  - tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - .planning/phases/116-model-visible-tool-registry/116-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - VAL-116
must_haves:
  truths:
    - "Purpose call_model requests pass non-empty assembled native tools to provider calls."
    - "Purpose call_model requests omit provider tools entirely when the final model-visible set is empty."
    - "Hard-excluded native tools are visible in public-surface diagnostics without exposing them to providers."
    - "At least one public directed scenario proves model-visible native tool-list behavior through the MCP surface."
  artifacts:
    - path: "src/llm/types.ts"
      provides: "CallModelMetadata tools diagnostics shape"
      contains: "tools?:"
    - path: "src/mcp/tools/llm.ts"
      provides: "call_model purpose-path registry assembly and provider parameter wiring"
      contains: "assembleNativeToolRegistry"
    - path: "tests/unit/llm-tool.test.ts"
      provides: "MCP handler tests for registry diagnostics and purpose provider parameters"
    - path: "tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py"
      provides: "Managed public scenario for VAL-116"
    - path: ".planning/phases/116-model-visible-tool-registry/116-VALIDATION.md"
      provides: "Updated phase validation status and exact runnable commands"
  key_links:
    - from: "src/mcp/tools/llm.ts"
      to: "src/llm/tool-registry.ts"
      via: "assembleNativeToolRegistry before purpose dispatch"
      pattern: "assembleNativeToolRegistry"
    - from: "src/mcp/tools/llm.ts"
      to: "client.completeByPurpose"
      via: "params.parameters merged with non-empty providerTools"
      pattern: "completeByPurpose\\("
    - from: "tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py"
      to: "call_model MCP tool"
      via: "FQCClient.call_tool('call_model', resolver='purpose', ...)"
      pattern: "call_tool\\(.*call_model"
---

<objective>
Wire assembled native tools into purpose provider calls and close Phase 116 validation.

Purpose: The registry must affect real provider requests but still stop short of executing delegated tool calls. This plan proves non-empty provider tools, empty-tool omission, hard-exclusion diagnostics, and public-surface behavior.
Output: call_model wiring, unit tests, directed scenario, coverage matrix update, validation traceability, and roadmap/requirements closure.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/116-model-visible-tool-registry/116-RESEARCH.md
@.planning/phases/116-model-visible-tool-registry/116-VALIDATION.md
@.planning/phases/116-model-visible-tool-registry/116-PATTERNS.md
@.planning/phases/116-model-visible-tool-registry/116-02-SUMMARY.md
@.planning/phases/116-model-visible-tool-registry/116-03-SUMMARY.md
@src/mcp/tools/llm.ts
@src/llm/types.ts
@src/llm/client.ts
@src/llm/tool-registry.ts
@tests/scenarios/directed/testcases/test_call_model_agent_loop_capabilities.py
@tests/scenarios/directed/WRITING_SCENARIOS.md

<interfaces>
Existing call_model purpose guard location:
```ts
if (resolvedResolver === 'purpose') {
  const normalizedPurposeName = resolvedName.toLowerCase();
  const purpose = config.llm?.purposes.find((p) => p.name === normalizedPurposeName);
  for (const modelName of purpose?.models ?? []) {
    const capabilityCheck = assertResponseFormatAllowedWithTools(config, normalizedPurposeName, modelName, params.parameters);
    if (!capabilityCheck.ok) return { content: [{ type: 'text' as const, text: capabilityCheck.message }], isError: true };
  }
}
```

Existing provider omission behavior:
```ts
function normalizeProviderParameters(parameters: Record<string, unknown>): Record<string, unknown> {
  const normalized = { ...parameters };
  if (Array.isArray(normalized['tools']) && normalized['tools'].length === 0) {
    delete normalized['tools'];
  }
  return normalized;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add call_model registry wiring tests</name>
  <files>tests/unit/llm-tool.test.ts, tests/unit/llm-client.test.ts</files>
  <read_first>
    - tests/unit/llm-tool.test.ts
    - tests/unit/llm-client.test.ts
    - src/mcp/tools/llm.ts
    - src/llm/client.ts
    - src/llm/tool-registry.ts
  </read_first>
  <behavior>
    - Purpose calls with `tools: [get_document]` pass provider parameters containing one OpenAI function tool named `get_document`.
    - Purpose calls with `tools: [call_model]` do not pass any provider `tools` and expose hard-exclusion diagnostics in `metadata.tools`.
    - Purpose calls with `tools: [get_document]` and `excludedTools: [get_document]` do not pass `tools: []`; provider request body omits the `tools` key.
    - Existing direct `resolver='model'` behavior remains unchanged unless caller explicitly passes provider parameters.
  </behavior>
  <action>
    Add focused tests in `tests/unit/llm-tool.test.ts` for `registerLlmTools` purpose-path behavior using the existing handler invocation/mocking pattern. Assert the parameters passed to `client.completeByPurpose` contain `tools` only when `assembleNativeToolRegistry` returns non-empty provider tools. Assert the JSON envelope metadata includes `metadata.tools.native_tool_names` and `metadata.tools.diagnostics.hard_excluded` when hard exclusions were requested. Extend `tests/unit/llm-client.test.ts` only if needed to strengthen the existing empty tools omission assertion so it catches `tools: []` from the Phase 116 call path.
  </action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-client.test.ts tests/unit/llm-tool-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "metadata.tools" tests/unit/llm-tool.test.ts` returns assertions.
    - `grep -n "native_tool_names" tests/unit/llm-tool.test.ts` returns assertions.
    - `grep -n "hard_excluded" tests/unit/llm-tool.test.ts` returns assertions.
    - `grep -n "not.toHaveProperty('tools')" tests/unit/llm-client.test.ts tests/unit/llm-tool.test.ts` returns an empty-tool omission assertion.
    - The first run fails until `src/mcp/tools/llm.ts` wiring is implemented.
  </acceptance_criteria>
  <done>call_model purpose-path provider parameter behavior is pinned before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire registry assembly into purpose calls</name>
  <files>src/llm/types.ts, src/mcp/tools/llm.ts, tests/unit/llm-tool.test.ts, tests/unit/llm-client.test.ts</files>
  <read_first>
    - src/llm/types.ts
    - src/mcp/tools/llm.ts
    - src/llm/tool-registry.ts
    - src/llm/capabilities.ts
    - src/llm/client.ts
    - tests/unit/llm-tool.test.ts
    - tests/unit/llm-client.test.ts
    - .planning/phases/116-model-visible-tool-registry/116-PATTERNS.md
  </read_first>
  <action>
    In `src/llm/types.ts`, extend `CallModelMetadata` with optional `tools?: { native_tool_names: string[]; diagnostics: Record<string, unknown> }`. In `src/mcp/tools/llm.ts`, import `assembleNativeToolRegistry`, `getNativeToolCatalog`, and `modelCapabilitiesWithDefaults`. For `resolvedResolver === 'purpose'`, assemble native tools after reference hydration and before trace pre-snapshot/provider dispatch. For each fallback model, keep the existing `assertResponseFormatAllowedWithTools` guard. For the selected provider parameters sent to `client.completeByPurpose`, merge caller `params.parameters ?? {}` with `{ tools: registry.providerTools }` only when `registry.providerTools` is a non-empty array; never pass `tools: []`. Determine strict support for each request from `client.getModelForPurpose(resolvedName)?.config.capabilities` with `modelCapabilitiesWithDefaults` and the selected provider from `config.llm.providers`; if no selected model is available, use non-strict. Add `metadata.tools = { native_tool_names, diagnostics }` to the success envelope when the purpose had `tools` configured or diagnostics are non-empty. Keep direct model calls unchanged. Do not implement delegated tool dispatch.
  </action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-client.test.ts tests/unit/llm-tool-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "assembleNativeToolRegistry" src/mcp/tools/llm.ts` returns an import and usage.
    - `grep -n "getNativeToolCatalog" src/mcp/tools/llm.ts` returns an import and usage.
    - `grep -n "tools?:" src/llm/types.ts` returns the optional metadata field.
    - `grep -n "metadata.tools" src/mcp/tools/llm.ts` returns metadata wiring.
    - `grep -n "tools: \\[\\]" src/mcp/tools/llm.ts` returns no matches.
    - `npm test -- tests/unit/llm-tool.test.ts tests/unit/llm-client.test.ts tests/unit/llm-tool-registry.test.ts` passes.
  </acceptance_criteria>
  <done>Purpose-based call_model requests pass provider tools only when the final registry is non-empty and return tool diagnostics in the public envelope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add public scenario and close validation traceability</name>
  <files>tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py, tests/scenarios/directed/DIRECTED_COVERAGE.md, .planning/phases/116-model-visible-tool-registry/116-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py
    - tests/scenarios/directed/testcases/test_call_model_agent_loop_capabilities.py
    - tests/scenarios/directed/WRITING_SCENARIOS.md
    - tests/scenarios/directed/DIRECTED_COVERAGE.md
    - .planning/phases/116-model-visible-tool-registry/116-VALIDATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Create `tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py` using the managed scenario pattern from `test_call_model_agent_loop_capabilities.py`. Use a local deterministic OpenAI-compatible mock provider or the existing scenario mock-provider helper if available; configure a purpose with `tools: [get_document, call_model]` and model capabilities `tool_calling: true`, `usage_on_tool_calls: true`, and `strict_tools: true`. Call `call_model` with `resolver='purpose'`, then assert the public response includes `metadata.tools.native_tool_names` containing `get_document`, excludes `call_model`, and includes hard-exclusion diagnostics naming `call_model`. Add a second scenario step with `tools: [get_document]` plus `excluded_tools: [get_document]` and assert the provider/mock request omits `tools` rather than receiving `tools: []`. Update `DIRECTED_COVERAGE.md` with a new row for VAL-116/native registry behavior covered by `test_call_model_native_tool_registry`. Update `116-VALIDATION.md` so quick/full commands use actual repo commands: `npm test -- tests/unit/llm-tool-registry.test.ts tests/unit/llm-config.test.ts tests/unit/llm-client.test.ts tests/unit/llm-tool.test.ts` and `python3 tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py --managed`. Mark `VAL-116` complete in `.planning/REQUIREMENTS.md` only after the tests pass, and update `.planning/ROADMAP.md` Phase 116 plan list/status.
  </action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool-registry.test.ts tests/unit/llm-config.test.ts tests/unit/llm-client.test.ts tests/unit/llm-tool.test.ts &amp;&amp; python3 tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py --managed</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py` succeeds.
    - `grep -n "VAL-116" tests/scenarios/directed/DIRECTED_COVERAGE.md .planning/phases/116-model-visible-tool-registry/116-VALIDATION.md .planning/REQUIREMENTS.md` returns matches.
    - `grep -n "test_call_model_native_tool_registry" tests/scenarios/directed/DIRECTED_COVERAGE.md .planning/phases/116-model-visible-tool-registry/116-VALIDATION.md` returns matches.
    - `grep -n "python3 tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py --managed" .planning/phases/116-model-visible-tool-registry/116-VALIDATION.md` returns one or more matches.
    - The combined automated verification command passes.
  </acceptance_criteria>
  <done>VAL-116 has runnable unit and public directed scenario evidence, and planning traceability reflects completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call_model request to provider request | Host parameters and registry output are merged into an outbound provider request body. |
| registry diagnostics to MCP response | Security-relevant omissions become model/user-visible metadata. |
| provider request shape | Empty provider tools must be omitted to avoid provider/API behavior changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-13 | Elevation of Privilege | `call_model` purpose path | mitigate | Only purpose resolver calls receive assembled native tools; direct model calls remain unchanged unless caller supplies parameters. |
| T-116-14 | Tampering | parameter merge | mitigate | Preserve caller provider params and add registry `tools` only when non-empty; tests assert no `tools: []` leaves FlashQuery. |
| T-116-15 | Information Disclosure | `metadata.tools` | mitigate | Metadata exposes names and diagnostics only, not handler references, schemas containing secrets, or provider credentials. |
| T-116-16 | Repudiation | hard-exclusion warnings | mitigate | Public envelope and scenario assert hard-exclusion diagnostics so omission is not silent. |
| T-116-17 | Denial of Service | provider `tools: []` | mitigate | Existing `normalizeProviderParameters` omission behavior is preserved and strengthened by unit and directed scenario coverage. |
</threat_model>

<verification>
Run after this plan:
```bash
npm test -- tests/unit/llm-tool-registry.test.ts tests/unit/llm-config.test.ts tests/unit/llm-client.test.ts tests/unit/llm-tool.test.ts
python3 tests/scenarios/directed/testcases/test_call_model_native_tool_registry.py --managed
npm run build
```
</verification>

<success_criteria>
- TOOL-01 through TOOL-04 are exercised through unit tests and call_model wiring.
- VAL-116 public-surface scenario exists and passes.
- No provider request receives `tools: []` from the model-visible registry path.
- Phase validation and traceability files name exact runnable commands and coverage evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/116-model-visible-tool-registry/116-04-SUMMARY.md`.
</output>
