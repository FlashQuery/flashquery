---
phase: 116-model-visible-tool-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm/tool-registry.ts
  - tests/unit/llm-tool-registry.test.ts
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
must_haves:
  truths:
    - "Purpose tools can expand tier:read-only, tier:read-write, and explicit native tool names into one de-duplicated final allowlist."
    - "Purpose excluded_tools subtracts from the expanded allowlist after tiers and explicit names are combined."
    - "Hard-excluded tools including call_model and plugin admin tools are never present in nativeToolNames and produce diagnostics."
  artifacts:
    - path: "src/llm/tool-registry.ts"
      provides: "Native tool policy constants, expansion, exclusions, hard-exclusion diagnostics, and assembly result types"
      exports: ["TOOL_TIERS", "HARD_EXCLUDED_NATIVE_TOOLS", "assembleNativeToolRegistry"]
    - path: "tests/unit/llm-tool-registry.test.ts"
      provides: "Focused unit coverage for tier expansion, named tools, exclusions, hard exclusions, and empty result behavior"
  key_links:
    - from: "src/llm/tool-registry.ts"
      to: "FlashQueryConfig['llm']['purposes'][number].tools"
      via: "purpose lookup by name"
      pattern: "purpose\\.tools"
    - from: "src/llm/tool-registry.ts"
      to: "hard-exclusion diagnostics"
      via: "HARD_EXCLUDED_NATIVE_TOOLS check after expansion"
      pattern: "HARD_EXCLUDED_NATIVE_TOOLS"
---

<objective>
Create the pure policy boundary for native model-visible tool exposure.

Purpose: Phase 117 must not infer delegated tool safety from the full MCP server surface. This plan defines the allowlist tiers, explicit-name behavior, exclusion order, and hard-exclusion diagnostics that all later registry wiring consumes.
Output: `src/llm/tool-registry.ts` and focused unit tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/116-model-visible-tool-registry/116-RESEARCH.md
@.planning/phases/116-model-visible-tool-registry/116-VALIDATION.md
@.planning/phases/116-model-visible-tool-registry/116-PATTERNS.md
@.planning/phases/115-purpose-config-bindings-capabilities/115-01-SUMMARY.md
@.planning/phases/115-purpose-config-bindings-capabilities/115-04-SUMMARY.md
@src/config/loader.ts
@src/llm/capabilities.ts

<interfaces>
Existing contracts the executor must use:

From `src/config/loader.ts`:
```ts
export interface FlashQueryConfig {
  llm?: {
    models: Array<{
      name: string;
      providerName: string;
      capabilities?: {
        tool_calling?: boolean;
        usage_on_tool_calls?: boolean;
        strict_tools?: boolean;
        parallel_tool_calls?: boolean;
        structured_outputs_with_tools?: boolean;
      };
    }>;
    purposes: Array<{
      name: string;
      description: string;
      models: string[];
      defaults?: Record<string, unknown>;
      tools?: string[];
      excludedTools?: string[];
      templates?: string[];
    }>;
  };
}
```

From `src/llm/capabilities.ts`:
```ts
export function modelCapabilitiesWithDefaults(
  model: Pick<LlmModel, 'capabilities'>,
  provider: LlmProvider
): StructuredModelCapabilities;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock native tool expansion behavior</name>
  <files>tests/unit/llm-tool-registry.test.ts</files>
  <read_first>
    - tests/unit/llm-config.test.ts
    - tests/unit/llm-client.test.ts
    - .planning/phases/116-model-visible-tool-registry/116-RESEARCH.md
    - .planning/phases/116-model-visible-tool-registry/116-VALIDATION.md
  </read_first>
  <behavior>
    - Test `tier:read-only` expands to read-safe native tools including `search_documents`, `get_document`, `search_memory`, `list_memories`, `search_records`, `get_record`, `search_all`, and `get_briefing`.
    - Test `tier:read-write` includes every read-only tool plus write-capable tools including `create_document`, `update_document`, `append_to_doc`, `move_document`, `save_memory`, `update_memory`, `create_record`, `update_record`, `apply_tags`, `archive_document`, `archive_memory`, `archive_record`, `create_directory`, and `remove_directory`.
    - Test explicit named tools union with tier expansion and duplicates are ignored while preserving deterministic output order.
    - Test `excludedTools` removes tools after tier and named expansion.
    - Test `call_model`, `register_plugin`, `unregister_plugin`, and `get_plugin_info` never appear in `nativeToolNames` and appear in `diagnostics.hardExcluded`.
  </behavior>
  <action>
    Create `tests/unit/llm-tool-registry.test.ts` with RED tests importing the future exports `TOOL_TIERS`, `HARD_EXCLUDED_NATIVE_TOOLS`, and `assembleNativeToolRegistry` from `../../src/llm/tool-registry.js`. Use a small in-test catalog array with `{ name, description, inputSchema }` definitions for the named tools under test; do not import MCP handlers. Assert exact arrays for `nativeToolNames`, exact diagnostic object entries for hard exclusions, and exact empty-result behavior when all requested tools are excluded.
  </action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tier:read-only" tests/unit/llm-tool-registry.test.ts` shows a test case for read-only tier expansion.
    - `grep -n "tier:read-write" tests/unit/llm-tool-registry.test.ts` shows a test case for read-write tier expansion.
    - `grep -n "call_model" tests/unit/llm-tool-registry.test.ts` shows a hard-exclusion assertion.
    - The first run of `npm test -- tests/unit/llm-tool-registry.test.ts` fails because `src/llm/tool-registry.ts` does not exist or exports are missing.
  </acceptance_criteria>
  <done>The behavior contract exists as failing tests before production implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure native registry assembly</name>
  <files>src/llm/tool-registry.ts, tests/unit/llm-tool-registry.test.ts</files>
  <read_first>
    - src/llm/tool-registry.ts
    - tests/unit/llm-tool-registry.test.ts
    - src/config/loader.ts
    - src/llm/capabilities.ts
    - .planning/phases/116-model-visible-tool-registry/116-PATTERNS.md
  </read_first>
  <action>
    Create `src/llm/tool-registry.ts` as a pure module. Export `NativeToolDefinition`, `ToolRegistryDiagnostics`, `ToolRegistryAssembly`, `TOOL_TIERS`, `HARD_EXCLUDED_NATIVE_TOOLS`, and `assembleNativeToolRegistry(config, purposeName, catalog, options?)`. Use exactly these tier keys: `tier:read-only` and `tier:read-write`. Use exactly these hard-excluded names: `call_model`, `register_plugin`, `unregister_plugin`, `get_plugin_info`. Lowercase purpose lookup names to match Phase 115 normalization. Expand `purpose.tools ?? []`, de-duplicate by first occurrence, apply `purpose.excludedTools ?? []` after expansion, then remove hard-excluded names while adding diagnostics `{ tool, reason }`. Return `nativeToolNames: string[]`, `providerTools: undefined` for now, and diagnostics with arrays `expandedTiers`, `explicitTools`, `excluded`, `hardExcluded`, `unknown`. Do not dispatch handlers and do not mutate `config`.
  </action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const TOOL_TIERS" src/llm/tool-registry.ts` returns one match.
    - `grep -n "HARD_EXCLUDED_NATIVE_TOOLS" src/llm/tool-registry.ts` returns matches including `call_model` and `register_plugin`.
    - `grep -n "export function assembleNativeToolRegistry" src/llm/tool-registry.ts` returns one match.
    - `grep -n "providerTools: undefined" src/llm/tool-registry.ts` or equivalent explicit no-schema state exists before Plan 02 adds translation.
    - `npm test -- tests/unit/llm-tool-registry.test.ts` passes.
  </acceptance_criteria>
  <done>Tier and explicit native tool exposure policy is implemented as a deterministic pure function with hard-exclusion diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file to registry | User-authored YAML selects delegated model-visible native tools. |
| registry policy to future dispatcher | Registry output constrains what Phase 117 may dispatch internally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Elevation of Privilege | `assembleNativeToolRegistry` | mitigate | Hard-code `HARD_EXCLUDED_NATIVE_TOOLS` with `call_model`, `register_plugin`, `unregister_plugin`, and `get_plugin_info`; remove them after expansion and test exact diagnostics. |
| T-116-02 | Tampering | `excludedTools` handling | mitigate | Apply exclusions after tier/name union so broad tiers cannot override explicit removals; unit tests assert exclusion order. |
| T-116-03 | Information Disclosure | `tier:read-write` policy | mitigate | Define explicit tier arrays instead of deriving from all registered MCP tools; tests assert tier contents do not include admin/plugin tools. |
| T-116-04 | Repudiation | hard-exclusion omission | mitigate | Return `diagnostics.hardExcluded` entries instead of silent drops. |
</threat_model>

<verification>
Run after this plan:
```bash
npm test -- tests/unit/llm-tool-registry.test.ts
```
</verification>

<success_criteria>
- TOOL-01 tier and named native expansion has deterministic unit coverage.
- TOOL-02 exclusion order is implemented in the pure registry result.
- TOOL-03 hard-excluded native tools are removed and reported in diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/116-model-visible-tool-registry/116-01-SUMMARY.md`.
</output>
