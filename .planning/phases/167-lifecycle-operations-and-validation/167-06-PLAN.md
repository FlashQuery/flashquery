---
phase: 167-lifecycle-operations-and-validation
plan: 06
type: execute
wave: 6
depends_on:
  - 167-03
  - 167-04
  - 167-05
files_modified:
  - tests/scenarios/directed/testcases/test_lifecycle_lock_per_entry.py
  - tests/scenarios/directed/testcases/test_lifecycle_lock_heartbeat.py
  - tests/scenarios/directed/testcases/test_abort_background_job.py
  - tests/scenarios/directed/testcases/test_abort_unknown_job.py
autonomous: true
requirements:
  - REQ-038
  - REQ-039
must_haves:
  truths:
    - "same-entry lock conflicts, different-entry parallelism, stale heartbeat recovery, and abort behavior are verified through directed public-surface scenarios."
    - "abort on a running background lifecycle job preserves completed rows, reports partial counts, transitions status to aborted, and releases the lock."
  artifacts:
    - path: tests/scenarios/directed/testcases/test_lifecycle_lock_per_entry.py
      provides: "D-114 directed scenario"
    - path: tests/scenarios/directed/testcases/test_lifecycle_lock_heartbeat.py
      provides: "D-115 directed scenario"
    - path: tests/scenarios/directed/testcases/test_abort_background_job.py
      provides: "D-116 directed scenario"
    - path: tests/scenarios/directed/testcases/test_abort_unknown_job.py
      provides: "D-117 directed scenario"
  key_links:
    - from: tests/scenarios/directed/testcases/test_lifecycle_lock_per_entry.py
      to: src/services/maintenance.ts
      via: "public maintain_vault conflict envelope"
      pattern: "in_flight_job_id"
    - from: tests/scenarios/directed/testcases/test_abort_background_job.py
      to: src/services/maintenance.ts
      via: "public maintain_vault abort and status envelope"
      pattern: "aborted"
read_first:
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md
---

<objective>
Verify lifecycle lock, heartbeat, and abort behavior through public directed scenarios.

Purpose: REQ-038 and REQ-039 are only complete when public `maintain_vault` scenarios prove the durable helpers and processor dispatch work together.
Output: directed scenarios D-114 through D-117.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/167-lifecycle-operations-and-validation/167-CONTEXT.md
@.planning/phases/167-lifecycle-operations-and-validation/167-RESEARCH.md
@.planning/phases/167-lifecycle-operations-and-validation/167-PATTERNS.md
@.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
@.planning/phases/167-lifecycle-operations-and-validation/167-03-SUMMARY.md
@.planning/phases/167-lifecycle-operations-and-validation/167-04-SUMMARY.md
@.planning/phases/167-lifecycle-operations-and-validation/167-05-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md
@tests/scenarios/directed/WRITING_SCENARIOS.md

<interfaces>
Scenario contracts:
- Directed runner uses positional patterns: `python3 tests/scenarios/directed/run_suite.py --managed "test_name"`.
- Embedding-required directed scenarios must force a dedicated managed server with embedding configuration per `WRITING_SCENARIOS.md`.
- Use public `maintain_vault` calls only, except stale heartbeat may use a managed-test-only DB update or test hook isolated to the scenario instance.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per-entry lock and heartbeat scenarios</name>
  <files>tests/scenarios/directed/testcases/test_lifecycle_lock_per_entry.py, tests/scenarios/directed/testcases/test_lifecycle_lock_heartbeat.py</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md section 6.6.4
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md section 4.8.4, T-S-015 through T-S-016 and D-114 through D-115
    @.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
    @tests/scenarios/directed/WRITING_SCENARIOS.md
  </read_first>
  <behavior>
    - D-114: two lifecycle invocations for the same entry return conflict on the second call with `in_flight_job_id`; two different entries can proceed in parallel.
    - D-115: stale heartbeat older than threshold is released and a new caller acquires the lock.
  </behavior>
  <action>Create `test_lifecycle_lock_per_entry.py` and `test_lifecycle_lock_heartbeat.py` exactly for external test plan T-S-015/D-114 and T-S-016/D-115. Use public `maintain_vault` calls; if stale heartbeat requires a test hook, use a test-only helper or controlled DB update isolated to the managed scenario instance and document the guard in scenario comments. Assert response fields `details.in_flight_action`, `details.in_flight_job_id`, `details.started_at`, `details.elapsed_ms`, and lock release by successfully starting a follow-up lifecycle action for the same entry after stale recovery.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed "test_lifecycle_lock_*"</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: external requirements §6.6.4 are covered by D-114 and D-115 scenario files.
    Command assertions: build exits 0; directed runner positional pattern exits 0.
    Behavior assertions: lifecycle lock scenarios prove public conflict/status semantics, not only internal helper behavior.
  </acceptance_criteria>
  <done>Per-entry lock and heartbeat behavior are verified end-to-end by D-114 and D-115.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add abort scenarios</name>
  <files>tests/scenarios/directed/testcases/test_abort_background_job.py, tests/scenarios/directed/testcases/test_abort_unknown_job.py</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md section 6.6.5
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md section 4.8.5, T-S-017 through T-S-018 and D-116 through D-117
    @.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
    @.planning/phases/167-lifecycle-operations-and-validation/167-03-SUMMARY.md
    @tests/scenarios/directed/WRITING_SCENARIOS.md
  </read_first>
  <behavior>
    - D-116: background rebuild aborts at checkpoint, keeps already embedded rows, status is `aborted`, and the lock is released.
    - D-117: abort for unknown/completed/already-aborted job returns expected `not_found` or `unsupported` matching status convention.
  </behavior>
  <action>Create `test_abort_background_job.py` and `test_abort_unknown_job.py` exactly for external test plan T-S-017/D-116 and T-S-018/D-117. Use public `maintain_vault` calls only. The scenarios must start a background `rebuild_embeddings` job, call `abort` with the returned `job_id`, poll status until `aborted`, assert partial counts remain readable, assert no new rows start after abort checkpoint, and assert a follow-up lifecycle action for the same entry can acquire the lock. For unknown/completed/already-aborted jobs, assert the expected `not_found` or `unsupported` envelope matching the durable status convention.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed "test_abort_*"</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: external requirements §6.6.5 are covered by D-116 and D-117 scenario files.
    Command assertions: build exits 0; directed runner positional pattern exits 0.
    Behavior assertions: abort scenarios prove public abort/status semantics and lock release, not only internal helper behavior.
  </acceptance_criteria>
  <done>Abort behavior is verified end-to-end by D-116 and D-117.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenario harness to managed server | Test code starts background lifecycle jobs and invokes public MCP tools. |
| Stale heartbeat test hook to database | Managed-test-only hook may alter heartbeat timestamp to simulate stale lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-06-01 | Tampering | stale heartbeat scenario | mitigate | Any direct DB update must be guarded to managed scenario mode and documented in scenario comments. |
| T-167-06-02 | Repudiation | abort scenario status assertions | mitigate | Assert job id, status, partial counts, and terminal timestamps through public status response. |
| T-167-06-SC | Tampering | package installs | accept | No npm/pip/cargo installs are planned; if an executor adds one, stop and run the package legitimacy gate from RESEARCH.md first. |
</threat_model>

<verification>
Plan-level automated checks:
- `npm run build`
- `python3 tests/scenarios/directed/run_suite.py --managed "test_lifecycle_lock_*"`
- `python3 tests/scenarios/directed/run_suite.py --managed "test_abort_*"`
</verification>

<success_criteria>
- D-114 through D-117 scenario files exist and pass.
- Lifecycle lock, heartbeat, and abort public behavior is verified before Plan 7 recipes run.
</success_criteria>

<source_audit>
GOAL covered: lifecycle concurrency and abort behavior are validated through public scenarios.
REQ covered: REQ-038, REQ-039.
RESEARCH covered: heartbeat/abort directed coverage and managed-only stale heartbeat testing.
CONTEXT decisions covered: external requirements/test plan are in every task `read_first`; recipe validation remains out of this plan and depends on lifecycle plans.
</source_audit>

<output>
Create `.planning/phases/167-lifecycle-operations-and-validation/167-06-SUMMARY.md` when done.
</output>
