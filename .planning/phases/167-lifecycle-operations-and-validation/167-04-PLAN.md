---
phase: 167-lifecycle-operations-and-validation
plan: 04
type: execute
wave: 3
depends_on:
  - 167-02
  - 167-03
files_modified:
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
  - tests/scenarios/directed/testcases/test_first_time_enablement.py
  - tests/scenarios/directed/testcases/test_legacy_schema_reset.py
  - tests/scenarios/integration/tests/embedding_first_time_enablement_search.yml
  - flashquery.example.yml
autonomous: true
requirements:
  - REQ-042
  - REQ-043
must_haves:
  truths:
    - "First-time enablement recipe runs through YAML edit, restart, dry-run, core backfill, plugin re-registration plus records backfill, and semantic search verification."
    - "Legacy schema reset recipe detects the documented migration error, wipes legacy schema, updates YAML, restarts, re-registers plugins, backfills, and verifies."
    - "D-104 through D-121 and IS-50 coverage rows exist and point at the correct scenario files from the external test plan."
    - "flashquery.example.yml contains the final embeddings example needed to reproduce the operator recipe."
  artifacts:
    - path: tests/scenarios/directed/testcases/test_first_time_enablement.py
      provides: "D-120 first-time enablement recipe"
    - path: tests/scenarios/directed/testcases/test_legacy_schema_reset.py
      provides: "D-121 legacy schema reset recipe"
    - path: tests/scenarios/integration/tests/embedding_first_time_enablement_search.yml
      provides: "IS-50 YAML integration scenario"
    - path: flashquery.example.yml
      provides: "Operator-facing example embeddings configuration"
  key_links:
    - from: tests/scenarios/directed/DIRECTED_COVERAGE.md
      to: tests/scenarios/directed/testcases
      via: "D-104 through D-121 Covered By rows"
      pattern: "D-104|D-121"
    - from: tests/scenarios/integration/INTEGRATION_COVERAGE.md
      to: tests/scenarios/integration/tests/embedding_first_time_enablement_search.yml
      via: "IS-50 coverage row"
      pattern: "IS-50"
    - from: flashquery.example.yml
      to: tests/scenarios/directed/testcases/test_first_time_enablement.py
      via: "recipe config expectations"
      pattern: "embeddings:"
read_first:
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md
---

<objective>
Validate the complete lifecycle feature through the two operator recipes and update scenario coverage.

Purpose: Phase 167 is only complete when the lifecycle operations are reproducible from public operator workflows, not merely unit-tested.
Output: D-120 first-time enablement scenario, D-121 legacy schema reset scenario, IS-50 YAML integration scenario, coverage matrix rows D-104 through D-121 and IS-50, and final example config.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/167-lifecycle-operations-and-validation/167-CONTEXT.md
@.planning/phases/167-lifecycle-operations-and-validation/167-RESEARCH.md
@.planning/phases/167-lifecycle-operations-and-validation/167-VALIDATION.md
@.planning/phases/167-lifecycle-operations-and-validation/167-PATTERNS.md
@.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
@.planning/phases/167-lifecycle-operations-and-validation/167-03-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md
@tests/scenarios/directed/WRITING_SCENARIOS.md
@tests/scenarios/directed/DIRECTED_COVERAGE.md
@tests/scenarios/integration/README.md
@tests/scenarios/integration/INTEGRATION_COVERAGE.md
@flashquery.example.yml

<interfaces>
Scenario contracts:
- Directed runner uses positional patterns: `python3 tests/scenarios/directed/run_suite.py --managed "test_name"`.
- YAML integration runner uses positional filename substrings: `python3 tests/scenarios/integration/run_integration.py --managed "embedding_first_time_enablement_search"`.
- Use directed runner positional arguments and Vitest file targeting.
- Scenario authoring must follow `tests/scenarios/directed/WRITING_SCENARIOS.md` and `tests/scenarios/integration/README.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update coverage matrices for lifecycle and recipe scenarios</name>
  <files>tests/scenarios/directed/DIRECTED_COVERAGE.md, tests/scenarios/integration/INTEGRATION_COVERAGE.md</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md sections 6.6 and 6.7
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md sections 4.8, 4.9, and 7, especially D-104 through D-121 and IS-50
    @tests/scenarios/directed/DIRECTED_COVERAGE.md
    @tests/scenarios/integration/INTEGRATION_COVERAGE.md
    @.agents/skills/flashquery-directed-covgen/SKILL.md
    @.agents/skills/flashquery-integration-covgen/SKILL.md
  </read_first>
  <action>Add directed coverage rows D-104 through D-121 to `tests/scenarios/directed/DIRECTED_COVERAGE.md` using the exact behavior titles, coverage IDs, and test filenames from the external test plan §7: D-104 `test_backfill_embeddings_full_scope`, D-105 `test_backfill_embeddings_dry_run`, D-106 `test_backfill_embeddings_background`, D-107 `test_backfill_embeddings_failures`, D-108 `test_rebuild_embeddings_stale_only`, D-109 `test_rebuild_embeddings_confirm_mismatch`, D-110 `test_rebuild_embeddings_max_rows_required`, D-111 `test_retire_embedding_transactional`, D-112 `test_retire_embedding_plugin_conflict`, D-113 `test_retire_embedding_deactivated_entry`, D-114 `test_lifecycle_lock_per_entry`, D-115 `test_lifecycle_lock_heartbeat`, D-116 `test_abort_background_job`, D-117 `test_abort_unknown_job`, D-118 `test_records_scope_embedding_resolution`, D-119 `test_records_scope_mixed`, D-120 `test_first_time_enablement`, and D-121 `test_legacy_schema_reset`. Add integration coverage row IS-50 to `tests/scenarios/integration/INTEGRATION_COVERAGE.md` for `embedding_first_time_enablement_search.yml`. Set `Date Updated` to 2026-06-11 and leave `Last Passing` blank or matrix-conventional until runner updates it. Preserve existing IDs and do not renumber older rows.</action>
  <verify>
    <automated>rg -n "D-10[4-9]|D-11[0-9]|D-12[0-1]" tests/scenarios/directed/DIRECTED_COVERAGE.md</automated>
    <automated>rg -n "IS-50|embedding_first_time_enablement_search" tests/scenarios/integration/INTEGRATION_COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: external test plan §7 coverage rows D-104..D-121 and IS-50 are represented exactly once.
    Command assertions: both `rg` commands find the expected IDs and filenames.
    Behavior assertions: coverage rows reference scenario files created by Plans 2, 3, and this plan; no deferred or out-of-scope row is added.
  </acceptance_criteria>
  <done>Coverage matrices contain every Phase 167 required directed and YAML integration coverage row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add first-time enablement directed and YAML integration recipes</name>
  <files>tests/scenarios/directed/testcases/test_first_time_enablement.py, tests/scenarios/integration/tests/embedding_first_time_enablement_search.yml, flashquery.example.yml</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md section 6.7.1 and Research §11.1 references in that file
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md section 4.9.1, T-S-021/D-120 and T-Y-001/IS-50
    @.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
    @.planning/phases/167-lifecycle-operations-and-validation/167-03-SUMMARY.md
    @tests/scenarios/directed/WRITING_SCENARIOS.md
    @tests/scenarios/integration/README.md
    @flashquery.example.yml
  </read_first>
  <behavior>
    - D-120: six-step first-time enablement recipe runs YAML edit, restart, dry-run, core backfill, plugin re-registration plus records backfill, and semantic search verification.
    - IS-50: YAML integration scenario proves post-recipe search returns documents and memories with correct ranking.
    - Recipe assertions include row added plus columns added after restart, dry-run counts match expectations, backfill counts match dry-run, plugin records populated, and search returns expected top hit with `match_source: ["semantic"]`.
  </behavior>
  <action>Create `test_first_time_enablement.py` exactly for external test plan T-S-021/D-120. Use the managed directed scenario framework to start from no configured `embeddings:`, populate documents/memories and optional plugin records, modify managed config with the final `embeddings:` shape, restart, call `maintain_vault` dry-run, call core `backfill_embeddings`, re-register plugin with the chosen embedding name, call records backfill, and verify semantic search. Create `embedding_first_time_enablement_search.yml` exactly for T-Y-001/IS-50 with `deps: [embeddings]`, public tool steps, and assertions aligned to the integration README. Update `flashquery.example.yml` so the embeddings example is sufficient to reproduce the recipe and does not reintroduce legacy `embedding:` purpose semantics. Do not build a web UI.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed "test_first_time_enablement"</automated>
    <automated>python3 tests/scenarios/integration/run_integration.py --managed "embedding_first_time_enablement_search"</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: external requirements §6.7.1 criteria 1-3 are asserted; external test plan T-S-021 and T-Y-001 files exist.
    Command assertions: build exits 0; directed and YAML integration runners exit 0 with positional patterns.
    Behavior assertions: final search result includes semantic match source and expected document/memory hits; the recipe can be followed from `flashquery.example.yml`.
  </acceptance_criteria>
  <done>First-time enablement is reproducible and passing as both directed D-120 and YAML integration IS-50.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add legacy schema reset directed recipe</name>
  <files>tests/scenarios/directed/testcases/test_legacy_schema_reset.py</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md section 6.7.2 and Research §11.2 references in that file
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md section 4.9.2, T-S-022/D-121
    @.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
    @.planning/phases/167-lifecycle-operations-and-validation/167-03-SUMMARY.md
    @tests/scenarios/directed/WRITING_SCENARIOS.md
  </read_first>
  <behavior>
    - D-121: seven-step legacy schema reset recipe detects the documented migration error, stops, drops legacy columns, updates YAML, restarts, re-registers plugins, backfills, and verifies semantic search.
    - Scenario runs only against a managed/test database and does not expose destructive reset behavior against non-test environments.
  </behavior>
  <action>Create `test_legacy_schema_reset.py` exactly for external test plan T-S-022/D-121. The scenario must set up a managed database snapshot with legacy singular `embedding` columns populated and a legacy `purposes: - name: embedding` config, assert the documented startup failure from Research §5.3/§9.1 as referenced by requirements §6.7.2, stop the server, drop legacy columns in the managed test database, update YAML to the top-level `embeddings:` catalog, restart, re-register plugins with explicit embedding choice, run `backfill_embeddings`, and verify semantic search. Put any direct SQL reset helper behind the managed scenario test harness only, with a guard that refuses non-managed/external mode.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed "test_legacy_schema_reset"</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: external requirements §6.7.2 criteria are tested; external test plan T-S-022/D-121 file exists.
    Command assertions: build exits 0; directed runner positional pattern `test_legacy_schema_reset` exits 0.
    Behavior assertions: legacy reset scenario proves failure-before-reset and success-after-reset through public behavior, and destructive SQL is impossible outside managed test mode.
  </acceptance_criteria>
  <done>Legacy schema reset migration path is validated by D-121.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenario harness to managed server | Test code edits config, restarts FlashQuery, and invokes public MCP tools. |
| Legacy reset scenario to database | Test-only direct SQL drops legacy columns in a managed database. |
| Example config to operators | `flashquery.example.yml` influences user setup for embedding providers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-04-01 | Tampering | coverage matrices | mitigate | Add only external test plan D-104..D-121 and IS-50 rows; preserve existing IDs and history. |
| T-167-04-02 | Tampering | legacy reset scenario | mitigate | Guard destructive SQL behind managed scenario mode; refuse external server mode. |
| T-167-04-03 | Information Disclosure | scenario logs | mitigate | Do not log embedding provider secrets, vector contents, or full user content beyond test fixture strings. |
| T-167-04-04 | Spoofing | example config | mitigate | Use explicit provider/model/dimensions fields from external docs; do not include real API keys. |
| T-167-04-SC | Tampering | package installs | accept | No npm/pip/cargo installs are planned; if an executor adds one, stop and run the package legitimacy gate from RESEARCH.md first. |
</threat_model>

<verification>
Plan-level automated checks:
- `npm run build`
- `python3 tests/scenarios/directed/run_suite.py --managed "test_first_time_enablement"`
- `python3 tests/scenarios/directed/run_suite.py --managed "test_legacy_schema_reset"`
- `python3 tests/scenarios/integration/run_integration.py --managed "embedding_first_time_enablement_search"`
- `rg -n "D-10[4-9]|D-11[0-9]|D-12[0-1]" tests/scenarios/directed/DIRECTED_COVERAGE.md`
- `rg -n "IS-50|embedding_first_time_enablement_search" tests/scenarios/integration/INTEGRATION_COVERAGE.md`
</verification>

<success_criteria>
- Operator recipes D-120, D-121, and IS-50 pass from public surfaces.
- Coverage matrices include D-104 through D-121 and IS-50 with correct filenames.
- `flashquery.example.yml` reflects the final embeddings catalog recipe.
- Phase 167 can be verified with the full command set from `.planning/phases/167-lifecycle-operations-and-validation/167-VALIDATION.md`.
</success_criteria>

<source_audit>
GOAL covered: complete feature validation through first-time enablement and legacy schema reset operator recipes.
REQ covered: REQ-042, REQ-043.
RESEARCH covered: recipe scenarios, coverage matrices, example config, directed/YAML runner syntax.
CONTEXT decisions covered: external requirements/test plan are in every task `read_first`; this plan depends on lifecycle plans, preserving sub-step 3.1 before 3.2.
</source_audit>

<output>
Create `.planning/phases/167-lifecycle-operations-and-validation/167-04-SUMMARY.md` when done.
</output>
