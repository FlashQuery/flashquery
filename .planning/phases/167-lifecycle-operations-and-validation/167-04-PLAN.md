---
phase: 167-lifecycle-operations-and-validation
plan: 04
type: execute
wave: 4
depends_on:
  - 167-01
  - 167-02
  - 167-03
files_modified:
  - src/services/maintenance.ts
  - src/embedding/lifecycle/retire.ts
  - tests/scenarios/directed/testcases/test_retire_embedding_transactional.py
  - tests/scenarios/directed/testcases/test_retire_embedding_plugin_conflict.py
  - tests/scenarios/directed/testcases/test_retire_embedding_deactivated_entry.py
autonomous: true
requirements:
  - REQ-037
must_haves:
  truths:
    - "retire_embedding drops core and plugin per-entry columns, indexes, RPCs, and the catalog row in one transaction."
    - "retire_embedding refuses conflict when any registered plugin row stores the target embedding_name and lists affected plugin IDs."
    - "retire_embedding inventories plugin artifacts by schema/proc/index discovery for `embedding_<name>` artifacts, so stale plugin columns/RPCs left after re-registration are dropped even when no current registry row points at the retired entry."
    - "retire_embedding works on deactivated catalog entries but rejects dry_run, background, scope, and max_rows."
    - "maintain_vault dispatches retire_embedding to runRetireEmbedding through src/services/maintenance.ts."
  artifacts:
    - path: src/embedding/lifecycle/retire.ts
      provides: "REQ-037 retire transaction orchestration"
    - path: tests/scenarios/directed/testcases/test_retire_embedding_transactional.py
      provides: "D-111 directed scenario"
  key_links:
    - from: src/embedding/lifecycle/retire.ts
      to: src/storage/supabase.ts
      via: "existing DDL naming conventions and per-entry artifacts"
      pattern: "match_documents_|match_memories_|match_records_"
    - from: src/services/maintenance.ts
      to: src/embedding/lifecycle/retire.ts
      via: "maintain_vault action dispatch to runRetireEmbedding"
      pattern: "runRetireEmbedding"
read_first:
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md
---

<objective>
Implement retire transactions and maintain_vault dispatch for `retire_embedding`.

Purpose: Phase 167 must complete destructive lifecycle behavior before records-scope execution and operator recipes can validate the full feature stack.
Output: retire lifecycle module, service dispatch, and directed scenarios D-111 through D-113.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/167-lifecycle-operations-and-validation/167-CONTEXT.md
@.planning/phases/167-lifecycle-operations-and-validation/167-RESEARCH.md
@.planning/phases/167-lifecycle-operations-and-validation/167-PATTERNS.md
@.planning/phases/167-lifecycle-operations-and-validation/167-01-SUMMARY.md
@.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
@.planning/phases/166-embedding-pipeline/166-04-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md
@src/storage/supabase.ts
@src/mcp/tools/plugins.ts
@src/mcp/tools/records.ts
@tests/scenarios/directed/WRITING_SCENARIOS.md

<interfaces>
Contracts from Plan 1:
- Retire must reject invalid parameter combinations before acquiring locks or opening transactions.
Contracts from Plan 2:
- `src/services/maintenance.ts` status/abort dispatch uses durable lifecycle jobs; retire must use the same job ID and lock lifecycle.
- Use Plan 2 durable lifecycle lock/job helpers for retire, status, stale heartbeat, and abort assertions.

Existing implementation contracts:
- Phase 165/166 DDL names core artifacts as `embedding_<name>`, stamp columns, HNSW indexes, `match_memories_<name>`, `match_documents_<name>`, and `match_records_<table>_<name>`.
- Phase 166 stores frozen plugin choices in `fqc_plugin_registry.embedding_name` and plugin record code resolves active choices from that stored value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement retire_embedding transaction and plugin conflict refusal</name>
  <files>src/embedding/lifecycle/retire.ts, tests/scenarios/directed/testcases/test_retire_embedding_transactional.py, tests/scenarios/directed/testcases/test_retire_embedding_plugin_conflict.py, tests/scenarios/directed/testcases/test_retire_embedding_deactivated_entry.py</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md section 6.6.3
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Test Plan.md section 4.8.3, T-S-012 through T-S-014 and D-111 through D-113
    @.planning/phases/167-lifecycle-operations-and-validation/167-01-SUMMARY.md
    @.planning/phases/166-embedding-pipeline/166-04-SUMMARY.md
    @src/storage/supabase.ts
    @src/mcp/tools/plugins.ts
  </read_first>
  <behavior>
    - D-111: successful retire drops `embedding_<name>`, all four stamping columns when `drop_stamping_columns` is true, HNSW indexes, `match_documents_<name>`, `match_memories_<name>`, all `match_records_<plugin_table>_<name>` RPCs, and the `fqc_embeddings` row atomically.
    - D-112: retire against an entry used by a registered plugin returns `conflict` with `details.affected_plugins`.
    - D-113: retire works against a `status = 'deactivated'` catalog entry.
  </behavior>
  <action>Implement `runRetireEmbedding` in `src/embedding/lifecycle/retire.ts`. Validate `embedding_name`, `confirm === embedding_name`, optional `drop_stamping_columns` default true, and reject `scope`, `max_rows`, `dry_run`, `background`, `stale_only`, and `mismatched_width_only` before lock acquisition. Query `fqc_plugin_registry` for active rows whose frozen `embedding_name` equals the target; if any exist, return `conflict` with `details.affected_plugins` listing plugin IDs before destructive DDL. Acquire the per-entry lifecycle lock through Plan 2 helpers. Build a retire artifact inventory for `fqc_documents`, `fqc_memory`, and plugin tables by schema/proc/index discovery, not only current registry rows: inspect registered plugin table names plus database metadata for existing `embedding_<name>` vector columns, `embedding_<name>_model`, `embedding_<name>_dimensions`, `embedding_<name>_provider`, `embedding_<name>_truncated`, `idx_<table>_embedding_<name>`, and `match_records_<plugin_table>_<name>` artifacts. This must include stale plugin artifacts left after a plugin re-registers from X to Y and no longer has `fqc_plugin_registry.embedding_name = X`. Use existing safe SQL identifier helpers such as `validateEmbeddingSqlName` and existing plugin table naming helpers. Execute one PostgreSQL transaction that drops discovered per-entry RPCs, HNSW indexes, vector column, stamp columns when requested, and deletes the `fqc_embeddings` row. Return counts exactly `{ tables_affected, columns_dropped, indexes_dropped, catalog_rows_deleted }`. Create the three directed scenario files named by the external test plan, including an assertion that a stale plugin `embedding_<old>` artifact from re-registration is removed by retire after current plugin conflicts are cleared.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed "test_retire_embedding_*"</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: external requirements §6.6.3 criteria 1-8 are satisfied; D-111..D-113 files exist.
    Command assertions: build exits 0; directed runner positional pattern `test_retire_embedding_*` exits 0.
    Behavior assertions: conflict refusal runs before destructive DDL, deactivated entries retire, stale plugin artifacts left after re-registration are discovered and dropped, and post-retire metadata shows no per-entry core/plugin RPCs or columns.
  </acceptance_criteria>
  <done>Retire is transactional, plugin-conflict-safe, deactivated-compatible, and verified by D-111 through D-113.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire maintain_vault dispatch for retire_embedding</name>
  <files>src/services/maintenance.ts, src/embedding/lifecycle/retire.ts</files>
  <read_first>
    @/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Embedding Purpose Dimensions/Embedding Mgmt and Multi-Provider Support Requirements.md section 6.6.3
    @.planning/phases/167-lifecycle-operations-and-validation/167-01-SUMMARY.md
    @.planning/phases/167-lifecycle-operations-and-validation/167-02-SUMMARY.md
    @src/services/maintenance.ts
  </read_first>
  <behavior>
    - `maintain_vault({ action: "retire_embedding", embedding_name, confirm })` invokes `runRetireEmbedding`.
    - Retire dispatch acquires/releases durable lifecycle lock and persists final counts/status for polling.
    - Invalid retire parameters are rejected before lock acquisition or DDL.
  </behavior>
  <action>Replace the Plan 1 `unsupported` dispatcher placeholder in `src/services/maintenance.ts` with a concrete `retire_embedding` branch that calls `runRetireEmbedding`. The dispatch must pass validated `embedding_name`, `confirm`, and `drop_stamping_columns`, use Plan 2 job helpers for per-entry lock acquisition/status count persistence, and return expected error envelopes through the existing maintenance response path. Retire does not support `dry_run` or `background`; keep those refusals before lock acquisition.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed "test_retire_embedding_*"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: `src/services/maintenance.ts` imports and calls `runRetireEmbedding`; external requirements §6.6.3 criteria 1-8 are satisfied.
    Command assertions: build, retire directed scenarios, and typecheck exit 0.
    Behavior assertions: public MCP calls exercise retire through `maintain_vault`, not direct helper calls.
  </acceptance_criteria>
  <done>`maintain_vault` dispatch to retire is concrete and covered by D-111 through D-113.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller to retire | Caller controls destructive retire action and confirm value. |
| Lifecycle retire to PostgreSQL DDL | Retire executes DROP FUNCTION, DROP INDEX, DROP COLUMN, and DELETE operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-03-01 | Tampering | `runRetireEmbedding` | mitigate | Require `confirm === embedding_name`; reject dry_run/background/scope/max_rows before DDL; validate identifiers with existing safe SQL helpers. |
| T-167-03-02 | Tampering | retire transaction | mitigate | Execute all per-entry artifact drops and catalog deletion in one PostgreSQL transaction. |
| T-167-03-03 | Denial of Service | plugin retire conflict | mitigate | Refuse retire while any plugin registry row stores the target embedding_name and list affected plugin IDs. |
| T-167-03-SC | Tampering | package installs | accept | No npm/pip/cargo installs are planned; if an executor adds one, stop and run the package legitimacy gate from RESEARCH.md first. |
</threat_model>

<verification>
Plan-level automated checks:
- `npm run build`
- `python3 tests/scenarios/directed/run_suite.py --managed "test_retire_embedding_*"`
- `npm run typecheck`
</verification>

<success_criteria>
- D-111 through D-113 scenario files exist and pass.
- Retire drops core and plugin artifacts transactionally and refuses plugin conflicts.
- Retire is invoked through `maintain_vault` dispatch in `src/services/maintenance.ts`.
</success_criteria>

<source_audit>
GOAL covered: operators can retire embeddings safely.
REQ covered: REQ-037.
RESEARCH covered: retire-time RPC drops, plugin conflict refusal, frozen registry choices for conflict detection.
CONTEXT decisions covered: external requirements/test plan are in every task `read_first`; recipe validation remains out of this plan and depends on lifecycle plans.
</source_audit>

<output>
Create `.planning/phases/167-lifecycle-operations-and-validation/167-04-SUMMARY.md` when done.
</output>
