---
phase: 120-cross-phase-atl-validation-coverage-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/scenarios/integration/tests/llm_template_reference_freshness.yml
  - tests/scenarios/integration/tests/llm_template_document_param_freshness.yml
  - tests/scenarios/integration/tests/llm_discovery_then_call.yml
  - tests/scenarios/integration/tests/llm_mixed_reference_modes.yml
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
autonomous: true
requirements:
  - VAL-120
  - TEST-04
must_haves:
  truths:
    - "YAML integration coverage has final local rows for ATL-INT-01, ATL-INT-02, ATL-INT-03, and ATL-INT-05."
    - "ATL-INT-04 remains mapped to the existing TypeScript integration row because no public runtime binding YAML surface exists."
    - "The ATL YAML subset runs through the managed integration runner with PASS or explicit environment SKIP statuses."
  artifacts:
    - path: tests/scenarios/integration/tests/llm_template_reference_freshness.yml
      provides: "ATL-INT-01 YAML scenario"
      contains: "ATL-INT-01"
    - path: tests/scenarios/integration/tests/llm_template_document_param_freshness.yml
      provides: "ATL-INT-02 YAML scenario"
      contains: "ATL-INT-02"
    - path: tests/scenarios/integration/tests/llm_discovery_then_call.yml
      provides: "ATL-INT-03 discovery-to-invocation YAML scenario"
      contains: "ATL-INT-03"
    - path: tests/scenarios/integration/tests/llm_mixed_reference_modes.yml
      provides: "ATL-INT-05 mixed reference modes YAML scenario"
      contains: "ATL-INT-05"
    - path: tests/scenarios/integration/INTEGRATION_COVERAGE.md
      provides: "Final local ATL integration matrix rows"
      contains: "ATL-INT-01"
  key_links:
    - from: tests/scenarios/integration/tests/*.yml
      to: tests/scenarios/integration/INTEGRATION_COVERAGE.md
      via: "coverage IDs declared in YAML header"
      pattern: "coverage: \\[IL-"
    - from: tests/scenarios/integration/run_integration.py
      to: tests/scenarios/integration/INTEGRATION_COVERAGE.md
      via: "managed runner updates Last Passing"
      pattern: "--managed"
---

<objective>
Close the ATL YAML integration rows for reference freshness, document-parameter freshness, discovery-to-invocation closure, and mixed reference modes.

Purpose: Phase 120 must fill only cross-phase YAML gaps while preserving Phase 115's runtime-binding TypeScript integration exception for ATL-INT-04.
Output: Four ATL YAML scenarios or extensions and final `INTEGRATION_COVERAGE.md` rows with passing managed-run evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-CONTEXT.md
@.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-RESEARCH.md
@.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-PATTERNS.md
@.agents/skills/flashquery-integration-testgen/SKILL.md
@.agents/skills/flashquery-integration-run/SKILL.md
@.agents/skills/flashquery-integration-covgen/SKILL.md
@tests/scenarios/integration/README.md
@tests/scenarios/integration/INTEGRATION_COVERAGE.md
@tests/scenarios/integration/tests/llm_ref_reflects_current_write_state.yml
@tests/scenarios/integration/tests/llm_discovery_then_call.yml
@tests/scenarios/integration/tests/llm_reference_syntax_section.yml
@tests/scenarios/integration/tests/llm_mixed_ref_and_id_placeholders.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ATL integration coverage rows before authoring YAML</name>
  <files>tests/scenarios/integration/INTEGRATION_COVERAGE.md</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - tests/scenarios/integration/INTEGRATION_COVERAGE.md
    - .agents/skills/flashquery-integration-covgen/SKILL.md
    - .planning/phases/120-cross-phase-atl-validation-coverage-closure/120-RESEARCH.md
  </read_first>
  <behavior>
    - Matrix has local rows for ATL-INT-01, ATL-INT-02, ATL-INT-03, and ATL-INT-05.
    - Existing ATL-INT-04 row remains mapped to `llm-config-sync.test.ts` with the public-surface rationale intact.
    - New row IDs are local `IL-*` IDs and do not reuse existing IDs.
  </behavior>
  <action>
    Update `tests/scenarios/integration/INTEGRATION_COVERAGE.md` before running YAML, following the matrix row format. Add the next available `IL-*` rows for: `ATL-INT-01: reference/template freshness after document or template update`, `ATL-INT-02: document-parameter freshness after referenced document update`, `ATL-INT-03: discovery-to-invocation closure from list/search discovery to call_model invocation`, and `ATL-INT-05: mixed path/section/pointer references plus alias/list template parameters in one call_model flow`. Set `Covered By` to the intended YAML test names, `Date Updated` to `2026-05-07`, and leave `Last Passing` blank until the managed runner updates it. Preserve the existing `IL-35` ATL-INT-04 TypeScript integration row and do not create a fake YAML runtime-binding test.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/scenarios/integration/INTEGRATION_COVERAGE.md | grep -c 'ATL-INT-01'` returns `1`.
    - `grep -v '^#' tests/scenarios/integration/INTEGRATION_COVERAGE.md | grep -c 'ATL-INT-02'` returns `1`.
    - `grep -v '^#' tests/scenarios/integration/INTEGRATION_COVERAGE.md | grep -c 'ATL-INT-03'` returns `1`.
    - `grep -v '^#' tests/scenarios/integration/INTEGRATION_COVERAGE.md | grep -c 'ATL-INT-05'` returns `1`.
    - `rg -n 'ATL-INT-04.*llm-config-sync\\.test\\.ts' tests/scenarios/integration/INTEGRATION_COVERAGE.md` finds the preserved layer-exception row.
  </acceptance_criteria>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path('tests/scenarios/integration/INTEGRATION_COVERAGE.md').read_text()
for token in ['ATL-INT-01','ATL-INT-02','ATL-INT-03','ATL-INT-04','ATL-INT-05']:
    assert token in text, token
assert 'llm-config-sync.test.ts' in text
PY</automated>
  </verify>
  <done>Coverage rows exist before YAML execution, so the runner can update passing dates for Phase 120 ATL integration scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author ATL-INT-01, ATL-INT-02, and ATL-INT-05 YAML scenarios</name>
  <files>tests/scenarios/integration/tests/llm_template_reference_freshness.yml, tests/scenarios/integration/tests/llm_template_document_param_freshness.yml, tests/scenarios/integration/tests/llm_mixed_reference_modes.yml</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - tests/scenarios/integration/README.md
    - tests/scenarios/integration/tests/llm_template_reference_freshness.yml
    - tests/scenarios/integration/tests/llm_template_document_param_freshness.yml
    - tests/scenarios/integration/tests/llm_mixed_reference_modes.yml
    - tests/scenarios/integration/tests/llm_ref_reflects_current_write_state.yml
    - tests/scenarios/integration/tests/llm_reference_syntax_section.yml
    - tests/scenarios/integration/tests/llm_mixed_ref_and_id_placeholders.yml
    - tests/scenarios/integration/INTEGRATION_COVERAGE.md
    - .agents/skills/flashquery-integration-testgen/SKILL.md
  </read_first>
  <behavior>
    - `llm_template_reference_freshness.yml` updates a template or referenced document and asserts the subsequent `call_model` sees the updated marker.
    - `llm_template_document_param_freshness.yml` updates a document parameter target and asserts template rendering uses the updated document content.
    - `llm_mixed_reference_modes.yml` combines path, section, pointer, alias, and `_items` reference/template modes without using active `{{id:...}}`.
  </behavior>
  <action>
    Create `tests/scenarios/integration/tests/llm_template_reference_freshness.yml` with `coverage: [<local IL row for ATL-INT-01>]` and a description containing `ATL-INT-01`. Use `vault.write`, `update_document`, and `call_model` asserts modeled on `llm_ref_reflects_current_write_state.yml`; include unique marker tokens such as `ATL-INT-01-ALPHA` and `ATL-INT-01-BETA`, and assert only the updated token appears.

    Create `tests/scenarios/integration/tests/llm_template_document_param_freshness.yml` with `coverage: [<local IL row for ATL-INT-02>]` and a description containing `ATL-INT-02`. Seed a `fq_template: true` document with a required document parameter, seed the parameter target, update the parameter target to marker `ATL-INT-02-BETA`, then call `call_model` with `template_params` and assert the response includes the updated marker and not the original marker.

    Create `tests/scenarios/integration/tests/llm_mixed_reference_modes.yml` with `coverage: [<local IL row for ATL-INT-05>]` and a description containing `ATL-INT-05`. Exercise one call_model prompt that includes a direct `{{ref:_integration/...}}`, a `#Section` reference, a `->frontmatter.pointer` reference, and `{{ref:@alias}}` whose `template_params` entry uses `_template` plus `_items` and `_separator`. Do not use active `{{id:...}}`; the ATL release treats it as literal text.
  </action>
  <acceptance_criteria>
    - `test -f tests/scenarios/integration/tests/llm_template_reference_freshness.yml`
    - `test -f tests/scenarios/integration/tests/llm_template_document_param_freshness.yml`
    - `test -f tests/scenarios/integration/tests/llm_mixed_reference_modes.yml`
    - `grep -v '^#' tests/scenarios/integration/tests/llm_template_reference_freshness.yml | grep -c 'ATL-INT-01'` returns at least `1`.
    - `grep -v '^#' tests/scenarios/integration/tests/llm_template_document_param_freshness.yml | grep -c 'ATL-INT-02'` returns at least `1`.
    - `grep -v '^#' tests/scenarios/integration/tests/llm_mixed_reference_modes.yml | grep -c 'ATL-INT-05'` returns at least `1`.
    - `! grep -v '^#' tests/scenarios/integration/tests/llm_mixed_reference_modes.yml | grep -q '{{id:'`
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml, pathlib; [yaml.safe_load(pathlib.Path(p).read_text()) for p in ['tests/scenarios/integration/tests/llm_template_reference_freshness.yml','tests/scenarios/integration/tests/llm_template_document_param_freshness.yml','tests/scenarios/integration/tests/llm_mixed_reference_modes.yml']]"</automated>
  </verify>
  <done>Three new ATL YAML scenarios parse successfully and target their ATL integration behaviors with public MCP calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend discovery-to-invocation YAML and run the ATL subset</name>
  <files>tests/scenarios/integration/tests/llm_discovery_then_call.yml, tests/scenarios/integration/INTEGRATION_COVERAGE.md</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - tests/scenarios/integration/README.md
    - tests/scenarios/integration/tests/llm_discovery_then_call.yml
    - tests/scenarios/integration/INTEGRATION_COVERAGE.md
    - .agents/skills/flashquery-integration-run/SKILL.md
  </read_first>
  <behavior>
    - `llm_discovery_then_call.yml` contains explicit ATL-INT-03 coverage.
    - The ATL YAML subset runs through `run_integration.py --managed`.
    - `INTEGRATION_COVERAGE.md` records the new ATL YAML tests as passing or explicitly environment-skipped in the generated report.
  </behavior>
  <action>
    Extend `tests/scenarios/integration/tests/llm_discovery_then_call.yml` so its `coverage:` includes the local `IL-*` row for `ATL-INT-03`, and add a step sequence that first calls `call_model` discovery (`resolver: "list_purposes"` or `resolver: "search"`) to observe the callable purpose/template metadata, then calls `call_model` using the discovered purpose/tool/template surface in a subsequent assert. Keep this as a real two-step public workflow, not a static duplicate of a normal model call.

    Run the ATL YAML subset with: `python3 tests/scenarios/integration/run_integration.py --managed llm_template_reference_freshness llm_template_document_param_freshness llm_discovery_then_call llm_mixed_reference_modes`. If a scenario skips because a declared dependency is unavailable, record that exact SKIP status in the plan summary and leave the matrix Last Passing field unchanged for that skipped row. If a scenario fails due to a YAML/test bug, fix the YAML. If it fails due to FlashQuery behavior, mark the matrix row `FAIL (2026-05-07)` and suffix the test name with `*`.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/scenarios/integration/tests/llm_discovery_then_call.yml | grep -c 'ATL-INT-03'` returns at least `1`.
    - `rg -n 'llm_template_reference_freshness|llm_template_document_param_freshness|llm_discovery_then_call|llm_mixed_reference_modes' tests/scenarios/integration/INTEGRATION_COVERAGE.md` finds all four test names.
    - `rg -n 'FAIL \\(2026-05-07\\)|2026-05-07' tests/scenarios/integration/INTEGRATION_COVERAGE.md` reflects the run outcome for touched ATL rows.
  </acceptance_criteria>
  <verify>
    <automated>python3 tests/scenarios/integration/run_integration.py --managed llm_template_reference_freshness llm_template_document_param_freshness llm_discovery_then_call llm_mixed_reference_modes</automated>
  </verify>
  <done>The managed ATL YAML subset has been run, and coverage rows reflect PASS/SKIP/FAIL accurately without inventing ATL-INT-04 YAML coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML runner -> managed FlashQuery server | Declarative YAML steps become public MCP tool calls. |
| Managed server -> Supabase test database | Integration runner deletes `fqc_*` rows before/after tests and requires throwaway test DB credentials. |
| YAML `call_model` -> mock/local LLM path | Tests must use configured deterministic integration fixtures, not real provider correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-04 | T | YAML scenario arguments | mitigate | Use public MCP tool arguments from `tests/scenarios/integration/README.md`; validate YAML syntax before running. |
| T-120-05 | I | Integration environment credentials | mitigate | Do not print secrets; rely on `.env.test` and runner-managed config. |
| T-120-06 | D | Supabase cleanup | mitigate | Use `run_integration.py --managed`, which performs built-in cleanup around every test; do not bypass cleanup with custom scripts. |
</threat_model>

<verification>
Run:
```bash
python3 -c "import yaml, pathlib; [yaml.safe_load(p.read_text()) for p in pathlib.Path('tests/scenarios/integration/tests').glob('llm_*atl*.yml')]"
python3 tests/scenarios/integration/run_integration.py --managed llm_template_reference_freshness llm_template_document_param_freshness llm_discovery_then_call llm_mixed_reference_modes
```
</verification>

<success_criteria>
- ATL-INT-01, ATL-INT-02, ATL-INT-03, and ATL-INT-05 have local `IL-*` rows and runnable YAML coverage.
- ATL-INT-04 remains explicitly mapped to `llm-config-sync.test.ts` until a public runtime-binding scenario surface exists.
- YAML outcomes are reflected in `INTEGRATION_COVERAGE.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-02-SUMMARY.md`
</output>
