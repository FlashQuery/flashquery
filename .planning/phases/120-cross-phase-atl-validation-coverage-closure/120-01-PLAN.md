---
phase: 120-cross-phase-atl-validation-coverage-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/call-model-agent-loop.e2e.test.ts
autonomous: true
requirements:
  - VAL-120
  - TEST-04
must_haves:
  truths:
    - "The E2E suite contains explicit ATL-E2E-01 evidence for Mode 1 compatibility and return_messages behavior."
    - "The E2E suite contains explicit ATL-E2E-08 evidence for provider/capability incompatibility failures."
    - "Existing ATL-E2E-02 through ATL-E2E-07 tests still pass after the closure additions."
  artifacts:
    - path: tests/e2e/call-model-agent-loop.e2e.test.ts
      provides: "Cross-phase E2E closure for ATL-E2E-01 and ATL-E2E-08"
      contains: "ATL-E2E-01"
    - path: tests/e2e/call-model-agent-loop.e2e.test.ts
      provides: "Provider compatibility failure E2E evidence"
      contains: "ATL-E2E-08"
  key_links:
    - from: tests/e2e/call-model-agent-loop.e2e.test.ts
      to: src/index.ts
      via: "StdioClientTransport managed MCP server"
      pattern: "StdioClientTransport"
    - from: tests/e2e/call-model-agent-loop.e2e.test.ts
      to: "local OpenAI-compatible mock provider"
      via: "ScriptedOpenAiProvider requests and scripted responses"
      pattern: "ScriptedOpenAiProvider"
---

<objective>
Add explicit cross-phase E2E evidence for the two missing ATL E2E IDs while preserving the already-passing ATL native/template loop gate.

Purpose: Phase 120 must prove the accepted ATL Test Plan E2E taxonomy is represented by runnable local tests, not only lower-tier directed/unit evidence.
Output: `tests/e2e/call-model-agent-loop.e2e.test.ts` contains passing `ATL-E2E-01` and `ATL-E2E-08` cases alongside existing ATL-E2E-02/03/06/07.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-CONTEXT.md
@.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-RESEARCH.md
@.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-PATTERNS.md
@tests/e2e/call-model-agent-loop.e2e.test.ts
@tests/e2e/call-model-template-tools.e2e.test.ts

<interfaces>
Use the existing `ScriptedOpenAiProvider`, `withManagedMcp`, `callModel`, `finalTextResponse`, and `toolCallResponse` patterns in `tests/e2e/call-model-agent-loop.e2e.test.ts`. Keep the tests deterministic and local; do not call a real LLM provider.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ATL-E2E-01 Mode 1 compatibility evidence</name>
  <files>tests/e2e/call-model-agent-loop.e2e.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - tests/e2e/call-model-agent-loop.e2e.test.ts
    - tests/e2e/call-model-template-tools.e2e.test.ts
    - .planning/phases/120-cross-phase-atl-validation-coverage-closure/120-PATTERNS.md
  </read_first>
  <behavior>
    - Test name includes the exact text `ATL-E2E-01`.
    - A resolver=`model`, name=`fast` Mode 1 call returns a text response without `metadata.tools.calls_log`.
    - The same public path with `return_messages: true` returns post-hydration user/assistant messages in the envelope.
    - Discovery resolver raw shape remains outside the Mode 1 envelope when `resolver: "list_models"` is called.
  </behavior>
  <action>
    In `tests/e2e/call-model-agent-loop.e2e.test.ts`, add one focused `it('ATL-E2E-01 ...')` case near the top-level E2E group. Use the existing managed MCP fixture and local `ScriptedOpenAiProvider`. The test must make a public `call_model` request for `resolver: "model", name: "fast"` with `messages: [{ role: "user", content: "ATL-E2E-01 mode one compatibility." }]` and `return_messages: true`, assert `response` equals the scripted text, assert `messages` contains a user message and an assistant message, and assert `metadata.tools` is absent or has no `calls_log`. In the same test or a second `ATL-E2E-01` test, call `resolver: "list_models"` without `messages` and assert the raw discovery payload contains configured model data rather than a `CallModelEnvelope` wrapper. Do not weaken existing ATL-E2E-02/03/06/07 assertions.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/e2e/call-model-agent-loop.e2e.test.ts | grep -c 'ATL-E2E-01'` returns at least `2`.
    - `rg -n "return_messages: true|returnMessages" tests/e2e/call-model-agent-loop.e2e.test.ts` finds the ATL-E2E-01 case.
    - `rg -n "resolver: 'list_models'|resolver: \"list_models\"" tests/e2e/call-model-agent-loop.e2e.test.ts` finds the discovery assertion path.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts -t ATL-E2E-01</automated>
  </verify>
  <done>`ATL-E2E-01` passes through the public MCP E2E harness and proves Mode 1 compatibility plus raw discovery behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ATL-E2E-08 provider compatibility failure evidence</name>
  <files>tests/e2e/call-model-agent-loop.e2e.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - tests/e2e/call-model-agent-loop.e2e.test.ts
    - tests/unit/llm-config.test.ts
    - tests/unit/llm-tool.test.ts
    - .planning/phases/120-cross-phase-atl-validation-coverage-closure/120-RESEARCH.md
  </read_first>
  <behavior>
    - Test name includes the exact text `ATL-E2E-08`.
    - A public `call_model` request requiring model-visible tools fails before provider dispatch when selected model capabilities are false or unknown.
    - A public `call_model` request combining response_format and model-visible tools fails when the selected model lacks `structured_outputs_with_tools`.
    - Provider request capture proves rejected calls do not reach the mock provider.
  </behavior>
  <action>
    Add `ATL-E2E-08` E2E coverage in `tests/e2e/call-model-agent-loop.e2e.test.ts` using a managed config variant in the existing fixture style. Configure one purpose that exposes a native safe tier or named native tool while its selected model declares `tool_calling: false` or omits the required capability, then call `call_model` with `resolver: "purpose", name: "<capability-failing-purpose>"`. Assert the MCP result is an error with a stable capability/admission failure string and assert the mock provider `requests.length` remains `0`. Add a second `ATL-E2E-08` branch only if the existing config/helpers make it straightforward: purpose exposes tools, model has tool calling but lacks `structured_outputs_with_tools`, request includes `parameters: { response_format: { type: "json_object" } }`, and the call fails before provider dispatch. Keep assertions on public MCP result text/JSON and provider request count; do not inspect private DB rows.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/e2e/call-model-agent-loop.e2e.test.ts | grep -c 'ATL-E2E-08'` returns at least `2`.
    - `rg -n "structured_outputs_with_tools|tool_calling|capabil" tests/e2e/call-model-agent-loop.e2e.test.ts` finds the capability failure setup/assertions.
    - `rg -n "requests\\.length.*0|toHaveLength\\(0\\)" tests/e2e/call-model-agent-loop.e2e.test.ts` finds a provider-not-called assertion.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts -t ATL-E2E-08</automated>
  </verify>
  <done>`ATL-E2E-08` passes and proves incompatible provider/model capability paths fail before unsafe delegated calls.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full focused ATL E2E gate</name>
  <files>tests/e2e/call-model-agent-loop.e2e.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - tests/e2e/call-model-agent-loop.e2e.test.ts
    - tests/e2e/call-model-template-tools.e2e.test.ts
  </read_first>
  <action>
    Run the full focused E2E command that preserves existing ATL-E2E-02/03/04/05/06/07 evidence: `npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts tests/e2e/call-model-template-tools.e2e.test.ts`. If an existing test fails because the new helper/config shape changed shared setup, fix the shared setup without weakening any existing assertion text or ATL ID naming.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/e2e/call-model-agent-loop.e2e.test.ts | grep -c 'ATL-E2E-0'` returns at least `7`.
    - `grep -v '^#' tests/e2e/call-model-template-tools.e2e.test.ts | grep -c 'ATL-E2E-0'` returns at least `6`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts tests/e2e/call-model-template-tools.e2e.test.ts</automated>
  </verify>
  <done>The focused ATL E2E suite passes with explicit ATL-E2E-01 through ATL-E2E-08 evidence represented across the E2E files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E MCP client -> FlashQuery subprocess | Public `call_model` input crosses into the server through stdio MCP. |
| FlashQuery -> mock OpenAI-compatible provider | Provider request payloads cross to a local HTTP mock and must not use real credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | I | `tests/e2e/call-model-agent-loop.e2e.test.ts` mock provider | mitigate | Use a local deterministic provider URL and temporary config; do not read or require real provider API keys. |
| T-120-02 | T | Capability-failure E2E assertions | mitigate | Assert rejected Mode 2 calls leave `provider.requests.length === 0`, proving no unsafe provider dispatch occurred. |
| T-120-03 | D | Managed MCP subprocess lifecycle | mitigate | Reuse existing `withManagedMcp` cleanup with `client.close`, `transport.close`, temp-dir removal, and no watch-mode commands. |
</threat_model>

<verification>
Run:
```bash
npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts tests/e2e/call-model-template-tools.e2e.test.ts
```
</verification>

<success_criteria>
- `ATL-E2E-01` and `ATL-E2E-08` appear in passing E2E test names.
- Existing ATL-E2E-02/03/04/05/06/07 tests still pass.
- No real LLM/provider credentials are used.
</success_criteria>

<output>
After completion, create `.planning/phases/120-cross-phase-atl-validation-coverage-closure/120-01-SUMMARY.md`
</output>
