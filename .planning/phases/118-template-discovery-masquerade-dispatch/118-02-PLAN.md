---
phase: 118-template-discovery-masquerade-dispatch
plan: 02
type: execute
wave: 1
depends_on: [118-01]
files_modified:
  - src/llm/template-tools.ts
  - src/llm/reference-resolver.ts
  - src/llm/purpose-template-bindings.ts
  - tests/unit/llm-template-tools.test.ts
  - tests/integration/template-tools.integration.test.ts
autonomous: true
requirements: [TMPL-06, TMPL-07, VAL-118]
must_haves:
  truths:
    - "Template discovery reads current vault frontmatter during each registry assembly."
    - "Only templates with valid `fq_template: true`, `fq_expose_as_tool: true`, valid namespace, non-empty slug, description, and supported params become model-visible tools."
    - "Generated template tool names and reverse maps are deterministic and path-canonical."
  artifacts:
    - path: "src/llm/template-tools.ts"
      provides: "Fresh discovery, contract validation, slug/name/schema generation, diagnostics, and reverse-map assembly"
      exports: ["assembleTemplateToolRegistry", "generateTemplateToolName", "dispatchTemplateToolCall"]
    - path: "src/llm/reference-resolver.ts"
      provides: "Exported template render primitive reused by model-initiated dispatch"
    - path: "src/llm/purpose-template-bindings.ts"
      provides: "Purpose binding lookup helpers for explicit and runtime/API template rows"
  key_links:
    - from: "src/llm/template-tools.ts"
      to: "src/llm/reference-resolver.ts"
      via: "render template with existing validation"
      pattern: "render.*Template"
    - from: "src/llm/template-tools.ts"
      to: "src/llm/purpose-template-bindings.ts"
      via: "purpose template path lookup"
      pattern: "purpose.*template"
    - from: "src/llm/template-tools.ts"
      to: "src/llm/tool-registry.ts"
      via: "OpenAI schema normalization"
      pattern: "normalizeToolJsonSchema"
---

<objective>
Implement the template-tool discovery and assembly helper.

Purpose: TMPL-06 and TMPL-07 require fresh frontmatter discovery, contract validation, generated `flashquery_<fq_namespace>_<slug>` names, and an explicit reverse map before registry merging or dispatch can be correct.
Output: `src/llm/template-tools.ts` plus any narrow exports needed from existing resolver/binding modules.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/118-template-discovery-masquerade-dispatch/118-CONTEXT.md
@.planning/phases/118-template-discovery-masquerade-dispatch/118-RESEARCH.md
@.planning/phases/118-template-discovery-masquerade-dispatch/118-VALIDATION.md
@.planning/phases/118-template-discovery-masquerade-dispatch/118-PATTERNS.md
@.planning/phases/117-agent-loop-executor/117-02-SUMMARY.md
@src/llm/reference-resolver.ts
@src/llm/tool-registry.ts
@src/llm/purpose-template-bindings.ts
@src/config/loader.ts

<interfaces>
From `src/llm/reference-resolver.ts`:
```typescript
export function isTemplateDocument(result: Record<string, unknown>): boolean;
export function normalizeTemplateParamDeclarations(raw: unknown): Record<string, TemplateParamDeclaration>;
export function renderTemplateContent(body: string, values: Record<string, string>, declarations: Record<string, TemplateParamDeclaration>, warnings?: TemplateWarning[]): string;
```

From `src/llm/tool-registry.ts`:
```typescript
export interface OpenAiToolDefinition {
  type: 'function';
  function: { name: string; description: string; parameters: Record<string, unknown>; strict?: true };
}
export function normalizeToolJsonSchema(schema: unknown, options: ToolSchemaNormalizationOptions): Record<string, unknown>;
```
</interfaces>
</context>

<source_authority>
For every task in this plan, source docs win if they conflict with local artifacts unless `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` explicitly narrows Phase 118 scope.
</source_authority>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build fresh template discovery and assembly contracts</name>
  <files>src/llm/template-tools.ts, src/llm/purpose-template-bindings.ts, tests/unit/llm-template-tools.test.ts, tests/integration/template-tools.integration.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-CONTEXT.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-RESEARCH.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-VALIDATION.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-PATTERNS.md
    - src/llm/template-tools.ts
    - src/llm/purpose-template-bindings.ts
    - src/llm/reference-resolver.ts
    - src/llm/tool-registry.ts
    - src/config/loader.ts
    - tests/unit/llm-template-tools.test.ts
    - tests/integration/template-tools.integration.test.ts
  </read_first>
  <behavior>
    - `assembleTemplateToolRegistry(config, purposeName, options)` reads bound template paths fresh with `effectiveInclude: ['body', 'frontmatter']`.
    - `config.templates.defaultAccess === 'permissive'` exposes all current `fq_expose_as_tool: true` templates to purposes without explicit bindings; `restrictive` exposes only explicit persisted/configured bindings.
    - Invalid namespace, missing `fq_desc`, empty slug, unsupported params, non-template docs, non-exposed templates, and dangling paths produce diagnostics and no provider tool.
    - Diagnostics include `template_tools`, `template_tool_warnings`, `dangling_template_paths`, and conflict-ready source entries with canonical `template_path`.
  </behavior>
  <action>
    Create `src/llm/template-tools.ts` as the cohesive helper recommended by D-02 through D-07. Implement `generateTemplateSlug(filenameStem)`, `generateTemplateToolName({ namespace, path })`, `assembleTemplateToolRegistry(...)`, and exported types for `TemplateToolDefinition`, `TemplateToolDiagnostics`, `TemplateToolRegistryAssembly`, and `TemplateToolReverseMap`. Use namespace default `"template"` and regex `/^[a-z][a-z0-9_]*$/`. Slug generation must use filename stem only, lowercase, replace non-alphanumeric runs with `_`, trim leading/trailing `_`, and reject empty. Use `fq_desc` as description, `fq_params` as schema source, and map `type: "document"` to JSON Schema `{ type: "string" }`. Reuse `normalizeToolJsonSchema()` for strict/non-strict object schema normalization. Add binding lookup support in `purpose-template-bindings.ts` only if the existing module lacks a reusable query for runtime rows; preserve YAML/API/webapp precedence semantics.
  </action>
  <acceptance_criteria>
    - `rg "export async function assembleTemplateToolRegistry|export function generateTemplateToolName|TemplateToolReverseMap" src/llm/template-tools.ts`
    - `rg "fq_namespace|fq_expose_as_tool|fq_desc|fq_params|defaultAccess|permissive|restrictive" src/llm/template-tools.ts`
    - `rg "flashquery\\.\\$\\{namespace\\}|flashquery\\." src/llm/template-tools.ts`
    - `rg "normalizeToolJsonSchema|type: 'document'|type: 'string'" src/llm/template-tools.ts`
    - `rg "effectiveInclude: \\['body', 'frontmatter'\\]" src/llm/template-tools.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-template-tools.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/template-tools.integration.test.ts</automated>
  </verify>
  <done>Fresh template assembly produces provider tools, reverse-map entries, and diagnostics matching D-02 through D-05 and the tests from 118-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose a reusable template dispatch render primitive</name>
  <files>src/llm/reference-resolver.ts, src/llm/template-tools.ts, tests/unit/llm-template-tools.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-CONTEXT.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-RESEARCH.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-VALIDATION.md
    - .planning/phases/118-template-discovery-masquerade-dispatch/118-PATTERNS.md
    - src/llm/reference-resolver.ts
    - src/llm/template-tools.ts
    - tests/unit/reference-resolver.test.ts
    - tests/unit/llm-template-tools.test.ts
  </read_first>
  <behavior>
    - Template-tool dispatch reuses the same `string` and `document` parameter validation, defaults, document identifier ladder, substitution, warnings, and non-recursive behavior as host-authored template references.
    - Missing required params surface `template_missing_required_param`; document params that cannot resolve surface `template_param_doc_not_found`; wrong types surface `template_param_invalid_type`.
  </behavior>
  <action>
    Refactor the existing private `renderTemplateReference(...)` logic in `src/llm/reference-resolver.ts` into an exported helper such as `renderTemplateDocument(...)` without changing host reference behavior. The helper must accept a resolved template document record, raw params, config, Supabase manager, embedding provider, and logger, and return rendered content, params used, and warnings. `src/llm/template-tools.ts` must call this helper for model-initiated template dispatch instead of duplicating template validation or substitution. Keep `TemplateReferenceError` handling internal or expose a typed result wrapper; do not make callers regex-classify thrown strings.
  </action>
  <acceptance_criteria>
    - `rg "export .*renderTemplate" src/llm/reference-resolver.ts`
    - `rg "renderTemplate.*from './reference-resolver|renderTemplate" src/llm/template-tools.ts`
    - `rg "template_missing_required_param|template_param_doc_not_found|template_param_invalid_type" tests/unit/llm-template-tools.test.ts`
    - `npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-template-tools.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-template-tools.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Template-tool rendering uses the Phase 114 resolver behavior directly and keeps host reference tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault frontmatter to provider tool schema | User-authored markdown frontmatter becomes model-visible tool metadata. |
| Purpose binding to template discovery | YAML/API/runtime bindings select which vault templates a delegated model may see. |
| Template params to document resolver | Model-supplied document identifiers cross into vault/database lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-04 | Tampering | `src/llm/template-tools.ts` | mitigate | Validate `fq_namespace`, `fq_desc`, `fq_expose_as_tool`, slug, and `fq_params` before adding provider tools. |
| T-118-05 | Information Disclosure | permissive template discovery | mitigate | Respect `templates.defaultAccess` and purpose bindings exactly; diagnostics expose dangling paths but not template body content. |
| T-118-06 | Denial of Service | fresh vault reads | accept | Phase 118 requires fresh reads; per-template work is bounded to purpose bindings or current exposed templates and uses existing document resolver limits. |
| T-118-07 | Elevation of Privilege | document params | mitigate | Reuse standard document identifier resolution and existing path-safety checks instead of manual file reads. |
</threat_model>

<verification>
Focused unit and integration commands pass. `npm run build` passes after the export/refactor.
</verification>

<success_criteria>
- `src/llm/template-tools.ts` owns fresh discovery, validation, name generation, schema generation, diagnostics, and reverse-map assembly.
- Host-authored template reference behavior from Phase 114 remains green.
- Generated provider tools use only source-doc-approved names and schema fields.
</success_criteria>

<output>
After completion, create `.planning/phases/118-template-discovery-masquerade-dispatch/118-02-SUMMARY.md`
</output>
