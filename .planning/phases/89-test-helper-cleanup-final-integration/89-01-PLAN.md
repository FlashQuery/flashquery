---
phase: 89-test-helper-cleanup-final-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/discovery-coordinator.test.ts
  - tests/unit/record-tools.test.ts
  - tests/unit/pending-plugin-review.test.ts
autonomous: true
requirements:
  - TEST-11
must_haves:
  truths:
    - "tests/unit/discovery-coordinator.test.ts does not exist (deleted or was absent)"
    - "npm test exits 0 for record-tools.test.ts (no v2.8-attributable failures)"
    - "npm test exits 0 for pending-plugin-review.test.ts (no v2.8-attributable failures)"
  artifacts:
    - path: "tests/unit/record-tools.test.ts"
      provides: "Updated reconcilePluginDocuments mock expectation with 3rd arg"
      contains: "toHaveBeenCalledWith('crm', 'default', expect.any(String))"
    - path: "tests/unit/pending-plugin-review.test.ts"
      provides: "setupTool() passes config with instance.id so clear mode resolves correctly"
      contains: "instance: { id: 'test-instance' }"
  key_links:
    - from: "tests/unit/record-tools.test.ts"
      to: "src/mcp/tools/records.ts"
      via: "mock expectation must match 3-arg call signature"
      pattern: "reconcilePluginDocuments.*crm.*default.*expect\\.any"
    - from: "tests/unit/pending-plugin-review.test.ts"
      to: "src/mcp/tools/pending-review.ts"
      via: "config.instance.id must be defined for delete branch to execute"
      pattern: "instance.*test-instance"
---

<objective>
Fix the two v2.8-attributable unit test regressions that are currently failing.

Purpose: Clear the unit test baseline of all v2.8-caused failures so subsequent waves have a clean foundation to build on.
Output: Both failing tests pass; the 20 pre-existing deferred failures remain unaffected.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/.planning/PROJECT.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/.planning/ROADMAP.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Delete tests/unit/discovery-coordinator.test.ts if it exists (D-08)</name>
  <read_first>
    - tests/unit/discovery-coordinator.test.ts (check existence only — do not read further if it exists, just delete it)
  </read_first>
  <files>tests/unit/discovery-coordinator.test.ts</files>
  <action>
    Check whether `tests/unit/discovery-coordinator.test.ts` exists. The discovery coordinator service was deleted in Phase 88; any surviving test file for it imports deleted modules and causes TypeScript compilation failure, blocking the full suite.

    Run: `ls tests/unit/discovery-coordinator.test.ts 2>/dev/null`

    - If the file DOES NOT exist: nothing to do; log "discovery-coordinator.test.ts not found, skipping" and proceed.
    - If the file EXISTS: delete it with `rm tests/unit/discovery-coordinator.test.ts`.

    Do NOT modify or rewrite the file. Delete it entirely. Per D-08: the discovery coordinator was deleted in Phase 88 and this test file should not exist.
  </action>
  <verify>
    <automated>ls tests/unit/discovery-coordinator.test.ts 2>&1 | grep -E "No such file|not found" || echo "FILE DELETED OR NEVER EXISTED"</automated>
  </verify>
  <done>tests/unit/discovery-coordinator.test.ts does not exist on the filesystem. npm test no longer attempts to compile it.</done>
</task>

<task type="auto">
  <name>Task 1: Fix record-tools.test.ts — reconcilePluginDocuments 3-arg expectation</name>
  <read_first>
    - tests/unit/record-tools.test.ts (lines 955–975: the failing test and its mock setup)
    - src/mcp/tools/records.ts (line 159: the 3-arg call site being mocked)
  </read_first>
  <files>tests/unit/record-tools.test.ts</files>
  <action>
    Find line ~972 in tests/unit/record-tools.test.ts containing:
    ```
    expect(reconcilePluginDocuments).toHaveBeenCalledWith('crm', 'default');
    ```
    Replace it with:
    ```
    expect(reconcilePluginDocuments).toHaveBeenCalledWith('crm', 'default', expect.any(String));
    ```

    Background: `src/mcp/tools/records.ts` now calls `reconcilePluginDocuments(plugin_id, instanceName, config.supabase.databaseUrl)` with a third argument (the database URL from config). The old 2-arg assertion fails because the mock receives 3 arguments. The fix uses `expect.any(String)` for the third argument so the test does not depend on the specific URL value from `makeConfig()`.

    Do NOT change any other assertions in this file. Do NOT modify the mock setup for `reconcilePluginDocuments`. Only change the single `toHaveBeenCalledWith` call on line ~972.
  </action>
  <verify>
    <automated>cd /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery && npm test -- --reporter=verbose tests/unit/record-tools.test.ts 2>&1 | grep -E "PASS|FAIL|create_record calls reconcile"</automated>
  </verify>
  <done>The test "create_record calls reconcilePluginDocuments before core op" passes. npm test for record-tools.test.ts shows no failures on the reconciliation assertion test.</done>
</task>

<task type="auto">
  <name>Task 2: Fix pending-plugin-review.test.ts — pass config with instance.id to setupTool()</name>
  <read_first>
    - tests/unit/pending-plugin-review.test.ts (lines 50–66: setupTool() function; lines 134–154: failing "clear mode" test)
    - src/mcp/tools/pending-review.ts (lines 52–66: clear mode delete chain, reads config.instance.id as fqcInstanceId)
  </read_first>
  <files>tests/unit/pending-plugin-review.test.ts</files>
  <action>
    The root cause: `setupTool()` at line 62 passes `{} as FlashQueryConfig` to `registerPendingReviewTools()`. Inside the tool handler, `fqcInstanceId = config?.instance?.id` evaluates to `undefined`. The production code at `src/mcp/tools/pending-review.ts` lines 54–59 executes:
    ```typescript
    .eq('instance_id', fqcInstanceId)  // 'instance_id' = undefined
    ```
    While the mock chain still resolves, the UUID validation on `fqc_ids` values (if any) may cause an early return before the delete branch. The safest fix is to provide a config with a real `instance.id`.

    Change line 62 in `setupTool()` from:
    ```typescript
    registerPendingReviewTools(mockServer, {} as FlashQueryConfig);
    ```
    to:
    ```typescript
    registerPendingReviewTools(mockServer, { instance: { id: 'test-instance' } } as unknown as FlashQueryConfig);
    ```

    This ensures `fqcInstanceId` is the string `'test-instance'` when the tool handler runs. The UUID validation on `fqc_ids` elements still passes because the test uses values like `'some-uuid-1'` (UUID format validation may be lenient — check pending-review.ts for any strict UUID guard; if there is a strict UUID guard that rejects `'some-uuid-1'`, change the test values to proper UUID format: `'aaaaaaaa-0000-0000-0000-000000000001'` and `'aaaaaaaa-0000-0000-0000-000000000002'` in the "clear mode" test at line 140–143).

    Do NOT change makeSupabaseChain or any other test. Only change the `setupTool()` function and, if needed, the fqc_ids values in the "clear mode calls DELETE" test.
  </action>
  <verify>
    <automated>cd /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery && npm test -- --reporter=verbose tests/unit/pending-plugin-review.test.ts 2>&1 | grep -E "PASS|FAIL|clear mode calls DELETE"</automated>
  </verify>
  <done>The test "clear mode calls DELETE then returns remaining items" passes. npm test for pending-plugin-review.test.ts shows 0 failures (all 6 tests pass or any pre-existing ones remain unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production mock | Test assertions verify mock call signatures; no production data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Tampering | test assertions | accept | Test-only file changes; no production code modified; mock values are hardcoded test strings with no security surface |
</threat_model>

<verification>
Run after both tasks complete:
```
cd /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery && npm test -- tests/unit/record-tools.test.ts tests/unit/pending-plugin-review.test.ts
```
Both files should pass with 0 v2.8-attributable failures. The 20 pre-existing deferred failures (in auth-middleware, git-manager, config, embedding, compound-tools, resolve-document test files) remain unaffected.
</verification>

<success_criteria>
- `grep "toHaveBeenCalledWith('crm', 'default', expect.any(String))" tests/unit/record-tools.test.ts` returns a match
- `grep "instance: { id: 'test-instance' }" tests/unit/pending-plugin-review.test.ts` returns a match
- `npm test -- tests/unit/record-tools.test.ts` exits 0 with the reconciliation test passing
- `npm test -- tests/unit/pending-plugin-review.test.ts` exits 0 with the "clear mode" test passing
</success_criteria>

<output>
After completion, create `.planning/phases/89-test-helper-cleanup-final-integration/89-01-SUMMARY.md`
</output>
