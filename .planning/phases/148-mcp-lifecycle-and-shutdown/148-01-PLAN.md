---
phase: 148-mcp-lifecycle-and-shutdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/request-lifecycle.ts
  - tests/unit/mcp-request-drain.test.ts
autonomous: true
requirements:
  - REQ-009
must_haves:
  truths:
    - "In-flight MCP request tracking can be exercised without starting a full MCP server."
    - "Success, isError results, and thrown handlers each decrement exactly once."
    - "Hung handlers produce timeout metadata with the remaining in-flight count."
  artifacts:
    - path: "src/mcp/request-lifecycle.ts"
      provides: "Dependency-light MCP request lifecycle tracker and typed handler wrapper helpers"
      exports: ["McpDrainResult", "McpRequestLifecycle", "createMcpRequestLifecycle"]
    - path: "tests/unit/mcp-request-drain.test.ts"
      provides: "T-U-019 and T-U-020 unit coverage"
  key_links:
    - from: "src/mcp/request-lifecycle.ts"
      to: "tests/unit/mcp-request-drain.test.ts"
      via: "exported lifecycle helper API"
      pattern: "createMcpRequestLifecycle"
---

<objective>
Create the request lifecycle contract that later wrapper and shutdown plans consume.

Purpose: REQ-009 needs a small, dependency-light helper that can prove request counting and wait-for-idle behavior before it is wired into production MCP registration.
Output: `src/mcp/request-lifecycle.ts` plus focused unit coverage for T-U-019 and T-U-020.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-RESEARCH.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-PATTERNS.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md
@src/llm/cost-tracker.ts
@src/logging/context.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing request lifecycle drain tests</name>
  <files>tests/unit/mcp-request-drain.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-VALIDATION.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md
    - src/llm/cost-tracker.ts
    - tests/unit/shutdown.test.ts
  </read_first>
  <behavior>
    - T-U-019: A tracked handler that returns a normal result increments while running and decrements to zero after completion.
    - T-U-019: A tracked handler that returns `{ isError: true }` also decrements to zero after completion.
    - T-U-019: A tracked handler that throws decrements to zero before the thrown error reaches the caller.
    - T-U-020: A never-settling tracked handler causes `waitForIdle(shortTimeoutMs)` to resolve with `timedOut: true` and `remaining: 1`.
  </behavior>
  <action>Create `tests/unit/mcp-request-drain.test.ts` with Vitest tests that import the planned `createMcpRequestLifecycle` API from `src/mcp/request-lifecycle.ts`. Assert exact active-count transitions through an exported `getInFlightCount()` or equivalent read-only inspection method. Use short test timeouts such as 20-50ms for timeout metadata tests; do not wait 15 seconds in unit tests.</action>
  <verify>
    <automated>npm test -- tests/unit/mcp-request-drain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new test file contains explicit T-U-019 and T-U-020 references.
    - The focused test command fails before `src/mcp/request-lifecycle.ts` exists or before the API is implemented.
    - No test uses a production 15-second wait.
  </acceptance_criteria>
  <done>RED coverage exists for exact counter balance and timeout metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dependency-light MCP request lifecycle helper</name>
  <files>src/mcp/request-lifecycle.ts, tests/unit/mcp-request-drain.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-RESEARCH.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md
    - src/llm/cost-tracker.ts
    - src/mcp/request-lifecycle.ts
    - tests/unit/mcp-request-drain.test.ts
  </read_first>
  <action>Create `src/mcp/request-lifecycle.ts` exporting `McpDrainResult`, `McpRequestLifecycle`, and `createMcpRequestLifecycle`. The lifecycle must provide `trackHandler(handler)`, `waitForIdle(timeoutMs)`, and `getInFlightCount()` or an equivalent read-only count method. `trackHandler` must increment immediately before invoking the real handler and decrement in `finally` so success, `isError` envelopes, and thrown failures all balance. `waitForIdle(0 or positive timeoutMs)` must return promptly when idle and return `{ timedOut, remaining, elapsedMs }` metadata; on timeout it must not clear active work. Do not import server shutdown code, transport objects, Supabase, or session state.</action>
  <verify>
    <automated>npm test -- tests/unit/mcp-request-drain.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/request-lifecycle.ts` exports the lifecycle types/functions named in this task.
    - T-U-019 passes for success, `isError: true`, and thrown-handler paths.
    - T-U-020 passes with timeout metadata containing `timedOut: true` and `remaining: 1`.
    - `rg -n "setTimeout\\(15_000|15000" tests/unit/mcp-request-drain.test.ts` returns no matches.
  </acceptance_criteria>
  <done>Lifecycle helper is implemented and unit-tested without production shutdown wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP handler wrapper -> lifecycle helper | Untrusted tool call execution crosses into process-level request accounting. |
| Shutdown coordinator -> lifecycle helper | Process shutdown consumes drain metadata to decide whether active handlers remain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01-DOS | Denial of Service | `src/mcp/request-lifecycle.ts` | mitigate | Use `try/finally` decrement in `trackHandler`; unit-test success, `isError`, and thrown paths. |
| T-148-01-TAMPER | Tampering | `waitForIdle` metadata | mitigate | Return `remaining` from actual active count and do not mutate active requests on timeout. |
| T-148-01-REP | Repudiation | drain result | mitigate | Include `elapsedMs`, `timedOut`, and `remaining` so later shutdown logging can report factual drain outcome. |
| T-148-SC | Tampering | npm installs | accept | No package-manager installs are planned; if execution adds one, stop for slopcheck/package legitimacy gate. |
</threat_model>

<verification>
Run `npm test -- tests/unit/mcp-request-drain.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
REQ-009 has a tested lifecycle helper ready for production wrapper and shutdown integration.
</success_criteria>

<output>
Create `.planning/phases/148-mcp-lifecycle-and-shutdown/148-01-SUMMARY.md` when done.
</output>
