---
phase: 148-mcp-lifecycle-and-shutdown
plan: 03
type: execute
wave: 3
depends_on:
  - 148-02
files_modified:
  - src/server/shutdown.ts
  - tests/unit/shutdown.test.ts
  - tests/integration/server/shutdown-mcp-drain.test.ts
  - tests/config/vitest.integration.config.ts
autonomous: true
requirements:
  - REQ-009
must_haves:
  truths:
    - "Shutdown returns promptly when no MCP handlers are in flight."
    - "Shutdown waits for an already-running MCP handler to settle before continuing."
    - "Shutdown logs a warning with remaining in-flight count when the MCP drain deadline expires."
    - "The production MCP drain deadline is 15 seconds."
  artifacts:
    - path: "src/server/shutdown.ts"
      provides: "15-second MCP drain integration before cost-write drain"
      contains: "15_000"
    - path: "tests/integration/server/shutdown-mcp-drain.test.ts"
      provides: "T-I-009, T-I-010, and T-I-011 coverage"
    - path: "tests/config/vitest.integration.config.ts"
      provides: "Curated integration test inclusion for shutdown MCP drain tests"
  key_links:
    - from: "src/server/shutdown.ts"
      to: "src/mcp/request-lifecycle.ts"
      via: "waitForIdle(15_000)"
      pattern: "waitForIdle"
    - from: "tests/config/vitest.integration.config.ts"
      to: "tests/integration/server/shutdown-mcp-drain.test.ts"
      via: "integration include list"
      pattern: "shutdown-mcp-drain"
---

<objective>
Replace placeholder MCP shutdown sleep with real lifecycle drain behavior and integration coverage.

Purpose: REQ-009 requires shutdown to drain active MCP requests with a 15-second deadline, return promptly when idle, and warn with the remaining count on timeout.
Output: `src/server/shutdown.ts` uses the lifecycle helper and focused integration tests cover idle, active, and hung shutdown behavior.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-RESEARCH.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-PATTERNS.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-VALIDATION.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-01-SUMMARY.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-02-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md
@src/server/shutdown.ts
@src/mcp/request-lifecycle.ts
@tests/unit/shutdown.test.ts
@tests/integration/shutdown.integration.test.ts
@tests/config/vitest.integration.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shutdown MCP drain integration tests</name>
  <files>tests/integration/server/shutdown-mcp-drain.test.ts, tests/config/vitest.integration.config.ts, tests/unit/shutdown.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-VALIDATION.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Test Plan.md
    - src/server/shutdown.ts
    - src/mcp/request-lifecycle.ts
    - tests/unit/shutdown.test.ts
    - tests/integration/shutdown.integration.test.ts
    - tests/config/vitest.integration.config.ts
  </read_first>
  <behavior>
    - T-I-009: Shutdown with zero in-flight MCP requests returns promptly and does not include a fixed 100ms delay.
    - T-I-010: Shutdown waits for an already-started tracked handler to resolve before continuing to later shutdown steps.
    - T-I-011: A hung tracked handler causes the drain to time out and emits a warning containing the remaining count.
  </behavior>
  <action>Create `tests/integration/server/shutdown-mcp-drain.test.ts` using existing ShutdownCoordinator and logger mocking patterns. Add the new file to `tests/config/vitest.integration.config.ts` if the integration config uses a curated include list. The tests may drive the lifecycle helper directly through the production shutdown coordinator instead of spinning up a real Supabase-backed tool; they must prove the coordinator consumes the same exported lifecycle instance used by `src/mcp/server.ts`. Add or adjust `tests/unit/shutdown.test.ts` only for production constant/static assertions such as the 15-second drain value if that is the clearer fast gate.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/server/shutdown-mcp-drain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The integration test file exists and references T-I-009, T-I-010, and T-I-011.
    - Idle test asserts elapsed time below 100ms or another tight threshold that proves there is no unconditional 100ms sleep.
    - Active test starts the tracked handler before triggering shutdown.
    - Hung test asserts a warning message includes the remaining in-flight count.
  </acceptance_criteria>
  <done>Integration coverage exists for all required REQ-009 shutdown drain behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ShutdownCoordinator to waitForIdle with 15-second deadline</name>
  <files>src/server/shutdown.ts, tests/unit/shutdown.test.ts, tests/integration/server/shutdown-mcp-drain.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-RESEARCH.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Test Plan.md
    - src/server/shutdown.ts
    - src/mcp/request-lifecycle.ts
    - tests/unit/shutdown.test.ts
    - tests/integration/server/shutdown-mcp-drain.test.ts
  </read_first>
  <action>Replace the `drainMcpRequests()` placeholder in `src/server/shutdown.ts` with a call to the MCP lifecycle helper's `waitForIdle(15_000)` or an exported `drainMcpRequests(15_000)` wrapper. Keep this step before `drainCostWritesStep()`. Log an info message before the drain that includes the 15-second timeout. If the drain result has `timedOut: true`, call `this.logWarn(...)` with a message containing the exact remaining in-flight count. If idle, return promptly with no sleep. Do not add transport session state or per-client session state.</action>
  <verify>
    <automated>npm test -- tests/unit/shutdown.test.ts tests/unit/mcp-request-drain.test.ts</automated>
    <automated>npm run test:integration -- tests/integration/server/shutdown-mcp-drain.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/shutdown.ts` no longer contains the placeholder 100ms `setTimeout` MCP drain.
    - The production MCP drain deadline is 15_000ms.
    - Timeout logging includes the remaining in-flight request count.
    - T-I-009 through T-I-011 pass through the focused integration command.
  </acceptance_criteria>
  <done>Shutdown drains MCP requests with the required 15-second deadline and observable timeout metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS signal -> ShutdownCoordinator | External termination starts resource cleanup. |
| ShutdownCoordinator -> MCP lifecycle | Shutdown decides whether active handlers have settled. |
| active MCP handler -> resource cleanup | In-flight writes must complete or be reported before dependent resources close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-03-DOS | Denial of Service | `src/server/shutdown.ts` | mitigate | Use deadline-bounded `waitForIdle(15_000)` so hung handlers do not deadlock shutdown. |
| T-148-03-TAMPER | Tampering | shutdown during write | mitigate | Drain active tracked handlers before cost writes and resource cleanup; integration-test active handler sequencing. |
| T-148-03-REP | Repudiation | timeout path | mitigate | Warn with remaining in-flight count when timed out instead of returning false success. |
| T-148-03-INFO | Information Disclosure | shutdown logs | accept | Warning includes count only, not request arguments or document contents. |
| T-148-SC | Tampering | npm installs | accept | No package installs are planned; if execution adds one, stop for slopcheck/package legitimacy gate. |
</threat_model>

<verification>
Run focused unit and integration commands plus `npm run typecheck`.
</verification>

<success_criteria>
REQ-009 shutdown behavior is fully implemented and integration-tested.
</success_criteria>

<output>
Create `.planning/phases/148-mcp-lifecycle-and-shutdown/148-03-SUMMARY.md` when done.
</output>
