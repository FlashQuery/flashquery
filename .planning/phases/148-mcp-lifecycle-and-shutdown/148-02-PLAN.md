---
phase: 148-mcp-lifecycle-and-shutdown
plan: 02
type: execute
wave: 2
depends_on:
  - 148-01
files_modified:
  - src/mcp/server.ts
  - src/mcp/tool-catalog.ts
  - tests/unit/mcp-server-correlation.test.ts
  - tests/unit/native-tool-catalog.test.ts
autonomous: true
requirements:
  - REQ-008
  - REQ-009
must_haves:
  truths:
    - "Production MCP registration uses one typed `registerTool` wrapping path."
    - "Dead `server.tool` wrapping is removed unless a production caller is added and tested."
    - "Correlation-ID context and native tool catalog capture both still work."
    - "Registered handlers enter the lifecycle tracker created in Plan 148-01."
  artifacts:
    - path: "src/mcp/server.ts"
      provides: "Typed wrapper composition for correlation IDs, request lifecycle, and search_tools handler substitution"
      contains: "McpServer['registerTool']"
    - path: "src/mcp/tool-catalog.ts"
      provides: "Native catalog capture and host exposure filtering"
      contains: "McpServer['registerTool']"
    - path: "tests/unit/mcp-server-correlation.test.ts"
      provides: "T-U-017 and T-U-018 coverage"
    - path: "tests/unit/native-tool-catalog.test.ts"
      provides: "T-U-016 regression coverage"
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/mcp/request-lifecycle.ts"
      via: "tracked registered handler wrapper"
      pattern: "trackHandler"
    - from: "src/mcp/server.ts"
      to: "src/logging/context.ts"
      via: "initializeContext and generateCorrelationId"
      pattern: "initializeContext"
    - from: "src/mcp/tool-catalog.ts"
      to: "hostEnabledToolNames"
      via: "catalog capture before host exposure filtering"
      pattern: "hostEnabledToolNames"
---

<objective>
Consolidate MCP server registration wrapping around a typed `registerTool` path and wire registered handlers into lifecycle tracking.

Purpose: REQ-008 requires eliminating broad wrapper casts and dead `.tool` wrapping while preserving correlation IDs and native tool catalog behavior. REQ-009 requires in-flight tracking to sit on the registered-handler path.
Output: Typed wrapper consolidation in `src/mcp/server.ts` / `src/mcp/tool-catalog.ts`, plus focused unit coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-RESEARCH.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-PATTERNS.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-VALIDATION.md
@.planning/phases/148-mcp-lifecycle-and-shutdown/148-01-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md
@src/mcp/server.ts
@src/mcp/tool-catalog.ts
@src/mcp/request-lifecycle.ts
@src/logging/context.ts
@tests/unit/native-tool-catalog.test.ts
@tests/unit/mcp-server-tools.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add correlation and no-dead-tool-branch tests</name>
  <files>tests/unit/mcp-server-correlation.test.ts, tests/unit/native-tool-catalog.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-VALIDATION.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Test Plan.md
    - src/mcp/server.ts
    - src/mcp/tool-catalog.ts
    - src/logging/context.ts
    - tests/unit/native-tool-catalog.test.ts
    - tests/unit/mcp-server-tools.test.ts
  </read_first>
  <behavior>
    - T-U-017: A handler registered through `registerTool` observes a defined `getCurrentCorrelationId()` inside the handler body.
    - T-U-017: Two handler invocations receive non-empty correlation IDs, and the test does not require a stable ID value.
    - T-U-018: Production wrapper coverage proves registration works through `registerTool` without using a `.tool` branch.
    - T-U-016: Existing native tool catalog tests still assert tool names and injected help schemas.
  </behavior>
  <action>Create `tests/unit/mcp-server-correlation.test.ts` using the local capturing-server pattern from `tests/unit/mcp-server-tools.test.ts`. Exercise the wrapper path through `createMcpServer(config, 'test')` or an exported focused wrapper helper if the executor extracts one. Add or adjust `tests/unit/native-tool-catalog.test.ts` only to label/strengthen T-U-016 assertions; do not weaken existing catalog or help-schema expectations.</action>
  <verify>
    <automated>npm test -- tests/unit/mcp-server-correlation.test.ts tests/unit/native-tool-catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/mcp-server-correlation.test.ts` exists and references T-U-017 and T-U-018.
    - The test invokes a `registerTool`-registered handler and asserts `getCurrentCorrelationId()` is defined inside it.
    - `tests/unit/native-tool-catalog.test.ts` still asserts catalog names and help schema injection.
  </acceptance_criteria>
  <done>Focused RED/updated tests pin correlation, no `.tool` dependency, and catalog behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace broad correlation monkey-patch with typed registerTool composition</name>
  <files>src/mcp/server.ts, src/mcp/tool-catalog.ts, tests/unit/mcp-server-correlation.test.ts, tests/unit/native-tool-catalog.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-CONTEXT.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-RESEARCH.md
    - .planning/phases/148-mcp-lifecycle-and-shutdown/148-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Requirements/Codebase Audit Priority Remediation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Tech Debt/Codebase Audit (23-May-2026)/Codebase Audit Priority Remediation Test Plan.md
    - src/mcp/server.ts
    - src/mcp/tool-catalog.ts
    - src/mcp/request-lifecycle.ts
    - src/logging/context.ts
    - tests/unit/mcp-server-correlation.test.ts
    - tests/unit/native-tool-catalog.test.ts
  </read_first>
  <action>In `src/mcp/server.ts`, delete the `server.tool` wrapping branch and replace `(server as any).registerTool` correlation monkey-patching with a `McpServer['registerTool']` typed wrapper. The registered callback must be wrapped so each invocation first enters the Plan 148-01 lifecycle tracker and runs the original handler inside `initializeContext(generateCorrelationId(), ...)`. Preserve the current startup order: install correlation/lifecycle wrapping before `wrapServerWithToolCatalog`, then preserve the `search_tools` handler substitution without adding another broad `any` cast. In `src/mcp/tool-catalog.ts`, keep catalog capture before host exposure filtering and keep `registerUncatalogedTool` using the uncataloged original registration path. If a shared `RegisterToolFunction` type is useful, export it from one MCP module and use it consistently; do not introduce a production `.tool()` caller.</action>
  <verify>
    <automated>npm test -- tests/unit/mcp-server-correlation.test.ts tests/unit/native-tool-catalog.test.ts tests/unit/mcp-request-drain.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>rg -n "server\\.tool|\\(server as any\\)\\.registerTool|\\(server as any\\)\\.tool" src/mcp src/server src/llm; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/server.ts` no longer contains a `server.tool` wrapping branch.
    - Touched wrapper code uses `McpServer['registerTool']` or an equivalent typed function shape.
    - No touched wrapper path uses `(server as any).registerTool` or `(server as any).tool`.
    - Correlation, lifecycle drain unit tests, and native catalog tests all pass.
    - Host exposure filtering still records hidden tools in the native catalog before deciding whether to call the SDK registration.
  </acceptance_criteria>
  <done>Typed wrapper composition replaces broad monkey-patching and preserves catalog/correlation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP SDK -> FlashQuery wrapper | SDK invokes registered callbacks through the wrapper chain. |
| wrapper -> native tool catalog | Registration metadata is captured for native dispatch and host filtering. |
| wrapper -> logging context | Correlation IDs enter AsyncLocalStorage for each request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-02-SDK | Tampering | `src/mcp/server.ts` wrapper typing | mitigate | Use `McpServer['registerTool']` and remove broad `(server as any).registerTool` casts so SDK signature drift is type-visible. |
| T-148-02-CAT | Elevation of Privilege | `src/mcp/tool-catalog.ts` host filtering | mitigate | Preserve catalog capture before `hostEnabledToolNames` filtering and run T-U-016. |
| T-148-02-REP | Repudiation | correlation wrapper | mitigate | Run each handler inside fresh `initializeContext(generateCorrelationId(), ...)` and assert with T-U-017. |
| T-148-02-DOS | Denial of Service | lifecycle wrapper | mitigate | Wrap registered handlers with Plan 148-01 `trackHandler` so counter balance is shared with T-U-019. |
| T-148-SC | Tampering | npm installs | accept | No package installs are planned; if execution adds one, stop for slopcheck/package legitimacy gate. |
</threat_model>

<verification>
Run focused unit tests, `npm run typecheck`, and the static grep gate listed in Task 2.
</verification>

<success_criteria>
REQ-008 is implemented and REQ-009 tracking is attached to the registered MCP handler path.
</success_criteria>

<output>
Create `.planning/phases/148-mcp-lifecycle-and-shutdown/148-02-SUMMARY.md` when done.
</output>
