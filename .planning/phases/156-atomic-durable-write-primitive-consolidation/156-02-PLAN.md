---
phase: 156-atomic-durable-write-primitive-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 156-01-PLAN.md
files_modified:
  - src/storage/vault.ts
  - src/utils/frontmatter.ts
  - src/mcp/utils/document-resolver-primitives.ts
  - src/services/plugin-reconciliation.ts
  - tests/integration/atomic-write-frontmatter.integration.test.ts
autonomous: true
requirements:
  - REQ-020
  - REQ-021
requirements_addressed:
  - REQ-020
  - REQ-021
must_haves:
  truths:
    - "VaultManager.writeMarkdown delegates to writeVaultFile and inherits durable atomic behavior."
    - "atomicWriteFrontmatter no longer swallows write failures."
    - "Document resolver targeted repair writes use the same primitive as normal document writes."
    - "Plugin reconciliation frontmatter writes surface failures through the shared primitive."
    - "Stale temp cleanup recognizes the new unique temp-name pattern while keeping the legacy fixed-temp sweep."
  artifacts:
    - path: "src/storage/vault.ts"
      provides: "VaultManager.writeMarkdown delegation and stale temp cleanup pattern support"
    - path: "src/utils/frontmatter.ts"
      provides: "atomicWriteFrontmatter wrapper around writeVaultFile with propagated errors"
    - path: "src/mcp/utils/document-resolver-primitives.ts"
      provides: "targeted repair write routing through writeVaultFile"
    - path: "src/services/plugin-reconciliation.ts"
      provides: "caller behavior preserving surfaced frontmatter write errors"
    - path: "tests/integration/atomic-write-frontmatter.integration.test.ts"
      provides: "T-I-039 frontmatter error propagation coverage"
  key_links:
    - from: "src/storage/vault.ts"
      to: "src/storage/vault-write.ts"
      via: "writeMarkdown delegates content commit"
      pattern: "writeVaultFile"
    - from: "src/utils/frontmatter.ts"
      to: "src/storage/vault-write.ts"
      via: "atomicWriteFrontmatter delegates serialized markdown"
      pattern: "writeVaultFile"
    - from: "src/mcp/utils/document-resolver-primitives.ts"
      to: "src/storage/vault-write.ts"
      via: "targeted scan/repair write helper"
      pattern: "writeVaultFile"
---

<objective>
Migrate existing vault write callers onto `writeVaultFile` without widening Phase 156 into later locking, destination, EXDEV, or version-token work.

Purpose: make frontmatter repair, scanner repair, document writes, resolver repair, and plugin reconciliation inherit the same atomic durable commit and error-surfacing behavior.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Downstream implementation and verification agents MUST read these product docs first before asking scope questions:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md

Then read:
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-atomic-durable-write-primitive-consolidation/156-CONTEXT.md
@.planning/phases/156-atomic-durable-write-primitive-consolidation/156-RESEARCH.md
@.planning/phases/156-atomic-durable-write-primitive-consolidation/156-VALIDATION.md
@.planning/phases/156-atomic-durable-write-primitive-consolidation/156-01-SUMMARY.md
@.planning/phases/155-per-file-tier-1-live-defect-close/155-01-SUMMARY.md
@.planning/phases/155-per-file-tier-1-live-defect-close/155-02-SUMMARY.md
@src/storage/vault.ts
@src/utils/frontmatter.ts
@src/mcp/utils/document-resolver-primitives.ts
@src/services/scanner.ts
@src/services/plugin-reconciliation.ts

Boundaries:
- Phase 156 migrates existing write helpers; it does not implement REQ-022 EXDEV fallback completeness. If `src/storage/vault.ts` trash/archive fallback writes are touched, treat them only as existing vault-write routing, not destination-lock or cross-device move acceptance.
- Phase 162 owns `version_token` response/precondition semantics. This plan may preserve or expose returned `contentHash` internally but must not add public schema changes.
- Preserve Phase 155 locking behavior. Do not remove or redesign `withDocumentLock`.
</context>

<tasks>

<task id="156-02-01" type="auto" tdd="true">
  <name>Migrate VaultManager and stale temp cleanup</name>
  <read_first>
    @src/storage/vault.ts
    @src/storage/vault-write.ts
    @tests/unit/vault.test.ts
  </read_first>
  <files>src/storage/vault.ts</files>
  <action>Replace the fixed `<absolutePath>.fqc-tmp` temp-write/rename implementation inside `VaultManager.writeMarkdown` with `writeVaultFile`. Preserve gray-matter serialization, frontmatter timestamp behavior, parent directory creation semantics, and existing git auto-commit behavior. Update `cleanStaleTempFiles(vaultPath)` so it removes both legacy `.fqc-tmp` files and the new unique `.fqc-tmp-<pid>-<counter or suffix>` pattern. Do not refactor unrelated `rename` paths into destination-lock or EXDEV behavior.</action>
  <verify>
    <automated>npm test -- tests/unit/vault.test.ts tests/unit/vault-write-primitive.test.ts tests/unit/vault-write-durable.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>`VaultManager.writeMarkdown` reaches disk through `writeVaultFile`, stale temp cleanup supports old and new temp names, and existing vault serialization behavior remains compatible.</acceptance_criteria>
  <done>`src/storage/vault.ts` delegates normal markdown writes to `writeVaultFile` and cleanup recognizes unique temp files.</done>
</task>

<task id="156-02-02" type="auto" tdd="true">
  <name>Migrate frontmatter and resolver repair writes</name>
  <read_first>
    @src/utils/frontmatter.ts
    @src/mcp/utils/document-resolver-primitives.ts
    @src/storage/vault-write.ts
    @tests/unit/scanner.test.ts
    @tests/integration/frontmatter-sync.integration.test.ts
  </read_first>
  <files>src/utils/frontmatter.ts, src/mcp/utils/document-resolver-primitives.ts, tests/integration/atomic-write-frontmatter.integration.test.ts</files>
  <action>Change `atomicWriteFrontmatter` to serialize with existing gray-matter behavior and delegate to `writeVaultFile`; remove the catch-and-debug branch that currently swallows failures. Change the private `writeMarkdownFile` path in `document-resolver-primitives.ts` to delegate to `writeVaultFile` or delete the helper in favor of direct primitive use, preserving targeted repair behavior and returning or retaining the post-write `contentHash` where the local flow can use it later. Add T-I-039 coverage proving `atomicWriteFrontmatter` propagates a real or injected write failure to the caller.</action>
  <verify>
    <automated>npm test -- tests/unit/scanner.test.ts tests/unit/vault-write-primitive.test.ts</automated>
    <automated>npm run test:integration -- --grep "frontmatter-write|atomic-write"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Frontmatter and resolver repair writes use `writeVaultFile`; write/fync/rename failures are no longer swallowed by `atomicWriteFrontmatter`; scanner repair semantics remain otherwise unchanged.</acceptance_criteria>
  <done>`atomicWriteFrontmatter` and document resolver repair writes route through the durable primitive with surfaced errors.</done>
</task>

<task id="156-02-03" type="auto" tdd="false">
  <name>Preserve plugin reconciliation error visibility</name>
  <read_first>
    @src/services/plugin-reconciliation.ts
    @src/utils/frontmatter.ts
    @tests/unit/plugin-reconciliation.test.ts
    @tests/integration/plugin-reconciliation.integration.test.ts
  </read_first>
  <files>src/services/plugin-reconciliation.ts</files>
  <action>Review plugin reconciliation's `atomicWriteFrontmatter` call site and remove or narrow any local catch behavior that would hide the surfaced write error from Phase 156. Preserve existing reconciliation response/logging style and plugin folder boundaries. If no source change is required beyond the frontmatter wrapper migration, record that explicitly in the plan summary with the exact call path from plugin reconciliation to `writeVaultFile`.</action>
  <verify>
    <automated>npm test -- tests/unit/plugin-reconciliation.test.ts tests/unit/vault-write-primitive.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Plugin reconciliation frontmatter writes inherit `writeVaultFile` behavior and no local code path silently hides durable write failures.</acceptance_criteria>
  <done>Plugin reconciliation write routing is confirmed or updated and documented in `156-02-SUMMARY.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool/scanner/plugin callers -> storage wrappers | Existing callers serialize content and provide validated vault paths before invoking the storage primitive. |
| Frontmatter serialization -> filesystem commit | Serialization must remain compatible while commit behavior changes from fixed-temp rename to durable primitive. |
| Plugin reconciliation -> vault files | Plugin-managed files must keep existing folder constraints while write errors surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-05 | Tampering | `VaultManager.writeMarkdown` | mitigate | Delegate normal markdown writes to `writeVaultFile` instead of duplicating temp-write logic. |
| T-156-06 | Repudiation | `atomicWriteFrontmatter` | mitigate | Remove catch-and-debug swallow path; integration test T-I-039 proves errors reach the caller. |
| T-156-07 | Tampering | document resolver repair | mitigate | Route targeted repair writes through the same primitive; preserve Phase 155 lock wrapper behavior. |
| T-156-08 | Elevation of Privilege | plugin reconciliation paths | mitigate | Preserve existing plugin path boundaries; only change commit primitive and error visibility. |
| T-156-SC | Tampering | package/native dependencies | mitigate | No package install in this plan. |
</threat_model>

<verification>
- `npm test -- tests/unit/vault.test.ts tests/unit/scanner.test.ts tests/unit/plugin-reconciliation.test.ts tests/unit/vault-write-primitive.test.ts tests/unit/vault-write-durable.test.ts`
- `npm run test:integration -- --grep "frontmatter-write|atomic-write"`
- `npm run typecheck`
</verification>

<success_criteria>
1. Frontmatter repair, scanner repair, document writes, and plugin reconciliation writes all route through `writeVaultFile` either directly or through `VaultManager.writeMarkdown` / `atomicWriteFrontmatter`.
2. `atomicWriteFrontmatter` no longer swallows filesystem errors.
3. Stale temp cleanup recognizes the new unique temp-name pattern.
4. Phase 156 does not claim or implement REQ-022 EXDEV fallback completeness.
</success_criteria>

<output>
Create `.planning/phases/156-atomic-durable-write-primitive-consolidation/156-02-SUMMARY.md` when done.
</output>
