---
phase: 162-version-fingerprint-check
plan: 03
type: execute
wave: 2
depends_on: [162-01, 162-02]
files_modified:
  - src/mcp/utils/document-version.ts
  - src/mcp/utils/document-resolver-primitives.ts
  - src/mcp/utils/document-output.ts
  - src/mcp/tools/documents/get.ts
  - src/mcp/tool-help/get_document.tool.md
autonomous: true
requirements: [REQ-011, REQ-014, REQ-016]
must_haves:
  truths:
    - "get_document returns a whole-file version_token equal to the current on-disk SHA-256 without acquiring a write lock."
    - "Read-triggered targetedScan repairs propagate the post-repair writeVaultFile contentHash to response version_token and fqc_documents.content_hash."
  artifacts:
    - path: "src/mcp/utils/document-version.ts"
      provides: "computeVersionToken and shared version envelope helpers"
      exports: ["computeVersionToken", "pickExpectedVersion", "buildVersionMismatchEnvelope"]
    - path: "src/mcp/utils/document-resolver-primitives.ts"
      provides: "targetedScan FrontmatterSnapshot contentHash from actual repair write"
    - path: "src/mcp/utils/document-output.ts"
      provides: "DocumentEnvelope.version_token and DB content_hash synchronization"
    - path: "src/mcp/tool-help/get_document.tool.md"
      provides: "help output discoverability for version_token"
  key_links:
    - from: "src/mcp/utils/document-resolver-primitives.ts"
      to: "src/storage/vault-write.ts"
      via: "writeVaultFile returned contentHash"
      pattern: "contentHash"
    - from: "src/mcp/utils/document-output.ts"
      to: "fqc_documents.content_hash"
      via: "same token used for DB update and response"
      pattern: "version_token.*content_hash"
---

<objective>
Implement read-side version-token plumbing and the token-equals-disk invariant for get_document.

Purpose: Make reads expose the version fingerprint callers need for opt-in write conflict detection, while preserving read-no-lock behavior and fixing the read-triggered repair hash bug.
Output: Shared version helper, repaired targetedScan hash propagation, `DocumentEnvelope.version_token`, and get_document help text.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-version-fingerprint-check/162-CONTEXT.md
@.planning/phases/162-version-fingerprint-check/162-RESEARCH.md
@.planning/phases/162-version-fingerprint-check/162-PATTERNS.md
@.planning/phases/156-atomic-durable-write-primitive-consolidation/156-02-SUMMARY.md
@src/storage/vault-write.ts
@src/mcp/utils/document-resolver-primitives.ts
@src/mcp/utils/document-output.ts
@src/mcp/tools/documents/get.ts
@src/mcp/tool-help/get_document.tool.md
@tests/unit/document-output-version-token.test.ts
@tests/unit/get-document-no-lock.test.ts
@tests/unit/version-token-shape.test.ts
@tests/integration/version-token-shape.integration.test.ts
@tests/integration/token-equals-disk.integration.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create shared version helper and repair hash return path</name>
  <files>src/mcp/utils/document-version.ts, src/mcp/utils/document-resolver-primitives.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    - .planning/phases/162-version-fingerprint-check/162-CONTEXT.md
    - .planning/phases/162-version-fingerprint-check/162-RESEARCH.md
    - .planning/phases/162-version-fingerprint-check/162-PATTERNS.md
    - src/storage/vault-write.ts
    - src/mcp/utils/document-resolver-primitives.ts
    - tests/unit/version-token-shape.test.ts
    - tests/integration/token-equals-disk.integration.test.ts
  </read_first>
  <behavior>
    - REQ-014: `writeVaultFile` returned `contentHash` is the post-write source of truth for repair paths.
    - REQ-016: token calculation uses raw bytes, frontmatter plus body, no normalization.
  </behavior>
  <action>Create `src/mcp/utils/document-version.ts` with `computeVersionToken(raw: string | Buffer): string`, `pickExpectedVersion({ expected_version, if_match })`, and `buildVersionMismatchEnvelope(...)` exported for later plans. Update the private `writeMarkdownFile` in `src/mcp/utils/document-resolver-primitives.ts` to return the `contentHash` from `writeVaultFile`. In `targetedScan`, when `frontmatterChanged` is true, set `FrontmatterSnapshot.contentHash` to the returned post-repair hash; when no write occurs, keep the fresh pre-scan hash already computed from disk bytes. Do not add write locks to read handlers.</action>
  <verify>
    <automated>npm test -- tests/unit/version-token-shape.test.ts tests/unit/resolve-document.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `computeVersionToken` uses `createHash('sha256').update(raw).digest('hex')` and returns lowercase hex.
    - `targetedScan` no longer records caller-provided `newContentHash` after it actually writes repaired frontmatter.
    - Existing resolver tests still pass with the durable primitive boundary.
  </acceptance_criteria>
  <done>Acceptance criteria satisfied and automated verification completed, or any environment-gated skip is recorded in the plan summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return version_token from get_document with DB/disk consistency</name>
  <files>src/mcp/utils/document-output.ts, src/mcp/tools/documents/get.ts, src/mcp/tool-help/get_document.tool.md</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Vault Write Coherency Locking Research/Vault Write Coherency Locking Test Plan.md
    - .planning/phases/162-version-fingerprint-check/162-CONTEXT.md
    - .planning/phases/162-version-fingerprint-check/162-PATTERNS.md
    - src/mcp/utils/document-output.ts
    - src/mcp/tools/documents/get.ts
    - src/mcp/tool-help/get_document.tool.md
    - tests/unit/document-output-version-token.test.ts
    - tests/unit/get-document-no-lock.test.ts
    - tests/integration/version-token-shape.integration.test.ts
    - tests/integration/token-equals-disk.integration.test.ts
  </read_first>
  <behavior>
    - REQ-011: `get_document` response envelope contains `version_token`.
    - REQ-014: returned token, current disk SHA-256, and `fqc_documents.content_hash` are mutually consistent.
    - REQ-016: section/frontmatter-only reads still return the whole-file token.
  </behavior>
  <action>Extend `DocumentEnvelope` with `version_token: string`. In `resolveAndBuildDocument`, derive the response token from the current disk read when no repair occurred and from `preScan.capturedFrontmatter.contentHash` after `targetedScan` repair. Update the stale-hash DB write to use the same `version_token` value for `fqc_documents.content_hash`, not the pre-repair `contentHash`. Surface `version_token` in `get_document` help output and schema docs. Preserve `get_document` as read-only with no `withDocumentLock` or directory-lock calls.</action>
  <verify>
    <automated>npm test -- tests/unit/document-output-version-token.test.ts tests/unit/get-document-no-lock.test.ts tests/unit/version-token-shape.test.ts && npm run test:integration -- tests/integration/version-token-shape.integration.test.ts tests/integration/token-equals-disk.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `get_document` returns `version_token` for full document, section, and frontmatter outputs.
    - A `get_document` repair path returns a token accepted by a subsequent `write_document(expected_version: token)` once write plumbing lands.
    - Static no-lock test `T-U-037` passes.
  </acceptance_criteria>
  <done>Acceptance criteria satisfied and automated verification completed, or any environment-gated skip is recorded in the plan summary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault file -> response | Raw file bytes become caller-visible `version_token`. |
| Vault file -> database | Raw file hash updates `fqc_documents.content_hash`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-07 | Tampering | `document-output.ts` repair path | mitigate | Use `writeVaultFile` returned `contentHash` after repair; tests T-I-026/T-I-028 verify disk/DB/response equality. |
| T-162-08 | Information Disclosure | `get_document` response | accept | `version_token` is an opaque whole-file fingerprint, not secret content; do not expose internal `content_hash` naming. |
| T-162-09 | Elevation of Privilege | `get_document` path resolution | mitigate | Keep existing `resolveDocumentIdentifier` and do not add alternate file reads that bypass resolver validation. |
| T-162-SC | Tampering | npm installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run task commands plus `npm run typecheck`. Run the ROADMAP unit selector with `--testNamePattern` fallback if `--grep` is rejected.
</verification>

<success_criteria>
REQ-011, REQ-014, and REQ-016 read-side behavior is implemented: `get_document` returns whole-file `version_token`, repair paths use post-write `contentHash`, and DB/disk/response values agree.
</success_criteria>

<output>
Create `.planning/phases/162-version-fingerprint-check/162-03-SUMMARY.md` when done.
</output>
