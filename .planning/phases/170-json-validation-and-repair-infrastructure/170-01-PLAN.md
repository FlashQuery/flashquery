---
phase: 170-json-validation-and-repair-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/llm/json-repair.ts
  - tests/unit/llm-json-repair.test.ts
autonomous: true
requirements:
  - REQ-001
  - REQ-002
  - REQ-003
  - REQ-011
must_haves:
  truths:
    - "A developer can import parseLlmJson through ESM and use it without macro or MCP dependencies."
    - "Valid and repairable LLM JSON returns typed success with raw text and repair metadata."
    - "Syntax and schema failures return non-throwing discriminated failures with bounded summaries."
  artifacts:
    - path: "src/llm/json-repair.ts"
      provides: "Pure jsonrepair + JSON.parse + Zod safeParse utility"
      exports: ["parseLlmJson"]
    - path: "tests/unit/llm-json-repair.test.ts"
      provides: "T-U-001 through T-U-010 parser evidence"
    - path: "package.json"
      provides: "jsonrepair runtime dependency"
      contains: "\"jsonrepair\""
  key_links:
    - from: "src/llm/json-repair.ts"
      to: "jsonrepair"
      via: "ESM import"
      pattern: "import \\{ jsonrepair \\} from 'jsonrepair'"
    - from: "src/llm/json-repair.ts"
      to: "zod"
      via: "safeParse schema validation"
      pattern: "\\.safeParse\\("
---

<objective>
Create the shared LLM JSON repair foundation required by every retrofit lane.

Purpose: stop duplicating raw JSON.parse behavior at LLM-adjacent boundaries while preserving caller-specific failure policy.
Output: runtime dependency, pure parser module, and focused parser unit tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-CONTEXT.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-RESEARCH.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-VALIDATION.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Test Plan.md

<interfaces>
Plan creates this contract for downstream plans:
- `parseLlmJson<T>(raw: string, schema: z.ZodType<T>): LlmJsonParseResult<T>` or semantically equivalent names per D-19.
- Success result: `ok: true`, `data`, `raw`, `repaired`.
- Failure result: `ok: false`, `raw`, `repaired`, `failure: 'syntax' | 'schema'`, optional machine-readable Zod issues, and concise `summary`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add jsonrepair dependency and parser test shell</name>
  <files>package.json, package-lock.json, tests/unit/llm-json-repair.test.ts</files>
  <read_first>
    - package.json
    - package-lock.json
    - tests/unit/config-loader.test.ts
    - tests/unit/response-formats.test.ts
    - .planning/phases/170-json-validation-and-repair-infrastructure/170-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Test Plan.md
  </read_first>
  <behavior>
    - T-U-001: importing the parser module through Vitest proves ESM compatibility for `jsonrepair`.
    - T-U-002: source-boundary assertion proves `src/llm/json-repair.ts` does not import from `src/macro/` or `src/mcp/`.
  </behavior>
  <action>Install `jsonrepair` as a runtime dependency with `npm install jsonrepair` per D-03 and REQ-001. Create `tests/unit/llm-json-repair.test.ts` with RED tests for T-U-001 and T-U-002 before implementation. Do not add a custom repair parser, CommonJS imports, or devDependency-only install.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/llm-json-repair.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists `jsonrepair` under `dependencies`, not `devDependencies`.
    - package-lock.json contains the resolved `jsonrepair` package.
    - T-U-001 and T-U-002 are present and initially fail before `src/llm/json-repair.ts` exists, then pass after Task 2.
  </acceptance_criteria>
  <done>`jsonrepair` is installed, lockfile updated, and parser unit test file exists with dependency and source-boundary coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parseLlmJson result contract</name>
  <files>src/llm/json-repair.ts, tests/unit/llm-json-repair.test.ts</files>
  <read_first>
    - src/llm/cost-tracker.ts
    - src/llm/errors.ts
    - src/macro/registry.ts
    - tests/unit/llm-json-repair.test.ts
    - .planning/phases/170-json-validation-and-repair-infrastructure/170-CONTEXT.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Test Plan.md
  </read_first>
  <behavior>
    - T-U-003: valid JSON and matching Zod schema returns `ok: true`, typed `data`, `raw`, and `repaired: false`.
    - T-U-004, T-U-004a, T-U-004b, T-U-004c: fenced JSON, trailing comma, single quotes, unquoted keys, smart quotes, truncated JSON, and missing brackets repair with `repaired: true`.
    - T-U-004d: `z.unknown()` supports schema-free parse sites.
    - T-U-005, T-U-006, T-U-006a: schema failures, irreparable syntax failures, and `jsonrepair()` throws return non-throwing failures.
  </behavior>
  <action>Create `src/llm/json-repair.ts` per D-04 through D-07. Export `parseLlmJson<T>()`, result types, issue-summary helpers, and optionally a conservative `isJsonLikeText()` helper if useful for D-20. Pipeline order must be `jsonrepair(raw)`, `JSON.parse()`, `schema.safeParse()`. Catch ordinary repair, parse, and validation failures into typed results; do not log, write files, mutate global state, import macro/MCP modules, call LLMs, or own retry policy. Follow inline TDD per fixture group: write one focused RED test, implement minimal parser behavior, rerun GREEN, then continue.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/llm-json-repair.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - REQ-002 and REQ-003 contract from canonical Requirements §6.1.2-§6.1.3 is implemented.
    - Failures distinguish `failure: 'syntax'` from `failure: 'schema'`.
    - Schema failures expose machine-readable issue paths and concise summaries.
    - Parser source has no imports matching `../macro`, `../mcp`, `../../macro`, or `../../mcp`.
  </acceptance_criteria>
  <done>Parser handles valid, repairable, syntax-failed, schema-failed, and schema-free inputs with non-throwing typed results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove metadata and build behavior</name>
  <files>src/llm/json-repair.ts, tests/unit/llm-json-repair.test.ts</files>
  <read_first>
    - src/llm/json-repair.ts
    - tests/unit/llm-json-repair.test.ts
    - package.json
    - .planning/ROADMAP.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Test Plan.md
  </read_first>
  <behavior>
    - T-U-007: schema failures expose issue paths and summary text usable by retry-capable callers.
    - T-U-008: syntax and schema failures have stable discriminators.
    - T-U-009: repaired metadata is observable internally.
    - T-U-010: utility metadata does not impose public success-envelope fields.
  </behavior>
  <action>Extend parser tests for REQ-003 and REQ-011, then make any parser refinements needed for deterministic summaries and `repaired` metadata. Run typecheck and build to prove `jsonrepair` ESM imports compile under the production stack. Do not add repair metadata to public MCP success envelopes in this plan.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/llm-json-repair.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - T-U-007 through T-U-010 pass.
    - `npm run typecheck` passes with strict TypeScript.
    - `npm run build` passes with ESM output and no `jsonrepair` module-system warning.
  </acceptance_criteria>
  <done>REQ-001, REQ-002, REQ-003, and REQ-011 have green unit/build evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM text -> parser | Untrusted model/provider text enters repair and parse code. |
| Dependency supply chain -> runtime | New npm package enters production dependency graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-01-01 | Tampering | `jsonrepair` dependency | mitigate | Use `npm install jsonrepair`; research package legitimacy audit marks slopcheck OK; lockfile records resolved package. |
| T-170-01-02 | Denial of Service | `parseLlmJson()` | mitigate | Keep parser synchronous and bounded to current input string; do not add retry loops or LLM calls per D-07. |
| T-170-01-03 | Information Disclosure | failure summaries | mitigate | Summaries are concise and do not echo large raw payloads by default per REQ-010/REQ-011. |
| T-170-01-SC | Tampering | npm install | mitigate | Package legitimacy audit in 170-RESEARCH.md approves `jsonrepair`; no [ASSUMED], [SUS], or [SLOP] packages are planned. |
</threat_model>

<verification>
Run `npm run test:unit -- tests/unit/llm-json-repair.test.ts`, `npm run typecheck`, and `npm run build`.
</verification>

<success_criteria>
`jsonrepair` is a runtime dependency, `parseLlmJson<T>()` exists under `src/llm/`, parser results satisfy D-05/D-06, and all utility test IDs T-U-001 through T-U-010 are green.
</success_criteria>

<output>
Create `.planning/phases/170-json-validation-and-repair-infrastructure/170-01-SUMMARY.md` when done.
</output>
