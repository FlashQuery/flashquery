---
phase: 170-json-validation-and-repair-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 170-01
files_modified:
  - src/llm/client.ts
  - src/macro/coerce.ts
  - tests/unit/llm-client.test.ts
  - tests/unit/macro-coerce.test.ts
  - tests/unit/macro-registry.test.ts
autonomous: true
requirements:
  - REQ-007
  - REQ-008
  - REQ-009
must_haves:
  truths:
    - "Repairable provider tool-call argument strings normalize to objects before tool dispatch."
    - "Irreparable or non-object provider tool-call arguments still fail loudly with the existing invalid JSON message."
    - "Brokered tool coercion preserves structuredContent, prose fallback, and isError fail-fast semantics."
    - "Native FlashQuery tool response parsing remains behaviorally unchanged."
  artifacts:
    - path: "src/llm/client.ts"
      provides: "Provider tool-call argument repair before fail-loud path"
      contains: "normalizeToolCallArguments"
    - path: "src/macro/coerce.ts"
      provides: "Brokered text repair and JSON-like fallback warning"
      contains: "coerceCallToolResult"
    - path: "tests/unit/macro-registry.test.ts"
      provides: "REQ-009 unchanged native parsing regression evidence"
  key_links:
    - from: "src/llm/client.ts"
      to: "src/llm/json-repair.ts"
      via: "normalizeToolCallArguments"
      pattern: "parseLlmJson"
    - from: "src/macro/coerce.ts"
      to: "src/logging/logger.ts"
      via: "logger.warn on JSON-like fallback"
      pattern: "logger\\.warn"
---

<objective>
Retrofit compatibility parse sites without changing their established contracts.

Purpose: repair malformed structured strings where beneficial while keeping provider fail-loud behavior, external-tool prose fallback, and native tool parsing unchanged.
Output: provider and brokered coercion updates with unchanged-native regression tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-CONTEXT.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-RESEARCH.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-VALIDATION.md
@.planning/phases/170-json-validation-and-repair-infrastructure/170-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Test Plan.md

<interfaces>
Uses Plan 01 `parseLlmJson<T>()` from `src/llm/json-repair.ts`.
Provider invalid-argument message to preserve: `LLM error: ${providerName} returned invalid tool call arguments JSON.`
Brokered coercion order to preserve: `isError` fail-fast, `structuredContent`, parsed/repaired text JSON, raw prose, multimodal envelope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair provider tool-call argument strings before rejection</name>
  <files>src/llm/client.ts, tests/unit/llm-client.test.ts</files>
  <read_first>
    - src/llm/client.ts
    - src/llm/json-repair.ts
    - tests/unit/llm-client.test.ts
    - .planning/phases/170-json-validation-and-repair-infrastructure/170-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Test Plan.md
  </read_first>
  <behavior>
    - T-U-024: repairable provider tool-call argument string parses to object.
    - T-U-025: irreparable tool-call argument string still rejects with invalid JSON message.
    - T-U-026: repaired non-object argument value still rejects.
    - T-U-027: already-object and missing arguments preserve existing behavior.
  </behavior>
  <action>Update private `normalizeToolCallArguments(providerName, args)` per D-12 and REQ-007. For string args, call `parseLlmJson()` with an object-record schema before the existing invalid-argument throw path. Return only parsed records; reject arrays, strings, numbers, booleans, null, and irreparable strings with the same invalid JSON error text used today. Preserve object arguments and missing argument behavior exactly as current tests expect. Use inline TDD for T-U-024 through T-U-027.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/llm-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - T-U-024 through T-U-027 pass.
    - Existing test `chat() rejects invalid tool call arguments JSON` still passes with the same error substring.
    - Arrays and scalar parsed values are not accepted as tool-call arguments.
  </acceptance_criteria>
  <done>Provider tool-call normalization repairs object strings and keeps invalid provider data fail-loud.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair brokered coercion and prove native parsing unchanged</name>
  <files>src/macro/coerce.ts, tests/unit/macro-coerce.test.ts, tests/unit/macro-registry.test.ts</files>
  <read_first>
    - src/macro/coerce.ts
    - src/macro/registry.ts
    - src/llm/json-repair.ts
    - src/logging/logger.ts
    - tests/unit/macro-coerce.test.ts
    - tests/unit/macro-registry.test.ts
    - .planning/phases/170-json-validation-and-repair-infrastructure/170-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Graph Document Intelligence (Jun 2026)/JSON Validation Test Plan.md
  </read_first>
  <behavior>
    - T-U-028: `structuredContent` precedence over text remains unchanged.
    - T-U-029: repairable text JSON binds as parsed MacroValue.
    - T-U-030: plain prose falls back to raw string with no warning.
    - T-U-031: JSON-like malformed text falls back to raw string and logs one warning.
    - T-U-032: `isError: true` still throws before value binding.
    - T-U-033 and T-U-034: `parseNativeToolResponse()` valid JSON and raw-text fallback remain unchanged.
  </behavior>
  <action>Update `coerceCallToolResult()` per D-13 and REQ-008. Keep `isCallToolErrorResult()` fail-fast first and `structuredContent` precedence second. For text content without structuredContent, attempt repair/parse through `parseLlmJson(text, z.unknown())`; on success return `toMacroValue(parsed.data)`. On failure, return raw text; call `logger.warn()` exactly once only when the text is JSON-like by the conservative helper or local predicate. Do not modify `src/macro/registry.ts` for REQ-009; add regression tests T-U-033 and T-U-034 only.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/macro-coerce.test.ts tests/unit/macro-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - T-U-028 through T-U-034 pass.
    - Plain prose produces no warning; malformed JSON-like text produces one `logger.warn()` call.
    - `src/macro/registry.ts` has no production changes for this plan unless a separate failing regression proves a real issue, per D-14.
  </acceptance_criteria>
  <done>Compatibility retrofits improve repair behavior without broadening strictness or breaking native tool response parsing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider API -> tool dispatcher | Untrusted provider tool-call arguments become internal tool arguments. |
| Brokered MCP tool -> macro binding | External tool output becomes macro values. |
| Native tool response -> macro registry | App-controlled FlashQuery native output remains trusted by construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-03-01 | Tampering | `normalizeToolCallArguments()` | mitigate | Validate repaired provider arguments as records only; reject irreparable and non-object values through existing error path. |
| T-170-03-02 | Denial of Service | `coerceCallToolResult()` warning path | mitigate | Warn once per JSON-like fallback call; do not warn for ordinary prose. |
| T-170-03-03 | Elevation of Privilege | native tool parsing scope | mitigate | Do not change `parseNativeToolResponse()` strictness; regression tests lock current behavior per REQ-009. |
| T-170-03-SC | Tampering | shared parser dependency | mitigate | Uses Plan 01 approved `jsonrepair`; no new package installs in this plan. |
</threat_model>

<verification>
Run `npm run test:unit -- tests/unit/llm-client.test.ts tests/unit/macro-coerce.test.ts tests/unit/macro-registry.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
REQ-007, REQ-008, and REQ-009 have green unit evidence; existing compatibility fallbacks and fail-loud paths are preserved.
</success_criteria>

<output>
Create `.planning/phases/170-json-validation-and-repair-infrastructure/170-03-SUMMARY.md` when done.
</output>
