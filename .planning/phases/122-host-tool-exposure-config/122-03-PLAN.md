---
phase: 122-host-tool-exposure-config
plan: 03
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - src/llm/tool-registry.ts
  - src/config/loader.ts
  - tests/unit/llm-tool-registry.test.ts
  - tests/unit/llm-config.test.ts
  - tests/unit/config.test.ts
  - tests/integration/llm-config-sync.test.ts
  - tests/e2e/call-model-agent-loop.e2e.test.ts
autonomous: true
requirements: [CFG-04, CFG-05, CFG-06]
must_haves:
  truths:
    - "Delegated native tool assembly starts from the host-enabled catalog."
    - "A delegated purpose cannot regain a host-disabled tool through explicit tools or tiers."
    - "Delegated hard exclusions still win over explicit delegated tool names."
    - "Purpose config references to removed or merged legacy names fail startup with replacement suggestions."
    - "Suspicious host category warnings do not imply authorization or block startup."
  artifacts:
    - path: "src/llm/tool-registry.ts"
      provides: "Delegated assembly diagnostics for host-filtered catalog intersection"
    - path: "src/config/loader.ts"
      provides: "Legacy purpose tool-name hard-fail and warning accessors"
    - path: "tests/integration/llm-config-sync.test.ts"
      provides: "Integration validation of purpose config legacy-name failures"
  key_links:
    - from: "src/mcp/tool-catalog.ts"
      to: "src/llm/tool-registry.ts"
      via: "host-filtered NativeToolDefinition[] catalog"
      pattern: "catalogNames"
    - from: "src/config/loader.ts"
      to: "src/mcp/tool-metadata.ts"
      via: "getLegacyToolSuggestion for purpose tools/excluded_tools"
      pattern: "getLegacyToolSuggestion"
---

<objective>
Close the delegated-model side of host exposure parity and enforce legacy-name startup failures.

Purpose: Host filtering must constrain inner model tool belts, while purpose config errors must guide users away from removed/merged names without aliases or silent rewrites.
Output: Delegated intersection tests, legacy-name validation, warning tests, and E2E regression coverage for call_model native tool assembly.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Implementation agents MUST read these before requirements or test-scope decisions:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md

Treat @.planning/ROADMAP.md as the phase boundary and the two product docs above as the detailed contract inside that boundary. Do not ask the user questions until those docs and phase artifacts have been consulted.

@.planning/phases/122-host-tool-exposure-config/TRACEABILITY.md
@.planning/phases/122-host-tool-exposure-config/122-CONTEXT.md
@.planning/phases/122-host-tool-exposure-config/122-RESEARCH.md
@.planning/phases/122-host-tool-exposure-config/122-VALIDATION.md
@.planning/phases/122-host-tool-exposure-config/122-PATTERNS.md
@.planning/phases/122-host-tool-exposure-config/122-01-SUMMARY.md
@.planning/phases/122-host-tool-exposure-config/122-02-SUMMARY.md

<interfaces>
From `src/llm/tool-registry.ts`:
```typescript
export function assembleNativeToolRegistry(config: FlashQueryConfig, purposeName: string, catalog: NativeToolDefinition[], options?: ToolRegistryAssemblyOptions): ToolRegistryAssembly;
export interface ToolRegistryDiagnostics {
  expandedTiers: Array<{ tier: ToolTierName; tools: string[] }>;
  explicitTools: string[];
  excluded: string[];
  hardExcluded: Array<{ tool: string; reason: string }>;
  unknown: string[];
}
```

From Plan 01:
```typescript
export function getResolvedHostToolExposure(config: FlashQueryConfig): ResolvedHostToolExposure;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove delegated assembly cannot regain host-disabled tools</name>
  <files>src/llm/tool-registry.ts, tests/unit/llm-tool-registry.test.ts, tests/e2e/call-model-agent-loop.e2e.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
    - src/llm/tool-registry.ts
    - src/mcp/tool-catalog.ts
    - src/mcp/server.ts
    - tests/unit/llm-tool-registry.test.ts
    - tests/e2e/call-model-agent-loop.e2e.test.ts
    - .planning/phases/122-host-tool-exposure-config/TRACEABILITY.md
    - .planning/phases/122-host-tool-exposure-config/122-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: with a host-filtered catalog containing only `get_document` and `call_model`, purpose `tools: ['tier:read-write', 'save_memory']` returns only `get_document`; `save_memory` appears in `diagnostics.unknown`.
    - Test 2: purpose `tools: ['call_model']` with catalog containing `call_model` returns no native tools and reports `call_model` in `diagnostics.hardExcluded`.
    - Test 3: purpose `tools: ['tier:read-only']` with host-filtered doc-read catalog does not include `search_memory`, `get_memory`, or `list_memories`.
    - Test 4: E2E call_model native tool diagnostics reflect host-disabled tools as unavailable when host config excludes their category.
  </behavior>
  <action>Keep `assembleNativeToolRegistry` catalog-driven. If needed, adjust diagnostics to make host-disabled explicit requests appear in `diagnostics.unknown` or an existing unavailable bucket without adding a new public schema unless tests already require it. Do not let purpose `tools`, `excludedTools`, or tier selectors add any tool absent from the passed `catalog`. Preserve delegated hard exclusions after catalog intersection: explicit `call_model`, plugin admin tools, and system admin tools remain removed with reasons even if host-enabled. Add unit tests using a host-filtered `NativeToolDefinition[]` catalog rather than mocking metadata arrays. Add or update a focused E2E regression in `tests/e2e/call-model-agent-loop.e2e.test.ts` only if the existing harness can assert native diagnostics without live-provider dependence; otherwise keep E2E coverage in protocol listTools and document the environment-gated skip in the summary.</action>
  <acceptance_criteria>
    - `tests/unit/llm-tool-registry.test.ts` contains `host-disabled` or equivalent test text and asserts `save_memory` is not in `nativeToolNames`.
    - `tests/unit/llm-tool-registry.test.ts` asserts `call_model` remains hard-excluded when explicitly requested.
    - `npm test -- tests/unit/llm-tool-registry.test.ts` passes.
    - If `tests/e2e/call-model-agent-loop.e2e.test.ts` is modified, `npm run test:e2e -- tests/e2e/call-model-agent-loop.e2e.test.ts` passes or skips only through existing environment prerequisite mechanisms.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool-registry.test.ts</automated>
  </verify>
  <done>Delegated native assembly is proven to start from the host-filtered catalog and preserve hard exclusions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hard-fail legacy purpose tool names with suggestions</name>
  <files>src/config/loader.ts, tests/unit/llm-config.test.ts, tests/unit/config.test.ts, tests/integration/llm-config-sync.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
    - src/config/loader.ts
    - src/mcp/tool-metadata.ts
    - src/mcp/tool-exposure.ts
    - tests/unit/llm-config.test.ts
    - tests/unit/config.test.ts
    - tests/integration/llm-config-sync.test.ts
    - .planning/phases/122-host-tool-exposure-config/122-CONTEXT.md
    - .planning/phases/122-host-tool-exposure-config/122-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: purpose `tools: ['search_documents']` throws a startup config error containing `Tool 'search_documents' has been replaced by 'search'`.
    - Test 2: purpose `excluded_tools: ['create_document']` throws a startup config error containing `Tool 'create_document' has been replaced by 'write_document'`.
    - Test 3: errors say FlashQuery does not alias legacy tool names and do not mutate the loaded config to replacement names.
    - Test 4: transitional legacy tools `get_briefing` and `insert_doc_link` remain valid purpose tool names until their removal gates.
  </behavior>
  <action>Update LLM purpose config validation in `src/config/loader.ts` so names with `getLegacyToolSuggestion(name)` fail in purpose `tools` and `excluded_tools` when the metadata entry represents removed/merged legacy names that should not be used for new purpose config. Use actionable messages from `getLegacyToolSuggestion`, including the old name, replacement, and no-alias statement. Preserve CFG-05: do not silently rewrite names and do not accept compatibility aliases. Because `get_briefing` and `insert_doc_link` are explicitly transitional legacy survivors per DAQ-3, keep them valid until their removal gates; if `getLegacyToolSuggestion` currently marks them, add a validator distinction so macro-dependent transitional survivors remain allowed in this phase. Keep `excluded_tools requires tools` behavior unchanged.</action>
  <acceptance_criteria>
    - `tests/unit/llm-config.test.ts` or `tests/unit/config.test.ts` asserts errors for `search_documents`, `create_document`, `save_memory`, and `force_file_scan` with replacements `search`, `write_document`, `write_memory`, and `maintain_vault`.
    - Tests assert `get_briefing` and `insert_doc_link` are still accepted.
    - `grep -n "does not alias legacy tool names" src/config/loader.ts tests/unit/llm-config.test.ts tests/unit/config.test.ts` finds validation or assertion text.
    - `npm test -- tests/unit/llm-config.test.ts tests/unit/config.test.ts` passes.
    - `npm run test:integration -- tests/integration/llm-config-sync.test.ts` passes or skips only through existing Supabase prerequisite mechanisms.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-config.test.ts tests/unit/config.test.ts && npm run test:integration -- tests/integration/llm-config-sync.test.ts</automated>
  </verify>
  <done>Purpose configs with removed/merged legacy tool names fail at startup with specific replacement suggestions and no alias rewrite.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host catalog -> delegated model tools | Host-filtered catalog constrains provider-visible native tools. |
| Purpose YAML -> delegated tool registry | User-controlled purpose selectors influence inner model tool belts. |
| Legacy config -> startup validation | Old tool names must be rejected clearly rather than silently rewritten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-05 | Elevation of Privilege | `assembleNativeToolRegistry` | mitigate | Use catalog intersection so delegated tools cannot regain host-disabled tools. |
| T-122-06 | Tampering | `validateLlmConfig` | mitigate | Hard-fail removed/merged legacy names with metadata suggestions; no aliases or mutation. |
| T-122-07 | Information Disclosure | delegated hard exclusions | mitigate | Preserve hard-exclusion filtering for recursive model, plugin admin, and system admin tools. |
| T-122-11 | Repudiation | diagnostics | accept | Existing diagnostics expose unknown/hardExcluded lists for troubleshooting; no sensitive secrets are included. |
</threat_model>

<verification>
```bash
npm test -- tests/unit/llm-tool-registry.test.ts
npm test -- tests/unit/llm-config.test.ts tests/unit/config.test.ts
npm run test:integration -- tests/integration/llm-config-sync.test.ts
```
</verification>

<success_criteria>
CFG-04 delegated intersection, CFG-05 legacy startup failures, and CFG-06 warning behavior are tested without weakening existing hard-exclusion or purpose validation semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/122-host-tool-exposure-config/122-03-SUMMARY.md`.
</output>
