---
phase: 131-lexer-parser-fence-extraction
plan: 05
type: execute
wave: 3
depends_on:
  - 131-03
files_modified:
  - src/mcp/tools/macro.ts
  - tests/integration/macro-parse-error.test.ts
  - tests/config/vitest.integration.config.ts
requirements:
  - MACRO-PARSE-10
autonomous: true
must_haves:
  truths:
    - "`call_macro` returns a canonical parse_error envelope for invalid inline source without executing macros."
    - "The handler-boundary parse-error proof is an integration test matching Test Plan T-I-001."
    - "Phase 131 leaves evaluator, dispatch, source document resolution, dry-run, budgets, progress, and task lifecycle untouched."
  artifacts:
    - path: "src/mcp/tools/macro.ts"
      provides: "parser-facing invalid inline source boundary"
    - path: "tests/integration/macro-parse-error.test.ts"
      provides: "T-I-001 integration coverage"
    - path: "tests/config/vitest.integration.config.ts"
      provides: "explicit integration include entry when required by local config pattern"
  key_links:
    - from: "src/mcp/tools/macro.ts"
      to: "src/macro/parser.ts"
      via: "parseMacroSource for invalid inline source only"
      pattern: "parseMacroSource"
    - from: "tests/config/vitest.integration.config.ts"
      to: "tests/integration/macro-parse-error.test.ts"
      via: "explicit include list"
      pattern: "macro-parse-error.test.ts"
---

<objective>
Add the narrow handler-boundary parse-error proof for invalid inline macro source.

Purpose: Test Plan T-I-001 is integration coverage, so the public `call_macro` scaffold must prove parser errors cross the MCP handler boundary as expected errors without adding runtime execution.
Output: `call_macro` inline parse-error wiring, `tests/integration/macro-parse-error.test.ts`, and integration config inclusion if the suite uses an explicit include list.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/131-lexer-parser-fence-extraction/131-CONTEXT.md
@.planning/phases/131-lexer-parser-fence-extraction/131-RESEARCH.md
@.planning/phases/131-lexer-parser-fence-extraction/131-VALIDATION.md
@.planning/phases/131-lexer-parser-fence-extraction/131-PATTERNS.md
@.planning/phases/131-lexer-parser-fence-extraction/131-03-SUMMARY.md
@.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-01-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/parser.ts
@src/mcp/tools/macro.ts
@src/mcp/utils/response-formats.ts
@tests/config/vitest.integration.config.ts

<interfaces>
From Plan 03:
```typescript
export function parseMacroSource(source: string, identifier?: string): MacroParseResult;
```

From Phase 130:
```typescript
export function registerMacroTools(server: McpServer, config: FlashQueryConfig): void;
export function jsonExpectedError(error: ErrorEnvelope): ToolResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add call_macro inline parse-error integration proof</name>
  <files>src/mcp/tools/macro.ts, tests/integration/macro-parse-error.test.ts, tests/config/vitest.integration.config.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` §6.2.10 and §8.11 source-resolution boundary notes.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` T-I-001 intent and T-U-063 through T-U-066.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/parser.ts`.
    - `.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-01-SUMMARY.md`.
    - `src/mcp/tools/macro.ts`.
    - `src/mcp/utils/response-formats.ts`.
    - `tests/config/vitest.integration.config.ts`.
  </read_first>
  <behavior>
    - T-I-001: invalid inline `source` returns the parser's `{ error: "parse_error", details: { reason, at_line, near_token? } }` envelope through `jsonExpectedError` semantics (`isError: false`) at the handler boundary.
    - Valid inline `source` still returns the existing `unsupported` scaffold response until evaluator/runtime phases replace it.
    - `source_ref` document resolution, named-block handler lookup, evaluator execution, dry-run, budgets, progress, task lifecycle, and dispatch remain unimplemented in this phase.
  </behavior>
  <action>Modify `src/mcp/tools/macro.ts` to import `parseMacroSource` and use the existing `jsonExpectedError` helper. Change the handler to accept params, keep the shutdown guard first, and when `params.source` is a non-empty string call `parseMacroSource(params.source, 'inline')`. If parsing returns `{ ok: false }`, return `jsonExpectedError(parseResult.error)`. If parsing succeeds, continue returning the existing unsupported expected error with `details.reason: "phase_130_scaffold"`. Do not validate source/source_ref exclusivity here, do not resolve source_ref, do not extract document fences in the handler, and do not execute parsed programs. Create `tests/integration/macro-parse-error.test.ts` for Test Plan T-I-001 with a focused assertion for invalid inline source such as `for = 5` returning `error: "parse_error"`, `details.reason: "reserved_keyword_assignment"`, and `isError: false`. If `tests/config/vitest.integration.config.ts` uses an explicit `include` array, add `tests/integration/macro-parse-error.test.ts` to that list.</action>
  <verify>
    <automated>npm run test:integration -- --run tests/integration/macro-parse-error.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:integration -- --run tests/integration/macro-parse-error.test.ts` exits 0 and includes test name `T-I-001`.
    - `grep -n "parseMacroSource" src/mcp/tools/macro.ts tests/integration/macro-parse-error.test.ts` finds handler wiring and integration assertion.
    - `grep -n "macro-parse-error.test.ts" tests/config/vitest.integration.config.ts` finds the explicit include entry when the integration config is list-based.
    - `grep -n "resolveDocument\\|get_document\\|dry_run\\|budget\\|progressToken\\|execute\\|dispatch" src/mcp/tools/macro.ts | grep -v "inputSchema\\|description\\|unsupported"` returns no new runtime/source-resolution implementation path.
    - Requirement ID covered by this task: MACRO-PARSE-10.
  </acceptance_criteria>
  <done>`call_macro` exposes parser errors for invalid inline source through integration coverage while staying a non-executing scaffold for valid input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP handler params -> parser | Untrusted inline `source` reaches the parser-facing `call_macro` boundary. |
| parser error -> MCP response | Structured parser failures become public expected-error envelopes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-14 | Denial of Service | `registerMacroTools` handler | mitigate | Invalid inline macro source returns `jsonExpectedError(parse_error)` instead of crashing the MCP tool. |
| T-131-15 | Elevation of Privilege | `registerMacroTools` handler | mitigate | Handler still does not execute parsed programs, dispatch tools, resolve documents, or apply dry-run/budget/progress behavior in Phase 131. |
| T-131-16 | Spoofing | `source_ref` scope | mitigate | Plan does not implement handler-level `source_ref` lookup; pure selector semantics were completed in Plan 02 and document resolution remains Phase 138. |
</threat_model>

<verification>
Run `npm run test:integration -- --run tests/integration/macro-parse-error.test.ts`, `npm test -- macro-lexer macro-parser macro-fence-extractor macro-source-ref`, `npm test`, and `npm run build`.
</verification>

<success_criteria>
Invalid inline macro syntax returns a canonical parse_error through the public scaffold, T-I-001 is covered by integration test, and Phase 131 remains parser-layer only.
</success_criteria>

<source_doc_summary_requirement>
The executor summary `.planning/phases/131-lexer-parser-fence-extraction/131-05-SUMMARY.md` must contain a `Source Docs Read` section proving the Macro Language requirements doc, Macro Language test plan, and relevant POC files/read context were read before edits. Verify with `for token in "Source Docs Read" "FlashQuery Macro Language Requirements.md" "FlashQuery Macro Language Test Plan.md" "macro-prototype/src/parser.ts" "130-01-SUMMARY.md" "src/mcp/tools/macro.ts"; do grep -q "$token" .planning/phases/131-lexer-parser-fence-extraction/131-05-SUMMARY.md || exit 1; done`.
</source_doc_summary_requirement>

<output>
After completion, create `.planning/phases/131-lexer-parser-fence-extraction/131-05-SUMMARY.md`
</output>
