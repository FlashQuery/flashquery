---
phase: 131-lexer-parser-fence-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/macro/types.ts
  - src/macro/errors.ts
  - src/macro/tokens.ts
  - tests/unit/macro-lexer.test.ts
requirements:
  - MACRO-PARSE-01
  - MACRO-PARSE-03
  - MACRO-PARSE-10
autonomous: true
must_haves:
  truths:
    - "Macro source can be tokenized with the full v0 keyword, literal, comment, operator, and range-token surface."
    - "Parser-layer failures have stable structured parse_error data with 1-indexed line numbers."
    - "Later source utilities and parser grammar can import stable AST and error contracts from src/macro."
  artifacts:
    - path: "package.json"
      provides: "chevrotain dependency"
      contains: "\"chevrotain\""
    - path: "src/macro/types.ts"
      provides: "Macro Program, Statement, Expr, source block, and parse result contracts"
      exports: ["Program", "Statement", "Expr", "MacroSourceBlock", "MacroParseResult"]
    - path: "src/macro/errors.ts"
      provides: "Macro parse_error and invalid_input detail helpers"
      exports: ["MacroParseErrorReason", "MacroParseErrorDetails", "macroParseError", "macroInvalidInput"]
    - path: "src/macro/tokens.ts"
      provides: "Chevrotain token definitions and macroLexer"
      exports: ["macroLexer", "allTokens", "RESERVED_KEYWORDS", "BUILTIN_NAMES"]
    - path: "tests/unit/macro-lexer.test.ts"
      provides: "T-U-019, T-U-020, T-U-024 through T-U-029, T-U-034, T-U-043 coverage"
  key_links:
    - from: "src/macro/tokens.ts"
      to: "src/macro/types.ts"
      via: "token names and AST value-domain agreement"
      pattern: "export const macroLexer"
    - from: "src/macro/errors.ts"
      to: "src/mcp/utils/response-formats.ts"
      via: "ErrorEnvelope-compatible shape"
      pattern: "error: 'parse_error'"
---

<objective>
Create the parser-layer foundation: Chevrotain dependency, typed AST/error contracts, and the v0 lexer.

Purpose: Phase 131 depends on stable parser contracts before source extraction and grammar work can run independently.
Output: `src/macro/types.ts`, `src/macro/errors.ts`, `src/macro/tokens.ts`, lexer unit tests, and a locked Chevrotain install.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/131-lexer-parser-fence-extraction/131-CONTEXT.md
@.planning/phases/131-lexer-parser-fence-extraction/131-RESEARCH.md
@.planning/phases/131-lexer-parser-fence-extraction/131-VALIDATION.md
@.planning/phases/131-lexer-parser-fence-extraction/131-PATTERNS.md
@.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-01-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/lexer.ts
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/types.ts

<interfaces>
From `src/mcp/utils/response-formats.ts`:
```typescript
export interface ErrorEnvelope {
  error: string;
  message: string;
  identifier?: string;
  details?: Record<string, unknown>;
}
export function jsonExpectedError(error: ErrorEnvelope): ToolResult;
```

Create these parser-layer exports:
```typescript
export type MacroParseErrorReason =
  | 'unexpected_token'
  | 'missing_do'
  | 'missing_done'
  | 'missing_then'
  | 'missing_fi'
  | 'malformed_fence_attributes'
  | 'reserved_keyword_assignment'
  | 'builtin_name_shadowing'
  | 'invalid_literal'
  | 'input_var_key_must_be_literal';

export interface MacroParseErrorDetails {
  reason: MacroParseErrorReason;
  at_line: number;
  near_token?: string;
}

export type MacroParseResult = { ok: true; program: Program } | { ok: false; error: MacroParseErrorEnvelope };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Chevrotain and define parser contracts</name>
  <files>package.json, package-lock.json, src/macro/types.ts, src/macro/errors.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` sections §3.1, §6.2.1, §6.2.3, §6.2.10, §8.4.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` sections §4.1.3, §4.1.4, §4.1.9.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/types.ts`.
    - `src/mcp/utils/response-formats.ts`.
  </read_first>
  <action>Run `npm install chevrotain@12.0.0` so `package.json` and `package-lock.json` record the dependency. Create `src/macro/types.ts` exporting `Program`, `Statement`, `Binding`, `Pipeline`, `Call`, `ForLoop`, `WhileLoop`, `IfStmt`, `ToolCall`, `Expr`, `StringLit`, `NumLit`, `NullLit`, `VarRef`, `ListLit`, `ObjectLit`, `ObjectEntry`, `FieldAccess`, `RangeExpr`, `BinaryExpr`, `UnaryExpr`, `MacroSourceBlock`, and `MacroParseResult`. Do not include evaluator runtime state, task registry state, shell execution types, or dispatch registry types in this phase. Create `src/macro/errors.ts` exporting `MacroParseErrorReason`, `MacroParseErrorDetails`, `MacroParseErrorEnvelope`, `MacroInvalidInputReason`, `MacroInvalidInputEnvelope`, `macroParseError(details, message?, identifier?)`, and `macroInvalidInput(reason, details?, message?, identifier?)`. `MacroParseErrorReason` must include `missing_do` in addition to the other stable parse reasons resolved in research. `macroParseError` must return `{ error: 'parse_error', message, identifier?, details: { reason, at_line, near_token? } }` and `macroInvalidInput` must return `{ error: 'invalid_input', message, identifier?, details: { reason, ...details } }`.</action>
  <verify>
    <automated>test -f src/macro/types.ts && test -f src/macro/errors.ts && node -e "const p=require('./package.json'); if(!p.dependencies?.chevrotain) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export type Program\\|export type Statement\\|export type Expr\\|export type RangeExpr\\|export type BinaryExpr\\|export type WhileLoop" src/macro/types.ts` shows exported contracts.
    - `grep -n "unexpected_token\\|missing_do\\|missing_done\\|missing_then\\|missing_fi\\|malformed_fence_attributes\\|reserved_keyword_assignment\\|builtin_name_shadowing\\|invalid_literal\\|input_var_key_must_be_literal" src/macro/errors.ts` shows the exact parse reason strings required by MACRO-PARSE-10.
    - `node -e "const p=require('./package.json'); if(p.dependencies.chevrotain!=='^12.0.0' && p.dependencies.chevrotain!=='12.0.0') process.exit(1)"` exits 0.
    - Requirement IDs covered by this task: MACRO-PARSE-03, MACRO-PARSE-10.
  </acceptance_criteria>
  <done>Chevrotain is installed and stable parser contracts/errors exist for downstream Phase 131 plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the v0 Chevrotain lexer</name>
  <files>src/macro/tokens.ts, tests/unit/macro-lexer.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` sections §5.4, §6.2.1, §6.2.3, §6.2.4, §6.2.5, §6.2.6, §6.2.10.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` tests T-U-019, T-U-020, T-U-024 through T-U-029, T-U-034, T-U-043.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/lexer.ts`.
  </read_first>
  <behavior>
    - T-U-019: `for`, `in`, `do`, `done`, `if`, `then`, `else`, `fi`, `while`, and `null` lex as keyword tokens.
    - T-U-020: `forecasted` lexes as one `Identifier` using Chevrotain `longer_alt`.
    - T-U-024/T-U-025: signed integers and signed decimal floats lex as `NumberLit`; exponent, hex, and octal forms produce invalid tokenization or a parser-facing lexical error path.
    - T-U-034/T-U-043: `==`, `!=`, `<`, `>`, `<=`, `>=`, `&&`, `||`, and `..` lex as distinct operator tokens, with `0..10` tokenized as `NumberLit`, `Range`, `NumberLit`.
  </behavior>
  <action>Create `src/macro/tokens.ts` using Chevrotain `createToken` and `Lexer`. Export `Identifier`, keyword tokens `For`, `In`, `Do`, `Done`, `If`, `Then`, `Else`, `Fi`, `While`, `NullTok`, operators `EqEq`, `BangEq`, `LessEq`, `GreaterEq`, `LessThan`, `GreaterThan`, `AndAnd`, `OrOr`, `Range`, `Bang`, punctuation tokens, literal tokens, `VarRefTok`, `LongFlag`, `ShortFlag`, `Newline`, `LineContinuation`, `Comment`, `WhiteSpace`, `RESERVED_KEYWORDS`, `BUILTIN_NAMES`, `allTokens`, and `macroLexer`. Token order must place skipped whitespace/continuation/comment, newlines, keywords with `longer_alt: Identifier`, multi-character operators before single-character operators, `Range` before `Dot`, literals before punctuation where needed, and `Identifier` last. Implement helpers `varRefName`, `longFlagName`, `shortFlagLetters`, `unquoteDouble`, `unquoteSingle`, and `validateDoubleQuotedEscapes(image)` where only `\\n`, `\\t`, `\\r`, `\\"`, `\\\\`, and `\\$` are accepted. Create `tests/unit/macro-lexer.test.ts` covering the listed T-U IDs with direct `macroLexer.tokenize()` assertions.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-lexer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/unit/macro-lexer.test.ts` exits 0 and includes test names `T-U-019`, `T-U-020`, `T-U-024`, `T-U-025`, `T-U-026`, `T-U-027`, `T-U-028`, `T-U-029`, `T-U-034`, and `T-U-043`.
    - `grep -n "longer_alt: Identifier" src/macro/tokens.ts | wc -l` returns at least `10`.
    - `grep -n "Do\\|While\\|Range\\|AndAnd\\|OrOr\\|EqEq\\|BangEq\\|LessEq\\|GreaterEq" src/macro/tokens.ts` finds exported tokens.
    - Requirement IDs covered by this task: MACRO-PARSE-01, MACRO-PARSE-03, MACRO-PARSE-04, MACRO-PARSE-05, MACRO-PARSE-06.
  </acceptance_criteria>
  <done>The lexer supports the final v0 token surface and focused lexer tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macro source string -> lexer | Untrusted macro text becomes tokens consumed by parser-layer code. |
| lexer/parser errors -> MCP response layer | Internal parse failures become user-visible expected-error envelopes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Tampering | `src/macro/tokens.ts` | mitigate | Reserve all v0 keywords with `longer_alt: Identifier`; add T-U-019/T-U-020 tests so malformed prefix tokens cannot alter parse meaning. |
| T-131-02 | Denial of Service | `src/macro/errors.ts` | mitigate | Normalize lexer/parser failures into bounded `parse_error` envelopes with 1-indexed line data; no thrown raw parser exception crosses planned parser entrypoints. |
| T-131-03 | Tampering | `src/macro/types.ts` | mitigate | Encode reserved assignment and builtin-name shadowing reason strings for parser validation before execution in Plan 03. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/macro-lexer.test.ts` and `npm run build` after this plan. Do not proceed to dependent plans if Chevrotain install or lexer tests fail.
</verification>

<success_criteria>
Chevrotain is installed, parser-layer contracts compile, and lexer tests prove the v0 keyword/literal/operator/range token surface for MACRO-PARSE-01, MACRO-PARSE-03, and parser-side portions of MACRO-PARSE-04 through MACRO-PARSE-06.
</success_criteria>

<source_doc_summary_requirement>
The executor summary `.planning/phases/131-lexer-parser-fence-extraction/131-01-SUMMARY.md` must contain a `Source Docs Read` section proving the Macro Language requirements doc, Macro Language test plan, and relevant POC files were read before edits. Verify with `for token in "Source Docs Read" "FlashQuery Macro Language Requirements.md" "FlashQuery Macro Language Test Plan.md" "macro-prototype/src/lexer.ts" "macro-prototype/src/types.ts"; do grep -q "$token" .planning/phases/131-lexer-parser-fence-extraction/131-01-SUMMARY.md || exit 1; done`.
</source_doc_summary_requirement>

<output>
After completion, create `.planning/phases/131-lexer-parser-fence-extraction/131-01-SUMMARY.md`
</output>
