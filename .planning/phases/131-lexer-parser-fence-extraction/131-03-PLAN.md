---
phase: 131-lexer-parser-fence-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - 131-01
files_modified:
  - src/macro/parser.ts
  - tests/unit/macro-parser.test.ts
requirements:
  - MACRO-PARSE-02
  - MACRO-PARSE-03
  - MACRO-PARSE-04
  - MACRO-PARSE-05
  - MACRO-PARSE-06
  - MACRO-PARSE-07
  - MACRO-PARSE-08
  - MACRO-PARSE-09
  - MACRO-PARSE-10
autonomous: true
must_haves:
  truths:
    - "Macro source parses into a typed AST for bindings, literals, objects, lists, field access, pipelines, tool calls, if, while, and for."
    - "Reserved keyword and builtin-name assignment are rejected before evaluator phases."
    - "Comparison, boolean, and range operators produce stable AST shapes without executing semantics."
    - "Parse failures return stable parse_error envelopes with reason, 1-indexed at_line, and near_token where available."
  artifacts:
    - path: "src/macro/parser.ts"
      provides: "parseMacroSource entrypoint and Chevrotain grammar"
      exports: ["parseMacroSource"]
    - path: "tests/unit/macro-parser.test.ts"
      provides: "T-U-021 through T-U-066 parser-layer coverage"
  key_links:
    - from: "src/macro/parser.ts"
      to: "src/macro/tokens.ts"
      via: "Chevrotain allTokens and macroLexer"
      pattern: "macroLexer.tokenize"
    - from: "src/macro/parser.ts"
      to: "src/macro/errors.ts"
      via: "macroParseError for lex/parse/validation failures"
      pattern: "macroParseError"
---

<objective>
Implement the Phase 131 parser grammar and CST-to-AST conversion for the full v0 parser-layer DSL.

Purpose: Later evaluator phases need one stable AST and parse-error contract instead of ad hoc syntax handling.
Output: `src/macro/parser.ts` and focused parser unit coverage for MACRO-PARSE-02 through MACRO-PARSE-10.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/131-lexer-parser-fence-extraction/131-CONTEXT.md
@.planning/phases/131-lexer-parser-fence-extraction/131-RESEARCH.md
@.planning/phases/131-lexer-parser-fence-extraction/131-VALIDATION.md
@.planning/phases/131-lexer-parser-fence-extraction/131-PATTERNS.md
@.planning/phases/131-lexer-parser-fence-extraction/131-01-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/parser.ts
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/types.ts

<interfaces>
Use Plan 01 exports:
```typescript
export function parseMacroSource(source: string, identifier?: string): MacroParseResult;
export const BUILTIN_NAMES: readonly string[];
export const RESERVED_KEYWORDS: readonly string[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse literals, bindings, pipelines, and assignment validation</name>
  <files>src/macro/parser.ts, tests/unit/macro-parser.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` §6.2.1, §6.2.2, §6.2.3, §6.2.10.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` tests T-U-021 through T-U-033 and T-U-063 through T-U-066.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/parser.ts`.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/types.ts`.
  </read_first>
  <behavior>
    - T-U-021/T-U-022/T-U-023: assignment to reserved keyword or every v0 builtin name returns `parse_error` with `reserved_keyword_assignment` or `builtin_name_shadowing`.
    - T-U-030/T-U-031/T-U-032/T-U-033: lists, objects, `null`, and comments parse.
    - T-U-063/T-U-066: parse errors expose `{ error: "parse_error", details: { reason, at_line, near_token? } }`.
  </behavior>
  <action>Create `src/macro/parser.ts` with a singleton `MacroParser extends CstParser`, `performSelfAnalysis()`, and exported `parseMacroSource(source, identifier?)`. The initial grammar must parse top-level statements, bindings (`Identifier = expr`), builtin pipelines (`call (| call)*`), positional and named flag args, string/number/null/list/object literals with trailing commas, variable refs, chained field access, and comments via skipped tokens. Convert Chevrotain lexing errors, parser errors, invalid double-quoted escapes, reserved keyword assignment, and builtin-name assignment into `MacroParseResult` errors using `macroParseError`. Use `at_line` from Chevrotain `startLine` when available, defaulting to `1` for source-level failures, and use `near_token` as the offending token image when available. Do not throw raw Chevrotain errors across `parseMacroSource`.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/unit/macro-parser.test.ts` exits 0 for test names `T-U-021` through `T-U-033` and `T-U-063` through `T-U-066`.
    - `grep -n "reserved_keyword_assignment\\|builtin_name_shadowing\\|invalid_literal\\|unexpected_token" src/macro/parser.ts tests/unit/macro-parser.test.ts` finds implementation and assertions.
    - `grep -n "throw new ParseError\\|throw new Error" src/macro/parser.ts | grep -v '^#'` returns no parser-boundary raw throw path for normal lex/parse/validation failures.
    - Requirement IDs covered by this task: MACRO-PARSE-02, MACRO-PARSE-03, MACRO-PARSE-10.
  </acceptance_criteria>
  <done>Basic macro statements, values, and validation parse to AST or structured parse_error results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add comparison, boolean, range, and control-flow grammar</name>
  <files>src/macro/parser.ts, tests/unit/macro-parser.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` §6.2.4, §6.2.5, §6.2.6, §6.2.7, §6.2.8, §6.2.10.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` tests T-U-042, T-U-052 through T-U-056, and parser-side parts of T-U-034 through T-U-043.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/parser.ts`.
  </read_first>
  <behavior>
    - Comparison operators parse into `BinaryExpr` nodes.
    - Boolean combinators parse with precedence `!` > `&&` > `||`; T-U-042 asserts `a || b && c` shape as `a || (b && c)`.
    - MACRO-PARSE-06: `RangeExpr` represents `start..end`; `range 5` and `range $start $end` parse as builtin call AST nodes without executing the builtin; `for i in range 5 do ... done` parses with that call as the iterable expression.
    - Pascal-style `for i = 1 to 10` returns `parse_error`.
    - `while $cond do ... done` and `for X in $list do ... done` parse; missing `do` returns structured parse_error.
  </behavior>
  <action>Extend grammar with expression precedence rules: `orExpr`, `andExpr`, `comparisonExpr`, `rangeExpr`, `unaryExpr`, and `primaryExpr`. Convert to AST nodes `BinaryExpr`, `UnaryExpr`, and `RangeExpr` using operator images `'||'`, `'&&'`, `'=='`, `'!='`, `'<'`, `'>'`, `'<='`, `'>='`, and `'..'`. Add parser coverage for the `range` builtin syntax as a builtin `Call` expression/iterable, specifically `range 5`, `range $start $end`, and `for i in range 5 do ... done`; this phase only parses and represents the call AST and must not implement runtime range builtin behavior. Add `whileLoop` requiring `While`, condition expression, `Do`, body block, and `Done`. Change `forLoop` to require `For Identifier In expression Do block Done`; do not accept the POC's old no-`do` form. Classify parser errors near `Done`, `Then`, and `Fi` into `missing_done`, `missing_then`, or `missing_fi` when the failed rule context proves that reason; otherwise return `unexpected_token`.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/unit/macro-parser.test.ts` exits 0 for test names `T-U-042`, `T-U-052`, `T-U-053`, `T-U-054`, `T-U-055`, `T-U-056`, plus explicit MACRO-PARSE-06 cases named `range 5`, `range $start $end`, and `for i in range 5 do`.
    - `grep -n "whileLoop\\|forLoop\\|RangeExpr\\|BinaryExpr\\|UnaryExpr\\|range" src/macro/parser.ts src/macro/types.ts tests/unit/macro-parser.test.ts` finds grammar/conversion usage, AST types, and range-builtin parser assertions.
    - `grep -n "missing_done\\|missing_then\\|missing_fi" src/macro/parser.ts tests/unit/macro-parser.test.ts` finds classifier code and assertions.
    - Requirement IDs covered by this task: MACRO-PARSE-04, MACRO-PARSE-05, MACRO-PARSE-06, MACRO-PARSE-07, MACRO-PARSE-08, MACRO-PARSE-10.
  </acceptance_criteria>
  <done>Expression/control-flow parser support matches the final v0 grammar and rejects outdated POC loop syntax.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parse namespaced JSON-arg tool calls as statements and expressions</name>
  <files>src/macro/parser.ts, tests/unit/macro-parser.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` §6.2.9 and §6.2.10.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` tests T-U-059 through T-U-062.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/parser.ts`.
  </read_first>
  <behavior>
    - T-U-059: `fq.search({query: "x"})` parses both as a statement and as assignment RHS.
    - T-U-060: object-literal args accept variable refs and chained field access values.
    - T-U-061: `if fq.x._exists() then ... fi` parses as a tool-call condition.
    - T-U-062: dotted server names such as `a.b.tool` return `parse_error`.
  </behavior>
  <action>Extend `toolCall` grammar to parse exactly `Identifier Dot Identifier LParen (objectLit | varOrField)? RParen`. In expression positions, allow tool calls anywhere `primaryExpr` is accepted, including assignment RHS and conditions. Preserve `ToolCall` AST as `{ kind: 'ToolCall', server, tool, arg, line }`; `arg` may be `ObjectLit`, bare `VarRef`, or `undefined`. Reject `a.b.tool(...)` because v0 supports exactly one server segment and one tool segment. Do not perform dispatch, permission pre-scan, broker checks, or tool existence checks in this phase.</action>
  <verify>
    <automated>npm test -- --run tests/unit/macro-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/unit/macro-parser.test.ts` exits 0 for test names `T-U-059`, `T-U-060`, `T-U-061`, and `T-U-062`.
    - `grep -n "ToolCall" src/macro/parser.ts src/macro/types.ts tests/unit/macro-parser.test.ts` finds conversion and assertions.
    - `grep -n "dispatch\\|broker\\|assembleNativeToolRegistry\\|getNativeToolCatalog" src/macro/parser.ts` returns no matches.
    - Requirement ID covered by this task: MACRO-PARSE-09.
  </acceptance_criteria>
  <done>Namespaced tool-call syntax is represented in AST without crossing into runtime dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| token stream -> parser | Untrusted token sequences become AST nodes used by later evaluator phases. |
| parser validation -> expected error | Syntax/validation failures become structured parse_error data, not crashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-08 | Tampering | binding parser | mitigate | Reject reserved keyword and builtin-name assignment with `reserved_keyword_assignment` and `builtin_name_shadowing` before execution. |
| T-131-09 | Denial of Service | `parseMacroSource` | mitigate | Return `MacroParseResult` errors for lex/parse/validation failures; no raw parser exceptions for malformed macro source. |
| T-131-10 | Spoofing | namespaced tool-call grammar | mitigate | Support exactly `server.tool(...)`; reject dotted server names to avoid ambiguous namespace interpretation. |
| T-131-11 | Tampering | operator parser | mitigate | Encode comparison/boolean/range AST nodes with explicit operator strings so later evaluator cannot infer from raw token text inconsistently. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/macro-parser.test.ts`, then `npm test -- macro-lexer macro-parser macro-fence-extractor macro-source-ref`, then `npm run build`.
</verification>

<success_criteria>
The parser produces typed ASTs and structured parse errors for every parser-layer Phase 131 grammar requirement without implementing evaluator/runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/131-lexer-parser-fence-extraction/131-03-SUMMARY.md`
</output>
