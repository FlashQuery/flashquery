---
phase: 124-document-write-primitives
plan: 03
type: execute
wave: 3
depends_on: [124-02]
files_modified:
  - src/mcp/tools/compound.ts
  - src/mcp/utils/markdown-sections.ts
  - src/mcp/tool-metadata.ts
  - tests/unit/replace-doc-section.test.ts
  - tests/unit/tool-metadata.test.ts
  - tests/integration/documents.integration.test.ts
  - tests/e2e/protocol.test.ts
  - .planning/phases/124-document-write-primitives/TRACEABILITY.md
autonomous: true
requirements: [DOC-07]
must_haves:
  truths:
    - "replace_doc_section uses include_nested instead of include_subheadings."
    - "replace_doc_section(content:\"\") deletes the matched heading line."
    - "replace_doc_section returns document identification plus extracted_section metadata."
    - "replace_doc_section does not expose old content, hashes, or line ranges in the response."
  artifacts:
    - path: "src/mcp/tools/compound.ts"
      provides: "replace_doc_section final schema and JSON output"
      contains: "extracted_section"
    - path: "tests/unit/replace-doc-section.test.ts"
      provides: "section replacement/deletion tests"
  key_links:
    - from: "src/mcp/tools/compound.ts"
      to: "src/mcp/utils/markdown-sections.ts"
      via: "shared heading boundary matching"
      pattern: "getSectionBoundaries|include_nested"
---

<objective>
Migrate `replace_doc_section` to the Phase 124 DOC-07 contract.

Purpose: make heading-anchored replacement/deletion explicit, structured, and aligned with `get_document.sections` nested behavior.
Output: final replace schema/output and focused tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/124-document-write-primitives/124-CONTEXT.md
@.planning/phases/124-document-write-primitives/124-RESEARCH.md
@.planning/phases/124-document-write-primitives/124-PATTERNS.md
@.planning/phases/124-document-write-primitives/TRACEABILITY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
@src/mcp/tools/compound.ts
@src/mcp/utils/markdown-sections.ts
@src/mcp/utils/response-formats.ts
@tests/integration/documents.integration.test.ts
@tests/e2e/protocol.test.ts

<interfaces>
Executors must read the two product docs above before making any requirement or test-scope decision. If a question remains after those docs, phase artifacts, and code analogs, ask the user.

Final replacement metadata:
```json
{
  "extracted_section": {
    "heading": "Summary",
    "level": 2,
    "old_content_length": 42,
    "new_content_length": 17,
    "include_nested": false,
    "heading_removed": false
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin replace_doc_section Boundary And Deletion Semantics</name>
  <files>src/mcp/utils/markdown-sections.ts, tests/unit/replace-doc-section.test.ts</files>
  <read_first>
    - src/mcp/utils/markdown-sections.ts
    - tests/unit/advanced-document-tools.test.ts
    - tests/unit/insert-in-doc.test.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <behavior>
    - Normal replacement preserves the matched heading line and replaces only selected body.
    - `include_nested:true` replaces body plus nested child sections through next same-or-higher heading.
    - `include_nested:false` replaces only direct body and preserves child headings/content.
    - `content:""` removes the heading line; with `include_nested:false`, child headings remain and are promoted in place.
    - Response metadata lengths use character counts for old and new selected content.
  </behavior>
  <action>Add or extend pure helper coverage for section replacement boundaries. If needed, add helper functions in `markdown-sections.ts` that return replacement slices and metadata without performing I/O. Tests must assert exact body output for nested replacement, direct-body replacement, deletion with nested children removed, and deletion with children preserved. Keep matching behavior aligned with Plan 02 `heading_match`/`heading_level` rules.</action>
  <verify>
    <automated>npm test -- tests/unit/replace-doc-section.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/replace-doc-section.test.ts` contains `content: ""`.
    - `tests/unit/replace-doc-section.test.ts` contains `heading_removed`.
    - `tests/unit/replace-doc-section.test.ts` contains `include_nested:false`.
    - `tests/unit/replace-doc-section.test.ts` contains `old_content_length`.
  </acceptance_criteria>
  <done>DOC-07 replacement and deletion edge cases are pinned before handler migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate replace_doc_section Handler And Metadata</name>
  <files>src/mcp/tools/compound.ts, src/mcp/tool-metadata.ts, tests/unit/replace-doc-section.test.ts, tests/unit/tool-metadata.test.ts</files>
  <read_first>
    - src/mcp/tools/compound.ts
    - src/mcp/utils/markdown-sections.ts
    - src/mcp/utils/response-formats.ts
    - src/mcp/tool-metadata.ts
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <action>Change public schema from `include_subheadings` to `include_nested` with default true. Add `heading_match: "contains" | "exact"` and `heading_level?: 1..6` to match insert behavior. Return `jsonToolResult({ ...documentIdentification(...), extracted_section: { heading, level, old_content_length, new_content_length, include_nested, heading_removed } })`. Use `jsonExpectedError` for no headings, heading not found, ambiguous heading, invalid occurrence, and invalid heading_level. Remove response fields/prose that expose `oldContent`, hashes, line ranges, or undo text. Replace metadata description with the literal Requirements §4.14 registered description.</action>
  <verify>
    <automated>npm test -- tests/unit/replace-doc-section.test.ts tests/unit/tool-metadata.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/tools/compound.ts` contains `include_nested`.
    - `src/mcp/tools/compound.ts` does not contain `include_subheadings` outside migration comments or removed legacy tests.
    - `src/mcp/tools/compound.ts` contains `extracted_section`.
    - `src/mcp/tools/compound.ts` contains `heading_removed`.
    - `src/mcp/tool-metadata.ts` contains `Passing content: "" deletes the selected section`.
  </acceptance_criteria>
  <done>`replace_doc_section` exposes final DOC-07 input and output contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove replace_doc_section Through Storage And MCP</name>
  <files>tests/integration/documents.integration.test.ts, tests/e2e/protocol.test.ts, .planning/phases/124-document-write-primitives/TRACEABILITY.md</files>
  <read_first>
    - tests/integration/documents.integration.test.ts
    - tests/e2e/protocol.test.ts
    - .planning/phases/124-document-write-primitives/TRACEABILITY.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/MCP Tool Consolidation/MCP Tool Consolidation Test Plan.md
  </read_first>
  <action>Add integration tests for nested replacement, direct-body replacement, `content:""` deletion including heading line, not-found expected error, and frontmatter preservation. Add an E2E MCP round-trip for replacement then `get_document.sections` to verify final body. Update TRACEABILITY DOC-07 with implemented unit/integration/E2E evidence.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/documents.integration.test.ts && npm run test:e2e -- tests/e2e/protocol.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/documents.integration.test.ts` contains `replace_doc_section`.
    - `tests/integration/documents.integration.test.ts` contains `heading_removed`.
    - `tests/e2e/protocol.test.ts` contains `replace_doc_section`.
    - `TRACEABILITY.md` DOC-07 row contains `implemented`.
  </acceptance_criteria>
  <done>DOC-07 is proven through handler/storage and MCP protocol coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> replace_doc_section | Untrusted heading/content selects and rewrites document sections. |
| replace_doc_section -> vault filesystem | Replacement body is written back to markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | create destinations | transfer | Covered by Plan 01. |
| T-124-02 | Tampering/Spoofing | reserved frontmatter | accept | Handler preserves existing frontmatter and accepts no frontmatter object. |
| T-124-03 | Tampering | heading selection | mitigate | Ambiguous heading matches return `ambiguous_identifier`; no arbitrary regex/line/byte replacement API is added. |
| T-124-04 | Denial of Service/Race | document write lock | mitigate | Preserve lock acquisition/release and canonical conflict mapping for lock contention. |
| T-124-05 | Information Disclosure | disabled-category leakage in apply_tags | transfer | Covered by Plan 04. |
</threat_model>

<verification>
`npm test -- tests/unit/replace-doc-section.test.ts tests/unit/tool-metadata.test.ts`
`npm run test:integration -- tests/integration/documents.integration.test.ts`
`npm run test:e2e -- tests/e2e/protocol.test.ts`
</verification>

<success_criteria>
- DOC-07 uses `include_nested`, supports empty-string deletion including heading line, and returns `extracted_section`.
- No public `include_subheadings` final parameter remains for `replace_doc_section`.
- No response includes old content, content hash, undo blob, or line-range metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/124-document-write-primitives/124-03-SUMMARY.md`.
</output>
