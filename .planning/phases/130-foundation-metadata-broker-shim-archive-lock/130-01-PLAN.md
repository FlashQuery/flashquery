---
phase: 130-foundation-metadata-broker-shim-archive-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/utils/response-formats.ts
  - src/mcp/tool-metadata.ts
  - src/mcp/tools/macro.ts
  - src/mcp/server.ts
  - src/services/mcp-broker.ts
  - tests/unit/response-formats.test.ts
  - tests/unit/tool-metadata.test.ts
  - tests/unit/mcp-server-tools.test.ts
  - tests/unit/mcp-broker.test.ts
autonomous: true
requirements:
  - MACRO-RESP-01
  - MACRO-RESP-02
  - MACRO-RESP-03
  - MACRO-RESP-04
  - MACRO-OBS-01
  - MACRO-INT-05
  - MACRO-INT-06
must_haves:
  truths:
    - "Implementation agents read the canonical Macro Language requirements and test plan before editing Phase 130 files per D-01."
    - "Macro response/type exports exist in src/mcp/utils/response-formats.ts without removing or changing existing response helpers per D-02."
    - "call_macro is present in canonical tool metadata and registered on the MCP server with safe scaffold behavior per D-03."
    - "NullMcpBroker exists and exposes no brokered connectivity or handlers per D-04."
  artifacts:
    - path: "src/mcp/utils/response-formats.ts"
      provides: "MACRO_ERROR_CODES, MacroErrorCode, TraceStep, MacroExecutionResult, MacroDryRunResult, MacroSuccessPayload, macroResult"
      contains: "export const MACRO_ERROR_CODES"
    - path: "src/mcp/tool-metadata.ts"
      provides: "call_macro metadata entry"
      contains: "current('call_macro', ['llm'], 'admin', D.callMacro, RECURSIVE_MODEL_REASON)"
    - path: "src/mcp/tools/macro.ts"
      provides: "registerMacroTools scaffold registrar"
      contains: "registerMacroTools"
    - path: "src/services/mcp-broker.ts"
      provides: "McpBroker interface and NullMcpBroker implementation"
      contains: "export class NullMcpBroker"
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/mcp/tools/macro.ts"
      via: "import and registerMacroTools(server, config) before validateAndCacheNativeToolSchemas"
      pattern: "registerMacroTools\\(server, config\\)"
    - from: "src/mcp/tool-metadata.ts"
      to: "src/llm/tool-registry.ts"
      via: "delegatedHardExcludedReason: RECURSIVE_MODEL_REASON"
      pattern: "call_macro.*RECURSIVE_MODEL_REASON"
---

<objective>
Add the Phase 130 macro foundation surface: macro response types/helpers, `call_macro` metadata and MCP registration scaffold, and broker-ready null shim.

Purpose: Later macro phases need typed response contracts, canonical metadata, and a broker seam before parser/evaluator work starts.
Output: Additive TypeScript exports, a registered but non-executing `call_macro` scaffold, focused unit tests, and a null broker service.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-CONTEXT.md
@.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-RESEARCH.md
@.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-PATTERNS.md
@.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md

<read_first>
Before any edit, read these canonical docs in this order per D-01:
1. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md`
2. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md`

Then read the specific files named in each task's `<files>` block before modifying them.
</read_first>

<interfaces>
Key existing contracts:

```typescript
export const CANONICAL_ERROR_CODES = ['not_found', 'ambiguous_identifier', 'permission_denied', 'invalid_input', 'conflict', 'unsupported', 'not_supported_in_mode'] as const;
export type WarningCode = string;
export interface ToolResult { content: Array<{ type: 'text'; text: string }>; isError?: boolean; }
export function jsonToolResult(payload: unknown): ToolResult;
export function jsonExpectedError(error: ErrorEnvelope): ToolResult;
export function jsonRuntimeError(message: string, details?: Record<string, unknown>): ToolResult;
export function withWarnings<T extends Record<string, unknown>>(payload: T, warnings: WarningCode[]): T & { warnings?: WarningCode[] };
```

```typescript
const RECURSIVE_MODEL_REASON = 'Tool can recursively call models and is not safe for delegated native access.';
current('call_model', ['llm'], 'admin', D.callModel, RECURSIVE_MODEL_REASON);
```

```typescript
function createMcpServer(config: FlashQueryConfig, version: string): McpServer {
  registerLlmTools(server, config);
  registerLlmUsageTools(server, config);
  validateAndCacheNativeToolSchemas(getNativeToolCatalog(server));
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add macro response contracts and helper</name>
  <files>src/mcp/utils/response-formats.ts, tests/unit/response-formats.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` sections §6.7.1, §6.8.1-§6.8.4, and §7.1.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` rows T-U-191, T-U-192, T-U-199, T-U-200, T-U-205, T-U-206, T-U-207, T-U-208.
    - `src/mcp/utils/response-formats.ts`
    - `tests/unit/response-formats.test.ts`
  </read_first>
  <behavior>
    - T-U-191: `TraceStep` shape supports flat records only: `kind`, optional `name`, `args`, `result`, `message`, required ISO `at`, optional `elapsed_ms`.
    - T-U-192: `TraceStep` must not include `children`.
    - T-U-199: `MacroExecutionResult` includes `task_id`, `result`, optional `trace`, `token_total`, `model_calls`, `external_tool_calls`, and `warnings`.
    - T-U-200: `MacroDryRunResult` includes `task_id`, `parsed_ok: true`, `input_var_contract`, `tool_references`, and `server_references`.
    - T-U-205/T-U-206: `MACRO_ERROR_CODES` is exported alongside `CANONICAL_ERROR_CODES` with the full v0 stable set.
  </behavior>
  <action>
    Add only additive exports per D-02. Do not edit existing implementations of `jsonToolResult`, `jsonExpectedError`, `jsonRuntimeError`, `withWarnings`, `ErrorEnvelope`, `ToolResult`, `CANONICAL_ERROR_CODES`, or `WarningCode`.

    In `src/mcp/utils/response-formats.ts`, add:
    - `export const MACRO_ERROR_CODES = [...] as const` with exactly these strings: `macro_aborted`, `forbidden_tools`, `unknown_server`, `unknown_tool`, `forbidden_path`, `forbidden_shell_flag`, `template_masquerade_tools_not_callable_from_macro`, `budget_exceeded`, `timeout`, `tool_call_failed`, `cancelled`, `parse_error`.
    - `export type MacroErrorCode = (typeof MACRO_ERROR_CODES)[number];`
    - `export interface TraceStep` with `kind: 'tool_call' | 'model_call' | 'log' | 'progress' | 'fail' | 'exit'`, optional `name?: string`, `args?: unknown`, `result?: unknown`, `message?: string`, required `at: string`, optional `elapsed_ms?: number`; no `children`.
    - `export interface MacroExecutionResult` with `task_id: string`, `result: unknown`, optional `trace?: TraceStep[]`, `token_total?: number`, `model_calls?: number`, `external_tool_calls?: number`, and `warnings?: WarningCode[]`.
    - `export interface MacroDryRunResult` with `task_id: string`, `parsed_ok: true`, `input_var_contract: Record<string, unknown>`, `tool_references: string[]`, `server_references: string[]`, and optional `warnings?: WarningCode[]`.
    - `export type MacroSuccessPayload = MacroExecutionResult | MacroDryRunResult;`
    - `export function macroResult(payload: MacroSuccessPayload): MacroSuccessPayload { return payload; }`

    Update `tests/unit/response-formats.test.ts` with focused tests asserting the exports, the exact error-code array contents, the dry-run discriminator `parsed_ok: true`, `macroResult(payload) === payload`, and that existing `jsonToolResult`, `jsonExpectedError`, and `withWarnings` behavior still passes.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/response-formats.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export const MACRO_ERROR_CODES|export type MacroErrorCode|export interface TraceStep|export interface MacroExecutionResult|export interface MacroDryRunResult|export type MacroSuccessPayload|export function macroResult" src/mcp/utils/response-formats.ts` prints all seven symbols.
    - `rg -n "children" src/mcp/utils/response-formats.ts tests/unit/response-formats.test.ts` has no `TraceStep` children assertion or field.
    - `npm test -- --run tests/unit/response-formats.test.ts` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>MACRO-RESP-01, MACRO-RESP-02, MACRO-RESP-03, MACRO-RESP-04, and MACRO-OBS-01 type/helper exports are present and tested without changing existing response helper semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register call_macro metadata and safe MCP scaffold</name>
  <files>src/mcp/tool-metadata.ts, src/mcp/tools/macro.ts, src/mcp/server.ts, tests/unit/tool-metadata.test.ts, tests/unit/mcp-server-tools.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` sections §6.9.5, §7.5, and §8 Phase 1.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` rows T-U-228, T-U-229, and the `call_macro` checklist items.
    - `src/mcp/tool-metadata.ts`
    - `src/mcp/server.ts`
    - existing registrar analogs in `src/mcp/tools/llm.ts` and `src/mcp/tools/llm-usage.ts`
    - `tests/unit/tool-metadata.test.ts`
    - `tests/unit/mcp-server-tools.test.ts`
  </read_first>
  <behavior>
    - `call_macro` metadata is final/current, category `['llm']`, tier `admin`, delegated-ineligible through `RECURSIVE_MODEL_REASON`.
    - `get_briefing` and `insert_doc_link` legacy replacement expectations continue to point at `call_macro`.
    - Public MCP registration includes `call_macro`, but Phase 130 returns a canonical expected-error scaffold and does not parse, resolve, dry-run, or execute macro sources.
  </behavior>
  <action>
    In `src/mcp/tool-metadata.ts`, add `D.callMacro` with the existing four-line `description(summary, useWhen, doNotUseWhen, example)` helper. Use these concrete values:
    - Summary: `Run a FlashQuery macro as one structured orchestration request.`
    - Use when: `Use when you need deterministic multi-step FlashQuery orchestration through call_macro.`
    - Do not use when: `Do not use when you need direct model recursion or when macro execution is unavailable in this phase.`
    - Example: `call_macro({ "source": "echo \"hello\"" })`

    Add `current('call_macro', ['llm'], 'admin', D.callMacro, RECURSIVE_MODEL_REASON)` immediately after the existing `call_model` row per D-03. Preserve the existing `get_briefing` and `insert_doc_link` replacement behavior; do not add aliases.

    Create `src/mcp/tools/macro.ts` exporting `registerMacroTools(server: McpServer, config: FlashQueryConfig): void`. Use ESM imports, `@modelcontextprotocol/sdk/server/mcp.js`, `zod`, `getIsShuttingDown`, and `jsonExpectedError`/`jsonRuntimeError` as appropriate. Register `call_macro` with a Zod-backed permissive scaffold input shape, such as `inputSchema: { source: z.string().optional(), source_ref: z.string().optional(), input_vars: z.record(z.string(), z.unknown()).optional(), budget: z.record(z.string(), z.unknown()).optional(), dry_run: z.boolean().optional(), trace: z.enum(['full', 'summary', 'none']).optional(), progress: z.enum(['full', 'milestones', 'silent']).optional() }`, but do not implement full source validation. Handler behavior:
    - first checks `getIsShuttingDown()` and returns runtime `isError: true` shutdown response consistent with other tools;
    - otherwise returns `jsonExpectedError({ error: 'unsupported', message: 'call_macro is registered but macro execution is not implemented in Phase 130.', details: { reason: 'phase_130_scaffold' } })`.

    In `src/mcp/server.ts`, import `registerMacroTools` and call `registerMacroTools(server, config)` immediately after `registerLlmUsageTools(server, config)` and before `validateAndCacheNativeToolSchemas(getNativeToolCatalog(server))`.

    Update unit tests to assert metadata and registrar visibility. Do not implement parser, evaluator, source_ref resolution, dry-run behavior, progress emission, budgets, scenario matrices, or real recursive macro execution in this phase.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/tool-metadata.test.ts tests/unit/mcp-server-tools.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "callMacro: description|current\\('call_macro', \\['llm'\\], 'admin', D.callMacro, RECURSIVE_MODEL_REASON\\)" src/mcp/tool-metadata.ts` prints both metadata additions.
    - `rg -n "registerMacroTools|call_macro|phase_130_scaffold|unsupported" src/mcp/tools/macro.ts` prints the registrar, tool name, scaffold reason, and canonical expected-error code.
    - `rg -n "registerMacroTools\\(server, config\\)" src/mcp/server.ts` prints exactly one registration after `registerLlmUsageTools(server, config)`.
    - `npm test -- --run tests/unit/tool-metadata.test.ts tests/unit/mcp-server-tools.test.ts` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>MACRO-INT-05 is satisfied: `call_macro` is visible in metadata and MCP registration with safe scaffold behavior and delegated recursive access blocked by existing metadata policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add broker-ready NullMcpBroker shim</name>
  <files>src/services/mcp-broker.ts, tests/unit/mcp-broker.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md` sections §3.1, §6.9.6, and §7.3.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md` rows T-U-153, T-U-155, T-U-231, T-U-232.
    - `src/llm/tool-registry.ts` for `NativeToolHandler`
    - `src/llm/client.ts` for null implementation style
    - `src/services/write-lock.ts` for service module style
  </read_first>
  <behavior>
    - `NullMcpBroker.isConnected(_)` always resolves `false`.
    - `NullMcpBroker.getToolHandler(_, _)` always returns `null`.
    - The shim does not start processes, open transports, cache connectivity, discover tools, or grant access to brokered servers.
  </behavior>
  <action>
    Create `src/services/mcp-broker.ts` per D-04. Import `type { NativeToolHandler }` from `../llm/tool-registry.js` and export:
    - `export interface McpBroker { isConnected(serverId: string): Promise<boolean>; getToolHandler(serverId: string, toolName: string): NativeToolHandler | null; }`
    - `export class NullMcpBroker implements McpBroker` with `async isConnected(_serverId: string): Promise<boolean> { return false; }` and `getToolHandler(_serverId: string, _toolName: string): NativeToolHandler | null { return null; }`

    Add `tests/unit/mcp-broker.test.ts` with T-U-231 and T-U-232 coverage. Keep tests independent of the macro evaluator and `_exists()` implementation, because those land in later phases.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/mcp-broker.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/services/mcp-broker.ts` exits 0.
    - `rg -n "export interface McpBroker|isConnected\\(serverId: string\\): Promise<boolean>|getToolHandler\\(serverId: string, toolName: string\\): NativeToolHandler \\| null|export class NullMcpBroker" src/services/mcp-broker.ts` prints all required contracts.
    - `rg -n "return false|return null" src/services/mcp-broker.ts` prints the null behavior.
    - `npm test -- --run tests/unit/mcp-broker.test.ts` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>MACRO-INT-06 is satisfied: broker readiness exists as a typed seam and `NullMcpBroker` exposes no unavailable brokered access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> call_macro scaffold | Untrusted MCP input reaches a registered tool before the macro engine exists. |
| Tool metadata -> delegated native registry | Tool eligibility controls what a delegated model may call recursively. |
| Macro engine -> broker shim | Later macro dispatch will consult broker connectivity across an external-service boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | T/I | `src/mcp/utils/response-formats.ts` | mitigate | Add macro exports only; tests assert existing helpers still serialize expected payloads and errors unchanged. |
| T-130-02 | E | `src/mcp/tool-metadata.ts` | mitigate | Register `call_macro` as admin with `RECURSIVE_MODEL_REASON`; tests assert `delegatedEligible === false` so delegated recursive access is blocked. |
| T-130-03 | E/T | `src/mcp/tools/macro.ts` | mitigate | Scaffold returns `unsupported` with `details.reason: "phase_130_scaffold"` and performs no parsing, dispatch, source_ref resolution, dry-run execution, or mutation. |
| T-130-04 | S/E | `src/services/mcp-broker.ts` | mitigate | `NullMcpBroker` always returns disconnected/null handler results; no real broker process, transport, or handler discovery exists in Phase 130. |
| T-130-05 | I | Scaffold overreach | mitigate | Acceptance criteria grep for only scaffold reason and registration; parser/evaluator/progress/budget/source_ref files are not part of this plan. |
</threat_model>

<verification>
Focused checks:
- `npm test -- --run tests/unit/response-formats.test.ts tests/unit/tool-metadata.test.ts tests/unit/mcp-server-tools.test.ts tests/unit/mcp-broker.test.ts`
- `npm run build`

Coverage audit:
- MACRO-RESP-01/02/03/04: response contracts and `macroResult`.
- MACRO-OBS-01: flat `TraceStep` type.
- MACRO-INT-05: metadata and MCP registrar.
- MACRO-INT-06: `McpBroker` and `NullMcpBroker`.
</verification>

<success_criteria>
- Canonical docs are named in every task read gate per D-01.
- No existing response helper behavior is modified.
- `call_macro` is metadata-visible, MCP-registered, delegated-hard-excluded, and returns a canonical non-executing scaffold.
- `NullMcpBroker` is typed and tested.
- Parser, evaluator, source_ref resolution, real execution, progress, budgets, scenario matrices, and real broker transport remain outside this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/130-foundation-metadata-broker-shim-archive-lock/130-01-SUMMARY.md`
</output>
