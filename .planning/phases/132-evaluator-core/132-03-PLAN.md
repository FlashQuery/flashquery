---
phase: 132-evaluator-core
plan: 03
type: execute
wave: 3
depends_on:
  - 132-01
  - 132-02
files_modified:
  - src/macro/evaluator.ts
  - tests/unit/macro-termination.test.ts
autonomous: true
requirements:
  - MACRO-EVAL-06
must_haves:
  truths:
    - "Fall-off completion returns a macro success payload with `result: null` and no fatal error flag."
    - "`exit` halts immediately and returns its value through the success payload."
    - "`fail` halts immediately as expected `macro_aborted`, not fatal runtime failure."
    - "Unexpected tool errors and evaluator runtime errors halt as `isError: true` runtime envelopes."
  artifacts:
    - path: "src/macro/evaluator.ts"
      provides: "Termination errors and ToolResult envelope mapping"
      exports: ["MacroExitError", "MacroFailError", "MacroRuntimeError", "evaluateProgram"]
    - path: "tests/unit/macro-termination.test.ts"
      provides: "T-U-084 through T-U-091 termination coverage"
      contains: "T-U-084"
  key_links:
    - from: "src/macro/evaluator.ts"
      to: "src/mcp/utils/response-formats.ts"
      via: "macroResult, jsonExpectedError, jsonRuntimeError"
      pattern: "macroResult|jsonExpectedError|jsonRuntimeError"
---

<objective>
Implement the Phase 132 termination contract and envelope mapping for fall-off, `exit`, `fail`, expected tool-error values, unexpected tool failures, and runtime errors.

Purpose: This satisfies MACRO-EVAL-06 and makes the evaluator return the same ToolResult shape later handler phases can call directly.
Output: Termination classes/mapping in `src/macro/evaluator.ts` and `tests/unit/macro-termination.test.ts`.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-evaluator-core/132-CONTEXT.md
@.planning/phases/132-evaluator-core/132-RESEARCH.md
@.planning/phases/132-evaluator-core/132-VALIDATION.md
@.planning/phases/132-evaluator-core/132-PATTERNS.md
@.planning/phases/132-evaluator-core/132-02-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/builtins.ts
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/run.ts
@src/macro/evaluator.ts
@src/mcp/utils/response-formats.ts

<interfaces>
From `src/mcp/utils/response-formats.ts`:
```typescript
export interface MacroExecutionResult { task_id: string; result: unknown; trace?: TraceStep[]; token_total?: number; model_calls?: number; external_tool_calls?: number; warnings?: WarningCode[]; }
export function macroResult(payload: MacroSuccessPayload): ToolResult;
export function jsonExpectedError(error: ErrorEnvelope): ToolResult;
export function jsonRuntimeError(error: Omit<ErrorEnvelope, 'error'> & { error?: string }): ToolResult;
```

Executor source-order rule: read the two primary Macro Language docs first for any requirements, spec, or test question before asking the user. Requirements doc wins over POC; requirements/test-plan conflict must be reported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map fall-off, exit, and fail to canonical envelopes</name>
  <files>src/macro/evaluator.ts, tests/unit/macro-termination.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md (REQ-024, REQ-036 note only)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md (T-U-084 through T-U-087, T-U-090, T-U-091)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts (MacroExitError, MacroFailError)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/builtins.ts (fail/exit behavior)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/run.ts (POC envelope divergence)
    - src/macro/evaluator.ts
    - src/mcp/utils/response-formats.ts
    - tests/unit/macro-envelopes.test.ts
    - tests/unit/response-formats.test.ts
    - tests/unit/macro-termination.test.ts (create if absent)
  </read_first>
  <behavior>
    - T-U-084: Fall-off-end returns `{ result: null }` inside a macro success payload and `isError` is absent/false.
    - T-U-085: `exit "done"` returns result `"done"` and statements below do not run.
    - T-U-086: `exit { a: 1, b: [2,3] }` returns the structured value.
    - T-U-087: `fail "msg"` returns `{ error: "macro_aborted", message: "msg", details: { line }, isError: false }` and statements below do not run.
    - T-U-090: zero-arg `exit` returns `null`.
    - T-U-091: multi-arg `exit a b` returns a parse-time or pre-flight invalid_input-style expected error with `details.reason = "exit_argument_count"` before any statement executes.
  </behavior>
  <action>
    Export `MacroExitError` and `MacroFailError`. The evaluator may implement `exit` and `fail` through injected builtins that throw these errors, or intercept `Call` names `exit` and `fail` as control calls for this phase. Because Phase 131 parses bare calls such as `exit "done"` and `fail "msg"` as `Pipeline` statements, the termination implementation MUST work through `execStatement(Pipeline)` / injected `Call` evaluation, not only direct `Call` expression handling. The concrete outcome must match REQ-024: `exit` produces `macroResult({ task_id, result })`; `fail` produces `jsonExpectedError({ error: "macro_aborted", message, details: { line } })`.

    `evaluateProgram` must wrap execution in one boundary that maps:
    fall-off -> `macroResult({ task_id, result: null, trace })`;
    `MacroExitError` -> `macroResult({ task_id, result: error.value, trace })`;
    `MacroFailError` -> `jsonExpectedError({ error: "macro_aborted", message: error.message, details: { line: error.line } })`;
    `MacroRuntimeError` -> `jsonRuntimeError({ error: "tool_call_failed", message: error.message, details: error.details })`.
    Append trace steps with `kind: "exit"` and `kind: "fail"` before terminal mapping. Keep full trace modes out of scope; this phase only needs the flat trace array already in context.
  </action>
  <acceptance_criteria>
    - `grep -n "export class MacroExitError" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "export class MacroFailError" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "case 'Pipeline'\\|case \"Pipeline\"" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "exit \\\"done\\\"\\|fail \\\"msg\\\"" tests/unit/macro-termination.test.ts` returns at least one line.
    - `grep -n "macro_aborted" src/macro/evaluator.ts tests/unit/macro-termination.test.ts` returns at least two lines.
    - `grep -n "exit_argument_count" src/macro/evaluator.ts tests/unit/macro-termination.test.ts` returns at least one line.
    - `grep -n "T-U-084\\|T-U-085\\|T-U-086\\|T-U-087\\|T-U-090\\|T-U-091" tests/unit/macro-termination.test.ts | wc -l | tr -d ' '` returns `6`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/macro-termination.test.ts</automated>
  </verify>
  <done>Fall-off, exit, fail, zero-arg exit, multi-arg exit, and terminal trace steps satisfy REQ-024.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize expected tool envelopes and unexpected tool failures</name>
  <files>src/macro/evaluator.ts, tests/unit/macro-termination.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md (REQ-024 criteria 4 and 5)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md (T-U-088, T-U-089)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts (runToolCall reference)
    - src/macro/evaluator.ts
    - src/mcp/utils/response-formats.ts
    - tests/unit/macro-termination.test.ts
  </read_first>
  <behavior>
    - T-U-088: A tool handler result with `{ isError: false, error: "not_found" }` becomes a normal MacroValue object and can be branched on via `.error`.
    - T-U-089: A tool handler throw or `{ isError: true }` halts with `tool_call_failed` and `isError: true`.
  </behavior>
  <action>
    Implement `ToolCall` expression/statement evaluation against injected `context.dispatchTool(server, tool, arg, context)`. Only normalize the result shape; do not implement real namespaced dispatch, permission pre-scan, or broker/native registry integration in this phase. For `ToolCall.arg`, support undefined as `{}`, object literals evaluated to an object, variable references resolving to an object, and field access resolving to an object; invalid non-object args throw `MacroRuntimeError` with `details.reason = "tool_argument_type_mismatch"`.

    If injected dispatch returns a ToolResult-like object with `isError: true`, parse its first JSON text payload when possible and throw `MacroRuntimeError` with `details.server`, `details.tool`, `details.line`, and `details.underlying_error`. If injected dispatch throws, map to the same `tool_call_failed` runtime path. If injected dispatch returns a JSON payload without `isError: true`, return the parsed payload as a MacroValue, including expected envelopes with an `error` field. Record a `kind: "tool_call"` trace step containing `name: "server.tool"`, `args`, and `result`.
  </action>
  <acceptance_criteria>
    - `grep -n "dispatchTool" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "tool_argument_type_mismatch" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "tool_call_failed" src/macro/evaluator.ts tests/unit/macro-termination.test.ts` returns at least two lines.
    - `grep -n "T-U-088\\|T-U-089" tests/unit/macro-termination.test.ts | wc -l | tr -d ' '` returns `2`.
    - `grep -n "isError: false" tests/unit/macro-termination.test.ts` returns at least one line.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/macro-termination.test.ts</automated>
  </verify>
  <done>Expected tool envelopes continue as values, while thrown or `isError: true` tool failures halt with fatal runtime envelopes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluator -> ToolResult envelope | Evaluator must distinguish expected nonfatal envelopes from fatal tool failures. |
| injected dispatch -> evaluator | Dispatch can throw, return malformed payloads, or return fatal ToolResult objects. |
| macro control calls -> response envelope | `exit` and `fail` intentionally stop execution but differ in fatality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-09 | R/T | `fail` mapping | mitigate | Map deliberate `fail` to `jsonExpectedError` with `error: "macro_aborted"` and `isError: false`; test T-U-087. |
| T-132-10 | T | tool failure normalization | mitigate | Treat `isError: false` envelopes as values and only `isError: true` or throws as fatal; tests T-U-088 and T-U-089. |
| T-132-11 | I | runtime error details | mitigate | Include structured underlying error details without stack traces in `tool_call_failed`. |
| T-132-12 | D | statements after termination | mitigate | Use thrown terminal errors or equivalent immediate return to prevent later statements running; tests assert no post-terminal mutation. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/macro-termination.test.ts`.
Then run `npm test -- --reporter=verbose macro-evaluator macro-scope macro-termination`.
</verification>

<success_criteria>
`tests/unit/macro-termination.test.ts` passes T-U-084 through T-U-091, and evaluator termination returns canonical MacroExecutionResult, expected error, or runtime error ToolResult shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/132-evaluator-core/132-03-SUMMARY.md`.
</output>
