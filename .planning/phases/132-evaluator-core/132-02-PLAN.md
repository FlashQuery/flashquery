---
phase: 132-evaluator-core
plan: 02
type: execute
wave: 2
depends_on:
  - 132-01
files_modified:
  - src/macro/evaluator.ts
  - tests/unit/macro-evaluator.test.ts
autonomous: true
requirements:
  - MACRO-EVAL-03
  - MACRO-EVAL-04
  - MACRO-EVAL-05
  - MACRO-EVAL-08
must_haves:
  truths:
    - "Truthiness is deterministic across every v0 value type."
    - "Double-quoted interpolation resolves variables and chained field references."
    - "Invalid field access halts with a predictable runtime error instead of returning undefined."
    - "Assignment captures RHS values at evaluation time."
  artifacts:
    - path: "src/macro/evaluator.ts"
      provides: "Expression evaluator, truthiness, interpolation, field access, RHS evaluation order"
      exports: ["isTruthy", "evaluateProgram"]
    - path: "tests/unit/macro-evaluator.test.ts"
      provides: "T-U-073 through T-U-083 and T-U-095 through T-U-096 coverage"
      contains: "T-U-073"
  key_links:
    - from: "src/macro/evaluator.ts"
      to: "src/macro/types.ts"
      via: "BinaryExpr, UnaryExpr, FieldAccess, RangeExpr, StringLit"
      pattern: "BinaryExpr|UnaryExpr|FieldAccess|StringLit"
---

<objective>
Complete core expression semantics: truthiness, boolean short-circuiting, comparisons, interpolation, field access, range expression support needed by loops, and assignment evaluation order.

Purpose: This satisfies MACRO-EVAL-03, MACRO-EVAL-04, MACRO-EVAL-05, and MACRO-EVAL-08 without implementing the later standard-library builtin phase.
Output: Expanded evaluator expression support and `tests/unit/macro-evaluator.test.ts`.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-evaluator-core/132-CONTEXT.md
@.planning/phases/132-evaluator-core/132-RESEARCH.md
@.planning/phases/132-evaluator-core/132-VALIDATION.md
@.planning/phases/132-evaluator-core/132-PATTERNS.md
@.planning/phases/132-evaluator-core/132-01-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
@src/macro/evaluator.ts
@src/macro/types.ts

<interfaces>
From `src/macro/types.ts`:
```typescript
export interface FieldAccess { kind: 'FieldAccess'; target: VarRef | FieldAccess | ToolCall | ToolExistsCall; field: string; }
export interface BinaryExpr { kind: 'BinaryExpr'; op: '==' | '!=' | '<' | '>' | '<=' | '>=' | '&&' | '||'; left: Expr; right: Expr; }
export interface UnaryExpr { kind: 'UnaryExpr'; op: '!'; expr: Expr; }
export interface RangeExpr { kind: 'RangeExpr'; start: Expr; end: Expr; }
export interface StringLit { kind: 'StringLit'; raw: string; interpolated: boolean; }
```

Executor source-order rule: read the two primary Macro Language docs first for any requirements, spec, or test question before asking the user. Requirements doc wins over POC; requirements/test-plan conflict must be reported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement truthiness, comparison, range, and boolean operators</name>
  <files>src/macro/evaluator.ts, tests/unit/macro-evaluator.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md (REQ-021, REQ-026, comparison notes in REQ-012)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md (T-U-073 through T-U-075, T-U-095)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts (isTruthy, BinaryExpr handling)
    - src/macro/evaluator.ts
    - src/macro/types.ts
    - tests/unit/macro-evaluator.test.ts (create if absent)
  </read_first>
  <behavior>
    - T-U-073: `null`, `0`, `0.0`, `""`, `[]`, `{}` are falsy in `if`.
    - T-U-074: `"false"` and `"0"` are truthy because they are non-empty strings.
    - T-U-075: `-1` is truthy.
    - T-U-035: Equality returns true for same-typed equal values including numbers, strings, arrays, and `null`.
    - T-U-036: `"5" == 5` returns false with no implicit coercion.
    - T-U-037: Numeric ordering comparisons `<`, `<=`, `>`, and `>=` return deterministic boolean values.
    - T-U-038: String ordering such as `"a" < "b"` raises `comparison_type_mismatch`.
    - T-U-039: `&&` short-circuits and does not evaluate RHS when LHS is falsy.
    - T-U-040: `||` short-circuits and does not evaluate RHS when LHS is truthy.
    - T-U-041: `!truthy` returns `false` and `!falsy` returns `true`.
    - T-U-044: `0..5` evaluates to `[0,1,2,3,4]`.
    - T-U-045: `$start..$end` evaluates using integer variables.
    - T-U-046: Non-integer range operands raise `range_operand_type_mismatch`.
    - T-U-095: `x = 1; y = $x; x = 2; exit $y` returns `1`.
  </behavior>
  <action>
    Export `isTruthy(value: MacroValue): boolean` from `src/macro/evaluator.ts`. It must return false only for `null`, `false`, numeric `0`, empty string, empty list, and empty object; it must return true for non-zero numbers, non-empty strings, non-empty arrays, non-empty objects, and internal boolean `true`.

    Implement `BinaryExpr` equality by deep JSON-like structural comparison with no implicit coercion. Implement ordering operators `<`, `>`, `<=`, `>=` as numeric-only; non-number operands throw `MacroRuntimeError` with `details.reason = "comparison_type_mismatch"`. Implement `&&` and `||` as short-circuiting special cases that do not evaluate the RHS when skipped. Implement `UnaryExpr !` through `isTruthy`. Implement `RangeExpr` as start-inclusive/end-exclusive integer range and throw `MacroRuntimeError` with `details.reason = "range_operand_type_mismatch"` for non-integers.

    Add mandatory tests for T-U-035 through T-U-041, T-U-044 through T-U-046, T-U-073 through T-U-075, and T-U-095. These tests must prove comparison, numeric ordering, boolean short-circuiting, unary negation, range evaluation, truthiness, and RHS capture behavior from parsed macro source or direct ASTs. The required IDs must be present by literal test name in `tests/unit/macro-evaluator.test.ts`.
  </action>
  <acceptance_criteria>
    - `grep -n "export function isTruthy" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "comparison_type_mismatch" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "range_operand_type_mismatch" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "T-U-035\\|T-U-036\\|T-U-037\\|T-U-038\\|T-U-039\\|T-U-040\\|T-U-041\\|T-U-044\\|T-U-045\\|T-U-046" tests/unit/macro-evaluator.test.ts | wc -l | tr -d ' '` returns `10`.
    - `grep -n "T-U-073\\|T-U-074\\|T-U-075\\|T-U-095" tests/unit/macro-evaluator.test.ts | wc -l | tr -d ' '` returns `4`.
    - `grep -n "&&\\|||\\|short-circuit" src/macro/evaluator.ts tests/unit/macro-evaluator.test.ts` returns at least one line.
    - `grep -n "0\\.\\.5\\|start\\.\\.\\$end\\|range_operand_type_mismatch" tests/unit/macro-evaluator.test.ts` returns at least one line.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/macro-evaluator.test.ts</automated>
  </verify>
  <done>MACRO-EVAL-03 and the RHS capture part of MACRO-EVAL-08 pass with deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement interpolation and chained field access</name>
  <files>src/macro/evaluator.ts, tests/unit/macro-evaluator.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md (REQ-022, REQ-023, REQ-026)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md (T-U-076 through T-U-083, T-U-096)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts (interpolate, stepField, stringifyValue)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/builtins.ts (append/unique/concat immutability reference only)
    - src/macro/evaluator.ts
    - src/macro/types.ts
    - tests/unit/macro-evaluator.test.ts
  </read_first>
  <behavior>
    - T-U-076: `"hello $name"` interpolates a variable.
    - T-U-077: `"path: $obj.field.nested"` interpolates chained field access.
    - T-U-078: `"${var.field}"` braced form interpolates chained field access.
    - T-U-079: `"\$name"` produces literal `$name`.
    - T-U-080: Single-quoted parser output with `interpolated: false` remains literal.
    - T-U-081: `$obj.a.b.c` returns the leaf value.
    - T-U-082: Field access on `null` returns a runtime error.
    - T-U-083: Field access on number returns a runtime error.
    - T-U-096: Stubbed immutable list helpers do not mutate the source list.
  </behavior>
  <action>
    Implement `StringLit` evaluation so `interpolated: false` returns `raw` unchanged and `interpolated: true` runs double-quoted interpolation. Supported interpolation forms are `$name`, `$name.field.chain`, `${name}`, and `${name.field.chain}`. Preserve escaped dollars from parser raw strings so `\$name` becomes literal `$name`. Convert interpolated values to strings with a deterministic `stringifyMacroValue` helper: strings unchanged, numbers/booleans via `String(value)`, null as `null`, arrays/objects via `JSON.stringify`.

    Implement `FieldAccess` evaluation by evaluating the target then stepping through object fields. Missing field, null target, primitive target, and array target must throw `MacroRuntimeError` with stable `details.reason` values: `missing_field` or `invalid_field_target`. Include `details.field` and, where available, `details.line`.

    Add T-U-076 through T-U-083. For T-U-096, do not implement production `append`, `unique`, or `concat`; add test-local stub builtins that return new arrays and prove the evaluator captures immutable returned values without source-list mutation.
  </action>
  <acceptance_criteria>
    - `grep -n "function interpolate\\|const interpolate" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "stringifyMacroValue" src/macro/evaluator.ts` returns at least one line.
    - `grep -n "missing_field\\|invalid_field_target" src/macro/evaluator.ts | wc -l | tr -d ' '` returns at least `2`.
    - `grep -n "T-U-076\\|T-U-077\\|T-U-078\\|T-U-079\\|T-U-080\\|T-U-081\\|T-U-082\\|T-U-083\\|T-U-096" tests/unit/macro-evaluator.test.ts | wc -l | tr -d ' '` returns `9`.
    - `grep -n "append\\|unique\\|concat" tests/unit/macro-evaluator.test.ts` returns at least one line.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/macro-evaluator.test.ts</automated>
  </verify>
  <done>MACRO-EVAL-04, MACRO-EVAL-05, and the immutable helper proof for MACRO-EVAL-08 pass without pulling full builtins into this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macro expressions -> evaluator | Untrusted expression AST controls comparisons, object traversal, and interpolation output. |
| macro values -> field access | Runtime values may be null, primitive, arrays, or objects from user input or tool/builtin results. |
| macro text -> interpolation | Macro-authored strings may include variable names and escaped dollars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-05 | T/I | `FieldAccess` | mitigate | Reject invalid targets and missing fields with typed `MacroRuntimeError` details; never return `undefined`. |
| T-132-06 | D | `BinaryExpr` boolean operators | mitigate | Implement `&&` and `||` short-circuiting so skipped RHS builtins/tool calls do not execute. |
| T-132-07 | T | assignment evaluation order | mitigate | Preserve RHS-before-mutation path from Plan 01 and prove value capture with T-U-095 and T-U-096. |
| T-132-08 | I | interpolation | mitigate | Resolve only `$name`, `$name.field`, `${name}`, and `${name.field}` through Env; do not eval arbitrary expressions from strings. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/macro-evaluator.test.ts`.
Then run `npm test -- --reporter=verbose macro-evaluator macro-scope`.
</verification>

<success_criteria>
`tests/unit/macro-evaluator.test.ts` passes T-U-073 through T-U-083 and T-U-095 through T-U-096, and `src/macro/evaluator.ts` exports deterministic truthiness plus stable runtime errors for invalid field access.
</success_criteria>

<output>
After completion, create `.planning/phases/132-evaluator-core/132-02-SUMMARY.md`.
</output>
