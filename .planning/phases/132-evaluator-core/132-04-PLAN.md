---
phase: 132-evaluator-core
plan: 04
type: execute
wave: 4
depends_on:
  - 132-01
  - 132-02
  - 132-03
files_modified:
  - src/macro/evaluator.ts
  - tests/unit/macro-isolation.test.ts
autonomous: true
requirements:
  - MACRO-EVAL-07
must_haves:
  truths:
    - "Sequential macro invocations cannot see each other's variables."
    - "The same macro source can run twice without residual state."
    - "Trace, budget, task, progress, and cancellation containers are invocation-owned."
    - "Cancellation safe-point hooks exist between statements, before call boundaries, and between loop iterations."
  artifacts:
    - path: "src/macro/evaluator.ts"
      provides: "Invocation-owned context isolation and cancellation hook surface"
      exports: ["createInvocationContext", "evaluateProgram"]
    - path: "tests/unit/macro-isolation.test.ts"
      provides: "T-U-092 through T-U-094 isolation coverage plus unit concurrency smoke"
      contains: "T-U-092"
  key_links:
    - from: "src/macro/evaluator.ts"
      to: "src/mcp/utils/response-formats.ts"
      via: "per-run task_id in MacroExecutionResult"
      pattern: "task_id"
---

<objective>
Finish Phase 132 by hardening per-invocation isolation and cancellation-safe-point hooks around the evaluator built by Plans 01 through 03.

Purpose: This satisfies MACRO-EVAL-07 while keeping final task registry behavior, progress streaming, public budgets, and integration-level session concurrency in their later phases.
Output: Isolation tests in `tests/unit/macro-isolation.test.ts` and evaluator context hardening.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-evaluator-core/132-CONTEXT.md
@.planning/phases/132-evaluator-core/132-RESEARCH.md
@.planning/phases/132-evaluator-core/132-VALIDATION.md
@.planning/phases/132-evaluator-core/132-PATTERNS.md
@.planning/phases/132-evaluator-core/132-03-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/taskregistry.ts
@src/macro/evaluator.ts

<interfaces>
Phase decision on optional integration coverage:
`tests/integration/macro-concurrency.test.ts` and T-I-002 are not in scope for Phase 132. The final public/session invocation boundary and task/session scoping land in later macro phases. Phase 132 must still prove isolation with unit tests T-U-092 through T-U-094 and a unit-level concurrent smoke test using separate `evaluateProgram` calls and injected async builtins.

Executor source-order rule: read the two primary Macro Language docs first for any requirements, spec, or test question before asking the user. Requirements doc wins over POC; requirements/test-plan conflict must be reported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove sequential invocation state isolation</name>
  <files>src/macro/evaluator.ts, tests/unit/macro-isolation.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md (REQ-025)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md (T-U-092 through T-U-094, T-I-002 row)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts (EvaluateOptions and context shape)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/taskregistry.ts (anti-pattern: singleton task state)
    - src/macro/evaluator.ts
    - tests/unit/macro-isolation.test.ts (create if absent)
  </read_first>
  <behavior>
    - T-U-092: MacroA assigns `counter = 5`; MacroB run afterward references `$counter` and gets a runtime error.
    - T-U-093: Same macro source run twice starts from fresh variables on the second run.
    - T-U-094: Trace buffer is per-invocation; second invocation trace does not include first invocation terminal steps.
  </behavior>
  <action>
    Add `tests/unit/macro-isolation.test.ts` with parser helper and response JSON helper. Tests must create separate `evaluateProgram(program, options)` calls and assert that scope, inputVars ownership, trace array, budget object, progress array, task ID, cancellation flag/state, and any cancellation hook container are not reused across invocations. Harden `createInvocationContext` so it always creates owned copies/containers: `inputVars` must be shallow-cloned or deep-cloned enough that mutating the caller's `input_vars` object after context creation does not change the invocation's visible inputs; `trace: []`, `budget: { token_total: 0, model_calls: 0, external_tool_calls: 0 }`, `progress: []`, and cancellation state must be fresh per run; `taskId` must be unique per run when caller does not supply one.

    Add a source-level guard test or grep acceptance that `src/macro/evaluator.ts` does not import `taskregistry`, does not declare module-level `const env`, and does not declare module-level `const trace`. The production evaluator must not copy the POC singleton state pattern.
  </action>
  <acceptance_criteria>
    - `grep -n "T-U-092\\|T-U-093\\|T-U-094" tests/unit/macro-isolation.test.ts | wc -l | tr -d ' '` returns `3`.
    - `grep -n "inputVars\\|input_vars" tests/unit/macro-isolation.test.ts src/macro/evaluator.ts` returns at least two lines.
    - `grep -n "budget" tests/unit/macro-isolation.test.ts src/macro/evaluator.ts` returns at least two lines.
    - `grep -n "progress" tests/unit/macro-isolation.test.ts src/macro/evaluator.ts` returns at least two lines.
    - `grep -n "cancelled\\|cancellation" tests/unit/macro-isolation.test.ts src/macro/evaluator.ts` returns at least two lines.
    - `grep -n "randomUUID\\|crypto.randomUUID" src/macro/evaluator.ts` returns at least one line, or tests explicitly supply and assert unique task IDs through `createInvocationContext`.
    - `grep -n "taskregistry" src/macro/evaluator.ts` returns no lines.
    - `grep -n "const env =" src/macro/evaluator.ts | grep -v "evaluateProgram"` returns no lines.
    - `grep -n "const trace =" src/macro/evaluator.ts | grep -v "createInvocationContext"` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/macro-isolation.test.ts</automated>
  </verify>
  <done>MACRO-EVAL-07 sequential isolation is proven for variables, inputVars, trace, budget, task ID/state, progress, cancellation state, and other invocation-owned containers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cancellation safe-point hooks and unit concurrency smoke</name>
  <files>src/macro/evaluator.ts, tests/unit/macro-isolation.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Requirements.md (REQ-025, INV-03, INV-04)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/FlashQuery Macro Language Test Plan.md (T-U-092 through T-U-094, optional T-I-002)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/evaluator.ts (checkCancelled safe-point locations)
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Macro Language/macro-prototype/src/taskregistry.ts (anti-pattern/reference)
    - src/macro/evaluator.ts
    - tests/unit/macro-isolation.test.ts
  </read_first>
  <behavior>
    - Cancellation hook is called between top-level statements.
    - Cancellation hook is called before builtin/tool call boundaries.
    - Cancellation hook is called between for-loop and while-loop iterations.
    - Cancellation hook is called between pipeline stages for multi-stage pipelines.
    - Two concurrent unit-level invocations using async stub builtins do not observe each other's variables, inputVars, trace buffers, budget counters, progress arrays, task IDs, or cancellation state.
  </behavior>
  <action>
    Ensure every semantic safe point calls `await context.checkCancelled(where)` with specific `where` strings: `"between statements"`, `"before call <name>"`, `"before tool call <server>.<tool>"`, `"for-loop iteration"`, `"while-loop iteration"`, and `"between pipeline stages"`. Phase 132 owns only the hook placement and fresh cancellation state. Do not finalize the public cancellation response envelope in this phase; the later task/cancellation phase owns REQ-050 envelope mapping. If a hook throws in Phase 132, map it through the same internal evaluator-control path the later phase can replace, and do not require `jsonRuntimeError({ error: "cancelled" })`.

    Add tests in `tests/unit/macro-isolation.test.ts` that inject a `checkCancelled` spy and assert calls occur at statement, call, loop, and multi-stage pipeline boundaries. The pipeline safe-point test MUST use a parsed multi-stage pipeline and assert `where === "between pipeline stages"` appears at least once. Add a unit concurrency smoke using `Promise.all` over two `evaluateProgram` calls with async stub builtins that yield once; assert each result sees only its own variables and inputVars and each trace, budget, progress, task, and cancellation container is distinct. Do not create `tests/integration/macro-concurrency.test.ts`; record in the test or comments that T-I-002 is assigned to the later public/session-boundary phase and unit coverage is the Phase 132 proof layer.
  </action>
  <acceptance_criteria>
    - `grep -n "between statements\\|before call\\|before tool call\\|for-loop iteration\\|while-loop iteration\\|between pipeline stages" src/macro/evaluator.ts | wc -l | tr -d ' '` returns at least `6`.
    - `grep -n "between pipeline stages" tests/unit/macro-isolation.test.ts src/macro/evaluator.ts | wc -l | tr -d ' '` returns at least `2`.
    - `grep -n "Promise.all" tests/unit/macro-isolation.test.ts` returns at least one line.
    - `grep -n "inputVars\\|input_vars" tests/unit/macro-isolation.test.ts` returns at least one line.
    - `grep -n "budget" tests/unit/macro-isolation.test.ts` returns at least one line.
    - `grep -n "progress" tests/unit/macro-isolation.test.ts` returns at least one line.
    - `grep -n "cancelled\\|cancellation" tests/unit/macro-isolation.test.ts` returns at least one line.
    - `grep -n "T-I-002" .planning/phases/132-evaluator-core/132-04-PLAN.md tests/unit/macro-isolation.test.ts` returns at least one line.
    - `test ! -f tests/integration/macro-concurrency.test.ts` exits 0.
    - `grep -n "jsonRuntimeError({ error: \"cancelled\"\\|jsonRuntimeError({ error: 'cancelled'" src/macro/evaluator.ts` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/macro-isolation.test.ts</automated>
  </verify>
  <done>Cancellation hook locations and fresh cancellation state exist, public cancellation envelope mapping is deferred to the later task/cancellation phase, and Phase 132 isolation is proven with unit-level sequential and concurrent tests; T-I-002 is explicitly out of this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invocation -> invocation | Variables, trace, budget, task, progress, and cancellation state must not leak across runs. |
| POC singleton -> production implementation | POC task registry is a reference and anti-pattern; production must use invocation-owned context. |
| cancellation hook -> evaluator | External cancellation is not wired yet; the evaluator must place injected safe-point hooks and keep cancellation state fresh, while public cancellation envelope mapping remains deferred. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-13 | I/T | `createInvocationContext` | mitigate | Allocate fresh scope, inputVars, trace, budget, task, progress, and cancellation containers per run; tests T-U-092 through T-U-094. |
| T-132-14 | E/I | POC singleton state copied into production | mitigate | Acceptance greps reject `taskregistry` import and module-level env/trace state. |
| T-132-15 | D | unbounded loops before public budgets | mitigate | Cancellation hooks at statement, call, and loop safe points; public cancellation envelope and budget enforcement remain later phases. |
| T-132-16 | I | concurrent unit invocations | mitigate | Unit `Promise.all` smoke proves no shared variables, inputVars, traces, budgets, progress, task IDs, or cancellation state before final T-I-002 integration phase. |
</threat_model>

<verification>
Run `npm test -- --run tests/unit/macro-isolation.test.ts`.
Then run `npm test -- --reporter=verbose macro-evaluator macro-scope macro-termination macro-isolation`.
Finally run `npm run build`.
</verification>

<success_criteria>
`tests/unit/macro-isolation.test.ts` passes T-U-092 through T-U-094 plus inputVars, budget, progress, cancellation-state, cancellation-hook, and unit concurrency smoke coverage. `tests/integration/macro-concurrency.test.ts` is intentionally absent in Phase 132, with T-I-002 and public cancellation envelope mapping left for later session/task-boundary phases.
</success_criteria>

<output>
After completion, create `.planning/phases/132-evaluator-core/132-04-SUMMARY.md`.
</output>
