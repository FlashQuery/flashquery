---
phase: 113-document-reference-system-core
plan: 03
type: execute
wave: 3
depends_on: [113-02]
files_modified:
  - src/mcp/tools/llm.ts
  - tests/unit/llm-tool.test.ts
autonomous: true
requirements: [REF-01, REF-02, REF-03, REF-05, REF-06, REF-07, REF-08, VAL-113]
must_haves:
  truths:
    - "`call_model` scans only host-authored input message content"
    - "Reference failures abort before LLM dispatch with stable failed_references entries"
    - "`return_messages` uses hydrated inputs while assistant/tool content remains ordinary data"
  artifacts:
    - path: "src/mcp/tools/llm.ts"
      provides: "MCP call_model integration for host-only reference hydration"
    - path: "tests/unit/llm-tool.test.ts"
      provides: "Handler boundary regression tests"
  key_links:
    - from: "src/mcp/tools/llm.ts"
      to: "src/llm/reference-resolver.ts"
      via: "parseReferences, resolveReferences, hydrateMessages"
      pattern: "parseReferences"
---

<objective>
Wire the Phase 113 resolver contract into `call_model` with the host-authored-only safety boundary.

Purpose: Preserve the Phase 112 envelope while making reference hydration safe, typed, and non-recursive at the public MCP boundary.
Output: `call_model` handler changes and passing MCP unit tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/113-document-reference-system-core/113-CONTEXT.md
@.planning/phases/113-document-reference-system-core/113-RESEARCH.md
@.planning/phases/113-document-reference-system-core/113-PATTERNS.md
@.planning/phases/113-document-reference-system-core/113-VALIDATION.md
@.planning/phases/112-chat-primitive-envelope-migration/112-03-SUMMARY.md
@.planning/phases/113-document-reference-system-core/113-02-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md

<interfaces>
Downstream implementation agents MUST read the three canonical product docs above before questions, architecture decisions, or test coverage decisions per D-01.

From `src/mcp/tools/llm.ts`:
```typescript
export const callModelMessageSchema = z.discriminatedUnion('role', [
  z.object({ role: z.literal('system'), content: z.string().optional(), name: z.string().optional() }),
  z.object({ role: z.literal('user'), content: z.string().optional(), name: z.string().optional() }),
  z.object({ role: z.literal('assistant'), content: z.string().nullable().optional(), name: z.string().optional(), tool_calls: z.array(toolCallSchema).optional() }),
  z.object({ role: z.literal('tool'), content: z.string().optional(), tool_call_id: z.string(), name: z.never().optional(), tool_calls: z.never().optional() }),
]);
export function registerLlmTools(server: McpServer, config: FlashQueryConfig): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter scan targets to host-authored string content</name>
  <files>src/mcp/tools/llm.ts, tests/unit/llm-tool.test.ts</files>
  <read_first>
    - `src/mcp/tools/llm.ts`
    - `tests/unit/llm-tool.test.ts`
    - `.planning/phases/112-chat-primitive-envelope-migration/112-03-SUMMARY.md`
    - `Document Reference System.md` §4.5 and §8.1
  </read_first>
  <action>
    Change Step 1.5 in `src/mcp/tools/llm.ts` to build a scan target list only from original messages where `role` is `system` or `user` and `typeof content === "string"`. Preserve original message indexes so hydrated results write back into `messagesForRefs` without compacting indexes. Do not pass assistant messages, tool messages, assistant `tool_calls`, or tool result content to `parseReferences`. Discovery resolvers must still return before any reference parsing. This implements D-06, REF-08, and T-113-01.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/mcp/tools/llm.ts | grep -c "role === 'system'"` returns at least `1`.
    - `grep -v '^#' src/mcp/tools/llm.ts | grep -c "role === 'user'"` returns at least `1`.
    - `grep -v '^#' src/mcp/tools/llm.ts | grep -c "tool_calls"` remains nonzero only for schema/message handling, not scan-target iteration.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts</automated>
  </verify>
  <done>`parseReferences` receives only host-authored system/user string content with original indexes preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return typed reference failures through MCP without dispatch</name>
  <files>src/mcp/tools/llm.ts, tests/unit/llm-tool.test.ts</files>
  <read_first>
    - `src/mcp/tools/llm.ts`
    - `src/llm/reference-resolver.ts`
    - `src/constants/reference-failures.ts`
    - `tests/unit/llm-tool.test.ts`
    - `Document Reference System.md` §8.2
  </read_first>
  <action>
    Update parse and resolution failure responses to return the existing outer `{ error: "reference_resolution_failed" }` shape with `failed_references` entries containing `ref`, `reason`, and `detail`. Preserve `kind: "failed"` internally when present but do not require callers to parse it unless the existing envelope includes it consistently. Ensure `llmClient.complete` and `llmClient.completeByPurpose` are not called when `parseReferences` returns `invalid_reference_syntax` or any `resolveReferences` entry has `kind: "failed"`. This implements D-05, REF-06, VAL-113, and T-113-04.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/mcp/tools/llm.ts | grep -c "failed_references"` returns at least `2`.
    - `grep -v '^#' src/mcp/tools/llm.ts | grep -c "detail"` returns at least `1`.
    - `grep -v '^#' tests/unit/llm-tool.test.ts | grep -c "not.toHaveBeenCalled"` returns at least `2` for failed reference paths.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts tests/unit/reference-resolver.test.ts</automated>
  </verify>
  <done>Failed references are public, typed, actionable, and fail-fast before any provider call or usage write can occur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve hydrated envelope metadata and returned messages</name>
  <files>src/mcp/tools/llm.ts, tests/unit/llm-tool.test.ts</files>
  <read_first>
    - `src/mcp/tools/llm.ts`
    - `src/llm/types.ts`
    - `src/llm/reference-resolver.ts`
    - `.planning/phases/112-chat-primitive-envelope-migration/112-VERIFICATION.md`
    - `Document Reference System.md` §9
  </read_first>
  <action>
    Ensure successful reference hydration sets `metadata.injected_references` and `metadata.prompt_chars` only when active references resolved. Keep omitted fields absent when no active references exist. When `return_messages: true`, return the post-hydration `system`/`user` message content plus untouched assistant/tool messages and the final assistant output, matching Phase 112 envelope behavior. Preserve `messages: []` by default. This implements REF-07, REF-08, and VAL-113.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/mcp/tools/llm.ts | grep -c "injected_references"` returns at least `1`.
    - `grep -v '^#' src/mcp/tools/llm.ts | grep -c "prompt_chars"` returns at least `1`.
    - `grep -v '^#' tests/unit/llm-tool.test.ts | grep -c "return_messages"` returns at least `3`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts tests/unit/reference-resolver.test.ts</automated>
  </verify>
  <done>Public `call_model` envelope remains Phase-112-compatible while reflecting Phase 113 metadata and safety behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller -> `call_model` | External tool input enters Zod validation and reference hydration. |
| host messages -> vault reads | Only system/user content may authorize document resolution. |
| reference failure -> provider dispatch | Any failed reference must stop before LLM call and usage recording. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | E/I | `src/mcp/tools/llm.ts` | mitigate | Filter scan targets to original `system`/`user` string content; skip assistant/tool/tool-call data. |
| T-113-02 | I | `src/mcp/tools/llm.ts` | mitigate | Depend on Plan 02 span-table escape parity before parsing; preserve escaped spans in returned messages. |
| T-113-03 | T/R | `src/mcp/tools/llm.ts` | mitigate | Surface `ambiguous_document_identifier` from resolver without provider dispatch. |
| T-113-04 | R | `src/mcp/tools/llm.ts` | mitigate | Return stable `reason` plus human `detail`; no free-form reason strings. |
</threat_model>

<verification>
Run `npm test -- tests/unit/llm-tool.test.ts tests/unit/reference-resolver.test.ts`.
</verification>

<success_criteria>
`call_model` uses the Phase 113 resolver safely, preserves the Phase 112 envelope, and passes focused MCP unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/113-document-reference-system-core/113-03-SUMMARY.md`.
</output>
