---
phase: 113-document-reference-system-core
plan: 04
type: execute
wave: 4
depends_on: [113-03]
files_modified:
  - tests/integration/reference-resolver.integration.test.ts
  - tests/scenarios/directed/testcases/test_call_model_reference_system_core.py
  - tests/scenarios/directed/DIRECTED_COVERAGE.md
  - tests/scenarios/integration/INTEGRATION_COVERAGE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/113-document-reference-system-core/113-VALIDATION.md
autonomous: true
requirements: [REF-01, REF-02, REF-03, REF-04, REF-05, REF-06, REF-07, REF-08, VAL-113]
must_haves:
  truths:
    - "Real vault files prove path, filename, fq_id, ambiguity, and metadata behavior"
    - "Public directed scenario proves grammar, escape, id-literal removal, typed failures, and host-only safety through call_model"
    - "Phase 113 traceability is closed only after focused validation commands pass or record explicit environmental skips"
  artifacts:
    - path: "tests/integration/reference-resolver.integration.test.ts"
      provides: "TypeScript integration coverage for ATL-I-04"
    - path: "tests/scenarios/directed/testcases/test_call_model_reference_system_core.py"
      provides: "Directed public call_model scenario for ATL-DS-02 and ATL-DS-03"
    - path: "tests/scenarios/directed/DIRECTED_COVERAGE.md"
      provides: "Public scenario coverage ledger updates"
    - path: "tests/scenarios/integration/INTEGRATION_COVERAGE.md"
      provides: "Integration coverage ledger updates and legacy {{id:...}} row correction"
  key_links:
    - from: "tests/scenarios/directed/testcases/test_call_model_reference_system_core.py"
      to: "src/mcp/tools/llm.ts"
      via: "managed MCP server call_model calls"
      pattern: "call_model"
    - from: "tests/integration/reference-resolver.integration.test.ts"
      to: "src/llm/reference-resolver.ts"
      via: "real vault resolver calls"
      pattern: "resolveReferences"
---

<objective>
Prove Phase 113 through real vault integration, public directed scenarios, coverage ledgers, and final traceability.

Purpose: VAL-113 requires runnable unit, directed, and integration tests; this plan closes the public behavior and phase bookkeeping.
Output: Integration test, directed scenario, coverage matrix updates, validation status, and roadmap/requirements traceability.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/113-document-reference-system-core/113-CONTEXT.md
@.planning/phases/113-document-reference-system-core/113-RESEARCH.md
@.planning/phases/113-document-reference-system-core/113-PATTERNS.md
@.planning/phases/113-document-reference-system-core/113-VALIDATION.md
@.planning/phases/112-chat-primitive-envelope-migration/112-04-SUMMARY.md
@.planning/phases/112-chat-primitive-envelope-migration/112-05-SUMMARY.md
@.planning/phases/113-document-reference-system-core/113-03-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md

<interfaces>
Downstream implementation agents MUST read the three canonical product docs above before questions, architecture decisions, or test coverage decisions per D-01.

From `tests/scenarios/directed/testcases/test_call_model_ref_error_taxonomy.py`:
```python
from fqc_test_utils import TestRun, FQCServer
from fqc_client import FQCClient, _find_project_dir, _load_env_file
r = client.call_tool("call_model", resolver="model", name="fast", messages=[...])
resp = json.loads(r.text) if r.text else {}
run.step(label="...", passed=all(checks.values()), detail=f"checks={checks}", timing_ms=r.timing_ms, tool_result=r)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real-vault TypeScript integration coverage</name>
  <files>tests/integration/reference-resolver.integration.test.ts</files>
  <read_first>
    - `tests/integration/documents.integration.test.ts`
    - `tests/helpers/test-env.ts`
    - `src/llm/reference-resolver.ts`
    - `.planning/phases/113-document-reference-system-core/113-PATTERNS.md` `tests/integration/reference-resolver.integration.test.ts`
    - `ATL Test Plan.md` ATL-I-04
  </read_first>
  <action>
    Create `tests/integration/reference-resolver.integration.test.ts` using existing integration skip/setup patterns. Use a temporary vault and integration instance id. Seed real markdown files and matching `fqc_documents` rows as needed to prove: path reference hydrates body and metadata; fq_id reference resolves through database identity; bare filename ambiguity produces `ambiguous_document_identifier` with at least two matching paths and path/fq_id guidance; `{{ref:path#Section}}` extracts a section; `{{ref:source.md->frontmatter.path}}` returns target body and `resolved_to`; injected content containing `{{ref:nested.md}}` is not recursively resolved. Prefer `embedding.provider: "none"` or the repo's integration no-embedding pattern; do not require semantic search. This implements REF-01, REF-04, REF-07, REF-08, VAL-113, and ATL-I-04.
  </action>
  <acceptance_criteria>
    - `test -f tests/integration/reference-resolver.integration.test.ts` succeeds.
    - `grep -v '^#' tests/integration/reference-resolver.integration.test.ts | grep -c "ambiguous_document_identifier"` returns at least `1`.
    - `grep -v '^#' tests/integration/reference-resolver.integration.test.ts | grep -c "resolved_to"` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- tests/integration/reference-resolver.integration.test.ts</automated>
  </verify>
  <done>Integration coverage proves the standard document identifier ladder and resolver metadata against real vault/database state, with graceful skip only for missing `.env.test` infrastructure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add directed public call_model reference scenario</name>
  <files>tests/scenarios/directed/testcases/test_call_model_reference_system_core.py</files>
  <read_first>
    - `tests/scenarios/directed/WRITING_SCENARIOS.md`
    - `tests/scenarios/directed/testcases/test_call_model_return_messages.py`
    - `tests/scenarios/directed/testcases/test_call_model_ref_error_taxonomy.py`
    - `.planning/phases/113-document-reference-system-core/113-PATTERNS.md` directed scenario section
    - `ATL Test Plan.md` ATL-DS-02 and ATL-DS-03
  </read_first>
  <action>
    Create `tests/scenarios/directed/testcases/test_call_model_reference_system_core.py` using the managed directed scenario pattern and deterministic local OpenAI-compatible mock provider from Phase 112. Cover public `call_model` behavior for: `{{ref:path}}`, `{{ref:filename}}`, `{{ref:fq_id}}`, `{{ref:path#Section}}`, `{{ref:path->pointer}}`, positional `@` parse errors without implementing Phase 114 template substitution, escaped `\{{ref:path}}` literal and excluded from metadata, even-parity `\\{{ref:path}}` active with one literal backslash preserved, active and escaped `{{id:...}}` literal/no metadata, malformed opener literal/no failure, ambiguity returning `ambiguous_document_identifier` with detail guidance, typed pointer/section failures with `reason` plus `detail`, no LLM call on failed references, and assistant/tool messages containing `{{ref:...}}` not hydrated. This implements REF-01 through REF-08, VAL-113, ATL-DS-02, ATL-DS-03, and T-113-01 through T-113-04.
  </action>
  <acceptance_criteria>
    - `test -f tests/scenarios/directed/testcases/test_call_model_reference_system_core.py` succeeds.
    - `grep -v '^#' tests/scenarios/directed/testcases/test_call_model_reference_system_core.py | grep -c "ambiguous_document_identifier"` returns at least `1`.
    - `grep -v '^#' tests/scenarios/directed/testcases/test_call_model_reference_system_core.py | grep -c "{{id:"` returns at least `1`.
    - `grep -v '^#' tests/scenarios/directed/testcases/test_call_model_reference_system_core.py | grep -c "ATL-DS-02\\|ATL-DS-03"` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>python3 tests/scenarios/directed/run_suite.py --managed test_call_model_reference_system_core</automated>
  </verify>
  <done>Directed scenario proves the user-visible Phase 113 reference system through MCP, not only helper functions.</done>
</task>

<task type="auto">
  <name>Task 3: Update coverage ledgers, validation status, and requirements traceability</name>
  <files>tests/scenarios/directed/DIRECTED_COVERAGE.md, tests/scenarios/integration/INTEGRATION_COVERAGE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/113-document-reference-system-core/113-VALIDATION.md</files>
  <read_first>
    - `tests/scenarios/directed/DIRECTED_COVERAGE.md`
    - `tests/scenarios/integration/INTEGRATION_COVERAGE.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/ROADMAP.md`
    - `.planning/phases/113-document-reference-system-core/113-VALIDATION.md`
    - `ATL Test Plan.md` §4.3 through §4.5
  </read_first>
  <action>
    Update directed coverage rows for Phase 113 public behavior, adding or revising rows under the `call_model` reference section so ATL-DS-02 and ATL-DS-03 map to `test_call_model_reference_system_core`. Correct legacy ATL rows that still describe active `{{id:...}}` resolution by marking them superseded/replaced by literal-id behavior for ATL v1 instead of leaving contradictory coverage. Update integration coverage with the TypeScript integration test or YAML-row note for ATL-I-04 and remove or annotate old mixed `{{ref}}` + `{{id}}` assumptions. After the full verification command passes or records explicit environment skips, mark REF-01 through REF-08 and VAL-113 complete in `.planning/REQUIREMENTS.md`, update Phase 113 in `.planning/ROADMAP.md` with `**Plans:** 4 plans` and the plan list, and set `nyquist_compliant: true` plus green task rows in `113-VALIDATION.md`. Do not mark requirements complete before the automated commands in this plan run successfully or skip for documented environment reasons.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/scenarios/directed/DIRECTED_COVERAGE.md | grep -c "test_call_model_reference_system_core"` returns at least `1`.
    - `grep -v '^#' tests/scenarios/integration/INTEGRATION_COVERAGE.md | grep -c "reference-resolver.integration\\|reference resolver"` returns at least `1`.
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -c "\\[x\\] \\*\\*REF-0"` returns `8`.
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -c "\\[x\\] \\*\\*VAL-113"` returns `1`.
    - `grep -v '^#' .planning/ROADMAP.md | grep -c "113-04-PLAN.md"` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-tool.test.ts tests/unit/resolve-document.test.ts && npm run test:integration -- tests/integration/reference-resolver.integration.test.ts && python3 tests/scenarios/directed/run_suite.py --managed test_call_model_reference_system_core</automated>
  </verify>
  <done>Coverage matrices and planning traceability reflect Phase 113 only after focused validation passes or records explicit environmental skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public MCP scenario -> FlashQuery server | External behavior proves the same safety as unit tests through managed server calls. |
| coverage matrices -> future planning | Ledger drift can cause later phases to rely on obsolete `{{id:...}}` behavior. |
| real vault files -> resolver | Integration fixtures exercise actual path/fq_id/filename resolution and ambiguity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | E/I | directed scenario | mitigate | Public scenario includes assistant/tool content containing `{{ref:...}}` and asserts it remains data. |
| T-113-02 | I | directed + integration tests | mitigate | Directed tests assert escaped refs and escaped invalid refs do not resolve or enter metadata. |
| T-113-03 | T/R | integration + directed tests | mitigate | Real-vault ambiguity returns `ambiguous_document_identifier` with path/fq_id guidance. |
| T-113-04 | R | coverage and validation ledgers | mitigate | Scenario and unit checks assert stable `ReferenceFailureReason` values, not free-form wording. |
</threat_model>

<verification>
Run `npm run build && npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-tool.test.ts tests/unit/resolve-document.test.ts && npm run test:integration -- tests/integration/reference-resolver.integration.test.ts && python3 tests/scenarios/directed/run_suite.py --managed test_call_model_reference_system_core`.
</verification>

<success_criteria>
VAL-113 is satisfied with runnable unit, integration, and directed coverage. REF-01 through REF-08 are marked complete only after validation. Coverage ledgers no longer contradict ATL's removal of active `{{id:...}}`.
</success_criteria>

<output>
After completion, create `.planning/phases/113-document-reference-system-core/113-04-SUMMARY.md`.
</output>
