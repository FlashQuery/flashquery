---
phase: 113-document-reference-system-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/reference-failures.ts
  - tests/unit/reference-resolver.test.ts
  - tests/unit/llm-tool.test.ts
  - tests/unit/resolve-document.test.ts
autonomous: true
requirements: [REF-01, REF-02, REF-03, REF-04, REF-05, REF-06, REF-07, REF-08, VAL-113]
must_haves:
  truths:
    - "Reference failure reasons are stable API constants, not free-form strings"
    - "Parser, escape, ambiguity, metadata, and host-only hydration expectations exist before implementation"
    - "Legacy {{id:...}} behavior is pinned as literal text"
  artifacts:
    - path: "src/constants/reference-failures.ts"
      provides: "ReferenceFailureReason runtime array, union, and guard"
      exports: ["REFERENCE_FAILURE_REASONS", "ReferenceFailureReason", "isReferenceFailureReason"]
    - path: "tests/unit/reference-resolver.test.ts"
      provides: "Scanner/parser/resolver/failure taxonomy unit expectations"
    - path: "tests/unit/llm-tool.test.ts"
      provides: "MCP boundary tests for host-only hydration and typed failed_references"
    - path: "tests/unit/resolve-document.test.ts"
      provides: "Identifier ambiguity regression guard"
  key_links:
    - from: "tests/unit/reference-resolver.test.ts"
      to: "src/constants/reference-failures.ts"
      via: "imports REFERENCE_FAILURE_REASONS and checks each reason has coverage"
      pattern: "REFERENCE_FAILURE_REASONS"
    - from: "tests/unit/llm-tool.test.ts"
      to: "src/mcp/tools/llm.ts"
      via: "captureCallModelHandler"
      pattern: "captureCallModelHandler"
---

<objective>
Create the Phase 113 test and constant contracts that downstream implementation plans must satisfy.

Purpose: Lock the canonical DRS/ATL behavior before source changes so parser grammar, escape parity, failure reasons, metadata, ambiguity, and host-authored-only hydration cannot drift.
Output: `ReferenceFailureReason` constants and focused failing or updated unit tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/113-document-reference-system-core/113-CONTEXT.md
@.planning/phases/113-document-reference-system-core/113-RESEARCH.md
@.planning/phases/113-document-reference-system-core/113-PATTERNS.md
@.planning/phases/113-document-reference-system-core/113-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md

<interfaces>
Downstream implementation agents MUST read the three canonical product docs above before questions, architecture decisions, or test coverage decisions per D-01.

From `src/constants/llm.ts`:
```typescript
export const FINISH_REASONS = ['stop', 'tool_calls', 'length', 'content_filter', 'unknown'] as const;
export type FinishReason = typeof FINISH_REASONS[number];
export function isFinishReason(value: string): value is FinishReason;
```

From `src/llm/reference-resolver.ts`:
```typescript
export function parseReferences(messages: Array<{ role: string; content: string }>): ParsedRef[] | ParseRefError;
export async function resolveReferences(parsed: ParsedRef[], config, sm, ep, log): Promise<Array<ResolvedRef | FailedRef>>;
export function hydrateMessages(messages: Array<{ role: string; content: string }>, resolved: ResolvedRef[]): Array<{ role: string; content: string }>;
export function buildInjectedReferences(resolved: ResolvedRef[]): Array<{ ref: string; chars: number; resolved_to?: string }>;
export function computePromptChars(messages: Array<{ content: string }>): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add centralized reference failure constants</name>
  <files>src/constants/reference-failures.ts, tests/unit/reference-resolver.test.ts</files>
  <read_first>
    - `src/constants/llm.ts`
    - `.planning/phases/113-document-reference-system-core/113-CONTEXT.md` decisions D-02 and D-05
    - `Document Reference System.md` §8.2
    - `Agentic-LLM-Tool-Loop.md` OQ-24
  </read_first>
  <action>
    Create `src/constants/reference-failures.ts` using the `src/constants/llm.ts` runtime-array pattern. Export `REFERENCE_FAILURE_REASONS`, `ReferenceFailureReason`, and `isReferenceFailureReason(value: string): value is ReferenceFailureReason`. Include these exact values: `invalid_reference_syntax`, `document_not_found`, `ambiguous_document_identifier`, `read_error`, `section_not_found`, `occurrence_out_of_range`, `reference_path_not_found`, `reference_path_not_string`, `pointer_target_not_found`, `template_missing_required_param`, `template_param_invalid_type`, `template_param_doc_not_found`, `alias_template_not_found`, `alias_missing_template_field`, `alias_key_not_found`, `multi_ref_invalid_value`, `multi_ref_item_failed`, `unknown_reference_error`. Add a unit meta-test proving every reason is unique, accepted by `isReferenceFailureReason`, and no arbitrary string is accepted. This implements D-05 and T-113-04.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/constants/reference-failures.ts | grep -c "REFERENCE_FAILURE_REASONS"` returns at least `1`.
    - `grep -v '^#' src/constants/reference-failures.ts | grep -c "ambiguous_document_identifier"` returns at least `1`.
    - `grep -v '^#' tests/unit/reference-resolver.test.ts | grep -c "isReferenceFailureReason"` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/reference-resolver.test.ts</automated>
  </verify>
  <done>`ReferenceFailureReason` exists as a runtime-enumerable API contract and tests can import it without TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin scanner, parser, escape, id-literal, ambiguity, and metadata behavior</name>
  <files>tests/unit/reference-resolver.test.ts, tests/unit/resolve-document.test.ts</files>
  <read_first>
    - `tests/unit/reference-resolver.test.ts`
    - `tests/unit/resolve-document.test.ts`
    - `.planning/phases/113-document-reference-system-core/113-PATTERNS.md` sections for these files
    - `Document Reference System.md` §4.1, §4.2, §4.3, §4.4, §4.5, §8.1, §8.2, §9
    - `ATL Test Plan.md` ATL-U-02, ATL-U-03, ATL-U-04, ATL-U-07
  </read_first>
  <action>
    Update `tests/unit/reference-resolver.test.ts` with table-style Vitest cases for D-02 through D-05: `{{ref:path.md}}`, `{{ref:uuid}}`, `{{ref:People/alice@example.com.md}}`, `{{ref:@alias}}`, `{{ref:@}}`, `{{ref:@alias#Section}}`, `{{ref:@alias->pointer}}`, `{{ref:doc.md#}}`, `{{ref:doc.md->}}`, `{{ref:doc.md # Section}}`, `{{ref:doc.md -> key}}`, malformed opener `{{ref:doc.md` literal/no metadata, active `{{id:uuid}}` literal/no metadata, escaped `\{{id:uuid}}` literal/no escape stripping, `\{{ref:doc.md}}`, `\\{{ref:doc.md}}`, `\\\{{ref:doc.md}}`, `\{{ref:}}`, `\\\\{{ref:doc.md}}`, `\\\\\{{ref:doc.md}}`, duplicate identical placeholders, and injected content containing literal `{{ref:nested.md}}` not recursively resolved. Add resolver mapping tests for `DocumentRequestError.envelope.error` values mapping to the exact `ReferenceFailureReason` values listed in the research. Keep or extend `tests/unit/resolve-document.test.ts` so multiple active filename matches still throw a message containing both matching paths and guidance to use a vault-relative path or `fq_id`. This implements D-02, D-03, D-04, D-05, REF-01 through REF-07, and T-113-02/T-113-03/T-113-04.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/unit/reference-resolver.test.ts | grep -c "{{id:"` returns at least `2`.
    - `grep -v '^#' tests/unit/reference-resolver.test.ts | grep -c "ambiguous_document_identifier"` returns at least `1`.
    - `grep -v '^#' tests/unit/reference-resolver.test.ts | grep -c "invalid_reference_syntax"` returns at least `5`.
    - `grep -v '^#' tests/unit/resolve-document.test.ts | grep -c "Use a vault-relative path or fq_id"` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/reference-resolver.test.ts tests/unit/resolve-document.test.ts</automated>
  </verify>
  <done>Unit tests express the complete Phase 113 grammar and failure contract, including failing tests where implementation has not caught up yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin MCP handler host-only scan and typed failure envelope</name>
  <files>tests/unit/llm-tool.test.ts</files>
  <read_first>
    - `tests/unit/llm-tool.test.ts`
    - `src/mcp/tools/llm.ts`
    - `.planning/phases/112-chat-primitive-envelope-migration/112-03-SUMMARY.md`
    - `.planning/phases/113-document-reference-system-core/113-PATTERNS.md` `tests/unit/llm-tool.test.ts`
    - `Document Reference System.md` §4.5 and §8.2
  </read_first>
  <action>
    Add or update handler tests using `captureCallModelHandler`. Required cases: discovery resolvers return before `parseReferences`; only `system` and `user` messages with string `content` are passed to `parseReferences`; `assistant` messages, `tool` messages, assistant `tool_calls[].function.arguments`, and tool result content containing `{{ref:secret.md}}` are not scanned; parse failures return `isError: true` with `{ error: "reference_resolution_failed", failed_references: [{ ref, reason: "invalid_reference_syntax", detail }] }`; resolver failures return `{ kind: "failed", ref, reason: "document_not_found", detail }` style entries; no LLM dispatch occurs on any failed reference; `return_messages: true` returns hydrated host inputs and preserves skipped assistant/tool messages as data. This implements D-06, REF-08, VAL-113, and T-113-01.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/unit/llm-tool.test.ts | grep -c "assistant"` returns at least `1` in a reference-boundary test.
    - `grep -v '^#' tests/unit/llm-tool.test.ts | grep -c "tool_call"` returns at least `1` in a reference-boundary test.
    - `grep -v '^#' tests/unit/llm-tool.test.ts | grep -c "failed_references"` returns at least `3`.
    - `grep -v '^#' tests/unit/llm-tool.test.ts | grep -c "detail"` returns at least `2`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts</automated>
  </verify>
  <done>MCP boundary tests prove host-authored-only hydration and typed failure response shape through the public `call_model` handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host input -> reference parser | Host-authored `system`/`user` content authorizes vault reads. |
| model/tool data -> handler | Assistant and tool-origin content is untrusted for hydration authorization. |
| parser/resolver -> API response | Failure strings become public API metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | E/I | `tests/unit/llm-tool.test.ts` | mitigate | Add tests requiring host-authored-only hydration and no scan of assistant/tool/tool-call data. |
| T-113-02 | I | `tests/unit/reference-resolver.test.ts` | mitigate | Add escape parity tests before parser implementation: odd escaped, even active, escaped invalid excluded. |
| T-113-03 | T/R | `tests/unit/resolve-document.test.ts` | mitigate | Preserve ambiguity guard and require path/fq_id guidance. |
| T-113-04 | R | `src/constants/reference-failures.ts` | mitigate | Centralize `ReferenceFailureReason` constants and assert every value is stable at runtime. |
</threat_model>

<verification>
Run `npm test -- tests/unit/reference-resolver.test.ts tests/unit/llm-tool.test.ts tests/unit/resolve-document.test.ts`. RED failures are acceptable only when they are the planned implementation gaps for later plans; TypeScript import/compile failures in `src/constants/reference-failures.ts` are not acceptable.
</verification>

<success_criteria>
The phase has executable tests for REF-01 through REF-08 and VAL-113 before implementation. Tests reference D-01 canonical docs in comments or test names where useful, but do not implement deferred Phase 114 template substitution.
</success_criteria>

<output>
After completion, create `.planning/phases/113-document-reference-system-core/113-01-SUMMARY.md`.
</output>
