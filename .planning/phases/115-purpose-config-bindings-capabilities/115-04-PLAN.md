---
phase: 115-purpose-config-bindings-capabilities
plan: 04
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - src/llm/capabilities.ts
  - src/config/loader.ts
  - src/mcp/tools/llm.ts
  - tests/unit/llm-config.test.ts
  - tests/unit/llm-tool.test.ts
autonomous: true
requirements: [CAP-01, CAP-03, CAP-05]
must_haves:
  truths:
    - "OpenAI provider profile defaults all five structured capabilities to true."
    - "OpenRouter, custom OpenAI-compatible providers, and Ollama default structured capabilities to unknown unless declared."
    - "Any purpose exposing tools or template tools fails validation unless every fallback model has tool_calling and usage_on_tool_calls true."
    - "`response_format` plus model-visible tools fails before provider dispatch when the resolved model lacks structured_outputs_with_tools."
  artifacts:
    - path: "src/llm/capabilities.ts"
      provides: "Capability defaulting, diagnostics, and admission checks"
      contains: "usage_on_tool_calls"
    - path: "src/config/loader.ts"
      provides: "Startup admission hook"
      contains: "validateMode2"
    - path: "src/mcp/tools/llm.ts"
      provides: "Pre-dispatch response_format plus tools guard and structured discovery projection"
      contains: "structured_outputs_with_tools"
  key_links:
    - from: "src/config/loader.ts"
      to: "src/llm/capabilities.ts"
      via: "post-parse admission hook"
      pattern: "validate.*Capability"
    - from: "src/mcp/tools/llm.ts"
      to: "src/llm/capabilities.ts"
      via: "pre-dispatch guard"
      pattern: "response_format.*structured_outputs_with_tools"
---

<objective>
Implement shared model capability defaulting, diagnostics, and Mode 2 admission gates.

Purpose: Unsafe Mode 2 purposes must fail before startup completes or before a provider call can run, and runtime drift must produce actionable diagnostics.
Output: Capability service, startup admission wiring, `call_model` pre-dispatch guard, and focused unit tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-purpose-config-bindings-capabilities/115-CONTEXT.md
@.planning/phases/115-purpose-config-bindings-capabilities/115-RESEARCH.md
@.planning/phases/115-purpose-config-bindings-capabilities/115-PATTERNS.md
@.planning/phases/115-purpose-config-bindings-capabilities/115-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
@.planning/phases/115-purpose-config-bindings-capabilities/115-01-SUMMARY.md

<interfaces>
Capability service contracts to create:
```typescript
export type StructuredModelCapabilities = {
  tool_calling?: boolean;
  usage_on_tool_calls?: boolean;
  strict_tools?: boolean;
  parallel_tool_calls?: boolean;
  structured_outputs_with_tools?: boolean;
};
export function modelCapabilitiesWithDefaults(model, provider): StructuredModelCapabilities;
export function validatePurposeMode2Admission(config, purposeName): CapabilityAdmissionResult;
export function assertResponseFormatAllowedWithTools(config, purposeName, modelName, parameters): CapabilityAdmissionResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add capability admission and pre-dispatch tests</name>
  <files>tests/unit/llm-config.test.ts, tests/unit/llm-tool.test.ts</files>
  <read_first>
    - tests/unit/llm-config.test.ts
    - tests/unit/llm-tool.test.ts
    - src/config/loader.ts
    - src/mcp/tools/llm.ts
    - .planning/phases/115-purpose-config-bindings-capabilities/115-CONTEXT.md
    - .planning/phases/115-purpose-config-bindings-capabilities/115-PATTERNS.md
    - .planning/phases/115-purpose-config-bindings-capabilities/115-VALIDATION.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Implementation Gap.md
  </read_first>
  <behavior>
    - Test ATL-U-08: official provider `name: openai`, `type: openai-compatible` defaults all five structured capabilities to true.
    - Test ATL-U-08: `openrouter`, custom OpenAI-compatible, and `ollama` providers have unknown capability diagnostics unless model YAML declares booleans.
    - Test ATL-U-08: purpose with `tools: ["read"]` fails if any fallback model has `tool_calling` false or unknown.
    - Test ATL-U-08: purpose with `templates: ["Templates/research-skill.md"]` fails if any fallback model has `usage_on_tool_calls` false or unknown.
    - Test ATL-U-08: purpose with no explicit tools and restrictive `templates.default_access` plus no bindings remains Mode 1.
    - Test CAP-05: `call_model` with `resolver: "purpose"`, `defaults.response_format`, and model-visible exposure returns an `isError: true` response before provider dispatch when `structured_outputs_with_tools: false`.
  </behavior>
  <action>Add focused unit tests in `tests/unit/llm-config.test.ts` and `tests/unit/llm-tool.test.ts` for CAP-03/CAP-05 and the capability defaulting matrix. Use exact diagnostic substrings `unknown declaration`, `declared unsupported`, `tool_calling`, `usage_on_tool_calls`, and `structured_outputs_with_tools` so errors remain actionable. Do not assemble native tool definitions or dispatch model tool calls in this plan.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-config.test.ts tests/unit/llm-tool.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ATL-U-08|unknown declaration|declared unsupported|tool_calling|usage_on_tool_calls|structured_outputs_with_tools|response_format" tests/unit/llm-config.test.ts tests/unit/llm-tool.test.ts` returns matches.
    - `npm test -- tests/unit/llm-config.test.ts tests/unit/llm-tool.test.ts` runs; failures at this point are acceptable only if they are RED failures for missing capability admission.
  </acceptance_criteria>
  <done>Tests pin admission and pre-dispatch behavior before the shared service is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared capability defaulting and startup admission</name>
  <files>src/llm/capabilities.ts, src/config/loader.ts, tests/unit/llm-config.test.ts</files>
  <read_first>
    - src/llm/capabilities.ts
    - src/config/loader.ts
    - tests/unit/llm-config.test.ts
    - .planning/phases/115-purpose-config-bindings-capabilities/115-CONTEXT.md
    - .planning/phases/115-purpose-config-bindings-capabilities/115-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Implementation Gap.md
  </read_first>
  <action>Create `src/llm/capabilities.ts` as a pure service for structured capability defaults, diagnostics, and purpose admission. Treat `undefined` capability values as unknown, not false. Default all five booleans to true only when provider `name === "openai"` and `type === "openai-compatible"`. Leave OpenRouter, other OpenAI-compatible providers, and Ollama unknown unless declared on the model. In `loadConfig()`, after LLM name normalization and cross-reference validation, call the admission service for any purpose that can expose model-visible tools through `tools`, explicit `templates`, or permissive template default access; require every fallback model to have `tool_calling: true` and `usage_on_tool_calls: true`. Add diagnostics that distinguish unknown declarations from declared unsupported values.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "modelCapabilitiesWithDefaults|validatePurpose.*Admission|unknown declaration|declared unsupported|tool_calling|usage_on_tool_calls|structured_outputs_with_tools" src/llm/capabilities.ts src/config/loader.ts` returns matches.
    - `rg -n "provider.*openai|openrouter|ollama" src/llm/capabilities.ts tests/unit/llm-config.test.ts` returns matches.
    - `npm test -- tests/unit/llm-config.test.ts` passes.
  </acceptance_criteria>
  <done>Startup config validation blocks Mode 2-ineligible purposes before FlashQuery starts unsafe exposure paths.</done>
</task>

<task type="auto">
  <name>Task 3: Wire discovery projection and response_format guard to capability service</name>
  <files>src/mcp/tools/llm.ts, tests/unit/llm-tool.test.ts</files>
  <read_first>
    - src/mcp/tools/llm.ts
    - src/llm/capabilities.ts
    - tests/unit/llm-tool.test.ts
    - .planning/phases/115-purpose-config-bindings-capabilities/115-CONTEXT.md
    - .planning/phases/115-purpose-config-bindings-capabilities/115-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Agentic-LLM-Tool-Loop.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/Document Reference System.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Test Plan.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Roadmap/Features/Agentic Tool Loop and Doc References/ATL Implementation Gap.md
  </read_first>
  <action>Update `src/mcp/tools/llm.ts` so `list_models` projects structured `capabilities` and `tags` according to the final schema, no longer exposing the old string-array capability behavior surface. Add a pre-dispatch guard for purpose calls that combine `response_format` from caller parameters or purpose defaults with model-visible exposure; if the resolved model has `structured_outputs_with_tools: false` or unknown, return `{ isError: true }` before invoking the LLM client. Preserve discovery early returns and the existing unconfigured-LLM guard.</action>
  <verify>
    <automated>npm test -- tests/unit/llm-tool.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "structured_outputs_with_tools|response_format|tags|modelCapabilitiesWithDefaults|isError" src/mcp/tools/llm.ts tests/unit/llm-tool.test.ts` returns matches.
    - `rg -n "capabilities\\)\\.toEqual\\(\\['tools'|capabilities.*\\['tools', 'vision'\\]" tests/unit/llm-tool.test.ts` returns no matches.
    - `npm test -- tests/unit/llm-tool.test.ts && npm run build` passes.
  </acceptance_criteria>
  <done>`call_model` exposes the new discovery shape and blocks unsupported structured-output-with-tools combinations before provider dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config capability declarations -> Mode 2 admission | User-authored model capability values authorize delegated tool exposure. |
| MCP request -> provider dispatch | Runtime caller parameters can request `response_format` with a tool-enabled purpose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-13 | Elevation of privilege | `capabilities.ts` | mitigate | Unknown and false required capabilities both deny Mode 2, with distinct diagnostics. |
| T-115-14 | Tampering | `loadConfig()` admission hook | mitigate | Validate every fallback model in a tool/template-exposing purpose before startup completes. |
| T-115-15 | Elevation of privilege | `call_model` response_format guard | mitigate | Reject `response_format` plus tools before provider dispatch when structured outputs with tools are unsupported or unknown. |
| T-115-16 | Repudiation | discovery projection | mitigate | Emit structured capabilities/tags so callers can distinguish behavior support from metadata. |
</threat_model>

<verification>
Plan gate: `npm test -- tests/unit/llm-config.test.ts tests/unit/llm-tool.test.ts && npm run build`
</verification>

<success_criteria>
- CAP-03 validates all fallback models for required Mode 2 capability support.
- CAP-05 rejects unsupported `response_format` with model-visible exposure before external calls.
- CAP-01 structured capability diagnostics are reusable by Plan 03 runtime binding.
</success_criteria>

<output>
After completion, create `.planning/phases/115-purpose-config-bindings-capabilities/115-04-SUMMARY.md`.
</output>
