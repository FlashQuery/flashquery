# Sample Vault

Local fixture vault for macro POC execution tests.

The files in this directory are intentionally small and deterministic so shell
verbs can run without touching the developer's real vault.
