// Leftover scaffolding artifact from the Phase 2 agent run (could not be
// deleted via the sandbox shell). Safe to remove manually — the real
// macro-framework config is `vitest.macro-framework.config.ts` in this
// same directory. Run via `npm run test:macro-framework`.

export {};
