kind: edge
description: Two chunks state substantially the same claim — duplicates (symmetric).
source:
  chunk_id: limits-doc-a
  key_claims:
    - "The API rate limit is 100 requests per minute"
target:
  chunk_id: limits-doc-b
  key_claims:
    - "Clients may send up to 100 requests per minute to the API"
expect:
  primary_relation: duplicates
  expect_relations: [duplicates]
  min_edges: 1
