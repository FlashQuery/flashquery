kind: edge
description: Source adds detail to a claim the target states briefly — elaborates.
source:
  chunk_id: notif-detail
  key_claims:
    - "Email notifications are sent for mentions, task assignments, and a weekly digest, and can be muted per project"
target:
  chunk_id: notif-overview
  key_claims:
    - "The platform sends email notifications"
expect:
  primary_relation_in: [elaborates, extends]
  min_edges: 1
