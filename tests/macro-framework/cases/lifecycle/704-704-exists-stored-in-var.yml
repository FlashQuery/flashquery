# Saving the probe in a variable and reading it later returns the same boolean.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-l-704-exists-stored-in-var
name: "_exists() result stored in variable then re-checked"
intent: |
  _exists() result stored in variable then re-checked
description: |
  Saving the probe in a variable and reading it later returns the same boolean.

covers:
  - MTF-L-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  e1 = svc._exists()
  e2 = $e1
  exit { same: $e1 == $e2 }

input_vars: {}
vault: {}

tools:
  svc:
    archetype: "ReadOnlyTool"
    tool_name: "n"
    returns: "r"

expect:
  outcome: success
  return_result: {"same":true}
predicted_expect:
  outcome: success
  return_result: {"same":true}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
