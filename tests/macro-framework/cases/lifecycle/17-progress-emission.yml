# Generated pilot — mtf-l-17-progress-emission
#
# Generator: flashquery-macro-testgen (Phase 5 skill).
# Targeted cells: MTF-L-008
# Grounding: Macro Language Requirements REQ-052
#
# This file was emitted by the generator after running the macro through
# the macro-golden-model (v0.3.0) and embedding the snapshot.
# Do NOT hand-edit `expect:` or `golden_snapshot:` — re-run the
# generator (or its --mode=refresh) to update.
id: mtf-l-17-progress-emission
name: Lifecycle — explicit status milestones during iteration
intent: |
  Lifecycle — explicit status milestones during iteration
description: >-
  A for-loop iterates 1..8 (7 iterations); each iteration emits an explicit `status` milestone via the builtin. The
  runner captures progress events and the comparator asserts at least one milestone reached the sink (lower-bound
  assertion per REQ-052 cadence guarantees).
covers:
  - MTF-L-008
  - MTF-C-001
generator:
  skill: flashquery-macro-testgen
  version: 1
  model: flashquery-macro-testgen-cli
  generated_at: '2026-05-19T01:38:04.629Z'
  targeted_cells:
    - MTF-L-008
  grounding_refs:
    - Macro Language Requirements REQ-052
golden_version: 0.3.0
golden_run_at: '2026-05-19T01:38:04.629Z'
deps: []
require_embedding: false
require_git: false
macro: |
  acc = 0
  for n in 1..8 do
    acc = add $acc $n
    status --progress $n --total 7 "iter $n"
  done
  exit { sum: $acc }
input_vars: {}
vault: {}
tools: {}
expect:
  outcome: success
  return_result:
    sum: 28
  progress_milestones_min: 1
golden_snapshot:
  captured_trace_kinds: ["progress", "exit"]
  captured_tool_calls: []
predicted_expect:
  outcome: success
  return_result:
    sum: 28
  progress_milestones_min: 1

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
