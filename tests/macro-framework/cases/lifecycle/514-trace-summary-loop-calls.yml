# Pilot — MTF-L-005: Lifecycle — trace_mode=summary across tool calls inside a loop
id: mtf-l-514-trace-summary-loop-calls
name: Lifecycle — trace_mode=summary across tool calls inside a loop
intent: |
  Tool calls issued inside a for-loop are traced under summary mode with args/result stripped.

covers:
  - MTF-L-005
  - MTF-L-006

golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:50:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  results = []
  for i in 1..3 do
    r = lp.fetch({ n: $i })
    results = append $results $r
  done
  exit { results: $results }

input_vars: {}
vault: {}

tools:
  lp:
    archetype: ReadOnlyTool
    tool_name: fetch
    returns: {"fetched": true}

trace_mode: summary

predicted_expect:
  outcome: success
  return_result:
    results: [{"fetched": true}, {"fetched": true}]
  trace_has_no_args: true
  trace_kinds_contain:
    - tool_call
    - exit

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    results: [{"fetched": true}, {"fetched": true}]
  trace_has_no_args: true
  trace_kinds_contain:
    - tool_call
    - exit
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "lp"
      tool: "fetch"
    - server: "lp"
      tool: "fetch"
