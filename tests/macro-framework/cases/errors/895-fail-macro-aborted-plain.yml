# Pilot — MTF-E-004: Errors — fail() raises a macro_aborted envelope
id: mtf-e-895-fail-macro-aborted-plain
name: Errors — fail() raises a macro_aborted envelope
intent: |
  A bare fail() statement halts the macro immediately with a macro_aborted envelope carrying the message.

covers:
  - MTF-E-004
  - MTF-C-004

golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:40:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  fail "stop now"
  echo "unreached"

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: fail
  error:
    code: macro_aborted
    message_contains: "stop now"

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: fail
  error:
    code: macro_aborted
    message_contains: "stop now"
golden_snapshot:
  captured_trace_kinds: ["fail"]
  captured_tool_calls: []
