id: mtf-e-1409-filter-named-arg
name: filter — named argument rejected (preflight)
intent: |
  §14.3.0: the data builtins accept no named (--flag) arguments; a named arg is caught at preflight.
description: |
  filter called with a --foo named argument. Named args are visible in the AST,
  so preflight raises invalid_input + filter_named_argument.
  Graph-EDI §14.3.0 (preflight tier).

covers:
  - MTF-S-200

golden_version: "0.4.0"
golden_run_at: 2026-06-22T00:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  rows = [ { id: 1, stale: true } ]
  bad = filter $rows "stale" "==" true --foo "bar"
  exit { result: $bad }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: error
  error:
    code: invalid_input
    details:
      reason: filter_named_argument
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: error
  error:
    code: invalid_input
    details:
      reason: filter_named_argument
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
