id: mtf-e-1413-filter-ordering-non-numeric
name: filter — ordering operator on a non-numeric field (runtime)
intent: |
  §14.3.0: ordering ops require both operands numeric; a string field value raises the shared comparison_type_mismatch.
description: |
  filter with ">" against a string field value. The ordering comparison requires
  both operands numeric, so it raises the shared comparison_type_mismatch code at
  runtime (tool_call_failed) — the same code evalBinaryExpr raises for `$x < "a"`.
  Graph-EDI §14.3.0 (runtime tier, shared code).

covers:
  - MTF-S-200

golden_version: "0.4.0"
golden_run_at: 2026-06-22T00:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  rows = [ { id: 1, name: "alpha" }, { id: 2, name: "beta" } ]
  bad = filter $rows "name" ">" 5
  exit { result: $bad }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: error
  error:
    code: tool_call_failed
    details:
      reason: comparison_type_mismatch
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: error
  error:
    code: tool_call_failed
    details:
      reason: comparison_type_mismatch
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
