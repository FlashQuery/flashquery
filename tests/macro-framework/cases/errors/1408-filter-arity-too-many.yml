id: mtf-e-1408-filter-arity-too-many
name: filter — too many arguments (preflight)
intent: |
  §14.3.0: a 5-arg filter call is a statically-visible arity fault caught at preflight.
description: |
  filter called with 5 positional args. Preflight raises invalid_input +
  filter_argument_count. Graph-EDI §14.3.0 (preflight tier).

covers:
  - MTF-S-200

golden_version: "0.4.0"
golden_run_at: 2026-06-22T00:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  rows = [ { id: 1, stale: true } ]
  bad = filter $rows "stale" "==" true "extra"
  exit { result: $bad }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: error
  error:
    code: invalid_input
    details:
      reason: filter_argument_count
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: error
  error:
    code: invalid_input
    details:
      reason: filter_argument_count
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
