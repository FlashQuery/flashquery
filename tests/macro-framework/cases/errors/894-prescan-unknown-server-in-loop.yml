# Pilot — MTF-E-001: Errors — prescan rejects an unknown server referenced inside a loop body
id: mtf-e-894-prescan-unknown-server-in-loop
name: Errors — prescan rejects an unknown server referenced inside a loop body
intent: |
  An unknown-server reference inside a for-loop body is caught at prescan; the loop never runs.

covers:
  - MTF-E-001
  - MTF-E-002

golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:40:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  for i in 1..3 do
    x = absent_srv.run({ i: $i })
  done
  exit { ok: true }

input_vars: {}
vault: {}
tools:
  fq: real

predicted_expect:
  outcome: fail
  error:
    code: unknown_server
    message_contains: "absent_srv"

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: fail
  error:
    code: unknown_server
    message_contains: "absent_srv"
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
