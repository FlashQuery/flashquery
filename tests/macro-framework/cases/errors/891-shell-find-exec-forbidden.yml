# Pilot — REQ-043 + INV-06: `find -exec` is rejected at parse-time pre-scan.
id: mtf-e-891-shell-find-exec-forbidden
name: Errors — `find -exec` rejected at pre-scan (forbidden_shell_flag)
intent: |
  `find -exec` (run a command per result) is a forbidden shell flag.
description: |
  Per REQ-043 §6.6.3 + INV-06, `find -exec` MUST be rejected at parse-time
  pre-scan — it would allow arbitrary command execution. The envelope is
  `forbidden_shell_flag` identifying verb `find` and flag `-exec`.
covers: [MTF-E-007]
golden_version: "0.3.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = find "/notes" -exec "ls"
  exit { r: $r }
input_vars: {}
vault:
  "/notes/file.txt": "content"
expect:
  outcome: error
  error:
    code: forbidden_shell_flag
    details:
      verb: find
      flag: "-exec"
predicted_expect:
  outcome: error
  error:
    code: forbidden_shell_flag
    details:
      verb: find
      flag: "-exec"
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
