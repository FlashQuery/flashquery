# Pilot — MTF-E-001: Errors — prescan rejects a call to an unregistered server
id: mtf-e-893-prescan-unknown-server-bind
name: Errors — prescan rejects a call to an unregistered server
intent: |
  A macro references a server absent from the registry; prescan emits unknown_server before execution.

covers:
  - MTF-E-001
  - MTF-E-002

golden_version: "0.4.0"
golden_run_at: 2026-05-20T17:40:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  r = ghostsrv.lookup({ q: "x" })
  exit { r: $r }

input_vars: {}
vault: {}
tools:
  fq: real

predicted_expect:
  outcome: fail
  error:
    code: unknown_server
    message_contains: "ghostsrv"

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: fail
  error:
    code: unknown_server
    message_contains: "ghostsrv"
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
