id: mtf-e-1108-parse-input-var-key-must-be-literal
name: input_var_key_must_be_literal — VarRef key
intent: |
  Use a VarRef where a string-literal key is required.
description: |
  Use a VarRef where a string-literal key is required.

covers:
  - MTF-E-005

golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  k = "name"
  result = input_var $k
  exit { result: $result }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: error
  error:
    code: invalid_input
    details:
      reason: input_var_key_must_be_literal
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: error
  error:
    code: invalid_input
    details:
      reason: input_var_key_must_be_literal
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
