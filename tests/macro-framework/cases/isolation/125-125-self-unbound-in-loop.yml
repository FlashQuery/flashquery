# Reading _self.x inside a loop body when unbound still raises runtime error.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-i-125-self-unbound-in-loop
name: "_self unbound inside a loop — runtime error per iteration boundary"
description: |
  Reading _self.x inside a loop body when unbound still raises runtime error.

covers:
  - MTF-I-102

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  for i in 1..3 do
    p = $_self.path
  done

input_vars: {}
vault: {}

expect:
  outcome: fail
