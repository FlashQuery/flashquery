# Verifies _self values flow through if-conditions. Avoids boolean literals (Macro Lang §3.3) by returning a comparison-derived value via string match.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-i-110-self-in-conditional
name: "_self used inside if-condition"
intent: |
  _self used inside if-condition
description: |
  Verifies _self values flow through if-conditions. Avoids boolean literals (Macro Lang §3.3) by returning a comparison-derived value via string match.

covers:
  - MTF-I-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  kind = $_self.frontmatter.type
  out = "miss"
  if $kind == "macro" then
    out = "match"
  fi
  exit { v: $out }

input_vars: {}
vault: {}
self_binding: {"path":"/x.md","frontmatter":{"type":"macro"},"title":"t","tags":[],"fq_id":"fq:doc:x:1"}

expect:
  outcome: success
  return_result: {"v":"match"}
predicted_expect:
  outcome: success
  return_result: {"v":"match"}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T15:29:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
