# Pilot — MTF-C-006: Control flow — while-loop terminated by fail()
id: mtf-c-507-while-fail-termination
name: Control flow — while-loop terminated by fail()
intent: |
  A while-loop driven by a mutated counter is terminated by fail() once the counter reaches a threshold.

covers:
  - MTF-C-006
  - MTF-C-004

golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:40:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  n = 0
  while $n < 100 do
    n = add $n 1
    if $n == 3 then
      fail "while halt"
    fi
  done
  echo "unreached"

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: fail
  error:
    code: macro_aborted
    message_contains: "while halt"

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: fail
  error:
    code: macro_aborted
    message_contains: "while halt"
golden_snapshot:
  captured_trace_kinds: ["fail"]
  captured_tool_calls: []
