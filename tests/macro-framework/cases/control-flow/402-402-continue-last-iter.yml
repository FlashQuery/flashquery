# continue fires on i==5; sum is 1+2+3+4 = 10.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-402-continue-last-iter
name: "continue on last iteration"
intent: |
  continue on last iteration
description: |
  continue fires on i==5; sum is 1+2+3+4 = 10.

covers:
  - MTF-C-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  t = 0
  for i in 1..6 do
    if $i == 5 then
      continue
    fi
    t = add $t $i
  done
  exit { sum: $t }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"sum":10}
predicted_expect:
  outcome: success
  return_result: {"sum":10}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T15:29:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
