# After break exits the loop, execution continues with statements after `done`.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-414-break-then-after-loop
name: "break followed by post-loop statement"
intent: |
  break followed by post-loop statement
description: |
  After break exits the loop, execution continues with statements after `done`.

covers:
  - MTF-C-102

golden_version: "0.4.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  seen = 0
  for i in 1..10 do
    seen = add $seen 1
    if $seen == 3 then
      break
    fi
  done
  post = add $seen 100
  exit { p: $post }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"p":103}
predicted_expect:
  outcome: success
  return_result: {"p":103}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
