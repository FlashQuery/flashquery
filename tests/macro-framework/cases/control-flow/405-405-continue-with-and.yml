# Compound condition with && controls continue. Range 1..11 = {1..10}; continue on even <8 skips 2,4,6 — kept: 1+3+5+7+8+9+10 = 43.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-405-continue-with-and
name: "continue gated by &&"
description: |
  Compound condition with && controls continue. Range 1..11 = {1..10}; continue on even <8 skips 2,4,6 — kept: 1+3+5+7+8+9+10 = 43.

covers:
  - MTF-C-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  t = 0
  for i in 1..11 do
    rem = mod $i 2
    if $rem == 0 && $i < 8 then
      continue
    fi
    t = add $t $i
  done
  exit { sum: $t }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"sum":43}
