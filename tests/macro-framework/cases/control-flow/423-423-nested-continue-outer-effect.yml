# continue in the outer loop skips both inner work and outer accumulator update. Range 1..4 = {1,2,3}, skip i=2 → outer = 1+3 = 4. Inner loop fires for i=1 and i=3, each adds 1+2 = 3, total inner = 6.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-423-nested-continue-outer-effect
name: "Nested loops — outer continue carries through"
intent: |
  Nested loops — outer continue carries through
description: |
  continue in the outer loop skips both inner work and outer accumulator update. Range 1..4 = {1,2,3}, skip i=2 → outer = 1+3 = 4. Inner loop fires for i=1 and i=3, each adds 1+2 = 3, total inner = 6.

covers:
  - MTF-C-103

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  outer_sum = 0
  inner_sum = 0
  for i in 1..4 do
    if $i == 2 then
      continue
    fi
    for j in 1..3 do
      inner_sum = add $inner_sum $j
    done
    outer_sum = add $outer_sum $i
  done
  exit { o: $outer_sum, i: $inner_sum }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"o":4,"i":6}
predicted_expect:
  outcome: success
  return_result: {"o":4,"i":6}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T15:29:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
