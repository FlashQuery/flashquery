# Two separate `if continue` blocks before the body — both filters apply.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-407-continue-multiple-conditions
name: "Multiple continue conditions"
intent: |
  Multiple continue conditions
description: |
  Two separate `if continue` blocks before the body — both filters apply.

covers:
  - MTF-C-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  acc = []
  for x in [1, 2, 3, 4, 5, 6, 7, 8] do
    if $x < 3 then
      continue
    fi
    if $x > 6 then
      continue
    fi
    acc = append $acc $x
  done
  exit { n: count $acc }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"n":4}
predicted_expect:
  outcome: success
  return_result: {"n":4}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
