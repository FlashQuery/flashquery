# continue fires on i==1; remaining iterations sum 2+3+4+5 = 14.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-401-continue-first-iter
name: "continue on first iteration only"
intent: |
  continue on first iteration only
description: |
  continue fires on i==1; remaining iterations sum 2+3+4+5 = 14.

covers:
  - MTF-C-101

golden_version: "0.4.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  t = 0
  for i in 1..6 do
    if $i == 1 then
      continue
    fi
    t = add $t $i
  done
  exit { sum: $t }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"sum":14}
predicted_expect:
  outcome: success
  return_result: {"sum":14}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
