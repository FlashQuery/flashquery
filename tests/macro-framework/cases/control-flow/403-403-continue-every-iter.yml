# Unconditional continue at the top of every iteration; accumulator never advances.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-403-continue-every-iter
name: "continue on every iteration — accumulator unchanged"
description: |
  Unconditional continue at the top of every iteration; accumulator never advances.

covers:
  - MTF-C-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  t = 0
  for i in 1..6 do
    continue
    t = add $t $i
  done
  exit { sum: $t }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"sum":0}
