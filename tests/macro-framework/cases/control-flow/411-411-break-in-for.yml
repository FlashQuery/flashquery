# break exits a for-loop at i==4; remaining iterations skipped.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-411-break-in-for
name: "break inside for-loop"
intent: |
  break inside for-loop
description: |
  break exits a for-loop at i==4; remaining iterations skipped.

covers:
  - MTF-C-102

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  t = 0
  for i in 1..11 do
    if $i == 4 then
      break
    fi
    t = add $t $i
  done
  exit { sum: $t }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"sum":6}
predicted_expect:
  outcome: success
  return_result: {"sum":6}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
