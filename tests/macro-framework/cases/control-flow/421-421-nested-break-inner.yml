# Inner break exits inner loop; outer continues to completion.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-421-nested-break-inner
name: "nested loops — break exits only inner"
intent: |
  nested loops — break exits only inner
description: |
  Inner break exits inner loop; outer continues to completion.

covers:
  - MTF-C-103

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  outer = 0
  inner_total = 0
  for i in 1..4 do
    outer = add $outer 1
    for j in 1..10 do
      inner_total = add $inner_total 1
      if $j == 2 then
        break
      fi
    done
  done
  exit { outer: $outer, inner: $inner_total }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"outer":3,"inner":6}
predicted_expect:
  outcome: success
  return_result: {"outer":3,"inner":6}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
