# Skip exactly one value (the pivot) and accumulate the rest.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-406-continue-skip-pivot
name: "continue skips the pivot value in a sort-ish pass"
intent: |
  continue skips the pivot value in a sort-ish pass
description: |
  Skip exactly one value (the pivot) and accumulate the rest.

covers:
  - MTF-C-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  t = 0
  pivot = 4
  for i in 1..8 do
    if $i == $pivot then
      continue
    fi
    t = add $t $i
  done
  exit { sum: $t }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"sum":24}
predicted_expect:
  outcome: success
  return_result: {"sum":24}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T15:29:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
