# Side effect (append) runs before break fires.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-c-413-break-after-side-effect
name: "break after executing side effect"
intent: |
  break after executing side effect
description: |
  Side effect (append) runs before break fires.

covers:
  - MTF-C-102

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  acc = []
  for x in [10, 20, 30, 40] do
    acc = append $acc $x
    if $x == 20 then
      break
    fi
  done
  exit { n: count $acc }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result: {"n":2}
predicted_expect:
  outcome: success
  return_result: {"n":2}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T15:29:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
