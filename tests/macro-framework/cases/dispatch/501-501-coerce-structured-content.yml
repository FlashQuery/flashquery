# REQ-106 path 2: when structuredContent is present, the macro engine binds it as the value (not the text content).
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-501-coerce-structured-content
name: "StructuredContentTool path: structuredContent binds directly"
intent: |
  StructuredContentTool path: structuredContent binds directly
description: |
  REQ-106 path 2: when structuredContent is present, the macro engine binds it as the value (not the text content).

covers:
  - MTF-D-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  v = thing_srv.get({})
  exit { val: $v }

input_vars: {}
vault: {}

tools:
  thing_srv:
    archetype: "StructuredContentTool"
    tool_name: "get"
    value: {"kind":"structured","n":42}

expect:
  outcome: success
  return_result: {"val":{"kind":"structured","n":42}}
predicted_expect:
  outcome: success
  return_result: {"val":{"kind":"structured","n":42}}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "thing_srv"
      tool: "get"
