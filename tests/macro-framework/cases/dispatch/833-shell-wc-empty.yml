# Pilot — REQ-038 wc -l on empty file: returns 0.
id: mtf-d-833-shell-wc-empty
name: Dispatch — `wc -l` on empty file returns 0
intent: |
  An empty file has zero lines.
description: |
  Per REQ-038, `wc -l` on a zero-byte file returns 0. Verifies the
  empty-input edge case is well-defined and doesn't surface an error.
covers: [MTF-D-010]
golden_version: "0.3.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = wc "/notes/empty.txt" -l
  exit { r: $r }
input_vars: {}
vault:
  "/notes/empty.txt": ""
expect:
  outcome: success
  return_result:
    r: 0
predicted_expect:
  outcome: success
  return_result:
    r: 0
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
