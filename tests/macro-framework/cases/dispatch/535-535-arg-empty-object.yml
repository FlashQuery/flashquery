# Calling with `{}` is valid and reaches the dispatcher.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-535-arg-empty-object
name: "Empty object argument passes through"
intent: |
  Empty object argument passes through
description: |
  Calling with `{}` is valid and reaches the dispatcher.

covers:
  - MTF-D-103

golden_version: "0.4.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  v = srv.run({})
  exit { v: $v }

input_vars: {}
vault: {}

tools:
  srv:
    archetype: "WriteTool"
    tool_name: "run"

expect:
  outcome: success
predicted_expect:
  outcome: success

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "srv"
      tool: "run"
