# Pilot — REQ-112f: `tail -n N` returns the last N lines (explicit count flag).
id: mtf-d-875-shell-tail-explicit-count
name: Dispatch — `tail -n N` returns the last N lines
intent: |
  tail -n 3 returns exactly the last 3 lines of the file as a list.
description: |
  Per REQ-112f (clarifier of REQ-038 §6.5.1), the head/tail count flag is
  the boolean short flag `-n` followed by a positional integer literal N.
  `tail -n 3 <file>` returns the last 3 lines as a LIST of strings. This
  pilot exercises the explicit-count surface that REQ-112f pins down — the
  short flag `-n` lexes as a boolean and N is a separate positional token.
covers: [MTF-D-010]
golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:15:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = tail -n 3 "/notes/twenty.txt"
  exit { r: $r }
input_vars: {}
vault:
  "/notes/twenty.txt": "l01\nl02\nl03\nl04\nl05\nl06\nl07\nl08\nl09\nl10\nl11\nl12\nl13\nl14\nl15\nl16\nl17\nl18\nl19\nl20\n"
expect:
  outcome: success
  return_result:
    r: ["l18", "l19", "l20"]
predicted_expect:
  outcome: success
  return_result:
    r: ["l18", "l19", "l20"]
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
