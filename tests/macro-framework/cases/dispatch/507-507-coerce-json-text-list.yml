# JSON text that parses to a list binds as a list (no double-wrap).
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-507-coerce-json-text-list
name: "JSONTextTool with list value"
intent: |
  JSONTextTool with list value
description: |
  JSON text that parses to a list binds as a list (no double-wrap).

covers:
  - MTF-D-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  v = list_srv.get({})
  exit { n: count $v }

input_vars: {}
vault: {}

tools:
  list_srv:
    archetype: "JSONTextTool"
    tool_name: "get"
    value: ["a","b","c"]

expect:
  outcome: success
  return_result: {"n":3}
predicted_expect:
  outcome: success
  return_result: {"n":3}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "list_srv"
      tool: "get"
