# Pilot — REQ-038 pipeline: ls | grep | wc -l (count filtered entries).
id: mtf-d-884-shell-pipe-ls-grep-wc
name: Dispatch — `ls | grep | wc -l` counts filtered directory entries
intent: |
  Three-stage pipeline: list a directory, filter entries, count the result.
description: |
  Per REQ-038 §6.5.1 + REQ-051, `ls /notes | grep "txt" | wc -l` lists
  the directory, keeps entries containing "txt", and counts them. With
  three .txt files and one .md, the count is 3.
covers: [MTF-D-010]
golden_version: "0.4.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = ls "/notes" | grep "txt" | wc -l
  exit { r: $r }
input_vars: {}
vault:
  "/notes/alpha.txt": "a"
  "/notes/beta.txt": "b"
  "/notes/gamma.txt": "c"
  "/notes/readme.md": "doc"
expect:
  outcome: success
  return_result:
    r: 3
predicted_expect:
  outcome: success
  return_result:
    r: 3
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
