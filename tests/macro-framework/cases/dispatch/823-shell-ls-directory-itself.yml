# Pilot — REQ-038 ls -d: return the directory itself, not its contents.
id: mtf-d-823-shell-ls-directory-itself
name: Dispatch — `ls -d` returns directory itself (not its contents)
intent: |
  The -d flag causes ls to return the directory's own name/path rather than its children.
description: |
  Per REQ-038 §6.5.1, `ls -d <dir>` returns the directory itself as a
  one-element list. Useful for testing whether a path exists and is a
  directory without enumerating its contents.
covers: [MTF-D-010]
golden_version: "0.4.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = ls "/notes/sub" -d
  exit { r: $r }
input_vars: {}
vault:
  "/notes/sub/a.txt": "a"
  "/notes/sub/b.txt": "b"
expect:
  outcome: success
  return_result:
    r:
      - "/notes/sub"
predicted_expect:
  outcome: success
  return_result:
    r:
      - "/notes/sub"
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
