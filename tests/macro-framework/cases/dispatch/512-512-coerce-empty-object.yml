# `structuredContent: {}` binds as `{}` (truthy or falsy depends on lang semantics).
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-512-coerce-empty-object
name: "StructuredContent returning empty object"
intent: |
  StructuredContent returning empty object
description: |
  `structuredContent: {}` binds as `{}` (truthy or falsy depends on lang semantics).

covers:
  - MTF-D-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  v = sc.get({})
  exit { v: $v }

input_vars: {}
vault: {}

tools:
  sc:
    archetype: "StructuredContentTool"
    tool_name: "get"
    value: {}

expect:
  outcome: success
  return_result: {"v":{}}
predicted_expect:
  outcome: success
  return_result: {"v":{}}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "sc"
      tool: "get"
