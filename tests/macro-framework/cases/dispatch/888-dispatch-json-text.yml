# Pilot — MTF-D dispatch: Dispatch — CallToolResult coercion path 3 (JSON-text)
id: mtf-d-888-dispatch-json-text
name: Dispatch — CallToolResult coercion path 3 (JSON-text)
intent: |
  A brokered tool returns JSON-parseable text content with no structuredContent; the engine parses it (REQ-106 path 3).

covers:
  - MTF-D-006
  - MTF-D-001
  - MTF-D-003

golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:40:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  v = json_srv.emit({})
  exit { v: $v }

input_vars: {}
vault: {}

tools:
  json_srv:
    archetype: JSONTextTool
    tool_name: emit
    value: {"source": "json_text", "n": 6}

predicted_expect:
  outcome: success
  return_result:
    v: {"source": "json_text", "n": 6}
  side_effects:
    tool_call_count: 1

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    v: {"source": "json_text", "n": 6}
  side_effects:
    tool_call_count: 1
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "json_srv"
      tool: "emit"
