# Empty-string body coerces to "" (truthy/falsy semantics preserved — "" is falsy). Outer-scope var pre-declared.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-603-coerce-empty-string
name: "ReadOnlyTool empty-string return"
description: |
  Empty-string body coerces to "" (truthy/falsy semantics preserved — "" is falsy). Outer-scope var pre-declared.

covers:
  - MTF-D-101

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  v = e.get({})
  out = "empty"
  if $v then
    out = "non-empty"
  fi
  exit { out: $out }

input_vars: {}
vault: {}

tools:
  e:
    archetype: "ReadOnlyTool"
    tool_name: "get"
    returns: ""

expect:
  outcome: success
  return_result: {"out":"empty"}
