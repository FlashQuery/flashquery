# Pilot — REQ-038 wc -c: byte/char count.
id: mtf-d-832-shell-wc-chars
name: Dispatch — `wc -c` counts bytes
intent: |
  -c returns the file's byte count (including newlines).
description: |
  Per REQ-038 §6.5.1, `wc -c` returns the byte length of the file as a
  number. Includes newline bytes in the count. For the 4-line content
  "alpha\nbeta\ngamma\ndelta\n", the byte count is 23 (5+1+4+1+5+1+5+1).
covers: [MTF-D-010]
golden_version: "0.4.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = wc "/notes/lines.txt" -c
  exit { r: $r }
input_vars: {}
vault:
  "/notes/lines.txt": "alpha\nbeta\ngamma\ndelta\n"
expect:
  outcome: success
  return_result:
    r: 23
predicted_expect:
  outcome: success
  return_result:
    r: 23
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
