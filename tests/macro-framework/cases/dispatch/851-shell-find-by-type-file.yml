# Pilot — REQ-038 find --type "f": filter to regular files.
id: mtf-d-851-shell-find-by-type-file
name: Dispatch — `find --type "f"` returns only regular files
intent: |
  --type "f" restricts results to regular files (excludes directories).
description: |
  Per REQ-038 §6.5.1, `find <root> --type "f"` filters out directories
  from the recursive walk, returning only file paths. Useful for content
  enumeration without dir-listing noise.
covers: [MTF-D-010]
golden_version: "0.3.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = find "/notes" --type "f"
  exit { r: $r }
input_vars: {}
vault:
  "/notes/a.txt": "a"
  "/notes/sub/b.txt": "b"
  "/notes/sub/deeper/c.txt": "c"
expect:
  outcome: success
  return_result:
    r:
      - "/notes/a.txt"
      - "/notes/sub/b.txt"
      - "/notes/sub/deeper/c.txt"
predicted_expect:
  outcome: success
  return_result:
    r:
      - "/notes/a.txt"
      - "/notes/sub/b.txt"
      - "/notes/sub/deeper/c.txt"
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
