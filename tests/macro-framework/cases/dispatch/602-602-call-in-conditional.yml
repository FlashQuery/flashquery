# Tool dispatch nested in an if-then-else branch. Variable declared at outer scope so it survives after the `fi` (REQ-021 walk-up scope).
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-602-call-in-conditional
name: "Brokered call only in then-branch (outer-scope var pre-declared)"
intent: |
  Brokered call only in then-branch (outer-scope var pre-declared)
description: |
  Tool dispatch nested in an if-then-else branch. Variable declared at outer scope so it survives after the `fi` (REQ-021 walk-up scope).

covers:
  - MTF-D-101

golden_version: "0.4.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  flag = 1
  r = "fallback"
  if $flag == 1 then
    r = jt.get({})
  fi
  exit { r: $r }

input_vars: {}
vault: {}

tools:
  jt:
    archetype: "JSONTextTool"
    tool_name: "get"
    value: {"k":"v"}

expect:
  outcome: success
  return_result: {"r":{"k":"v"}}
predicted_expect:
  outcome: success
  return_result: {"r":{"k":"v"}}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "jt"
      tool: "get"
