# Pilot — REQ-038 wc -l: line count.
id: mtf-d-830-shell-wc-lines
name: Dispatch — `wc -l` counts lines
intent: |
  The -l flag returns the number of lines in the file.
description: |
  Per REQ-038 §6.5.1, `wc -l <file>` returns the line count as a number.
  Lines are newline-terminated; a file with N newlines counts as N lines.
covers: [MTF-D-010]
golden_version: "0.3.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = wc "/notes/lines.txt" -l
  exit { r: $r }
input_vars: {}
vault:
  "/notes/lines.txt": "alpha\nbeta\ngamma\ndelta\n"
expect:
  outcome: success
  return_result:
    r: 4
predicted_expect:
  outcome: success
  return_result:
    r: 4
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
