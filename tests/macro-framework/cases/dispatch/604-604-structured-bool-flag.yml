# structuredContent number 1/0 used as truthy/falsy in if. Outer-scope var pre-declared.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-604-structured-bool-flag
name: "StructuredContentTool returning boolean-like flag (1/0)"
intent: |
  StructuredContentTool returning boolean-like flag (1/0)
description: |
  structuredContent number 1/0 used as truthy/falsy in if. Outer-scope var pre-declared.

covers:
  - MTF-D-101

golden_version: "0.4.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  r = flag_srv.get({})
  out = "no"
  if $r == 1 then
    out = "yes"
  fi
  exit { out: $out }

input_vars: {}
vault: {}

tools:
  flag_srv:
    archetype: "StructuredContentTool"
    tool_name: "get"
    value: 1

expect:
  outcome: success
  return_result: {"out":"yes"}
predicted_expect:
  outcome: success
  return_result: {"out":"yes"}

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["tool_call", "exit"]
  captured_tool_calls:
    - server: "flag_srv"
      tool: "get"
