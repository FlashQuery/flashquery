# Pilot — REQ-038 pipeline: cat | grep (2-stage pipeline).
id: mtf-d-880-shell-pipe-cat-grep
name: Dispatch — `cat | grep` 2-stage pipeline
intent: |
  cat's output streams to grep's stdin; grep filters lines.
description: |
  Per REQ-038 §6.5.1 + REQ-051, pipeline stages connect LHS output to
  RHS stdin. `cat /notes/lines.txt | grep "alpha"` reads the file then
  filters to matching lines. Same as `grep "alpha" "/notes/lines.txt"`
  but exercises the pipe semantic.
covers: [MTF-D-010]
golden_version: "0.3.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = cat "/notes/lines.txt" | grep "alpha"
  exit { r: $r }
input_vars: {}
vault:
  "/notes/lines.txt": "alpha\nbeta\ngamma\ndelta\n"
expect:
  outcome: success
  return_result:
    r: ["alpha"]
predicted_expect:
  outcome: success
  return_result:
    r: ["alpha"]
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
