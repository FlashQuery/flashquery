# Interpolated variables resolve before dispatch; the resulting string is passed through.
# Auto-generated 2026-05-19 by _tier2-batch-generator.ts. Hand edits OK.

id: mtf-d-536-arg-with-interpolation
name: "String-interpolated argument value"
description: |
  Interpolated variables resolve before dispatch; the resulting string is passed through.

covers:
  - MTF-D-103

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  name = "matt"
  v = srv.run({ greeting: "hi ${name}" })
  exit { v: $v }

input_vars: {}
vault: {}

tools:
  srv:
    archetype: "WriteTool"
    tool_name: "run"

expect:
  outcome: success
