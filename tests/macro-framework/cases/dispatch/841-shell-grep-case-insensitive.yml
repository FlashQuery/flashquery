# Pilot — REQ-038 grep -i: case-insensitive match.
id: mtf-d-841-shell-grep-case-insensitive
name: Dispatch — `grep -i` matches case-insensitively
intent: |
  -i ignores case when matching the pattern against each line.
description: |
  Per REQ-038 §6.5.1, `grep -i <pattern> <file>` performs a
  case-insensitive substring match. A file with three case variants
  of "apple" all match the pattern "APPLE" under -i.
covers: [MTF-D-010]
golden_version: "0.4.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = grep -i "APPLE" "/notes/case.txt"
  exit { r: $r }
input_vars: {}
vault:
  "/notes/case.txt": "Apple\napple\nAPPLE"
expect:
  outcome: success
  return_result:
    r: ["Apple", "apple", "APPLE"]
predicted_expect:
  outcome: success
  return_result:
    r: ["Apple", "apple", "APPLE"]
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
