# Pilot — REQ-038 find --name: glob-pattern path matching.
id: mtf-d-850-shell-find-by-name
name: Dispatch — `find --name "*.txt"` returns matching paths (recursive)
intent: |
  find --name matches glob patterns recursively, returning vault-relative paths.
description: |
  Per REQ-038 §6.5.1, `find <root> --name "<glob>"` recursively walks
  the directory tree and returns paths matching the glob. Paths are
  alphabetized and use vault-relative form (leading slash).
covers: [MTF-D-010]
golden_version: "0.4.0"
golden_run_at: 2026-05-20T16:30:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  r = find "/notes" --name "*.txt"
  exit { r: $r }
input_vars: {}
vault:
  "/notes/top.txt": "top"
  "/notes/skip.md": "markdown, not txt"
  "/notes/sub/inside.txt": "inside"
  "/notes/sub/deeper/leaf.txt": "leaf"
expect:
  outcome: success
  return_result:
    r:
      - "/notes/sub/deeper/leaf.txt"
      - "/notes/sub/inside.txt"
      - "/notes/top.txt"
predicted_expect:
  outcome: success
  return_result:
    r:
      - "/notes/sub/deeper/leaf.txt"
      - "/notes/sub/inside.txt"
      - "/notes/top.txt"
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
