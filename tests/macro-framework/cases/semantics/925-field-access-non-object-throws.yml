# Pilot — MTF-S-102 / REQ-112d ac7: field access on non-object value
# (number) MUST still throw per REQ-023 ac3.

id: mtf-s-925-field-access-non-object-throws
name: Semantics — field access on non-object value still throws (REQ-112d ac7)
intent: |
  Semantics — field access on non-object value still throws (REQ-112d ac7)
description: |
  Field access on a primitive (number) MUST raise per REQ-023 ac3. REQ-112d
  only relaxes the missing-key case on present objects.

covers:
  - MTF-S-102

golden_version: "0.4.0"
golden_run_at: 2026-05-19T15:05:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  obj = 42
  v = $obj.anything
  exit { v: $v }

input_vars: {}
vault: {}

expect:
  outcome: fail
predicted_expect:
  outcome: fail

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: []
  captured_tool_calls: []
