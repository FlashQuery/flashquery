id: mtf-s-988-input-var-optional-null-default-fires
name: input_var — optional with null default, default fires
intent: |
  Optional input_var with `--default null`. Pilot supplies nothing;
  $reviewer binds null. Tests null as a first-class default literal.
description: |
  REQ-007 ac2 (null is an allowed default literal) + REQ-008 ac4.

covers:
  - MTF-L-002
  - MTF-C-009

golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  reviewer = input_var "reviewer" --default null
  exit { reviewer: $reviewer }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    reviewer: null
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    reviewer: null
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
