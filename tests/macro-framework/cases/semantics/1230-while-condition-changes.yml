id: mtf-s-1230-while-condition-changes
name: |-
  while condition re-evaluated each iter
intent: |
  condition flips false during loop
covers: [MTF-C-009]
golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:55:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  flag = true
  run_count = 0
  while $flag do
    run_count = add $run_count 1
    if $run_count == 2 then
      flag = false
    fi
  done
  exit { runs: $run_count }
input_vars: {}
vault: {}
tools: {}
predicted_expect:
  outcome: success
  return_result: { runs: 2 }
  side_effects:
    tool_call_count: 0
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result: { runs: 2 }
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
