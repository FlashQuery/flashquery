id: mtf-s-1400-filter-basic-eq
name: filter — basic == on a boolean field
intent: |
  §14.3.1: filter returns the subset of a list of objects whose $field == $value.
description: |
  filter with the == operator over a list of object literals. Positive path,
  no tool surface — exercises the core subset behavior. Graph-EDI §14.3.1.

covers:
  - MTF-S-200

golden_version: "0.4.0"
golden_run_at: 2026-06-22T00:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  rows = [ { id: 1, stale: true }, { id: 2, stale: false }, { id: 3, stale: true } ]
  result = filter $rows "stale" "==" true
  exit { result: $result }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result: { result: [{ id: 1, stale: true }, { id: 3, stale: true }] }
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result: { result: [{ id: 1, stale: true }, { id: 3, stale: true }] }
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
