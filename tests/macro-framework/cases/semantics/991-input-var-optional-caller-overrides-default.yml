id: mtf-s-991-input-var-optional-caller-overrides-default
name: input_var — optional default overridden by caller-supplied value
intent: |
  Optional input_var with a default. Pilot supplies a different value;
  caller-supplied wins over default.
description: |
  REQ-007 ac6 + REQ-008 ac4 — caller-supplied value (even when present
  for an optional input) overrides the default.

covers:
  - MTF-L-002
  - MTF-C-009

golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  limit = input_var "limit" --default 10
  exit { limit: $limit }

input_vars:
  limit: 99
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    limit: 99
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-19T22:18:00Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. AI prediction, golden capture,
    and production runtime all agreed on the envelope.

expect:
  outcome: success
  return_result:
    limit: 99
  side_effects:
    tool_call_count: 0
