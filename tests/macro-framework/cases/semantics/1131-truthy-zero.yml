id: mtf-s-1131-truthy-zero
name: zero is falsy
intent: |
  REQ-022: 0 is falsy.

covers:
  - MTF-C-009

golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:55:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  x = 0
  if $x then
    result = "truthy"
  else
    result = "falsy"
  fi
  exit { result: $result }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result: { result: "falsy" }
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: null
  captured_at: null
  golden_version: "0.3.0"
  notes: |
    Awaiting capture.

expect:
  outcome: success
  return_result: { result: "falsy" }
  side_effects:
    tool_call_count: 0
