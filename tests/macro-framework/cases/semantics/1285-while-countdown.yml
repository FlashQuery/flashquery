id: mtf-s-1285-while-countdown
name: Control flow — while-loop with a decrementing counter
intent: |
  A while-loop runs while a counter is truthy, decrementing it to zero in the body.

covers:
  - MTF-C-005
  - MTF-S-004
  - MTF-S-003
  - MTF-G-005

golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  # while loop with a decrementing counter
  remaining = 5
  steps = 0
  while $remaining do
    remaining = sub $remaining 1
    steps = add $steps 1
  done
  exit { remaining: $remaining, steps: $steps }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    remaining: 0
    steps: 5
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T18:25:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    remaining: 0
    steps: 5
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
