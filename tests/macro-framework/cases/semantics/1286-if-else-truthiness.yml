id: mtf-s-1286-if-else-truthiness
name: Control flow — if/then/else/fi picks a branch by truthiness
intent: |
  Two if/else statements select a branch by the truthiness of a number and an empty list.

covers:
  - MTF-C-008
  - MTF-S-008
  - MTF-G-003
  - MTF-G-005

golden_version: "0.3.0"
golden_run_at: 2026-05-20T17:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  # if/else picks a branch by truthiness
  score = 7
  if $score then
    grade = "nonzero"
  else
    grade = "zero"
  fi
  empty = []
  if $empty then
    empty_check = "truthy"
  else
    empty_check = "falsy"
  fi
  exit { grade: $grade, empty_check: $empty_check }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    grade: "nonzero"
    empty_check: "falsy"
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    grade: "nonzero"
    empty_check: "falsy"
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
