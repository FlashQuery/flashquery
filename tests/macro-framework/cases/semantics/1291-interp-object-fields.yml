id: mtf-s-1291-interp-object-fields
name: Grammar — interpolating object fields into strings
intent: |
  Object field access is interpolated into strings via $obj.field and ${obj.field}.

covers:
  - MTF-G-001
  - MTF-G-004
  - MTF-G-005
  - MTF-G-006

golden_version: "0.4.0"
golden_run_at: 2026-05-20T17:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  # build a profile and render interpolated strings
  profile = { city: "London", year: 1936 }
  city_line = "city: $profile.city"
  year_line = "year: ${profile.year}"
  combined = "$profile.city in $profile.year"
  exit { city_line: $city_line, year_line: $year_line, combined: $combined }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    city_line: "city: London"
    year_line: "year: 1936"
    combined: "London in 1936"
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    city_line: "city: London"
    year_line: "year: 1936"
    combined: "London in 1936"
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
