id: mtf-s-1406-filter-dynamic-op-valid
name: filter — operator supplied via variable (valid)
intent: |
  §14.3.0 validate-if-literal/defer-if-dynamic: a $var operator is resolved at runtime; a valid one succeeds.
description: |
  filter where $op is a variable bound to "==". The literal-operator preflight
  check is skipped (operand is a VarRef); the runtime path validates and runs.
  Graph-EDI §14.3.0.

covers:
  - MTF-S-200

golden_version: "0.4.0"
golden_run_at: 2026-06-22T00:00:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  rows = [ { id: 1, stale: true }, { id: 2, stale: false } ]
  op = "=="
  result = filter $rows "stale" $op true
  exit { result: $result }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result: { result: [{ id: 1, stale: true }] }
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result: { result: [{ id: 1, stale: true }] }
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
