id: mtf-s-1270-truthy-negation-null
name: negating a falsy null yields truthy
intent: |
  REQ-021 ac3 + REQ-040 ac4: ! applies standard truthiness — negating falsy null is truthy, so the then-branch runs.

covers:
  - MTF-C-009

golden_version: "0.4.0"
golden_run_at: 2026-05-20T17:25:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  x = null
  if !$x then
    result = "then"
  else
    result = "else"
  fi
  exit { result: $result }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result: { result: "then" }
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result: { result: "then" }
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
