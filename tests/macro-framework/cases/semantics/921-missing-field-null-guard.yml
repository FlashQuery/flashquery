# Pilot — MTF-S-102 / Broker REQ-112d ac3: null guard pattern works.

id: mtf-s-921-missing-field-null-guard
name: Semantics — `if $obj.maybe == null` guard pattern (REQ-112d ac3)
intent: |
  Semantics — `if $obj.maybe == null` guard pattern (REQ-112d ac3)
description: |
  The canonical missing-field guard: reads a missing field, compares to
  null, branches accordingly.

covers:
  - MTF-S-102

golden_version: "0.3.0"
golden_run_at: 2026-05-19T14:35:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  obj = { present: "yes" }
  v = $obj.absent
  state = "found"
  if $v == null then
    state = "absent"
  fi
  exit { s: $state }

input_vars: {}
vault: {}

expect:
  outcome: success
  return_result:
    s: "absent"
predicted_expect:
  outcome: success
  return_result:
    s: "absent"

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-20T15:29:48Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
