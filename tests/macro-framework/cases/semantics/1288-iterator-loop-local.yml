id: mtf-s-1288-iterator-loop-local
name: Semantics — for-loop iterator is loop-local and does not leak
intent: |
  A pre-existing variable name reused as a for-loop iterator keeps its outer value after the loop (REQ-020).

covers:
  - MTF-S-002
  - MTF-S-003
  - MTF-G-003
  - MTF-G-005

golden_version: "0.4.0"
golden_run_at: 2026-05-20T17:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  # the for-loop iterator is loop-local and does not leak
  i = "untouched"
  seen = 0
  for i in [100, 200, 300] do
    seen = add $seen 1
  done
  exit { i: $i, seen: $seen }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    i: "untouched"
    seen: 3
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-06-22T18:53:49Z"
  golden_version: "0.4.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    i: "untouched"
    seen: 3
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
