id: mtf-s-990-input-var-optional-object-default-fires
name: input_var — optional with object literal default, default fires
intent: |
  Optional input_var with `--default { mode: "fast" }`. Pilot supplies
  nothing; $cfg binds the object literal default.
description: |
  REQ-007 ac2 (object literal as default) + REQ-008 ac4.

covers:
  - MTF-L-002
  - MTF-C-009

golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  cfg = input_var "cfg" --default { mode: "fast", retries: 3 }
  exit { cfg: $cfg }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    cfg:
      mode: "fast"
      retries: 3
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result:
    cfg:
      mode: "fast"
      retries: 3
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
