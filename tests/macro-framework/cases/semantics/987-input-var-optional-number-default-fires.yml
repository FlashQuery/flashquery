id: mtf-s-987-input-var-optional-number-default-fires
name: input_var — optional with number default, default fires
intent: |
  Optional input_var with an integer default. Pilot supplies nothing;
  default fires.
description: |
  REQ-007 ac6 + REQ-008 ac4 (numeric default).

covers:
  - MTF-L-002
  - MTF-C-009

golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:30:00Z

deps: []
require_embedding: false
require_git: false

macro: |
  limit = input_var "limit" --default 10
  exit { limit: $limit }

input_vars: {}
vault: {}
tools: {}

predicted_expect:
  outcome: success
  return_result:
    limit: 10
  side_effects:
    tool_call_count: 0

reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-19T22:18:00Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. AI prediction, golden capture,
    and production runtime all agreed on the envelope.

expect:
  outcome: success
  return_result:
    limit: 10
  side_effects:
    tool_call_count: 0
