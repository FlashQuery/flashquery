id: mtf-s-1197-req-112d-missing-then-default
name: |-
  REQ-112d ac3 guard pattern
intent: |
  if $obj.missing == null then default
covers: [MTF-C-009]
golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:55:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  obj = { a: 1 }
  default_val = "fallback"
  if $obj.missing == null then
    default_val = "applied"
  fi
  exit { default_val: $default_val }
input_vars: {}
vault: {}
tools: {}
predicted_expect:
  outcome: success
  return_result: { default_val: "applied" }
  side_effects:
    tool_call_count: 0
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result: { default_val: "applied" }
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
