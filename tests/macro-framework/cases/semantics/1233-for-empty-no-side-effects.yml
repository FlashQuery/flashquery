id: mtf-s-1233-for-empty-no-side-effects
name: |-
  for over empty list — body skipped
intent: |
  loop body not executed at all
covers: [MTF-C-009]
golden_version: "0.3.0"
golden_run_at: 2026-05-19T22:55:00Z
deps: []
require_embedding: false
require_git: false
macro: |
  before = "set"
  for i in [] do
    before = "changed"
  done
  exit { before: $before }
input_vars: {}
vault: {}
tools: {}
predicted_expect:
  outcome: success
  return_result: { before: "set" }
  side_effects:
    tool_call_count: 0
reconciliation:
  predicted_matched_captured: true
  captured_at: "2026-05-21T12:41:58Z"
  golden_version: "0.3.0"
  divergence_kind: clean_match
  notes: |
    Clean three-oracle reconciliation. Captured by generic capture runner.
expect:
  outcome: success
  return_result: { before: "set" }
  side_effects:
    tool_call_count: 0
golden_snapshot:
  captured_trace_kinds: ["exit"]
  captured_tool_calls: []
