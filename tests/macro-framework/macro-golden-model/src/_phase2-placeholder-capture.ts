// MOVED. The Phase 2 placeholder capture helper lives outside the golden
// package now — see `tests/macro-framework/golden-bridge/capture-placeholder.ts`.
// Per INV-MTF-04 the golden's source is read-only; this file remained as a
// leftover sandbox artifact and is safe to delete.

export {};
